---
phase: 19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions
plan: 04
type: execute
wave: 4
depends_on:
  - 19-02
  - 19-03
files_modified:
  - tests/router.autonomous-lifecycle.test.mjs
  - tests/router.test-mode-seam.test.mjs
  - tests/router.compiled-index.test.mjs
  - tests/router.lifecycle-recovery.test.mjs
  - tests/router.compiled-evolution.test.mjs
  - tests/router.context-prompt-integration.test.mjs
  - tests/router.registry-activate.test.mjs
  - tests/router.registry-watcher.test.mjs
  - tests/router.evolution-visibility.test.mjs
  - tests/router.evolve-proposal.test.mjs
  - tests/router.registry-diff.test.mjs
  - tests/router.weights-blend.test.mjs
  - tests/router.evolve-integration.test.mjs
  - tests/router.registry-schema.test.mjs
  - tests/helpers/latency-isolated.mjs
  - tests/router.evolution-canary.test.mjs
  - tests/router.installer-coexistence.test.mjs
  - release/v1.2-matrix.json
autonomous: true
requirements:
  - ORC-01
  - TOK-02
must_haves:
  truths:
    - "Every test file referencing the old schema_version:1 tuple shape or the old COMPILED_INDEX_COMPATIBILITY is updated to schema 2 with the new compatibility members, so the full suite is green after the Plan 02 schema bump (per D-04, Pitfall #1)."
    - "tests/router.autonomous-lifecycle.test.mjs and tests/router.test-mode-seam.test.mjs are extended with D-09 assertions: closure/budget/summary-index present in the published tuple; empty mapping → blocked (no canonical_record route); TOK-02 required-overflow → non-dispatchable; Flow 11 dispatch_eligible flips to PASS; bundle presence of the four new orchestrator module files (per D-09)."
    - "tests/router.lifecycle-recovery.test.mjs and tests/router.compiled-index.test.mjs no longer rely on the removed canonical_record fallback — every publish call provides at least one mapped subject (per D-06, Pitfall #4)."
    - "release/v1.2-matrix.json gains a Phase 19 secondary evidence entry labeled phase-19-live-path for ORC-01 and TOK-02, citing the extended Phase 18 E2E as the live-path evidence (per RESEARCH.md Open Question #3 recommendation)."
    - "D-09 backstop: the extended Phase 18 E2E proves Flow 11 dispatch_eligible flips to PASS via the baked budget flag — { statement: 'Flow 11 dispatch_eligible flips to PASS via the baked budget flag in the extended Phase 18 E2E', verification: backstop }."
    - "D-10: ACT-01 live prod-verifier integration test is deferred to Phase 20 (EVO-05 canary production trigger) — the natural home for production-trigger-adjacent verifier wiring. This plan does NOT implement ACT-01; it is out of scope per D-10."
  artifacts:
    - "tests/router.autonomous-lifecycle.test.mjs (extended: D-09 assertions for closure/budget/summary-index presence, ORC-01 empty-mapping, TOK-02 required-overflow, Flow 11 dispatch_eligible PASS)"
    - "tests/router.test-mode-seam.test.mjs (extended: D-09 assertions for bundle presence of the four new orchestrator module files + baked closure readable from tuple)"
    - "~13-15 fixture test files updated to schema 2 + new COMPILED_INDEX_COMPATIBILITY members (grep-verified tuple-context set)"
    - "release/v1.2-matrix.json (extended: phase-19-live-path secondary evidence entry for ORC-01 + TOK-02)"
  key_links:
    - "autonomous-lifecycle.test.mjs D-09 assertions read loadCompiledIndex({ ownedRoot }).closure / .budget / .summaryIndex — the additive return keys added in Plan 02"
    - "test-mode-seam.test.mjs D-09 assertions read <ownedRoot>/modules/orchestrator/{select,transitions,budget}.mjs and workflow-declarations.json — the four files deployed via Plan 03 moduleNames extension"
    - "v1.2-matrix.json phase-19-live-path entry cites tests/router.autonomous-lifecycle.test.mjs + tests/router.test-mode-seam.test.mjs as the secondary evidence for ORC-01 + TOK-02"
---

<objective>
Close the validation loop for Phase 19: sweep the grep-verified ~13-15 test files that hardcode the schema-1 tuple shape and bump them to schema 2 with the new COMPILED_INDEX_COMPATIBILITY members (Pitfall #1 churn management — split into Task 1a core compatibility constructors + Pitfall #4 audit, and Task 1b remaining tuple-context fixture sweep), extend the Phase 18 E2E tests with D-09 assertions proving ORC-01/TOK-02/Flow 11 on the live publish→route path, and add the Phase 19 secondary evidence entry to the v1.2 release matrix. This is the wave that turns the schema bump from a breaking change into a clean migration and produces the live-path evidence that closes the ORC-01/TOK-02 traceability gap.

Purpose: RESEARCH.md Pitfall #1 warns that the schema bump invalidates ~10 test files; without a dedicated fixture-update wave the full suite stays red and the phase cannot seal. D-09 requires live-path E2E evidence (not just the Phase 16 unit tests that already marked ORC-01/TOK-02 Complete). Q3 (RESEARCH.md) recommends adding the matrix secondary entry so the release gate recognizes the live-path evidence. The fixture sweep is split into two tasks (Task 1a + Task 1b) because the grep-verified tuple-context set exceeds 10 files — a single 15-file sweep risks context-budget degradation per the scope-sanity threshold.

Output: Green full suite (`node --test tests/*.test.mjs`), extended D-09 E2E tests, audited direct-publish tests, updated v1.2-matrix.json.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-CONTEXT.md
@.planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-RESEARCH.md
@.planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-PATTERNS.md
@.planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-ORCHESTRATOR-INPUT-DECISION.md
@.planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-VALIDATION.md
@.planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-02-SUMMARY.md
@.planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-03-SUMMARY.md
@tests/router.autonomous-lifecycle.test.mjs
@tests/router.test-mode-seam.test.mjs
@tests/router.compiled-index.test.mjs
@tests/router.lifecycle-recovery.test.mjs
@release/v1.2-matrix.json
</context>

<tasks>

<task type="auto">
  <name>Task 1a: Schema-bump core — Pitfall #4 audit targets + compatibility-shape constructors (compiled-index, lifecycle-recovery, compiled-evolution, context-prompt-integration, registry-watcher, registry-activate, helpers/latency-isolated)</name>
  <files>tests/router.compiled-index.test.mjs, tests/router.lifecycle-recovery.test.mjs, tests/router.compiled-evolution.test.mjs, tests/router.context-prompt-integration.test.mjs, tests/router.registry-watcher.test.mjs, tests/router.registry-activate.test.mjs, tests/helpers/latency-isolated.mjs</files>
  <read_first>
    - .planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-RESEARCH.md (Pitfall #1 schema-bump churn list — ~10 files via grep; Pitfall #4 canonical_record fallback reliance in lifecycle-recovery + compiled-index)
    - .planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-PATTERNS.md (fixture-update wave pattern :394-413 — bump tuple schema_version to 2, extend compatibility objects with new members, leave registry/mapping/capsule schema_version:1 alone; Pitfall #4 warning)
    - .planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-ORCHESTRATOR-INPUT-DECISION.md (LOCKED: item #6 — the two new compatibility members `orchestrator_contract_version: 'workflow-first-v1'` + `context_contract_version: <inlined literal>`; item #8 — pointer schema_version 2)
    - .planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-02-SUMMARY.md (the exact new COMPILED_INDEX_COMPATIBILITY shape + the inlined CONTEXT_CONTRACT_VERSION literal value chosen by Plan 02)
    - src/prompt/compile-index.mjs (post-Plan-02 — the authoritative new COMPILED_INDEX_COMPATIBILITY + COMPILED_INDEX_SCHEMA_VERSION)
    - The Bash command `grep -rln "schema_version: 1\|COMPILED_INDEX_SCHEMA_VERSION\|COMPILED_INDEX_COMPATIBILITY" tests/` to enumerate the actual fixture churn set
    - tests/router.compiled-index.test.mjs (Pitfall #4 audit target — fixtures at :19, :26, :49, :60, :67, :80, :85, :101, :102, :112)
    - tests/router.lifecycle-recovery.test.mjs (Pitfall #4 audit target — fixtures at :14, :17, :21, :62, :107, :316)
  </read_first>
  <action>
    First, run `grep -rln "schema_version: 1\|COMPILED_INDEX_SCHEMA_VERSION\|COMPILED_INDEX_COMPATIBILITY" tests/` to enumerate the authoritative churn set (the files_modified list above is the superset from the initial grep in planning; the actual edit may touch a subset — edit only the files the grep identifies as containing the targeted patterns).

    This task (Task 1a) handles the 7 core files: the two Pitfall #4 audit targets (compiled-index, lifecycle-recovery), the four compatibility-shape constructors (compiled-evolution, compiled-index, context-prompt-integration — verified via `grep -rln "router_contract: 'prompt-route-v1'" tests/`), plus registry-watcher, registry-activate, and the helpers/latency-isolated tuple-context helper. Task 1b handles the remaining 8 tuple-context fixture files.

    For each test file in THIS task containing `schema_version: 1` in a TUPLE context (manifest, index.json, pointer, known-good) OR constructing a `COMPILED_INDEX_COMPATIBILITY` object:

    1. **Bump tuple `schema_version: 1` → `schema_version: 2`** ONLY in tuple contexts (manifest, index.json, pointer, known-good). Do NOT bump `schema_version: 1` in registry/mapping/capsule contexts — those are unrelated schemas (PATTERNS.md warning). Distinguish by surrounding field names: tuple contexts carry `tuple_version_id`, `compiled`, `version_id: 'v1-...'`, `policy_version`, `capsule_contract_version`, `compatibility`; registry contexts carry `registry_version_id`, `records`, `canonical_identity`; capsule contexts carry `position`, `workflow`, `goal_id`; mapping contexts carry `subjects`, `policy_fingerprint`.

    2. **Extend every constructed `COMPILED_INDEX_COMPATIBILITY` object** with the two new members from Plan 02: `orchestrator_contract_version: 'workflow-first-v1'` and `context_contract_version: <inlined CONTEXT_CONTRACT_VERSION literal — copy the exact value from src/prompt/compile-index.mjs post-Plan-02>`. The compatibility object must match the post-Plan-02 `COMPILED_INDEX_COMPATIBILITY` exactly so `compatible()` passes.

    3. **Extend every tuple `compatibility` field** (inside manifest fixtures) with the same two new members.

    4. **Extend every `index.json` fixture's `capsule_contract_version`** — this field is unchanged (still `1`, matching `capsule_schema_version: 1`). Do NOT bump it. The compatibility `capsule_schema_version: 1` is also unchanged.

    5. **For tests/router.compiled-index.test.mjs and tests/router.lifecycle-recovery.test.mjs (Pitfall #4 audit):** scan every `publishCompiledIndex({...})` call for `mapping.subjects: []` or a mapping where no subject has `disposition: 'mapped'`. Such calls previously relied on the removed :63-67 fallback to produce `canonical_record` routes. Per D-06, those calls now throw at :68. For each such call, EITHER:
       - Add at least one mapped subject to the `mapping.subjects` array (preferred — matches the new contract), OR
       - Change the assertion to expect the `TypeError('compiled index requires at least one dispatch route')` throw (if the test's intent is to exercise the empty-mapping failure path).
       Do NOT leave any publish call relying on the old fallback behavior. Note: grep confirms `canonical_record` currently appears only in `src/prompt/publish-index.mjs` (the source to be deleted by Plan 02) — no test file currently references it, so the Pitfall #4 audit is about preventing future breakage from empty-mapping publish calls, not removing existing canonical_record assertions.

    6. **Pointer fixtures**: bump `schema_version: 1` → `schema_version: 2` in active.json/known-good.json pointer fixtures (the pointer schema_version bumped per Plan 02 decision #8).

    Run `node --test tests/router.compiled-index.test.mjs tests/router.lifecycle-recovery.test.mjs tests/router.compiled-evolution.test.mjs tests/router.context-prompt-integration.test.mjs tests/router.registry-watcher.test.mjs tests/router.registry-activate.test.mjs` after editing these 7 files. The full suite will not be green until Task 1b completes; only these 7 files' tests must pass at this checkpoint.

    Do NOT modify: test assertions about behavior that is unchanged (validRoutes, routeContextPrompt's existing projection, registry build, watcher reconcile, etc.). Only the schema_version numbers, the compatibility objects, the pointer schema_version, and the Pitfall #4 empty-mapping publish calls are edited.
  </action>
  <verify>
    <automated>set -o pipefail; node --test tests/router.compiled-index.test.mjs tests/router.lifecycle-recovery.test.mjs tests/router.compiled-evolution.test.mjs tests/router.context-prompt-integration.test.mjs tests/router.registry-watcher.test.mjs tests/router.registry-activate.test.mjs 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/router.compiled-index.test.mjs tests/router.lifecycle-recovery.test.mjs tests/router.compiled-evolution.test.mjs tests/router.context-prompt-integration.test.mjs tests/router.registry-watcher.test.mjs tests/router.registry-activate.test.mjs` exits 0 (these 7 files' tests green at this checkpoint)
    - `grep -rln "canonical_record" tests/` returns empty (no test references the removed reason_code)
    - `grep -c "orchestrator_contract_version" tests/router.compiled-index.test.mjs tests/router.compiled-evolution.test.mjs tests/router.context-prompt-integration.test.mjs` returns ≥1 for each (compatibility constructors carry the new member)
    - `tests/router.compiled-index.test.mjs` and `tests/router.lifecycle-recovery.test.mjs` contain no `publishCompiledIndex` call with `mapping.subjects: []` unless the surrounding test explicitly asserts the TypeError throw
    - Every tuple fixture's `compatibility` field in these 7 files contains both new members
    - Every pointer fixture (active.json/known-good.json shape) in these 7 files has `schema_version: 2`
  </acceptance_criteria>
  <done>
    The 7 core files (Pitfall #4 audit targets + compatibility-shape constructors + tuple-context helpers) are updated to schema 2 with the new compatibility members; the Pitfall #4 empty-mapping publish calls are fixed; these 7 files' tests pass at the Task 1a checkpoint. Task 1b handles the remaining tuple-context sweep.
  </done>
</task>

<task type="auto">
  <name>Task 1b: Schema-bump remaining tuple-context fixture sweep — evolution-visibility, evolve-proposal, registry-diff, weights-blend, evolve-integration, registry-schema, evolution-canary, installer-coexistence</name>
  <files>tests/router.evolution-visibility.test.mjs, tests/router.evolve-proposal.test.mjs, tests/router.registry-diff.test.mjs, tests/router.weights-blend.test.mjs, tests/router.evolve-integration.test.mjs, tests/router.registry-schema.test.mjs, tests/router.evolution-canary.test.mjs, tests/router.installer-coexistence.test.mjs</files>
  <read_first>
    - .planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-02-SUMMARY.md (the exact new COMPILED_INDEX_COMPATIBILITY shape + the inlined CONTEXT_CONTRACT_VERSION literal value chosen by Plan 02)
    - src/prompt/compile-index.mjs (post-Plan-02 — the authoritative new COMPILED_INDEX_COMPATIBILITY + COMPILED_INDEX_SCHEMA_VERSION)
    - The Bash command `grep -rln "schema_version: 1\|COMPILED_INDEX_SCHEMA_VERSION\|COMPILED_INDEX_COMPATIBILITY" tests/` to confirm these 8 files are in the churn set
    - tests/router.compiled-index.test.mjs (post-Task-1a — the reference for the updated compatibility shape to copy)
  </read_first>
  <action>
    This task (Task 1b) handles the remaining 8 tuple-context fixture files identified by the grep verification. Task 1a already established the reference pattern; this task applies it to the rest.

    For each test file in THIS task containing `schema_version: 1` in a TUPLE context (manifest, index.json, pointer, known-good) OR constructing a compatibility-shape object:

    1. **Bump tuple `schema_version: 1` → `schema_version: 2`** ONLY in tuple contexts (manifest, index.json, pointer, known-good). Do NOT bump `schema_version: 1` in registry/mapping/capsule contexts. Distinguish by surrounding field names (same rules as Task 1a).

    2. **Extend every constructed compatibility object** with the two new members from Plan 02: `orchestrator_contract_version: 'workflow-first-v1'` and `context_contract_version: <inlined CONTEXT_CONTRACT_VERSION literal — copy the exact value from src/prompt/compile-index.mjs post-Plan-02>`.

    3. **Extend every tuple `compatibility` field** (inside manifest fixtures) with the same two new members.

    4. **Pointer fixtures**: bump `schema_version: 1` → `schema_version: 2` in active.json/known-good.json pointer fixtures.

    Run `node --test tests/*.test.mjs` after this task AND Task 1a complete. Iterate until the full suite is green. If a fixture file is NOT in the grep output, do NOT edit it.

    Do NOT modify: test assertions about behavior that is unchanged. Only the schema_version numbers and compatibility objects are edited.
  </action>
  <verify>
    <automated>set -o pipefail; node --test tests/*.test.mjs 2>&1 | tail -30 && grep -rln "schema_version: 1" tests/ | grep -v "registry\|mapping\|capsule\|schema.mjs" | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/*.test.mjs` exits 0 (full suite green — the schema-bump churn across Task 1a + Task 1b is fully absorbed)
    - `grep -rln "schema_version: 1" tests/` returns ONLY files where the `schema_version: 1` is in a registry/mapping/capsule context (NOT a tuple context) — no remaining tuple-context schema_version:1
    - `grep -rln "orchestrator_contract_version" tests/` returns at least the test files that construct compatibility objects (they all carry the new member)
    - Every tuple fixture's `compatibility` field in these 8 files contains both new members
    - Every pointer fixture (active.json/known-good.json shape) in these 8 files has `schema_version: 2`
  </acceptance_criteria>
  <done>
    The remaining 8 tuple-context fixture files are updated to schema 2 with the new compatibility members. Combined with Task 1a, the full `node --test tests/*.test.mjs` suite is green; no tuple-context fixture retains `schema_version: 1`; every compatibility constructor includes the two new members. The schema bump is fully absorbed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-09 E2E — extend tests/router.autonomous-lifecycle.test.mjs + tests/router.test-mode-seam.test.mjs with closure/budget/summary-index presence, ORC-01 empty-mapping, TOK-02 required-overflow, Flow 11 dispatch_eligible PASS, and bundle presence of the four new orchestrator module files</name>
  <files>tests/router.autonomous-lifecycle.test.mjs, tests/router.test-mode-seam.test.mjs</files>
  <read_first>
    - .planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-CONTEXT.md (D-09: extend Phase 18 E2E with closure/budget/summary-index presence, ORC-01 no-fallback, TOK-02 required-overflow, Flow 11 dispatch_eligible PASS — one extended test covers all three)
    - .planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-PATTERNS.md (autonomous-lifecycle analog :284-346: test scaffolding :1-33, installRouter+seam :45-54, assertion pattern :107-117, cleanup :131-137; test-mode-seam analog :350-390: existing seam assertion :77-93, bundle-presence extension :370-379, static-invariant :103-110 MUST be preserved)
    - .planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-RESEARCH.md (Validation Architecture Phase Requirements→Test Map rows for ORC-01, TOK-02, D-04, D-07)
    - .planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-02-SUMMARY.md (the locked sibling shape — closure/budget/summaryIndex keys on loadCompiledIndex return, by_workflow maps)
    - tests/router.autonomous-lifecycle.test.mjs (current full file — focus :1-33 scaffolding, :45-54 install options, :73-121 verify helper, :107-117 assertion pattern, :131-137 cleanup)
    - tests/router.test-mode-seam.test.mjs (current full file — focus :56-101 opt-in seam test, :103-110 static-invariant test that MUST be preserved)
    - tests/helpers/test-mode-seam.mjs (stubVerificationRunners, inProcessControllerLauncher, testMode — the seam D-09 rides)
  </read_first>
  <behavior>
    - Test: after the existing installRouter + waitUntil(tupleId) sequence in autonomous-lifecycle.test.mjs, `loadCompiledIndex({ ownedRoot })` returns an object with non-null `closure`, `budget`, `summaryIndex` keys
    - Test: `compiled.closure.by_workflow` contains an entry for the published workflow_id (e.g. 'alpha' in the existing test fixture)
    - Test: `compiled.budget.by_workflow[<workflowId>].dispatch_eligible === true` for a normal publish (Flow 11 dispatch_eligible PASS — D-09 backstop)
    - Test: a publish with `mapping.subjects: []` (empty mapping) — driven via filesystem state that produces an empty mapping — results in `loadCompiledIndex` returning `blocked()` OR the publish throw path; NO route carries `reason_code: 'canonical_record'` (ORC-01 closure)
    - Test: a publish where a required source exceeds DEFAULT_CONTEXT_CONTRACT.total_max_bytes (12288) — driven via a registry record with a ~13KB descriptor — results in `budget.by_workflow[<workflowId>].dispatch_eligible === false` and `reason_code === 'required_source_budget_exceeded'` (TOK-02 required-overflow baked at publish — E2E assertion, NOT deferred to a unit test per D-09)
    - Test: in test-mode-seam.test.mjs, after installRouter, the four files `<ownedRoot>/modules/orchestrator/select.mjs`, `<ownedRoot>/modules/orchestrator/transitions.mjs`, `<ownedRoot>/modules/orchestrator/budget.mjs`, `<ownedRoot>/modules/orchestrator/workflow-declarations.json` exist and their bytes equal the source `src/orchestrator/<name>` bytes (D-07 bundle presence)
    - Test: in test-mode-seam.test.mjs, `loadCompiledIndex({ ownedRoot }).closure` is non-null (baked closure readable from tuple — D-01)
    - Test: the static-invariant test at test-mode-seam.test.mjs:103-110 still passes (the file does NOT import publishCompiledIndex directly; the new assertions do NOT add that import)
  </behavior>
  <action>
    Extend the two E2E test files per D-09. Concrete additions:

    **tests/router.autonomous-lifecycle.test.mjs** — add ONE new `test('Phase 19 D-09: orchestrator siblings baked, ORC-01 no-fallback, TOK-02 required-overflow, Flow 11 dispatch_eligible PASS', async (t) => { ... })` block that:

    1. Reuses the existing installRouter + stubVerificationRunners + inProcessControllerLauncher + waitUntil(tupleId) scaffolding at :45-54. Drive a normal publish (at least one mapped subject), then assert:
       - `const compiled = loadCompiledIndex({ ownedRoot });`
       - `assert.ok(compiled.closure, 'closure sibling present');`
       - `assert.ok(compiled.budget, 'budget sibling present');`
       - `assert.ok(compiled.summaryIndex, 'summary-index sibling present');`
       - `const workflowId = Object.keys(compiled.index.routes)[0];`
       - `assert.ok(compiled.closure.by_workflow[workflowId], 'closure entry for published workflow_id');`
       - `assert.equal(compiled.budget.by_workflow[workflowId].dispatch_eligible, true, 'Flow 11 dispatch_eligible PASS — baked budget flag');` (this is the D-09 backstop assertion — Flow 11 flips to PASS via the baked budget flag)
    2. Drive a second publish with `mapping.subjects` empty (or filesystem state that produces an empty mapping — e.g. remove all skills/dispatchable artifacts so the watcher reconciles to an empty mapping). Assert:
       - `assert.throws(() => loadCompiledIndex({ ownedRoot }) ... )` OR the publish throws `TypeError('compiled index requires at least one dispatch route')` OR `loadCompiledIndex` returns `blocked()` with no `canonical_record` route present in any prior tuple.
       - Concretely: assert that the most-recent publish did NOT produce a route with `reason_code: 'canonical_record'` — read the active tuple's `index.routes` and assert no route has that reason_code (or that the publish threw).
    3. Drive a third publish where a required source exceeds the budget ceiling. Construct a fixture that produces an oversized required source so the E2E assertion stays in the E2E test (per D-09 — the required-overflow assertion is E2E-level, not deferred to a unit test). Concrete approach: add a registry record whose descriptor (the `summary` or `canonical_identity` field that `planContextLoad` consumes as a required source) exceeds `DEFAULT_CONTEXT_CONTRACT.total_max_bytes` (12288 bytes) — e.g. a record with a ~13KB `summary` string. This causes `planContextLoad` to return `status: 'blocked', dispatch_eligible: false, reason_code: 'required_source_budget_exceeded'` for that workflow. Assert:
       - `const compiled = loadCompiledIndex({ ownedRoot });`
       - `assert.equal(compiled.budget.by_workflow[<workflowId>].dispatch_eligible, false, 'TOK-02 required-overflow → non-dispatchable baked at publish');`
       - `assert.equal(compiled.budget.by_workflow[<workflowId>].reason_code, 'required_source_budget_exceeded', 'required-overflow reason_code baked from planContextLoad');`
       - Then drive `routeContextPrompt({ prompt: '...', ownedRoot, projectRoot })` for that workflow_id and assert the route path observes the baked `dispatch_eligible: false` and synthesizes a blocked resolution (mirrors the existing `:105-110` blocked-resolution pattern).
       If the fixture proves infeasible during execution (e.g. the registry build rejects oversized descriptors), the executor MUST document the blocker in the plan summary and note the E2E required-overflow variant as a Phase 20 follow-up — but do NOT silently defer to a unit test. Prefer the oversized-descriptor fixture; it is the D-09-compliant evidence.
    4. Preserve the existing cleanup pattern at :131-137 (kill the controller child, rmSync the root).

    **tests/router.test-mode-seam.test.mjs** — extend the existing opt-in seam test at :56-101 with D-09 bundle-presence assertions. Inside the existing test body, after the existing `assert.equal(config.test_mode, true)`:

    1. Add a loop asserting the four new orchestrator module files are deployed and byte-equal to source:
       ```
       for (const name of ['orchestrator/select.mjs', 'orchestrator/transitions.mjs', 'orchestrator/budget.mjs', 'orchestrator/workflow-declarations.json']) {
         const deployed = join(ownedRoot, 'modules', name);
         const source = join('src', name);
         assert.equal(readFileSync(deployed, 'utf8'), readFileSync(source, 'utf8'), `${name} deployed in bundle`);
       }
       ```
       (Use the existing `readFileSync` import at :3; use `join` from `node:path` at :4 — both already imported.)
    2. Add `const compiled = loadCompiledIndex({ ownedRoot });` (already called elsewhere in the test) and `assert.ok(compiled.closure, 'closure sibling baked by publish-time orchestrator');` (D-01 baked closure readable from tuple).
    3. Do NOT modify the static-invariant test at :103-110 — it must still pass unchanged. The new assertions do NOT import `publishCompiledIndex` directly (the seam proves the real watcher→controller→publish path). Do NOT import `src/orchestrator/*` into the test (the route-path constraint is mirrored in the test where relevant).

    The D-09 backstop statement: `Flow 11 dispatch_eligible flips to PASS via the baked budget flag in the extended Phase 18 E2E` — record this in the test as a comment near the `assert.equal(compiled.budget.by_workflow[workflowId].dispatch_eligible, true, ...)` line so the verify-work checker can find it.
  </action>
  <verify>
    <automated>set -o pipefail; node --test tests/router.autonomous-lifecycle.test.mjs tests/router.test-mode-seam.test.mjs 2>&1 | tail -25 && grep -c "dispatch_eligible, true" tests/router.autonomous-lifecycle.test.mjs | grep -qv '^0$' && grep -c "orchestrator/workflow-declarations.json" tests/router.test-mode-seam.test.mjs | grep -qv '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/router.autonomous-lifecycle.test.mjs` exits 0
    - `node --test tests/router.test-mode-seam.test.mjs` exits 0
    - `grep -c "compiled.closure\|compiled.budget\|compiled.summaryIndex" tests/router.autonomous-lifecycle.test.mjs` returns ≥3 (D-09 sibling-presence assertions)
    - `grep -c "dispatch_eligible, true" tests/router.autonomous-lifecycle.test.mjs` returns ≥1 (Flow 11 PASS assertion — D-09 backstop)
    - `grep -c "required_source_budget_exceeded\|dispatch_eligible, false" tests/router.autonomous-lifecycle.test.mjs` returns ≥1 (TOK-02 required-overflow E2E assertion — NOT deferred to a unit test per D-09)
    - `grep -c "canonical_record" tests/router.autonomous-lifecycle.test.mjs` returns ≥1 ONLY in an assertion that no route carries that reason_code (negative assertion) — NOT in any positive fixture
    - `grep -c "orchestrator/workflow-declarations.json" tests/router.test-mode-seam.test.mjs` returns ≥1 (D-07 bundle presence for the static declarations file)
    - `grep -c "from '../src/prompt/publish-index.mjs'\|from '../src/orchestrator" tests/router.test-mode-seam.test.mjs` returns the same count as before this plan (the static-invariant test at :103-110 is preserved — no new direct imports of the publisher or orchestrator)
    - The existing test blocks in both files still pass (no regression from the extensions)
  </acceptance_criteria>
  <done>
    Both E2E test files are extended with D-09 assertions covering: closure/budget/summary-index presence in the published tuple; ORC-01 no-fallback (empty mapping → blocked, no canonical_record route); TOK-02 required-overflow E2E assertion (oversized-descriptor fixture → dispatch_eligible:false baked at publish — NOT deferred to a unit test per D-09); Flow 11 dispatch_eligible PASS (backstop assertion); bundle presence of the four new orchestrator module files. The static-invariant test is preserved. Both files pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: v1.2-matrix.json — add phase-19-live-path secondary evidence entry for ORC-01 + TOK-02</name>
  <files>release/v1.2-matrix.json</files>
  <read_first>
    - .planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-RESEARCH.md (Open Question #3 — recommendation: add the secondary entry; the matrix is the authoritative release gate per Phase 18 D-10/D-11)
    - release/v1.2-matrix.json (current structure — ORC-01 + TOK-02 primary entries point to Phase 16 unit tests; secondary phase-18-cross-cutting points to tests/router.compiled-evolution.test.mjs)
    - .planning/phases/18-autonomous-lifecycle-and-release-gates/18-CONTEXT.md (D-10/D-11 — the matrix is the authoritative release gate)
  </read_first>
  <action>
    Add a new secondary evidence entry to `release/v1.2-matrix.json` for ORC-01 and TOK-02 labeled `phase-19-live-path`, citing the two extended Phase 18 E2E test files as the live-path evidence. Concretely:

    1. Read the current matrix JSON structure. Identify the ORC-01 entry and the TOK-02 entry (they have `primary` evidence pointing to Phase 16 and `secondary` evidence with the `phase-18-cross-cutting` label).
    2. Append a new entry to the `secondary` array of ORC-01: `{ "label": "phase-19-live-path", "phase": 19, "tests": ["tests/router.autonomous-lifecycle.test.mjs", "tests/router.test-mode-seam.test.mjs"], "scope": "live publish→route path (publish-time orchestrator wiring + read-only sibling projection + bundle deployment)", "closure": "ORC-01: no capability dispatch without workflow-rooted mapping (canonical_record fallback removed); closure baked from selectCapabilities at publish" }`.
    3. Append a parallel new entry to the `secondary` array of TOK-02: `{ "label": "phase-19-live-path", "phase": 19, "tests": ["tests/router.autonomous-lifecycle.test.mjs", "tests/router.test-mode-seam.test.mjs"], "scope": "live publish→route path (context budget baked at publish via planContextLoad; dispatch_eligible flag observed on hot path)", "closure": "TOK-02: each workflow carries a baked budget report; required-overflow → dispatch_eligible:false at publish → route path synthesizes blocked resolution" }`.
    4. Do NOT modify the `primary` entries (Phase 16 unit tests remain the primary evidence — they are still valid). Do NOT modify the existing `phase-18-cross-cutting` secondary entries (they remain valid). Do NOT modify any other requirement's entry (ORC-02, TOK-01, EVO-05, REL-01, etc.).
    5. Preserve the matrix's existing JSON shape (do not reorder keys, do not change the top-level structure). The edit is purely additive: two new objects appended to two existing arrays.

    After the edit, run `node --test tests/router.v12-release.test.mjs` (the release-gate test that consumes the matrix) to confirm the new entries are accepted by the gate. If the test fails because the cited test files do not exist or do not match the expected pattern, fix the citation (the test files exist after Task 2 of this plan).
  </action>
  <verify>
    <automated>set -o pipefail; node --test tests/router.v12-release.test.mjs 2>&1 | tail -15 && grep -c "phase-19-live-path" release/v1.2-matrix.json | grep -q '^2$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "phase-19-live-path" release/v1.2-matrix.json` returns exactly 2 (one new secondary entry for ORC-01, one for TOK-02)
    - `node --test tests/router.v12-release.test.mjs` exits 0 (the release gate accepts the new entries)
    - `git diff release/v1.2-matrix.json` shows ONLY the two new objects appended to the two secondary arrays — no other keys modified, no primary entries changed
    - The ORC-01 primary entry and the TOK-02 primary entry are byte-identical to the pre-edit version (Phase 16 unit tests remain the primary evidence)
    - The existing `phase-18-cross-cutting` secondary entries are byte-identical to the pre-edit version
  </acceptance_criteria>
  <done>
    release/v1.2-matrix.json carries the phase-19-live-path secondary evidence entry for both ORC-01 and TOK-02, citing the two extended E2E test files. The release gate test passes with the new entries. The traceability gap (REQUIREMENTS.md marks both Complete in Phase 16 but the live path was not covered) is now closed with live-path evidence recognized by the authoritative release matrix.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-fixture→test-runner | Schema-bump fixture edits cross from the test author into the test execution; a malformed fixture could mask a real regression |
| matrix→release-gate | The v1.2-matrix.json secondary entries are consumed by the release-gate test; a malformed entry could weaken the gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-07 | Tampering | test fixtures — schema-bump sweep could mask a regression by making fixtures match new shape without exercising new behavior | medium | mitigate | Task 2 adds dedicated D-09 assertions that exercise the NEW behavior (sibling presence, ORC-01 no-fallback, TOK-02 required-overflow, Flow 11 PASS, bundle presence). The fixture sweep (Task 1) only updates the shape; Task 2 proves the behavior. The two tasks are deliberately separate so a fixture-shape pass cannot mask a behavior gap. |
| T-19-08 | Tampering | v1.2-matrix.json — a malformed secondary entry could let the release gate pass without real evidence | low | mitigate | The release-gate test (`tests/router.v12-release.test.mjs`) validates the matrix structure; the new entries cite concrete test files (Task 2 outputs) that must exist and pass. A malformed entry fails the gate test (Task 3 verify). |
| T-19-09 | Elevation of privilege | test fixture edits accidentally widen permissions or disable safety checks | low | accept | Test fixtures are test-only; they do not affect production code. The edits are schema_version bumps and compatibility-member additions, not permission changes. |
| T-19-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs. N/A — satisfied trivially. |
</threat_model>

<verification>
- `node --test tests/*.test.mjs` exits 0 (full suite green — the schema-bump churn is fully absorbed AND the D-09 E2E assertions pass).
- `grep -c "phase-19-live-path" release/v1.2-matrix.json` returns 2.
- `node --test tests/router.v12-release.test.mjs` exits 0 (release gate accepts the new matrix entries).
- `grep -rln "canonical_record" tests/` returns empty (no test references the removed reason_code).
- `grep -c "dispatch_eligible, true" tests/router.autonomous-lifecycle.test.mjs` returns ≥1 (Flow 11 PASS backstop assertion present).
- D-09 backstop statement `Flow 11 dispatch_eligible flips to PASS via the baked budget flag in the extended Phase 18 E2E` is present as a comment in tests/router.autonomous-lifecycle.test.mjs near the assertion.
</verification>

<success_criteria>
- ORC-01 live-path evidence: the extended E2E proves no capability dispatch without workflow-rooted mapping (empty mapping → blocked, no canonical_record route).
- TOK-02 live-path evidence: the extended E2E proves required-overflow bakes dispatch_eligible:false at publish and the route path observes it.
- Flow 11 dispatch_eligible PASS backstop: the extended E2E asserts `compiled.budget.by_workflow[workflowId].dispatch_eligible === true` for a normal publish.
- D-07 bundle presence: the extended test-mode-seam test asserts the four new orchestrator module files are deployed and byte-equal to source.
- D-04 schema bump fully absorbed: the full test suite is green with all tuple fixtures at schema 2 and all compatibility objects carrying the two new members.
- v1.2 release matrix carries the phase-19-live-path secondary evidence entry for ORC-01 and TOK-02.
- Phase 17 D-02 fail-closed integrity: the D-09 E2E exercises the sibling-hash verification path (a sibling tampering fixture is out of scope — that is a Plan 02 unit test responsibility; the E2E proves the happy path).
</success_criteria>

<output>
Create `.planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-04-SUMMARY.md` when done.

## Artifacts this phase produces

**New exported symbols:** none.

**New files created:** none (all edits are to existing test files and the existing release matrix).

**Modified files:**
- `tests/router.autonomous-lifecycle.test.mjs` — +1 test block with D-09 assertions (closure/budget/summaryIndex presence, ORC-01 empty-mapping, TOK-02 required-overflow, Flow 11 dispatch_eligible PASS backstop comment)
- `tests/router.test-mode-seam.test.mjs` — +D-09 bundle-presence assertions (four new orchestrator module files deployed and byte-equal to source) + +closure-readable-from-tuple assertion. Static-invariant test at :103-110 preserved unchanged.
- ~13-15 fixture test files — schema_version:1 → schema_version:2 in tuple contexts; COMPILED_INDEX_COMPATIBILITY objects extended with the two new members; pointer fixtures bumped to schema_version:2; Pitfall #4 empty-mapping publish calls in lifecycle-recovery + compiled-index fixed (mapped subject added OR throw asserted). Split across Task 1a (7 core files: Pitfall #4 audit + compatibility constructors) and Task 1b (8 remaining tuple-context fixture files).
- `release/v1.2-matrix.json` — +2 secondary evidence entries (phase-19-live-path for ORC-01 and TOK-02), appended to the existing secondary arrays

**New matrix entries:** `phase-19-live-path` secondary evidence label for ORC-01 and TOK-02, citing `tests/router.autonomous-lifecycle.test.mjs` + `tests/router.test-mode-seam.test.mjs`

**New D-09 backstop statement:** `Flow 11 dispatch_eligible flips to PASS via the baked budget flag in the extended Phase 18 E2E` (recorded as a comment in autonomous-lifecycle.test.mjs)

The newly-created symbols that downstream plan-review-convergence source-grounding must exclude from drift verification: the `phase-19-live-path` matrix label, the D-09 backstop statement comment, the new test-block name in autonomous-lifecycle.test.mjs, the new bundle-presence assertion loop in test-mode-seam.test.mjs.
</output>
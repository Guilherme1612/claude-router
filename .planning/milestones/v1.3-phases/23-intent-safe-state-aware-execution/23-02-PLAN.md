---
phase: 23-intent-safe-state-aware-execution
plan: 02
type: execute
wave: 2
depends_on: ['23-01']
files_modified:
  - src/intent/classify.mjs
  - src/orchestrator/actions.mjs
  - src/orchestrator/next-prompt.mjs
  - tests/router.intent-adversarial.test.mjs
  - tests/router.actions.test.mjs
autonomous: true
requirements: [INT-03, INT-06, EXEC-01, EXEC-02, EXEC-03, EXEC-04, EXEC-06, EXEC-10]
must_haves:
  truths:
    - "Every adversarial minimal pair (go/don't go, run/never run, debug/don't debug) produces opposite dispositions and the negative member has dispatch_eligible=false (INT-06 minimal pairs)"
    - "Nested quotations, mixed negation, corrections, and conditional language are classified as non-execute with dispatch_eligible=false (INT-06)"
    - "Multilingual prompts (Spanish + Portuguese) with execute-like verbs are classified as ambiguous/abstain, not execute (INT-06 multilingual)"
    - "Unsafe-target prompts (e.g., 'delete the manifest') do not dispatch without approval; the adversarial corpus asserts the dispatcher was never called for non-execute dispositions (INT-06 negative invocation assertion)"
    - "resolveAction reads contract.fields.workflow_transitions of installed capabilities only; no read of workflow-declarations.json compatible/owners lists as authority (EXEC-01)"
    - "'go to the next phase' against fresh state with one eligible transition selects one capability (EXEC-02 full)"
    - "'there is a bug' / 'debug this' maps to the debugging semantic category and selects the compatible debugging capability by contract (EXEC-03)"
    - "'create a phase about X' derives the next phase number from readRoadmapSource and invokes the phase-creation capability with the topic as structured argument (EXEC-04)"
    - "Ties (two eligible capabilities for one transition) → status='clarify', reason_code='material_capability_tie', one focused question — never first-wins (EXEC-06, Pitfall 5)"
    - "Stale state (freshness !== 'fresh'), terminal workflows, unresolved gaps, missing dependencies → status='blocked' with the corresponding stable reason_code (EXEC-06)"
    - "synthesizeNextPrompt returns a framework-neutral ready-to-use prompt built from the selected capability's invocation shape — no hardcoded `/gsd-` slash (EXEC-10)"
    - statement: "Next-phase numbering boundary (current phase = max) produces a blocked reason_code rather than an off-by-one — covered when roadmap fixture is at the last phase"
      verification: backstop
    - statement: "Running 'create a phase about X' twice on the same roadmap produces the same derived next number (idempotent read) — tested with a frozen roadmap fixture"
      verification: backstop
  prohibitions:
    - "The action mapper MUST NOT read workflow-declarations.json compatible/owners lists as dispatch authority (EXEC-01) — only the workflow-id namespace is reused"
    - "The action mapper MUST NOT resolve ties by picking the first capability (Pitfall 5, EXEC-06)"
    - "The next-prompt synthesizer MUST NOT emit a hardcoded `/gsd-...` slash command (EXEC-10)"
    - "The classifier MUST NOT classify non-English execute-like verbs as execute (INT-06 multilingual abstention)"
  artifacts:
    - tests/router.intent-adversarial.test.mjs
    - tests/router.actions.test.mjs
  key_links:
    - "classifyIntent adversarial minimal pairs → negative invocation assertion (dispatcher never called for non-execute)"
    - "resolveAction debug/create-phase verbs → contract.fields.workflow_transitions match → one selected capability or blocked/clarify"
    - "synthesizeNextPrompt(selection) → framework-neutral prompt from capability.invocation shape"
---

<objective>
Close the adversarial intent corpus (INT-03, INT-06) and expand the action mapper to the full set of natural-language verbs (next phase, debug, create phase) with framework-neutral contract resolution and the full blocked/clarify vocabulary (EXEC-01, 02, 03, 04, 06, 10).

Purpose: After the tracer proves the architecture end-to-end, this plan hardens the intent classifier against adversarial minimal pairs and multilingual prompts, and broadens the action mapper from the single "next phase" verb to the three verbs the success criteria name.
Output: Two new test files (adversarial corpus, action mapper unit+integration) and the expanded `actions.mjs` / `next-prompt.mjs` / `classify.mjs` behavior.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-intent-safe-state-aware-execution/23-RESEARCH.md
@.planning/phases/23-intent-safe-state-aware-execution/23-PATTERNS.md
@.planning/phases/23-intent-safe-state-aware-execution/23-01-SUMMARY.md
@src/intent/classify.mjs
@src/orchestrator/actions.mjs
@src/orchestrator/transitions.mjs
@src/registry/contract.mjs
@src/context/sources.mjs
@tests/router.contract-eligibility.test.mjs
@tests/helpers/inventory-fixture.mjs
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Adversarial intent corpus — minimal pairs, nested quotes, multilingual, unsafe targets (INT-03/06)</name>
  <files>tests/router.intent-adversarial.test.mjs, src/intent/classify.mjs</files>
  <read_first>
    - src/intent/classify.mjs (current classifier from Plan 01)
    - tests/router.contract-eligibility.test.mjs (negative-assertion pattern at 73-79; phase-tagged names at 51)
    - .planning/phases/23-intent-safe-state-aware-execution/23-RESEARCH.md (Pitfall 1 minimal-pair ordering; Pitfall 3 unsafe targets)
  </read_first>
  <behavior>
    - Minimal pairs (each pair differs by one token, opposite dispatch_eligible): "go to the next phase"/"don't go to the next phase", "run the tests"/"never run the tests", "debug this"/"don't debug this", "ship it"/"don't ship it"
    - Nested quotations: "he said 'run the deploy' yesterday" → quoted (not execute); triple-backtick block → quoted
    - Mixed negation: "run the tests but do not commit" → negated (precedence: negation wins)
    - Corrections: "actually, don't run X" → negated (newest token wins per INT-04)
    - Conditional language: "if the build passes, deploy" → hypothetical (not execute)
    - Multilingual: "ejecuta la siguiente fase" (Spanish), "execute a próxima fase" (Portuguese) → ambiguous (abstain, not execute)
    - Unsafe targets: "delete the manifest", "rm -rf the registry" → not execute (prohibited or ambiguous), dispatch_eligible=false
    - Negative invocation assertion: a spy/mock on resolveAction is never called for any non-execute disposition in the corpus
  </behavior>
  <action>
    Create `tests/router.intent-adversarial.test.mjs` with the corpus above as parameterized cases. For each prompt, assert `classifyIntent(prompt).disposition !== 'execute'` AND `dispatch_eligible === false`. For minimal pairs, assert the pair produces opposite `dispatch_eligible`. Install a spy on `resolveAction` (lazy-imported) and assert it is never invoked for any non-execute corpus member (INT-06 negative invocation assertion). Phase-tag `[phase23-red:intent-adversarial]`.

    Run RED first — if any case passes as execute, fix the classifier precedence/regex in `src/intent/classify.mjs` (e.g., tighten the `EXECUTE_VERBS` regex to require leading word boundary, add a multilingual guard that requires an English execute-verb token for `execute`, strengthen nested-quote detection). Do NOT loosen the corpus — the corpus is the spec (INT-06).
  </action>
  <verify>
    <automated>rtk node --test tests/router.intent-adversarial.test.mjs</automated>
  </verify>
  <done>
    - All minimal pairs produce opposite dispositions
    - No non-execute corpus member has dispatch_eligible=true
    - resolveAction spy never called for non-execute corpus members
    - Multilingual prompts abstain (ambiguous), never execute
    - `rtk node --test tests/router.intent-adversarial.test.mjs` exits 0
  </done>
  <reversibility rating="reversible">Test corpus + classifier regex tightening; no schema/state change.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Action mapper — debug, create-phase verbs + full blocked/clarify vocabulary (EXEC-01/02/03/04/06/10)</name>
  <files>src/orchestrator/actions.mjs, src/orchestrator/next-prompt.mjs, tests/router.actions.test.mjs</files>
  <read_first>
    - src/orchestrator/actions.mjs (from Plan 01 tracer — expand resolveAction verb parsing)
    - src/orchestrator/transitions.mjs (nextValidTransitions freshness/terminal/gates gates; clarification() at 131-137)
    - src/orchestrator/select.mjs (selectCapabilities, hook exclusion at 131)
    - src/registry/contract.mjs (CONTRACT_FIELDS, workflow_transitions envelope, purpose/triggers fields)
    - src/context/sources.mjs (readStateSource, readRoadmapSource — call per dispatch, do not cache)
    - src/orchestrator/workflow-declarations.json (confirm empty owners/compatible — NOT the authority)
    - tests/router.contract-eligibility.test.mjs (safeRecord + contractEvidence fixture pattern)
  </read_first>
  <behavior>
    - "debug this" / "there is a bug" → resolveAction maps to debugging semantic category (contract.fields.purpose or triggers contains debug/troubleshooting) → one eligible debugging capability selected (EXEC-03)
    - "create a phase about authentication" → resolveAction reads readRoadmapSource, derives next phase number (current max + 1), maps to phase-creation capability whose contract.workflow_transitions includes the plan transition, passes { next_number, topic:'authentication' } as structured args (EXEC-04)
    - Two eligible capabilities for one transition → status='clarify', reason_code='material_capability_tie', one focused question (EXEC-06)
    - Stale state (freshness='stale') → status='blocked', reason_code='authoritative_evidence_stale' (EXEC-06)
    - Terminal workflow (status='terminal') → status='blocked', reason_code='terminal_workflow' (EXEC-06)
    - Missing dependency (dependencies_safe=false) → status='blocked', reason_code='dependency_unavailable' or 'dependency_conflict' (EXEC-06)
    - No eligible capability (empty registry for the transition) → status='blocked', reason_code='no_eligible_capability' (EXEC-01, EXEC-06 empty edge)
    - synthesizeNextPrompt for a debug selection → framework-neutral prompt from capability.invocation shape, no `/gsd-` slash (EXEC-10)
  </behavior>
  <action>
    Expand `src/orchestrator/actions.mjs` `resolveAction` to parse the three verb families:
    (1) "next phase" — already in tracer; reuse.
    (2) "debug"/"bug"/"troubleshoot" — match capabilities whose `contract.fields.purpose` or `contract.fields.triggers` envelope (state='known') contains `debug`/`troubleshooting` semantic tokens; filter by eligibility; select the unique one or block/clarify.
    (3) "create a phase about <topic>"/"plan a phase for <topic>" — call `readRoadmapSource` to derive `next_number = current_max_phase + 1`; match capabilities whose `contract.fields.workflow_transitions` includes the phase-creation transition; pass `{ next_number, topic }` as structured args to the selected capability.

    Reuse `nextValidTransitions`'s freshness/terminal/gates gating for all three verbs — do NOT re-implement. Add the blocked reason codes `authoritative_evidence_stale`, `terminal_workflow`, `dependency_unavailable`, `dependency_conflict`, `no_eligible_capability`, `material_capability_tie` (per RESEARCH EXEC-06 + existing `transitions.mjs` vocabulary). Never read `src/orchestrator/workflow-declarations.json` `compatible`/`owners` as authority — only as the workflow-id namespace.

    Expand `src/orchestrator/next-prompt.mjs` `synthesizeNextPrompt` to handle debug and create-phase selections — still framework-neutral (build from `capability.invocation` shape, never a hardcoded slash).

    Create `tests/router.actions.test.mjs` with the behaviors above as parameterized cases. Use the `safeRecord` + `contractEvidence` pattern from `tests/router.contract-eligibility.test.mjs:13-23`, populating `workflow_transitions` and `purpose`/`triggers` envelopes per case. Phase-tag `[phase23-red:actions]`. Assert blocked/clarify reason codes exactly. Run `rtk node --test tests/router.actions.test.mjs` (RED→GREEN).
  </action>
  <verify>
    <automated>rtk node --test tests/router.actions.test.mjs</automated>
  </verify>
  <done>
    - "debug this" selects one debugging capability by contract purpose/triggers
    - "create a phase about X" derives next number from roadmap and passes topic as structured arg
    - Ties → clarify (never first-wins); stale/terminal/missing-dep/empty → blocked with exact reason codes
    - synthesizeNextPrompt emits no `/gsd-` slash for any selection
    - `grep -c 'gsd-' src/orchestrator/actions.mjs` returns 0
    - `rtk node --test tests/router.actions.test.mjs` exits 0
  </done>
  <reversibility rating="reversible">Additive verb parsing in actions.mjs; next-prompt expansion. No persisted state.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adversarial prompt → classifier | Minimal pairs and multilingual prompts test the negation/quote/conditional precedence |
| action mapper → contract envelope | Framework-neutral read of `contract.fields.workflow_transitions` — hardcoded-name bypass lives here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-02 | Tampering | classify.mjs (negation bypass on minimal pairs) | high | mitigate | Adversarial minimal-pair corpus asserts opposite dispositions; multilingual guard requires English execute-verb token. |
| T-23-06 | Spoofing/Tampering | actions.mjs (framework-name fallback) | medium | mitigate | `grep -c 'gsd-' src/orchestrator/actions.mjs` done-gate at 0; tests assert contract-envelope-only resolution. |
| T-23-08 | Tampering | actions.mjs (stale state across dispatch boundary) | high | mitigate | `readStateSource` called per dispatch; `nextValidTransitions` freshness gate; test asserts `authoritative_evidence_stale` on stale fixture. |
| T-23-09 | Elevation of privilege | actions.mjs (discovery treated as authority) | medium | mitigate | Discovery records are evidence; only `registry.eligibility[stableCapabilityId(r)].eligible === true` capabilities are selected — re-authoring eligibility is forbidden (Anti-Pattern). |
| T-23-V5 | Input Validation | classify.mjs (multilingual + nested-quote input) | medium | mitigate | Regex precedence never evals content; nested-quote detection handles triple-backtick and nested single/double quotes. |
| T-23-SC | Tampering | (no package installs) | n/a | accept | Stdlib-only; supply-chain gate n/a. |
</threat_model>

<verification>
- `rtk node --test tests/router.intent-adversarial.test.mjs tests/router.actions.test.mjs` exits 0
- `grep -c 'gsd-' src/orchestrator/actions.mjs` → 0
- `grep -c 'gsd-' src/orchestrator/next-prompt.mjs` → 0
- Full Phase 23 + Phase 22 regression: `rtk node --test tests/router.intent.test.mjs tests/router.intent-adversarial.test.mjs tests/router.actions.test.mjs tests/router.contract-eligibility.test.mjs tests/router.contracts.test.mjs`
</verification>

<success_criteria>
- Adversarial corpus green: no non-execute prompt dispatches; minimal pairs opposite; multilingual abstains
- Action mapper handles next-phase, debug, create-phase verbs via contract authority only
- Ties/stale/terminal/missing-dep/empty all produce the exact stable blocked reason codes
- Framework-neutral next-prompt for all three verb selections
</success_criteria>

<output>
Create `.planning/phases/23-intent-safe-state-aware-execution/23-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

**Plan 02 symbols (new/expanded):**
- `tests/router.intent-adversarial.test.mjs`: adversarial corpus (no new exports — test-only)
- `tests/router.actions.test.mjs`: action mapper unit+integration tests (no new exports)
- `src/intent/classify.mjs`: (expanded) tightened `EXECUTE_VERBS`/`NEGATION`/`PROHIBITION`/`QUOTED` regexes; multilingual abstention guard; no new exports
- `src/orchestrator/actions.mjs`: (expanded) `resolveAction` debug + create-phase verb parsing; blocked reason codes `authoritative_evidence_stale`, `terminal_workflow`, `dependency_unavailable`, `dependency_conflict`, `no_eligible_capability`, `material_capability_tie`; no new exports
- `src/orchestrator/next-prompt.mjs`: (expanded) `synthesizeNextPrompt` debug + create-phase selection handling; no new exports
- No new CLI flags, no new struct fields, no new data files.
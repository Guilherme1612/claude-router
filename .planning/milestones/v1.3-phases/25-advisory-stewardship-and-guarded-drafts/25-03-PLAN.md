---
phase: 25-advisory-stewardship-and-guarded-drafts
plan: 03
type: execute
wave: 3
depends_on: [25-01, 25-02]
files_modified:
  - src/cli/router-control.mjs
  - tests/router.steward-cli.test.mjs
autonomous: true
requirements: [UX-01, UX-02, UX-03, UX-04, UX-05, UX-06, UX-07, UX-08, UX-09]
must_haves:
  truths:
    - "UX-03/UX-04: `/router suggestion` exposes exactly one prioritized recommendation and compact overview in the canonical envelope and approved terminal copy."
    - "UX-05: inspect, dismiss, snooze, and correct are the only interaction actions and never mutate routing."
    - "UX-06/UX-07: draft is proposal-first; exact approval creates the draft artifact and only then exposes its complete preview."
    - "UX-09: one suggestion command family exists; no dashboard, timeline, finding dump, summary, or maintenance suite is added."
  artifacts:
    - path: src/cli/router-control.mjs
      provides: "Strict suggestion grammar, canonical envelopes, text rendering, and dependency seams."
    - path: tests/router.steward-cli.test.mjs
      provides: "End-to-end CLI state/copy/privacy/no-mutation coverage."
  key_links:
    - from: src/cli/router-control.mjs
      to: src/steward/suggestion.mjs
      via: "Single selector result projected through canonical response."
      pattern: "suggestion"
    - from: src/cli/router-control.mjs
      to: src/steward/draft.mjs
      via: "Preview-first draft action and explicit approval execution."
      pattern: "draft"
---

<objective>
Expose the entire advisory workflow through the one approved `/router suggestion` command family.

Purpose: turn the policy/store/draft modules into the exact bounded terminal interaction while preserving existing CLI contracts.
Output: surgical router-control wiring and one CLI integration suite.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-advisory-stewardship-and-guarded-drafts/25-CONTEXT.md
@.planning/phases/25-advisory-stewardship-and-guarded-drafts/25-UI-SPEC.md
@.planning/phases/25-advisory-stewardship-and-guarded-drafts/25-RESEARCH.md
@.planning/phases/25-advisory-stewardship-and-guarded-drafts/25-PATTERNS.md
@.planning/phases/25-advisory-stewardship-and-guarded-drafts/25-01-SUMMARY.md
@.planning/phases/25-advisory-stewardship-and-guarded-drafts/25-02-SUMMARY.md

<interfaces>
From src/cli/router-control.mjs:
- canonical(command, ok, reasonCode, data, warnings), strict parse(argv), EXIT values, runRouterControl dependency injection, and specialized text renderers are the required patterns.
From Plans 25-01/02:
- selectSuggestion, createStewardStore, previewDraft, and approveDraftCreation are the only domain/storage operations this controller may invoke.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire inspect, empty, dismiss, snooze, and correct states</name>
  <read_first>src/cli/router-control.mjs, src/steward/suggestion.mjs, src/steward/state.mjs, tests/router.control-cli.test.mjs, tests/router.health.admin.test.mjs, .planning/phases/25-advisory-stewardship-and-guarded-drafts/25-UI-SPEC.md</read_first>
  <files>src/cli/router-control.mjs, tests/router.steward-cli.test.mjs</files>
  <behavior>
    - suggestion with no qualifying item succeeds as suggestion_none and leaks no rejected candidates.
    - suggestion detail contains exactly one item and every UX-04 field.
    - dismiss/snooze/correct require the current exact fingerprint and return the approved copy.
    - Unknown subcommands/options, malformed fingerprints, unsafe timestamps, and oversized JSON use existing canonical exit semantics.
    - Text and JSON remain deterministic, color-independent, bounded, and private.
  </behavior>
  <action>Write CLI tests first. Add one `suggestion` family to existing parser, usage, `runRouterControl`, and rendering, using dependency injection for selection/store operations. Reuse canonical envelope/exit codes and the UI-SPEC copy exactly. Keep grammar narrow: inspect by default plus dismiss, snooze, correct, and draft. Extend only the strict bounded options needed for fingerprint, expiry, correction payload, execute, and approval. The overview is aggregate-only and the detail holds one suggestion. No new executable, renderer abstraction, dashboard, list endpoint, or maintenance action.</action>
  <verify>
    <automated>rtk node --test tests/router.steward-cli.test.mjs --test-name-pattern="suggestion|dismiss|snooze|correct|empty"</automated>
  </verify>
  <acceptance_criteria>The single command family renders all non-draft states with canonical shapes/copy, strict validation, exactly one suggestion, and byte-identical protected artifacts.</acceptance_criteria>
  <done>UX-03 through UX-05 and UX-09 are reachable through the canonical operator surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire proposal-first draft approval without expanding CLI authority</name>
  <read_first>src/cli/router-control.mjs, src/steward/draft.mjs, src/orchestrator/approval.mjs, tests/router.control-cli.test.mjs, tests/router.steward-draft.test.mjs</read_first>
  <files>src/cli/router-control.mjs, tests/router.steward-cli.test.mjs</files>
  <behavior>
    - suggestion draft without execute returns only the bounded approval proposal and approval-only warning, with no write.
    - Execute without exact fresh approval fails with existing usage/unsafe semantics.
    - Execute with exact fresh approval creates the draft, returns its complete preview, and changes only the steward draft root.
    - Only the post-approval output includes the exact preview-only non-publication warning and every UX-07 field.
  </behavior>
  <action>Route the draft subcommand to Plan 25-02. Mirror the existing proposal/execute/exact-confirmation controller shape, but bind the approval token rather than destination text and keep the only effect `draft_file_only`. Before approval render only the bounded proposal scope; after approved creation render the complete UX-07 preview. Preserve existing status/diff/rollback/canary/health behaviors and usage text. Add regression assertions that no forbidden maintenance subcommands exist and no protected artifact bytes change.</action>
  <verify>
    <automated>rtk node --test tests/router.steward-cli.test.mjs tests/router.control-cli.test.mjs</automated>
  </verify>
  <acceptance_criteria>Draft CLI flow is recommendation → explicit approval → draft creation → complete preview/private artifact; no install/publish action or extra command family is reachable.</acceptance_criteria>
  <done>UX-06 through UX-09 are reachable and guarded through the one approved CLI surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Terminal argv/stdin → router-control | Untrusted command, fingerprint, timestamp, JSON, and approval enter parsing. |
| Domain result → terminal | Private local state is projected to text/JSON. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-25-09 | Tampering | CLI parser | high | mitigate | Strict allowlist, length/type bounds, exact fingerprint/approval checks. |
| T-25-10 | Information Disclosure | CLI renderer | high | mitigate | Canonical allowlisted projections and private-root/raw-content negative assertions. |
| T-25-11 | Elevation of Privilege | draft execute branch | critical | mitigate | Delegate to draft-only fresh approval and protected-byte tests. |
| T-25-12 | Denial of Service | correction JSON | medium | mitigate | Bound argument size, parsed collection size, and output cardinality. |
| T-25-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package install exists; stdlib and current modules only. |
</threat_model>

<verification>
Nyquist: focused CLI tests cover every interaction state and exact UI copy. Run focused and existing control tests under node:test.
</verification>

<success_criteria>
`/router suggestion` is the sole complete interaction surface and preserves canonical output, strict validation, privacy, and no-publication authority.
</success_criteria>

<output>
Create `.planning/phases/25-advisory-stewardship-and-guarded-drafts/25-03-SUMMARY.md` when done.
</output>

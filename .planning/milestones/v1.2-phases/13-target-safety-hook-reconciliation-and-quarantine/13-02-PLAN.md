---
phase: 13-target-safety-hook-reconciliation-and-quarantine
plan: "02"
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/registry/reconcile.mjs
  - src/registry/watcher.mjs
  - tests/router.registry-reconcile.test.mjs
  - tests/router.registry-watcher.test.mjs
autonomous: true
requirements: [SAF-09, SAF-10]
must_haves:
  truths:
    - "Missing dependencies, denied or missing permissions, inapplicable scope, blocking collisions, and ambiguous mappings each produce deterministic structured non-dispatchable verdicts."
    - "Full and incremental candidates derived from the same acquired state and lifecycle diff produce byte-identical reconciliation reports."
    - "A quarantined candidate publishes portable corrective diagnostics but does not write active registry bytes, change its fingerprint, invoke activation, or advance a version pointer."
  artifacts:
    - path: src/registry/reconcile.mjs
      provides: "Whole-candidate dependency, permission, scope, collision, and ambiguity safety matrix"
      exports: [reconcileCandidate]
    - path: src/registry/watcher.mjs
      provides: "Candidate/quarantine report publication boundary preserving last-known-good active state"
      exports: [createRegistryReconciler]
    - path: tests/router.registry-reconcile.test.mjs
      provides: "SAF-09 whole-candidate gate and full/incremental equivalence fixtures"
    - path: tests/router.registry-watcher.test.mjs
      provides: "SAF-10 publication failure and unchanged-active-state coverage"
  key_links:
    - from: src/registry/reconcile.mjs
      to: src/registry/schema.mjs
      via: "validated dependency, permission, lifecycle, invocation, scope, and conflict fields"
      pattern: "dependencies|permissions|scope|conflicts"
    - from: src/registry/watcher.mjs
      to: src/registry/reconcile.mjs
      via: "installed reconciler evaluates assembled inactive candidate before publishing candidate/report"
      pattern: "reconcileCandidate"
    - from: src/registry/watcher.mjs
      to: active registry snapshot
      via: "read-only before/after bytes and fingerprint assertion; no active write path"
      pattern: "active|candidate_path|report_path"
---

<objective>
Complete the whole-candidate safety matrix and wire quarantine reporting into the installed reconciliation boundary without replacing last-known-good active state.

Purpose: Make dependency, permission, scope, collision, and ambiguity failures portable and non-dispatchable while preserving deterministic full/incremental behavior.
Output: Extended reconciliation gates, transactional candidate/report publication, and SAF-09/SAF-10 focused evidence.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-target-safety-hook-reconciliation-and-quarantine/13-CONTEXT.md
@.planning/phases/13-target-safety-hook-reconciliation-and-quarantine/13-RESEARCH.md
@.planning/phases/13-target-safety-hook-reconciliation-and-quarantine/13-PATTERNS.md
@.planning/phases/13-target-safety-hook-reconciliation-and-quarantine/13-VALIDATION.md
@.planning/phases/13-target-safety-hook-reconciliation-and-quarantine/13-01-SUMMARY.md
@.planning/phases/12-incremental-change-detection-and-watcher/12-02-SUMMARY.md
@.planning/phases/12-incremental-change-detection-and-watcher/12-03-SUMMARY.md
@.planning/phases/12-incremental-change-detection-and-watcher/12-04-SUMMARY.md
@src/registry/reconcile.mjs
@src/registry/build.mjs
@src/registry/watcher.mjs
@src/registry/schema.mjs

<interfaces>
- `buildFullRegistry`, `buildIncrementalRegistry`, and `assembleRegistry` converge on canonical inactive registry bytes.
- `createRegistryReconciler` owns candidate/report publication and advances acquisition state only after both publications succeed.
- The active registry is an explicitly supplied last-known-good snapshot. Phase 13 has no authority to activate or advance immutable version pointers.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply the whole-candidate dependency, permission, scope, collision, and ambiguity matrix</name>
  <read_first>
    <file>src/registry/reconcile.mjs</file>
    <file>src/registry/schema.mjs</file>
    <file>src/registry/build.mjs</file>
    <file>tests/router.registry-reconcile.test.mjs</file>
    <file>tests/router.registry-build.test.mjs</file>
    <file>tests/router.adapters.test.mjs</file>
    <file>tests/router.route-targets.test.mjs</file>
  </read_first>
  <files>src/registry/reconcile.mjs, tests/router.registry-reconcile.test.mjs</files>
  <behavior>
    - A required dependency that is absent, disabled, invalid, ambiguous, or not dispatchable rejects each dependent subject.
    - Required permissions that are missing or denied reject the subject; undeclared ambient permission does not silently satisfy the requirement.
    - Global, project, and worktree scope constraints prevent leakage and never fall back outward or sideways after resolution fails.
    - Canonical-ID/native-identity collisions, duplicate authoritative claims, and more than one plausible mapping quarantine every affected subject.
    - Full and incremental candidates built from identical acquired state and lifecycle evidence yield identical report bytes under input permutations.
  </behavior>
  <action>Add failing table-driven fixtures for the complete SAF-09 matrix, then extend `reconcileCandidate` with independent pure verdict producers for dependency availability, declared permission, applicability of scope, canonical/native identity collision, and mapping ambiguity. Evaluate the complete candidate rather than only changed lifecycle records so an unchanged dependent cannot remain dispatchable after its dependency disappears. Use the established precedence and scope contracts from schema/build; do not create fallback resolution. Normalize each affected subject and evidence through the shared Plan 13 verdict constructor and sort only after all gates run. A high/blocking verdict must force `quarantined`; multiple findings may coexist but must not duplicate across full versus incremental entry paths. Add paired full/incremental fixtures using Phase 12 acquisition and lifecycle seams, covering every matrix row, order permutations, diagnostics, and byte equality. Keep verdict codes stable and machine-portable; use the agent's discretion only for exact vocabulary and internal helper layout.</action>
  <acceptance_criteria>
    <criterion>Every missing dependency, permission denial, scope leak, collision, and ambiguous mapping case yields a stable subject-specific non-dispatchable verdict.</criterion>
    <criterion>No rejected edge silently resolves through runtime, scope, precedence, or same-name fallback.</criterion>
    <criterion>Full and incremental paths produce identical reconciliation bytes from the same authoritative state and diff.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/router.registry-reconcile.test.mjs tests/router.registry-build.test.mjs tests/router.adapters.test.mjs tests/router.route-targets.test.mjs</automated>
  </verify>
  <done>The complete SAF-09 candidate matrix deterministically rejects every unsafe dispatch-authority edge and preserves full/incremental equivalence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish quarantine diagnostics while preserving last-known-good active state</name>
  <read_first>
    <file>src/registry/watcher.mjs</file>
    <file>src/registry/reconcile.mjs</file>
    <file>src/registry/build.mjs</file>
    <file>tests/router.registry-watcher.test.mjs</file>
    <file>tests/router.registry-reconcile.test.mjs</file>
    <file>.planning/phases/12-incremental-change-detection-and-watcher/12-04-SUMMARY.md</file>
  </read_first>
  <files>src/registry/watcher.mjs, tests/router.registry-watcher.test.mjs, tests/router.registry-reconcile.test.mjs</files>
  <behavior>
    - An eligible candidate publishes deterministic inactive candidate bytes plus its reconciliation report; `activated` remains false.
    - A quarantined candidate publishes only deterministic corrective report/candidate diagnostics permitted by the existing inactive paths and never invokes an active-registry write or activation callback.
    - Candidate or report publication failure leaves acquisition, fingerprint, active bytes, and active fingerprint at their last-known-good values.
    - Startup with an unsafe candidate fails closed instead of publishing a misleading ready/eligible status.
    - Reports contain portable corrective action and no absolute host path.
  </behavior>
  <action>Inject or import `reconcileCandidate` at the installed `createRegistryReconciler` boundary after full/incremental candidate assembly and before candidate/report publication. Read the configured active snapshot only as evidence and capture its exact bytes plus canonical fingerprint; do not add any active-write path. For `eligible`, retain inactive candidate publication and publish the deterministic report. For `quarantined`, publish a portable report that names affected subjects, reasons, and corrective actions while preserving active bytes/fingerprint and withholding dispatch authority. Keep both publications atomic using the existing temp-file/rename helper and advance normalized acquisition/fingerprint baselines only after the required writes succeed. Add injected failures before evaluation, during candidate publication, and during report publication; after each, assert byte-for-byte active equality, unchanged fingerprint, no activation callback, and retry from the prior acquisition baseline. Preserve readiness fail-closed behavior. Do not implement Phase 14 version pointer, activation, or rollback semantics.</action>
  <acceptance_criteria>
    <criterion>Quarantine produces a deterministic portable corrective report and no active-state mutation or activation callback.</criterion>
    <criterion>Every publication failure preserves active, fingerprint, and acquisition baselines and remains retryable.</criterion>
    <criterion>Eligible output remains inactive and startup cannot advertise a healthy baseline after failed reconciliation.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/router.registry-watcher.test.mjs tests/router.registry-reconcile.test.mjs tests/router.registry-build.test.mjs</automated>
  </verify>
  <done>SAF-10 quarantine is wired into the installed controller with portable correction guidance and proven byte-for-byte last-known-good preservation.</done>
</task>

</tasks>

<threat_model>
**Enforcement:** OWASP ASVS Level 1. Blocking threshold: `high`; every high or critical threat must be mitigated before this plan can complete.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Candidate graph -> safety matrix | Dependencies, permissions, scopes, and identity claims may be absent, contradictory, or attacker-controlled. |
| Reconciliation -> watcher publication | A report disposition controls whether inactive candidate material can be treated as eligible. |
| Watcher -> active registry | Active state is last-known-good authority and must remain read-only throughout Phase 13. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-05 | Spoofing | identity collision and ambiguity gates | high | mitigate | Reject duplicate authoritative claims and all multi-target mappings deterministically. |
| T-13-06 | Elevation of Privilege | scope and permission gates | high | mitigate | Require explicit applicability and declared permission; forbid fallback across scope/runtime. |
| T-13-07 | Tampering | candidate/report publication | high | mitigate | Atomic writes and paired baseline advancement only after required publications succeed. |
| T-13-08 | Repudiation | corrective verdict report | medium | mitigate | Stable codes, subjects, evidence, reasons, and corrective actions in canonical sorted bytes. |
| T-13-09 | Denial of Service | malformed candidate graph | medium | mitigate | Bounded deterministic validation, fail-closed disposition, and retry from last successful baselines. |
</threat_model>

<prohibitions>
- Do not write active registry bytes, invoke activation, advance a version pointer, or implement rollback.
- Do not make watcher hints, weak lifecycle continuity, ambient permission, or fallback scope into authority.
- Do not suppress a blocking verdict to preserve candidate availability.
- Do not introduce third-party dependencies or host-absolute evidence in canonical outputs.
</prohibitions>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Unsafe capabilities never become dispatchable or displace last-known-good | 13-02 | COVERED | Whole-candidate matrix plus immutable publication boundary.
REQ | SAF-09 | Dependencies, permissions, scope, collisions, ambiguity | 13-02 | COVERED | Table-driven deterministic gate coverage.
REQ | SAF-10 | Quarantine without replacing last-known-good | 13-02 | COVERED | Active bytes/fingerprint and publication-baseline assertions.
RESEARCH | — | Whole-candidate safety matrix and last-known-good invariant | 13-02 | COVERED | Pure gates feed installed transactional publisher.
RESEARCH | — | Full/incremental output equivalence and portable diagnostics | 13-02 | COVERED | Paired fixtures and canonical serialization.
CONTEXT | D-01 through D-04 | Alias/target guarantees remain intact | 13-01, 13-02 | COVERED | Added gates compose with, never weaken, locked alias semantics.
BOUNDARY | Phase 14 | Activation, immutable version pointers, rollback | — | EXCLUDED | Explicitly out of Phase 13 scope.
</source_audit>

<verification>
After each task run its focused command. At Wave 2 completion run `node --test tests/router.registry-schema.test.mjs tests/router.registry-diff.test.mjs tests/router.registry-build.test.mjs tests/router.registry-reconcile.test.mjs tests/router.hook-reconcile.test.mjs tests/router.registry-watcher.test.mjs tests/router.adapters.test.mjs tests/router.route-targets.test.mjs`. The full phase gate remains `node --test tests/*.test.mjs` after Wave 3.
</verification>

<success_criteria>
- The full dependency/permission/scope/collision/ambiguity matrix is structured and fail closed.
- Equivalent full and incremental state yields byte-identical reconciliation reports.
- Quarantine publishes portable correction guidance without changing active bytes/fingerprint or activating anything.
- Failed publication preserves every last-known-good baseline and remains retryable.
</success_criteria>

<output>
Create `.planning/phases/13-target-safety-hook-reconciliation-and-quarantine/13-02-SUMMARY.md` when done.
</output>

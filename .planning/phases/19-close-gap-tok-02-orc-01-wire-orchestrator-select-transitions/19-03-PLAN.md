---
phase: 19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions
plan: 03
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - src/lifecycle/router-lifecycle.mjs
  - src/context/prompt-route.mjs
autonomous: true
requirements:
  - ORC-01
  - TOK-02
must_haves:
  truths:
    - "The three orchestrator modules (select.mjs, transitions.mjs, budget.mjs) and the new workflow-declarations.json are added to the moduleNames bundle array so the deployed controller can import them at publish time (per D-07)."
    - "The hook import graph is unchanged — the hook imports only context/prompt-route.mjs, which adds NO new imports from src/orchestrator/* (per D-08)."
    - "The route path reads baked closure/budget/summaryIndex siblings via the additive loadCompiledIndex return keys and projects them per-workflow_id mirroring the existing routes?.[workflowId] projection — it never calls an orchestrator function (per D-01, D-02)."
    - "The route path observes the baked dispatch_eligible flag from the budget sibling and synthesizes the existing blocked resolution when dispatch_eligible is false (required-overflow baked at publish per D-03, closes TOK-02 on the hot path)."
    - "Sibling reads are lazy — only when a dispatch-eligible projection exists — so blocked routes do not pay the closure/budget read cost (per Pitfall #5, preserves REL-01 p95 <25ms)."
  artifacts:
    - "src/lifecycle/router-lifecycle.mjs (modified: moduleNames array +4 entries — 3 orchestrator .mjs + workflow-declarations.json)"
    - "src/context/prompt-route.mjs (modified: read-only projection of compiledIndex.closure/budget/summaryIndex, dispatch_eligible gate, no new imports)"
  key_links:
    - "router-lifecycle.mjs moduleNames contains 'orchestrator/select.mjs', 'orchestrator/transitions.mjs', 'orchestrator/budget.mjs', 'orchestrator/workflow-declarations.json' — moduleValues deploys each into both claude and codex runtime modules/ dirs"
    - "prompt-route.mjs reads compiledIndex.closure?.[workflowId], compiledIndex.budget?.[workflowId], compiledIndex.summaryIndex — all additive keys from the Plan 02 loadCompiledIndex extension"
    - "prompt-route.mjs imports NO new modules (D-08); the existing import of ../prompt/compile-index.mjs suffices because the new keys ride on the existing loadCompiledIndex return"
---

<objective>
Extend the deployed bundle manifest with the four new module entries (three orchestrator .mjs files per D-07 plus the workflow-declarations.json static source from Plan 01) and extend the route path as a read-only projection consumer of the baked sibling tuple files. This plan is the read-side complement to Plan 02's write-side: the bundle ships the orchestrator files the controller needs, and the route path reads what the controller baked — without itself calling any orchestrator function.

Purpose: D-07 requires the orchestrator modules in the deployed bundle so the bundled controller can import them at publish time (Plan 02's publish-index.mjs is deployed via the existing 'prompt/publish-index.mjs' moduleNames entry; it now imports orchestrator/*, so those files must ship too). D-08 requires the hook import graph unchanged — the hook imports only prompt-route.mjs, which stays read-only. This plan satisfies both.

Output: Modified `src/lifecycle/router-lifecycle.mjs` (moduleNames +4) and `src/context/prompt-route.mjs` (read-only sibling projection + dispatch_eligible gate). No new files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-CONTEXT.md
@.planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-RESEARCH.md
@.planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-PATTERNS.md
@.planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-ORCHESTRATOR-INPUT-DECISION.md
@.planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-VALIDATION.md
@src/lifecycle/router-lifecycle.mjs
@src/context/prompt-route.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: router-lifecycle.mjs moduleNames +4 entries (D-07 + workflow-declarations.json)</name>
  <files>src/lifecycle/router-lifecycle.mjs</files>
  <read_first>
    - .planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-ORCHESTRATOR-INPUT-DECISION.md (LOCKED: item #1 — workflow-declarations.json deployed via moduleNames extension)
    - .planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-PATTERNS.md (router-lifecycle.mjs analog :212-238: moduleNames array :308-317, moduleValues deployment :318-320)
    - .planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-CONTEXT.md (D-07: add orchestrator/select.mjs, transitions.mjs, budget.mjs to moduleNames)
    - src/lifecycle/router-lifecycle.mjs (current :305-325 — focus moduleNames array :308-317 and moduleValues :318-320)
  </read_first>
  <behavior>
    - Test: `moduleNames` array contains the literal strings `'orchestrator/select.mjs'`, `'orchestrator/transitions.mjs'`, `'orchestrator/budget.mjs'`, and `'orchestrator/workflow-declarations.json'`
    - Test: after `installRouter(options)` completes, the files `<ownedRoot>/modules/orchestrator/select.mjs`, `<ownedRoot>/modules/orchestrator/transitions.mjs`, `<ownedRoot>/modules/orchestrator/budget.mjs`, and `<ownedRoot>/modules/orchestrator/workflow-declarations.json` exist and their bytes equal the source `src/orchestrator/*` bytes
    - Test: the same four files are also deployed into `<codexOwnedRoot>/modules/orchestrator/` (moduleValues deploys into both runtime roots)
    - Test: the existing moduleNames entries are unchanged — no entry removed, no order changed beyond appending the four new entries
  </behavior>
  <action>
    Edit `src/lifecycle/router-lifecycle.mjs` at the `moduleNames` array (:308-317). Append four new entries to the existing array (do NOT reorder existing entries):

    After the existing `'context/prompt-route.mjs', 'prompt/compile-index.mjs', 'prompt/publish-index.mjs',` line, add:
    - `'orchestrator/select.mjs',`
    - `'orchestrator/transitions.mjs',`
    - `'orchestrator/budget.mjs',`
    - `'orchestrator/workflow-declarations.json',`

    The existing `moduleValues` deployment loop at :318-320 (`[p.ownedRoot, p.codexOwnedRoot].flatMap(runtimeRoot => moduleNames.map(name => [join(runtimeRoot, 'modules', name), readFileSync(join(sourceRoot, name))]))`) handles all four new entries automatically — no change to moduleValues is needed. The `readFileSync(join(sourceRoot, name))` resolves to `src/orchestrator/<name>` for each new entry; the destination is `<runtimeRoot>/modules/orchestrator/<name>`.

    Do NOT modify the `controllerConfig` object, the install sequence, the `stableStringify` import, or any other part of router-lifecycle.mjs. Only the moduleNames array literal is extended.

    Note on D-08: this edit does NOT touch the hook import graph. The hook (~/.claude/hooks/router.mjs) imports context/prompt-route.mjs; router-lifecycle.mjs is controller-side and deployed into ownedRoot, not the hook. Adding orchestrator entries to the bundle does not change what the hook imports.
  </action>
  <verify>
    <automated>node --test tests/router.test-mode-seam.test.mjs 2>&1 | tail -15; grep -c "orchestrator/select.mjs" src/lifecycle/router-lifecycle.mjs | grep -q '^1$'; grep -c "orchestrator/workflow-declarations.json" src/lifecycle/router-lifecycle.mjs | grep -q '^1$'; grep -c "'orchestrator/budget.mjs'" src/lifecycle/router-lifecycle.mjs | grep -q '^1$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'orchestrator/select.mjs'" src/lifecycle/router-lifecycle.mjs` returns 1
    - `grep -c "'orchestrator/transitions.mjs'" src/lifecycle/router-lifecycle.mjs` returns 1
    - `grep -c "'orchestrator/budget.mjs'" src/lifecycle/router-lifecycle.mjs` returns 1
    - `grep -c "'orchestrator/workflow-declarations.json'" src/lifecycle/router-lifecycle.mjs` returns 1
    - `git diff src/lifecycle/router-lifecycle.mjs` shows ONLY the four new lines added to the moduleNames array literal — no other lines changed
    - `node --test tests/router.test-mode-seam.test.mjs` exits 0 (existing test still passes; the four new moduleNames entries do not break installRouter)
    - After running the test seam, the four deployed files exist under `<ownedRoot>/modules/orchestrator/` and `<codexOwnedRoot>/modules/orchestrator/` (the test_mode-seam test already asserts deployed-bytes-equal-source-bytes for existing modules — Plan 04 D-09 extends it to cover the four new entries)
  </acceptance_criteria>
  <done>
    moduleNames contains all four new entries; moduleValues deploys each into both runtime modules/orchestrator/ dirs; existing installRouter behavior is unchanged; the existing test-mode-seam test still passes. Plan 04 adds the bundle-presence D-09 assertions for the four new files.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: prompt-route.mjs read-only sibling projection (D-01/D-02) + dispatch_eligible gate (D-03) + no new imports (D-08)</name>
  <files>src/context/prompt-route.mjs</files>
  <read_first>
    - .planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-ORCHESTRATOR-INPUT-DECISION.md (LOCKED: item #9 loadCompiledIndex return keys — closure, budget, summaryIndex flat additive keys; item #10 dispatch_eligible flag baked in budget sibling)
    - .planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-PATTERNS.md (prompt-route.mjs analog :162-208: imports :1-4 UNCHANGED, routeContextPrompt :84-128, loadCompiledIndex call :91, projection :104, compiled return :120-125, blocked-resolution pattern :105-110)
    - .planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-RESEARCH.md (Anti-Patterns: route-time orchestrator calls forbidden; Pitfall #5 route-path read surface bloat — lazy sibling reads)
    - .planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-CONTEXT.md (D-08: hook import graph unchanged; D-01/D-02: route path is read-only projection)
    - src/context/prompt-route.mjs (current :84-128 — focus :91 loadCompiledIndex call, :104 projection, :105-110 blocked pattern, :120-125 compiled return)
  </read_first>
  <behavior>
    - Test: `routeContextPrompt` with a published tuple whose `budget.by_workflow[workflowId].dispatch_eligible === false` returns a `blocked` resolution with `reason_code` drawn from the baked budget sibling (NOT a generic `compiled_workflow_missing`)
    - Test: `routeContextPrompt` with a published tuple whose `budget.by_workflow[workflowId].dispatch_eligible === true` includes `closure`, `budget`, `summaryIndex` keys in the `compiled` return field (alongside the existing `version_id`, `source`, `tuple_version_id`, `registry_version_id`, `workflow_id`, `transition_id`, `reason_code`)
    - Test: `routeContextPrompt` does NOT import from `src/orchestrator/*` — `grep -c "from '../orchestrator" src/context/prompt-route.mjs` returns 0 (D-08 preserved)
    - Test: blocked routes (no projection) do NOT read siblings — verify via instrumentation or by asserting the compiled return field is absent when `projection` is falsy (the existing `projection ?` gate at :120 already enforces this; extend it to cover siblings)
    - Test: a tuple without siblings (e.g. a legacy schema-1 tuple that somehow survived — should not happen post-Plan-02, but defensive) does NOT crash prompt-route.mjs — `compiledIndex.closure?.[workflowId]` returns undefined, the projection falls back to the existing behavior, no throw
  </behavior>
  <action>
    Edit `src/context/prompt-route.mjs` per the locked contract. Concrete changes:

    1. **NO new imports.** The existing `import { loadCompiledIndex } from '../prompt/compile-index.mjs';` (line 4) suffices — the new `closure`, `budget`, `summaryIndex` keys ride on the existing loadCompiledIndex return (Plan 02 added them as additive flat keys). Do NOT add any `import ... from '../orchestrator/*'` — that would violate D-08 and pull the orchestrator into the hook import graph.

    2. **Extend the dispatch_eligible gate** at :105-110. After the existing `if (resolution.dispatch_eligible && !projection)` block, add a parallel gate that observes the baked budget flag:
       Read `const bakedBudget = compiledIndex.budget?.by_workflow?.[workflowId];` (defensive — `compiledIndex.budget` may be undefined for a legacy tuple; optional-chain handles it).
       If `resolution.dispatch_eligible && projection && bakedBudget && bakedBudget.dispatch_eligible === false`, synthesize a blocked resolution mirroring the existing :105-110 pattern: `{ outcome: 'blocked', dispatch_eligible: false, reason_code: bakedBudget.reason_code || 'required_context_overflow', diagnostic: 'Baked budget declared this workflow non-dispatchable at publish time.' }` and return `{ handled: true, resolution: blockedResolution, additional_context: injection(blockedResolution) }`. This closes TOK-02 on the hot path — required-overflow baked at publish (Plan 02) is observed here.
       Place this gate AFTER the existing `compiled_workflow_missing` gate and BEFORE the save/refresh block so a non-dispatchable baked budget short-circuits before any capsule mutation.

    3. **Extend the `compiled` return field** at :120-125 to include the three sibling projections, behind the existing `projection ?` gate (so blocked/no-projection paths do NOT read siblings — Pitfall #5):
       Inside the existing `...(projection ? { compiled: { ...existing keys..., } } : {})`, add three keys to the compiled object:
       - `closure: compiledIndex.closure?.by_workflow?.[workflowId] ?? null,`
       - `budget: compiledIndex.budget?.by_workflow?.[workflowId] ?? null,`
       - `summaryIndex: compiledIndex.summaryIndex?.by_workflow?.[workflowId] ?? null,`
       The `?? null` default handles a legacy tuple missing siblings without throwing. The `projection ?` gate ensures siblings are only read when a dispatch-eligible route exists (lazy read — REL-01 preserved).

    4. Do NOT modify: the `parseInstruction`, `loadCapsule`, `saveCapsule`, `resolveContextAction`, `injection`, `overrideCapsule`, `refreshedCapsule`, `authoritativeEvidence`, `collectAuthoritativeSnapshot`, or any other helper. Only the `routeContextPrompt` body at :84-128 is extended, and only in the two places above.

    Run `node --test tests/router.context-prompt-integration.test.mjs` (existing route-path integration test must still pass; if it references schema-1 fixtures, it will fail until Plan 04 updates fixtures — that is expected). Create `tests/router.prompt-route.baked-sibling.test.mjs` if no test covers the new sibling-projection behavior (Wave 0 gap). The test asserts: (a) dispatch_eligible:false baked budget → blocked resolution; (b) dispatch_eligible:true baked budget → compiled return includes closure/budget/summaryIndex; (c) no `from '../orchestrator'` import in the source file (static-invariant test mirroring `tests/router.test-mode-seam.test.mjs:103-110`).
  </action>
  <verify>
    <automated>node --test tests/router.prompt-route.baked-sibling.test.mjs 2>&1 | tail -15; grep -c "from '../orchestrator" src/context/prompt-route.mjs | grep -q '^0$'; grep -c "compiledIndex.closure" src/context/prompt-route.mjs | grep -qv '^0$'; grep -c "compiledIndex.budget" src/context/prompt-route.mjs | grep -qv '^0$'; grep -c "bakedBudget.dispatch_eligible === false" src/context/prompt-route.mjs | grep -qv '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from '../orchestrator" src/context/prompt-route.mjs` returns 0 (D-08 preserved — no orchestrator import added)
    - `grep -c "compiledIndex.closure" src/context/prompt-route.mjs` returns ≥1 (read-only sibling projection per D-01/D-02)
    - `grep -c "compiledIndex.budget" src/context/prompt-route.mjs` returns ≥1
    - `grep -c "compiledIndex.summaryIndex" src/context/prompt-route.mjs` returns ≥1
    - `grep -c "bakedBudget.dispatch_eligible === false\|bakedBudget?.dispatch_eligible === false" src/context/prompt-route.mjs` returns ≥1 (D-03 dispatch_eligible gate)
    - `grep -c "?? null" src/context/prompt-route.mjs` returns ≥3 in the compiled return extension (defensive defaults for legacy tuples)
    - The `loadCompiledIndex` call at :91 is byte-identical to the pre-edit version (no new args, no new fs override)
    - `node --test tests/router.prompt-route.baked-sibling.test.mjs` exits 0
    - `git diff src/context/prompt-route.mjs` shows changes ONLY in the `routeContextPrompt` body — no import line added, no helper modified
  </acceptance_criteria>
  <done>
    prompt-route.mjs reads baked closure/budget/summaryIndex siblings via the additive loadCompiledIndex return keys, observes the baked dispatch_eligible flag to synthesize a blocked resolution for required-overflow workflows (TOK-02 hot-path closure), adds NO new imports (D-08 preserved), and reads siblings lazily only when a dispatch-eligible projection exists (REL-01 preserved). New sibling-projection tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bundle-source→deployed-modules | installRouter deploys orchestrator .mjs + workflow-declarations.json bytes from sourceRoot into ownedRoot/modules/ and codexOwnedRoot/modules/ |
| disk→route-path | Route path reads baked siblings via loadCompiledIndex; untrusted disk bytes cross into the hot path (already mitigated by Plan 02 verifyTuple hash checks) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-02 | Tampering | deployed modules/orchestrator/* — files swapped after install | medium | mitigate | moduleValues deploys via readFileSync(sourceRoot) at install time; the controller imports from the deployed path. A post-install swap is outside the install trust boundary and would require OS-level access. The existing installRouter integrity path covers install-time integrity. D-09 E2E (Plan 04) asserts deployed-bytes-equal-source-bytes for the four new files. |
| T-19-05 | Tampering | route path reads a sibling missing from a legacy schema-1 tuple | low | mitigate | Defensive `?? null` defaults in prompt-route.mjs Task 2 step 3 ensure a missing sibling does not crash the route path — the projection falls back to the existing behavior (no closure/budget in the compiled return). The Plan 02 schema bump invalidates schema-1 tuples so this is a defense-in-depth, not a live threat. |
| T-19-06 | Information disclosure | sibling projection leaks into additional_context on a blocked route | low | mitigate | Sibling reads are gated by `projection ?` (Task 2 step 3) — blocked routes with no projection do NOT read siblings, so no sibling data crosses into additional_context on a blocked path. The existing injection() pattern is unchanged. |
| T-19-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs (stdlib-only). N/A — satisfied trivially. |
</threat_model>

<verification>
- `node --test tests/router.test-mode-seam.test.mjs tests/router.prompt-route.baked-sibling.test.mjs` passes.
- `grep -c "'orchestrator/select.mjs'" src/lifecycle/router-lifecycle.mjs` returns 1 (D-07).
- `grep -c "from '../orchestrator" src/context/prompt-route.mjs` returns 0 (D-08 preserved).
- `git diff src/lifecycle/router-lifecycle.mjs` shows only the four new moduleNames lines.
- `git diff src/context/prompt-route.mjs` shows changes only inside the routeContextPrompt body — no import added.
</verification>

<success_criteria>
- D-07 satisfied: the deployed bundle ships orchestrator/select.mjs, transitions.mjs, budget.mjs, and workflow-declarations.json.
- D-08 preserved: the hook import graph is unchanged — prompt-route.mjs imports no new modules.
- D-01/D-02 satisfied on the read side: the route path is a read-only projection of baked siblings, never calls an orchestrator function.
- D-03 satisfied on the hot path: the baked dispatch_eligible flag from the budget sibling synthesizes the blocked resolution for required-overflow workflows (TOK-02 closure).
- Pitfall #5 mitigated: sibling reads are lazy (gated by `projection ?`), preserving REL-01 p95 <25ms.
</success_criteria>

<output>
Create `.planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-03-SUMMARY.md` when done.

## Artifacts this phase produces

**New exported symbols:** none.

**New files created:**
- `tests/router.prompt-route.baked-sibling.test.mjs` — new-behavior test for read-only sibling projection + dispatch_eligible gate + D-08 static-invariant (no orchestrator import) — Wave 0 scaffold created by Task 2

**Modified files:**
- `src/lifecycle/router-lifecycle.mjs` — moduleNames array +4 entries (`'orchestrator/select.mjs'`, `'orchestrator/transitions.mjs'`, `'orchestrator/budget.mjs'`, `'orchestrator/workflow-declarations.json'`); moduleValues unchanged (existing loop deploys all four automatically)
- `src/context/prompt-route.mjs` — `routeContextPrompt` body extended: dispatch_eligible gate observing `compiledIndex.budget?.by_workflow?.[workflowId].dispatch_eligible === false` → blocked resolution; `compiled` return field extended with `closure`, `budget`, `summaryIndex` keys (each `compiledIndex.<sibling>?.by_workflow?.[workflowId] ?? null`) behind the existing `projection ?` gate. NO new imports.

**New moduleNames entries:** the four listed above (consumed by moduleValues deployment into both runtime modules/orchestrator/ dirs)

**New route-path read keys (consuming Plan 02's additive loadCompiledIndex return):** `compiledIndex.closure?.by_workflow?.[workflowId]`, `compiledIndex.budget?.by_workflow?.[workflowId]`, `compiledIndex.summaryIndex?.by_workflow?.[workflowId]`

**New dispatch_eligible gate in routeContextPrompt:** observes the baked budget flag (D-03 hot-path closure)

The newly-created symbols that downstream plan-review-convergence source-grounding must exclude from drift verification: the four new moduleNames entries, the three new `compiledIndex.<sibling>?.by_workflow?.[workflowId]` read expressions in prompt-route.mjs, the `bakedBudget` dispatch_eligible gate, the new `tests/router.prompt-route.baked-sibling.test.mjs` file.
</output>
---
phase: 60-runtime-truth-and-capability-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/coverage/audit.mjs
  - src/registry/build.mjs
  - src/registry/reconcile.mjs
  - tests/router.coverage-audit.test.mjs
  - tests/router.coverage.test.mjs
  - tests/router.registry-reconcile.test.mjs
autonomous: true
requirements: [CAP-01, CAP-02]
must_haves:
  truths:
    - "Every discovered capability is represented by one deterministic runtime-aware coverage row with category, identity, availability, provenance, and a safe selection disposition."
    - "Unavailable, stale, invalid, project-scoped, hook-owned, excluded, and missing-MCP records remain inspectable with reason codes but cannot become dispatchable route targets."
    - "Claude and Codex records with equivalent display names retain their native runtime identity and locator rather than being merged by name alone."
    - "Coverage distinguishes intentional exclusions and non-routeable diagnostics from actionable unmapped capability gaps."
  prohibitions:
    - "Coverage auditing MUST NOT scan roots or add work to src/runtime/router.mjs prompt handling."
    - "The implementation MUST NOT invent a route for an unavailable, stale, invalid, excluded, project-scoped, hook-owned, or missing-MCP record."
    - "The phase MUST NOT add a second registry, router, network classifier, database, daemon, embeddings store, or automatic external installation."
  artifacts:
    - src/coverage/audit.mjs
    - src/registry/build.mjs
    - src/registry/reconcile.mjs
    - tests/router.coverage.test.mjs
    - tests/router.registry-reconcile.test.mjs
  key_links:
    - "Runtime adapter observations -> canonical capability identity/provenance -> eligibility -> coverage disposition"
    - "Coverage disposition -> route target audit -> reconciliation safety verdict"
---

<objective>
Make the existing off-hot-path registry and coverage audit the single truthful source for what the user's local Claude/Codex capability stack can and cannot safely provide.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/60-runtime-truth-and-capability-coverage/60-RESEARCH.md
@.planning/phases/60-runtime-truth-and-capability-coverage/60-PATTERNS.md
@.planning/phases/60-runtime-truth-and-capability-coverage/60-VALIDATION.md
@src/registry/build.mjs
@src/coverage/audit.mjs
@src/registry/eligibility.mjs
@src/registry/reconcile.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Align coverage rows with canonical runtime capability truth</name>
  <files>src/coverage/audit.mjs, src/registry/build.mjs</files>
  <read_first>
    - src/coverage/audit.mjs (current manifest categories, target indexes, typedMappings, and baseline policy)
    - src/registry/build.mjs (assembleRegistry, classifyCoverage, full/incremental acquisition, and runtime provenance)
    - src/registry/schema.mjs (canonical record shape and coverage fields)
    - src/registry/eligibility.mjs (eligibility gates and quarantine reason codes)
    - src/adapters/claude.mjs (Claude observation categories and native locator shape)
    - src/adapters/codex.mjs (Codex observation categories and native locator shape)
    - tests/router.coverage-audit.test.mjs (direct auditCoverage export seam and manifest fixtures)
    - tests/router.coverage.test.mjs (existing public coverage behavior)
  </read_first>
  <behavior>
    - Test 1: auditCoverage preserves deterministic runtime-local identity and provenance for skills, agents, commands, plugins, tools, hooks, integrations/MCP, and runtime-local source records.
    - Test 2: unavailable, stale, invalid, project-scoped, hook-owned, excluded, and missing-MCP records remain inspectable but are not actionable gaps or dispatchable targets.
    - Test 3: equivalent Claude/Codex display names remain separate by runtime and native locator.
  </behavior>
  <action>
    Extend the existing `auditCoverage` export and its helper indexes so every CAP-01 collection emitted by the Claude and Codex adapters—skills, agents, commands, plugins, tools, hooks, integrations/MCP, plus runtime-local provenance—is represented by a deterministic row with `runtime`, stable category/id identity, `scope`, availability, `dispatchable`, provenance/source evidence, classification, and sorted `reason_codes`. Use `tests/router.coverage-audit.test.mjs` as the direct source seam and enumerate each collection in its fixtures; preserve existing legacy fields and classifications for compatibility. Routeable categories must be joined to canonical eligibility/target existence when registry evidence is available; rows for hooks, MCP/integration diagnostics, project-scoped records, explicit exclusions, invalid metadata, unavailable records, and missing-MCP agents must remain visible but must not be counted as actionable route gaps. Update `classifyCoverage`/assembly only where needed to ensure the same eligibility reason codes and coverage classification are emitted by full and incremental builds. Keep all audit work outside `src/runtime/router.mjs`.
  </action>
  <acceptance_criteria>
    - `auditCoverage` returns deterministic rows for every discovered CAP-01 collection—skills, agents, commands, plugins, tools, hooks, integrations/MCP—and includes runtime-local provenance and safe-selection disposition fields.
    - The direct `tests/router.coverage-audit.test.mjs` fixtures exercise all CAP-01 collections and prove Claude/Codex records with equivalent names retain distinct runtime/provenance/native-locator identity.
    - A record with `dispatchable: false`, stale/unavailable invocation evidence, missing MCP, project scope, hook ownership, explicit exclusion, or invalid lifecycle is never classified as an actionable routeable gap.
    - Equivalent names from Claude and Codex remain distinguishable by runtime/provenance/native locator in the audit output.
    - Existing `mappedTargets` and legacy coverage consumers continue to receive their current fields and classifications.
    - Full and incremental `assembleRegistry` results preserve the same coverage and eligibility semantics for the same source evidence.
  </acceptance_criteria>
  <verify>
    <automated>rtk node --test tests/router.coverage-audit.test.mjs tests/router.coverage.test.mjs tests/router.registry-schema.test.mjs tests/router.registry-reconcile.test.mjs</automated>
  </verify>
  <done>The off-hot-path audit and canonical registry agree on capability identity, availability, eligibility, and coverage disposition.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce stale-target exclusion at reconciliation boundaries</name>
  <files>src/registry/reconcile.mjs, tests/router.registry-reconcile.test.mjs</files>
  <read_first>
    - src/registry/reconcile.mjs (reconcileCandidate, alias validation, and target_not_dispatchable verdicts)
    - src/registry/eligibility.mjs (stale_unavailable and quarantine behavior)
    - src/registry/map.mjs (safe route target resolution)
    - src/registry/contract.mjs (freshness and evidence envelopes)
    - tests/router.registry-reconcile.test.mjs (candidate, alias, and quarantine fixtures)
    - tests/router.route-targets.test.mjs (route target safety assertions)
  </read_first>
  <action>
    Make the reconciliation result expose one deterministic, actionable reason for every coverage row that cannot be selected, and ensure aliases, explicit mappings, and route targets consult the same canonical `dispatchable`/eligibility truth. Preserve advisory verdicts as visible diagnostics, but keep any target with stale dispatch evidence, missing native invocation, failed dependency closure, quarantine reasons, or unavailable runtime state out of eligible mappings. Add no new authority path and do not promote recommendation-only records.
  </action>
  <acceptance_criteria>
    - Reconciliation returns `disposition: quarantined` or an equivalent blocking verdict for stale, missing, non-ready, non-invocable, or quarantined targets.
    - An alias pointing at a non-dispatchable target receives a stable `alias_target_not_dispatchable` or `alias_target_missing` reason and cannot be published as a safe route.
    - A target with only advisory hygiene findings remains inspectable without changing its eligibility or dispatchability.
    - The same candidate produces byte-stable verdicts and reason ordering across repeated reconciliation runs.
  </acceptance_criteria>
  <verify>
    <automated>rtk node --test tests/router.registry-reconcile.test.mjs tests/router.route-targets.test.mjs tests/router.trust-quarantine.test.mjs</automated>
  </verify>
  <done>No stale or unavailable capability can cross the reconciliation boundary into selectable routing.</done>
</task>

</tasks>

<threat_model>
## Phase 60 Plan 01 Threat Model — ASVS Level 1

| Threat ID | Severity | Threat | Mitigation | Automated proof |
|---|---|---|---|---|
| T-60-01 | high | A stale or unavailable local target is selected after inventory drift | Join coverage with canonical eligibility and fail closed at mapping/reconciliation | Stale-target and alias tests |
| T-60-02 | high | Claude/Codex records are merged by display name and invoke the wrong native locator | Preserve stable runtime/provenance identity through audit and registry assembly | Asymmetric runtime fixture |
| T-60-03 | medium | Coverage repair adds prompt-time filesystem work | Keep audit and reconciliation in build/watcher paths and assert router source has no root scan | Hot-path regression suite |
</threat_model>

<verification>
- `rtk node --test tests/router.coverage-audit.test.mjs tests/router.coverage.test.mjs tests/router.registry-schema.test.mjs tests/router.registry-reconcile.test.mjs tests/router.route-targets.test.mjs tests/router.trust-quarantine.test.mjs tests/router.registry-incremental.test.mjs tests/router.registry-diff.test.mjs tests/router.watcher-telemetry-ingest.test.mjs tests/router.v19-observability.test.mjs` exits 0, matching the full-run command in `60-VALIDATION.md`.
- `rtk git diff --check` exits 0.
- A full/incremental fixture comparison reports identical canonical coverage, eligibility, and reconciliation semantics.
- The executable production verification path asserts the required gate IDs `privacy`, `latency`, `token_budget`, `reconciliation_safety`, `regression_suite`, and `incremental_full_equivalence`; the latency/hot-path checks also run the existing `tests/router.perf-evolved.test.mjs`, `tests/phase-38/budget.test.mjs`, and `tests/router.hot-path-resolve.integration.test.mjs` checks.
- The production evidence asserts truthful zero-dispatchable state: zero safe targets produces no fabricated route and records the bounded empty-target disposition.
</verification>

<success_criteria>
- CAP-01 is satisfied by one inspectable, runtime-aware capability truth model.
- CAP-02 is satisfied by actionable gap classification without false gaps or invented targets.
- The prompt hook remains unchanged in behavior and budget.
</success_criteria>

## Artifacts this phase produces

- `src/coverage/audit.mjs`: runtime-aware, explainable coverage rows.
- `src/registry/build.mjs`: aligned full/incremental coverage semantics if required.
- `src/registry/reconcile.mjs`: shared stale-target safety reasons.
- `tests/router.coverage.test.mjs`: coverage taxonomy and asymmetric inventory proof.
- `tests/router.registry-reconcile.test.mjs`: stale, alias, and selection-boundary proof.
</artifacts>

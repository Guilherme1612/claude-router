---
phase: 60-runtime-truth-and-capability-coverage
plan: 02
type: execute
wave: 2
depends_on: ["60-01"]
files_modified:
  - src/registry/watcher.mjs
  - src/registry/validate.mjs
  - tests/router.watcher-telemetry-ingest.test.mjs
  - tests/router.registry-incremental.test.mjs
  - tests/router.registry-watcher.test.mjs
  - tests/router.v19-observability.test.mjs
autonomous: true
requirements: [CAP-01, CAP-02]
must_haves:
  truths:
    - "Background startup and incremental publication records the inventory epoch, runtime roots, coverage summary, stale roots, and reconciliation disposition without persisting raw prompts or capability bodies."
    - "An incomplete or stale runtime root cannot leave an old dispatchable route active while the current capability truth is unknown."
    - "Full rebuild, incremental dirty-root rebuild, removal, and rename/move flows converge to the same canonical capability and coverage result."
    - "The installed/runtime evidence reports zero dispatchable capabilities truthfully when no safe target exists instead of fabricating or promoting a route."
  prohibitions:
    - "Watcher publication MUST NOT place root scanning, hashing, or reconciliation on the prompt hook path."
    - "Observability artifacts MUST NOT contain raw prompts, session bodies, capability source bodies, telemetry bodies, or unbounded logs."
    - "Fallback behavior MUST NOT silently retain a stale dispatchable route when current root truth is incomplete."
  artifacts:
    - src/registry/watcher.mjs
    - src/registry/validate.mjs
    - tests/router.watcher-telemetry-ingest.test.mjs
    - tests/router.registry-incremental.test.mjs
    - tests/router.v19-observability.test.mjs
  key_links:
    - "Watcher dirty roots -> incremental acquisition -> full/incremental semantic equivalence gate"
    - "Coverage/reconciliation evidence -> bounded runtime publication -> truthful active authority"
---

<objective>
Publish the reconciled capability truth safely and prove that live/incremental watcher behavior cannot preserve or invent an unsafe route when runtime state changes.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/60-runtime-truth-and-capability-coverage/60-RESEARCH.md
@.planning/phases/60-runtime-truth-and-capability-coverage/60-PATTERNS.md
@.planning/phases/60-runtime-truth-and-capability-coverage/60-VALIDATION.md
@src/registry/watcher.mjs
@src/registry/build.mjs
@src/registry/validate.mjs
@src/registry/activate.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Publish bounded coverage and stale-root evidence</name>
  <files>src/registry/watcher.mjs, src/registry/validate.mjs</files>
  <read_first>
    - src/registry/watcher.mjs (startup, dirty-root reconciliation, publication, stale-root handling, and evidence fields)
    - src/registry/build.mjs (registry summary, coverage, tuple, and incremental result shape)
    - src/registry/validate.mjs (required activation gates and privacy/token runners)
    - src/registry/activate.mjs (last-known-good and atomic publication invariants)
    - .planning/evidence/v1.9/live-after.json (current live evidence shape and zero-dispatchable truth)
    - tests/router.registry-watcher.test.mjs (existing active-authority and production-gate runner seams)
  </read_first>
  <behavior>
    - Test 1: an active route plus an incomplete, stale, unreadable, removed, or unknown root transitions the existing active authority to the repository's empty-authority state (`authority_status: empty`, no active tuple/pointer), candidate disposition to quarantined, dispatchable count to zero, and recovery state to authoritative repair.
    - Test 2: complete roots publish bounded runtime/coverage/reconciliation evidence without raw prompt or capability bodies.
    - Test 3: the executable production verifier reports passing `privacy`, `latency`, `token_budget`, `reconciliation_safety`, `regression_suite`, and `incremental_full_equivalence` gate identities only when their existing runners/checks pass.
  </behavior>
  <action>
    Extend the existing background watcher publication record, preserving its schema and bounded-size conventions, so it includes the current inventory epoch/fingerprint, per-runtime observation counts, coverage classification counts, stale or unreadable roots, and reconciliation/activation disposition. Define and implement the exact fail-closed transition for an existing active route: when any root is incomplete, stale, unreadable, removed, or unknown, transition `authority_status: active -> empty` by withdrawing the active tuple/pointer, then set `candidate -> quarantined`, `dispatchable_count -> 0`, `watcher state -> degraded`, and `next_recovery_action -> authoritative-repair`; only a fresh complete authoritative build may transition back to `current/active`. Add the existing-active-route test in `tests/router.registry-watcher.test.mjs`; do not silently reuse an old dispatchable target. Keep all values privacy-safe, bounded, deterministic, and outside `src/runtime/router.mjs`.
  </action>
  <acceptance_criteria>
    - A normal startup publication contains runtime counts, coverage counts, inventory identity, stale-root state, and reconciliation disposition with no raw prompt/capability body fields.
    - With an existing active route, an incomplete, stale, unreadable, removed, or unknown root performs the explicit `authority_status: active -> empty` transition, withdraws the active tuple/pointer, publishes no active dispatchable pointer, and records `candidate -> quarantined`, `dispatchable_count -> 0`, `watcher state -> degraded`, and `next_recovery_action -> authoritative-repair` until authoritative repair succeeds.
    - Empty dispatchable coverage is represented as a valid truthful state with zero count and no fabricated fallback route.
    - Existing activation gates continue to reject privacy, token-budget, reconciliation, or regression failures independently; production verification names and executes `privacy`, `latency`, `token_budget`, `reconciliation_safety`, `regression_suite`, and `incremental_full_equivalence` through `PRODUCTION_GATE_RUNNERS`/`REQUIRED_ACTIVATION_GATES`.
  </acceptance_criteria>
  <verify>
    <automated>rtk node --test tests/router.watcher-telemetry-ingest.test.mjs tests/router.v19-observability.test.mjs tests/router.registry-build.test.mjs tests/router.registry-watcher.test.mjs tests/router.release-preflight.test.mjs</automated>
  </verify>
  <done>Background publication exposes enough bounded evidence to diagnose coverage truth without weakening activation or privacy gates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove full and incremental runtime truth convergence</name>
  <files>tests/router.registry-incremental.test.mjs, tests/router.watcher-telemetry-ingest.test.mjs</files>
  <read_first>
    - src/registry/build.mjs (refreshIncrementalAcquisition and compareable registry output)
    - src/registry/watcher.mjs (dirty-root and removal/rename event handling)
    - src/registry/validate.mjs (incremental_full_equivalence gate)
    - tests/helpers/inventory-fixture.mjs (anonymous runtime inventory variants and CAP-01 collection fixtures)
    - tests/router.registry-diff.test.mjs (lifecycle events and dirty-root fixtures)
    - tests/router.watcher-telemetry-ingest.test.mjs (bounded evidence assertions)
  </read_first>
  <action>
    Add anonymous dual-runtime fixtures for every CAP-01 collection—skills, agents, commands, plugins, tools, hooks, integrations/MCP—with runtime-local provenance, plus asymmetric inventories, duplicate display names with distinct native locators, unavailable/missing-MCP records, project-scoped and excluded records, stale roots, removal, and rename/move events. Assert that full and incremental builds converge on identical canonical records, eligibility, coverage classifications, reason codes, and dispatchable counts; assert that watcher evidence records the changed root and bounded disposition. Keep fixtures independent of private live-home contents and raw prompt strings.
  </action>
  <acceptance_criteria>
    - `rtk node --test tests/router.registry-incremental.test.mjs` proves exact full/incremental semantic convergence for changed Claude and Codex roots.
    - Removal and rename/move fixtures do not leave stale route targets or stale dispatchable counts in the incremental result.
    - Asymmetric runtime fixtures explicitly cover skills, agents, commands, plugins, tools, hooks, and integrations/MCP, preserving runtime-local provenance and native locator identity.
    - Stale-root and empty-dispatchable cases produce deterministic, privacy-safe watcher evidence.
  </acceptance_criteria>
  <verify>
    <automated>rtk node --test tests/router.registry-incremental.test.mjs tests/router.registry-diff.test.mjs tests/router.watcher-telemetry-ingest.test.mjs</automated>
  </verify>
  <done>Runtime mutation and watcher fallback behavior are proven equivalent, bounded, and truthful.</done>
</task>

</tasks>

<threat_model>
## Phase 60 Plan 02 Threat Model — ASVS Level 1

| Threat ID | Severity | Threat | Mitigation | Automated proof |
|---|---|---|---|---|
| T-60-04 | high | A failed watcher refresh preserves an unsafe old route | Block affected dispatchability until fresh authoritative evidence passes | Stale-root/removal fixture |
| T-60-05 | high | Runtime evidence leaks raw prompt or capability content | Publish only bounded counts, fingerprints, classifications, and reason codes | Privacy-negative observability tests |
| T-60-06 | medium | Incremental refresh diverges from full discovery and routes stale capability state | Required exact full/incremental equivalence gate | Dual-runtime mutation suite |
</threat_model>

<verification>
- `rtk node --test tests/router.coverage-audit.test.mjs tests/router.coverage.test.mjs tests/router.registry-schema.test.mjs tests/router.registry-reconcile.test.mjs tests/router.route-targets.test.mjs tests/router.trust-quarantine.test.mjs tests/router.registry-incremental.test.mjs tests/router.registry-diff.test.mjs tests/router.watcher-telemetry-ingest.test.mjs tests/router.v19-observability.test.mjs` exits 0, matching the full-run command in `60-VALIDATION.md`.
- `rtk node --test tests/router.registry-watcher.test.mjs tests/router.production-verifier-e2e.test.mjs tests/router.registry-activate.test.mjs tests/router.release-preflight.test.mjs tests/router.perf-evolved.test.mjs tests/phase-38/budget.test.mjs tests/router.hot-path-resolve.integration.test.mjs` exits 0 and exercises the existing production gate runners/checks; evidence explicitly contains passing `privacy`, `latency`, `token_budget`, `reconciliation_safety`, `regression_suite`, and `incremental_full_equivalence` results.
- The zero-target fixture and `tests/router.release-preflight.test.mjs` assert truthful `dispatchable_count: 0`, absent active tuple/pointer, and no fabricated fallback route.
- `rtk git diff --check` exits 0.
</verification>

<success_criteria>
- CAP-01 is proven through full, incremental, stale-root, and runtime-parity evidence.
- CAP-02 is proven through bounded persisted coverage and actionable gap diagnostics.
- No stale or unavailable route is active merely because a watcher refresh failed.
</success_criteria>

## Artifacts this phase produces

- `src/registry/watcher.mjs`: bounded runtime-aware coverage and stale-root publication.
- `src/registry/validate.mjs`: preserved independent gates for new truth evidence.
- `tests/router.registry-incremental.test.mjs`: full/incremental and mutation convergence proof.
- `tests/router.watcher-telemetry-ingest.test.mjs`: watcher evidence and privacy proof.
- `tests/router.v19-observability.test.mjs`: live evidence contract regression.
</artifacts>

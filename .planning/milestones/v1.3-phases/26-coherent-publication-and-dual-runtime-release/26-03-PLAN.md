---
phase: 26-coherent-publication-and-dual-runtime-release
plan: 03
type: execute
wave: 2
depends_on: [26-02]
files_modified:
  - src/registry/build.mjs
  - src/registry/reconcile.mjs
  - src/registry/watcher.mjs
  - src/prompt/publish-index.mjs
  - tests/router.phase26-invalidation.test.mjs
  - tests/router.phase26-equivalence.test.mjs
  - tests/router.registry-reconcile.test.mjs
autonomous: true
requirements: [REL-04, REL-08]
must_haves:
  truths:
    - "All eight specified change classes invalidate their transitive tuple dependents atomically."
    - "Equivalent full and incremental inputs produce byte-identical members, manifest, fingerprints, and tuple ID."
    - "Any failure before pointer replacement leaves active bytes unchanged."
  artifacts:
    - "src/registry/reconcile.mjs"
    - "tests/router.phase26-invalidation.test.mjs"
    - "tests/router.phase26-equivalence.test.mjs"
  key_links:
    - "Authoritative reconcile descriptions feed complete tuple inputs."
    - "Watcher coalescing and missed-event reconciliation converge on the same canonical build value."
---

<objective>
Make tuple invalidation dependency-complete and prove full/incremental equivalence at the complete publication boundary.

Purpose: Ensure no stale sibling or partial background work can alter active routing.
Output: Canonical invalidation descriptors, complete equivalence checks, and atomic failure evidence.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-coherent-publication-and-dual-runtime-release/26-RESEARCH.md
@.planning/phases/26-coherent-publication-and-dual-runtime-release/26-VALIDATION.md
@.planning/phases/26-coherent-publication-and-dual-runtime-release/26-02-SUMMARY.md
@src/registry/reconcile.mjs
@src/registry/watcher.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend invalidation to every dependency class and tuple sibling</name>
  <files>src/registry/reconcile.mjs, src/registry/build.mjs, src/prompt/publish-index.mjs, tests/router.phase26-invalidation.test.mjs</files>
  <behavior>
    - Node, edge, dependency, adapter, inference-rule, manifest, correction, and negative-evidence changes invalidate exact transitive dependents.
    - Affected members and tuple ID change together while unrelated canonical bytes stay stable.
    - Readers see only the old complete tuple or the new complete tuple.
  </behavior>
  <action>Extend the existing reconcile diff/dependency closure with deterministic descriptors for the missing change classes. Reuse canonical ordering and fingerprints already used by registry reconciliation. Feed one complete rebuild value to the existing publisher; do not create per-member activation. Keep unaffected bytes stable and test each class through production reconciliation and publication.</action>
  <verify>
    <automated>rtk node --test tests/router.phase26-invalidation.test.mjs</automated>
  </verify>
  <done>The eight-class matrix and transitive old-or-new visibility assertions pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove complete full/incremental byte equivalence and pre-pointer crash safety</name>
  <files>src/registry/reconcile.mjs, src/registry/watcher.mjs, tests/router.phase26-equivalence.test.mjs, tests/router.registry-reconcile.test.mjs</files>
  <behavior>
    - Ordered, reordered, duplicated, coalesced, and missed-then-reconciled events converge byte-identically.
    - Every tuple member, manifest, fingerprint, and tuple ID matches the full build.
    - Failures before pointer replacement preserve the exact active snapshot.
  </behavior>
  <action>Expand `incremental_full_equivalence` from registry semantics to the complete deterministic tuple input/output. Route watcher event shapes through authoritative reconciliation, then compare canonical member bytes and identity. Inject build/member/manifest/verification failures before the existing pointer replacement and assert active bytes do not change.</action>
  <verify>
    <automated>rtk node --test tests/router.phase26-equivalence.test.mjs tests/router.registry-reconcile.test.mjs</automated>
  </verify>
  <done>All event permutations converge byte-identically and every pre-pointer failure preserves the old tuple.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem events -> authoritative reconcile | Events may be lost, duplicated, reordered, or coalesced. |
| reconcile result -> tuple pointer | Incomplete derived state must not become active. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-06 | Tampering | invalidation closure | high | mitigate | Table-driven eight-class transitive dependency tests and canonical fingerprints. |
| T-26-07 | Denial of Service | event delivery | medium | mitigate | Authoritative reconciliation converges after missed/coalesced events. |
| T-26-08 | Elevation of Privilege | partial rebuild | critical | mitigate | Single complete value and pointer-last atomic activation. |
</threat_model>

<verification>
Run the invalidation and equivalence gates serially with the adjacent registry reconcile suite.
</verification>

<success_criteria>
REL-04 and REL-08 pass for complete tuple bytes, not merely registry semantics.
</success_criteria>

<output>
Create `.planning/phases/26-coherent-publication-and-dual-runtime-release/26-03-SUMMARY.md` when done.
</output>

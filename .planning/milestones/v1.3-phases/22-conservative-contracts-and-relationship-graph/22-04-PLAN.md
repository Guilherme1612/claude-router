---
phase: 22-conservative-contracts-and-relationship-graph
plan: 04
type: execute
wave: 3
depends_on: [22-01, 22-02, 22-03]
files_modified:
  - tests/router.contract-eligibility.test.mjs
  - src/registry/eligibility.mjs
  - src/registry/schema.mjs
  - src/registry/build.mjs
autonomous: true
requirements: [CONT-03, CONT-08]
nyquist_compliant: true
must_haves:
  truths:
    - "Dispatch eligibility is derived by one shared fail-closed validator and is never authored by a record or overlay."
    - "Target existence, invocation shape, adapter, dependency closure, permission, scope, side effects, reversibility, risk, and field confidence each have passed, failed, or unknown state."
    - "Any failed or unknown gate yields recommendation-only with stable canonically ordered reasons."
  artifacts:
    - path: "src/registry/eligibility.mjs"
      provides: "Single shared eligibility evaluator and stable gate result"
    - path: "tests/router.contract-eligibility.test.mjs"
      provides: "Complete gate-state and multi-failure oracle"
  key_links:
    - from: "src/registry/build.mjs"
      to: "src/registry/eligibility.mjs"
      via: "canonical assembly derives eligibility after contracts, overlays, and relationships are resolved"
    - from: "src/registry/eligibility.mjs"
      to: "src/registry/schema.mjs"
      via: "schema accepts only derived canonical eligibility shape"
---

<objective>
Centralize conservative dispatch eligibility for CONT-03 and CONT-08.

Purpose: Every safety gate must fail closed consistently, with visible reasons, before Phase 23 can select or invoke anything.
Output: One eligibility module, canonical build/schema wiring, and a complete Wave 0 gate matrix.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-conservative-contracts-and-relationship-graph/22-CONTEXT.md
@.planning/phases/22-conservative-contracts-and-relationship-graph/22-RESEARCH.md
@.planning/phases/22-conservative-contracts-and-relationship-graph/22-PATTERNS.md
@.planning/phases/22-conservative-contracts-and-relationship-graph/22-01-SUMMARY.md
@.planning/phases/22-conservative-contracts-and-relationship-graph/22-02-SUMMARY.md
@.planning/phases/22-conservative-contracts-and-relationship-graph/22-03-SUMMARY.md

<interfaces>
Consume normalized contracts from Plan 01, accepted/rejected overlays from Plan 02, and validated relationships from Plan 03.
Preserve Phase 21 `dispatchable` compatibility, but derive its value from the shared eligibility result during assembly.
Publish a pure evaluator returning `eligible`, `recommendation_only`, gate states, and canonically ordered reason codes.
</interfaces>
</context>

## Artifacts this plan creates/modifies

- `tests/router.contract-eligibility.test.mjs` — passed/failed/unknown matrix for every gate and canonical multi-failure reasons.
- `src/registry/eligibility.mjs` — sole eligibility evaluator.
- `src/registry/build.mjs` — calls evaluator once after authoritative enrichment.
- `src/registry/schema.mjs` — validates derived eligibility output and recommendation-only invariants.

<tasks>

<task type="auto">
  <name>Task 1: Establish the Wave 0 fail-closed gate matrix</name>
  <files>tests/router.contract-eligibility.test.mjs</files>
  <read_first>
    - .planning/phases/22-conservative-contracts-and-relationship-graph/22-CONTEXT.md (dispatch eligibility decisions)
    - .planning/phases/22-conservative-contracts-and-relationship-graph/22-VALIDATION.md (CONT-03/08 matrix)
    - tests/router.contracts.test.mjs (field uncertainty)
    - tests/router.contract-overlays.test.mjs (overlay authority boundary)
    - tests/router.relationships.test.mjs (conflict/dependency graph state)
    - src/registry/schema.mjs (Phase 21 dispatch invariants)
  </read_first>
  <action>Create tests for target existence, invocation shape, adapter support, dependency closure, permission, scope, side effects, reversibility, risk, and dispatch-field confidence. Exercise passed, failed, and unknown for every gate, then multiple simultaneous failures in permuted input order. Require eligibility only when all gates pass; any failure or unknown produces recommendation-only and a stable ordered reason list. Prove authored `dispatchable`, overlay eligibility keys, and precomputed eligibility input cannot bypass evaluation. Cover dependency cycles, missing endpoints, blocking conflict edges, disabled targets, and unknown contract fields.</action>
  <acceptance_criteria>
    - Completeness assertion: every locked gate has passed, failed, and unknown coverage.
    - Centralization assertion: all eligibility decisions are expected from one evaluator contract.
    - Ordering assertion: simultaneous failures produce one canonical reason order regardless of input ordering.
    - RED assertion marker: intentionally failing names contain `[phase22-red:eligibility]` and no unrelated failure is accepted.
  </acceptance_criteria>
  <verify>
    <automated>rtk node -e "const {spawnSync}=require('node:child_process');const r=spawnSync(process.execPath,['--test','tests/router.contract-eligibility.test.mjs'],{encoding:'utf8'});const out=(r.stdout||'')+(r.stderr||'');const failed=[...out.matchAll(/^\s*not ok \d+ - (.+)$/gm)].map(m=&gt;m[1]);process.exit(r.status!==0&amp;&amp;failed.length&gt;0&amp;&amp;failed.every(name=&gt;name.includes('[phase22-red:eligibility]'))?0:1)"</automated>
  </verify>
  <done>The complete shared eligibility contract is specified before production wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement and wire the sole eligibility evaluator</name>
  <files>src/registry/eligibility.mjs, src/registry/build.mjs, src/registry/schema.mjs, tests/router.contract-eligibility.test.mjs</files>
  <read_first>
    - tests/router.contract-eligibility.test.mjs (Task 1 oracle)
    - src/registry/build.mjs (single authoritative assembler)
    - src/registry/schema.mjs (dispatchable and invocation invariants)
    - src/registry/contract.mjs (field envelopes and overlay resolution)
    - src/registry/relationships.mjs (dependency/conflict edge state)
    - .planning/phases/22-conservative-contracts-and-relationship-graph/22-PATTERNS.md (`eligibility.mjs` analog)
  </read_first>
  <behavior>
    - Only an all-passed gate set is dispatch eligible.
    - Failed and unknown gates are distinguishable and always recommendation-only.
    - Authored or overlay eligibility values have no authority.
  </behavior>
  <action>Create a pure `src/registry/eligibility.mjs` evaluator that computes all locked gates and returns a bounded canonical result with explicit gate states, `eligible`, `recommendation_only`, and stable reasons. Evaluate target/invocation/adapter facts from authoritative normalized records; dependency closure and conflict state from validated graph data; permissions/scope/effects/reversibility/risk/confidence from field envelopes. Use iterative bounded closure for dependency cycles. Call the evaluator once in the existing assembler after contracts, overlays, and relationships resolve. Extend schema validation so `dispatchable` agrees with derived eligibility and no authored eligibility input survives normalization. Do not add execution, intent classification, routing preference, health scoring, or alternate validators.</action>
  <acceptance_criteria>
    - Sole-authority assertion: repository eligibility consumers use the shared evaluator result rather than duplicate gate subsets.
    - Fail-closed assertion: every failed or unknown gate returns recommendation-only.
    - Compatibility assertion: known-safe Phase 21 dispatchable fixtures remain eligible; inert/unknown/disabled fixtures remain ineligible.
    - Determinism assertion: reason and gate ordering is stable across all permutations.
  </acceptance_criteria>
  <verify>
    <automated>rtk node --test tests/router.contract-eligibility.test.mjs tests/router.contracts.test.mjs tests/router.contract-overlays.test.mjs tests/router.relationships.test.mjs tests/router.registry-build.test.mjs</automated>
  </verify>
  <done>One canonical validator derives all dispatch eligibility and keeps every uncertain or unsafe capability recommendation-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| enriched registry → eligibility | Contract, overlay, and graph state influence dispatch authority |
| eligibility → future routing | Phase 23 will treat this result as the dispatch safety boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-13 | Elevation of Privilege | eligibility evaluator | high | mitigate | One shared all-gates-pass evaluator; failed/unknown gates force recommendation-only. |
| T-22-14 | Tampering | authored eligibility | high | mitigate | Schema discards/rejects authored eligibility and assembler derives the only accepted result. |
| T-22-15 | Denial of Service | dependency closure | medium | mitigate | Bounded iterative traversal with visited-set cycle handling and adversarial tests. |
| T-22-16 | Repudiation | multi-gate decision | medium | mitigate | Explicit per-gate states and stable canonically ordered reason codes. |
| T-22-SC | Tampering | package installation | high | mitigate | No package installation task exists. |
</threat_model>

<verification>
- `rtk node --test tests/router.contract-eligibility.test.mjs tests/router.contracts.test.mjs tests/router.contract-overlays.test.mjs tests/router.relationships.test.mjs`
- `rtk node --test tests/router.registry-schema.test.mjs tests/router.registry-build.test.mjs`
</verification>

<success_criteria>
- CONT-03 and CONT-08 pass for every gate state and multi-failure permutation.
- Eligibility is derived once and cannot be authored or overlaid.
- Unsafe and unknown states remain visibly recommendation-only.
</success_criteria>

<output>
Create `.planning/phases/22-conservative-contracts-and-relationship-graph/22-04-SUMMARY.md` when done.
</output>

---
phase: 36-release-gate-cleanup
plan: 01
objective: Make the live installer bundle self-contained and prove fresh-home onboarding.
---

# Plan 36-01: Live Install and Cold Start

## Tasks

1. Trace every root-level runtime dependency used by the deployed manifest builder; add only missing owned inputs and their removable parent directories to the lifecycle bundle.
2. Add or reuse a fresh-home test that checks manifest, coverage report, baseline, bindings, and cold-start default thresholds after install.
3. Run the installer coexistence matrix, lifecycle release suite, and snapshot parity check.

## Verification

- `tests/router.installer-coexistence.test.mjs`
- `tests/router.lifecycle.test.mjs`
- `tests/router.phase26-release.test.mjs`
- `tests/router.phase26-lifecycle.test.mjs`
- `tests/router.v12-release.test.mjs`

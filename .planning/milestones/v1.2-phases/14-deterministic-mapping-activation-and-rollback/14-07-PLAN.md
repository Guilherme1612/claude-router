---
phase: 14-deterministic-mapping-activation-and-rollback
plan: "07"
type: execute
wave: 7
depends_on: ["14-06"]
files_modified:
  - src/cli/router-control.mjs
  - tests/router.control-cli.test.mjs
autonomous: true
gap_closure: true
requirements: [ACT-01]
must_haves:
  truths:
    - Diff and rollback preview disclose deterministic totals and any bounded truncation or pagination.
    - Corrupt active history returns a stable unsafe verdict before projection.
    - Human-readable and JSON output expose the same complete canonical result and meaningful exit status.
  artifacts:
    - path: src/cli/router-control.mjs
      provides: Complete bounded diff/preview contract and corrupt-source verdicts
    - path: tests/router.control-cli.test.mjs
      provides: Large-history and invalid-active-version subprocess regressions
  key_links:
    - from: src/cli/router-control.mjs
      to: src/registry/activate.mjs
      via: semantic verifyVersion and previewRollback verdicts
    - from: canonical CLI result
      to: text and JSON renderers
      via: shared totals, truncation/page metadata, reason codes, and exit taxonomy
---

<objective>
Close the operator-control gap for large and corrupt histories while retaining preview-first typed rollback semantics.

Purpose: Ensure operators and automation never receive silently incomplete comparisons or internal errors for known unsafe active state per D-15 through D-20.
Output: Structured bounded diff/preview metadata, stable unsafe-state verdicts, and CLI regression coverage.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-deterministic-mapping-activation-and-rollback/14-CONTEXT.md
@.planning/phases/14-deterministic-mapping-activation-and-rollback/14-VERIFICATION.md
@.planning/phases/14-deterministic-mapping-activation-and-rollback/14-03-SUMMARY.md
@.planning/phases/14-deterministic-mapping-activation-and-rollback/14-06-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make large diff and preview results explicitly complete or bounded</name>
  <files>src/cli/router-control.mjs, tests/router.control-cli.test.mjs</files>
  <behavior>
    - More than 256 changed identifiers never disappear without deterministic totals and truncation metadata or pagination.
    - Text and JSON renderers expose identical counts, bounds, and continuation semantics.
    - Repeated runs over identical histories produce byte-stable ordered results.
  </behavior>
  <action>Add subprocess tests with more than 256 identifiers before changing diffVersions and rollback preview. Replace silent slicing with one deterministic contract: either explicit total/returned/truncated metadata and stable bounded ordering, or stable pagination with validated cursor/page parameters. Propagate the same canonical result through human and JSON renderers, status reason codes, and exit taxonomy per D-15, D-16, and D-20. Preserve read-only behavior and exact active/candidate or explicit version selection.</action>
  <verify><automated>node --test tests/router.control-cli.test.mjs</automated></verify>
  <done>Large comparisons are deterministically inspectable and never silently omit history in either output format.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fail closed on corrupt active sources before projection</name>
  <files>src/cli/router-control.mjs, tests/router.control-cli.test.mjs</files>
  <behavior>
    - A corrupt active version returns invalid_active_version or recovery_required before reading its registry projection.
    - JSON contains a stable reason code and nonzero exit status; text names the recovery action without an internal stack error.
    - Rollback remains preview-first and exact-destination confirmation remains mandatory.
  </behavior>
  <action>Create corrupt active-source fixtures covering manifest, registry, and semantic verification failures. Check the source verdict from the Plan 14-06 known-good verifier before projecting records or mappings; return stable invalid_active_version or recovery_required canonical results instead of internal_error. Render deterministic safety warnings and next action in both formats. Do not bypass D-17 compatibility verification, D-18 typed confirmation, or D-19 mutation/audit authority.</action>
  <verify><automated>node --test tests/router.control-cli.test.mjs tests/router.registry-activate.test.mjs</automated></verify>
  <done>Corrupt active history fails closed with stable structured operator guidance before any unsafe projection or rollback mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| immutable history -> operator CLI | Potentially corrupt or very large registry data is rendered to humans and automation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-14-07-01 | Information Disclosure | diff/preview | medium | mitigate | Emit deterministic bounded metadata without raw prompts or secrets. |
| T-14-07-02 | Tampering | corrupt active projection | high | mitigate | Verify active source before projection and return a stable fail-closed verdict. |
| T-14-07-03 | Denial of Service | large history | medium | mitigate | Bound or paginate output with stable totals and ordering. |
</threat_model>

<verification>
Run `node --test tests/router.control-cli.test.mjs tests/router.registry-activate.test.mjs` and confirm large-history text/JSON parity plus corrupt-source reason codes and exits.
</verification>

<success_criteria>
- Diff and preview expose deterministic completeness or bounded-result metadata.
- Corrupt active sources return stable unsafe-state verdicts before projection.
- Text and JSON share one canonical result and meaningful exit taxonomy.
- ACT-01 operator rollback inspection remains preview-first, compatible, and exact-confirmed.
</success_criteria>

<output>
Create `.planning/phases/14-deterministic-mapping-activation-and-rollback/14-07-SUMMARY.md` when done.
</output>

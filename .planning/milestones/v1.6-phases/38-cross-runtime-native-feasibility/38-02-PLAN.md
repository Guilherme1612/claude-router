---
phase: 38-cross-runtime-native-feasibility
plan: 02
type: execute
wave: 2
depends_on: [38-01]
files_modified:
  - src/adapters/dispatch/codex.mjs
  - tests/phase-38/codex-adapter.test.mjs
  - tests/phase-38/recommendation-only.test.mjs
autonomous: true
requirements: [HOST-02, HOST-03]
must_haves:
  truths:
    # --- HOST-02 covered acceptance criteria ---
    - "An authorized harmless fixture invoked through the Codex NativeDispatchAdapter produces a receipt whose invocation_identity.adapter, .runtime='codex', .pid, .command, .args, .lease_id, and .idempotency_key are populated and adapter-issued."
    - "The Codex receipt's completion_evidence.exit_code is 0, .stdout_sha256 equals the sha256 of the same deterministic fixture stdout as the Claude adapter (shared fixture), .wall_ms is non-negative, and .state is 'completed'."
    - "Removing the Codex adapter from the invocation path yields no receipt with state 'invoked'/'completed'; a test helper running the fixture alone produces no Codex receipt with state 'invoked'/'completed'."
    # --- HOST-03 covered acceptance criteria ---
    - "Both runtimes produce receipts with structurally equal intent, authority, and risk tuples (same field values from the same action/lease fixture) and the same terminal completion state ('completed')."
    - "When the Codex adapter's canDispatch() returns { ok: false } (simulated incompatibility: missing fixture, missing ~/.codex/router/installed.json marker, or sandboxed spawn), the adapter writes a receipt with state='recommendation_only' and the prompt path emits text only — it NEVER claims 'invoked'/'completed'."
    - "An incompatible Codex adapter disables autonomous dispatch ONLY for the Codex runtime; the Claude adapter's canDispatch() and invoke() are unaffected (structural independence via the promoted contract)."
    - "Receipts are partitioned: Claude receipts at ~/.claude/router/receipts/, Codex receipts at ~/.codex/router/receipts/; a Claude receipt cannot authorize a Codex dispatch and vice versa (receipt.runtime validated on read)."
    # --- Edge-probe coverage (resolved) ---
    # HOST-02 empty
    - "Invoking the Codex adapter with empty/null/missing action returns a 'recommendation_only' receipt and does NOT spawn (mirrors the Claude adapter's empty-input contract)."
    # HOST-02 encoding
    - "Codex receipt command/args are exact UTF-8 byte strings; stdout_sha256 is computed over raw stdout bytes (Buffer), byte-identical to the Claude adapter for the same fixture."
    # HOST-03 unclassified → flagged assumption (not a plain truth)
    - { statement: "Equivalence across runtimes is defined as structural equality of the receipt's intent/authority/risk tuple and identical terminal completion state; behavioral equivalence is deferred to Phase 44 causal attribution.", verification: backstop }
  prohibitions:
    - statement: "The Codex adapter must NEVER claim an invocation that did not happen — no receipt with state 'invoked'/'completed' without a real child_process.spawn it issued (pid + stdout_sha256 present)."
      status: unverified
      flagged: true
    - statement: "A runtime that cannot prove native dispatch must NEVER silently downgraded to text without a 'recommendation_only' receipt — the audit trail must record the decision (no silent fallback)."
      status: unverified
      flagged: true
    - statement: "Receipts must NEVER cross runtime boundaries — a Claude receipt must not authorize a Codex dispatch and vice versa (partition by ~/.claude vs ~/.codex, validate runtime on read)."
      status: unverified
      flagged: true
  artifacts:
    - src/adapters/dispatch/codex.mjs
    - tests/phase-38/codex-adapter.test.mjs
    - tests/phase-38/recommendation-only.test.mjs
  key_links:
    - "Codex NativeDispatchAdapter.invoke → child_process.spawn(harmless fixture) → ~/.codex/router/receipts/ atomic publish"
    - "Both adapters share NativeDispatchAdapter contract from 38-01 (identity continuity, same Receipt schema)"
---

<objective>
Extend the proven Claude dispatch slice (Plan 01) to the Codex runtime and prove cross-runtime equivalence plus the truthful recommendation-only fallback. This is the pluralization step the assumption_delta_decision anticipated: a second adapter variant of the same contract, not a parallel single-runtime assumption.

Purpose: HOST-02 proves Codex can also natively invoke and observe. HOST-03 proves the two runtimes are equivalent in intent/authority/risk/pause/resume/receipt and that an incompatible adapter truthfully downgrades to recommendation-only without disabling the other runtime or faking an invocation. Together these close the feasibility gate for both runtimes.

Output: Codex NativeDispatchAdapter impl, Codex anti-cheat test, parity + recommendation-only fallback test.
</objective>

<execution_context>
@/Users/guilherme/.claude/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/38-cross-runtime-native-feasibility/38-RESEARCH.md
@.planning/phases/38-cross-runtime-native-feasibility/38-PATTERNS.md
@.planning/phases/38-cross-runtime-native-feasibility/38-VALIDATION.md
@.claude/CLAUDE.md

# Prior plan SUMMARY dependency (genuine — this plan extends Plan 01's contract):
@.planning/phases/38-cross-runtime-native-feasibility/38-01-PLAN.md

# Primary analogs:
@src/adapters/codex.mjs
@src/adapters/dispatch/claude.mjs
@src/adapters/dispatch/contract.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (tdd): Codex NativeDispatchAdapter + Codex anti-cheat test (HOST-02)</name>
  <files>src/adapters/dispatch/codex.mjs, tests/phase-38/codex-adapter.test.mjs</files>
  <read_first>
    - src/adapters/dispatch/claude.mjs (from Plan 01 — the impl to mirror; dispatch MECHANISM is identical, only runtime field + receipt partition path differ)
    - src/adapters/dispatch/contract.mjs (from Plan 01 — createDispatchAdapter factory; invoke with runtime='codex')
    - src/adapters/dispatch/receipt.mjs (from Plan 01 — ReceiptStore, partition path resolution)
    - src/adapters/codex.mjs (createAdapter delegation pattern at :42-51, codex layout / home)
    - tests/phase-38/claude-adapter.test.mjs (from Plan 01 — test structure to mirror)
    - 38-RESEARCH.md §Pitfall 3 (dispatch mechanism identical; difference is binding/observation surface), §Environment Availability (~/.codex/router/installed.json marker)
  </read_first>
  <behavior>
    - Test 1 (codex adapter path): invoking the Codex adapter with an authorized fixture action produces a receipt with invocation_identity.adapter='codex-dispatch/1', runtime='codex', pid is a number > 0, completion_evidence.exit_code=0, stdout_sha256 matches the SAME fixture stdout as the Claude adapter (shared fixture), state='completed'.
    - Test 2 (partition): the receipt file is written under $TEMP_HOME/.codex/router/receipts/, NOT under .claude/router/receipts/ (detectRuntime() returns 'codex' via ROUTER_RUNTIME=codex env).
    - Test 3 (anti-cheat, test helper alone): running tests/phase-38/fixtures/harmless.mjs directly with ROUTER_RUNTIME=codex produces NO codex receipt with state 'invoked'/'completed'.
    - Test 4 (anti-cheat, recommendation-only): when canDispatch() returns { ok: false } (fixture missing or installed.json marker absent), invoke() produces a receipt with state='recommendation_only' and NO pid.
    - Test 5 (empty/null action): null/empty/{} action → 'recommendation_only' receipt, no spawn, no pid (mirrors Claude adapter).
    - Test 6 (encoding): multi-byte UTF-8 fixture stdout → stdout_sha256 byte-exact over raw Buffer, identical to the Claude adapter's hash for the same stdout.
  </behavior>
  <action>
    src/adapters/dispatch/codex.mjs — Codex NativeDispatchAdapter impl. Use createDispatchAdapter from contract.mjs with { runtime: 'codex', adapterVersion: 'codex-dispatch/1', receiptRoot: <os.homedir() joined ~/.codex/router/receipts/>, fixture: <same harmless.mjs abs path as Claude> }. The dispatch MECHANISM (child_process.spawn + unref) is IDENTICAL to claude.mjs — do NOT branch on runtime inside the spawn path (Pitfall 3). Branch ONLY at: (1) the receipt partition path (~/.codex/router/receipts/ via os.homedir + detectRuntime), (2) the canDispatch() probe which additionally checks for the ~/.codex/router/installed.json marker (RESEARCH §Environment Availability — marker contents: {"managed_by":"claude-router","control_authority_root":...}). Reuse detectRuntime() (router.mjs:83-92) and the same commandReference/portableTarget path validation. Same pause/resume minimal state (idempotency key + 'paused' state). Same fail-open contract.

    tests/phase-38/codex-adapter.test.mjs — mirror tests/phase-38/claude-adapter.test.mjs structure. Set ROUTER_RUNTIME=codex in the spawnSync env and HOME to a temp dir so receipts isolate to $TEMP_HOME/.codex/router/receipts/. For Test 2 (partition), assert the receipt path is under .codex/router/receipts/ and NOT under .claude/router/receipts/. For Test 3, spawnSync the fixture directly with ROUTER_RUNTIME=codex and assert no 'completed' codex receipt. RED first for the no-adapter baseline, GREEN against the codex adapter.
  </action>
  <verify>
    <automated>node --test tests/phase-38/codex-adapter.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/phase-38/codex-adapter.test.mjs` exits 0 with all 6 behavior cases green.
    - The Codex receipt's `invocation_identity.runtime` is 'codex' and `adapter` is 'codex-dispatch/1'.
    - The Codex receipt's `stdout_sha256` equals the Claude adapter's `stdout_sha256` for the same fixture (byte-exact).
    - No Codex receipt is written under .claude/router/receipts/ and vice versa (partition enforced).
    - canDispatch() returns { ok: false, reason } when ~/.codex/router/installed.json marker is absent (simulated in a temp HOME); invoke() then returns a 'recommendation_only' receipt with no pid.
  </acceptance_criteria>
  <done>The Codex adapter produces a real native invocation receipt equivalent to the Claude adapter (HOST-02), partitioned to ~/.codex/router/receipts/, and the Codex anti-cheat test is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (tdd): Parity + recommendation-only fallback test (HOST-03)</name>
  <files>tests/phase-38/recommendation-only.test.mjs</files>
  <read_first>
    - tests/phase-38/claude-adapter.test.mjs (from Plan 01)
    - tests/phase-38/codex-adapter.test.mjs (from Task 1 of this plan)
    - tests/router.coexistence.test.mjs (spawnSync hook driver pattern driving both runtimes via ROUTER_RUNTIME)
    - src/adapters/dispatch/contract.mjs (from Plan 01 — pause/resume minimal state)
    - src/adapters/dispatch/claude.mjs and src/adapters/dispatch/codex.mjs (the two adapter variants)
    - 38-RESEARCH.md §Pattern 2 (Recommendation-Only Fallback — truthful, not silent), §Open Question 4 (structural vs behavioral equivalence), §Pitfall 4 (pause/resume as router-internal state)
  </read_first>
  <behavior>
    - Test 1 (equivalence, intent/authority/risk): driving the same authorized action through both the Claude and Codex adapters produces receipts whose intent, authority, and risk fields are structurally equal (deepEqual) and whose terminal state is both 'completed'.
    - Test 2 (equivalence, pause/resume): pause() on the Claude adapter writes a receipt with state='paused' + idempotency_key; resume() with the same key re-spawns and produces state='completed'. The same sequence on the Codex adapter produces the same state transitions. Both adapters reject a duplicate 'invoked'/'completed' state for the same idempotency_key.
    - Test 3 (recommendation-only fallback, Codex incompatible): force the Codex adapter's canDispatch() to { ok: false } (missing installed.json marker). The Codex adapter writes a receipt with state='recommendation_only' and NO autonomous dispatch occurs (no pid). The prompt path emits text only. The Claude adapter's canDispatch() and invoke() are unaffected (structural independence).
    - Test 4 (recommendation-only fallback, no silent downgrade): assert that a 'recommendation_only' receipt EXISTS in the store (the audit trail records the decision — not a silent text-only downgrade). The receipt's completion_evidence.state is exactly 'recommendation_only'.
    - Test 5 (cross-runtime isolation): a Claude receipt presented to the Codex observe() path is rejected (runtime mismatch); a Codex receipt presented to Claude observe() is rejected. No cross-runtime authorization.
  </behavior>
  <action>
    Write tests/phase-38/recommendation-only.test.mjs using node:test + assert/strict + the spawnSync hook driver pattern (tests/router.coexistence.test.mjs). For each test, create a temp HOME, set ROUTER_RUNTIME appropriately, drive the adapter (or the hook for the prompt-path text-only assertion). For Test 3, simulate Codex incompatibility by removing/renaming the installed.json marker in the temp HOME and asserting canDispatch() returns { ok: false }. Assert the Claude adapter in the same temp HOME still returns canDispatch() { ok: true } (structural independence — the promoted contract means each variant reports independently). For Test 5, read a Claude receipt file and pass its receipt_id to the Codex adapter's observe(); assert it throws or returns a runtime-mismatch sentinel. Use deepEqual for the intent/authority/risk tuple (Test 1). For Test 2, exercise pause() then resume() on both adapters against the shared fixture; assert idempotency-key rejection of duplicate 'completed'. RED first against a baseline that treats the two adapters as independent (Plan 01 already gives structural independence via the promoted contract; this test codifies HOST-03).
  </action>
  <verify>
    <automated>node --test tests/phase-38/recommendation-only.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/phase-38/recommendation-only.test.mjs` exits 0 with all 5 behavior cases green.
    - Test 1: assert.deepEqual(claudeReceipt.intent/authority/risk, codexReceipt.intent/authority/risk) passes; both terminal states are 'completed'.
    - Test 3: a 'recommendation_only' Codex receipt exists and NO Codex receipt with state 'invoked'/'completed' exists when canDispatch() is false; the Claude adapter still completes (canDispatch ok: true) — structural independence verified.
    - Test 4: the 'recommendation_only' receipt is present in the store (not a silent text-only downgrade) — assert the receipt file exists with state='recommendation_only'.
    - Test 5: observe() rejects a cross-runtime receipt (runtime mismatch).
  </acceptance_criteria>
  <done>HOST-03 is satisfied: both runtimes produce equivalent intent/authority/risk/pause/resume/receipt outcomes; an incompatible Codex adapter disables autonomous dispatch only for Codex, writes a truthful 'recommendation_only' receipt (no silent downgrade), and preserves Claude's autonomy + truthful recommendations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Codex adapter → host process | child_process.spawn under Codex; same fixed-fixture constraint as Claude. |
| runtime partition | ~/.codex/router/receipts/ must not bleed into ~/.claude/router/receipts/ and vice versa. |
| recommendation-only downgrade | Incompatible adapter must record the decision in a receipt, not silently emit text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-08 | Elevation | cross-runtime receipt bleed (Claude receipt authorizes Codex dispatch) | high | mitigate | Receipts partitioned by runtime dir via os.homedir() + detectRuntime(); receipt.runtime field validated on read in observe(). Test 5 verifies rejection of cross-runtime receipts. |
| T-38-09 | Spoofing | silent downgrade hides incompatibility (no audit trail) | high | mitigate | canDispatch()==false MUST write a 'recommendation_only' receipt (Pattern 2 — truthful, not silent). Test 4 asserts the receipt exists. |
| T-38-10 | Tampering | Codex-specific branching inside spawn path (over-engineering divergence) | medium | mitigate | Dispatch MECHANISM is identical across runtimes (Pitfall 3); branch only at binding/observation + partition path. Test 1's byte-identical stdout_sha256 across runtimes verifies. |
| T-38-11 | Tampering | Codex installed.json marker spoofed to grant dispatch | medium | mitigate | canDispatch() checks marker presence + contents (managed_by:'claude-router'); marker is a local file under operator-controlled ~/.codex/router/. Not a credential — dispatch is still gated by the operator-issued lease marker from Plan 01. |
| T-38-SC | Tampering | (none — no new package installs this plan) | low | accept | Stdlib only; Plan 01's package audit covers the phase. |
</threat_model>

<verification>
- `node --test tests/phase-38/codex-adapter.test.mjs tests/phase-38/recommendation-only.test.mjs` is green.
- A probe running both adapters against the shared fixture produces receipts with byte-identical stdout_sha256 and structurally equal intent/authority/risk tuples.
- Removing the Codex installed.json marker in a temp HOME causes the Codex adapter to return 'recommendation_only' while the Claude adapter still returns 'completed'.
</verification>

<success_criteria>
- HOST-02 is satisfied: Codex produces a real native invocation identity + linked completion evidence; recommendation text and a test helper alone cannot pass (Codex anti-cheat Tests 3-4 green).
- HOST-03 is satisfied: equivalent intent/authority/risk/pause/resume/receipt on both runtimes (Test 1-2); incompatible Codex adapter → 'recommendation_only', no autonomous dispatch, Claude unaffected (Test 3); no silent downgrade (Test 4); cross-runtime isolation enforced (Test 5).
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces (Plan 02)

| Symbol / File | Kind | Purpose |
|---------------|------|---------|
| `src/adapters/dispatch/codex.mjs` | module | Codex NativeDispatchAdapter impl (canDispatch/invoke/observe/pause/resume; runtime='codex', partition ~/.codex/router/receipts/, installed.json marker probe) |
| `tests/phase-38/codex-adapter.test.mjs` | test | Codex adapter path + partition + anti-cheat + encoding (HOST-02) |
| `tests/phase-38/recommendation-only.test.mjs` | test | Parity + recommendation-only fallback + cross-runtime isolation (HOST-03) |

New symbols: `codex-dispatch/1` adapter id, Codex installed.json canDispatch probe. The NativeDispatchAdapter contract and Receipt/ReceiptStore from Plan 01 are reused (not re-created).
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/38-cross-runtime-native-feasibility/38-02-SUMMARY.md` when done
</output>
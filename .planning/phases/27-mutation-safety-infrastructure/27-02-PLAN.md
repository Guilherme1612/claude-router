---
phase: 27-mutation-safety-infrastructure
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/evolution/perf-measure.mjs
  - tests/router.perf-calibration.test.mjs
  - build-manifest.mjs
  - tests/router.build-manifest.test.mjs
autonomous: true
requirements: [SAF-03, SAF-04]
user_setup: []

must_haves:
  truths:
    - "assessMutationSafetyRegression({ performance: { warm: { p95_ms: 39, max_ms: 99 } } }) returns pass: true (SAF-03)"
    - "assessMutationSafetyRegression({ performance: { warm: { p95_ms: 40 } } }) returns pass: false, reason_code: mutation_safety_p95_exceeded (SAF-03 boundary: < 40, not <= 40)"
    - "assessMutationSafetyRegression({ performance: { warm: { p95_ms: 35, max_ms: 100 } } }) returns pass: false, reason_code: mutation_safety_max_exceeded (SAF-03 boundary: < 100, not <= 100)"
    - "assessMutationSafetyRegression returns ceilings: { p95_ms: 40, max_ms: 100 } in its result object (SAF-03)"
    - "assessMutationSafetyRegression does NOT take an evaluation parameter — quality/context_budget are canary-only concerns (SAF-03 separation from canary)"
    - "assessCalibration p95<25ms gate is unchanged — the 25ms canary ceiling is NOT relaxed (Phase 26 locked decision)"
    - "measureRoutes against the current mode-map + CALIBRATION_CORPUS produces warm p95 < 40ms and max < 100ms (SAF-03 regression)"
    - "The regression test reuses CALIBRATION_CORPUS and measureRoutes — no new corpus or percentile logic invented (SAF-03)"
    - "build-manifest.mjs (or an exported function) checks mode-map.json byte size against MODE_MAP_SIZE_CEILING = 30000 and exits non-zero when size > 30000 (SAF-04 size guard)"
    - "The size guard reuses the existing fileStatSize helper — no second stat helper added (SAF-04)"
    - "The size guard runs off-hot-path in build-manifest.mjs, NOT in router.mjs (SAF-04, Pitfall 5)"
    - statement: "Empty corpus input to measureRoutes produces warm p95/max of 0 or undefined, which assessMutationSafetyRegression handles without crashing (SAF-03 empty input)"
      verification: backstop
    - statement: "percentile computation is stable across runs — same durations produce same p95 (deterministic nearest-rank, already tested in perf-calibration.test.mjs:43-57) (SAF-03 ordering stability)"
      verification: backstop
    - statement: "At exactly 30000 bytes mode-map.json the size guard passes (boundary is > 30000); at 30001 bytes it fails (SAF-04 boundary)"
      verification: backstop
  prohibitions:
    - "Must NOT relax the 25ms assessCalibration canary gate to 40ms — add a parallel assessMutationSafetyRegression function instead"
    - "Must NOT reinvent percentile or p95 computation — reuse the existing percentile function in perf-measure.mjs"
    - "Must NOT invent a new calibration corpus — reuse CALIBRATION_CORPUS from perf-measure.mjs"
    - "Must NOT add the 30KB size guard to router.mjs hot path — it belongs in build-manifest.mjs or src/registry/validate.mjs"
    - "Must NOT add a second file-stat helper — reuse fileStatSize from build-manifest.mjs"
    - "Must NOT take an evaluation parameter in assessMutationSafetyRegression — quality and context_budget are canary-only"
  artifacts:
    - src/evolution/perf-measure.mjs (modified: new assessMutationSafetyRegression export)
    - tests/router.perf-calibration.test.mjs (extended)
    - build-manifest.mjs (modified: MODE_MAP_SIZE_CEILING constant + size guard)
    - tests/router.build-manifest.test.mjs (extended)
  key_links:
    - "assessMutationSafetyRegression → measureRoutes output (performance.warm.p95_ms / max_ms)"
    - "mode-map size guard → fileStatSize helper (reuse, not duplicate)"

artifacts_this_phase_produces:
  functions:
    - assessMutationSafetyRegression({ performance }) — new export in perf-measure.mjs, p95<40ms / max<100ms gate
  constants:
    - MODE_MAP_SIZE_CEILING — 30000, in build-manifest.mjs
  modified_files:
    - build-manifest.mjs (size guard added after mode-map write block)
  test_extensions:
    - tests/router.perf-calibration.test.mjs (assessMutationSafetyRegression gate tests + regression run)
    - tests/router.build-manifest.test.mjs (30KB size guard test)
---

<objective>
Close the two off-hot-path safety gaps: (SAF-03) add a p95<40ms / max<100ms mutation-safety regression gate parallel to the existing 25ms canary, re-runnable each subsequent phase against the full mode-map; (SAF-04 size) add a 30KB mode-map.json size guard in the builder that exits non-zero when the file exceeds the ceiling.

Purpose: The regression gate proves mutations in Phases 28-29 do not creep latency past the safety ceiling, and the size guard prevents mode-map bloat past 30KB during curation.
Output: Modified src/evolution/perf-measure.mjs with assessMutationSafetyRegression, modified build-manifest.mjs with the size guard, extended test files for both.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-mutation-safety-infrastructure/27-RESEARCH.md
@.planning/phases/27-mutation-safety-infrastructure/27-PATTERNS.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Add assessMutationSafetyRegression gate to perf-measure.mjs + regression test</name>
  <files>src/evolution/perf-measure.mjs, tests/router.perf-calibration.test.mjs</files>
  <read_first>
    - src/evolution/perf-measure.mjs lines 62-96 (measureRoutes, percentile, assessCalibration — the pattern to parallel, NOT modify)
    - tests/router.perf-calibration.test.mjs lines 1-60 (existing test structure, deterministic-clock pattern, CALIBRATION_CORPUS import)
    - .planning/phases/27-mutation-safety-infrastructure/27-PATTERNS.md lines 117-138 (perf-measure analog: apply instructions for assessMutationSafetyRegression)
    - .planning/phases/27-mutation-safety-infrastructure/27-RESEARCH.md lines 208-223 (Pattern 4: parallel latency gate code example)
  </read_first>
  <behavior>
    - Test: assessMutationSafetyRegression({ performance: { warm: { p95_ms: 39, max_ms: 99 } } }) returns { pass: true, reason_code: 'mutation_safety_pass' }
    - Test: assessMutationSafetyRegression({ performance: { warm: { p95_ms: 40, max_ms: 90 } } }) returns { pass: false, reason_code: 'mutation_safety_p95_exceeded' }
    - Test: assessMutationSafetyRegression({ performance: { warm: { p95_ms: 35, max_ms: 100 } } }) returns { pass: false, reason_code: 'mutation_safety_max_exceeded' }
    - Test: result includes ceilings: { p95_ms: 40, max_ms: 100 }
    - Test: assessMutationSafetyRegression({}) or missing performance returns { pass: false } without crashing
    - Test: assessCalibration still enforces p95<25ms — existing canary test still passes unchanged
    - Test: regression — measureRoutes against CALIBRATION_CORPUS with the current mode-map produces warm p95 < 40ms and max < 100ms, and assessMutationSafetyRegression passes
  </behavior>
  <action>
    Write the 7 tests in tests/router.perf-calibration.test.mjs first (RED), importing assessMutationSafetyRegression from the perf-measure module. Use the existing import path pattern (perfUrl variable, line 7 area). Run to confirm RED (function does not exist yet).

    Then implement in src/evolution/perf-measure.mjs (GREEN):

    Add a new exported function assessMutationSafetyRegression({ performance: measured } = {}) after the existing assessCalibration (line 96). Structure: const p95Pass = measured?.warm?.p95_ms < 40; const maxPass = measured?.warm?.max_ms < 100; return freeze({ pass: p95Pass && maxPass, reason_code: !p95Pass ? 'mutation_safety_p95_exceeded' : !maxPass ? 'mutation_safety_max_exceeded' : 'mutation_safety_pass', ceilings: { p95_ms: 40, max_ms: 100 } }). Use the same freeze(...) convention as assessCalibration. Do NOT take an evaluation parameter. Do NOT modify assessCalibration or its 25ms ceiling. Per SAF-03, Pattern 4 in RESEARCH.md.

    For the regression test, reuse CALIBRATION_CORPUS and measureRoutes — call measureRoutes with the current mode-map fixtures and assert assessMutationSafetyRegression({ performance: result }) passes. Use the deterministic-clock pattern from tests/router.perf-calibration.test.mjs:43-57 if needed, or the real measureRoutes call if the existing regression test pattern already does so. Per SAF-03.

    Run tests to confirm GREEN.
  </action>
  <verify>
    <automated>rtk node --test tests/router.perf-calibration.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - tests/router.perf-calibration.test.mjs exits 0 with all existing tests + 7 new tests passing
    - node -e "import('./src/evolution/perf-measure.mjs').then(m => assert(typeof m.assessMutationSafetyRegression === 'function'))" succeeds
    - assessCalibration 25ms ceiling unchanged: grep -n 'p95_ms < 25' src/evolution/perf-measure.mjs returns the existing assessCalibration line (not modified)
    - assessMutationSafetyRegression uses 40ms ceiling: grep -n 'p95_ms < 40' src/evolution/perf-measure.mjs returns the new function line
  </acceptance_criteria>
  <done>
    assessMutationSafetyRegression is exported, enforces p95<40ms / max<100ms, returns correct reason codes and ceilings. The 25ms canary gate is untouched. The regression test proves warm p95 stays below 40ms on the current mode-map.
  </done>
  <reversibility rating="reversible">New function is additive — removing it has no impact on existing assessCalibration or measureRoutes.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add 30KB mode-map size guard to build-manifest.mjs + test</name>
  <files>build-manifest.mjs, tests/router.build-manifest.test.mjs</files>
  <read_first>
    - build-manifest.mjs lines 525-536 (final atomic-write + size-log block — the analog and insertion point)
    - build-manifest.mjs line 20 (fileStatSize helper — reuse, do not duplicate)
    - tests/router.build-manifest.test.mjs (existing test structure to extend)
    - tests/router.cache.test.mjs lines 16-20 (withTempDir pattern — copy verbatim for filesystem isolation)
    - .planning/phases/27-mutation-safety-infrastructure/27-PATTERNS.md lines 141-160 (build-manifest analog: apply instructions for the size guard)
    - .planning/phases/27-mutation-safety-infrastructure/27-RESEARCH.md lines 355-358 (Open Question 2: error in strict/CI, warn in local dev)
  </read_first>
  <behavior>
    - Test: a mode-map.json of 30000 bytes passes the size guard (boundary: > 30000, not >=)
    - Test: a mode-map.json of 30001 bytes fails the size guard (exit code non-zero or function returns false)
    - Test: a mode-map.json of 15000 bytes passes (well within ceiling, current size is ~15304)
    - Test: fileStatSize is reused — no second stat helper exists in build-manifest.mjs
  </behavior>
  <action>
    Write the 4 tests in tests/router.build-manifest.test.mjs first (RED). Copy the withTempDir helper verbatim from tests/router.cache.test.mjs:16-20 for filesystem isolation. Write a temp mode-map.json of controlled size, run the size guard (either as an exported function or via subprocess), assert pass/fail. Run to confirm RED.

    Then implement in build-manifest.mjs (GREEN):

    Add a constant MODE_MAP_SIZE_CEILING = 30_000 near the top of the file (after existing constants). After the mode-map file is written (or as a validator step near lines 525-536), add: const modeMapSize = fileStatSize(MODE_MAP_PATH); if (modeMapSize > MODE_MAP_SIZE_CEILING) { console.error(`mode-map.json exceeds 30KB: ${modeMapSize} bytes`); process.exitCode = 1; }. Reuse the existing fileStatSize helper (line 20) — do not add a second stat helper. If the guard is better as an exported function for testability, export assertModeMapSize(path, ceiling = 30_000) and call it from the write block. Per SAF-04, Pitfall 5 (off-hot-path), Open Question 2 (default to error exit). Per RESEARCH A1.

    Run tests to confirm GREEN.
  </action>
  <verify>
    <automated>rtk node --test tests/router.build-manifest.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - tests/router.build-manifest.test.mjs exits 0 with all existing tests + 4 new size-guard tests passing
    - grep -c 'MODE_MAP_SIZE_CEILING' build-manifest.mjs returns >= 1
    - grep -c 'fileStatSize' build-manifest.mjs returns exactly 1 definition (no duplicate helper)
    - The size guard is NOT in router.mjs: grep -c 'MODE_MAP_SIZE_CEILING' ~/.claude/hooks/router.mjs returns 0
  </acceptance_criteria>
  <done>
    The 30KB mode-map size guard is implemented in build-manifest.mjs, reuses fileStatSize, exits non-zero on overflow, and is tested at the boundary (30000 passes, 30001 fails). The guard is off-hot-path.
  </done>
  <reversibility rating="reversible">Guard is a single conditional + constant — removing it restores unconditional write behavior.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mode-map.json (disk) → builder process | The mode-map file is written by the builder; the size guard prevents unbounded bloat from curation mutations |
| perf-measure.mjs → CI gate | The regression gate output determines whether a mutation is safe to ship; a false pass could let a latency regression through |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-06 | Denial of Service | mode-map.json bloat via curation | medium | mitigate | SAF-04 30KB size guard in build-manifest.mjs exits non-zero on overflow, preventing Phase 29 expansion from silently bloating past 30KB (SAF-04) |
| T-27-07 | Denial of Service | hot-path latency regression after mode-map expansion | high | mitigate | SAF-03 assessMutationSafetyRegression gate (p95<40ms / max<100ms) re-runnable each phase against the full mode-map (SAF-03) |
| T-27-08 | Tampering | canary gate ceiling relaxation | high | mitigate | assessCalibration 25ms ceiling is explicitly untouched; assessMutationSafetyRegression is a parallel function — tests assert the 25ms gate is unchanged (Phase 26 locked decision) |
</threat_model>

<verification>
- rtk node --test tests/router.perf-calibration.test.mjs — all SAF-03 tests green, canary gate unchanged
- rtk node --test tests/router.build-manifest.test.mjs — SAF-04 size guard tests green
- Full suite: rtk node --test tests/*.test.mjs — no regressions
</verification>

<success_criteria>
- assessMutationSafetyRegression enforces p95<40ms / max<100ms with correct reason codes and ceilings
- The 25ms assessCalibration canary gate is unchanged
- Regression test proves warm p95 < 40ms on the current mode-map
- mode-map.json size guard exits non-zero when file > 30KB
- Size guard reuses fileStatSize, runs off-hot-path in build-manifest.mjs
- All tests green, no regressions
</success_criteria>

<output>
Create .planning/phases/27-mutation-safety-infrastructure/27-02-SUMMARY.md when done
</output>
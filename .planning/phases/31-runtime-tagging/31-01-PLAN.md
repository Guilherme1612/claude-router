---
phase: 31-runtime-tagging
plan: 01
type: test
wave: 0
depends_on: []
files_modified:
  - tests/router.runtime-tagging.test.mjs
  - tests/router.health.outcome-schema.test.mjs
  - tests/router.cache.test.mjs
  - tests/router.mjs.snapshot.diff.test.mjs
autonomous: true
requirements: [PARITY-01, PARITY-02]
user_setup: []
estimate:
  tokens: 55000
  raw_tokens: 28000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - A RED spec exists that will prove runtime detection determinism, cache-key divergence, and the telemetry runtime field once the feature tasks land.
    - The outcome-schema enforcement test is updated to expect 16 fields (runtime/epoch) so the Wave-2 schema bump cannot be silent.
    - A mirror-desync guard asserts tests/router.mjs.snapshot is byte-identical to ~/.claude/hooks/router.mjs.
  artifacts:
    - tests/router.runtime-tagging.test.mjs
    - tests/router.health.outcome-schema.test.mjs (bumped to 16-field expectation)
    - tests/router.cache.test.mjs (extended with cross-runtime divergence)
    - tests/router.mjs.snapshot.diff.test.mjs
  key_links:
    - Detector tests reference the future module-level RUNTIME constant and runtime-conditional RUNTIME_CONFIG_DIR.
    - CacheKey divergence test references the future runtime key slot.
---

<objective>
Create the Wave-0 RED test infrastructure for Phase 31 so every feature task that follows has a runnable, deterministic assertion to turn GREEN.

Purpose: TDD detection-first. The tests are written before the runtime detection/tagging code exists, so each feature task in plans 31-02 and 31-03 has a concrete failing target. Also plants the mirror-desync guard that makes every future hook edit load-bearing (snapshot must equal the live hook).

Output: One new spec (tests/router.runtime-tagging.test.mjs), one extended spec (tests/router.cache.test.mjs), one updated enforcement spec (tests/router.health.outcome-schema.test.mjs), and one mirror-desync guard (tests/router.mjs.snapshot.diff.test.mjs).

Deferred note (decision-coverage): D-07 (resolve evaluation considers only active runtime's capabilities) and D-08 (cross-runtime capability-equivalent fixture) are deferred to Phase 32 with rationale — the resolve-first capability-resolution hot path does not exist in this codebase until Phase 32 ships PARITY-03/04 (mapped to ROUTE-01/03/04/05). Phase 31 lays the per-runtime foundation they build on (runtime-conditional dirs + runtime tag on cache/telemetry). D-08's fixture is kept minimal/test-scoped, reusing the phase-26 provenance[{runtime}] shape. These decisions are NOT dropped.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-runtime-tagging/31-CONTEXT.md
@.planning/phases/31-runtime-tagging/31-RESEARCH.md
@.planning/phases/31-runtime-tagging/31-VALIDATION.md

# Reference files (read once, extract the exact assertions/localities):
@tests/router.mjs.snapshot  # isMain():54-61; RUNTIME_CONFIG_DIR:104; cacheKey():1692-1700; telemetryEntryFromState():2554-2585; telemetry write:2664
@tests/router.cache.test.mjs  # HOOK import:13; cacheKey calls:23-56 (positional args, no runtime slot yet)
@tests/router.health.outcome-schema.test.mjs  # frozen/size/membership test:68-74
@tests/router.phase26-dual-runtime.test.mjs  # PROFILES + provenance[{runtime}] shape to reuse (D-08 fixture)
@src/health/outcome-schema.mjs  # OUTCOME_FIELDS allowlist:33-38 (14 fields), frozen
</context>

## Artifacts this phase produces

| Kind | Artifact | What it is |
|------|----------|------------|
| New test file | `tests/router.runtime-tagging.test.mjs` | RED spec importing the live hook (`~/.claude/hooks/router.mjs`). Asserts the future `RUNTIME`/`RUNTIME_CONFIG_DIR` exports, `cacheKey` runtime slot (claude !== codex), `telemetryEntryFromState().runtime` field, and the fail-open enum clamp. RED until 31-02/31-03 export the API. |
| New test file | `tests/router.mjs.snapshot.diff.test.mjs` | Mirror-desync guard: asserts `tests/router.mjs.snapshot` is byte-identical to `~/.claude/hooks/router.mjs`. GREEN at creation; becomes load-bearing in 31-02/31-03 lockstep edits. |
| Updated test file | `tests/router.health.outcome-schema.test.mjs` | Enforcement test re-pointed at the deliberate 31-03 bump target: `OUTCOME_FIELDS.size === 16`, `runtime`/`epoch` added to the membership loop, frozen guard kept. RED against today's 14-field schema. |
| Updated test file | `tests/router.cache.test.mjs` | Cross-runtime cacheKey divergence block (claude !== codex; claude === claude) via the future 7th `runtime` parameter. RED against the 6-param cacheKey. |
| Referenced future API (produced in 31-02/31-03) | `detectRuntime()`, `RUNTIME`, `runtimeBaseDir()`, rewired `RUNTIME_CONFIG_DIR`/`ROUTER_DIR`/`HOOKS_DIR`, `cacheKey` runtime slot, `telemetryEntryFromState().runtime` | Symbols this spec creates import bindings for; undefined until 31-02/31-03 ship — that undefined-import is the RED state. |

<tasks>

<task type="test">
  <name>Task 1: Author runtime-tagging spec + mirror-desync guard (RED)</name>
  <files>tests/router.runtime-tagging.test.mjs, tests/router.mjs.snapshot.diff.test.mjs</files>
  <read_first>tests/router.mjs.snapshot</read_first>
  <action>
    Create `tests/router.runtime-tagging.test.mjs` importing the live hook at `join(homedir(), '.claude', 'hooks', 'router.mjs')` (the same HOOK pattern tests/router.cache.test.mjs:13 uses — this is what makes the mirror load-bearing). The spec targets the future Phase-31 API surface, so it is intentionally RED until plans 31-02/31-03 ship:

    - Detection precedence test group: assert that the module-level exported `RUNTIME` constant is one of `"claude"` or `"codex"` (per D-02 string convention). Via `child_process.spawnSync(node, [HOOK], { env: { ...process.env, ROUTER_RUNTIME: 'codex' } })` the process exit/`RUNTIME` must be `codex` when `ROUTER_RUNTIME=codex` (D-04 override wins). When `ROUTER_RUNTIME` is absent and no codex marker is present in env/argv, `RUNTIME` must be `claude` (default, fail-open). When `ROUTER_RUNTIME=codex` is set, NOT `claude`. Refer to the exported `detectRuntime`/`RUNTIME` symbol the snapshot must expose (create import bindings for `RUNTIME`, `RUNTIME_CONFIG_DIR`, `cacheKey`, `telemetryEntryFromState`). These imports will be undefined until 31-02/31-03 land — that is the RED state.

    - Cache-key divergence test group: `assert.notEqual(cacheKey('fix bug', ['fix'], 'fp', '', '', '', 'codex'), cacheKey('fix bug', ['fix'], 'fp', '', '', '', 'claude'))` — same prompt+manifest under different runtime → different key (D-05 key identity). Assert `cacheKey('x', [], 'fp', '', '', '', 'claude')` equals itself (deterministic). RED until 31-03 adds the runtime param.

    - Telemetry runtime field test group: after driving a minimal decision through the hook (mirror the dry-run pattern in the snapshot), assert the produced `telemetry_entry.runtime` equals the module `RUNTIME` constant, or directly assert `telemetryEntryFromState(decisionFixture, process.hrtime.bigint()).runtime` is a non-empty string in `{"claude","codex"}`. RED until 31-03 adds the field.

    - Fail-open test group: assert that setting `ROUTER_RUNTIME` to an out-of-enum value (e.g. `'bogus'`) still yields `RUNTIME === 'claude'` (enum-clamped, never an unbounded string flowing into paths — V5 input validation boundary).

    Create `tests/router.mjs.snapshot.diff.test.mjs`: read `tests/router.mjs.snapshot` and `~/.claude/hooks/router.mjs` as utf8 strings and `assert.equal(snapshot, live)` — the byte-identical mirror invariant. The live hook exists and is currently identical (verified), so this guard is GREEN at creation and becomes load-bearing in 31-02/31-03 (which must edit both files in lockstep).
  </action>
  <verify>
    <automated>rtk node --test tests/router.runtime-tagging.test.mjs tests/router.mjs.snapshot.diff.test.mjs 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    Both test files exist and parse. The mirror-desync guard test PASSES (snapshot == live hook byte-identical). The runtime-tagging detection/cacheKey/telemetry groups FAIL (RED) because the Phase-31 API symbols are not yet exported — proving the tests are load-bearing, not tautological. Note in the summary which assertions are RED and which symbol they await.
  </acceptance_criteria>
  <done>tests/router.runtime-tagging.test.mjs and tests/router.mjs.snapshot.diff.test.mjs exist and parse; snapshot.diff PASSES; detection/cacheKey/telemetry groups are RED awaiting the 31-02/31-03 API.</done>
</task>

<task type="test">
  <name>Task 2: Bump outcome-schema enforcement test to 16 fields (RED)</name>
  <files>tests/router.health.outcome-schema.test.mjs</files>
  <read_first>tests/router.health.outcome-schema.test.mjs</read_first>
  <action>
    In the frozen-allowlist test at tests/router.health.outcome-schema.test.mjs:68-74, change `assert.equal(OUTCOME_FIELDS.size, 14)` to `assert.equal(OUTCOME_FIELDS.size, 16)` and add `'runtime'` and `'epoch'` to the membership-assertion loop array (alongside the existing 14 field names, at :71). Keep the `assert.ok(Object.isFrozen(OUTCOME_FIELDS))` guard. Do NOT edit src/health/outcome-schema.mjs in this plan — leave it at 14 fields so this test is RED until the deliberate schema bump lands in 31-03. This enforces ROADMAP criterion 3 (the runtime/epoch fields land only via an intentional policy-version bump, never a silent schema add).
  </action>
  <verify>
    <automated>rtk node --test tests/router.health.outcome-schema.test.mjs 2>&1 | grep -E "size|pass|fail" | head</automated>
  </verify>
  <acceptance_criteria>
    The test file asserts size === 16 and includes runtime/epoch in membership. Running it against the current 14-field schema FAILS the size assertion (RED). The assertion is the enforcement guard that 31-03 must satisfy in the same change that adds runtime/epoch to OUTCOME_FIELDS.
  </acceptance_criteria>
  <done>tests/router.health.outcome-schema.test.mjs asserts OUTCOME_FIELDS.size === 16 with runtime/epoch in the membership loop; it is RED against the untouched 14-field schema (the 31-03 bump target).</done>
</task>

<task type="test">
  <name>Task 3: Extend cache spec with cross-runtime cacheKey divergence (RED)</name>
  <files>tests/router.cache.test.mjs</files>
  <read_first>tests/router.cache.test.mjs</read_first>
  <action>
    Add a test block to tests/router.cache.test.mjs after the existing cacheKey tests (~:56). The new test passes a trailing `runtime` argument to `cacheKey(normalizedPrompt, intentKeywords, manifestFingerprint, modeMapHash, weightsHash, graphHash, runtime)` and asserts:
    - `cacheKey('fix bug', ['fix'], 'fp', '', '', '', 'codex')` !== `cacheKey('fix bug', ['fix'], 'fp', '', '', '', 'claude')` (D-05 runtime is key identity).
    - `cacheKey('fix bug', ['fix'], 'fp', '', '', '', 'claude')` === `cacheKey('fix bug', ['fix'], 'fp', '', '', '', 'claude')` (deterministic within a runtime).
    These calls use the 7th param that does not exist yet — RED until 31-03 extends cacheKey. Do not modify the existing cacheKey tests or their positional-argument call sites in this plan.
  </action>
  <verify>
    <automated>rtk node --test tests/router.cache.test.mjs 2>&1 | grep -E "cross-runtime|pass|fail" | head</automated>
  </verify>
  <acceptance_criteria>
    The new divergence test asserts codex and claude keys differ and same-runtime keys match. It FAILS (RED) against the current 6-param cacheKey because the extra argument is dropped and both keys collide — proving the runtime slot is not yet wired. The existing cacheKey tests continue to pass unchanged.
  </acceptance_criteria>
  <done>tests/router.cache.test.mjs gains a cross-runtime cacheKey divergence test (claude !== codex, claude === claude) that is RED against the 6-param cacheKey; existing cacheKey tests remain GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process env / argv → runtime detection | Untrusted env could spoof `ROUTER_RUNTIME` or a codex marker at module load |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-01 | Spoofing | detectRuntime() | medium | mitigate | `ROUTER_RUNTIME` + detection clamped to the `"claude"\|"codex"` enum; any other/ambiguous value → default `claude` (fail-open). Wave-0 test asserts out-of-enum → `claude`. |
| T-31-02 | Tampering | RUNTIME_CONFIG_DIR derivation | medium | mitigate | Dir derived from `os.homedir()` + a fixed runtime base (`.claude`/`.codex`); runtime value is enum-clamped so the path segment can never be attacker-controlled. |
| T-31-03 | Information Disclosure | telemetry.jsonl | high | mitigate | Telemetry stays append-only JSONL, 0600 via logTelemetry; only runtime/tag fields, never raw prompt text (promptSignature sha256). |
| T-31-04 | Tampering | manifest/mirror trust | medium | mitigate | Phase-31 tests import the LIVE hook (~/.claude/hooks/router.mjs); the snapshot.diff guard asserts byte-identity, preventing a drifting/decomposed router between repo and deploy. Tests are not a tamper boundary but guard drift. |
</threat_model>

<verification>
Wave-0 targeted run:
`rtk node --test tests/router.runtime-tagging.test.mjs tests/router.mjs.snapshot.diff.test.mjs tests/router.health.outcome-schema.test.mjs tests/router.cache.test.mjs`
Expected: snapshot.diff PASSES; runtime-tagging (detection/cacheKey/telemetry groups) RED; outcome-schema size assert RED; cache divergence RED. Full suite at wave merge.
</verification>

<success_criteria>
- Three RED test surfaces exist targeting the Phase-31 API: runtime detection precedence, cacheKey cross-runtime divergence, telemetry runtime field.
- Outcome-schema test now expects 16 frozen fields (runtime/epoch) — the enforcement guard for an intentional, non-silent schema bump.
- Mirror-desync guard confirms snapshot == live hook (green proof the lockstep base holds).
- No feature code was written in this plan; feature implementation is deferred to 31-02 (detection + dirs) and 31-03 (cache/telemetry/schema).
</success_criteria>

<output>
Create `.planning/phases/31-runtime-tagging/31-01-SUMMARY.md` when done
</output>

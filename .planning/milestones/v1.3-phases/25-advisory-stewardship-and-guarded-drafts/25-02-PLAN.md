---
phase: 25-advisory-stewardship-and-guarded-drafts
plan: 02
type: execute
wave: 2
depends_on: [25-01]
files_modified:
  - src/steward/draft.mjs
  - tests/router.steward-draft.test.mjs
autonomous: true
requirements: [UX-01, UX-02, UX-03, UX-04, UX-05, UX-06, UX-07, UX-08, UX-09]
must_haves:
  truths:
    - "UX-06: missing-capability remediation follows recommendation → explicit fresh approval → draft artifact → complete preview and never installation/publication."
    - "UX-07: every post-approval draft preview contains exact contained paths, semantic changes, dependencies, conflicts, representative before/after routes, verification, reversibility, and rollback implications."
    - "UX-08: approval authority is exactly draft_file_only and protected capability/routing artifacts remain byte-identical."
  artifacts:
    - path: src/steward/draft.mjs
      provides: "Bounded preview schema and approval-bound draft-only persistence."
      exports: [previewDraft, approveDraftCreation, verifyDraftPreview]
    - path: tests/router.steward-draft.test.mjs
      provides: "Nyquist tests for preview completeness, stale approval, containment, and no publication."
  key_links:
    - from: src/steward/draft.mjs
      to: src/orchestrator/approval.mjs
      via: "bindApproval and verifyApproval over current fingerprint, exact targets/effects, and proposal version."
      pattern: "draft_file_only"
    - from: src/steward/draft.mjs
      to: src/steward/state.mjs
      via: "Writes only content-addressed draft bundles below the steward root."
      pattern: "drafts"
---

<objective>
Add preview-complete remediation drafts whose only mutating authority is creation of a private draft artifact.

Purpose: satisfy UX-06 through UX-08 without sharing any install, activation, or publication path.
Output: one stdlib-only draft module and focused security/integration tests.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-advisory-stewardship-and-guarded-drafts/25-CONTEXT.md
@.planning/phases/25-advisory-stewardship-and-guarded-drafts/25-UI-SPEC.md
@.planning/phases/25-advisory-stewardship-and-guarded-drafts/25-RESEARCH.md
@.planning/phases/25-advisory-stewardship-and-guarded-drafts/25-PATTERNS.md
@.planning/phases/25-advisory-stewardship-and-guarded-drafts/25-01-SUMMARY.md

<interfaces>
From src/orchestrator/approval.mjs:
- bindApproval({capability,args,targets,effects,proposalVersion}) returns a deterministic binding.
- verifyApproval({bound,presented,expected}) fails closed for missing, stale, or mismatched approval.
From src/steward/suggestion.mjs and state.mjs (Plan 25-01):
- selected suggestion fingerprints are semantic and correction/draft storage is confined below the steward root.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Produce bounded, read-only draft proposals for approval</name>
  <read_first>src/orchestrator/approval.mjs, src/registry/activate.mjs, src/steward/suggestion.mjs, src/steward/state.mjs, tests/router.registry-activate.test.mjs</read_first>
  <files>src/steward/draft.mjs, tests/router.steward-draft.test.mjs</files>
  <behavior>
    - Only missing_category and missing_dependency suggestions are draft-eligible.
    - The pre-approval proposal is read-only and contains only the exact scope needed for informed approval: contained targets, semantic-effect tokens, proposal version, draft_file_only effect, and the approval-only warning.
    - Every proposed path resolves beneath the draft root; escapes, malformed records, and unbounded collections fail closed.
    - Preview fingerprint binds the current suggestion, complete semantic payload, exact paths, proposal version, and draft_file_only effect.
  </behavior>
  <action>Write failing tests then implement the read-only proposal branch of `previewDraft` and `verifyDraftPreview`. Per UX-06's locked order, the pre-approval result is a bounded proposal scope, not the completed draft preview: include only contained target paths, semantic-effect tokens, proposal version, `draft_file_only`, and the exact approval-only warning required to bind informed approval. Use fixed keys/tokens rather than invented prose, validate all path containment, sort canonical collections, and fingerprint canonical bytes. This proposal performs zero writes. Non-missing suggestion kinds return a stable ineligible result. Reuse `stableStringify`/SHA-256 and the preview-fingerprint pattern only; do not import registry mutation functions.</action>
  <verify>
    <automated>rtk node --test tests/router.steward-draft.test.mjs --test-name-pattern="preview"</automated>
  </verify>
  <acceptance_criteria>Every approval proposal is deterministic, contained, bounded, read-only, and sufficient to bind exact draft-only approval; invalid or non-missing remediation fails closed.</acceptance_criteria>
  <done>The pre-approval proposal cannot mutate Router authority or expose a completed draft before approval.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind fresh explicit approval to draft-only persistence</name>
  <read_first>src/orchestrator/approval.mjs, src/registry/activate.mjs, src/steward/state.mjs, tests/router.approval.test.mjs, tests/router.control-cli.test.mjs</read_first>
  <files>src/steward/draft.mjs, tests/router.steward-draft.test.mjs</files>
  <behavior>
    - Missing approval returns approval_required and writes nothing.
    - Stale current suggestion/preview or mismatched token fails closed and writes nothing.
    - Exact fresh approval creates one idempotent immutable 0600 draft bundle beneath the steward draft root, then returns its complete UX-07 preview.
    - Creation changes no registry version, active/known-good pointer, release tuple, capability definition, mode map, weights, or routing policy.
  </behavior>
  <action>Implement `approveDraftCreation` by re-deriving the current proposal scope immediately before persistence, binding `{capability,args,targets,effects:['draft_file_only'],proposalVersion}`, and calling existing `verifyApproval` with the freshly derived expected binding. Only an approved result may materialize the complete UX-07 draft bundle and write it content-addressed using the Plan 25-01 durable store discipline; return the complete post-approval preview containing exact paths, semantic changes, dependencies, conflicts, representative before/after routes, verification steps, reversibility, rollback implications, and the exact warning that nothing was installed or published. Add import-deny and before/after protected-byte tests. Never call or import activation, pointer replacement, publication, installer, lifecycle, adapter, or settings mutation code.</action>
  <verify>
    <automated>rtk node --test tests/router.steward-draft.test.mjs</automated>
  </verify>
  <acceptance_criteria>Only fresh exact approval creates a draft and exposes its complete UX-07 preview; all other paths are no-write failures, and the only changed bytes are beneath the steward draft root.</acceptance_criteria>
  <done>UX-06 and UX-08 are enforced through existing approval semantics without granting publication authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Suggestion → draft preview | Advisory data becomes a proposed filesystem change description. |
| Approval token → persistence | User authority crosses into one local write operation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-25-05 | Spoofing | approval binding | high | mitigate | Re-derive expected binding from current suggestion, targets, effect, and version. |
| T-25-06 | Tampering | draft paths | high | mitigate | Resolve and contain all targets below the private draft root before preview/write. |
| T-25-07 | Elevation of Privilege | draft.mjs | critical | mitigate | Effect is draft_file_only; deny mutation-module imports and hash protected artifacts. |
| T-25-08 | Repudiation | immutable draft bundle | medium | mitigate | Content-addressed version and canonical preview fingerprint make the approved payload attributable. |
| T-25-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package install exists; stdlib and current modules only. |
</threat_model>

<verification>
Nyquist: preview and approval behaviors are test-first in one focused file. Run the focused suite and confirm protected hashes before/after every action.
</verification>

<success_criteria>
Users can prepare a complete draft only through explicit fresh approval, and no Phase 25 code can install or publish it.
</success_criteria>

<output>
Create `.planning/phases/25-advisory-stewardship-and-guarded-drafts/25-02-SUMMARY.md` when done.
</output>

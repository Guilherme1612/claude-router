---
phase: 12-incremental-change-detection-and-watcher
plan: "02"
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/registry/build.mjs
  - tests/router.registry-build.test.mjs
autonomous: true
requirements: [REG-03, CHG-01]
must_haves:
  truths:
    - "After every supported mutation, incremental construction and a clean full rebuild return byte-identical registry, diagnostics, summary, and fingerprints."
    - "Incremental acquisition reuses unchanged observations but never forks canonical merge, precedence, conflict, diagnostic, or serialization semantics."
    - "Removal and replacement semantics consume the evidence-gated lifecycle diff without activating or mutating the prompt router."
  artifacts:
    - path: src/registry/build.mjs
      provides: "Shared canonical assembly plus full and incremental registry entry points"
      exports: [assembleRegistry, buildFullRegistry, buildIncrementalRegistry]
    - path: tests/router.registry-build.test.mjs
      provides: "REG-03 full-return parity after every mutation in deterministic sequences"
  key_links:
    - from: src/registry/build.mjs
      to: src/registry/diff.mjs
      via: "incremental observation removal/replacement consumes authoritative lifecycle diff records"
      pattern: "buildIncrementalRegistry"
    - from: src/registry/build.mjs
      to: src/adapters/claude.mjs
      via: "full and changed-root acquisition both feed one canonical assembler"
      pattern: "discoverRoots"
---

<objective>
Guarantee REG-03 by making full and incremental builds share one semantic assembly path while preserving CHG-01 removal and replacement meaning.

Purpose: Make incrementality an acquisition optimization whose result cannot drift from a clean canonical rebuild.
Output: Shared registry assembly, `buildIncrementalRegistry`, and mutation-sequence byte-equivalence evidence.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-incremental-change-detection-and-watcher/12-CONTEXT.md
@.planning/phases/12-incremental-change-detection-and-watcher/12-RESEARCH.md
@.planning/phases/12-incremental-change-detection-and-watcher/12-VALIDATION.md
@.planning/phases/12-incremental-change-detection-and-watcher/12-PATTERNS.md
@.planning/phases/11-canonical-registry-and-runtime-adapters/11-03-SUMMARY.md
@.planning/phases/11-canonical-registry-and-runtime-adapters/11-04-SUMMARY.md
@.planning/phases/11-canonical-registry-and-runtime-adapters/11-05-SUMMARY.md
@.planning/phases/11-canonical-registry-and-runtime-adapters/11-06-SUMMARY.md
@src/registry/build.mjs
@tests/router.registry-build.test.mjs
@src/registry/diff.mjs

<interfaces>
Existing `buildFullRegistry(options)` returns `{ registry, diagnostics, summary }`; `summary` includes record/diagnostic/dispatchable/runtime counts and registry/diagnostics fingerprints. Existing canonical grouping uses `stableCapabilityId`, `mergeGroup`, `annotatePrecedence`, portable diagnostic normalization, and stable sorting. Plan 12-01 provides deterministic snapshot diffs with primary lifecycle events and facets.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the registry suite with per-step full/incremental equivalence</name>
  <files>tests/router.registry-build.test.mjs</files>
  <behavior>
    - After each add, edit, strong rename/move, compound rename/edit, disable, dependency, declared-permission, scope, delete, malformed-to-valid, valid-to-malformed, and access-denied diagnostic step, stable bytes of the complete incremental return equal a fresh full build.
    - Weak structural candidates remain remove-plus-add and precedence changes across global/project records remain identical.
    - Results never contain temporary absolute roots and reversed mutation input ordering does not alter bytes.
  </behavior>
  <action>Extend the existing temporary-root fixture rather than replacing its Phase 11 coverage. Maintain an explicit prior complete observation/snapshot state, mutate one supported dimension per step plus compound cases, obtain the Plan 12-01 diff, call the new incremental entry point, and compare `stableStringify(incrementalReturn)` to `stableStringify(buildFullRegistry(options))` immediately after every step. Compare the entire return value—not only records—so diagnostics, summary counts, precedence, and both fingerprints are covered. Per the resolved permission decision, use declared normalized permission metadata for the permission lifecycle step and include a separate injected access-denial transition that preserves diagnostic evidence without classifying deletion or `permission_changed`.</action>
  <verify>
    <automated>node --test tests/router.registry-build.test.mjs</automated>
  </verify>
  <done>The existing full-build tests remain green and the added REG-03 sequence fails only because shared assembly/incremental construction is not yet implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor to one canonical assembler and implement incremental acquisition</name>
  <files>src/registry/build.mjs</files>
  <behavior>
    - Full and incremental entry points invoke the same canonical validation, grouping, conflict, precedence, diagnostic, summary, sorting, and fingerprint code.
    - Incremental processing applies complete removals/replacements to the prior complete observation set and reparses only dirty roots/artifacts through adapter seams.
    - Candidate construction stays read-only and inactive.
  </behavior>
  <action>Extract the existing post-discovery logic into an exported pure `assembleRegistry` contract accepting complete Claude/Codex observation and diagnostic sets. Keep `buildFullRegistry(options)` as the semantic oracle that discovers all roots then delegates. Add `buildIncrementalRegistry(previous, diff, options)` that validates its prior snapshot, applies diff removals/replacements deterministically, refreshes affected observations through explicit adapter roots, and delegates the complete resulting sets to `assembleRegistry`. Preserve all Phase 11 conflict, scope-precedence, portable diagnostic, count, fingerprint, and `activated: false` behavior; do not add activation, quarantine, service management, prompt-hook reads, or an independent merge implementation.</action>
  <verify>
    <automated>node --test tests/router.registry-schema.test.mjs tests/router.adapters.test.mjs tests/router.registry-diff.test.mjs tests/router.registry-build.test.mjs</automated>
  </verify>
  <done>Every mutation step has byte-identical complete full/incremental output and all Phase 11 registry semantics continue to pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prior snapshot/diff → incremental builder | Cached observations and lifecycle data may be stale, malformed, or incomplete. |
| Adapter observations → canonical assembler | Native runtime artifacts remain untrusted until canonical validation and containment checks pass. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-04 | Tampering | incremental observation merge | high | mitigate | Validate prior/diff contracts, apply complete removals/replacements, and prove full-return equivalence after every mutation. |
| T-12-05 | Information Disclosure | registry diagnostics and fingerprints | high | mitigate | Retain Phase 11 local-path stripping and assert temporary absolute roots never serialize. |
| T-12-06 | Denial of Service | dirty-root reparse boundary | medium | mitigate | Reparse only explicitly dirty bounded roots while canonical assembly remains deterministic over complete logical observations. |
| T-12-SC | Tampering | package installation | low | accept | No package installation; only existing standard-library and local ESM contracts are used. |
</threat_model>

<verification>
Run the focused build test after each task. At Wave 2 completion run `node --test tests/router.registry-schema.test.mjs tests/router.adapters.test.mjs tests/router.registry-diff.test.mjs tests/router.registry-build.test.mjs tests/router.lifecycle.test.mjs` to preserve Nyquist sampling continuity.
</verification>

<success_criteria>
- REG-03 compares and passes on the complete return value after every supported mutation and operational access-denial step.
- Full and incremental entry points share canonical assembly rather than duplicating semantics.
- No candidate activation or prompt-time inventory work is introduced.
</success_criteria>

<output>
Create `.planning/phases/12-incremental-change-detection-and-watcher/12-02-SUMMARY.md` when done.
</output>

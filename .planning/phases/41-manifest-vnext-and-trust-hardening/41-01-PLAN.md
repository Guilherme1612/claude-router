---
phase: 41-manifest-vnext-and-trust-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/registry/contract.mjs
  - src/registry/schema.mjs
  - src/registry/trust.mjs
  - src/cli/router-control.mjs
  - src/lifecycle/router-lifecycle.mjs
  - tests/helpers/inventory-fixture.mjs
  - tests/router.trust-contract.test.mjs
  - tests/router.trust-evidence.test.mjs
  - tests/router.contract-inspection.test.mjs
autonomous: true
requirements:
  - TRUST-01
  - TRUST-02
user_setup: []

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Contract inspection shows evidence_class (explicit/inferred/conflicting/unknown) for every contract field"
    - "New contract fields action, cost, completion, native_invocation appear in CONTRACT_FIELDS and in the inspect output"
    - "Conflicting evidence is a distinct inspectable state from unknown (not collapsed)"
    - "Manifest/plugin/private/learned evidence cannot populate authority-critical fields (permissions, side_effects, risk, reversibility, invocation_kind)"
    - "Untrusted evidence is retained in rejected_evidence with reason code untrusted_evidence_rejected"
    - "Trust hardening runs at build time (contract construction) or dispatch time, never on the prompt hot path"
    - "trust.mjs deploys to both ~/.claude/router/modules/ and ~/.codex/router/modules/ via moduleNames"
  artifacts:
    - src/registry/contract.mjs (CONTRACT_FIELDS extended, envelope evidence_class added)
    - src/registry/trust.mjs (NEW — classifyEvidence, AUTHORITY_CRITICAL_FIELDS, TRUSTED_PROVENANCE)
    - src/registry/schema.mjs (validateCapabilityContract extended for new fields)
    - src/cli/router-control.mjs (fieldProjection extended with evidence_class)
    - tests/router.trust-contract.test.mjs
    - tests/router.trust-evidence.test.mjs
  key_links:
    - "trust.mjs::classifyEvidence ← contract.mjs::envelope() (untrusted rejection integration)"
    - "contract.mjs::CONTRACT_FIELDS ↔ schema.mjs::validateCapabilityContract (field-set invariant, atomic)"
    - "router-lifecycle.mjs::moduleNames ← trust.mjs (deploy list entry)"
---

<objective>
Extend the contract envelope with provenance-class evidence states (TRUST-01) and add an untrusted-evidence policy module (TRUST-02). This is the tracer slice: it touches every registry layer the phase modifies (contract envelope → schema validation → CLI inspection projection) and wires one field end-to-end before expansion.

Purpose: Establish the trust boundary so capability metadata (descriptions, manifests, plugins, private integrations, learned records) cannot create authority, while operators can inspect provenance and status for every contract field.
Output: Extended contract.mjs with evidence_class + 4 new fields; new trust.mjs untrusted-evidence policy; extended schema validation, CLI projection, test fixtures, and deploy list.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-manifest-vnext-and-trust-hardening/41-RESEARCH.md
@.planning/phases/41-manifest-vnext-and-trust-hardening/41-PATTERNS.md

@src/registry/contract.mjs
@src/registry/schema.mjs
@src/registry/eligibility.mjs
@src/cli/router-control.mjs
@src/lifecycle/router-lifecycle.mjs
@tests/helpers/inventory-fixture.mjs
@tests/router.contract-inspection.test.mjs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: TRUST-01 — extend contract envelope with evidence_class + new fields, wire through to inspect output</name>
  <files>
    src/registry/contract.mjs
    src/registry/schema.mjs
    src/cli/router-control.mjs
    tests/helpers/inventory-fixture.mjs
    tests/router.trust-contract.test.mjs
    tests/router.contract-inspection.test.mjs
  </files>
  <read_first>
    src/registry/contract.mjs (CONTRACT_FIELDS at line 4-19, envelope() at line 104-174, validateContractFieldValue at line 69-89, validateCapabilityContract at line 440-494, authoritativeEvidence, DISPATCH_FIELDS at line 28-39)
    src/registry/schema.mjs (validateCapabilityContract delegation, validateEligibility at line 191-225, ELIGIBILITY_GATES at line 49, stableStringify at line 339-341)
    src/cli/router-control.mjs (fieldProjection at line 396-413, evidenceProjection, contractDetailProjection at line 438-464, safeToken at line 167, MAX_VALUE at line 25)
    tests/helpers/inventory-fixture.mjs (contractEvidence at line 160-176, variant overrides at line 184-206, variant-throw guard at line 204-206)
    tests/router.contract-inspection.test.mjs (test patterns, privacy-safe assertions at line 113-114)
  </read_first>
  <behavior>
    - Test 1: buildCapabilityContract with adapter-provenance evidence for a field produces evidence_class='explicit' in the envelope
    - Test 2: buildCapabilityContract with manifest-provenance evidence (confidence >= 8500) produces evidence_class='inferred'
    - Test 3: buildCapabilityContract with two distinct asserted values produces evidence_class='conflicting' (not 'unknown')
    - Test 4: buildCapabilityContract with no eligible evidence produces evidence_class='unknown'
    - Test 5: CONTRACT_FIELDS contains 'action', 'cost', 'completion', 'native_invocation' alongside the existing 14 fields
    - Test 6: contractDetailProjection output includes evidence_class for each projected field
    - Test 7: No evidence item in the projection exposes a raw 'value' key (privacy invariant)
    - Test 8: Existing tests in router.contract-inspection.test.mjs and router.contracts.test.mjs remain green
  </behavior>
  <action>
    ATOMIC INVARIANT (Pitfall 1): update ALL of the following in a single edit pass — CONTRACT_FIELDS array, validateContractFieldValue dispatch, validateCapabilityContract field-set check (line 451), authoritativeEvidence in buildCapabilityContract, DISPATCH_FIELDS set, and contractEvidence() in the test helper. Any partial update breaks the field-set invariant.

    1. Add four new fields to CONTRACT_FIELDS (contract.mjs:4-19): 'action', 'cost', 'completion', 'native_invocation'. Per RESEARCH Open Question 1: 'cost' is a static contract field (per-capability, like 'risk'). Per Open Question 2: 'completion' is a contract field describing the required completion-evidence shape. 'native_invocation' describes the native invocation contract. 'action' describes the action type. Use string-list or enum types matching the existing validateContractFieldValue dispatch pattern (STRING_LIST_FIELDS / ENUM_FIELDS).

    2. Add the four new fields to DISPATCH_FIELDS (contract.mjs:28-39) if they are dispatch-relevant (action, cost, completion, native_invocation are all dispatch-relevant).

    3. Extend envelope() return shape (contract.mjs:161-173) with an 'evidence_class' field. Derive it as follows (per RESEARCH Pattern 1): 'explicit' when provenance is 'adapter' with confidence_basis_points === 10000 (structural_minimum); 'inferred' when provenance is 'manifest'/'correction' with confidence >= inferred_minimum_basis_points (8500); 'conflicting' when assertedValues.size > 1 (Pitfall 2 — currently collapsed to 'unknown' at line 155-156, surface it as a distinct class); 'unknown' when no eligible evidence. Do NOT change the existing 'state' field (known/unknown) — evidence_class is an addition, not a replacement.

    4. Update validateContractFieldValue (contract.mjs:69-89) to handle the four new field types. 'action' and 'cost' as enum or string fields; 'completion' and 'native_invocation' as object/structured fields (follow the existing dispatch by field type — add to STRING_LIST_FIELDS or create a new validation branch if needed).

    5. Update authoritativeEvidence in buildCapabilityContract to populate the new fields from evidence candidates. Follow the existing pattern: evidence for each field flows through envelope() which classifies it.

    6. Extend fieldProjection in router-control.mjs (line 396-413) to surface evidence_class: add `evidence_class: safeToken(value?.evidence_class, 'unknown')` to the return object. The existing contractDetailProjection (line 438-464) iterates all CONTRACT_FIELDS automatically — no change needed there.

    7. Extend contractEvidence() in tests/helpers/inventory-fixture.mjs (line 160-176): add 'action', 'cost', 'completion', 'native_invocation' to the structural object with sensible default values (e.g., action: 'invoke', cost: 'low', completion: { evidence_type: 'exit_code' }, native_invocation: { runtime: 'claude' }). Add new variant flags if needed (e.g., 'conflicting' variant that produces two distinct asserted values for a field). Update the variant-throw guard at line 204-206 to list any new variants.

    8. Create tests/router.trust-contract.test.mjs: import buildCapabilityContract from ../src/registry/contract.mjs, contractEvidence + buildClaudeHeavyProfile from ./helpers/inventory-fixture.mjs, control from ../src/cli/router-control.mjs. Write tests matching the behavior block above. Assert evidence_class values, new field presence in CONTRACT_FIELDS, and privacy-safe projections (no raw value in evidence items).

    9. Extend tests/router.contract-inspection.test.mjs with assertions that new fields appear in the projected fields keys and evidence_class is present in each field projection. Do NOT remove or weaken existing assertions.

    NEVER place validation logic on the prompt hot path (Pitfall 4). All envelope/contract work runs at build time (assembleRegistry → buildCapabilityContract), not in router.mjs.
  </action>
  <verify>
    <automated>rtk node --test tests/router.trust-contract.test.mjs tests/router.contract-inspection.test.mjs tests/router.contracts.test.mjs tests/router.contract-eligibility.test.mjs tests/router.contract-overlays.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - src/registry/contract.mjs CONTRACT_FIELDS array contains 'action', 'cost', 'completion', 'native_invocation' (grep -c "'action'" returns >= 1, same for the other three)
    - envelope() return object contains evidence_class key with value 'explicit', 'inferred', 'conflicting', or 'unknown'
    - A fixture with two distinct asserted values for a field yields evidence_class='conflicting' (not 'unknown')
    - src/cli/router-control.mjs fieldProjection return object contains evidence_class key
    - tests/helpers/inventory-fixture.mjs contractEvidence() structural object includes action, cost, completion, native_invocation keys
    - rtk node --test tests/router.trust-contract.test.mjs exits 0
    - rtk node --test tests/router.contract-inspection.test.mjs exits 0
    - rtk node --test tests/router.contracts.test.mjs exits 0 (regression — field-set invariant holds)
  </acceptance_criteria>
  <done>
    New contract fields (action, cost, completion, native_invocation) are in CONTRACT_FIELDS and visible in inspect output. Evidence_class (explicit/inferred/conflicting/unknown) is a distinct inspectable state per field. All existing contract/eligibility/inspection tests remain green. The field-set invariant (validateCapabilityContract) holds with 18 fields.
  </done>
  <reversibility rating="costly" rationale="Adding contract fields is additive but removing them after deployment breaks the field-set invariant for deployed contracts — requires a schema migration. Adding is safe; removing post-ship is costly." />
</task>

<task type="auto" tdd="true">
  <name>Task 2: TRUST-02 — create trust.mjs untrusted-evidence policy, integrate into envelope, add to deploy list</name>
  <files>
    src/registry/trust.mjs
    src/registry/contract.mjs
    src/lifecycle/router-lifecycle.mjs
    tests/router.trust-evidence.test.mjs
  </files>
  <read_first>
    src/registry/contract.mjs (SAFE_PROVENANCE at line 41, authored rejection at line 122-124, envelope() at line 104-174, CONTRACT_POLICY at line 21-26, threshold logic at line 130-138, reasonToken at line 65-67)
    src/lifecycle/router-lifecycle.mjs (moduleNames at line 384-428, moduleValues flatMap at line 429-431)
    tests/router.contract-eligibility.test.mjs (safeRecord pattern at line 13-23, withField at line 38-49)
    src/registry/trust.mjs (does not exist yet — create new)
  </read_first>
  <behavior>
    - Test 1: classifyEvidence('permissions', { provenance: 'manifest', confidence_basis_points: 10000 }) returns { trusted: false, reason_code: 'untrusted_evidence_rejected' }
    - Test 2: classifyEvidence('permissions', { provenance: 'adapter', confidence_basis_points: 10000 }) returns { trusted: true, reason_code: '' }
    - Test 3: classifyEvidence('permissions', { provenance: 'correction', confidence_basis_points: 10000 }) returns { trusted: true, reason_code: '' }
    - Test 4: classifyEvidence('purpose', { provenance: 'manifest', confidence_basis_points: 8500 }) returns { trusted: true, reason_code: '' } (informational field accepts inferred)
    - Test 5: A manifest claiming permissions: ['elevated'] does NOT reach envelope().value for the permissions field — it appears in rejected_evidence with reason_code 'untrusted_evidence_rejected'
    - Test 6: A plugin/private/learned provenance candidate for side_effects is rejected with 'untrusted_evidence_rejected'
    - Test 7: buildCapabilityContract with untrusted evidence for risk field produces state='unknown', evidence_class='unknown' (no authority granted)
  </behavior>
  <action>
    1. Create src/registry/trust.mjs as a pure-function module (no I/O, no node:fs/node:path — only policy logic). Import stableStringify from './schema.mjs' if needed for canonical comparisons (mirror contract.mjs:1). Follow the frozen-set + reason-token pattern from contract.mjs:41,65-67.

    2. Export AUTHORITY_CRITICAL_FIELDS as a frozen Set containing: 'permissions', 'side_effects', 'risk', 'reversibility', 'invocation_kind'. These fields require structural-minimum evidence (10000 basis points) from trusted provenance only.

    3. Export TRUSTED_PROVENANCE as a frozen Set containing: 'adapter', 'correction'. These are the only provenance sources allowed to populate authority-critical fields.

    4. Export classifyEvidence(field, candidate) returning { trusted: boolean, reason_code: string }. Logic: if field is in AUTHORITY_CRITICAL_FIELDS and candidate.provenance is NOT in TRUSTED_PROVENANCE, return { trusted: false, reason_code: 'untrusted_evidence_rejected' }. If field is in AUTHORITY_CRITICAL_FIELDS and candidate.provenance IS in TRUSTED_PROVENANCE, check confidence_basis_points >= structural_minimum_basis_points (10000) — if below, return { trusted: false, reason_code: 'below_structural_minimum' }. Otherwise return { trusted: true, reason_code: '' }. For informational fields (purpose, triggers, etc.), return { trusted: true, reason_code: '' } (they accept inferred evidence per existing policy).

    5. Integrate into contract.mjs envelope() (line 122-124): replace the single `if (candidate.provenance === 'authored')` check with a call to classifyEvidence(field, candidate). When classifyEvidence returns trusted=false, push the candidate to rejected[] with the returned reason_code (using the existing portableEvidence pattern). This generalizes the authored rejection to cover manifest/plugin/private/learned for authority-critical fields. Import classifyEvidence from './trust.mjs' at the top of contract.mjs.

    6. Add 'registry/trust.mjs' to the moduleNames array in src/lifecycle/router-lifecycle.mjs (line 384-428), placed next to the existing 'registry/contract.mjs' entry at line 389. The moduleValues flatMap at line 429-431 handles deployment to both ownedRoot and codexOwnedRoot automatically (Pitfall 6).

    7. Create tests/router.trust-evidence.test.mjs: import classifyEvidence from ../src/registry/trust.mjs, buildCapabilityContract from ../src/registry/contract.mjs, contractEvidence + buildClaudeHeavyProfile from ./helpers/inventory-fixture.mjs. Write tests matching the behavior block. Build a fixture where a manifest claims permissions: ['elevated'] and assert the permissions field has state='unknown' and the manifest evidence appears in rejected_evidence with reason_code 'untrusted_evidence_rejected' (Pitfall 3 backstop).
  </action>
  <verify>
    <automated>rtk node --test tests/router.trust-evidence.test.mjs tests/router.contracts.test.mjs tests/router.contract-eligibility.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - src/registry/trust.mjs exists and exports classifyEvidence, AUTHORITY_CRITICAL_FIELDS, TRUSTED_PROVENANCE
    - src/registry/contract.mjs imports classifyEvidence from './trust.mjs'
    - classifyEvidence('permissions', { provenance: 'manifest', confidence_basis_points: 10000 }) returns { trusted: false, reason_code: 'untrusted_evidence_rejected' }
    - classifyEvidence('permissions', { provenance: 'adapter', confidence_basis_points: 10000 }) returns { trusted: true, reason_code: '' }
    - A manifest-provenance evidence candidate for 'permissions' appears in rejected_evidence (not in evidence/value) with reason_code 'untrusted_evidence_rejected'
    - src/lifecycle/router-lifecycle.mjs moduleNames array contains 'registry/trust.mjs' (grep "'registry/trust.mjs'" returns a match)
    - rtk node --test tests/router.trust-evidence.test.mjs exits 0
    - rtk node --test tests/router.contracts.test.mjs exits 0 (regression — envelope integration does not break existing contracts)
  </acceptance_criteria>
  <done>
    trust.mjs classifies evidence provenance and prevents untrusted sources (manifest/plugin/private/learned) from populating authority-critical fields. The authored rejection in envelope() is generalized via classifyEvidence. trust.mjs is in the deploy list for both runtimes. Untrusted evidence is retained in rejected_evidence with attributable reason codes.
  </done>
  <reversibility rating="reversible" rationale="New module + single-call integration point. Removing trust.mjs and reverting the envelope() call restores prior behavior." />
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest → contract envelope | Untrusted capability metadata (descriptions, manifests, plugins, private integrations, learned records) crosses into the contract construction path |
| contract envelope → inspection CLI | Resolved contract fields with provenance/freshness/evidence_class are projected for operator inspection |
| build time → deploy | New trust.mjs module deploys to ~/.claude/router/modules/ and ~/.codex/router/modules/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-01 | Elevation of Privilege | contract.mjs envelope() / authority-critical fields | high | mitigate | TRUST-02: classifyEvidence rejects manifest/plugin/private/learned provenance for permissions/side_effects/risk/reversibility/invocation_kind with 'untrusted_evidence_rejected' reason code; authority-critical fields require adapter/correction provenance at structural_minimum (10000 bps) |
| T-41-02 | Tampering | contract.mjs envelope() / conflicting evidence | medium | mitigate | TRUST-01: evidence_class='conflicting' surfaces multi-value conflicts as a distinct state (Pitfall 2); conflicting evidence is not collapsed to 'unknown' |
| T-41-03 | Tampering | contract.mjs / raw value exposure in inspection | medium | mitigate | Existing validateCapabilityContract (line 477) enforces no raw 'value' in evidence items; new fields inherit the same invariant; fieldProjection never exposes raw values |
| T-41-04 | Information Disclosure | router-control.mjs fieldProjection / contract inspection | low | accept | Contract inspection is operator-facing CLI (not prompt path); provenance/evidence_class are metadata, not secrets; existing redaction patterns apply |
| T-41-SC | Tampering | npm/pip installs | low | accept | Phase 41 installs no external packages — stdlib-only, zero supply-chain surface |
</threat_model>

<verification>
- Contract field-set invariant holds: CONTRACT_FIELDS (18 fields) matches Object.keys(contract.fields).sort() in validateCapabilityContract
- Evidence class is inspectable: explicit/inferred/conflicting/unknown are distinct values in fieldProjection output
- Untrusted evidence rejection: manifest/plugin/private/learned evidence for authority-critical fields appears in rejected_evidence, never in evidence/value
- Deploy list: trust.mjs deploys to both runtime roots via moduleNames flatMap
- No regression: existing contract, eligibility, inspection, and overlay tests remain green
- No hot-path impact: all trust hardening runs at build time, not in router.mjs hook
</verification>

<success_criteria>
- TRUST-01: Operators can inspect provenance and explicit/inferred/unknown/stale/conflicting status for every contract field including action, cost, completion, and native-invocation
- TRUST-02: Descriptions, manifests, plugins, private integrations, and learned records are untrusted evidence and cannot populate authority-critical fields (permissions, side_effects, risk, reversibility, invocation_kind)
- All existing contract/eligibility/inspection/overlay tests remain green
- trust.mjs is deployed to both ~/.claude/router/modules/ and ~/.codex/router/modules/
</success_criteria>

<output>
Create `.planning/phases/41-manifest-vnext-and-trust-hardening/41-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `CONTRACT_FIELDS` extended with: `action`, `cost`, `completion`, `native_invocation` (4 new entries in the frozen array)
- `DISPATCH_FIELDS` extended with the 4 new dispatch-relevant fields
- `envelope()` return shape gains `evidence_class` field ('explicit' | 'inferred' | 'conflicting' | 'unknown')
- New module `src/registry/trust.mjs` exporting: `classifyEvidence(field, candidate)`, `AUTHORITY_CRITICAL_FIELDS` (Set), `TRUSTED_PROVENANCE` (Set)
- `fieldProjection()` in router-control.mjs gains `evidence_class` projection key
- `contractEvidence()` in inventory-fixture.mjs gains `action`, `cost`, `completion`, `native_invocation` default fields
- New test files: `tests/router.trust-contract.test.mjs`, `tests/router.trust-evidence.test.mjs`
- New reason tokens: `untrusted_evidence_rejected`, `below_structural_minimum`
- New `moduleNames` entry: `registry/trust.mjs`
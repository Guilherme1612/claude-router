---
phase: 32.1-phase-32-review-closure
plan: 01
subsystem: routing
tags: [runtime-parity, resolver, indexes, node-test]

# Dependency graph
requires:
  - phase: 32-intent-first-routing-mode-map-schema-v4-guard-hole-closure
    provides: runtime-aware resolveSlashRoute and resolve-aware route validation guards
provides:
  - Runtime-scoped command inventories from buildTargetIndexes
  - Index-path PARITY-03 coverage for routeTargetsExist and validateRouteTargets
  - Flat-manifest and empty-runtime-slice backward compatibility coverage
affects: [phase-32.1-review-closure, release-gate-verification]

# Tech tracking
tech-stack:
  added: []
  patterns: [active-runtime command slicing, subprocess runtime isolation, byte-identical hook mirror]

key-files:
  created:
    - tests/router.runtime-index-path.test.mjs
  modified:
    - tests/router.mjs.snapshot
    - /Users/guilherme/.claude/hooks/router.mjs

key-decisions:
  - "Use a non-empty runtime_commands[RUNTIME] slice for command indexes, with the flat commands array as the backward-compatible fallback."
  - "Expose the active runtime on each built index for attribution without changing the existing inventory set shapes."

patterns-established:
  - "Index-path resolver checks use the same active runtime selected at module load as raw-manifest resolution."
  - "Cross-runtime hook tests spawn fresh Node processes because ROUTER_RUNTIME is read once at module load."

requirements-completed: [PARITY-03]

coverage:
  - id: D1
    description: "buildTargetIndexes selects only the active runtime command slice and records runtime attribution."
    requirement: PARITY-03
    verification:
      - kind: unit
        ref: tests/router.runtime-index-path.test.mjs#index path isolates the active claude runtime
        status: pass
      - kind: unit
        ref: tests/router.runtime-index-path.test.mjs#index path isolates the active codex runtime
        status: pass
    human_judgment: false
  - id: D2
    description: "routeTargetsExist and validateRouteTargets cannot treat a Codex-only slash capability as present under Claude, while flat manifests retain their command inventory."
    requirement: PARITY-03
    verification:
      - kind: integration
        ref: node --test tests/router.runtime-index-path.test.mjs
        status: pass
      - kind: integration
        ref: node --test tests/router.phase32-cross-runtime.test.mjs tests/router.schema-v4-routing.test.mjs tests/router.resolve-tie-lint.test.mjs tests/router.mjs.snapshot.diff.test.mjs
        status: pass
    human_judgment: false

# Metrics
duration: 8min
completed: 2026-08-01
status: complete
---

# Phase 32.1 Plan 01: Runtime-aware index-path parity Summary

**Runtime-scoped command indexes now feed resolver guard paths, with Claude/Codex isolation and flat-manifest fallback tests.**

## Performance

- **Duration:** 8 min
- **Started:** 2026-08-01T17:14:04Z
- **Completed:** 2026-08-01T17:22:00Z
- **Tasks:** 2
- **Files modified:** 3 planned artifacts (plus the required deferred-items log)

## Accomplishments

- `buildTargetIndexes(manifest)` now selects a non-empty `runtime_commands[RUNTIME]` slice and exposes `runtime`, while preserving flat-manifest fallback behavior.
- The live hook at `/Users/guilherme/.claude/hooks/router.mjs` remains byte-identical to `tests/router.mjs.snapshot`.
- Added Claude/Codex subprocess coverage proving index-path isolation for `buildTargetIndexes`, `routeTargetsExist`, and `validateRouteTargets`, including empty-slice fallback.

## Task Commits

Each task was committed atomically:

1. **Task 1: Make buildTargetIndexes runtime-aware** - `e6daff3` (feat)
2. **Task 2: Index-path runtime-isolation tests** - `9f6ce23` (test)

**Plan metadata:** skipped (commit_docs disabled in `.planning/config.json`)

## Files Created/Modified

- `tests/router.mjs.snapshot` - Runtime-aware command index construction and runtime attribution.
- `/Users/guilherme/.claude/hooks/router.mjs` - Byte-identical live production mirror.
- `tests/router.runtime-index-path.test.mjs` - Fresh-process Claude/Codex index-path isolation tests.
- `.planning/phases/32.1-phase-32-review-closure-wire-resolver-into-production-hot-pa/deferred-items.md` - Out-of-scope live tie-lint CLI finding.

## Decisions Made

- Runtime-specific command slices are authoritative only when present and non-empty; otherwise the pre-runtime flat command list remains authoritative.
- Runtime attribution is additive on the index object; all existing skill and agent index sets remain unchanged.

## Deviations from Plan

None - plan implementation executed exactly as written. The standalone tie-lint CLI finding was logged as an out-of-scope deferred item and did not affect the plan’s required verification suite.

## Issues Encountered

- The additional `node scripts/resolve-tie-lint.mjs` probe exits 1 against the current live mode-map/manifest because of existing stale resolve targets. That CLI reads inventory files directly and does not load the changed hook/index code; no unrelated inventory changes were made.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

The index-path runtime parity closure is ready for the remaining Phase 32.1 review-closure plans. The live manifest/mode-map stale-target findings remain deferred for the phase-32 release-gate work.

---
*Phase: 32.1-phase-32-review-closure*
*Completed: 2026-08-01*

## Self-Check: PASSED

- Summary file exists.
- Task commits `e6daff3` and `9f6ce23` exist.
- Snapshot and live hook are byte-identical.

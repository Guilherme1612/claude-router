---
phase: 14-deterministic-mapping-activation-and-rollback
plan: "02"
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - src/registry/validate.mjs
  - src/registry/activate.mjs
  - src/registry/watcher.mjs
  - tests/router.registry-activate.test.mjs
  - tests/router.registry-watcher.test.mjs
autonomous: true
requirements: [ACT-01]
must_haves:
  truths:
    - "Only a complete, fresh, fully verified and calibrated candidate activates automatically; every failed or uncertain gate preserves the prior active pointer bytes (D-10)."
    - "A self-contained immutable version is durably complete before one same-directory `active.json` pointer replacement, so readers observe only complete versions (D-11)."
    - "Recovery verifies pointer and version integrity and deterministically restores the latest verified known-good version when necessary (D-12)."
    - "Verified and diagnostic histories are separately bounded while active, fallback, previous, and in-progress versions remain protected (D-13, D-14)."
    - "Rollback verifies an immutable destination and uses the same pointer primitive without rewriting history; success records a privacy-safe audit event and failure changes nothing (D-17, D-19)."
    - "The live background reconciler maps every eligible candidate before producing a trusted complete gate result and calls activation only when every required gate passes (D-02, D-09, D-10)."
  artifacts:
    - path: src/registry/validate.mjs
      provides: Trusted canonical producer for the complete Phase 14 validation and calibration gate result
      exports: [REQUIRED_ACTIVATION_GATES, produceActivationVerification]
    - path: src/registry/activate.mjs
      provides: Immutable bundle publication, verification, atomic pointer replacement, recovery, retention, rollback, and audit primitives
      exports: [DEFAULT_RETENTION_POLICY, writeImmutableVersion, verifyVersion, replaceActivePointer, activateCandidate, recoverActiveVersion, pruneVersionHistory, previewRollback, executeRollback]
    - path: tests/router.registry-activate.test.mjs
      provides: ACT-01 durability-boundary, integrity, recovery, retention, and rollback evidence
    - path: src/registry/watcher.mjs
      provides: Live reconciliation to mapping to validation to automatic activation orchestration
  key_links:
    - from: src/registry/activate.mjs
      to: src/registry/map.mjs
      via: mapping/evidence report and policy fingerprint closed into immutable bundle manifest
      pattern: "mappings.json|evidence.json|policy_fingerprint"
    - from: src/registry/activate.mjs
      to: active.json
      via: single verified same-directory temporary-file rename
      pattern: "renameSync"
    - from: src/registry/watcher.mjs
      to: src/registry/map.mjs
      via: eligible Phase 13 candidates enter deterministic mapping before any optional advisory resolver
      pattern: "mapCandidateRegistry"
    - from: src/registry/watcher.mjs
      to: src/registry/validate.mjs
      via: exact candidate, reconciliation, mapping, calibration, and regression inputs produce one trusted verification result
      pattern: "produceActivationVerification"
    - from: src/registry/watcher.mjs
      to: src/registry/activate.mjs
      via: only a trusted complete passing verification result reaches activateCandidate
      pattern: "activateCandidate"
---

<objective>
Implement verified immutable registry versions, one-pointer atomic activation, deterministic recovery/retention, and pointer-only rollback with full D-10 through D-14, D-17, and D-19 fidelity.

Purpose: Ensure passing candidates become active automatically without ever exposing partial state, while failures and crashes preserve or deterministically recover last-known-good authority.
Output: A trusted gate producer, zero-dependency activation/version store, live watcher orchestration, and focused failure-injection/controller-boundary suites.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-deterministic-mapping-activation-and-rollback/14-CONTEXT.md
@.planning/phases/14-deterministic-mapping-activation-and-rollback/14-RESEARCH.md
@.planning/phases/14-deterministic-mapping-activation-and-rollback/14-PATTERNS.md
@.planning/phases/14-deterministic-mapping-activation-and-rollback/14-01-SUMMARY.md
@src/registry/map.mjs
@src/registry/schema.mjs
@src/registry/watcher.mjs
@src/lifecycle/router-lifecycle.mjs
@tests/router.lifecycle.test.mjs
@tests/router.registry-watcher.test.mjs

<interfaces>
Consume the Plan 14-01 canonical mapping result and fingerprint. Create a self-contained version directory containing `registry.json`, `mappings.json`, `evidence.json`, `verification.json`, and manifest-last `manifest.json`.

Create `REQUIRED_ACTIVATION_GATES`, a frozen `PRODUCTION_GATE_RUNNERS` registry, and `produceActivationVerification(...)` as the sole authoritative producer of `verification.json`, following the updated `14-PATTERNS.md` analogs: `reconcile.mjs` for portable fail-closed verdicts, `router.calibrate.mjs` for bounded local subprocesses, and `watcher.mjs` for fixed production dependency selection. The canonical result must include `schema_version`, verification policy version/fingerprint, exact candidate/reconciliation/mapping/policy input fingerprints, `generated_at`, freshness deadline, sorted required gate IDs, per-gate runner ID/version, authoritative threshold/policy fingerprint, bounded measured output, outcome/evidence fingerprint, overall disposition, and verification fingerprint. Required gate IDs are `incremental_full_equivalence`, `reconciliation_safety`, `mapping_integrity`, `calibration_quality`, `regression_suite`, `privacy`, `latency`, and `token_budget`. Every required gate must run through its exact allowlisted production implementation with matching inputs, `passed` outcome, and current freshness; missing, duplicate, unknown, stale, caller-fabricated, incomplete, substituted-runner, timeout, malformed, or failed evidence yields a canonical non-passing result and cannot reach version creation.

Lock the immutable production runner registry as follows:
- `incremental_full_equivalence`: in-process canonical comparison of the current incremental candidate bytes/fingerprint against a clean full `assembleRegistry` build over the same acquisition snapshot; threshold is exact byte/fingerprint equality.
- `reconciliation_safety`: in-process validation that the Phase 13 report is `eligible`, its candidate fingerprint matches, and it contains no dispatch-blocking verdict; threshold is zero blocking findings.
- `mapping_integrity`: in-process validation of the Plan 14-01 report fingerprint, exact candidate/policy binding, deterministic re-evaluation equality, and target predicates; safe `unmapped` is passing, while `ambiguous` or invalid target evidence fails.
- `calibration_quality`: bounded `spawnSync(process.execPath, ['router.calibrate.mjs'])` from the fixed repository root with `shell: false`, 30-second timeout, bounded captured output, and the repository-owned dynamic threshold/fixture policy reported by `router.calibrate.mjs`; pass requires status 0 and well-formed summary markers.
- `regression_suite`: bounded `spawnSync(process.execPath, ['--test', ...ALLOWLISTED_REGRESSION_TESTS])` with a frozen repository-owned list covering schema, adapters, diff, reconciliation, route targets, and mapping; pass requires status 0 within 120 seconds.
- `privacy`: bounded Node test invocation of the frozen `tests/router.privacy.test.mjs` plus Phase 14 portable-output assertions; pass requires status 0, no forbidden raw/absolute evidence, and policy fingerprint matching the PROJECT privacy contract.
- `latency`: bounded Node test invocation of frozen `tests/router.perf-evolved.test.mjs`; authoritative Phase 14 threshold is the existing PROJECT hard ceiling of every measured prompt route below 100 ms, with measured maximum and sample count recorded.
- `token_budget`: bounded Node test invocation of frozen `tests/router-graphify-integration.test.mjs` and `tests/router.inject.test.mjs`; authoritative current compiled-injection ceiling is 500 estimated tokens, with measured maximum and sample count recorded.

All subprocess runners use `process.execPath`, fixed repository-owned relative paths, fixed arguments/cwd, `shell: false`, sanitized environment, bounded stdout/stderr summaries, and explicit timeout/status/signal/malformed-output reason codes. Production callers pass canonical inputs only—not runner functions, commands, paths, thresholds, or outcomes. Test doubles enter solely through a marked test-only factory/seam that is unavailable from persisted watcher config, CLI, activation API, or normal production construction; production `runRegistryWatcher` always closes over the frozen registry and rejects caller-supplied or substituted runners.

Create and keep stable for Plan 14-03:
- `verifyVersion({ ownedRoot, versionId, expectedFingerprint })`: contained, symlink-safe, manifest/file/schema/fingerprint/compatibility verification with structured verdicts.
- `replaceActivePointer({ ownedRoot, destination, reason, expectedSequence, io })`: the only active-pointer mutation primitive; write/sync/close/reread a unique sibling temp, rename to `active.json`, then sync the containing directory.
- `previewRollback(...)` and `executeRollback(...)`: fingerprinted preview bound to source pointer sequence and destination verification fingerprint; execution requires exact destination confirmation.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify trusted activation gates, immutable integrity, durability, and TOCTOU boundaries</name>
  <files>tests/router.registry-activate.test.mjs, tests/router.registry-watcher.test.mjs</files>
  <behavior>
    - Only complete validation/calibration evidence with candidate, mapping, policy, and verification fingerprints in agreement may reach version creation or pointer mutation (D-10).
    - The trusted verification producer requires the eight named gate IDs from the frozen production runner registry, exact input/freshness/policy fingerprints, bounded measured outputs and thresholds, and fail-closed overall disposition; missing, stale, incomplete, duplicated, fabricated, substituted, timed-out, malformed, or failed evidence cannot create a version or pointer.
    - Version IDs are content-derived `v1-<16 hex prefix>` backed by the full bundle digest; retries are idempotent and a duplicate short ID with different bytes is rejected.
    - Payloads are exclusively staged, each written and synchronized, `manifest.json` is written last with sorted relative filename/size/fingerprint entries and `state: complete`, and directories are synchronized before publication (D-11).
    - A non-mutating capability check plus disposable same-filesystem probe must prove directory synchronization works before pointer publication; unsupported/denied directory sync returns `activation_status: blocked` and `durability_unsupported` without creating or replacing `active.json`.
    - Failure injection at every open/write/sync/close/verify/rename/directory-sync boundary proves pre-swap failures return `durability_failed` and leave the active pointer and existing immutable trees byte-identical; post-rename final pointer-directory sync failure returns `recovery_required`/`pointer_durability_uncertain`, stops activation/rollback/pruning, and never claims success.
    - A named `verification_to_pointer_toctou` fixture replaces or symlink-swaps the destination after verification but before pointer publication; descriptor-anchored reread/fingerprint comparison immediately before rename detects substitution and fails closed without trusting replacement bytes.
    - Malformed pointers, traversal/symlink paths, missing versions, incomplete manifests, mismatched files/fingerprints, unsupported schemas, and no-valid-history states produce structured fail-closed recovery verdicts (D-12).
    - Retention defaults preserve active + previous + eight recent verified versions for 30 days and diagnostics for twenty entries or 14 days; active, fallback, previous, in-progress operation, and rollback-preview destination are protected before sorted pruning (D-13, D-14).
    - Rollback accepts only a reverified immutable compatible version, changes only the pointer, preserves displaced history, rejects stale previews, and records bounded local source/destination/time/outcome/reason audit evidence without prompts/secrets (D-17, D-19).
  </behavior>
  <action>Create isolated temporary-root fixtures with exact recursive base64 tree snapshots, deterministic clock/ID inputs, complete and corrupt bundle builders, pointer/operation references, trusted and fabricated gate inputs, and an injected I/O boundary adapter modeled on lifecycle transaction tests. Follow the updated pattern-map analogs explicitly: Phase 13 fixture/verdict canonicalization from `reconcile.mjs`, the fixed `spawnSync(process.execPath, ...)` calibration boundary from `router.calibrate.mjs`/`router.calibration-codebase.test.mjs`, and the captured dependency/call-order harness from `router.registry-watcher.test.mjs`. Cover each required gate alone and in combination; exact production runner IDs/versions, policy/threshold fingerprints, measured outputs, allowlist selection, bounded output, and timeout/signal/nonzero/malformed codes; missing/duplicate/unknown/stale/input-mismatched/fabricated/substituted-runner/failed evidence; production rejection of runner injection; and test-only local-function overrides that launch no process. Also cover clean bootstrap; safe mapped and safe-unmapped automatic activation through the watcher boundary; quarantined, ambiguous, and failed-candidate preservation; every durability boundary; recovery candidate ordering; separate retention policies; rollback preview/execution; stale pointer sequence; immutable-history preservation; and audit privacy per D-04, D-10 through D-14, D-17, and D-19. Carry the resolved research durability contract and named `verification_to_pointer_toctou` fixture unchanged. Keep tests independent of real Claude/Codex homes.</action>
  <verify>
    <automated>node --test tests/router.registry-activate.test.mjs tests/router.registry-watcher.test.mjs tests/router.calibration-codebase.test.mjs tests/router.lifecycle.test.mjs</automated>
  </verify>
  <done>The focused suite exists and initially demonstrates the missing activation module; all mutation/durability boundaries and required recovery, retention, rollback, integrity, containment, and privacy outcomes are mechanically specified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement trusted gate production and immutable activation primitives</name>
  <files>src/registry/validate.mjs, src/registry/activate.mjs, tests/router.registry-activate.test.mjs</files>
  <behavior>
    - Passing candidates publish a complete immutable version and activate through exactly one pointer replacement.
    - Failed, stale, incomplete, or uncertain gates never mutate the pre-swap active authority.
    - Recovery and rollback select only a currently verified immutable version and emit canonical structured outcomes.
  </behavior>
  <action>Implement `produceActivationVerification` as the sole trusted producer for the versioned canonical gate schema and immutable production runner registry defined in interfaces, explicitly following `reconcile.mjs` canonical fail-closed evidence and `router.calibrate.mjs` bounded subprocess patterns. Implement all eight concrete runners exactly as specified: three in-process exact-input validators and five fixed allowlisted Node subprocess gates with fixed repository paths, commands, timeouts, threshold/policy sources, bounded measurements, and stable error codes. Freeze the registry and runner metadata; validate selected function identity against the required-ID table; production callers cannot supply runners, commands, paths, thresholds, or outcomes. Permit local-function overrides only through the marked test-only factory/seam and make normal production construction reject that option. Bind all outcomes to exact candidate/reconciliation/mapping/policy inputs and freshness, sort/fingerprint evidence, and return fail-closed without accepting caller-authored `verification.json`. Preserve all previously planned immutable-store, directory-durability, TOCTOU, recovery, retention, rollback, and audit behavior.</action>
  <verify>
    <automated>node --test tests/router.registry-activate.test.mjs tests/router.registry-map.test.mjs tests/router.registry-reconcile.test.mjs tests/router.calibration-codebase.test.mjs tests/router.lifecycle.test.mjs</automated>
  </verify>
  <done>ACT-01 passes across normal activation, crash/failure injection, corrupt-state recovery, protected retention, stale rollback, and audit privacy; readers can resolve only an old or new manifest-complete version, and existing immutable bytes never change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire the live watcher through mapping, trusted validation, and automatic activation</name>
  <files>src/registry/watcher.mjs, tests/router.registry-watcher.test.mjs</files>
  <behavior>
    - Every live rebuild still reconciles first; quarantined candidates publish diagnostics and never invoke mapping, validation, or activation.
    - Every eligible candidate invokes `mapCandidateRegistry` before optional advisory resolution, then `produceActivationVerification`, then `activateCandidate` only for a complete fresh passing result (D-02, D-09, D-10).
    - Safe passing mapped and safe-unmapped candidates activate automatically; unmapped records remain in the active registry with no dispatch mapping and may emit a bounded advisory request (D-04). Only ambiguous, unsafe, failed/stale gates, or durability-blocked changes preserve exact prior active pointer/version bytes.
    - Controller restart runs recovery before accepting new activation work and surfaces blocked or recovery-required status without a second pointer mutation.
  </behavior>
  <action>Extend `createRegistryReconciler`/`runRegistryWatcher` as the existing background controller boundary, explicitly following the updated `watcher.mjs` analog: fixed production dependency selection at construction, strict awaited single-flight sequencing, portable failure publication, and baseline advancement only after the required activation/preservation outcome. Production construction imports `mapCandidateRegistry`, `produceActivationVerification`, `activateCandidate`, and recovery defaults directly; it must not accept gate runners, commands, paths, thresholds, or outcomes from config/dependencies. A marked test-only reconciler factory may inject mapper/verifier/activator/recovery local functions for captured call-order tests, but `runRegistryWatcher` cannot select it from persisted config. Preserve Phase 13 candidate/report publication. After eligible reconciliation call mapping synchronously before any optional advisory resolver per D-02/D-09. Treat both `mapped` and safe `unmapped` mapping results as eligible for the frozen trusted gates and automatic activation; for D-04, retain the safe unmapped record in the version registry, omit its dispatch mapping, and optionally publish only a bounded advisory request. Advisory output may trigger only a fresh full pipeline pass. Ambiguous mapping remains non-dispatchable and does not activate. Call `activateCandidate` only for a complete fresh passing trusted result per D-10. On quarantine, ambiguity, missing/stale/failed gate, durability unsupported/failure/uncertainty, mapper/runner exception, or publication error, retain exact prior active pointer/version bytes and structured status. On startup, call recovery before new work; if recovery is blocked or required, do not activate or prune. Add controller-boundary tests for mapped and safe-unmapped automatic activation, strict call order, resolver unavailability, advisory re-entry, ambiguity/failure preservation, production runner-injection rejection, trusted-gate failure, restart recovery, and no prompt-path work.</action>
  <verify>
    <automated>node --test tests/router.registry-watcher.test.mjs tests/router.registry-map.test.mjs tests/router.registry-activate.test.mjs tests/router.registry-reconcile.test.mjs</automated>
  </verify>
  <done>The production watcher executes reconciliation → deterministic mapping → frozen trusted gates → automatic activation in that order. Safe unmapped records activate without dispatch mappings per D-04; optional advisory evidence cannot bypass or reorder the pipeline; ambiguous, unsafe, or non-passing paths preserve last-known-good authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Candidate/verification evidence to immutable store | Untrusted or stale gate evidence attempts to become active authority. |
| Filesystem/version store to active pointer | Local paths, bytes, manifests, and durability outcomes may be tampered with or incomplete. |
| Historical version to rollback | Old content must be reverified without reinterpretation before pointer activation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-14-07 | Tampering | version payloads/manifests and `active.json` | high | mitigate | Contained symlink-safe paths, exclusive creation, sorted manifest-last closure, SHA-256 file/bundle verification, temp reread, sequence check, and atomic same-directory rename. |
| T-14-07A | Tampering | verification-to-pointer TOCTOU interval | high | mitigate | Named replacement/symlink-swap injection; descriptor-anchored no-follow containment and complete manifest/payload rehash immediately before pointer rename; abort on identity or fingerprint change. |
| T-14-08 | Elevation of Privilege | activation gate | high | mitigate | Freeze and identity-check the eight allowlisted production runners with fixed commands/threshold sources; reject caller outcomes, substituted runners, and production injection; require complete fresh bound fingerprints before mutation. |
| T-14-09 | Spoofing | rollback destination | high | mitigate | Resolve only exact contained immutable version IDs and reverify schema, compatibility, manifest, and full digest immediately before pointer swap. |
| T-14-10 | Denial of Service | retention/recovery | medium | mitigate | Separate bounded histories, deterministic protected-set calculation, count/age pruning, and explicit no-valid-history verdict rather than destructive guessing. |
| T-14-11 | Repudiation | activation/rollback outcomes | medium | mitigate | Append stable bounded audit events with source, destination, pointer sequence, reason, time, outcome, and fingerprints. |
| T-14-12 | Information Disclosure | manifests/verdicts/audit | medium | mitigate | Persist only portable relative paths and bounded structured reasons; prohibit raw prompts, secrets, and absolute runtime roots. |
</threat_model>

<verification>
- Focused activation plus lifecycle regression runs after each task.
- Controller-boundary tests prove live strict ordering and safe automatic activation/preservation.
- Failure injection covers each mutation boundary and exact recursive snapshots prove preservation.
- The named TOCTOU replacement/symlink-swap fixture runs for forward activation and rollback.
- Full Phase 14 mapping/activation suites pass before handing contracts to the CLI plan.
</verification>

<success_criteria>
- ACT-01 is executable and fully automated.
- D-10 through D-14, D-17, and D-19 are implemented without deferred behavior.
- Version publication, activation, recovery, retention, and rollback preserve immutable/atomic authority boundaries.
</success_criteria>

## Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Atomically activate or restore verified registry versions | 14-02 | COVERED | Immutable bundle and pointer-only authority. |
| REQ | ACT-01 | Atomic version pointer and rollback | 14-02 | COVERED | Tasks 1-2. |
| RESEARCH | Activation architecture | Trusted gate producer, live watcher pipeline, resolved directory-sync contract, durability boundaries, TOCTOU, recovery, protected retention, stale preview, audit privacy | 14-02 | COVERED | Tasks 1-3. |
| CONTEXT | D-10–D-14 | Automatic gates, immutable store, recovery, dual retention | 14-02 | COVERED | Tasks 1-2. |
| CONTEXT | D-17, D-19 | Immutable verified rollback and private audit | 14-02 | COVERED | Tasks 1-2. |

## Artifacts this phase produces

This plan produces `src/registry/validate.mjs`, `src/registry/activate.mjs`, live `src/registry/watcher.mjs` orchestration, and activation/watcher tests. Together with Plans 14-01 and 14-03, the phase produces deterministic mapping evidence, a trusted complete gate result, automatic live activation, immutable verified versions with atomic pointer activation, recovery/retention/rollback primitives, and a dual-runtime operator CLI.

<output>
Create `.planning/phases/14-deterministic-mapping-activation-and-rollback/14-02-SUMMARY.md` when done.
</output>

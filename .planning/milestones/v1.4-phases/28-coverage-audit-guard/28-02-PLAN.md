---
phase: 28-coverage-audit-guard
plan: 02
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - build-manifest.mjs
  - tests/router.coverage-audit.test.mjs
  - /Users/guilherme/.claude/hooks/router.mjs
  - tests/router.freshness.test.mjs
autonomous: true
requirements: [COV-04, COV-05]

must_haves:
  truths:
    - "Strict coverage exits non-zero only when the published report contains unacknowledged gaps (COV-04)."
    - "A missing, older, or unreadable report adds one fixed reminder without suppressing valid route context (COV-05)."
    - "Coverage freshness never rebuilds, parses the report, blocks the prompt, or changes the hook's exit-zero fail-open boundary."
  artifacts:
    - path: "build-manifest.mjs"
      provides: "Strict coverage CLI exit semantics after report publication"
      contains: "--strict-coverage"
    - path: "/Users/guilherme/.claude/hooks/router.mjs"
      provides: "Metadata-only coverage freshness check and reminder composition"
      exports: ["checkCoverageFreshness"]
    - path: "tests/router.freshness.test.mjs"
      provides: "Missing/older/equal/newer/error and composition regression coverage"
  key_links:
    - from: "build-manifest.mjs"
      to: "coverage-report.json"
      via: "strict decision reads the just-created report result"
      pattern: "unacknowledged_gaps"
    - from: "/Users/guilherme/.claude/hooks/router.mjs"
      to: "coverage-report.json"
      via: "existence and mtime checks only"
      pattern: "checkCoverageFreshness"
    - from: "/Users/guilherme/.claude/hooks/router.mjs"
      to: "hookSpecificOutput.additionalContext"
      via: "existing final context composer"
      pattern: "filter\\(Boolean\\).*join"
---

<objective>
Turn the Phase 28 report into an enforceable strict builder gate and a fail-open prompt-hook freshness reminder, covering COV-04 and COV-05.

Purpose: Make unacknowledged gaps CI-detectable while keeping prompt handling fast and non-blocking.
Output: Strict CLI behavior, metadata-only hook freshness composition, and focused subprocess tests.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-coverage-audit-guard/28-CONTEXT.md
@.planning/phases/28-coverage-audit-guard/28-RESEARCH.md
@.planning/phases/28-coverage-audit-guard/28-VALIDATION.md
@.planning/phases/28-coverage-audit-guard/28-PATTERNS.md
@.planning/phases/28-coverage-audit-guard/28-01-SUMMARY.md
@build-manifest.mjs
@tests/router.coverage-audit.test.mjs
@tests/router.freshness.test.mjs
@/Users/guilherme/.claude/hooks/router.mjs

<interfaces>
Consume `auditCoverage(...).unacknowledged_gaps` from Plan 28-01; forward diagnostics and reverse `gap` records are never baseline-suppressed.
`checkCoverageFreshness(manifestPath, reportPath)` returns `{status:"fresh"}` or `{status:"missing"|"stale"|"error", reminder:COVERAGE_REMINDER}`.
Freshness is ordering-only: the report is fresh when `report.mtimeMs >= manifest.mtimeMs`.
The reminder is appended through the existing `additionalContext` composition and must coexist with route, capsule, and manifest-staleness context.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce strict coverage after report publication</name>
  <files>build-manifest.mjs, tests/router.coverage-audit.test.mjs</files>
  <behavior>
    - Normal mode exits zero while still reporting gaps.
    - `--strict-coverage` exits non-zero when forward diagnostics or unacknowledged reverse gaps exist.
    - Valid explicit acknowledgements prevent only their matching reverse gaps from failing strict mode.
    - Stale acknowledgements remain visible warnings but do not fail strict mode.
    - The report exists and is complete before any non-zero exit status is observed.
  </behavior>
  <action>Extend the Plan 28-01 builder subprocess matrix. Parse `process.argv.includes('--strict-coverage')` once and, only after the atomic report rename, set `process.exitCode = 1` when the returned audit has unacknowledged gaps. Do not force-exit, add a CI provider workflow, or let stale baseline entries hide forward target failures. Preserve the independent Phase 27 mode-map size failure so either gate can set the eventual non-zero status.</action>
  <verify>
    <automated>rtk node --test tests/router.coverage-audit.test.mjs tests/router.build-manifest.test.mjs</automated>
  </verify>
  <done>The subprocess matrix proves normal/strict, acknowledged/unacknowledged, forward/reverse, stale-baseline, report-before-failure, and existing size-gate behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compose metadata-only coverage freshness into the fail-open hook</name>
  <files>/Users/guilherme/.claude/hooks/router.mjs, tests/router.freshness.test.mjs</files>
  <behavior>
    - Missing report returns the fixed one-line reminder.
    - Report older than manifest returns the same reminder.
    - Equal or newer report returns fresh with no reminder.
    - Missing manifest or stat failure never throws or blocks and follows the documented fail-open result.
    - A stale reminder appends to, rather than replaces, a valid route/context result.
    - Hook subprocess exits zero and never emits a block decision for every freshness case.
  </behavior>
  <action>Add the smallest separate exported `checkCoverageFreshness` beside `checkFreshness`, defaulting the report to the same router directory as the manifest. Use only `existsSync`/`statSync`; do not parse coverage JSON, execute the builder, import the audit module, or add network work to the prompt path. Call the helper in `main()` and append its fixed one-line reminder through the existing final context composition so routing remains unchanged. Extend the existing freshness and hook subprocess tests for missing, older, equal/newer, stat error, coexistence with route context, exit zero, and absence of a block decision.</action>
  <verify>
    <automated>rtk node --test tests/router.freshness.test.mjs tests/router.failopen.test.mjs</automated>
  </verify>
  <done>COV-05 cases pass, valid route context survives stale evidence, and the hook retains its fail-open exit-zero contract with metadata-only overhead.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| builder CLI/config → process status | Local flags and baseline policy determine whether CI accepts coverage. |
| filesystem metadata → prompt hook | Missing, stale, or unreadable evidence crosses into always-on prompt handling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-01 | Tampering | strict coverage decision | high | mitigate | Fail on every unacknowledged reverse gap and forward diagnostic; baseline matching remains exact and reverse-only. |
| T-28-06 | Tampering | configured report/baseline paths | low | accept | Paths come only from trusted local CLI/environment configuration and report content never selects a write path. |
| T-28-04 | Denial of Service | `checkCoverageFreshness` and `main()` | high | mitigate | Catch metadata errors, compose a fixed reminder, never rebuild/parse/audit, and retain exit zero with no block decision. |
</threat_model>

<verification>
Run `rtk node --test tests/router.coverage-audit.test.mjs tests/router.freshness.test.mjs tests/router.build-manifest.test.mjs`, then `rtk node --test tests/router.coverage.test.mjs tests/router.route-targets.test.mjs tests/router.failopen.test.mjs`, then the phase gate `rtk node --test tests/*.test.mjs`.
</verification>

<success_criteria>
- COV-04: strict mode is a deterministic CI command that fails only for unacknowledged gaps and always leaves its report.
- COV-05: missing/older/error evidence yields one line while prompt routing and exit-zero behavior continue unchanged.
- The hook performs metadata checks only; no new dependency or parallel coverage engine exists.
- All focused Phase 28 tests pass; full-suite results are recorded honestly, including any pre-existing unrelated failures.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | CI-gated and fail-open classified coverage | 28-02 | COVERED | Strict builder plus hook reminder
REQ | COV-04 | Strict failure with explicit baseline | 28-02 | COVERED | Task 1
REQ | COV-05 | Missing/stale report reminder | 28-02 | COVERED | Task 2
RESEARCH | — | Report always written before strict failure | 28-02 | COVERED | Task 1
RESEARCH | — | Freshness by mtime ordering, not age | 28-02 | COVERED | Task 2
RESEARCH | — | No audit/parse/rebuild on hot path | 28-02 | COVERED | Task 2
CONTEXT | discretion | Preserve fail-open and existing composition | 28-02 | COVERED | Task 2
</source_audit>

<output>
Create `.planning/phases/28-coverage-audit-guard/28-02-SUMMARY.md` when done.
</output>

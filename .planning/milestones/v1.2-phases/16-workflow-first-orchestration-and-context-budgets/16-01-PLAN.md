---
phase: 16-workflow-first-orchestration-and-context-budgets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/orchestrator/transitions.mjs
  - tests/router.workflow-orchestrator.test.mjs
autonomous: true
requirements: [ORC-01]
nyquist_compliant: true
must_haves:
  truths:
    - "D-01/D-02: authoritative Phase 15 workflow evidence is resolved before capabilities, while complete explicit instructions may choose only a valid, gate-safe, non-terminal transition."
    - "D-03/D-04: materially tied transitions produce exactly one smallest clarification and invalid, gated, terminal, interrupted, gap, and milestone transitions fail closed with stable reasons."
    - "D-05/D-06/D-07/D-08: this plan exposes one workflow outcome token but does not inspect, rank, merge, or invoke capabilities, dependencies, explicit capability requests, or hooks."
    - "D-09/D-10/D-11/D-12/D-13: transition policy remains pure and context-free; declared context contracts, accounting, summary reuse, and persistence/hot-path work are not implemented here."
  artifacts:
    - path: src/orchestrator/transitions.mjs
      provides: "Canonical states, transition matrix, gate evaluation, explicit precedence, tie clarification, and stable workflow outcomes"
    - path: tests/router.workflow-orchestrator.test.mjs
      provides: "ORC-01 transition and negative-guarantee behavioral matrix"
  key_links:
    - from: src/orchestrator/transitions.mjs
      to: src/context/resolve.mjs
      via: "consumes Phase 15 JSON-ready status, dispatch eligibility, action, position, and reason semantics without mutation"
    - from: tests/router.workflow-orchestrator.test.mjs
      to: src/orchestrator/transitions.mjs
      via: "permutation, gate, terminal, ambiguity, and workflow-family fixtures"
---

<objective>
Implement the canonical workflow transition policy required by ORC-01 and D-01 through D-04, while preserving the downstream boundaries in D-05 through D-13.

Purpose: Make the next valid workflow transition a deterministic, independently testable prerequisite for every later capability decision.
Output: `src/orchestrator/transitions.mjs` and the first workflow-orchestrator behavioral matrix.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-workflow-first-orchestration-and-context-budgets/16-CONTEXT.md
@.planning/phases/16-workflow-first-orchestration-and-context-budgets/16-RESEARCH.md
@.planning/phases/15-context-capsules-and-workflow-state-recovery/15-CONTEXT.md
@.planning/phases/15-context-capsules-and-workflow-state-recovery/15-VERIFICATION.md
@docs/superpowers/specs/2026-07-14-dual-runtime-auto-updating-router-design.md
@docs/superpowers/plans/2026-07-14-dual-runtime-auto-updating-router-implementation.md
@src/context/resolve.mjs
@tests/router.context-resume.test.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify canonical states, transitions, gates, and terminal safety</name>
  <read_first>.planning/phases/16-workflow-first-orchestration-and-context-budgets/16-CONTEXT.md, .planning/phases/16-workflow-first-orchestration-and-context-budgets/16-RESEARCH.md, .planning/phases/15-context-capsules-and-workflow-state-recovery/15-VERIFICATION.md, src/context/resolve.mjs, tests/router.context-resume.test.mjs</read_first>
  <files>tests/router.workflow-orchestrator.test.mjs, src/orchestrator/transitions.mjs</files>
  <behavior>
    - Brainstorming to design approval to implementation planning, GSD discuss/plan/execute/verify, interrupted execution resume, verification-gap closure, and milestone closeout each expose only evidence-valid next transitions.
    - Missing gates, verification gaps, terminal closure, invalid current state, and dependency-unsafe evidence return stable non-dispatchable reason codes.
    - Equivalent evidence and permuted policy/candidate inputs produce byte-equivalent outcomes.
    - Transition evaluation does not read or inspect registry records, capability names, MCPs, tools, or prompt lexical similarity.
  </behavior>
  <action>Write the failing behavioral matrix first, then implement frozen/versioned canonical state and transition records plus `nextValidTransitions` using Phase 15 resolved evidence. Per D-01 evaluate authoritative workflow position/status/freshness and transition preconditions before any downstream concern. Per D-04 encode the named workflow families, approval gates, interrupted execution, verification-gap routing, terminal closure, dependency safety, and milestone boundaries as data-driven predicates with stable IDs and reason codes. Return sorted JSON-ready outcomes with `status`, `dispatch_eligible`, `reason_code`, and bounded candidate facts. Keep the module pure, standard-library-only, and free of registry/capability imports per D-05 through D-08; do not add context loading or persistence per D-09 through D-13.</action>
  <verify><automated>node --test tests/router.workflow-orchestrator.test.mjs</automated></verify>
  <acceptance_criteria>All workflow families and negative gates are executable; permutation fixtures are byte-stable; instrumented inputs prove no capability or lexical selection occurs.</acceptance_criteria>
  <done>D-01 and D-04 have a deterministic transition-policy contract that fails closed before capability selection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resolve explicit precedence and exactly-one clarification</name>
  <read_first>src/orchestrator/transitions.mjs, tests/router.workflow-orchestrator.test.mjs, src/context/resolve.mjs, .planning/phases/16-workflow-first-orchestration-and-context-budgets/16-CONTEXT.md</read_first>
  <files>tests/router.workflow-orchestrator.test.mjs, src/orchestrator/transitions.mjs</files>
  <behavior>
    - A complete explicit instruction selects a matching valid transition and supersedes stale intent metadata without bypassing gates, terminal closure, or dependency safety.
    - An incomplete explicit instruction or materially different tied transitions returns exactly one deterministic smallest question and remains dispatch-ineligible.
    - Lexical ordering never decides a material tie; identical semantic transition duplicates collapse deterministically.
    - Outputs contain no raw prompt, prior goal prose, capability selection, or state mutation.
  </behavior>
  <action>Extend tests first, then add the exported workflow-selection operation (or equivalently named transition resolver) that consumes `nextValidTransitions` output and bounded explicit intent. Per D-02 allow a complete explicit instruction to supersede stale capsule intent only after the transition passes every gate and safety predicate. Per D-03 collapse semantically identical candidates, but return one focused question for materially distinct ties without lexical tie-breaking. Preserve the stable outcome algebra established by Phase 15 and expose a single selected workflow/transition token for Plan 16-02. Keep all results side-effect-free per D-13.</action>
  <verify><automated>node --test tests/router.workflow-orchestrator.test.mjs tests/router.context-resume.test.mjs</automated></verify>
  <acceptance_criteria>Explicit valid selection, stale supersession, incomplete intent, tied transition, terminal, and gated cases all have exact stable outcomes; only one selected outcome is dispatch eligible.</acceptance_criteria>
  <done>D-02 and D-03 complete the workflow-first token consumed by Plan 16-02 without weakening D-04 safety.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Risk | Mitigation |
|---|---|---|
| Phase 15 evidence to transition policy | Spoofed or stale state could unlock an invalid action | Validate status/position/freshness shape and gate every transition. |
| Explicit instruction to selected transition | User text could bypass approval, terminal, or dependency gates | Match only complete intent to already-valid transitions. |
| Ambiguous candidates to dispatch | Incidental ordering could choose a materially different action | Canonical deduplication plus one non-dispatchable clarification. |
</threat_model>

<verification>
Run `node --test tests/router.workflow-orchestrator.test.mjs tests/router.context-resume.test.mjs`, then `node --test tests/*.test.mjs` and `git diff --check`. Inspect imports to prove `transitions.mjs` has no registry, filesystem, deployment-hook, or evolution dependency.
</verification>

<success_criteria>
- ORC-01 begins with one deterministic, gate-safe workflow transition outcome.
- Explicit intent never bypasses gates or terminal closure, and ambiguity yields exactly one question without dispatch.
- Phase 17 hot-path compilation, telemetry, persistence, and deployment files remain untouched.
</success_criteria>

## Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Best valid workflow is selected first | 16-01 | COVERED | Canonical transition token. |
| REQ | ORC-01 | Workflow selection precedes capabilities | 16-01 | COVERED | Tests prohibit capability inspection. |
| RESEARCH | Transition matrix, explicit precedence, stable outcomes | 16-01 | COVERED | Tasks 1-2. |
| CONTEXT | D-01–D-04 | Precedence, clarification, and gates | 16-01 | COVERED | Direct implementation. |
| CONTEXT | D-05–D-13 | Downstream closure/context and purity boundaries | 16-01 | COVERED | Explicit negative boundary; implemented in Plans 16-02/03. |

<output>Create `.planning/phases/16-workflow-first-orchestration-and-context-budgets/16-01-SUMMARY.md` when done.</output>

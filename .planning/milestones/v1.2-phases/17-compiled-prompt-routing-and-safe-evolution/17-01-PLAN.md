---
phase: 17-compiled-prompt-routing-and-safe-evolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/prompt/compile-index.mjs
  - src/context/prompt-route.mjs
  - tests/router.compiled-index.test.mjs
autonomous: true
requirements: [REL-01]
nyquist_compliant: true
must_haves:
  truths:
    - "D-01/D-04: prompt routing reads one compact immutable version plus one bounded fresh capsule, and validation uses bounded metadata and fingerprints only."
    - "D-02/D-03: invalid active state fails closed to a verified compatible known-good version, or to a bounded non-dispatchable clarification/diagnostic when none exists."
    - "REL-01: the compiled seam performs no inventory scan, registry build, history replay, external model call, or uncompiled slow-path routing."
  artifacts:
    - path: src/prompt/compile-index.mjs
      provides: "Bounded compiled-index loader, compatibility validation, and known-good fallback resolution"
    - path: src/context/prompt-route.mjs
      provides: "Live prompt-path integration of compiled state and fresh workflow capsules"
    - path: tests/router.compiled-index.test.mjs
      provides: "Positive, corruption, compatibility, fallback, and forbidden-hot-path regression matrix"
  key_links:
    - from: src/context/prompt-route.mjs
      to: src/prompt/compile-index.mjs
      via: "single bounded compiled lookup before dispatch inspection"
    - from: src/prompt/compile-index.mjs
      to: src/registry/activate.mjs
      via: "atomic active pointer and verified known-good metadata contract"
    - from: src/context/prompt-route.mjs
      to: src/context/capsule.mjs
      via: "selected-workflow capsule freshness witness"
---

<objective>
Connect a compact compiled routing projection to the live prompt seam while preserving D-01 through D-04 and REL-01.

Purpose: Make every prompt route deterministic and locally bounded without permitting an inventory scan, registry rebuild, history replay, external classification call, or silent uncompiled fallback.
Output: A compiled-index loader, prompt-route integration, and focused behavioral suite.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-compiled-prompt-routing-and-safe-evolution/17-CONTEXT.md
@.planning/phases/17-compiled-prompt-routing-and-safe-evolution/17-RESEARCH.md
@src/context/prompt-route.mjs
@src/context/capsule.mjs
@src/context/sources.mjs
@src/registry/activate.mjs
@tests/router.workflow-orchestrator.test.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the bounded compiled-index loader</name>
  <files>src/prompt/compile-index.mjs, tests/router.compiled-index.test.mjs</files>
  <behavior>
    - A valid atomic pointer selects one immutable version whose schema, freshness, compatibility, payload hash, policy version, and capsule contract are verified with bounded reads.
    - Missing, stale, corrupt, incomplete, or incompatible active state selects only the most recent verified compatible known-good version; candidate and quarantined versions are ineligible.
    - With no eligible version, the result is structured, bounded, non-dispatchable, and contains either one focused clarification or an actionable diagnostic.
  </behavior>
  <action>Write failing node:test cases first, then implement `src/prompt/compile-index.mjs` using Node standard-library filesystem and SHA-256 conventions already present in `src/context/sources.mjs`. Return the established structured routing outcome shape. Resolve only the explicit active pointer and bounded known-good metadata; do not enumerate directories. Enforce D-02 fallback eligibility and D-03 terminal behavior, and validate D-04 freshness/compatibility without importing or invoking the registry builder.</action>
  <verify>
    <automated>node --test tests/router.compiled-index.test.mjs</automated>
  </verify>
  <done>The loader deterministically accepts verified active state, rejects every invalid-state class, never selects candidate/quarantined state, and returns a bounded non-dispatchable outcome when no verified compatible version exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate compiled state and fresh capsules into the live prompt seam</name>
  <files>src/context/prompt-route.mjs, tests/router.compiled-index.test.mjs</files>
  <behavior>
    - The prompt path reads only the selected compiled projection and the bounded fresh capsule for the selected workflow.
    - Explicit override, stale capsule, ambiguity, and no-compatible-index outcomes preserve Phase 16 dispatch semantics and reason codes.
    - No compiled version means D-03 non-dispatchable output, not invocation of the uncompiled orchestrator path.
  </behavior>
  <action>Extend tests before production wiring. Integrate the loader into `src/context/prompt-route.mjs` at the live `main(payload)` / decision-inspection seam. Preserve explicit-override precedence and capsule freshness checks inherited from Phases 15–16. Per D-01, ensure the hot path has no registry build, inventory traversal, history replay, or external model path; per D-03, do not silently invoke the full orchestrator when compiled state is unavailable.</action>
  <verify>
    <automated>node --test tests/router.compiled-index.test.mjs tests/router.workflow-orchestrator.test.mjs</automated>
  </verify>
  <done>Live routing consumes the verified compact index plus selected fresh capsule, while all non-dispatchable and explicit-override behaviors remain byte-stable with the inherited routing contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove the hot path remains compact and scan-free</name>
  <files>tests/router.compiled-index.test.mjs</files>
  <behavior>
    - Instrumented routing observes only explicitly addressed index, pointer, metadata, and capsule paths.
    - Registry-build, directory-enumeration, external-model, and history-replay sentinels are never called.
    - Corruption and fallback branches remain bounded and deterministic.</behavior>
  <action>Add explicit-negative regression tests around filesystem and dependency seams so D-01 and REL-01 are executable contracts rather than implementation assumptions. Assert exact allowed read paths and stable structured outcomes for active, known-good, and no-compatible-index branches. Keep the test hermetic with temporary directories and direct Node ESM imports.</action>
  <verify>
    <automated>node --test tests/router.compiled-index.test.mjs</automated>
  </verify>
  <done>The focused suite proves every prompt-path branch is compact, bounded, deterministic, and free of prohibited slow-path work.</done>
</task>

</tasks>

<verification>
Run `node --test tests/router.compiled-index.test.mjs tests/router.workflow-orchestrator.test.mjs`, then `node --test tests/*.test.mjs` at the wave boundary.
</verification>

<success_criteria>
- D-01 through D-04 are covered by executable tests and production wiring.
- Prompt routing never scans or rebuilds state and never calls an external model.
- Invalid compiled state fails closed without a silent slow path.
</success_criteria>


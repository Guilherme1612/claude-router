---
phase: 26-coherent-publication-and-dual-runtime-release
plan: 06
type: execute
wave: 4
depends_on: [26-04]
files_modified:
  - src/context/prompt-route.mjs
  - src/registry/watcher.mjs
  - tests/router.phase26-authority.test.mjs
autonomous: true
requirements: [REL-09]
must_haves:
  truths:
    - "Every automatic mutation still requires exact fresh approval and existing safety gates."
    - "Missing, stale, or mismatched approval produces no byte change."
    - "Suggestion corruption suppresses advice only and preserves verified routing."
  artifacts:
    - "tests/router.phase26-authority.test.mjs"
  key_links:
    - "Watcher mutation entry points retain the existing approval verifier."
    - "Prompt route treats recommendation projection as optional but dispatch projection as verified authority."
---

<objective>
Prove the complete tuple does not broaden automatic authority and recommendation failures remain fail-open.

Purpose: Preserve user control and last-known-good routing through the schema extension.
Output: Approval/no-write and recommendation-failure integration evidence.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-coherent-publication-and-dual-runtime-release/26-CONTEXT.md
@.planning/phases/26-coherent-publication-and-dual-runtime-release/26-04-SUMMARY.md
@src/context/prompt-route.mjs
@src/registry/watcher.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve exact approval and no-write safety across tuple mutations</name>
  <files>src/registry/watcher.mjs, tests/router.phase26-authority.test.mjs</files>
  <behavior>
    - Missing, stale, or mismatched approval blocks publish/activate mutation with byte-identical state.
    - Exact fresh approval still passes the established safety/verifier/canary sequence.
    - No Phase 26 path grants new install, delete, disable, merge, archive, rewrite, activate, or publish authority.
  </behavior>
  <action>Exercise each tuple mutation entry through the existing approval and safety gates. If new tuple metadata bypasses approval binding, extend the existing approval subject fingerprint rather than adding another authority mechanism. Snapshot owned state around all denied cases.</action>
  <verify>
    <automated>rtk node --test tests/router.phase26-authority.test.mjs tests/router.approval.test.mjs tests/router.safety-release.test.mjs</automated>
  </verify>
  <done>Denied mutations are byte-identical no-ops and an exact approved mutation still uses every existing gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep recommendation-only failures fail-open</name>
  <files>src/context/prompt-route.mjs, tests/router.phase26-authority.test.mjs</files>
  <behavior>
    - Missing, corrupt, stale, or mismatched suggestion reference suppresses the notice only.
    - Verified active routing continues; corrupt active dispatch falls back only to verified known-good.
    - No recommendation failure mutates state.
  </behavior>
  <action>Keep the suggestion projection optional at the final rendering boundary while dispatch remains tied to verified tuple eligibility. Test recommendation failure injection against active and known-good routes plus protected-tree byte snapshots. Do not catch or downgrade dispatch-integrity failures.</action>
  <verify>
    <automated>rtk node --test tests/router.phase26-authority.test.mjs</automated>
  </verify>
  <done>Optional advice fails silently without changing routing authority or filesystem state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| approval artifact -> mutation | User authorization must bind exact fresh candidate bytes. |
| optional advice -> prompt output | Advice must not acquire dispatch or mutation authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-13 | Spoofing | approval binding | critical | mitigate | Existing exact candidate fingerprint, freshness, and one-time gate. |
| T-26-14 | Elevation of Privilege | suggestion projection | high | mitigate | Render-only optional projection; verified dispatch authority remains separate. |
</threat_model>

<verification>
Run Phase 26 authority with existing approval and safety-release tests.
</verification>

<success_criteria>
REL-09 passes without new authority surfaces or recommendation-induced routing outages.
</success_criteria>

<output>
Create `.planning/phases/26-coherent-publication-and-dual-runtime-release/26-06-SUMMARY.md` when done.
</output>

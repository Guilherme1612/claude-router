---
phase: 22-conservative-contracts-and-relationship-graph
plan: 05
type: execute
wave: 4
depends_on: [22-02, 22-03, 22-04]
files_modified:
  - tests/router.contract-inspection.test.mjs
  - tests/router.control-cli.test.mjs
  - src/cli/router-control.mjs
autonomous: true
requirements: [CONT-09]
nyquist_compliant: true
must_haves:
  truths:
    - "Users can inspect contracts, field evidence/confidence, rejected overlays, typed relationships, eligibility reasons, and correction paths in bounded text or JSON."
    - "Text and JSON expose equivalent semantics with deterministic ordering and explicit unknown/recommendation-only reasons."
    - "Inspection is read-only and never leaks secrets, authored bodies, absolute roots, or terminal control characters."
  artifacts:
    - path: "src/cli/router-control.mjs"
      provides: "Bounded privacy-safe contract/relationship projections and text rendering"
    - path: "tests/router.contract-inspection.test.mjs"
      provides: "CLI parity, privacy, determinism, and read-only oracle"
  key_links:
    - from: "src/cli/router-control.mjs"
      to: "canonical active registry"
      via: "strict allowlisted projection reads existing last-complete state without discovery or mutation"
---

<objective>
Expose deterministic, privacy-safe inspection for CONT-09 through the existing router CLI.

Purpose: Users need to understand Router's evidence, uncertainty, relationships, rejected corrections, and eligibility without editing core or triggering work.
Output: Bounded text/JSON projections and automated parity, privacy, and side-effect tests.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-conservative-contracts-and-relationship-graph/22-CONTEXT.md
@.planning/phases/22-conservative-contracts-and-relationship-graph/22-RESEARCH.md
@.planning/phases/21-authoritative-personalized-inventory/21-05-SUMMARY.md
@.planning/phases/22-conservative-contracts-and-relationship-graph/22-02-SUMMARY.md
@.planning/phases/22-conservative-contracts-and-relationship-graph/22-03-SUMMARY.md
@.planning/phases/22-conservative-contracts-and-relationship-graph/22-04-SUMMARY.md

<interfaces>
Extend existing `canonical`, `boundedResult`, `textResult`, inventory projections, parser, and run dispatcher in `src/cli/router-control.mjs`.
Read only the canonical last-complete registry output produced by Plans 01-04.
Use a strict projection allowlist; never serialize raw registry records.
</interfaces>
</context>

## Artifacts this plan creates/modifies

- `tests/router.contract-inspection.test.mjs` — CONT-09 text/JSON parity, boundedness, privacy, rejected-candidate, relationship, correction-path, and read-only oracle.
- `tests/router.control-cli.test.mjs` — parser/dispatcher coverage for contract inspection.
- `src/cli/router-control.mjs` — contract summary/detail and relationship projections using existing CLI envelopes.

<tasks>

<task type="auto">
  <name>Task 1: Establish the Wave 0 inspection and privacy oracle</name>
  <files>tests/router.contract-inspection.test.mjs, tests/router.control-cli.test.mjs</files>
  <read_first>
    - .planning/phases/22-conservative-contracts-and-relationship-graph/22-CONTEXT.md (inspection and privacy decisions)
    - .planning/phases/22-conservative-contracts-and-relationship-graph/22-VALIDATION.md (CONT-09 oracle)
    - tests/router.inventory-security.test.mjs (secret/path/control-character canaries)
    - tests/router.control-cli.test.mjs (subprocess, canonical envelope, text/JSON conventions)
    - src/cli/router-control.mjs (parser, canonical, boundedResult, inventory projections, run dispatcher)
    - tests/router.contracts.test.mjs (field envelope shape)
    - tests/router.contract-overlays.test.mjs (rejected overlays/correction paths)
    - tests/router.relationships.test.mjs (active/inactive edge shape)
  </read_first>
  <action>Create CLI tests for bounded contract listing, one stable-ID contract detail, and relationship inspection in both text and JSON using the existing router control grammar and envelopes. Require semantic parity for normalized fields, per-field evidence/provenance/rule/freshness/confidence, rejected evidence, rejected overlays, active/inactive typed relationships, eligibility gates/reasons, and correction paths. Cover limit/offset/count/truncated continuation, deterministic sorting, explicit unknown/recommendation-only copy, malformed IDs/options/state, and no ANSI. Seed secrets, raw config/instruction/authored prose, absolute home/project/worktree/temp paths, traversal strings, and terminal controls and assert none appear. Snapshot active registry/state bytes before and after every inspection path and assert no discovery, reconciliation, overlay write, correction, or mutation occurs.</action>
  <acceptance_criteria>
    - Parity assertion: text and JSON expose the same semantic fields and reasons.
    - Boundedness assertion: collections report total/returned/truncated/limit/offset and canonical ordering.
    - Privacy assertion: canary secrets, raw bodies, absolute roots, and control sequences never appear.
    - RED assertion marker: intentionally failing names contain `[phase22-red:inspection]` and no unrelated failure is accepted.
  </acceptance_criteria>
  <verify>
    <automated>rtk node -e "const {spawnSync}=require('node:child_process');const r=spawnSync(process.execPath,['--test','tests/router.contract-inspection.test.mjs','tests/router.control-cli.test.mjs'],{encoding:'utf8'});const out=(r.stdout||'')+(r.stderr||'');const failed=[...out.matchAll(/^\s*not ok \d+ - (.+)$/gm)].map(m=&gt;m[1]);process.exit(r.status!==0&amp;&amp;failed.length&gt;0&amp;&amp;failed.every(name=&gt;name.includes('[phase22-red:inspection]'))?0:1)"</automated>
  </verify>
  <done>The complete bounded, equivalent, privacy-safe, read-only inspection contract is executable before CLI edits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement contract and relationship inspection</name>
  <files>src/cli/router-control.mjs, tests/router.contract-inspection.test.mjs, tests/router.control-cli.test.mjs</files>
  <read_first>
    - tests/router.contract-inspection.test.mjs (Task 1 oracle)
    - tests/router.control-cli.test.mjs (Task 1 parser/dispatcher expectations)
    - src/cli/router-control.mjs (existing inventory grammar and strict projections)
    - src/registry/contract.mjs (contract, evidence, rejected overlay fields)
    - src/registry/relationships.mjs (active/inactive relationship fields)
    - src/registry/eligibility.mjs (gate and reason fields)
    - tests/router.inventory-security.test.mjs (projection privacy regressions)
  </read_first>
  <behavior>
    - Contract inspection reads existing active state only and returns bounded deterministic text or JSON.
    - Unknown and rejected states remain visible with safe reasons.
    - Unsafe content never crosses the CLI projection boundary.
  </behavior>
  <action>Extend the existing router control parser/dispatcher with a compact read-only `router contract` inspection surface supporting bounded list, exact stable-ID detail, and relationship view plus `--format text|json`, `--limit`, and `--offset`. Reuse `canonical`, `boundedResult`, `textResult`, existing safe-token/path/fingerprint helpers, and last-complete inventory access. Add strict allowlisted projections for contract field envelopes, rejected evidence/overlays, active/inactive typed edges, eligibility gates/reasons, and correction paths; render deterministic one-key/value-per-line ANSI-free text with JSON semantic parity. Escape controls, use logical/relative paths and bounded fingerprints, and exclude raw authored values, arbitrary payloads, secrets, and absolute prefixes. Inspection must not start discovery/reconciliation, apply corrections, write manifests, execute capabilities, or add a dashboard/TUI.</action>
  <acceptance_criteria>
    - Reuse assertion: implementation extends existing CLI envelopes/projection helpers rather than creating another command framework.
    - Read-only assertion: active registry and operational state bytes remain unchanged for all success/error paths.
    - Explainability assertion: recommendation-only output identifies each failed/unknown gate and safe evidence/rejection reason.
    - Regression assertion: Phase 21 inventory inspection retains its exact behavior and privacy guarantees.
  </acceptance_criteria>
  <verify>
    <automated>rtk node --test tests/router.contract-inspection.test.mjs tests/router.control-cli.test.mjs tests/router.inventory-security.test.mjs</automated>
  </verify>
  <done>Users can safely inspect every contract decision, relationship, rejection, and correction path through deterministic text or JSON.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical registry → CLI projection | Rich local evidence is reduced to a safe public inspection shape |
| CLI arguments → state reader | Untrusted IDs, bounds, and formats select local data without mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-17 | Information Disclosure | contract projection | high | mitigate | Strict allowlist, bounded fingerprints, logical paths, and seeded secret/body/absolute-prefix tests. |
| T-22-18 | Spoofing | terminal rendering | high | mitigate | Escape control characters, keep ANSI-free deterministic labels, and assert text/JSON parity. |
| T-22-19 | Elevation of Privilege | inspection command | high | mitigate | Read last-complete state only; expose no discovery, correction, write, execution, or policy-authoring path. |
| T-22-20 | Denial of Service | large evidence/edge collections | medium | mitigate | Existing boundedResult pagination, collection caps, and deterministic continuation metadata. |
| T-22-SC | Tampering | package installation | high | mitigate | No package installation task exists. |
</threat_model>

<verification>
- `rtk node --test tests/router.contract-inspection.test.mjs tests/router.control-cli.test.mjs tests/router.inventory-security.test.mjs`
- `rtk node --test tests/router.contracts.test.mjs tests/router.contract-overlays.test.mjs tests/router.relationships.test.mjs tests/router.contract-eligibility.test.mjs`
- `rtk node --test tests/*.test.mjs`
</verification>

<source_coverage>
| Source | Items | Plans | Status |
|--------|-------|-------|--------|
| GOAL | Safe capability understanding, evidence, and dispatch eligibility | 01-05 | COVERED |
| REQ | CONT-01, CONT-02, CONT-03 | 01, 04 | COVERED |
| REQ | CONT-04, CONT-05, CONT-06 | 02 | COVERED |
| REQ | CONT-07 | 03 | COVERED |
| REQ | CONT-08 | 04 | COVERED |
| REQ | CONT-09 | 05 | COVERED |
| RESEARCH | Uniform envelopes, precedence, exact overlays, typed edges, one eligibility function, safe inspection | 01-05 | COVERED |
| CONTEXT | Contract evidence/confidence, overlay trust, relationship classification, dispatch eligibility/inspection | 01-05 | COVERED |
| CONTEXT | Phase 23 intent/invocation, Phase 24 outcomes/health, Phase 25 recommendations/drafts | NONE | EXCLUDED — deferred |
</source_coverage>

<success_criteria>
- CONT-09 passes with bounded deterministic text/JSON semantic parity.
- Inspection explains unknown, rejected, inactive, and recommendation-only states without disclosure or mutation.
- All CONT-01 through CONT-09 focused suites and the repository suite pass.
</success_criteria>

<output>
Create `.planning/phases/22-conservative-contracts-and-relationship-graph/22-05-SUMMARY.md` when done.
</output>

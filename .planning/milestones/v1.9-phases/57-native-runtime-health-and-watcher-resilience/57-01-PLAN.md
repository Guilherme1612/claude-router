---
phase: 57-native-runtime-health-and-watcher-resilience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/adapters/claude.mjs
  - src/lifecycle/router-lifecycle.mjs
  - src/registry/watcher.mjs
  - scripts/v19-live-snapshot.mjs
  - tests/router.adapters.test.mjs
  - tests/router.deployed-bundle.test.mjs
  - tests/router.lifecycle.test.mjs
  - tests/router.registry-watcher.test.mjs
  - tests/router.v19-live-snapshot.test.mjs
autonomous: true
requirements: [HEALTH-01, HEALTH-04]
---

# Phase 57 Plan 01: Native health truth and deployed verification

## Objective

Close the live discovery and deployment gaps that made native health appear worse or less explainable than it was.

## Tasks

1. Exclude Claude root `hooks/package.json` from hook discovery and lock the behavior with a regression test.
2. Mirror `runtime/router.mjs` and `runtime/router.evolve.mjs` into both owned deployment roots so the installed latency gate executes the same source boundary as the repository gate.
3. Publish bounded verification gate evidence through watcher status and the privacy-safe live snapshot.
4. Deploy the changes, restart the native controller, and prove ready/eligible/8-of-8 verification with preserved empty authority when no route is dispatchable.

## Verification

- Focused adapter, watcher, installer, deployed-bundle, and snapshot suites pass serially.
- Source and installed module hashes match.
- Live snapshot contains no raw prompt/session/telemetry bodies.
- No active tuple is claimed when the registry has zero dispatchable routes.

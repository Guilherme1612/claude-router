---
phase: 18-autonomous-lifecycle-and-release-gates
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - src/lifecycle/router-lifecycle.mjs
  - src/registry/watcher.mjs
  - src/prompt/publish-index.mjs
  - tests/router.lifecycle-recovery.test.mjs
  - tests/router.installer-coexistence.test.mjs
autonomous: true
requirements:
  - SAF-10
  - MAP-02
  - ACT-01

must_haves:
  truths:
    - "D-04/D-05/D-06: unsafe candidates, corruption, interruption, and missed events preserve or idempotently recover one verified registry/index tuple, and a later valid event advances it."
    - "D-07/D-08: install, atomic upgrade, idempotent reinstall, reversible disable, and uninstall use complete immutable installation generations selected by one atomic pointer, mutate only manifest-owned artifacts, and restore router-managed bindings."
    - "D-09: Claude and Codex lifecycle actions preserve the other runtime and all unrelated settings, hooks, plugins, skills, and user files independently and together."
  artifacts:
    - path: src/lifecycle/router-lifecycle.mjs
      provides: "Versioned installation generations, one atomic generation pointer, startup recovery, and reversible disable/enable lifecycle"
      exports: ["upgradeRouter", "disableRouter", "enableRouter"]
    - path: tests/router.lifecycle-recovery.test.mjs
      provides: "Startup/steady-state crash, corruption, reader, idempotency, and continued-advance evidence"
    - path: tests/router.installer-coexistence.test.mjs
      provides: "Dual-runtime five-verb ownership and coexistence matrix"
  key_links:
    - from: src/lifecycle/router-lifecycle.mjs
      to: "immutable installation generations and install-state/active.json"
      via: "upgrade stages and verifies a complete owned generation before one atomic pointer switch; bootstrap/controller readers resolve only that pointer"
    - from: tests/router.lifecycle-recovery.test.mjs
      to: src/registry/watcher.mjs
      via: "installed controller failure and restart drive authoritative recovery and later advancement"
---

<objective>
Close crash recovery and installer coexistence as one manifest-backed, reversible dual-runtime lifecycle (D-04 through D-09).

Purpose: Autonomous propagation is safe only if partial control-plane failures recover a verified tuple and lifecycle operations never damage unrelated Claude or Codex state.
Output: Explicit atomic upgrade/disable/enable operations plus hermetic recovery and coexistence gate suites.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-autonomous-lifecycle-and-release-gates/18-CONTEXT.md
@.planning/phases/18-autonomous-lifecycle-and-release-gates/18-RESEARCH.md
@.planning/phases/14-deterministic-mapping-activation-and-rollback/14-CONTEXT.md
@.planning/phases/13-target-safety-hook-reconciliation-and-quarantine/13-CONTEXT.md
@.planning/phases/18-autonomous-lifecycle-and-release-gates/18-01-SUMMARY.md
@src/lifecycle/router-lifecycle.mjs
@src/registry/activate.mjs
@src/registry/watcher.mjs
@src/prompt/publish-index.mjs
@tests/router.lifecycle.test.mjs
@tests/router.coexistence.test.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add atomic upgrade and reversible disable lifecycle operations</name>
  <files>src/lifecycle/router-lifecycle.mjs, tests/router.installer-coexistence.test.mjs</files>
  <behavior>
    - Upgrade writes a complete immutable vN+1 installation generation and selects it with one atomic `install-state/active.json` pointer; failure before commit leaves vN selected and failure after commit yields only vN+1 (D-08).
    - Bootstrap/controller readers resolve one validated generation pointer and never mix router, module, config, manifest, or binding metadata across generations; startup removes incomplete staging and recovers a corrupt pointer to verified known-good.
    - Disable stops the owned controller and removes only the router-managed binding while retaining owned artifacts/state; enable restores that exact binding and verified controller, and repeated operations are idempotent (D-08).
    - Install, upgrade, reinstall, disable/enable, and uninstall preserve byte-identical unrelated state in pre-populated Claude and Codex homes (D-07/D-09).
  </behavior>
  <action>Extend the existing installer with a realizable generation protocol rather than attempting a multi-file in-place transaction. Export `upgradeRouter`, `disableRouter`, and `enableRouter`. Keep a tiny stable owned bootstrap/hook entry whose only job is to containment-check and read `install-state/active.json`, validate the selected immutable generation manifest/hashes, and launch that generation's router/controller modules. Install/upgrade creates `install-state/generations/<generation-id>.staging`, writes the complete router, modules, controller config, ownership/binding metadata, and manifest, fsyncs every file and directory, renames staging to its immutable final directory, validates it through the bootstrap resolver, then atomically renames one active-generation pointer and fsyncs `install-state`; only afterward may it update known-good and stop/restart the controller on the selected generation. Startup discards incomplete staging, validates active, and recovers a corrupt/missing selection to the verified known-good generation. Disable/enable changes an atomic manifest-backed lifecycle state and only the exact router binding/controller state, retaining generation authority for reversal. Uninstall removes the stable bootstrap, generation store, and binding only when manifest-owned, conservatively retaining modified user files. Write crash injection at every write/fsync, staging rename, active-pointer rename, known-good update, and controller handoff boundary, and sample bootstrap/controller resolution throughout to assert every observation is wholly vN or wholly vN+1. Cover install, upgrade, reinstall, repeated disable/enable, and uninstall in pre-populated independent/together homes, with byte-identical unrelated state per D-07 through D-09.</action>
  <verify>
    <automated>node --test tests/router.installer-coexistence.test.mjs tests/router.lifecycle.test.mjs tests/router.coexistence.test.mjs</automated>
  </verify>
  <done>All lifecycle verbs are manifest-backed and idempotent; upgrade exposes only one complete generation before or after the atomic pointer, startup repairs incomplete/corrupt selection, disable is reversible, and unrelated/cross-runtime state is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove fail-closed tuple recovery and continued autonomy</name>
  <files>src/registry/watcher.mjs, src/prompt/publish-index.mjs, tests/router.lifecycle-recovery.test.mjs</files>
  <behavior>
    - Unsafe mapping/candidate, corrupt registry/index/schema, controller interruption, and missed/coalesced events preserve the verified active tuple or recover it from durable known-good/journal state (D-04).
    - Reader sampling across every injected publication boundary observes either the complete old tuple or complete new tuple, never mixed, partial, or unverified state; repeated recovery is idempotent (D-05).
    - Startup repair and steady-state failure each end with authoritative rescan and a later valid filesystem change advancing to a strictly newer verified tuple (D-06).
  </behavior>
  <action>Add hermetic crash-injection coverage around every registry write/fsync, compiled write/fsync, immutable tuple-manifest write, active-tuple pointer rename, known-good update, and recovery-journal boundary established by Plan 18-01. Exercise startup recovery and live installed-controller failure with corrupt tuple pointer, registry/index/schema/hash mismatch, unsafe hook/mapping input (SAF-10/MAP-02), stopped controller, and coalesced/missed mutation. Sample `loadCompiledIndex`, `routeContextPrompt`, and the watcher's public active-registry reader at each boundary; assert all resolve the same tuple id and only the complete old or complete new registry/index pair is observable. On mismatch they must block/fall back as one tuple, never combine independent component pointers. Adjust watcher/publisher recovery if needed so it validates both components, restores the latest verified compatible tuple, remains idempotent, and rebuilds incremental baseline from authoritative disk. Every case ends with a valid real mutation that advances to a strictly newer tuple and routes successfully per D-06.</action>
  <verify>
    <automated>node --test tests/router.lifecycle-recovery.test.mjs tests/router.registry-activate.test.mjs tests/router.registry-watcher.test.mjs tests/router.compiled-index.test.mjs</automated>
  </verify>
  <done>Startup and steady-state failure matrices prove fail-closed, reader-safe, idempotent LKG recovery followed by successful automatic advancement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Installer to user homes | Router-owned mutations coexist with unrelated user-controlled files and configuration. |
| Recovery journal/pointers to readers | Corrupt or interrupted durable state must not become dispatch authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-03 | Tampering | installer lifecycle | high | mitigate | Exact manifest ownership, containment, preflight, transactional rollback, and byte snapshots of unrelated state. |
| T-18-04 | Tampering | registry/index recovery | high | mitigate | Hash/schema/tuple validation, immutable versions, atomic pointers, journal recovery, and concurrent reader sampling. |
| T-18-05 | Denial | disable/controller recovery | medium | mitigate | Reversible idempotent state transitions and authoritative rescan after restart. |
</threat_model>

<verification>
Run both focused suites and their nearest regression suites. At the wave boundary run `node --test tests/router.installer-coexistence.test.mjs tests/router.lifecycle-recovery.test.mjs tests/router.lifecycle.test.mjs tests/router.coexistence.test.mjs tests/router.registry-activate.test.mjs tests/router.registry-watcher.test.mjs tests/router.compiled-index.test.mjs`.
</verification>

<success_criteria>
- Atomic upgrade, idempotent reinstall, reversible disable/enable, and conservative uninstall are explicit executable contracts.
- Both runtime homes retain byte-identical unrelated content through every lifecycle action independently and together.
- Every corruption/interruption class preserves or recovers a complete verified tuple, then accepts a later valid change.
</success_criteria>

<output>
Create `.planning/phases/18-autonomous-lifecycle-and-release-gates/18-02-SUMMARY.md` when done.
</output>

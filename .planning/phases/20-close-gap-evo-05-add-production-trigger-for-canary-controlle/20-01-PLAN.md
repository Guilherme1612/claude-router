---
phase: 20-close-gap-evo-05-add-production-trigger-for-canary-controlle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/evolution/telemetry-bridge.mjs
  - src/evolution/evidence.mjs
  - src/lifecycle/router-lifecycle.mjs
  - tests/router.telemetry-bridge.test.mjs
  - tests/router.evidence-persistence.test.mjs
  - tests/router.deployed-bundle.test.mjs
autonomous: true
requirements: [EVO-05]
user_setup: []

must_haves:
  truths:
    - "Telemetry records from ~/.claude/router/telemetry.jsonl are transformable into D-05 evidence envelopes using only allowlisted fields (route_id, confidence_band, guard_codes, reason_code, fixture_class, latency_us, candidate_version, policy_version, verdict, prompt_signature, timestamp_ms)."
    - "Privacy-denied telemetry records (deny_filtered / privacy guard codes) are skipped or stored with prompt_signature=null; no raw prompt text crosses into evidence (forbidden_evidence_field denial enforced before persistence)."
    - "Validated evidence envelopes persist to scoped disk files under ~/.claude/router/evidence/ with project and aggregate isolation, 7-day retention, 24-hour half-life decay, and a 30-sample floor enforced on window read."
    - "evolution/{canary-controller,evidence,perf-measure,telemetry-bridge}.mjs are listed in router-lifecycle.mjs moduleNames so the installer deploys them to ~/.claude/router/modules/evolution/."
    - "The hot path (src/context/prompt-route.mjs routeContextPrompt → loadCompiledIndex) is unchanged and imports none of the new bridge/persistence code."
  artifacts:
    - "src/evolution/telemetry-bridge.mjs"
    - "createPersistentEvidenceStore export added to src/evolution/evidence.mjs"
    - "evolution/* entries in src/lifecycle/router-lifecycle.mjs moduleNames"
    - "tests/router.telemetry-bridge.test.mjs"
    - "tests/router.evidence-persistence.test.mjs"
    - "tests/router.deployed-bundle.test.mjs"
  key_links:
    - "telemetry-bridge.mjs → evidence.mjs validateEvidenceEnvelope (rejects forbidden fields before persistence)"
    - "createPersistentEvidenceStore → evidence.mjs HALF_LIFE_MS/MAX_RETENTION_MS/MINIMUM_SAMPLES (same window logic)"
    - "router-lifecycle.mjs moduleNames → installer deploys evolution/* to ~/.claude/router/modules/evolution/"

# Specless probe fallback SKIPPED (visible, recorded choice): Phase 20 has no SPEC.md
# (EDGE_ABSENT=1, PROHIB_ABSENT=1) and no mapped requirement IDs in ROADMAP ("Requirements: TBD").
# The nothing-to-probe guard fired. $COVERAGE is EMPTY; no probe-derived predicates are
# generated. must_haves.truths are derived from the phase goal + RESEARCH.md Validation
# Architecture / Security Domain / Project Constraints + Phase 17 success criterion #4
# ("Privacy-safe signal or weight candidates run through canaries and automatically roll
# back when quality regresses") + audit BLOCKER 2 closure criteria. Do NOT fabricate
# edge/prohibition predicates.
---

<objective>
Establish the foundational layer for production canary triggers: a stdlib-only telemetry→evidence bridge that transforms ~/.claude/router/telemetry.jsonl records into D-05 evidence envelopes, a disk-backed persistent evidence store enforcing the Phase 17 D-07/D-08 retention/decay/floor contract, and inclusion of evolution/* in the deployed module bundle so trigger surfaces (Wave 2 watcher, Wave 3 CLI) can import them.

Purpose: Close BLOCKER 2 (audit lines 177-180) foundation — the canary-controller library is complete and test-validated but has no production evidence source and is absent from the deployed bundle. This plan adds the evidence source and ships the library to production, without adding any caller (callers ship in Wave 2/3).
Output: src/evolution/telemetry-bridge.mjs (new), createPersistentEvidenceStore added to src/evolution/evidence.mjs, evolution/* added to src/lifecycle/router-lifecycle.mjs:308-317 moduleNames, plus three test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-close-gap-evo-05-add-production-trigger-for-canary-controlle/20-RESEARCH.md
@.planning/v1.2-MILESTONE-AUDIT.md
@.claude/CLAUDE.md
@src/evolution/evidence.mjs
@src/evolution/canary-controller.mjs
@src/lifecycle/router-lifecycle.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Telemetry→evidence bridge module (telemetry-bridge.mjs) + tests</name>
  <files>src/evolution/telemetry-bridge.mjs, tests/router.telemetry-bridge.test.mjs</files>
  <read_first>
    - src/evolution/evidence.mjs (FIELDS set line 4, CONFIDENCE_BANDS line 9, FIXTURE_CLASSES line 10, VERDICTS line 11, PRIVACY_GUARDS line 12, validateEvidenceEnvelope line 27-46 — the allowlisted envelope schema + privacy suppression contract)
    - .planning/phases/20-close-gap-evo-05-add-production-trigger-for-canary-controlle/20-RESEARCH.md (Pattern 1 "Telemetry→Evidence Bridge" lines 177-240 — the field-by-field mapping table at lines 182-194 + the stdlib example at 197-240; Pitfalls 1-3 at lines 383-399)
    - tests/router.evolution-canary.test.mjs lines 1-25 (dynamic-import via URL + node:test + node:assert/strict test pattern to replicate)
    - ~/.claude/hooks/router.mjs telemetryEntryFromState (referenced at RESEARCH.md lines 182-194 + 484-494; the source telemetry schema: ts, prompt_signature, suggested_mode, suggested_skills, suggested_agents, confidence_tier, invoke_kind, graphify_queried, guards_fired, downstream_invocations, outcome, latency_ms, weight_applied, outcomes, evolved_after, surface_status, cwd)
  </read_first>
  <behavior>
    - Test 1 (field mapping): a telemetry record with confidence_tier='high', suggested_mode='gsd-debug', invoke_kind='skill', guards_fired=['route_selected'], latency_ms=12, ts=1700000000000, prompt_signature='abc123' transforms to an envelope with timestamp_ms=1700000000000, route_id='gsd-debug', confidence_band='high', guard_codes=['route_selected'], reason_code='route_selected', fixture_class='minimal-prompt', latency_us=12000, verdict='success', policy_version='workflow-transitions-v1', prompt_signature='abc123' AND validateEvidenceEnvelope accepts it.
    - Test 2 (privacy suppression): a record with confidence_tier='deny_filtered' (or guards_fired containing a PRIVACY_GUARDS code) is returned with status='skipped' reason_code='not_canary_evidence' (per RESEARCH.md Pitfall 3 + the bridge example line 208) — the bridge must NOT emit an envelope with a non-null prompt_signature for privacy-denied records.
    - Test 3 (fixture_class classification): confidence_tier='user_explicit' → fixture_class='explicit-override'; confidence_tier='stale' → fixture_class='stale-context'; invoke_kind='agent' (with high/medium/low) → fixture_class='dependency'; confidence_tier='trivial' OR 'reentry_skipped' OR 'manifest_missing' OR null suggested_mode → { status: 'skipped' } (no envelope emitted).
    - Test 4 (verdict v1 policy): every emitted envelope has verdict='success' (telemetry outcome is null today; regression is detected by calibration gates, not per-record verdicts — RESEARCH.md Pitfall 1 + Assumption A1).
    - Test 5 (candidate_version is a parameter): telemetryRecordToEvidence(record, { candidate_version: 'v1-canary-abc' }) sets envelope.candidate_version='v1-canary-abc'; absent the option → 'steady-state-v1' default.
    - Test 6 (forbidden field rejection): the bridge never emits a field outside evidence.mjs FIELDS set; passing an envelope with an extra field through validateEvidenceEnvelope denies with 'forbidden_evidence_field'.
    - Test 7 (latency unit conversion): latency_ms=12 → latency_us=12000 (multiply by 1000); bounds enforced by validateEvidenceEnvelope (0–10_000_000, evidence.mjs:36).
  </behavior>
  <action>
    Create src/evolution/telemetry-bridge.mjs as a pure stdlib-only ESM module. Export `telemetryRecordToEvidence(record, { candidate_version = null } = {})` returning either { status: 'skipped', reason_code } (for non-canary-relevant records) or the result of validateEvidenceEnvelope(envelope) (which yields { status: 'accepted', signal } or { status: 'denied', reason_code }). Implement per RESEARCH.md Pattern 1 (lines 197-240):
    - Import validateEvidenceEnvelope from './evidence.mjs'.
    - Define POLICY_VERSION = 'workflow-transitions-v1' (from compile-index.mjs:8 — do NOT import compile-index.mjs to avoid coupling; hardcode the constant per RESEARCH.md line 202).
    - classifyFixtureClass(record): map confidence_tier + invoke_kind to FIXTURE_CLASSES per the RESEARCH.md table (line 190): user_explicit→explicit-override, stale→stale-context, deny_filtered→null (skip), invoke_kind==='agent'→dependency, default high/medium/low→minimal-prompt. Return null for trivial/reentry_skipped/manifest_missing.
    - deriveReasonCode(record): map per RESEARCH.md line 189 (deny_filtered→deny_filtered, user_explicit→user_explicit, stale→stale_context, manifest_missing→manifest_missing, else→route_selected).
    - Skip early when fixture_class is null OR record.suggested_mode is null/empty (return { status: 'skipped', reason_code: 'no_route' | 'not_canary_evidence' }).
    - Build the envelope with the 11 allowlisted fields exactly (timestamp_ms=record.ts, route_id=record.suggested_mode, confidence_band=record.confidence_tier, guard_codes=record.guards_fired||[], reason_code, fixture_class, latency_us=Math.round((record.latency_ms||0)*1000), candidate_version=candidate_version||'steady-state-v1', policy_version=POLICY_VERSION, verdict='success', prompt_signature=record.prompt_signature ?? null). NEVER compute a new signature (RESEARCH.md Pitfall 3, line 398).
    - Return validateEvidenceEnvelope(envelope) — it rejects forbidden fields before persistence (evidence.mjs:29).
    - Export a second helper `ingestTelemetryFile(path, { candidate_version, onRecord } = {})` that reads a JSONL file line-by-line, calls telemetryRecordToEvidence per record, and invokes onRecord(result) for accepted/skipped results — used by trigger surfaces to bulk-load telemetry. Use node:fs readFileSync + String.split('\n') + JSON.parse per line (skip empty lines + parse failures with onRecord({ status:'skipped', reason_code:'parse_error' })).
    NEVER access raw prompts. NEVER read active.json. candidate_version is a parameter only (RESEARCH.md Pitfall 4, line 401).
    Create tests/router.telemetry-bridge.test.mjs using node:test + node:assert/strict + dynamic import via new URL('../src/evolution/telemetry-bridge.mjs', import.meta.url) (replicate the pattern in tests/router.evolution-canary.test.mjs lines 1-25). Implement the 7 behavior tests above.
  </action>
  <verify>
    <automated>node --test tests/router.telemetry-bridge.test.mjs</automated>
  </verify>
  <done>
    - telemetryRecordToEvidence exported and transforms telemetry per the RESEARCH.md field mapping.
    - Privacy-denied records skipped (no envelope with non-null signature emitted).
    - All emitted envelopes have verdict='success' and candidate_version from the parameter.
    - node --test tests/router.telemetry-bridge.test.mjs exits 0 with 7 passing tests.
    - No import of compile-index.mjs, active.json reading, or raw-prompt access in telemetry-bridge.mjs.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persistent evidence store (createPersistentEvidenceStore in evidence.mjs) + tests</name>
  <files>src/evolution/evidence.mjs, tests/router.evidence-persistence.test.mjs</files>
  <read_first>
    - src/evolution/evidence.mjs (existing createEvidenceStore lines 93-154, matchesScope 107-110, window logic 125-152, HALF_LIFE_MS line 15, MAX_RETENTION_MS line 16, MINIMUM_SAMPLES line 17, validateEvidenceEnvelope 27-46, evidenceWindowFingerprint 60-72, boundedToken 19-21, defaultHash 48-50)
    - .planning/phases/20-close-gap-evo-05-add-production-trigger-for-canary-controlle/20-RESEARCH.md (Pattern 2 "Persistent Evidence Store" lines 242-282 — the disk-backed store design + the example at 254-282; Project Constraints line 666-674)
    - src/registry/activate.mjs journalWrite pattern (referenced by RESEARCH.md line 249 as the established atomic-append pattern) — grep for `appendFileSync` + `flag: 'a'` usage to match project style
  </read_first>
  <behavior>
    - Test 1 (append + scope routing): append a validated project-scoped envelope with options.scope='project', options.project_id='proj-A' → writes a line to ~/.claude/router/evidence/project-proj-A.jsonl; options.scope='aggregate', options.aggregate_eligible=true → writes to aggregate.jsonl; options.scope='aggregate' without aggregate_eligible → returns { status:'denied', reason_code:'aggregate_eligibility_required' } and writes nothing.
    - Test 2 (scope isolation): project-A records never appear in a project-B window; a project-A window call with options.project_id='proj-B' returns zero samples from proj-A records.
    - Test 3 (retention): inject a clock (now=() => fixedMs); records older than 7d (MAX_RETENTION_MS) are excluded from window — window.sufficient=false when only old records exist.
    - Test 4 (decay): weighted_samples matches the exponential-half-life-v1 formula (2 ** (-age / HALF_LIFE_MS)) — reuse the exact arithmetic from evidence.mjs:135.
    - Test 5 (floor): window.sufficient === false when sample_count < 30; === true when ≥ 30 accepted records within retention.
    - Test 6 (fingerprint integrity): evidenceWindowFingerprint of a window read from disk matches the fingerprint computed over the same records in-memory (call evidenceWindowFingerprint exported from evidence.mjs line 60).
    - Test 7 (forbidden field rejection before persistence): append({ ..., raw_prompt: 'leak' }, { scope:'project', project_id:'x' }) returns { status:'denied', reason_code:'forbidden_evidence_field' } (validateEvidenceEnvelope rejects) and the file contains no line with 'leak'.
    - Test 8 (atomic append): append uses appendFileSync with flag:'a', mode:0o600; the directory is created with mode:0o700 (mkdirSync recursive).
  </behavior>
  <action>
    Add `export function createPersistentEvidenceStore({ root, now = Date.now, minimum_samples = MINIMUM_SAMPLES } = {})` to src/evolution/evidence.mjs (append at end, do NOT modify existing exports). Implement per RESEARCH.md Pattern 2 (lines 254-282):
    - mkdirSync(root, { recursive: true, mode: 0o700 }).
    - pathFor(scope): scope.kind==='aggregate' → join(root,'aggregate.jsonl'); scope.kind==='project' → join(root,`project-${scope.project_id}.jsonl`).
    - append(input, options): call validateEvidenceEnvelope(input) first (rejects forbidden fields BEFORE persistence — evidence.mjs:29); if status !== 'accepted' return it. Resolve scope: options.scope==='aggregate' → require options.aggregate_eligible===true else deny 'aggregate_eligibility_required'; options.scope==='project' → require boundedToken(options.project_id,128) (reuse evidence.mjs:19 boundedToken) else deny 'invalid_project_scope'. Append a single JSON line { scope, signal: validated.signal, fingerprint: defaultHash(JSON.stringify(validated.signal)) } with appendFileSync(path, line+'\n', { flag:'a', mode:0o600 }).
    - window(options): read pathFor(scope) if existsSync; parse each line; filter by matchesScope (evidence.mjs:107-110) + age ≤ MAX_RETENTION_MS; compute weighted_samples with HALF_LIFE_MS decay (copy evidence.mjs:125-152 logic verbatim — do NOT hand-roll); enforce minimum_samples floor; return { sufficient, sample_count, weighted_samples, source_evidence_fingerprint: evidenceWindowFingerprint(records), records }.
    - Object.freeze the returned store object (match evidence.mjs style).
    Reuse validateEvidenceEnvelope, evidenceWindowFingerprint, HALF_LIFE_MS, MAX_RETENTION_MS, MINIMUM_SAMPLES, boundedToken, defaultHash — all already exported or internal in evidence.mjs. Do NOT duplicate the decay/retention math.
    Create tests/router.evidence-persistence.test.mjs using node:test + assert/strict + dynamic import via URL (same pattern as Task 1). Use a unique tmpdir per test (os.tmpdir() + randomUUID or mkdtempSync) for root; clean up in try/finally. Implement the 8 behavior tests above.
  </action>
  <verify>
    <automated>node --test tests/router.evidence-persistence.test.mjs</automated>
  </verify>
  <done>
    - createPersistentEvidenceStore exported from src/evolution/evidence.mjs.
    - Append validates via validateEvidenceEnvelope BEFORE writing (forbidden_evidence_field denial tested).
    - Project + aggregate isolation enforced on disk (separate JSONL files).
    - 7d retention / 24h decay / 30-sample floor enforced on window read (matches evidence.mjs in-memory store).
    - node --test tests/router.evidence-persistence.test.mjs exits 0 with 8 passing tests.
    - No existing createEvidenceStore/validateEvidenceEnvelope behavior modified (additive only).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Deployed bundle inclusion (evolution/* in router-lifecycle.mjs moduleNames) + assertion test</name>
  <files>src/lifecycle/router-lifecycle.mjs, tests/router.deployed-bundle.test.mjs</files>
  <read_first>
    - src/lifecycle/router-lifecycle.mjs lines 305-320 (moduleNames array — the deployed module bundle list; moduleValues maps each name to [join(runtimeRoot,'modules',name), readFileSync(join(sourceRoot,name))] at 318-319)
    - .planning/phases/20-close-gap-evo-05-add-production-trigger-for-canary-controlle/20-RESEARCH.md (Pitfall 6 "Deployed bundle must include evolution/*" lines 413-417; Runtime State Inventory "Build artifacts" line 366)
    - .planning/v1.2-MILESTONE-AUDIT.md line 165 ("Orphaned modules (test-only, not deployed): src/orchestrator/{...}.mjs, src/evolution/{canary-controller,evidence,perf-measure}.mjs.")
  </read_first>
  <behavior>
    - Test 1 (bundle inclusion): import router-lifecycle.mjs (or read its source if export not available) and assert moduleNames includes 'evolution/canary-controller.mjs', 'evolution/evidence.mjs', 'evolution/perf-measure.mjs', 'evolution/telemetry-bridge.mjs'.
    - Test 2 (source files exist): for each evolution/* module name in moduleNames, readFileSync(join(sourceRoot, name)) does not throw (the files exist on disk at the expected relative path).
    - Test 3 (import succeeds): dynamic import of new URL('../src/evolution/telemetry-bridge.mjs', import.meta.url) resolves and exposes telemetryRecordToEvidence as a function (proves the module is syntactically valid and importable from the deployed location).
  </behavior>
  <action>
    Edit src/lifecycle/router-lifecycle.mjs lines 308-317 (the moduleNames array) to add four entries (per RESEARCH.md Pitfall 6 line 416): 'evolution/canary-controller.mjs', 'evolution/evidence.mjs', 'evolution/perf-measure.mjs', 'evolution/telemetry-bridge.mjs'. Place them in a logically grouped position (e.g., after the context/* entries, before prompt/* or at the end of the array). Do NOT reorder or remove existing entries — this is purely additive (audit line 165 calls these "orphaned modules"; adding them closes that finding).
    Create tests/router.deployed-bundle.test.mjs using node:test + assert/strict. Import the moduleNames array (if router-lifecycle.mjs exports it, import directly; otherwise read the source file and assert the strings appear). Implement the 3 behavior tests above. Use new URL('../src/evolution/telemetry-bridge.mjs', import.meta.url) for the import test.
    Verify the installer path still works: moduleValues at line 318-319 reads each module from sourceRoot and writes to runtimeRoot/modules/ — adding evolution/* means the installer will create ~/.claude/router/modules/evolution/ automatically (dirname grouping at lines 436-437 already handles nested dirs).
  </action>
  <verify>
    <automated>node --test tests/router.deployed-bundle.test.mjs</automated>
  </verify>
  <done>
    - src/lifecycle/router-lifecycle.mjs moduleNames includes all four evolution/* module names.
    - No existing moduleNames entry removed or reordered (additive only).
    - node --test tests/router.deployed-bundle.test.mjs exits 0 with 3 passing tests.
    - Dynamic import of src/evolution/telemetry-bridge.mjs succeeds (module is syntactically valid).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| telemetry.jsonl → evidence store | Untrusted-ish input (telemetry is router-produced but the bridge must not trust field presence/types) crosses into persistent evidence consumed by canary decisions. |
| evidence store disk → canary decision | Persisted evidence is read back and drives publication authority mutation; a poisoned store could promote a bad candidate. |
| sourceRoot → ~/.claude/router/modules/evolution/ | Deployed bundle write path — evolution/* modules are installed to the user's runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-01 | Information Disclosure | telemetry-bridge.mjs telemetryRecordToEvidence | high | mitigate | validateEvidenceEnvelope allowlists fields before persistence (evidence.mjs:29); bridge passes record.prompt_signature through unchanged (already null when privacy-denied per router.mjs:2365); bridge never accesses raw prompts (RESEARCH.md Pitfall 3 line 398). Test 2 (Task 1) verifies skipped/denied privacy records. |
| T-20-02 | Tampering | createPersistentEvidenceStore append | high | mitigate | validateEvidenceEnvelope is the FIRST call in append() — forbidden fields rejected BEFORE any disk write (Test 7, Task 2); atomic appendFileSync flag:'a' mode:0o600; directory mode 0o700; scope isolation enforced (separate JSONL files, Test 2). |
| T-20-03 | Tampering | persistent evidence window read | medium | mitigate | Window read reuses evidence.mjs:125-152 logic verbatim (retention + decay + floor); evidenceWindowFingerprint binds the window to its records (Test 6). 30-sample floor (MINIMUM_SAMPLES) prevents low-volume promotion (Phase 17 D-08). |
| T-20-04 | Information Disclosure | cross-project evidence contamination | high | mitigate | pathFor(scope) routes project records to project-{project_id}.jsonl and aggregate to aggregate.jsonl (separate files); matchesScope filter (evidence.mjs:107-110) enforced on window read; aggregate requires aggregate_eligible===true (Test 1+2, Task 2). |
| T-20-05 | Tampering | deployed bundle inclusion (router-lifecycle.mjs moduleNames) | low | accept | Adding evolution/* to moduleNames is additive; the installer reads each file from sourceRoot and writes to runtimeRoot/modules/ (line 318-319); no new write path is introduced, only new entries in an existing list. |
| T-20-06 | Tampering | telemetry replay (stale records dominating promotion) | medium | mitigate | 7d MAX_RETENTION_MS filter on window read + 24h HALF_LIFE_MS exponential decay (evidence.mjs:15-16, copied into persistent store window logic — Test 3+4, Task 2). |
| T-20-07 | Elevation of Privilege | bridge module input validation | medium | mitigate | telemetryRecordToEvidence skips unclassifiable records (returns {status:'skipped'}) rather than throwing; validateEvidenceEnvelope enforces bounded tokens, enum membership, latency bounds (evidence.mjs:27-46); fail-open per CLAUDE.md. |
| T-20-SC | Tampering | npm/pip/cargo installs | high | mitigate | N/A — zero-dependency phase (RESEARCH.md Package Legitimacy Audit line 66-74: no external packages). No install task; no [ASSUMED]/[SUS] packages. |
</threat_model>

<verification>
- node --test tests/router.telemetry-bridge.test.mjs exits 0 (7 tests).
- node --test tests/router.evidence-persistence.test.mjs exits 0 (8 tests).
- node --test tests/router.deployed-bundle.test.mjs exits 0 (3 tests).
- grep -v '^#' src/lifecycle/router-lifecycle.mjs | grep -c "'evolution/" returns 4 (four evolution/* entries added).
- grep -r "telemetry-bridge\|createPersistentEvidenceStore" src/context/prompt-route.mjs src/prompt/compile-index.mjs src/prompt/publish-index.mjs returns 0 (hot path untouched — the bridge runs only in the background control plane).
</verification>

<success_criteria>
- EVO-05 evidence foundation: telemetry→evidence transform + persistent store + deployed-bundle inclusion all shipped.
- Privacy invariant: no raw prompt text crosses into evidence (forbidden_evidence_field denial tested).
- Phase 17 D-07/D-08 contract preserved: 7d retention / 24h decay / 30-sample floor / project+aggregate isolation enforced on disk.
- Hot path unchanged: prompt-route.mjs imports none of the new code.
- evolution/* no longer orphaned (audit line 165 closed for the bundle inclusion dimension).
</success_criteria>

<output>
Create `.planning/phases/20-close-gap-evo-05-add-production-trigger-for-canary-controlle/20-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `src/evolution/telemetry-bridge.mjs` (new file) — exports `telemetryRecordToEvidence(record, options)` and `ingestTelemetryFile(path, options)`.
- `createPersistentEvidenceStore` (new export added to existing `src/evolution/evidence.mjs`) — disk-backed evidence store with `.append(input, options)` and `.window(options)`.
- `evolution/canary-controller.mjs`, `evolution/evidence.mjs`, `evolution/perf-measure.mjs`, `evolution/telemetry-bridge.mjs` (new entries in `src/lifecycle/router-lifecycle.mjs` moduleNames array at lines 308-317).
- `tests/router.telemetry-bridge.test.mjs` (new test file).
- `tests/router.evidence-persistence.test.mjs` (new test file).
- `tests/router.deployed-bundle.test.mjs` (new test file).
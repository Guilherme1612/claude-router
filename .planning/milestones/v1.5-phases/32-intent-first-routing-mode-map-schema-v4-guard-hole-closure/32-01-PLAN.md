---
phase: 32-intent-first-routing-mode-map-schema-v4-guard-hole-closure
plan: 01
type: test
wave: 0
depends_on: []
files_modified:
  - tests/router.schema-v4-routing.test.mjs
  - tests/router.phase32-cross-runtime.test.mjs
  - tests/router.coverage-audit.test.mjs
autonomous: true
requirements: [ROUTE-01, ROUTE-02, ROUTE-03, ROUTE-04, ROUTE-05, PARITY-03, PARITY-04]
user_setup: []
estimate:
  tokens: 60000
  raw_tokens: 31000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - A schema_version-SET fixture manifest with NO gsd-* commands currently emits gsd-* slash suggestions (the guard hole is real and untested), and a RED spec exists that will prove zero gsd-* suggestions once 32-02 closes the hole.
    - RED specs exist for every resolve behavior: top-absent fallback to next present, zero-resolvable silent low tier, and empty-resolve high-confidence emitting at most one generic native line.
    - RED cross-runtime fixtures exist proving resolve evaluation uses only the active runtime's present capabilities and a capability present in one runtime resolves to its local equivalent in the other.
    - A RED tie-lint + forward-orphan spec exists targeting resolve lists, and the coverage audit-guard test is extended to demand a schema_version-SET fixture produces a stale_target/forward-orphan diagnostic.
  artifacts:
    - tests/router.schema-v4-routing.test.mjs
    - tests/router.phase32-cross-runtime.test.mjs
    - tests/router.coverage-audit.test.mjs (extended)
  key_links:
    - RED specs reference the future resolve-first route decision consuming a per-entry `resolve` array and an `indexes.commands` presence check that does not exist yet.
    - The schema_version-SET fixture test asserts the CURRENT hole (zero commands yet suggestions leak) so 32-02's closure turns it GREEN.
    - Cross-runtime specs reuse the phase-26 PROFILES + runtime-conditional dirs to drive `ROUTER_RUNTIME`-dependent resolve evaluation.
---

<objective>
Create the Wave-0 RED test infrastructure for Phase 32 so every resolve-first / guard-hole-closure task in 32-02, 32-03 and 32-04 has a runnable, deterministic assertion to turn GREEN.

Purpose: TDD detection-first. The guard hole is currently UNTESTED (the existing fixtures omit `schema_version`, so the `mode===id && schema_version` path has no coverage). Writing a schema_version-SET fixture test FIRST locks the closure: today it leaks gsd-* suggestions on a manifest with no gsd-* commands (the hole), and 32-02 must flip it to zero. The same wave plants RED specs for fallback (ROUTE-03), generic fallback (ROUTE-04), tie-lint/forward-orphan (ROUTE-05), and cross-runtime resolve (PARITY-03/04).

Output: Two new specs and one extended spec, all RED against today's hook.

Deferred note (decision-coverage): D-07 (resolve evaluation considers only active runtime's capabilities) and D-08 (cross-runtime capability-equivalent fixture) were deferred from Phase 31 to Phase 32 — this plan lays their RED test surfaces, reused from the phase-26 dual-runtime PROFILES fixture shape. ROUTE-01/03/04/05 (greenfield) get their RED surfaces here too.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-intent-first-routing-mode-map-schema-v4-guard-hole-closure/32-CONTEXT.md
@.planning/phases/31-runtime-tagging/31-03-SUMMARY.md

# Reference files (read once, extract the exact assertions/localities):
@tests/router.mjs.snapshot  # guard hole: intentionalSchemaRoute:721 + :806; route verification:850-870; buildKnownNames:407; mappedTargets:621
@src/coverage/audit.mjs      # guard hole: schemaRoute:142; forward diagnostics:283
@tests/router.coverage-audit.test.mjs  # runBuilder() + manifest()+ schema_version param:16; validateRouteTargets import:15
@tests/router.phase26-dual-runtime.test.mjs  # PROFILES + runtime-conditional fixture to reuse for PARITY-03/04
@tests/router.perf.test.mjs  # perf-gate pattern reused by 32-04 (not this plan)
</context>

## Artifacts this phase produces (Wave 0 subset)

| Kind | Artifact | What it is |
|------|----------|------------|
| New test file | `tests/router.schema-v4-routing.test.mjs` | RED spec importing the live hook, targets the schema-v4 resolve-first API. Covers ROUTE-01/02/03/04. RED until 32-02/32-03 ship the resolve route decision. |
| New test file | `tests/router.phase32-cross-runtime.test.mjs` | RED cross-runtime resolve spec, reuses phase-26 PROFILES. Covers PARITY-03/04. RED until 32-04 ships runtime-conditional resolve evaluation. |
| Updated test file | `tests/router.coverage-audit.test.mjs` | Adds a schema_version-SET fixture that must produce a slate of stale_target/forward-orphan diagnostics (the hole). RED until 32-03 closes the audit guard. |

<tasks>

<task type="test">
  <name>Task 1: Author schema_version-SET guard-hole spec + resolve-first fallback spec (RED)</name>
  <files>tests/router.schema-v4-routing.test.mjs</files>
  <read_first>tests/router.mjs.snapshot</read_first>
  <action>
    Create `tests/router.schema-v4-routing.test.mjs` importing the live hook at `join(homedir(), '.claude', 'hooks', 'router.mjs')` (same HOOK pattern tests/router.cache.test.mjs:13 and tests/router.coverage-audit.test.mjs:15 use). Import `validateRouteTargets` (exported at tests/router.mjs.snapshot) and the future resolve-first decision helper the snapshot must expose (create an import binding for a symbol like `resolveSlashRoute` — undefined until 32-02 ships it; undefined-import is the RED state). Three test groups:

    Group A — GUARD-HOLE CLOSURE (ROUTE-02, the RED that locks the fix): build a fixture `modeMap` with `schema_version: 4` (SET, per the roadmap note the hole is only exercised when schema_version is set) and entries whose `id === mode` are `gsd-debug`/`gsd-ship` (so the current `intentionalSchemaRoute = mode && mode === id && modeMap?.schema_version` branch at router.mjs:721 fires). Build a manifest whose `commands` array is EMPTY — no `gsd-*` commands present. Assert `validateRouteTargets(manifest, modeMap, indexesWithNoGsdCommands)` produces a `stale_target` diagnostic for every gsd entry (mode does not match a manifest command AND no resolve-list member is present). Today the schema_version truthy branch passes these as intentional → this assertion FAILS (RED). This is the load-bearing closure test: 32-02 must make the resolve check replace the blanket schema_version pass, turning it GREEN.

    Group B — RESOLVE-FIRST PRESENCE (ROUTE-01): build a GSD fixture where `commands` contains `gsd-debug` and a resolve list is present; assert the resolver picks the highest-ranked PRESENT candidate and that a superpowers-style manifest (commands contain `systematic-debugging`, no `gsd-*`) resolves the same capability role to its local present candidate. Assert NO hardcoded framework name appears in the resolve evaluation logic — the candidate set comes from the entry's `resolve` array + `mode`, never a hardcoded `gsd-` prefix in the matcher. RED until 32-02 implements `resolveSlashRoute`.

    Group C — FALLBACK (ROUTE-03): (1) a manifest where the top-ranked candidate (mode) is ABSENT but a lower-ranked `resolve` member IS present → assert the router falls back to the next-best present entry (not silent, not the absent top); (2) a manifest where NO resolve member nor mode is present → assert result is `null`/silent-low (tier low, no slash injection). (3) ROUTE-04: a high-confidence intent whose entry has an EMPTY resolve set → assert at most one generic native-capability fallback line is emitted and no fabricated capability name is produced. RED until 32-02/32-03 implement fallback + generic fallback.

    All assertions reference the future resolve-list field (`entry.resolve` — array of `{ name, weight? }`) and the `indexes.commands` presence set. Keep the test importing only the live hook so the mirror is load-bearing. Note in assertions which symbol each group awaits.
  </action>
  <verify>
    <automated>rtk node --test tests/router.schema-v4-routing.test.mjs 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    The spec file exists and parses. Group A FAILS (RED) against today's hook because the schema_version truthy branch still passes `stale_target` entries — proving the hole is real and the test is load-bearing. Groups B/C FAIL (RED) awaiting `resolveSlashRoute` and the `resolve` field. The assertion set mirrors the future resolve-first decision and framework-neutral candidate list.
  </acceptance_criteria>
  <done>tests/router.schema-v4-routing.test.mjs exists, parses, and is RED on today's hook for all three groups (guard-hole closure, resolve-first presence, fallback/generic fallback), proving the schema_version guard hole is untested-and-leaking before 32-02 closes it.</done>
</task>

<task type="test">
  <name>Task 2: Author cross-runtime resolve spec + forward-orphan/tie-lint RED (PARITY-03/04 + ROUTE-05)</name>
  <files>tests/router.phase32-cross-runtime.test.mjs, tests/router.coverage-audit.test.mjs</files>
  <read_first>tests/router.phase26-dual-runtime.test.mjs</read_first>
  <action>
    Create `tests/router.phase32-cross-runtime.test.mjs` reusing the phase-26 PROFILES fixture pattern to build dual `.claude`/`.codex` roots. Purpoise: prove resolve evaluation consumes ONLY the active runtime's present capabilities. Test groups:

    Group A — ACTIVE-RUNTIME-ONLY EVALUATION (PARITY-03): a fixture where `.claude` commands contains `gsd-debug` but `.codex` does not. Drive the hook with `ROUTER_RUNTIME=claude` vs `ROUTER_RUNTIME=codex` (spawnSync, same pattern tests/router.perf.test.mjs uses) on the same intent. Assert the claude runtime emits the `gsd-debug` slash suggestion and the codex runtime does NOT (only the active runtime's present capabilities are considered; only the active runtime's suggestion is injected). Today the resolve-first evaluation does not exist → both runtimes behave identically (or the suggestion is gated by the wrong runtime) → this FAILS (RED) until 32-04.

    Group B — CROSS-RUNTIME EQUIVALENT RESOLUTION (PARITY-04): a capability present in `.claude` (e.g. `gsd-debug`) must resolve in `.codex` to its local equivalent — i.e. the resolve list carries framework-neutral members so that when `.codex` has `systematic-debugging` present and `.claude` has `gsd-debug`, the same capability role resolves to whichever is present in the ACTIVE runtime. Assert claude → `gsd-debug`, codex → the codex-local present equivalent, per the phase-26 `provenance: [{ runtime }]` shape. RED until 32-04 wires runtime-conditional resolve.

    Group C — TIE-LINT + FORWARD-ORPHAN (ROUTE-05, folded here as the resolve-list lint surface): assert that a resolve list with a near-tie (two members whose weights are within 0.05) must NOT emit an injection at a confident tier — the route is downgraded to `med` or suppressed (RED until 32-03's tie-lint lands). Assert that a resolve-list member targeting a name absent from the active manifest is quarantined (flagged, not shipped) — aligned with the coverage audit-guard's forward-orphan semantics.

    Extend `tests/router.coverage-audit.test.mjs`: add a `runBuilder` variant (mirroring the existing `runBuilder` helper at :16, adding a `schemaVersion` override defaulting to 4 for this block) whose modeMap carries `schema_version: 4` and a mix of present + absent resolve members. Assert `auditCoverage` reports the absent resolve member as a forward-orphan/stale-target diagnostic (the audit-guard must see the hole). Today audit.mjs:142's `schemaRoute = mode === route && Boolean(modeMap.schema_version)` treats those as mapped/valid → the assertion FAILS (RED) until 32-03 closes the audit guard to resolve-list membership.
  </action>
  <verify>
    <automated>rtk node --test tests/router.phase32-cross-runtime.test.mjs tests/router.coverage-audit.test.mjs 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    Both files exist and parse. Cross-runtime Group A/B FAIL (RED) because resolve evaluation is not runtime-conditional yet (PARITY-03/04). Cross-runtime Group C + the coverage-audit extension FAIL (RED) because tie-lint and the resolve-list forward-orphan guard do not exist yet (ROUTE-05). The pre-existing coverage-audit tests continue to pass unchanged.
  </acceptance_criteria>
  <done>tests/router.phase32-cross-runtime.test.mjs and the extended tests/router.coverage-audit.test.mjs exist and are RED on today's hook for active-runtime-only resolve, cross-runtime equivalent resolution, near-tie downgrade, and resolve-list forward-orphan diagnostics (PARITY-03, PARITY-04, ROUTE-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process env / ROUTER_RUNTIME → resolve evaluation | Untrusted/spoofed runtime tag could select a different runtime's capability set at route time |
| mode-map resolve list → emitted slug suggestion | A stale/absent resolve member could be injected as a fabricated slash suggestion |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-01 | Spoofing | runtime detection → resolve eval | medium | mitigate | RED PARITY-03 spec drives ROUTER_RUNTIME=claude vs =codex on same intent; asserts only the active runtime's present capability set is evaluated and injected. |
| T-32-02 | Repudiation | schema_version guard hole | high | mitigate | schema_version-SET fixture RED test (Group A) proves the hole leaks gsd-* suggestions on a no-gsd manifest; closure task in 32-02 must flip it to stale_target. |
| T-32-03 | Tampering | resolve list stale/fabricated targets | high | mitigate | ROUTE-05 RED tie-lint + forward-orphan fixtures demand quarantine of absent resolve members and near-tie downgrade; coverage audit-guard extension insists on a diagnostic. |
| T-32-04 | Information Disclosure | telemetry/cache | low | accept | This plan writes tests only (no telemetry/cache changes); the resolve list never leaks raw prompt text — covered by existing telemetry hashing. |

## Package legitimacy gate
No package-manager installs in this phase — stdlib-only per project CLAUDE.md. No gate required.
</threat_model>

<verification>
Wave-0 targeted run:
`rtk node --test tests/router.schema-v4-routing.test.mjs tests/router.phase32-cross-runtime.test.mjs tests/router.coverage-audit.test.mjs`
Expected: all three RED (guard-hole closure, resolve-first fallback, cross-runtime resolve, tie-lint, audit forward-orphan); pre-existing coverage-audit tests stay GREEN. Full suite at wave merge.
</verification>

<success_criteria>
- A schema_version-SET guard-hole RED test proves today's hook leaks gsd-* slash suggestions on a manifest with zero gsd-* commands (ROUTE-02 hole locked).
- RED resolve-first specs cover: top-absent fallback to next present, zero-resolvable silent low, empty-resolve single generic fallback (ROUTE-01/03/04).
- RED cross-runtime specs cover active-runtime-only evaluation and cross-runtime equivalent resolution (PARITY-03/04).
- RED tie-lint + forward-orphan specs and a coverage audit-guard extension locked (ROUTE-05).
- No feature code was written; implementation is deferred to 32-02 (resolve-first + closure), 32-03 (generic fallback + tie-lint/audit), 32-04 (cross-runtime + perf).
</success_criteria>

<output>
Create `.planning/phases/32-intent-first-routing-mode-map-schema-v4-guard-hole-closure/32-01-SUMMARY.md` when done
</output>

---
phase: 16-workflow-first-orchestration-and-context-budgets
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - src/orchestrator/select.mjs
  - tests/router.workflow-orchestrator.test.mjs
autonomous: true
requirements: [ORC-01]
nyquist_compliant: true
must_haves:
  truths:
    - "D-01/D-02/D-03/D-04: capability selection accepts only one resolved, dispatch-eligible, gate-safe workflow outcome from Plan 16-01."
    - "D-05/D-06: declared workflow ownership seeds a stable transitive closure across skills, commands, agents, MCPs, tools, models, permissions, and lifecycle requirements; lexical resemblance cannot seed it."
    - "D-07/D-08: compatible explicit capability requests may narrow the closure, incompatible requests block without workflow switching, and hooks remain non-invokable event bindings."
    - "D-09/D-10/D-11/D-12/D-13: closure output exposes required dependency facts for later budgeting but performs no context loading, accounting, summary reuse, persistence, or hot-path compilation."
  artifacts:
    - path: src/orchestrator/select.mjs
      provides: "Workflow-token validation, declared owner selection, explicit narrowing, stable dependency closure, hook separation, and blocker outcomes"
    - path: tests/router.workflow-orchestrator.test.mjs
      provides: "ORC-01 ordering, closure, safety, availability, cycle, explicit-request, and no-lexical-selection matrix"
  key_links:
    - from: src/orchestrator/select.mjs
      to: src/orchestrator/transitions.mjs
      via: "requires one selected and dispatch-eligible workflow outcome before registry traversal"
    - from: src/orchestrator/select.mjs
      to: src/registry/schema.mjs
      via: "consumes canonical dependency, permission, scope, lifecycle, dispatchability, and provenance fields"
---

<objective>
Implement workflow-compatible capability selection and deterministic dependency closure for ORC-01 and D-05 through D-08, preserving D-01 through D-04 as hard prerequisites and D-09 through D-13 as downstream boundaries.

Purpose: Ensure skills, commands, agents, MCPs, tools, models, permissions, and hooks can only follow the selected workflow through declared edges.
Output: `src/orchestrator/select.mjs` and the complete workflow/capability selection matrix.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-workflow-first-orchestration-and-context-budgets/16-CONTEXT.md
@.planning/phases/16-workflow-first-orchestration-and-context-budgets/16-RESEARCH.md
@.planning/phases/16-workflow-first-orchestration-and-context-budgets/16-01-SUMMARY.md
@src/orchestrator/transitions.mjs
@src/registry/schema.mjs
@src/registry/map.mjs
@tests/router.workflow-orchestrator.test.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce workflow-first ownership and explicit compatibility</name>
  <read_first>src/orchestrator/transitions.mjs, src/registry/schema.mjs, src/registry/map.mjs, tests/router.workflow-orchestrator.test.mjs, .planning/phases/16-workflow-first-orchestration-and-context-budgets/16-CONTEXT.md</read_first>
  <files>tests/router.workflow-orchestrator.test.mjs, src/orchestrator/select.mjs</files>
  <behavior>
    - Missing, blocked, ambiguous, terminal, or non-dispatchable workflow outcomes reject before any registry accessor is called.
    - One eligible workflow selects only its declared owners and requirements; prompt tokens and capability-name resemblance cannot add MCPs, tools, or other capabilities.
    - A compatible explicit capability request narrows the declared set; an incompatible request returns one focused non-dispatchable reason without changing or merging workflows.
    - Permuted workflow declarations, registry records, and explicit requests produce byte-stable results.
  </behavior>
  <action>Write accessor-instrumented failing tests first, then implement exported `selectCapabilities` (and `selectWorkflow` only if Plan 16-01 deliberately exposes selection separately) around a strict resolved-workflow token validator. Per D-01 through D-05 return before registry traversal unless exactly one selected workflow is dispatch eligible. Seed selection exclusively from declared workflow ownership/requirements; never score prompt text or names. Per D-07 accept an explicit capability request only when it belongs to the selected workflow's declared compatible set, narrowing rather than expanding it; otherwise return a focused stable blocker and preserve the workflow identity. Canonicalize input collections and JSON-ready outputs using repository conventions.</action>
  <verify><automated>node --test tests/router.workflow-orchestrator.test.mjs</automated></verify>
  <acceptance_criteria>Instrumented tests prove zero registry access before workflow eligibility, zero lexical additions, stable explicit narrowing, and no workflow switch/merge on incompatibility.</acceptance_criteria>
  <done>D-01 through D-05 and D-07 are enforced as an API boundary satisfying the ordering half of ORC-01.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resolve stable safe transitive dependency closure</name>
  <read_first>src/orchestrator/select.mjs, src/registry/schema.mjs, src/registry/map.mjs, tests/router.workflow-orchestrator.test.mjs, .planning/phases/16-workflow-first-orchestration-and-context-budgets/16-RESEARCH.md</read_first>
  <files>tests/router.workflow-orchestrator.test.mjs, src/orchestrator/select.mjs</files>
  <behavior>
    - Declared roots traverse skills, commands, agents, MCPs, tools, models, permissions, and lifecycle requirements in canonical kind/ID order with deterministic deduplication.
    - Missing, unavailable, unsafe, out-of-scope, permission-incomplete, non-dispatchable, conflicting, and cyclic dependencies return the first stable blocker and no dispatchable closure.
    - Hooks are reported only as event-bound lifecycle bindings and never occur in the invokable-capability collection.
    - Equivalent graphs with permuted records/edges produce byte-identical closure or blocker outcomes.
  </behavior>
  <action>Add the graph matrix first, then implement exported `resolveDependencies` and integrate it with selection. Per D-06 traverse only declared canonical IDs with explicit visiting/visited cycle handling, apply registry lifecycle/dispatchability/scope/permission/conflict/availability facts at each node, and choose the first blocker in documented stable kind-then-ID order. Keep invokable capabilities, required permissions/models, and lifecycle bindings distinct in the result. Per D-08 hooks may satisfy lifecycle declarations but must never be returned as an ordinary invocation. Emit bounded provenance and reason facts needed by the context planner, without reading source content or persisting results per D-09 through D-13.</action>
  <verify><automated>node --test tests/router.workflow-orchestrator.test.mjs tests/router.registry-map.test.mjs</automated></verify>
  <acceptance_criteria>All supported dependency kinds, multi-hop graphs, cycles, safety failures, explicit requests, and hook bindings have stable tested outcomes; unsafe/unavailable closure is never dispatch eligible.</acceptance_criteria>
  <done>D-06 and D-08 complete workflow-owned dependency closure and the full ORC-01 selection contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Risk | Mitigation |
|---|---|---|
| Workflow outcome to registry traversal | Forged eligibility could bypass workflow-first ordering | Strict selected-token shape and early return before accessor use. |
| Registry graph to dispatch closure | Missing, cyclic, unsafe, or permission-incomplete nodes could activate | Canonical traversal, cycle detection, and fail-closed safety checks. |
| Hook metadata to invocation output | Event bindings could be elevated into ordinary tools | Separate lifecycle-binding collection and explicit hook exclusion tests. |
</threat_model>

<verification>
Run `node --test tests/router.workflow-orchestrator.test.mjs tests/router.registry-map.test.mjs`, then `node --test tests/*.test.mjs` and `git diff --check`. Inspect imports/exports and fixtures to prove no prompt-lexical MCP/tool route and no hook invocation path exists.
</verification>

<success_criteria>
- ORC-01 is enforced structurally: workflow selection precedes every capability and dependency read.
- Only declared, compatible, safe, available closure nodes are dispatchable; the first blocker is stable.
- Context planning remains side-effect-free downstream work and Phase 17 integration surfaces are untouched.
</success_criteria>

## Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Compatible capabilities and dependencies follow the chosen workflow | 16-02 | COVERED | Declared-edge closure. |
| REQ | ORC-01 | Workflow precedes skill/command/agent/MCP/tool selection | 16-02 | COVERED | Accessor and no-lexical tests. |
| RESEARCH | Workflow token, stable closure, first blocker, hook separation | 16-02 | COVERED | Tasks 1-2. |
| CONTEXT | D-01–D-08 | Workflow prerequisite and capability closure | 16-02 | COVERED | Direct tests and implementation. |
| CONTEXT | D-09–D-13 | Context planner and purity boundary | 16-02 | COVERED | Emits facts only; Plan 16-03 implements budgets. |

<output>Create `.planning/phases/16-workflow-first-orchestration-and-context-budgets/16-02-SUMMARY.md` when done.</output>

---
phase: 26-coherent-publication-and-dual-runtime-release
plan: 07
type: execute
wave: 5
depends_on: [26-05, 26-06]
files_modified:
  - tests/helpers/inventory-fixture.mjs
  - tests/router.phase26-performance.test.mjs
autonomous: true
requirements: [REL-01, REL-06, REL-07]
must_haves:
  truths:
    - "A deterministic mixed registry contains at least 300 normalized records and every recommendation kind."
    - "Real installed prompt routes have warm p95 below 25 ms and every sample below 100 ms."
    - "Every emitted UTF-8 context remains within existing byte and token budgets."
  artifacts:
    - "tests/router.phase26-performance.test.mjs"
  key_links:
    - "Generated fixture passes through real compile, publish, load, install, and deployed route seams."
    - "Machine-readable RELEASE_METRICS feed the release gate."
---

<objective>
Produce isolated realistic performance and bounded-context evidence for installed Claude and Codex routing.

Purpose: Validate the complete tuple under a local registry scale that can expose latency or context regressions.
Output: Deterministic large fixture and machine-readable route metrics.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-coherent-publication-and-dual-runtime-release/26-VALIDATION.md
@.planning/phases/26-coherent-publication-and-dual-runtime-release/26-05-SUMMARY.md
@.planning/phases/26-coherent-publication-and-dual-runtime-release/26-06-SUMMARY.md
@tests/helpers/inventory-fixture.mjs
@src/evolution/perf-measure.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generate and route a realistic mixed installed registry</name>
  <files>tests/helpers/inventory-fixture.mjs, tests/router.phase26-performance.test.mjs</files>
  <behavior>
    - The fixture deterministically generates at least 300 unique normalized Claude/Codex records.
    - It includes command, skill, agent, workflow, MCP, tool, contracts, edges, and workflows.
    - It uses the real complete tuple and installed route entry points.
  </behavior>
  <action>Extend the existing inventory fixture helper with deterministic indexed repetition and stable unique IDs; do not hand-maintain hundreds of records. Build, publish, load, install, and route the complete tuple for both runtimes and representative cases for every recommendation kind.</action>
  <verify>
    <automated>rtk node --test tests/router.phase26-performance.test.mjs --test-name-pattern="registry|installed|kind"</automated>
  </verify>
  <done>The test proves at least 300 normalized records and executes real installed routes for both runtimes and all six kinds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce isolated latency and context budgets</name>
  <files>tests/router.phase26-performance.test.mjs</files>
  <behavior>
    - After warmup, each representative case has at least 20 measured routes.
    - Nearest-rank warm p95 is below 25 ms and every elapsed sample is below 100 ms.
    - UTF-8 bytes and the existing token estimate remain within current budgets.
  </behavior>
  <action>Reuse `measureRoutes()`/`assessCalibration()` and existing budget evaluators. Run the file alone, warm each case, collect at least 20 measurements, assert strict thresholds, and emit one machine-readable `RELEASE_METRICS` record containing registry size, samples, p95, max, and context bounds. Do not use full-suite wall time as route evidence.</action>
  <verify>
    <automated>rtk node --test tests/router.phase26-performance.test.mjs</automated>
  </verify>
  <done>The isolated test passes all strict latency and context thresholds and emits complete release metrics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated fixture -> benchmark | Synthetic data must represent real normalized runtime shapes. |
| measured process -> release evidence | Metrics must be isolated, sufficient, and machine-verifiable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-15 | Repudiation | performance evidence | high | mitigate | Minimum scale/samples, strict assertions, and machine-readable metrics. |
| T-26-16 | Denial of Service | prompt context | high | mitigate | Existing byte/token bounds enforced on every case. |
</threat_model>

<verification>
Run `rtk node --test tests/router.phase26-performance.test.mjs` alone and retain its final `RELEASE_METRICS`.
</verification>

<success_criteria>
REL-07 is proven on at least 300 records with strict p95, max, byte, and token assertions.
</success_criteria>

<output>
Create `.planning/phases/26-coherent-publication-and-dual-runtime-release/26-07-SUMMARY.md` when done.
</output>

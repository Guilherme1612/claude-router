---
phase: 19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-ORCHESTRATOR-INPUT-DECISION.md
  - .planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-VALIDATION.md
autonomous: true
requirements:
  - ORC-01
  - TOK-02
must_haves:
  truths:
    - "The orchestrator-input sourcing question (Q1 from RESEARCH.md) is resolved in a committed decision document before any wiring task begins (per D-01)."
    - "The sibling tuple file shape (closure.json/budget.json/summary-index.json), manifest hash extension, compatible() new members, and COMPILED_INDEX_LIMITS extension are locked concretely so Plans 02 and 03 implement without re-deciding (per D-04, D-05, security V6)."
    - "19-VALIDATION.md is populated with the phase's Test Infrastructure, Sampling Rate, Per-Task Verification Map, and Wave 0 Requirements so the Nyquist Dimension 8 gate has a concrete contract (per RESEARCH.md Validation Architecture)."
    - "Q1 is flagged [ASSUMED] so the plan-checker and the user can surface it; the decision is Claude's Discretion per CONTEXT.md but is not silently absorbed."
  artifacts:
    - ".planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-ORCHESTRATOR-INPUT-DECISION.md (the load-bearing design decision)"
    - ".planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-VALIDATION.md (populated, nyquist_compliant: true)"
  key_links:
    - "19-ORCHESTRATOR-INPUT-DECISION.md is referenced by 19-02-PLAN.md and 19-03-PLAN.md task <read_first> lists so wiring executors consume the locked shape verbatim."
---

<objective>
Resolve the load-bearing Q1 design question (orchestrator-input sourcing) and lock the sibling-tuple / manifest / compatibility-gate shape before any wiring work begins, then populate the phase validation scaffold. This unblocks Plans 02 (compile-index + publish-index wiring) and 03 (bundle + route-path projection) so they implement against a fixed contract rather than re-deciding during execution.

Purpose: RESEARCH.md Open Question Q1 flags that `workflowDeclarations` and per-workflow `evidence` have NO production source — only unit-test fixtures. Wiring cannot start without locking their source. CONTEXT.md marks exact field names/shapes as Claude's Discretion; this plan exercises that discretion and records the decision as `[ASSUMED]` per the planning context contract.

Output: A committed decision doc (19-ORCHESTRATOR-INPUT-DECISION.md) and a populated 19-VALIDATION.md. No production code changes in this plan.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-CONTEXT.md
@.planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-RESEARCH.md
@.planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-PATTERNS.md
@.planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-VALIDATION.md
@src/orchestrator/select.mjs
@src/orchestrator/transitions.mjs
@src/orchestrator/budget.mjs
@src/prompt/publish-index.mjs
@src/prompt/compile-index.mjs
@src/registry/watcher.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Q1 design spike — lock orchestrator-input source + sibling tuple shape + manifest/compatibility extension</name>
  <files>.planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-ORCHESTRATOR-INPUT-DECISION.md</files>
  <read_first>
    - .planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-CONTEXT.md (D-01..D-10 locked decisions + Claude's Discretion areas)
    - .planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-RESEARCH.md (Open Questions Q1/Q2, Assumptions A1/A2/A3, Common Pitfalls #2/#3, Validation Architecture)
    - .planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-PATTERNS.md (publish-index.mjs manifest shape :85-91; compile-index.mjs compatible() :72-76; sibling file shape recommendation :271-279)
    - src/orchestrator/select.mjs (selectCapabilities signature :160, workflowDeclarations consumption :164-165, blocked shape :9-11)
    - src/orchestrator/transitions.mjs (nextValidTransitions :69, selectWorkflow :143, evidence shape requirements :73-83, WORKFLOW_TRANSITIONS :9-19)
    - src/orchestrator/budget.mjs (planContextLoad :131, DEFAULT_CONTEXT_CONTRACT :21-30, CONTEXT_CONTRACT_VERSION :4, ESTIMATOR_VERSION :3, validateContextContract :46-69)
    - src/prompt/publish-index.mjs (publishCompiledIndex signature :54, durableWrite :10-14, manifest :85-91)
    - src/prompt/compile-index.mjs (COMPILED_INDEX_COMPATIBILITY :6-10, compatible() :72-76, verifyTuple hash checks :121-132, COMPILED_INDEX_LIMITS :11-18)
    - src/registry/watcher.mjs (publish call site :338-343)
    - tests/router.workflow-orchestrator.test.mjs (workflowDeclaration() and evidence() fixtures — the only existing templates)
  </read_first>
  <action>
    Create 19-ORCHESTRATOR-INPUT-DECISION.md. This is a design spike decision document, NOT production code. The document MUST record the following locked decisions (Claude's Discretion per CONTEXT.md, flagged [ASSUMED] per the planning context):

    1. **workflowDeclarations source [ASSUMED]** — Option chosen: a NEW static file `src/orchestrator/workflow-declarations.json` containing an array of `{ workflow_id, owners, requirements, compatible }` records, one per workflow family present in the v1.2 workflow set. This file is deployed via the `moduleNames` bundle extension (Plan 03 adds `'orchestrator/workflow-declarations.json'` alongside the three .mjs entries from D-07). `publishCompiledIndex` reads it via the relative path `join(dirname(fileURLToPath(import.meta.url)), '..', 'orchestrator', 'workflow-declarations.json')` — this resolves correctly in BOTH the source layout (`src/prompt/publish-index.mjs` → `src/orchestrator/workflow-declarations.json`) and the deployed layout (`modules/prompt/publish-index.mjs` → `modules/orchestrator/workflow-declarations.json`). No new `publishCompiledIndex` parameter is added; the watcher call site at `watcher.mjs:338-343` is NOT extended. Rejected alternatives: (a) new publishCompiledIndex param from watcher — rejected because it would require extending watcher config and the watcher has no declarations source either; (b) derive declarations from registry records — rejected because grep confirms registry records carry no `owners`/`requirements`/`declarations` fields.

    2. **Per-workflow evidence shape [ASSUMED]** — At publish time, for each mapped subject (whose `subject_id` becomes `workflow_id`), construct synthetic evidence: `{ status: 'active', freshness: 'fresh', position: { family: <family derived from workflow_id prefix before first '-' or the full workflow_id if no '-'>, state: 'planned' }, gates: { plan_approved: true }, dependencies_safe: true }`. This matches the `evidence()` fixture template in `tests/router.workflow-orchestrator.test.mjs:12-21`. The `position.state: 'planned'` is the canonical publish-time state (publish bakes for workflows, not one live capsule position).

    3. **Q2 resolution — bake candidate set AND selected transition per family [ASSUMED]** — Publish runs `nextValidTransitions(evidence, WORKFLOW_TRANSITIONS)` to get the candidate set, AND `selectWorkflow(candidates, undefined)` (no explicitIntent — defaults to uniqueness-based selection per `transitions.mjs:143-183`) to pick one selected transition per family. Bake BOTH into the closure sibling. The route path reads the selected transition for the canonical case, and filters the candidate set by the live capsule `position.state` when the capsule state does not match the selected transition's `from` state. This filtering is a pure read over baked data — D-01/D-02 compliant (no orchestrator call on the hot path). Rejected: baking only the selected transition (route path would be unable to route from a non-`planned` capsule state); baking only candidates (loses the canonical default, forces the route path to re-implement selectWorkflow).

    4. **Sibling tuple file shape (per D-05, per Pitfall #3, per CONTEXT.md Claude's Discretion)** — Per-workflow keyed maps mirroring the existing `index.routes?.[workflowId]` projection at `prompt-route.mjs:104`. Concrete shapes:
       - `closure.json`: `{ schema_version: 1, by_workflow: { "<workflow_id>": { selected_transition, candidates, closure, invokable_capabilities, required_models, required_permissions, lifecycle_bindings, dispatch_eligible, reason_code } } }` — inner fields aligned with `selectCapabilities` return (`select.mjs:128-139`) plus the transition results.
       - `budget.json`: `{ schema_version: 1, by_workflow: { "<workflow_id>": { report, dispatch_eligible, reason_code } } }` — inner `report` aligned with `planContextLoad` report (`budget.mjs:200-209`: `contract_version`, `estimator_version`, `total_max_bytes`, `canonical_bytes`, `estimated_tokens`, `included_sources`, `omitted_sources`, `regression_delta`).
       - `summary-index.json`: `{ schema_version: 1, by_workflow: { "<workflow_id>": <summary_index_ref or null> } }`.
       When an orchestrator call returns `blocked` or `dispatch_eligible: false` for a workflow, bake that `dispatch_eligible: false` + `reason_code` into the per-workflow entry (do NOT throw — D-03 pattern: required-overflow → non-dispatchable at publish, route path observes the flag).

    5. **Manifest extension (Security V6 — required for Phase 17 D-02 fail-closed integrity gate)** — Extend the manifest at `publish-index.mjs:85-91` with three new payload_sha256 fields alongside the existing `registry` and `compiled` entries: `closure: { payload_sha256: sha256(closureBytes) }`, `budget: { payload_sha256: sha256(budgetBytes) }`, `summary_index: { payload_sha256: sha256(summaryIndexBytes) }`. The existing `verifyTuple` at `compile-index.mjs:121-132` is extended (in Plan 02) to read each sibling via `boundedJson`, compute sha256, and compare against the manifest field — rejecting the tuple if any sibling hash mismatches or is missing. This closes the tampering threat T-19-01.

    6. **compatible() extension (per D-04)** — Extend `COMPILED_INDEX_COMPATIBILITY` at `compile-index.mjs:6-10` with two new members: `orchestrator_contract_version: 'workflow-first-v1'` and `context_contract_version: CONTEXT_CONTRACT_VERSION` (imported from `src/orchestrator/budget.mjs:4`). Extend `compatible()` at `:72-76` to check both new members equal the frozen constants. This invalidates all schema-1 tuples (re-publish via watcher recovery).

    7. **COMPILED_INDEX_LIMITS extension (per Pitfall #5)** — Add three size limits to `COMPILED_INDEX_LIMITS` at `compile-index.mjs:11-18`: `closure_bytes: 64 * 1024`, `budget_bytes: 32 * 1024`, `summary_index_bytes: 16 * 1024`. Siblings are bounded independently so a blocked route does not pay the closure/budget read cost (route path reads siblings lazily — only when a dispatch-eligible projection exists).

    8. **Pointer schema_version bump** — The active.json/known-good.json pointer `schema_version` bumps 1→2 alongside the tuple schema bump, and `verifyTuple`'s pointer check at `compile-index.mjs:121` updates from `pointer?.schema_version !== 1` to `pointer?.schema_version !== 2`. (The pointer schema is coupled to the tuple schema; bumping only the index.json schema_version would leave a mismatched pointer.)

    9. **loadCompiledIndex return shape (per D-05, Claude's Discretion)** — Additive flat keys: the `ready` return at `compile-index.mjs:137-152` gains `closure`, `budget`, `summaryIndex` keys (the parsed sibling JSON, already hash-verified by verifyTuple). Sub-object grouping rejected — flat keys keep `validRoutes()` and the hot-path read surface smallest (route path reads `compiledIndex.closure?.[workflowId]` mirroring `compiledIndex.index.routes?.[workflowId]`).

    10. **dispatch_eligible flag (per D-03)** — The per-workflow budget sibling entry carries `dispatch_eligible` baked from the `planContextLoad` result. Required-overflow at publish → `dispatch_eligible: false` → route path observes it and synthesizes the existing blocked resolution at `prompt-route.mjs:105-110`. Per-prompt budget estimation is DEFERRED to v2 (D-03) — do NOT add `estimateRoutingTokens` to the route path.

    The document MUST end with an "Assumptions Register" table listing A1..A5 from RESEARCH.md with the locked resolution and a `[ASSUMED]` tag on each decision that came from Claude's Discretion (items 1, 2, 3 above). Items 4-10 are mechanical implementations of CONTEXT.md locked decisions (D-04/D-05) or security requirements (V6) and are NOT tagged [ASSUMED].

    Do NOT modify production code in this task. The document is the deliverable.
  </action>
  <verify>
    <automated>test -f .planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-ORCHESTRATOR-INPUT-DECISION.md &amp;&amp; grep -c "workflow-declarations.json" .planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-ORCHESTRATOR-INPUT-DECISION.md | grep -qv '^0$' &amp;&amp; grep -c "ASSUMED" .planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-ORCHESTRATOR-INPUT-DECISION.md | grep -qv '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `.planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-ORCHESTRATOR-INPUT-DECISION.md`
    - Document contains the literal string `workflow-declarations.json` (the locked declarations source)
    - Document contains the literal string `[ASSUMED]` at least three times (declarations source, evidence shape, Q2 candidate+selected bake)
    - Document contains the literal strings `orchestrator_contract_version`, `context_contract_version`, `closure.payload_sha256`, `budget.payload_sha256`, `summary_index.payload_sha256` (the locked compatibility + manifest extension)
    - Document contains the literal strings `closure_bytes`, `budget_bytes`, `summary_index_bytes` (the locked COMPILED_INDEX_LIMITS extension)
    - Document contains the literal string `by_workflow` (the locked per-workflow-keyed sibling shape)
    - Document contains the literal string `dispatch_eligible` (the D-03 flag)
    - No production code file under `src/` is modified in this task (verify via `git diff --name-only src/` returning empty)
  </acceptance_criteria>
  <done>
    19-ORCHESTRATOR-INPUT-DECISION.md exists with all 10 locked decisions, at least three [ASSUMED] tags, and the concrete sibling/manifest/compatibility shape that Plans 02 and 03 implement verbatim. No production code touched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Populate 19-VALIDATION.md — Test Infrastructure, Sampling Rate, Per-Task Verification Map, Wave 0 Requirements</name>
  <files>.planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-VALIDATION.md</files>
  <read_first>
    - .planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-VALIDATION.md (current draft skeleton)
    - .planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-RESEARCH.md (Validation Architecture section — Test Framework, Phase Requirements→Test Map, Sampling Rate, Wave 0 Gaps)
    - .planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-ORCHESTRATOR-INPUT-DECISION.md (the locked sibling shape from Task 1)
  </read_first>
  <action>
    Fill the 19-VALIDATION.md template per RESEARCH.md `## Validation Architecture` (line 420). Concrete values:

    - **Frontmatter**: set `status: validated`, `nyquist_compliant: true`, `wave_0_complete: false` (Wave 0 is a Plan 04 responsibility; this plan only scaffolds the contract).
    - **Test Infrastructure table**: Framework = `node:test (built-in, Node 22) + node:assert/strict`; Config file = `none — tests are standalone .mjs files run via node --test`; Quick run command = `node --test tests/router.autonomous-lifecycle.test.mjs tests/router.test-mode-seam.test.mjs tests/router.compiled-index.test.mjs tests/router.workflow-orchestrator.test.mjs`; Full suite command = `node --test tests/*.test.mjs` (65 test files); Estimated runtime = ~25 seconds.
    - **Sampling Rate**: After every task commit = the quick run command; After every plan wave = `node --test tests/*.test.mjs`; Before `/gsd-verify-work` = full suite green + v1.2 release gate (`tests/router.v12-release.test.mjs` via `src/release/run-release.mjs`); Max feedback latency = 30s.
    - **Per-Task Verification Map**: populate one row per task across Plans 02/03/04 using the Requirement→Test Map from RESEARCH.md (ORC-01 and TOK-02 rows). Threat Ref column: T-19-01 for sibling-hash tasks (Plan 02), T-19-02 for bundle-deployment tasks (Plan 03), — for fixture/D-09 tasks. Test Type: unit for compile-index schema tests; E2E for autonomous-lifecycle/test-mode-seam. File Exists: ✅ for existing test files (EXTEND per D-09), ❌ W0 for the ~10 fixture-update files (Wave 0 audit). Status: ⬜ pending for all rows.
    - **Wave 0 Requirements**: list the 5 gaps from RESEARCH.md Validation Architecture Wave 0 Gaps section (autonomous-lifecycle D-09 assertions; test-mode-seam D-09 assertions; ~10 fixture-update files schema_version bump; lifecycle-recovery + compiled-index empty-mapping audit; release/v1.2-matrix.json Phase 19 secondary evidence entry for ORC-01 + TOK-02).
    - **Manual-Only Verifications**: "All phase behaviors have automated verification." (the test_mode seam drives the E2E; no manual UI steps exist for this wiring phase.)
    - **Validation Sign-Off**: leave checkboxes unchecked — the checker/validate-phase step sets them.

    Do NOT mark `nyquist_compliant: true` in frontmatter if any Wave 0 row is ❌ W0 — instead set `nyquist_compliant: false` and `wave_0_complete: false`, and note that Plan 04 completes Wave 0 during execution. (The Nyquist gate fires at validate-phase; this plan only scaffolds the contract.)
  </action>
  <verify>
    <automated>grep -c "node --test tests/router.autonomous-lifecycle.test.mjs" .planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-VALIDATION.md | grep -qv '^0$' &amp;&amp; grep -c "ORC-01" .planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-VALIDATION.md | grep -qv '^0$' &amp;&amp; grep -c "TOK-02" .planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-VALIDATION.md | grep -qv '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - 19-VALIDATION.md Test Infrastructure table is filled with the concrete `node --test` commands (no `{pytest 7.x}` placeholder remains)
    - Per-Task Verification Map contains at least one row citing `ORC-01` and at least one row citing `TOK-02`
    - Wave 0 Requirements section lists all 5 gaps from RESEARCH.md (autonomous-lifecycle, test-mode-seam, ~10 fixture files, lifecycle-recovery/compiled-index empty-mapping audit, v1.2-matrix.json secondary entry)
    - Frontmatter `status` is set to `validated` (not `draft`); `nyquist_compliant` reflects Wave 0 incomplete state (false if any ❌ W0 row, else true)
    - Sampling Rate section specifies the full-suite command `node --test tests/*.test.mjs` for wave merges
  </acceptance_criteria>
  <done>
    19-VALIDATION.md is a complete validation contract: Test Infrastructure populated, Sampling Rate concrete, Per-Task Verification Map covers ORC-01 + TOK-02 across Plans 02/03/04, Wave 0 Requirements enumerate the 5 gaps. The Nyquist Dimension 8 gate has a concrete contract to enforce.
  </done>
</task>

</tasks>

<assumption_delta_decision>
  **Assumption-Delta scan result:** `detected: true` (pluralization + chosen). The phase introduces a second source of `workflowDeclarations` (production static file alongside the existing test fixtures) and a derived value (`position.state='planned'`) becomes a chosen publish-time parameter.

  **Noun now primary:** `workflowDeclarations` — the generalized identity is "the authoritative workflow declarations source for the publish-time orchestrator". The test fixtures remain a secondary consumer (tests construct their own declarations for unit isolation); the static file is the production primary.

  **Decision:** `promote` — the new `src/orchestrator/workflow-declarations.json` becomes the production primary; the test fixtures in `tests/router.workflow-orchestrator.test.mjs` are demoted to test-only isolation helpers (their existing role, now made explicit). Rationale: the orchestrator was always intended to consume declarations from a production source (Phase 16 D-13 said "context planning is side-effect-free; persistence and hot-path compilation belong to later integration surfaces" — Phase 19 is that later surface). Adding the static file alongside without promoting would contradict the wiring intent.

  **Accepted debt:** none — the test fixtures continue to serve unit-test isolation; they are not "old primary demoted", they are "test helpers, now joined by a production source". No future force required; the promotion is complete in this phase.

  **Optional invariant test (suggested, not required):** a contract test asserting that `publishCompiledIndex` reads `workflow-declarations.json` from the deployed `modules/orchestrator/` path and that the deployed bytes equal the source bytes (mirrors the test-mode-seam static-invariant pattern at `tests/router.test-mode-seam.test.mjs:103-110`). This is folded into the D-09 bundle-presence assertions in Plan 04 — no separate test task added here.
</assumption_delta_decision>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| controller→tuple-disk | Publish-time orchestrator output crosses from in-memory computation to immutable on-disk sibling tuple files (closure.json/budget.json/summary-index.json) |
| disk→route-path | Route path reads baked siblings via loadCompiledIndex; untrusted disk bytes cross into the hot path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-01 | Tampering | sibling tuple files (closure.json/budget.json/summary-index.json) — could be swapped on disk undetected | high | mitigate | Plan 02 extends manifest with closure/budget/summary_index payload_sha256 + extends verifyTuple to hash-check siblings; missing/mismatched hash → tuple rejected → blocked(). This plan locks the manifest extension shape (decision #5) so Plan 02 implements it verbatim. |
| T-19-02 | Tampering | src/orchestrator/workflow-declarations.json — static declarations file could be tampered pre-publish | medium | accept | Declarations file is controller-owned (deployed via moduleNames into ownedRoot/modules/); the controller process is the trusted publisher. Tampering would require controller compromise, at which point the attacker already controls publication. The bundled file is verified by the existing installRouter integrity path. |
| T-19-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this phase (stdlib-only wiring). N/A — the gate is satisfied trivially. |
</threat_model>

<verification>
- 19-ORCHESTRATOR-INPUT-DECISION.md exists with all 10 locked decisions and ≥3 [ASSUMED] tags.
- 19-VALIDATION.md is populated with concrete node --test commands and the ORC-01/TOK-02 Per-Task Verification Map.
- No production code under `src/` is modified in this plan.
- `git diff --name-only src/` returns empty after this plan.
</verification>

<success_criteria>
- The load-bearing Q1 design question is resolved and committed; Plans 02 and 03 can implement against a fixed contract without re-deciding.
- The validation scaffold is concrete enough for the Nyquist Dimension 8 gate to enforce.
- The security threat model surfaces T-19-01 (sibling tampering) and assigns mitigation to Plan 02.
</success_criteria>

<output>
Create `.planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-01-SUMMARY.md` when done.

## Artifacts this phase produces

This plan (Plan 01) produces NO new production symbols. It produces:
- **New planning artefacts:**
  - `.planning/phases/19-.../19-ORCHESTRATOR-INPUT-DECISION.md` — the locked design decision (10 items + Assumptions Register)
  - `.planning/phases/19-.../19-VALIDATION.md` — populated validation contract (Test Infrastructure, Sampling Rate, Per-Task Verification Map, Wave 0 Requirements)
- **Locked contracts for Plans 02/03/04 to implement verbatim:**
  - New static file path: `src/orchestrator/workflow-declarations.json` (+ deployment via `moduleNames` entry `'orchestrator/workflow-declarations.json'` — Plan 03 adds it alongside the three D-07 .mjs entries)
  - New sibling tuple file names: `closure.json`, `budget.json`, `summary-index.json` (written by Plan 02's publish-index.mjs edit)
  - New `COMPILED_INDEX_COMPATIBILITY` members: `orchestrator_contract_version: 'workflow-first-v1'`, `context_contract_version: <CONTEXT_CONTRACT_VERSION from budget.mjs:4>` (Plan 02 adds)
  - New `COMPILED_INDEX_SCHEMA_VERSION` value: `2` (Plan 02 bumps from 1)
  - New `COMPILED_INDEX_LIMITS` members: `closure_bytes: 64*1024`, `budget_bytes: 32*1024`, `summary_index_bytes: 16*1024` (Plan 02 adds)
  - New manifest fields: `closure.payload_sha256`, `budget.payload_sha256`, `summary_index.payload_sha256` (Plan 02 adds to publish-index.mjs manifest + compile-index.mjs verifyTuple)
  - New `loadCompiledIndex` return keys: `closure`, `budget`, `summaryIndex` (Plan 02 adds — additive flat keys)
  - New per-workflow sibling shape: `{ schema_version: 1, by_workflow: { "<workflow_id>": {...} } }` (Plan 02 writes)
  - New `dispatch_eligible` flag field on each per-workflow budget entry (Plan 02 bakes from planContextLoad result; Plan 03's route path observes it)
  - New pointer `schema_version: 2` (Plan 02 bumps 1→2; verifyTuple pointer check updates)
- **Removed (Plan 02 executes, Plan 01 declares):** `reason_code: 'canonical_record'` and the blanket fallback at `publish-index.mjs:63-67` (D-06)

The newly-created symbols that downstream plan-review-convergence source-grounding must exclude from drift verification are all in the "Locked contracts" list above — they appear first in 19-ORCHESTRATOR-INPUT-DECISION.md and are implemented by Plans 02/03.
</output>
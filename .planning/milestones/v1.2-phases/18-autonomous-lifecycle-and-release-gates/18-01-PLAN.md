---
phase: 18-autonomous-lifecycle-and-release-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/prompt/publish-index.mjs
  - src/prompt/compile-index.mjs
  - src/context/prompt-route.mjs
  - src/registry/watcher.mjs
  - src/lifecycle/router-lifecycle.mjs
  - tests/router.autonomous-lifecycle.test.mjs
  - tests/router.compiled-index.test.mjs
  - tests/router.context-prompt-integration.test.mjs
autonomous: true
requirements:
  - REG-01
  - REG-02
  - REG-03
  - ADP-01
  - ADP-02
  - CHG-01
  - CHG-02
  - SAF-09
  - MAP-01

must_haves:
  truths:
    - "D-01/D-02: add, edit, rename, move, disable, dependency-change, and delete are real filesystem events observed through installed Claude and Codex controllers within bounded deadlines."
    - "D-03: every safe event produces registry bytes equivalent to a clean full build and a real compiled route with matching scope, invocation, dependency, and dispatchability semantics."
    - "Watcher activation publishes one verified immutable registry/index tuple; all public prompt and registry reads resolve that tuple as their sole authority and fail closed on mismatch, while unsafe changes preserve prior dispatch authority per D-03."
  artifacts:
    - path: src/prompt/publish-index.mjs
      provides: "Control-plane compiler and immutable compiled-index publisher bound to an activated registry version"
      exports: ["publishCompiledIndex"]
    - path: src/registry/watcher.mjs
      provides: "Activation-to-compiled-publication orchestration, atomic tuple commit, and committed-baseline semantics"
    - path: src/prompt/compile-index.mjs
      provides: "Tuple-authoritative compiled-index loading with compatible registry/index hash validation and known-good fallback"
    - path: src/context/prompt-route.mjs
      provides: "Public routing read that dispatches only from a mutually verified registry/index tuple"
    - path: tests/router.autonomous-lifecycle.test.mjs
      provides: "Shared seven-event installed-controller contract for Claude and Codex"
  key_links:
    - from: src/registry/watcher.mjs
      to: src/prompt/publish-index.mjs
      via: "successful verified registry activation passes the immutable registry version and mapping into compiled publication"
      pattern: "publishCompiledIndex|publishIndex"
    - from: tests/router.autonomous-lifecycle.test.mjs
      to: src/context/prompt-route.mjs
      via: "each scenario observes dispatch through routeContextPrompt rather than publishing a fixture index"
      pattern: "routeContextPrompt"
    - from: src/context/prompt-route.mjs
      to: "owned release-tuples/active.json and immutable tuple manifest"
      via: "loadCompiledIndex resolves one tuple, validates both component hashes/versions, and rejects mixed component pointers"
      pattern: "loadCompiledIndex"
---

<objective>
Deliver the real dual-runtime autonomous lifecycle from filesystem observation through verified registry activation to compiled prompt dispatch (D-01 through D-03).

Purpose: Phase 18 cannot claim end-to-end propagation while the watcher activates only registry state and tests fabricate compiled state.
Output: A production compiled-index publisher wired into the installed watcher and a table-driven Claude/Codex seven-event E2E suite.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-autonomous-lifecycle-and-release-gates/18-CONTEXT.md
@.planning/phases/18-autonomous-lifecycle-and-release-gates/18-RESEARCH.md
@.planning/phases/17-compiled-prompt-routing-and-safe-evolution/17-VERIFICATION.md
@src/registry/watcher.mjs
@src/registry/activate.mjs
@src/prompt/compile-index.mjs
@src/context/prompt-route.mjs
@src/lifecycle/router-lifecycle.mjs
@tests/router.lifecycle.test.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Publish a verified compiled index after registry activation</name>
  <files>src/prompt/publish-index.mjs, src/prompt/compile-index.mjs, src/context/prompt-route.mjs, src/registry/watcher.mjs, src/lifecycle/router-lifecycle.mjs, tests/router.autonomous-lifecycle.test.mjs, tests/router.compiled-index.test.mjs, tests/router.context-prompt-integration.test.mjs</files>
  <behavior>
    - A passing activation compiles the route projection from the activated canonical registry/mapping, durably writes a complete immutable compiled version, validates it with the existing strict loader contract, and atomically advances its pointer.
    - Registry and index versions plus their payload hashes are recorded in an immutable tuple manifest selected by one atomic tuple pointer; failure before pointer commit preserves the prior tuple and does not advance the watcher baseline.
    - `loadCompiledIndex` and `routeContextPrompt` resolve the tuple pointer first, verify the referenced registry and compiled payloads against that same tuple, and fail closed or use the verified tuple known-good pointer when either component is missing, corrupt, or mismatched.
    - The installed module inventory includes the publisher so spawned controllers execute the same production seam as source tests.
  </behavior>
  <action>Create `publishCompiledIndex` as a standard-library-only control-plane module and make an immutable release tuple the sole public authority. The commit protocol is: durably write and fsync the complete registry version; durably write and fsync the complete compiled version; write and fsync an immutable tuple manifest containing exact registry version/hash, compiled version/hash, policy fingerprint, mapping fingerprint, compatibility, and verification result; validate both referenced components through their strict readers; then atomically rename one `release-tuples/active.json` pointer and fsync its directory. Update a known-good tuple pointer only after successful active commit. Extend `compile-index.mjs` without adding mutation to the hot path: resolve active tuple first, containment-check and hash/schema-validate both referenced registry and compiled payloads, return only the compiled component from that mutually compatible tuple, and fall back only to a fully verified known-good tuple. Make `routeContextPrompt` propagate tuple version/reason evidence and block dispatch on tuple mismatch. Make the watcher's public active-registry read resolve the same tuple rather than an independent registry pointer, call the publisher after candidate verification, and advance `baseline` only after the tuple pointer commits. Copy all changed modules in the lifecycle installer. Write failing tests first for every commit boundary and component corruption/mismatch, including reader sampling that can observe only complete old or complete new tuples and never an independently advanced component. This closes D-01/D-03/D-05; fixture-written compiled pointers are not E2E authority.</action>
  <verify>
    <automated>node --test tests/router.autonomous-lifecycle.test.mjs tests/router.registry-watcher.test.mjs tests/router.compiled-index.test.mjs tests/router.context-prompt-integration.test.mjs</automated>
  </verify>
  <done>A successful watcher activation atomically commits one mutually verified registry/index tuple; every public routing/registry reader resolves it, and any partial/mismatched publication leaves the prior tuple and baseline authoritative.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the shared Claude and Codex filesystem-event matrix</name>
  <files>tests/router.autonomous-lifecycle.test.mjs</files>
  <behavior>
    - One scenario table covers add, edit, rename, move, disable, dependency-change, and delete for both runtime-specific fixtures (D-01).
    - Every mutation uses a real file operation in isolated temporary homes and waits for observable controller generation/pointer changes with a bounded poll or documented drain; no arbitrary sleep or network is used (D-02).
    - After every event, active canonical bytes match `buildFullRegistry`, and `routeContextPrompt` observes the compiled scope, invocation, dependency, target, and dispatchability result (D-03).
  </behavior>
  <action>Build runtime fixture adapters for Claude and Codex behind one operation table. Install the router/controller into pre-populated temporary homes, mutate native capability files, and poll status plus immutable tuple changes with a hard two-second normal-event deadline. For every matrix cell, independently run a clean full build and compare canonical registry bytes/semantic fields; then call `routeContextPrompt` against the production-published compiled index and assert the expected dispatch semantics. Include delete/disable/dependency cases that prove removed or unsafe targets cannot remain activatable (SAF-09), and assert deterministic mapping evidence precedes any ambiguity outcome (MAP-01). Add a stopped-controller mutation followed by restart/authoritative repair within the configured bounded test interval for CHG-02. Assert all 14 runtime-operation cells are present so omissions fail loudly, register cleanup, and never touch real user homes.</action>
  <verify>
    <automated>node --test tests/router.autonomous-lifecycle.test.mjs tests/router.lifecycle.test.mjs tests/router.registry-build.test.mjs tests/router.route-targets.test.mjs</automated>
  </verify>
  <done>All seven real filesystem operations pass for both runtimes through installed watcher, registry, compiled-index, and real route observation, with full-build equivalence after every safe event.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runtime homes to watcher | Native files and filesystem events are untrusted inputs to canonical discovery. |
| Registry activation to compiled publication | Separate immutable stores must not expose a mixed or partial authority tuple. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01 | Tampering | publish-index/watcher | high | mitigate | Containment, strict schemas and hashes, durable immutable writes, atomic pointers, tuple validation, and injected-boundary tests. |
| T-18-02 | Denial | watcher event stream | medium | mitigate | Existing debounce/single-flight controls plus bounded polling and authoritative repair. |
</threat_model>

<verification>
Run the focused commands after each task. At the wave boundary run `node --test tests/router.autonomous-lifecycle.test.mjs tests/router.lifecycle.test.mjs tests/router.registry-watcher.test.mjs tests/router.compiled-index.test.mjs tests/router.registry-build.test.mjs tests/router.route-targets.test.mjs`.
</verification>

<success_criteria>
- The complete 7 x 2 lifecycle matrix uses real files and installed controllers with bounded event observation.
- Registry state is full-build-equivalent and prompt dispatch reads production-published compiled state after each safe event.
- Partial publication and unsafe changes preserve the prior verified authority and do not poison incremental baseline state.
</success_criteria>

<output>
Create `.planning/phases/18-autonomous-lifecycle-and-release-gates/18-01-SUMMARY.md` when done.
</output>

---
phase: 24-privacy-safe-outcomes-and-capability-health
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/health/outcome-schema.mjs
  - src/health/store.mjs
  - src/health/observe.mjs
  - src/health/admin.mjs
  - src/cli/router-control.mjs
  - tests/router.health.outcome-schema.test.mjs
  - tests/router.health.privacy.test.mjs
  - tests/router.health.tracer.test.mjs
autonomous: true
requirements: [HLTH-01, HLTH-02, HLTH-03, HLTH-04]
must_haves:
  truths:
    - "A telemetry record with a route_id flows through observe.mjs into a bounded outcome record persisted at ~/.claude/router/health/outcomes.jsonl and is visible via `router health inspect`."
    - "No outcome record contains a raw prompt, transcript, secret, tool output, source document, or unbounded argument — only the sha256 prompt_signature (or null when deny_filtered)."
    - "The persisted field is named `outcome_kind`, never `outcome` (avoids collision with the v1 telemetry `outcome` field and the rollback journal `outcome`)."
    - "Health state lives under ~/.claude/router/health/ with 0600 perms; no src/health/* module makes any network call."
    - "An outcome record whose field set is not in the frozen OUTCOME_FIELDS allowlist is rejected with reason_code 'forbidden_outcome_field' and never persisted."
  artifacts:
    - "src/health/outcome-schema.mjs — validateOutcomeEnvelope, OUTCOME_FIELDS, OUTCOME_KINDS, OUTCOME_KIND enum, boundedToken, privacy posture."
    - "src/health/store.mjs — createHealthStore (mirror createPersistentEvidenceStore under ~/.claude/router/health/, 0600 perms, atomic temp+rename)."
    - "src/health/observe.mjs — minimal observer: derives outcome_kind='selected' from a telemetry record with a route_id (tracer path only)."
    - "src/health/admin.mjs — inspect (read-only projection of outcomes.jsonl with bounded pagination)."
    - "src/cli/router-control.mjs — `health` subcommand dispatch (inspect only this wave) with --help disambiguation from `router doctor`."
    - "tests/router.health.outcome-schema.test.mjs, tests/router.health.privacy.test.mjs, tests/router.health.tracer.test.mjs."
  key_links:
    - "outcome-schema.mjs.validateOutcomeEnvelope → store.mjs.append → outcomes.jsonl → admin.mjs.inspect → router-control.mjs `health inspect`"
    - "observe.mjs imports stableCapabilityId from src/registry/identity.mjs (framework-neutral capability_id, never record.name)."
---

<objective>
Wave 1 tracer: prove the privacy-bounded outcome-observation loop end-to-end on one path.

A telemetry record carrying a route_id is observed off-hot-path, validated against a frozen allowlist, persisted under ~/.claude/router/health/, and surfaced through `router health inspect` — with the full privacy posture (no raw content, sha256 signature only, deny_filtered → null signature) enforced from the first record.

Purpose: This is the architectural tracer for Phase 24. It proves every layer the phase touches (schema → observer → store → admin → CLI) wires correctly before any expansion. Per the tracer-first default, it is production-quality — the schema and privacy posture it ships become the contract every later wave builds on, not a throwaway.

Output: src/health/{outcome-schema,store,observe,admin}.mjs (minimal but real), src/cli/router-control.mjs `health` dispatch, three test files. The single end-to-end path is green; expansion (full outcome_kind coverage, scoring, catalog, admin reset/dispose/recover, thresholds) lands in later waves.

Load-bearing decisions (Claude's discretion, grounded in 24-RESEARCH.md + 24-PATTERNS.md):
- D-3 (post-work observation source): Option (c) — the observer derives outcomes OFF the hot path by reading telemetry.jsonl + workflow-state diff. The router hook (~/.claude/hooks/router.mjs) is NOT modified. Rationale: option (a) would add a stateful workflow-state read + comparison to the <100ms UserPromptSubmit hot path, which the architectural responsibility map and RESEARCH Pitfall 1 caution against. Option (c) keeps the hot path untouched and preserves per-prompt correlation via telemetry's prompt_signature. The watcher (or a parallel background trigger) is the off-hot-path surface.
- D-4 (CLI surface): `router health <sub>` subcommand family in src/cli/router-control.mjs. Disambiguated from the Phase 07 `router doctor`/`router coverage` route-coverage diagnostics via a one-line note in usage() output: "router doctor reports router plumbing health; router health reports capability health."
- D-5 (scope isolation): healthRoot = join(ownedRoot, 'health') — a SIBLING of evidence/, not a parent. admin.mjs must NOT import src/registry/activate.mjs or src/prompt/publish-index.mjs. Verified by a content-hash test in Wave 3.
- D-6 (name collision): the persisted field is `outcome_kind`, NEVER `outcome` (collision with the v1 telemetry `outcome: null` field and the rollback journal `outcome: 'completed'|'not_committed'` per RESEARCH Pitfall 2).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-privacy-safe-outcomes-and-capability-health/24-CONTEXT.md
@.planning/phases/24-privacy-safe-outcomes-and-capability-health/24-RESEARCH.md
@.planning/phases/24-privacy-safe-outcomes-and-capability-health/24-PATTERNS.md
@.claude/CLAUDE.md

# Direct analogs (read once, extract patterns, do not re-read):
@src/evolution/evidence.mjs            # FIELDS allowlist, validateEvidenceEnvelope, HALF_LIFE_MS/MAX_RETENTION_MS/MINIMUM_SAMPLES, boundedToken, PRIVACY_GUARDS, privacy_signature_forbidden
@src/registry/identity.mjs             # stableCapabilityId (framework-neutral capability_id)
@src/registry/relationships.mjs        # policy_version string discipline, bounded-slice overflow reporting
@src/cli/router-control.mjs           # canonical() result shape, EXIT codes, canary subcommand dispatch pattern (lines 860-1107)
</context>

<tasks>

<task type="tracer">
  <name>Tracer: telemetry → outcome record → inspect, one path, full privacy posture</name>
  <files>src/health/outcome-schema.mjs, src/health/store.mjs, src/health/observe.mjs, src/health/admin.mjs, src/cli/router-control.mjs, tests/router.health.tracer.test.mjs</files>
  <behavior>
    - Tracer test (HLTH-01): a telemetry fixture with prompt_signature and route_id, when passed to observe.deriveSelectedOutcome, yields an accepted outcome record with outcome_kind='selected' and NO raw prompt field.
    - Tracer test (HLTH-03): the OUTCOME_KINDS enum contains all 9 values (selected, actually_used, completed, corrected, retried, replaced, abandoned, overridden, helpful_reuse); the tracer path exercises only 'selected'.
    - Tracer test: validateOutcomeEnvelope rejects a record carrying an extra field 'prompt_text' with reason_code 'forbidden_outcome_field'.
    - Tracer test: validateOutcomeEnvelope rejects a record whose capability_id contains a framework prefix 'gsd-' (Pitfall 3 guard).
    - Tracer test: a deny_filtered record (guard_codes includes 'deny_filtered') with prompt_signature !== null is rejected with reason_code 'privacy_signature_forbidden'.
    - Tracer test end-to-end: feed telemetry fixture → observe → store.append → admin.inspect returns the persisted record with 0600 perms on outcomes.jsonl and no network call made.
  </behavior>
  <action>
    Create the Phase 24 health subsystem tracer touching every layer the phase will modify. This is the production skeleton, not a prototype — the schema and privacy posture it ships are the contract later waves extend.

    1. src/health/outcome-schema.mjs (analog: src/evolution/evidence.mjs lines 6-53). Export: OUTCOME_FIELDS (frozen Set), OUTCOME_KINDS (frozen Set of 9 per HLTH-03), OUTCOME_KIND enum, boundedToken (re-import from evidence.mjs — do NOT redefine), PRIVACY_GUARDS (re-import or mirror), validateOutcomeEnvelope(input). The FIELDS set is exactly: timestamp_ms, capability_id, outcome_kind, prompt_signature, route_id, confidence_band, guard_codes, reason_code, evidence_window_ms, sample_size, opportunity_count, freshness, policy_version, fingerprint. Reject any field not in the set with 'forbidden_outcome_field'. Reject outcome_kind not in OUTCOME_KINDS with 'invalid_outcome_kind'. Reject capability_id not matching boundedToken OR matching a known framework prefix (gsd-, gstack-, codex-) with 'invalid_capability_id' (Pitfall 3). Apply the privacy_signature_forbidden rule verbatim from evidence.mjs: deny_filtered records (guard_codes intersect PRIVACY_GUARDS) must carry prompt_signature === null; non-denied records must carry a 64-hex-char prompt_signature. Field name is `outcome_kind`, NEVER `outcome` (D-6, Pitfall 2). Object.freeze the accepted signal.

    2. src/health/store.mjs (analog: createPersistentEvidenceStore in src/evolution/evidence.mjs). Export createHealthStore({ root }) where root defaults to join(os.homedir(), '.claude/router/health'). The store creates the dir with mkdirSync mode 0o700 on first use, appends JSONL lines to outcomes.jsonl with appendFileSync flag 'a' mode 0o600, and exposes append(record), readWindow({ fromMs, toMs }), and count(). Reuse the boundedToken path-escape defense — capability_id is flat, never contains '/'. Do NOT import or call anything in src/registry/activate.mjs or src/prompt/publish-index.mjs (D-5 scope isolation).

    3. src/health/observe.mjs — TRACER MINIMAL. Export deriveSelectedOutcome(telemetryRecord, { stableCapabilityIdFn }) that takes a telemetry record (shape from router.mjs:2330-2358: ts, prompt_signature, suggested_mode, suggested_skills, suggested_agents, confidence_tier, guards_fired, route) and returns an outcome record with outcome_kind='selected', timestamp_ms=ts, capability_id=stableCapabilityIdFn(matchedCapability), prompt_signature, route_id, confidence_band, guard_codes, policy_version='health-policy-v1', fingerprint=sha256(stableStringify(canonicalRecord)). For the tracer, the matched capability is the first suggested_skill or suggested_agent; the full 9-kind derivation lands in Plan 24-02. validateOutcomeEnvelope MUST be called before returning; denied records are skipped (returned as {status:'denied', reason_code}). This module is OFF the hot path — it must NOT be imported by ~/.claude/hooks/router.mjs (Pitfall 1).

    4. src/health/admin.mjs — inspect only this wave (analog: src/cli/router-control.mjs canonical() + canary body). Export inspect({ healthRoot, limit=100, offset=0 }) returning a canonical('health', true, 'inspect_ok', { records, total, limit, offset }) result. Read-only: opens outcomes.jsonl, parses lines in the window [offset, offset+limit], returns bounded records. Never mutates state.json, registry/, or release-tuples/.

    5. src/cli/router-control.mjs — EXTEND. Add a `health` command branch parallel to the `canary` block (lines 860-1107). Validate positional[1] against ['inspect','reset','dispose','recover'] (only 'inspect' is wired this wave; the other three return canonical('health', false, 'not_implemented', { subcommand }) with exitCode EXIT.usage). Delegate inspect to admin.inspect. Update usage() (line 547) to list `health inspect|reset|dispose|recover` and add the one-line disambiguation: "router doctor reports router plumbing health; router health reports capability health" (D-4). Use the same absolute node binary and EXIT codes the file already uses.

    6. tests/router.health.tracer.test.mjs — node:test. The end-to-end tracer test: create a temp health root, write a telemetry fixture line (with a route_id and a valid 64-hex prompt_signature, no raw prompt text), call observe.deriveSelectedOutcome, store.append, admin.inspect, and assert the record appears with correct fields, 0600 perms on outcomes.jsonl, and NO raw prompt string anywhere in the persisted line. Assert the OUTCOME_KINDS enum has exactly 9 members. Assert forbidden_outcome_field, invalid_capability_id (gsd- prefix), and privacy_signature_forbidden denials all fire.

  Per D-3, this tracer does NOT modify ~/.claude/hooks/router.mjs. The observer reads telemetry.jsonl + workflow-state off the hot path; the tracer test feeds a fixture directly to deriveSelectedOutcome to prove the schema/store/admin loop. Plan 24-02 Task 1 wires the full observe.ingestTelemetryEvidence path that reads the real telemetry file.
  </action>
  <verify>
    <automated>rtk node --test tests/router.health.tracer.test.mjs</automated>
  </verify>
  <done>The single path telemetry → observe('selected') → validateOutcomeEnvelope → store.append → admin.inspect is green end-to-end. outcomes.jsonl has 0600 perms. The OUTCOME_KINDS enum has 9 values. forbidden_outcome_field, invalid_capability_id, and privacy_signature_forbidden denials all fire. No raw prompt text appears in any persisted record. The tracer is committed.</done>
  <reversibility rating="costly">The OUTCOME_FIELDS allowlist and OUTCOME_KINDS enum become the persistence contract once records exist; changing them later requires a policy_version bump and a migration. No human checkpoint needed — CONTEXT.md already locked the bounded-local posture; the schema is the natural product of HLTH-03/04.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Persistence hardening + privacy test suite (HLTH-02/04 completeness)</name>
  <files>src/health/store.mjs, src/health/outcome-schema.mjs, tests/router.health.outcome-schema.test.mjs, tests/router.health.privacy.test.mjs</files>
  <behavior>
    - HLTH-04 test: outcomes older than MAX_RETENTION_MS (7d, imported from evidence.mjs) are filtered out of store.readWindow and not counted.
    - HLTH-04 test: store.append writes via atomic temp+rename for state.json (not for the append-only outcomes.jsonl — JSONL is appendFileSync); state.json writes are temp+rename+fsync with 0600 perms.
    - HLTH-04 test: a corrupted outcomes.jsonl line (invalid JSON) is skipped during readWindow with a 'corrupt_line_skipped' counter in the result, never thrown.
    - HLTH-04 test: bounded compaction — when outcomes.jsonl exceeds a configurable maxBytes (default 1MB), the oldest records beyond MAX_RETENTION_MS are dropped and a compaction marker line is appended.
    - HLTH-02 test: no function in src/health/* imports node:http, node:https, node:net, node:dns, or fetch — assert via a module-import grep test.
    - HLTH-02 test: store.createHealthStore sets 0700 on the dir and 0600 on every file it creates.
    - HLTH-01 test: validateOutcomeEnvelope rejects every field named prompt, prompt_text, transcript, output, content, source, argument — each with 'forbidden_outcome_field'.
  </behavior>
  <action>
    Expand the tracer's store and schema to the full HLTH-04 surface. This is additive hardening of the files Plan 24-01 Task 1 created — do not rewrite, extend in place.

    1. src/health/store.mjs — add retention, atomic state writes, corruption tolerance, bounded compaction. Import HALF_LIFE_MS, MAX_RETENTION_MS, MINIMUM_SAMPLES from src/evolution/evidence.mjs (do NOT redefine — RESEARCH "Don't Hand-Roll"). readWindow({ fromMs, toMs, now }) filters records older than (now - MAX_RETENTION_MS). Add writeState(state) that writes state.json via temp+rename+fsync with mode 0o600 (analog: publish-index.mjs durableWrite). Add readState() that returns null on missing/corrupt file (never throws). Add compact({ maxBytes=1048576 }) that drops records older than MAX_RETENTION_MS and appends a compaction marker line { compacted_at_ms, dropped, policy_version }. readWindow must skip invalid JSON lines and increment a corrupt_line_skipped counter in the returned result.

    2. src/health/outcome-schema.mjs — add a bounded integer check for evidence_window_ms (0 to MAX_RETENTION_MS), sample_size (0 to 10_000_000), opportunity_count (0 to 10_000_000). Add a fingerprint validator (64 hex chars). Reject any string field longer than boundedToken's 128-char max with 'field_too_long'. The FIELDS set from Task 1 is final — do not add fields here.

    3. tests/router.health.outcome-schema.test.mjs — unit tests for HLTH-01/03/04 schema surface: every OUTCOME_KINDS value accepted; every forbidden field name rejected; deny_filtered + non-null signature rejected; bounded integer overflow rejected; framework-prefix capability_id rejected.

    4. tests/router.health.privacy.test.mjs — HLTH-02 suite. Assert no src/health/*.mjs module imports any network primitive (use a test that reads each src/health/*.mjs file and greps for import statements matching node:http, node:https, node:net, node:dns, or a global fetch call — fail on any hit). Assert createHealthStore produces 0700 dir + 0600 files. Assert no raw prompt fixture appears in any persisted record.

  Per D-5, this task must not introduce any import of src/registry/activate.mjs or src/prompt/publish-index.mjs into src/health/*.
  </action>
  <verify>
    <automated>rtk node --test tests/router.health.outcome-schema.test.mjs tests/router.health.privacy.test.mjs tests/router.health.tracer.test.mjs</automated>
  </verify>
  <done>HLTH-01/02/03/04 unit + privacy suites green. outcomes.jsonl lines outside MAX_RETENTION_MS are filtered. state.json writes are atomic (temp+rename+fsync, 0600). Corrupt JSONL lines are skipped with a counter. Bounded compaction drops stale records. No src/health/* module imports a network primitive. All committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UserPromptSubmit hot path → off-hot-path observer | Telemetry.jsonl is written on the hot path; the observer reads it off-hot-path. The observer must never run on the hot path. |
| Outcome record → persistent store | Every record crosses this boundary through validateOutcomeEnvelope (frozen FIELDS allowlist). |
| Health state → admin CLI | Admin commands read/write ~/.claude/router/health/; must not reach registry/ or release-tuples/. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-01 | Information Disclosure | outcome-schema.validateOutcomeEnvelope | critical | mitigate | Frozen OUTCOME_FIELDS allowlist rejects prompt_text/transcript/output/content/source/argument with 'forbidden_outcome_field'; deny_filtered records carry prompt_signature: null (HLTH-01). |
| T-24-02 | Information Disclosure | observe.deriveSelectedOutcome | high | mitigate | Only the sha256 prompt_signature is persisted; redact() runs BEFORE hashing (reuse promptSignature from router.mjs via the telemetry record's existing signature — never re-hash raw prompt in src/health/*). |
| T-24-03 | Tampering | outcome-schema capability_id | high | mitigate | boundedToken path-escape defense (reuse from evidence.mjs) + framework-prefix rejection (gsd-/gstack-/codex-) prevents Pitfall 3 + path traversal. |
| T-24-04 | Tampering | store.mjs state.json | medium | mitigate | Atomic temp+rename+fsync writes with 0600 perms; corrupt files return null, never throw. |
| T-24-05 | Elevation of Privilege | admin.mjs inspect | high | mitigate | admin.mjs must NOT import activate.mjs/publish-index.mjs (D-5); inspect is read-only; 0600 perms. Wave 3 adds the content-hash regression test. |
| T-24-06 | Information Disclosure | src/health/* network calls | critical | mitigate | HLTH-02 privacy test greps every src/health/*.mjs for node:http/https/net/dns/fetch imports — fails on any hit. |
| T-24-07 | Repudiation | outcomes.jsonl corruption | medium | mitigate | Corrupt JSONL lines are skipped with a 'corrupt_line_skipped' counter; never throw; bounded compaction drops stale records with a compaction marker. |
| T-24-SC | Tampering | npm/pip/cargo installs | low | accept | Phase 24 installs zero external packages (stdlib only, per .claude/CLAUDE.md "What NOT to Use"). No supply-chain surface. |
</threat_model>

<verification>
- `rtk node --test tests/router.health.tracer.test.mjs tests/router.health.outcome-schema.test.mjs tests/router.health.privacy.test.mjs` — all green.
- `grep -rE "import.*(node:http|node:https|node:net|node:dns|fetch)" src/health/` returns no matches (HLTH-02).
- `grep -rE "import.*(activate|publish-index)" src/health/` returns no matches (D-5 scope isolation).
- `ls -l ~/.claude/router/health/outcomes.jsonl` (after a tracer run) shows mode 0600.
- The OUTCOME_KINDS enum has exactly 9 values; the persisted field name is `outcome_kind` (grep src/health/ for `outcome_kind` — no bare `outcome` field in any persisted record shape).
</verification>

<success_criteria>
- The end-to-end tracer path is green: telemetry fixture → observe('selected') → validateOutcomeEnvelope → store.append → admin.inspect.
- HLTH-01/02/03/04 unit + privacy suites green.
- Health state is isolated under ~/.claude/router/health/ (sibling of evidence/), 0600 perms, no network calls, no imports of activate.mjs/publish-index.mjs.
- The `router health inspect` subcommand is wired in router-control.mjs with the `router doctor` disambiguation note.
- The OUTCOME_KINDS enum and the `outcome_kind` field name are frozen as the persistence contract for later waves.
</success_criteria>

<output>
Create `.planning/phases/24-privacy-safe-outcomes-and-capability-health/24-01-SUMMARY.md` when done.
</output>
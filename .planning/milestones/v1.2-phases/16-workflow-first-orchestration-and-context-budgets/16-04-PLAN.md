---
phase: 16-workflow-first-orchestration-and-context-budgets
plan: 04
type: execute
wave: 4
depends_on: ["16-03"]
files_modified:
  - src/orchestrator/select.mjs
  - src/orchestrator/budget.mjs
  - tests/router.workflow-orchestrator.test.mjs
  - tests/router.context-budget.test.mjs
autonomous: true
gap_closure: true
requirements: [ORC-01, TOK-01, TOK-02]
nyquist_compliant: true
must_haves:
  truths:
    - "G-03 / D-07 / ORC-01: every capability in the selected workflow's declared compatible set can safely narrow the declared owner-and-requirement closure without switching workflows or adding lexical matches."
    - "G-01 / D-09 / TOK-02: a context plan is dispatch eligible only when at least one valid descriptor exists for every contract source class marked required."
    - "G-02 / D-10 / TOK-02: context descriptors always load in transition facts, dependency facts, artifact summary, diagnostic order regardless of caller-supplied numeric priorities."
    - "TOK-01 remains intact: gap closure does not permit broad manifests, planning directories, conversation histories, complete design bodies, or fallback source bodies."
  artifacts:
    - path: src/orchestrator/select.mjs
      provides: "Compatible-set explicit narrowing aligned with the workflow declaration contract"
    - path: src/orchestrator/budget.mjs
      provides: "Required-class presence gate and canonical semantic source ordering"
    - path: tests/router.workflow-orchestrator.test.mjs
      provides: "Behavioral regression for compatible non-owner explicit capability narrowing"
    - path: tests/router.context-budget.test.mjs
      provides: "Missing-required-class and caller-priority override regression matrix"
  key_links:
    - from: src/orchestrator/select.mjs
      to: tests/router.workflow-orchestrator.test.mjs
      via: "explicit capability in compatible plus requirements, but not owners, resolves through the same safe dependency closure"
    - from: src/orchestrator/budget.mjs
      to: tests/router.context-budget.test.mjs
      via: "contract required flags gate descriptor completeness and canonical class rank controls ordering"
---

<objective>
Close Phase 16 verification gaps G-01 through G-03 by aligning explicit capability narrowing with D-07 and enforcing required context presence plus D-10 semantic ordering.

Purpose: Restore the verified workflow-first and least-sufficient-context contracts without changing completed plans or crossing into Phase 17 hot-path work.
Output: Targeted corrections in `select.mjs` and `budget.mjs` with adversarial regression coverage in the two existing Phase 16 test files.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-workflow-first-orchestration-and-context-budgets/16-CONTEXT.md
@.planning/phases/16-workflow-first-orchestration-and-context-budgets/16-RESEARCH.md
@.planning/phases/16-workflow-first-orchestration-and-context-budgets/16-VERIFICATION.md
@.planning/phases/16-workflow-first-orchestration-and-context-budgets/16-02-SUMMARY.md
@.planning/phases/16-workflow-first-orchestration-and-context-budgets/16-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Align compatible explicit narrowing with the declared workflow contract</name>
  <read_first>.planning/phases/16-workflow-first-orchestration-and-context-budgets/16-VERIFICATION.md, .planning/phases/16-workflow-first-orchestration-and-context-budgets/16-CONTEXT.md, src/orchestrator/select.mjs, tests/router.workflow-orchestrator.test.mjs, .planning/phases/16-workflow-first-orchestration-and-context-budgets/16-02-SUMMARY.md</read_first>
  <files>tests/router.workflow-orchestrator.test.mjs, src/orchestrator/select.mjs</files>
  <behavior>
    - A capability listed in `compatible` and present in the selected workflow's declared requirements, but absent from `owners`, is accepted as an explicit narrowing root.
    - Explicit narrowing retains all declared requirements and resolves their deterministic, safety-checked transitive closure.
    - A capability absent from the compatible set remains non-dispatchable with `explicit_capability_incompatible`; prompt text and registry names never expand roots or switch workflows.
    - Permuted declarations and registry records produce byte-equivalent closure outcomes.
  </behavior>
  <action>First add a failing G-03 regression fixture that distinguishes compatible membership from owner membership. Then update `selectCapabilities` per D-07 so a valid explicit request is authorized by the selected declaration's `compatible` set and narrows the owner roots while retaining every declared requirement. Do not require compatible requests to duplicate `owners`; instead let the existing `resolveDependencies` path prove identity, availability, dispatchability, scope, permissions, conflicts, and cycle safety. Preserve the strict workflow-token gate, D-05 declared-edge-only roots, D-06 blocker algebra, D-08 hook separation, canonical ordering, and the existing incompatible-request outcome. Do not add prompt lexical scoring, workflow merging, fallback owners, or registry traversal before workflow validation.</action>
  <verify><automated>node --test tests/router.workflow-orchestrator.test.mjs</automated></verify>
  <acceptance_criteria>The new compatible-non-owner fixture passes, incompatible requests still fail closed, and the complete workflow-orchestrator suite preserves access-order, lexical-negative, safety, hook, and permutation guarantees.</acceptance_criteria>
  <done>G-03 is closed: the implementation and public declaration contract agree that every declared compatible capability may narrow the selected workflow closure safely.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Require all mandatory context classes and freeze semantic load order</name>
  <read_first>.planning/phases/16-workflow-first-orchestration-and-context-budgets/16-VERIFICATION.md, .planning/phases/16-workflow-first-orchestration-and-context-budgets/16-CONTEXT.md, src/orchestrator/budget.mjs, tests/router.context-budget.test.mjs, .planning/phases/16-workflow-first-orchestration-and-context-budgets/16-03-SUMMARY.md</read_first>
  <files>tests/router.context-budget.test.mjs, src/orchestrator/budget.mjs</files>
  <behavior>
    - Empty descriptors under the default contract block before dispatch with a stable bounded missing-required-class reason.
    - Omitting any one required class blocks deterministically and identifies the first missing class in canonical semantic order without exposing source bodies.
    - A contract whose caller priorities put diagnostics before required facts is either rejected with a stable contract reason or normalized so output remains transition facts, dependency facts, artifact summaries, then diagnostics.
    - Exact ceilings, required overflow, optional omission, summary reuse, accounting, permutation stability, and forbidden broad-source behavior remain unchanged.
  </behavior>
  <action>Write failing G-01 and G-02 tests first, covering empty input, each single missing required class, reversed numeric priorities, and permuted descriptors. Then modify contract validation/planning per D-09 so the contract's required class set is compared with valid descriptor classes before byte accounting and a missing required class returns a stable non-dispatchable blocker with bounded class identity. Per D-10 define one internal canonical rank for `transition_facts`, `dependency_facts`, `artifact_summary`, and `diagnostic`; use it for descriptor ordering independently of caller numbers, and either require contract priorities to preserve that rank or normalize them consistently. Keep canonical ID as the within-class tie-breaker. Preserve TOK-01 broad-class rejection, hard per-source/total ceilings, no semantic truncation, optional-only omission, exact three-witness reuse, versioned estimator/accounting, privacy, purity, and all Phase 17 boundaries.</action>
  <verify><automated>node --test tests/router.context-budget.test.mjs tests/router.context-capsule.test.mjs tests/router.context-sources.test.mjs</automated></verify>
  <acceptance_criteria>Empty and partial required inputs cannot dispatch; caller priority inversion cannot alter semantic order; all prior boundary, privacy, reuse, accounting, and broad-source tests pass.</acceptance_criteria>
  <done>G-01 and G-02 are closed: mandatory context is present before dispatch and D-10 ordering is policy-owned rather than caller-controlled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Risk | Mitigation |
|---|---|---|
| Workflow declaration to explicit capability roots | A malformed or lexical request could expand or switch the selected workflow | Authorize only exact compatible IDs, retain declared requirements, then run the existing fail-closed closure checks. |
| Context contract to descriptor plan | Missing mandatory facts could produce a dispatchable but semantically incomplete plan | Compare every required contract class with validated descriptor classes before accounting. |
| Caller priorities to semantic ordering | User-controlled numbers could move diagnostics ahead of workflow and dependency facts | Enforce one policy-owned canonical class rank and reject or normalize inversions deterministically. |
| Source descriptors to output report | Broad or sensitive bodies could enter reports during gap handling | Preserve forbidden-class gates, bounded facts, and no-I/O/no-body behavior. |
</threat_model>

<verification>
Run `node --test tests/router.workflow-orchestrator.test.mjs tests/router.context-budget.test.mjs tests/router.context-capsule.test.mjs tests/router.context-sources.test.mjs`, then `node --test tests/router.perf.test.mjs tests/router.perf-evolved.test.mjs`, `node --test tests/*.test.mjs`, and `git diff --check`. Inspect the diff to confirm only the four declared files changed and no installed hook, deployment, persistence, telemetry, evolution, compiled-index, or performance-calibration surface was touched.
</verification>

<success_criteria>
- ORC-01 explicit narrowing accepts every capability authorized by the selected workflow's compatible set and still uses only declared safe closure edges.
- TOK-02 blocks context dispatch when any required class is absent and enforces the fixed D-10 semantic class order independently of caller priorities.
- TOK-01 broad-source, privacy, no-fallback, and no-I/O guarantees remain green.
- Focused Phase 16 tests, isolated performance tests, the full regression suite, and whitespace checks pass.
</success_criteria>

## Artifacts this phase produces

- `src/orchestrator/select.mjs`: corrected compatible-set narrowing behavior for G-03.
- `src/orchestrator/budget.mjs`: mandatory-class presence gate and policy-owned semantic ordering for G-01/G-02.
- `tests/router.workflow-orchestrator.test.mjs`: compatible non-owner regression proof.
- `tests/router.context-budget.test.mjs`: empty/partial required input and priority-inversion regression proof.

## Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Best valid workflow followed only by compatible capabilities and least-sufficient bounded context | 16-04 | COVERED | Closes all verifier blockers without changing phase boundaries. |
| REQ | ORC-01 | Workflow-first compatible capability selection | 16-04 | COVERED | Task 1 closes G-03. |
| REQ | TOK-01 | No broad default context bodies | 16-04 | COVERED | Task 2 preserves and re-runs forbidden-source/privacy gates. |
| REQ | TOK-02 | Required context, declared budgets, fixed ordering, and reusable summaries | 16-04 | COVERED | Task 2 closes G-01/G-02 while preserving accounting/reuse. |
| RESEARCH | D-07 compatible narrowing through declared safe closure | 16-04 | COVERED | Task 1 uses the existing closure safety path. |
| RESEARCH | Required-source presence and fixed semantic priority | 16-04 | COVERED | Task 2 adds adversarial missing-class and inversion fixtures. |
| CONTEXT | D-01–D-06, D-08 | Workflow prerequisite, declared edges, dependency safety, hook separation | 16-04 | COVERED | Preserved as regression constraints in Task 1. |
| CONTEXT | D-07 | Compatible requests narrow without switching workflows | 16-04 | COVERED | Direct G-03 correction. |
| CONTEXT | D-09–D-10 | Required classes and fixed semantic ordering | 16-04 | COVERED | Direct G-01/G-02 corrections. |
| CONTEXT | D-11–D-13 | Versioned accounting, exact reuse, purity, and Phase 17 boundary | 16-04 | COVERED | Preserved through focused/full regression and boundary inspection. |

<output>Create `.planning/phases/16-workflow-first-orchestration-and-context-budgets/16-04-SUMMARY.md` when done.</output>

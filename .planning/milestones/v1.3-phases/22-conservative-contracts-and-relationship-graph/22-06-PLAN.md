---
phase: 22-conservative-contracts-and-relationship-graph
plan: 06
type: execute
wave: 5
depends_on: [22-01, 22-02, 22-04, 22-05]
files_modified:
  - tests/router.contracts.test.mjs
  - tests/router.contract-eligibility.test.mjs
  - src/registry/contract.mjs
  - src/registry/build.mjs
  - src/registry/eligibility.mjs
autonomous: true
gap_closure: true
requirements: [CONT-01, CONT-02, CONT-03, CONT-04, CONT-05, CONT-06, CONT-08, CONT-09]
nyquist_compliant: true
must_haves:
  truths:
    - "Every authoritative Phase 21 record receives a complete normalized contract during production assembleRegistry() before exact-bound overlays are resolved or applied."
    - "Production contracts carry deterministic per-field evidence from the merged authoritative record, so overlays can enrich the same contract without inventing an installed identity."
    - "An absent contract or any absent required dispatch-field envelope yields unknown eligibility gates and recommendation-only disposition, never eligibility_all_gates_passed."
    - "The real four-record Phase 21 Claude-heavy fixture proves contract count equals discovered record count and proves an exact-bound overlay enriches assembled output."
  artifacts:
    - path: "src/registry/build.mjs"
      provides: "Production contract construction ordered before overlay resolution and eligibility derivation"
    - path: "src/registry/contract.mjs"
      provides: "Existing normalized contract builder deriving deterministic evidence from authoritative record fields"
    - path: "src/registry/eligibility.mjs"
      provides: "Shared fail-closed absent-contract and absent-field eligibility behavior"
    - path: "tests/router.contracts.test.mjs"
      provides: "Phase 21 fixture assembly and overlay enrichment regression oracle"
    - path: "tests/router.contract-eligibility.test.mjs"
      provides: "Contract-free and incomplete-contract recommendation-only regression oracle"
  key_links:
    - from: "src/registry/build.mjs"
      to: "src/registry/contract.mjs"
      via: "buildCapabilityContract() for every merged authoritative record before resolveContractOverlays()"
    - from: "src/registry/build.mjs"
      to: "src/registry/eligibility.mjs"
      via: "evaluateEligibility() consumes the constructed, optionally overlaid records"
    - from: "tests/helpers/inventory-fixture.mjs"
      to: "src/registry/build.mjs"
      via: "buildClaudeHeavyProfile() records are passed through assembleRegistry(), not manually decorated"
---

<objective>
Close the two Phase 22 verification gaps by wiring normalized contract construction into the production registry path and removing fail-open eligibility compatibility.

Purpose: Make the already-built contract, overlay, inspection, and eligibility features operate on every authoritative discovered record while preserving visible uncertainty.
Output: A production assembly oracle plus minimal shared-flow corrections in the existing contract builder, registry assembler, and eligibility validator.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-conservative-contracts-and-relationship-graph/22-CONTEXT.md
@.planning/phases/22-conservative-contracts-and-relationship-graph/22-RESEARCH.md
@.planning/phases/22-conservative-contracts-and-relationship-graph/22-VALIDATION.md
@.planning/phases/22-conservative-contracts-and-relationship-graph/22-VERIFICATION.md
@.planning/phases/22-conservative-contracts-and-relationship-graph/22-01-SUMMARY.md
@.planning/phases/22-conservative-contracts-and-relationship-graph/22-02-SUMMARY.md
@.planning/phases/22-conservative-contracts-and-relationship-graph/22-04-SUMMARY.md
@.planning/phases/22-conservative-contracts-and-relationship-graph/22-05-SUMMARY.md

<interfaces>
Reuse `buildCapabilityContract(record, fieldEvidence)` and `validateCapabilityContract()` from `src/registry/contract.mjs`.
Reuse `assembleRegistry(acquisition, options)` from `src/registry/build.mjs`; its required order is merge → contract construction → overlay resolution/application → relationships → eligibility.
Reuse `evaluateEligibility({ record, records, relationships })` from `src/registry/eligibility.mjs`.
Reuse `buildClaudeHeavyProfile()` and `contractEvidence()` from `tests/helpers/inventory-fixture.mjs`; do not create a second fixture framework.
</interfaces>
</context>

## Artifacts this plan creates/modifies

- `tests/router.contracts.test.mjs` — production assembly contract-count and exact-overlay enrichment oracle.
- `tests/router.contract-eligibility.test.mjs` — absent/incomplete contract fail-closed oracle.
- `src/registry/contract.mjs` — deterministic authoritative-record evidence fallback in the existing builder.
- `src/registry/build.mjs` — builder invocation before overlay processing.
- `src/registry/eligibility.mjs` — absent contract/field states become unknown.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add production assembly and fail-closed regression oracles</name>
  <files>tests/router.contracts.test.mjs, tests/router.contract-eligibility.test.mjs</files>
  <read_first>
    - .planning/phases/22-conservative-contracts-and-relationship-graph/22-VERIFICATION.md (the two actionable gaps and production reproduction)
    - .planning/phases/22-conservative-contracts-and-relationship-graph/22-CONTEXT.md (visible uncertainty, exact overlays, and shared fail-closed eligibility)
    - tests/helpers/inventory-fixture.mjs (real Phase 21 four-record Claude-heavy profile and contractEvidence helper)
    - tests/router.contracts.test.mjs (existing direct-builder contract assertions)
    - tests/router.contract-overlays.test.mjs (exact stable-ID/source-fingerprint/runtime/scope overlay shape)
    - tests/router.contract-eligibility.test.mjs (existing gate matrix and record helper)
    - src/registry/build.mjs (assembleRegistry acquisition shape and current enrichment order)
    - src/registry/eligibility.mjs (current absent-contract compatibility branches)
  </read_first>
  <behavior>
    - Assembling the unmodified four-record Claude-heavy Phase 21 fixture yields four records and four validated normalized contracts.
    - An exact-bound overlay passed to assembleRegistry() changes its targeted contract field in the assembled registry while leaving record identity and record count unchanged.
    - A record with no contract is recommendation-only and reports unknown contract-dependent gates.
    - A record whose contract omits a required dispatch field envelope is recommendation-only and reports field confidence unknown.
  </behavior>
  <action>Add focused regressions without changing the shared fixture helper. Name the production assembly regression `assembleRegistry constructs and overlays every authoritative contract [phase22-red:assembly-gap]` and the missing-safety regression `missing contract safety evidence is recommendation-only [phase22-red:eligibility-gap]`; keep all new assertions for each root cause under its named marker so the expected-RED verifier can distinguish intended failures from unrelated regressions. In `tests/router.contracts.test.mjs`, construct the real acquisition envelope from `buildClaudeHeavyProfile()` and empty diagnostics, call production `assembleRegistry()`, assert contract count equals discovered/assembled record count, validate every contract, and assert deterministic per-field evidence/reason metadata exists. In that same marked regression, add an exact-bound overlay using the existing overlay test shape and the fixture record's stable identity, exact source fingerprint, runtime, and scope; pass it through `assembleRegistry({ ... }, { overlays: [...] })` and assert the accepted correction enriches the target contract after base construction without creating or deleting a record, covering CONT-01/02/04/05/06/09. In `tests/router.contract-eligibility.test.mjs`, make the marked regression call the shared evaluator with an otherwise dispatch-ready record whose contract is absent, then with a normalized contract missing one required dispatch envelope, and require `eligible: false`, `recommendation_only: true`, no all-gates-passed reason, and stable unknown reason codes per CONT-03/08. Test public behavior rather than private helper call counts, and do not manually attach a contract in the production assembly oracle.</action>
  <acceptance_criteria>
    - Assembly assertion: four authoritative fixture observations produce four validated contracts through assembleRegistry().
    - Ordering assertion: an exact-bound overlay enriches the already-constructed production contract and cannot introduce an identity.
    - Evidence assertion: assembled field envelopes retain deterministic provenance, rule version, freshness, confidence basis points, and reasons.
    - Fail-closed assertion: absent contract and absent required envelope both remain recommendation-only with unknown gates.
    - RED assertion: the TAP-aware verifier requires exactly the two named `[phase22-red:assembly-gap]` and `[phase22-red:eligibility-gap]` failures and rejects a green run, a missing expected failure, or any unrelated failure.
  </acceptance_criteria>
  <verify>
    <automated>rtk node -e "const {spawnSync}=require('node:child_process');const r=spawnSync(process.execPath,['--test','tests/router.contracts.test.mjs','tests/router.contract-eligibility.test.mjs','tests/router.contract-overlays.test.mjs'],{encoding:'utf8'});const out=(r.stdout||'')+(r.stderr||'');const failed=[...out.matchAll(/^\s*not ok \d+ - (.+)$/gm)].map(m=&gt;m[1]);const expected=['[phase22-red:assembly-gap]','[phase22-red:eligibility-gap]'];const exact=failed.length===expected.length&amp;&amp;expected.every(marker=&gt;failed.some(name=&gt;name.includes(marker)))&amp;&amp;failed.every(name=&gt;expected.some(marker=&gt;name.includes(marker)));process.exit(r.status!==0&amp;&amp;exact?0:1)"</automated>
  </verify>
  <done>The focused suites reproduce both verifier failures through public production paths and pass only after production construction and eligibility are corrected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire contracts before overlays and make missing evidence fail closed</name>
  <files>src/registry/contract.mjs, src/registry/build.mjs, src/registry/eligibility.mjs, tests/router.contracts.test.mjs, tests/router.contract-eligibility.test.mjs</files>
  <read_first>
    - tests/router.contracts.test.mjs (Task 1 production assembly oracle)
    - tests/router.contract-eligibility.test.mjs (Task 1 absent/incomplete contract oracle)
    - src/registry/contract.mjs (CONTRACT_FIELDS, buildCapabilityContract, validateCapabilityContract, and overlay application)
    - src/registry/build.mjs (mergeGroup, assembleRegistry, overlay/relationship/eligibility ordering)
    - src/registry/eligibility.mjs (fieldState, dependencyState, confidenceState, and stable reason derivation)
    - src/registry/schema.mjs (authoritative record validation and canonicalization)
    - tests/helpers/inventory-fixture.mjs (authoritative field-to-evidence expectations)
  </read_first>
  <behavior>
    - Base contract construction is total over authoritative merged records and precedes overlay resolution.
    - Existing canonical builders and validators remain the only contract path.
    - Missing contract evidence cannot become positive dispatch evidence.
  </behavior>
  <action>Make the smallest shared-flow fix. In `src/registry/contract.mjs`, extend the existing `buildCapabilityContract()` path so an authoritative record supplies deterministic adapter-backed candidates for the canonical fields already evidenced by that record; preserve explicit caller-supplied field evidence and the existing envelope thresholds, unknown states, privacy bounds, validator, policy version, and canonical ordering. Do not add a second builder, registry, class, schema framework, or dependency. In `src/registry/build.mjs`, import and call `buildCapabilityContract()` for every merged authoritative record after precedence/mapping annotations and before `resolveContractOverlays()`; validate the constructed contract, attach it to the same record identity, then keep the existing overlay → relationship → eligibility pipeline so CONT-01/02/04/05/06/09 operate on production output. In `src/registry/eligibility.mjs`, remove the Phase 21 compatibility outcomes that treat an absent contract, absent contract field, or unavailable dependency evidence as passed: return unknown for contract-dependent gates, keep deterministic complete gate evaluation, and let the existing reason-code aggregation force recommendation-only per CONT-03/08. Preserve target, invocation, adapter, conflict, and dependency failure semantics; do not broaden this gap closure into Phase 23 intent selection or execution.</action>
  <acceptance_criteria>
    - Construction assertion: every record leaving assembleRegistry() has one complete validator-approved normalized contract before overlays.
    - Reuse assertion: buildCapabilityContract(), validateCapabilityContract(), resolveContractOverlays(), applyContractOverlays(), and evaluateEligibility() remain the shared path.
    - Safety assertion: no absent contract or required field can produce a passed contract-dependent gate or eligibility_all_gates_passed.
    - Compatibility assertion: direct explicit-evidence contract tests, exact-overlay tests, relationship tests, CLI inspection tests, and Phase 21 registry tests retain their behavior.
  </acceptance_criteria>
  <verify>
    <automated>rtk node --test tests/router.contracts.test.mjs tests/router.contract-overlays.test.mjs tests/router.contract-eligibility.test.mjs tests/router.contract-inspection.test.mjs tests/router.relationships.test.mjs tests/router.registry-schema.test.mjs tests/router.inventory-convergence.test.mjs</automated>
  </verify>
  <done>Production registry assembly constructs and validates contracts before enrichment, and every missing contract safety fact deterministically blocks dispatch eligibility.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authoritative discovery/merge → contract construction | Adapter observations become persisted safety claims with per-field evidence |
| optional overlay → constructed contract | Untrusted local corrections may enrich only an exact installed identity |
| constructed contract → eligibility | Missing or malformed safety evidence must not authorize dispatch |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-21 | Tampering | contract construction order | high | mitigate | Construct and validate every base contract before exact-bound overlay resolution; assert record count and identity remain unchanged. |
| T-22-22 | Elevation of Privilege | eligibility absent-contract path | critical | mitigate | Treat absent contract and required envelopes as unknown; assert they cannot emit eligibility_all_gates_passed. |
| T-22-23 | Spoofing | overlay binding | high | mitigate | Reuse exact stable ID, source fingerprint, runtime, and scope validation; production oracle proves enrichment only on the existing target. |
| T-22-24 | Information Disclosure | field evidence | high | mitigate | Reuse portable evidence envelopes and validators; retain no raw authored values, secrets, or absolute paths. |
| T-22-25 | Repudiation | eligibility explanations | medium | mitigate | Preserve complete deterministic gate states and stable reason codes for every failed or unknown gate. |
| T-22-SC | Tampering | package installation | high | mitigate | No package installation task exists. |
</threat_model>

<verification>
- `rtk node --test tests/router.contracts.test.mjs tests/router.contract-overlays.test.mjs tests/router.contract-eligibility.test.mjs`
- `rtk node --test tests/router.contract-inspection.test.mjs tests/router.relationships.test.mjs tests/router.registry-schema.test.mjs tests/router.inventory-convergence.test.mjs`
- `rtk node --test tests/*.test.mjs`
</verification>

<source_coverage>
| Source | ID | Gap item | Plan | Status | Notes |
|--------|----|----------|------|--------|-------|
| GOAL | — | Installed capabilities are safely understandable and dispatch eligibility is evidence-backed | 06 | COVERED | Production contracts plus fail-closed eligibility restore the phase outcome. |
| REQ | CONT-01 | Every discovered capability receives a normalized contract | 06 | COVERED | assembleRegistry() constructs one contract per authoritative record. |
| REQ | CONT-02 | Every contract field carries independent evidence metadata | 06 | COVERED | Authoritative record evidence feeds the existing field envelopes. |
| REQ | CONT-03 | Missing/uncertain dispatch fields stay unknown and recommendation-only | 06 | COVERED | Absent contract/envelope regression and shared validator fix. |
| REQ | CONT-04 | Optional overlays enrich only installed capabilities | 06 | COVERED | Production oracle applies an exact-bound overlay after base construction. |
| REQ | CONT-05 | Overlay schema and exact binding remain enforced | 06 | COVERED | Existing resolver/validator is reused in the assembly oracle. |
| REQ | CONT-06 | Correction continuity/invalidation has a constructed target | 06 | COVERED | Base contract exists before the existing exact-lineage overlay path. |
| REQ | CONT-07 | Eight typed relationships | NONE | EXCLUDED | Already verified satisfied; not an actionable gap. |
| REQ | CONT-08 | Only fully validated contracts become dispatch eligible | 06 | COVERED | Missing contract/field gates fail closed. |
| REQ | CONT-09 | Production contracts are inspectable | 06 | COVERED | Existing CLI receives contracts for every assembled record. |
| RESEARCH | — | Normalize authoritative records before overlays, then derive one fail-closed eligibility result | 06 | COVERED | Task 2 enforces the researched pipeline order. |
| RESEARCH | — | Reuse Phase 21 profiles, node:test, existing helpers, and no package | 06 | COVERED | Task 1 uses the Claude-heavy fixture and current suites. |
| CONTEXT | — | Contract evidence/confidence and visible uncertainty | 06 | COVERED | Complete envelopes plus unknown absent fields. |
| CONTEXT | — | Manifest/correction trust | 06 | COVERED | Exact-bound overlays enrich only constructed installed identities. |
| CONTEXT | — | Dispatch eligibility and inspection | 06 | COVERED | Shared fail-closed validator feeds existing inspection. |
| CONTEXT | — | Phase 23 intent/execution, Phase 24 outcomes/health, Phase 25 advisory changes | NONE | EXCLUDED | Explicitly deferred. |
</source_coverage>

<success_criteria>
- The Phase 21 Claude-heavy fixture assembles four records with four complete validated contracts.
- Exact-bound overlays enrich production-assembled contracts without changing installed identity cardinality.
- Absent contracts and absent required dispatch envelopes produce unknown gates and recommendation-only eligibility.
- CONT-01/02/03/04/05/06/08/09 focused regressions and the full repository suite pass.
</success_criteria>

<output>
Create `.planning/phases/22-conservative-contracts-and-relationship-graph/22-06-SUMMARY.md` when done.
</output>

---
phase: 40-project-identity-leases-continuity-and-safe-resume
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - src/lease/briefing.mjs
  - src/lifecycle/router-lifecycle.mjs
  - src/cli/router-control.mjs
  - tests/router.lease-briefing.test.mjs
  - tests/router.lifecycle.test.mjs
  - tests/router.control-cli.test.mjs
autonomous: true
requirements: [LEASE-06]
user_setup: []
estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "composeBriefing returns null for a first Router visit (no lease record for the current project fingerprint) so the first visit is silent (LEASE-06)."
    - "composeBriefing returns at most one evidence-backed briefing for a returning project with an active, non-expired, non-revoked, fingerprint-matching lease; the briefing references receipt IDs (operator inspects via CLI), keeping injection <=120 tokens (LEASE-06)."
    - "composeBriefing returns null for each of the eight invalid states — completed, blocked, expired, revoked, corrupt, stale, unauthorized, foreign — so invalid or non-active continuity never auto-runs (LEASE-06)."
    - "The 4 lease modules (identity.mjs, store.mjs, policy.mjs, briefing.mjs) deploy to BOTH ~/.claude/router/modules/lease/ and ~/.codex/router/modules/lease/ via the moduleValues flatMap (T-39-03 regression backstop)."
    - "An operator can inspect all leases, show a single lease's 9 fields, and revoke a lease via the `router-control leases` CLI command (LEASE-03/04 operator surface)."
    - { statement: "LEASE-06 'unclassified' edge — a 'returning project' is defined as a project fingerprint with at least one prior lease record; first visit is the absence of any lease record for the fingerprint, not the absence of any Router activity.", verification: backstop }
  artifacts:
    - src/lease/briefing.mjs
    - tests/router.lease-briefing.test.mjs
  key_links:
    - "composeBriefing({ projectFingerprint, leaseStore, now }) → { briefing, lease_id, evidence, briefing_status } | null (src/lease/briefing.mjs)"
    - "src/lifecycle/router-lifecycle.mjs moduleNames array (+4 lease modules) → moduleValues flatMap over [ownedRoot, codexOwnedRoot]"
    - "router-control.mjs `leases` command → { inspect, show <id>, revoke <id> } using createLeaseStore + inspect/setStatus from Plans 01/02"
  prohibitions:
    - { statement: "First Router visit MUST NOT emit a briefing; returning projects receive at most one evidence-backed briefing (LEASE-06).", status: resolved, verification: backstop }
    - { statement: "Completed/blocked/expired/revoked/corrupt/stale/unauthorized/foreign lease state MUST NOT auto-run or emit a briefing (LEASE-06).", status: resolved, verification: backstop }
    - { statement: "Lease modules MUST NOT deploy to only one runtime; the moduleValues flatMap over [ownedRoot, codexOwnedRoot] deploys to both (T-39-03 regression, Pitfall 6).", status: resolved, verification: backstop }
    - { statement: "Router MUST NOT spawn a daemon or subprocess for lease lifecycle; leases are plain files read on the hot path / next-prompt re-read (Out-of-Scope: independent autonomous daemon).", status: resolved, verification: backstop }
    - { statement: "The briefing MUST NOT inline raw prompt text or full receipt bodies; it references receipt IDs so the operator inspects via CLI (CLAUDE.md <=120 token injection constraint).", status: resolved, verification: backstop }
---

<objective>
Build the continuity briefing composer (LEASE-06), deploy all four lease modules to both Claude and Codex runtimes via the lifecycle bundle, and expose an operator-facing `router-control leases` CLI for inspecting and revoking leases. This plan closes Phase 40: it makes leases observable to the operator, silent on first visit, evidence-backed on return, and deployed to both runtimes.

Purpose: LEASE-06 is the operator-visible continuity contract — silence on first visit, one evidence-backed briefing on return, never auto-run on invalid state. Lifecycle deploy closes the T-39-03 regression (single-runtime deploy would ENOENT in Codex). The CLI gives the operator the inspect/revoke surface LEASE-03/04 promise.

Output: src/lease/briefing.mjs + test, router-lifecycle.mjs moduleNames bump + lifecycle test count bump, router-control.mjs `leases` command + CLI test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-project-identity-leases-continuity-and-safe-resume/40-RESEARCH.md
@.planning/phases/40-project-identity-leases-continuity-and-safe-resume/40-PATTERNS.md
@.planning/phases/40-project-identity-leases-continuity-and-safe-resume/40-01-PLAN.md
@.planning/phases/40-project-identity-leases-continuity-and-safe-resume/40-02-PLAN.md
@src/steward/startup-pointer.mjs
@src/lifecycle/router-lifecycle.mjs
@src/cli/router-control.mjs
@tests/router.steward-startup.test.mjs
@tests/router.lifecycle.test.mjs
@tests/router.control-cli.test.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Continuity briefing composer — first-visit silent, one briefing, 8 invalid states silent (LEASE-06)</name>
  <files>src/lease/briefing.mjs, tests/router.lease-briefing.test.mjs</files>
  <read_first>
    - src/steward/startup-pointer.mjs (loadStartupPointer validity gate + cooldown + unavailable() sentinel, lines 9-113)
    - src/lease/store.mjs (findByFingerprint, isExpired, inspect from Plan 01)
    - src/lease/policy.mjs (resolveLeaseAuthority reason_codes from Plan 02 — briefing reuses the active/revoked/expired/foreign distinction)
    - src/lease/identity.mjs (computeLeaseFingerprint — the projectFingerprint input shape)
    - tests/router.steward-startup.test.mjs (cooldown + validity-gate test pattern)
  </read_first>
  <behavior>
    - composeBriefing({ projectFingerprint, leaseStore }) with no lease for the fingerprint → null (first-visit silent)
    - composeBriefing with an active, non-expired, fingerprint-matching lease → { briefing:true, lease_id, evidence: lease.last_safe_checkpoint, briefing_status:'active' }
    - composeBriefing with a lease whose status is 'completed' → null (briefing_status:'completed' internally, no injection)
    - Same null for status in {blocked, expired, revoked, corrupt, stale, unauthorized, foreign} — each maps to a distinct internal briefing_status but all return null
    - composeBriefing with an expired lease (isExpired true, status active) → null (briefing_status:'expired')
    - composeBriefing with fingerprint_match false → null (briefing_status:'foreign')
    - composeBriefing when leaseStore.findByFingerprint throws → null (fail-open)
  </behavior>
  <action>
    Create src/lease/briefing.mjs. Export BRIEFING_POLICY_VERSION = 'briefing-policy-v1'. Export composeBriefing({ projectFingerprint, leaseStore, now = Date.now() }) wrapped in try/catch returning null on any throw (fail-open — a briefing-store error never blocks a prompt). Define the INVALID set as Object.freeze(['completed','blocked','expired','revoked','corrupt','stale','unauthorized','foreign']). Steps: const lease = leaseStore.findByFingerprint(projectFingerprint); if (!lease) return null (first-visit silent — the defining LEASE-06 case). Compute a briefing_status: if INVALID.has(lease.status) → that status string; else if leaseStore.isExpired(lease, now) → 'expired'; else if !lease.freshness_evidence?.fingerprint_match → 'foreign'; else → 'active'. If briefing_status !== 'active' return null (no injection — each invalid state is silent). If active, return { briefing:true, lease_id: lease.lease_id, evidence: lease.last_safe_checkpoint, briefing_status:'active', policy_version: BRIEFING_POLICY_VERSION }. The evidence field references receipt IDs from last_safe_checkpoint (operator inspects via `router-control leases show <id>`); do NOT inline receipt bodies or raw prompt text. The briefing body is composed by the caller (router startup path) as an additionalContext string; composeBriefing only returns the structured payload. Add a leading comment: LEASE-06 hard constraint — first visit silent, returning one evidence-backed briefing, eight invalid states silent, never auto-run.

    Write tests/router.lease-briefing.test.mjs using the tmpdir fixture + createLeaseStore. Cases: (1) no lease → null; (2) active lease (expiryMs NOW+60_000, fingerprint_match true) → { briefing:true, lease_id, evidence, briefing_status:'active' }; (3) setStatus('completed') → null; (4) setStatus('blocked') → null; (5) setStatus('revoked') → null; (6) expired lease (expiryMs NOW-1) → null; (7) foreign (lease.freshness_evidence.fingerprint_match = false via a fixture) → null; (8) corrupt store (writeFileSync bad JSON to the lease file) → null; (9) leaseStore.findByFingerprint monkey-patched to throw → null. Assert BRIEFING_POLICY_VERSION === 'briefing-policy-v1'. Assert no test produces a non-null result for any of the eight invalid states.
  </action>
  <verify>
    <automated>rtk node --test tests/router.lease-briefing.test.mjs</automated>
  </verify>
  <done>First-visit silent; active lease produces one evidence-backed briefing referencing receipt IDs; all eight invalid states produce null; throw paths fail-open to null.</done>
  <acceptance_criteria>
    - src/lease/briefing.mjs contains `export const BRIEFING_POLICY_VERSION` and `export function composeBriefing`
    - composeBriefing returns null for: no lease, each of 8 invalid statuses, expired, foreign, and any throw
    - composeBriefing returns { briefing:true, briefing_status:'active' } only for an active, non-expired, fingerprint-matching lease
    - tests/router.lease-briefing.test.mjs exits 0
    - No test or briefing output inlines raw prompt text (grep on test + briefing.mjs for 'prompt' returns only comments, not field values)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deploy 4 lease modules to both runtimes + lifecycle test bump (T-39-03 regression backstop)</name>
  <files>src/lifecycle/router-lifecycle.mjs, tests/router.lifecycle.test.mjs</files>
  <read_first>
    - src/lifecycle/router-lifecycle.mjs (moduleNames array lines 384-422, moduleValues flatMap lines 423-425 over [p.ownedRoot, p.codexOwnedRoot])
    - tests/router.lifecycle.test.mjs (existing module-count assertion + dual-runtime deploy assertion)
    - src/lease/identity.mjs, src/lease/store.mjs, src/lease/policy.mjs, src/lease/briefing.mjs (the 4 modules to deploy — confirm each exists and is stdlib-only)
  </read_first>
  <behavior>
    - moduleNames array contains 'lease/identity.mjs', 'lease/store.mjs', 'lease/policy.mjs', 'lease/briefing.mjs'
    - After deploy, both ~/.claude/router/modules/lease/identity.mjs and ~/.codex/router/modules/lease/identity.mjs exist (and the other three)
    - The lifecycle test count assertion is bumped by 4 (the count is the T-39-03 regression backstop)
    - No custom deploy path added — the moduleValues flatMap handles both runtimes
  </behavior>
  <action>
    In src/lifecycle/router-lifecycle.mjs, add the 4 new module names to the moduleNames array (lines ~384-422) in a clearly commented Phase 40 block: 'lease/identity.mjs', 'lease/store.mjs', 'lease/policy.mjs', 'lease/briefing.mjs'. Place the block after the existing 'intent/authority.mjs' (Phase 39) entry and the dispatch adapter block, with a comment: "Phase 40 LEASE-01..06: lease lifecycle deployed to BOTH ownedRoot and codexOwnedRoot via the moduleValues flatMap below (T-39-03 regression backstop; lifecycle test count bump is the regression gate)." Do NOT add a custom deploy path — the existing moduleValues flatMap over [p.ownedRoot, p.codexOwnedRoot] (lines ~423-425) handles both runtimes.

    In tests/router.lifecycle.test.mjs: bump the module-count assertion by 4 (find the existing count assertion — the count is the regression backstop). Add a test asserting all 4 lease modules exist under both runtime roots after deploy: existsSync(join(ownedRoot, 'modules/lease/identity.mjs')) && existsSync(join(codexOwnedRoot, 'modules/lease/identity.mjs')) and the same for store/policy/briefing. Add a test asserting no lease module imports node:http, node:net, or any npm package (read each deployed file, assert no `import ... from 'npmpkg'` — stdlib-only).
  </action>
  <verify>
    <automated>rtk node --test tests/router.lifecycle.test.mjs</automated>
  </verify>
  <done>All 4 lease modules deploy to both ~/.claude/router/modules/lease/ and ~/.codex/router/modules/lease/; lifecycle test count bumped; dual-runtime existence asserted.</done>
  <acceptance_criteria>
    - src/lifecycle/router-lifecycle.mjs moduleNames array contains 'lease/identity.mjs', 'lease/store.mjs', 'lease/policy.mjs', 'lease/briefing.mjs'
    - tests/router.lifecycle.test.mjs exits 0
    - The lifecycle test count assertion is increased by exactly 4
    - A test asserts existsSync for each of the 4 lease modules under both ownedRoot and codexOwnedRoot
    - No lease module imports any non-stdlib package (grep test passes)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: router-control `leases` CLI — inspect, show, revoke (LEASE-03/04 operator surface)</name>
  <files>src/cli/router-control.mjs, tests/router.control-cli.test.mjs</files>
  <read_first>
    - src/cli/router-control.mjs (command dispatch ~lines 555-700, 1073+; canonical() shape line 45; EXIT codes; safeToken; runContextCommand pattern)
    - src/lease/store.mjs (createLeaseStore, inspect, setStatus from Plans 01/02)
    - src/lease/policy.mjs (LEASE_POLICY_VERSION, resolveLeaseAuthority from Plan 02)
    - tests/router.control-cli.test.mjs (existing CLI command test pattern, tmpdir fixture)
  </read_first>
  <behavior>
    - `router-control leases inspect` on an empty store → canonical('leases inspect', true, 'ok', { leases: [] }), exit 0
    - `router-control leases inspect` with one active lease → data.leases[0] contains { lease_id, goal, status, expiry, authority_source }
    - `router-control leases show <lease_id>` → data contains all 9 inspection fields + is_expired + is_revoked
    - `router-control leases revoke <lease_id>` → ok true, data.status === 'revoked'; a subsequent `show` confirms status 'revoked'
    - `router-control leases revoke <missing_id>` → ok false, reason_code 'lease_absent'
    - `router-control leases` with no subcommand → ok false, reason_code 'invalid_arguments'
  </behavior>
  <action>
    Add a 'leases' command to src/cli/router-control.mjs command dispatch (alongside 'suggestion', 'context'). Subcommands: 'inspect' (default), 'show <lease_id>', 'revoke <lease_id>'. Use a createLeaseStore instance rooted at defaultLeaseRoot(RUNTIME) — import defaultLeaseRoot + createLeaseStore from '../lease/store.mjs' (deployed modules path; if import fails, return canonical('leases', false, 'lease_module_unavailable') fail-open — mirrors the existing module-absent pattern). 'inspect': read all leases (readdirSync root, readLease each that parses), return canonical('leases inspect', true, 'ok', { leases: leases.map(l => ({ lease_id, goal, status, expiry, authority_source })) }). 'show <id>': leaseStore.inspect(id); if null → canonical('leases show', false, 'lease_absent'); else canonical('leases show', true, 'ok', inspect_result). 'revoke <id>': leaseStore.setStatus(id, 'revoked'); if lease absent (readLease null) → canonical('leases revoke', false, 'lease_absent'); else canonical('leases revoke', true, 'ok', { lease_id, status:'revoked' }). Validate positional args (subcommand in ['inspect','show','revoke']; show/revoke require a lease_id token via safeToken). Reuse the canonical() output shape and EXIT codes. Do NOT emit raw prompt text in any CLI output — only the structured goal label + the 9 inspection fields.

    Add tests to tests/router.control-cli.test.mjs: import the leases command path. Cases: empty store inspect → ok true, data.leases []; create a lease (via createLeaseStore + buildLeaseRecord) → inspect shows 1 lease with the 5 summary fields; show <id> → data has all 9 fields + is_expired + is_revoked; revoke <id> → ok true, data.status 'revoked', then show <id> confirms 'revoked'; revoke <missing> → ok false 'lease_absent'; `leases` with no subcommand → ok false 'invalid_arguments'. Use the tmpdir fixture pattern; root the lease store at the tmpdir.
  </action>
  <verify>
    <automated>rtk node --test tests/router.control-cli.test.mjs</automated>
  </verify>
  <done>Operator can list leases, inspect a single lease's 9 fields, and revoke a lease via `router-control leases`; missing lease and no-subcommand cases fail-closed with reason_codes.</done>
  <acceptance_criteria>
    - src/cli/router-control.mjs contains a `leases` command dispatch with `inspect`, `show`, `revoke` subcommands
    - `router-control leases inspect` returns canonical(..., true, 'ok', { leases })
    - `router-control leases show <id>` returns all 9 fields + is_expired + is_revoked
    - `router-control leases revoke <id>` flips status to 'revoked' durably (a follow-up show confirms)
    - tests/router.control-cli.test.mjs exits 0
    - No CLI output inlines raw prompt text
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lease state → startup briefing injection | A briefing is injected as additionalContext on startup; an invalid/foreign lease must NOT produce a briefing (no auto-run). |
| CLI → lease store mutation | `router-control leases revoke` mutates the lease store; it is an operator-only tool (not hot-path). |
| lifecycle deploy → dual-runtime filesystem | Lease modules must exist under both ~/.claude and ~/.codex roots; a single-runtime deploy ENOENTs in the other runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-09 | Elevation of privilege | composeBriefing / startup injection | high | mitigate | First-visit silent (no lease → null); eight invalid states each return null; briefing requires active + non-expired + fingerprint-match. Test asserts all 8 invalid states + first-visit null. |
| T-40-10 | Information disclosure | briefing / CLI output | medium | mitigate | Briefing references receipt IDs only (no raw prompt, no full receipt bodies); CLI shows the structured goal label + 9 fields, never raw prompt. |
| T-40-11 | Tampering | router-control leases revoke | medium | mitigate | revoke uses the existing setStatus durable atomic write; missing lease → 'lease_absent'; CLI is operator-only, not hot-path. |
| T-40-12 | Tampering | lifecycle dual-runtime deploy | high | mitigate | 4 lease modules added to moduleNames; moduleValues flatMap deploys to both runtimes; lifecycle test count bump is the regression backstop; test asserts existsSync under both roots (Pitfall 6 / T-39-03). |
| T-40-SC | Tampering | npm/pip/cargo installs | high | accept | Stdlib-only; no packages installed this plan. |
</threat_model>

<verification>
- `rtk node --test tests/router.lease-briefing.test.mjs tests/router.lifecycle.test.mjs tests/router.control-cli.test.mjs` green
- `rtk node --test tests/router.lease-identity.test.mjs tests/router.lease-creation.test.mjs tests/router.lease-inspect.test.mjs tests/router.lease-revoke.test.mjs tests/router.lease-resume.test.mjs` green (Plans 01/02 regression)
- `rtk node --test tests/router.authority*.test.mjs tests/router.authority-gate.test.mjs` green (Phase 39 regression)
- `rtk node --test tests/router.perf.test.mjs` warm p95 <=25ms / hard max <100ms still holds (briefing runs on the startup path, not the per-prompt hot path — verify no prompt-path regression)
- Full suite gate before `/gsd-verify-work`: `rtk node --test tests/*.test.mjs` green
</verification>

<success_criteria>
- LEASE-06: first visit silent; returning project at most one evidence-backed briefing; eight invalid states silent; never auto-run
- T-39-03 regression backstop: all 4 lease modules deploy to both runtimes; lifecycle test count bumped
- Operator CLI: `router-control leases { inspect, show <id>, revoke <id> }` works
- Phase 38/39 regression suites green; prompt-path perf budget intact
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

**New symbols:**
- `BRIEFING_POLICY_VERSION = 'briefing-policy-v1'` (src/lease/briefing.mjs)
- `composeBriefing({ projectFingerprint, leaseStore, now })` → `{ briefing, lease_id, evidence, briefing_status, policy_version } | null` (src/lease/briefing.mjs)
- `router-control leases` command with subcommands `inspect`, `show <lease_id>`, `revoke <lease_id>` (src/cli/router-control.mjs)

**Modified:**
- src/lifecycle/router-lifecycle.mjs (moduleNames array +4 lease module entries)
- src/cli/router-control.mjs (+`leases` command dispatch)

**New files:**
- src/lease/briefing.mjs
- tests/router.lease-briefing.test.mjs

**Extended tests:**
- tests/router.lifecycle.test.mjs (count bump +4, dual-runtime lease-module existence assertion)
- tests/router.control-cli.test.mjs (`leases` command cases)
</artifacts_produced>

<output>
Create `.planning/phases/40-project-identity-leases-continuity-and-safe-resume/40-03-SUMMARY.md` when done
</output>
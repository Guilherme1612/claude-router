---
phase: 32-intent-first-routing-mode-map-schema-v4-guard-hole-closure
plan: 03
type: execute
wave: 2
depends_on: [32-02]
files_modified:
  - tests/router.mjs.snapshot
  - /Users/guilherme/.claude/hooks/router.mjs
  - src/coverage/audit.mjs
  - tests/router.coverage-audit.test.mjs
  - tests/router.schema-v4-routing.test.mjs
autonomous: true
requirements: [ROUTE-04, ROUTE-05]
user_setup: []
estimate:
  tokens: 65000
  raw_tokens: 35000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - A high-confidence intent with an empty resolve set emits at most one generic native-capability fallback line — never a fabricated capability name (32-01 ROUTE-04 RED → GREEN).
    - Resolve lists pass a tie-lint CI gate: a near-tie (members within a weight gap) downgrades the route to `med`; a stale-target quarantine flags absent resolve members instead of shipping them (32-01 cross-runtime Group C RED → GREEN).
    - The coverage audit-guard's forward-orphan check now covers resolve-list members: a schema_version-SET fixture with an absent resolve member produces a forward-orphan/stale-target diagnostic (32-01 coverage-audit extension RED → GREEN; audit.mjs:142 closed).
  artifacts:
    - tests/router.mjs.snapshot + ~/.claude/hooks/router.mjs (generic fallback; mirror)
    - src/coverage/audit.mjs (guard-hole closure at :142 + forward-orphan over resolve lists)
    - tests/router.coverage-audit.test.mjs (extension GREEN)
    - a resolve tie-lint check (new file, e.g. scripts/resolve-tie-lint.mjs or a spec-driven gate)
  key_links:
    - audit.mjs:142 `schemaRoute = mode === route && Boolean(modeMap.schema_version)` must flip to a resolve-aware check or the 32-01 coverage-audit extension stays RED.
    - The generic-fallback branch must only fire for high-confidence intents with an EMPTY resolve set (never after a real resolve attempt).
    - The tie-lint near-tie rule is the concrete trigger for the "downgrade to med" behavior — must be deterministic and coverable.
---

<objective>
Close the coverage audit-guard half of the guard hole (audit.mjs:142), add the ROUTE-04 generic native-capability fallback, and ship the ROUTE-05 resolve-list tie-lint + forward-orphan enforcement — the quality gates that keep resolve lists honest.

Purpose: With 32-02 having closed the router hot-path half of the guard hole, this plan closes the same hole in the coverage audit-guard (the third code-verified site) and adds the two remaining route behaviors (single generic fallback when nothing resolves; tie-lint/quarantine so resolve lists never ship a fabricated or weakly-tied target). This turns the remaining Wave-0 RED surfaces GREEN.

Output: audit.mjs forward-orphan closure over resolve lists; generic-fallback branch in the shipped route decision (mirror); a deterministic tie-lint gate with near-tie → `med` downgrade and stale-target quarantine; coverage-audit test extension GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-intent-first-routing-mode-map-schema-v4-guard-hole-closure/32-CONTEXT.md

# Reference files (read once, extract exact localities):
@src/coverage/audit.mjs  # typedMappings slash branch:138-144 (schemaRoute:142); forwardDiagnostics:283; functions:27,60,78,225
@tests/router.mjs.snapshot  # resolveSlashRoute + validation from 32-02; route verification:850-870 (generic fallback insertion point); buildKnownNames:407
@tests/router.coverage-audit.test.mjs  # runBuilder:16 (add schemaVersion default 4 variant); manifest()
@tests/router.schema-v4-routing.test.mjs  # 32-01 RED targets this plan turns GREEN (do NOT edit to weaken)
@tests/router.phase32-cross-runtime.test.mjs  # 32-01 Group C tie-lint RED target
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Close the coverage audit-guard at audit.mjs:142 + extend forward-orphan over resolve lists</name>
  <files>src/coverage/audit.mjs, tests/router.coverage-audit.test.mjs</files>
  <read_first>src/coverage/audit.mjs</read_first>
  <behavior>
    - Test 1: `auditCoverage` on a schema_version-4 modeMap whose slash entry is `gsd-debug` (id===mode) against a manifest with NO commands and no present resolve member produces a `stale_target` diagnostic (32-01 coverage-audit extension RED → GREEN). Today audit.mjs:142's `schemaRoute = mode === route && Boolean(modeMap.schema_version)` treats it as valid → the assertion must flip to RED-then-GREEN on this task.
    - Test 2: a slash entry whose resolve list contains a member whose name IS a present manifest command produces NO stale_target (explicit resolve member present → valid).
    - Test 3: a resolve-list member whose name is ABSENT from the manifest is reported through the forward-orphan / forward_diagnostics channel.
  </behavior>
  <action>
    In `src/coverage/audit.mjs`, replace the guard-hole logic at the slash branch (:138-144). Change
      `const schemaRoute = mode && mode === route && Boolean(modeMap.schema_version);`
    to a resolve-aware check mirroring 32-02's `resolveSlashRoute` semantics: a slash route maps to a command only when the mode is a present manifest command, OR an explicit resolve-list member resolves to a present manifest command. Remove the blanket `Boolean(modeMap.schema_version)` pass so schema_version truthiness no longer exempts slash routes in the audit. (Keep the `alias = mode && mode !== route && routeIds.has(mode)` branch unchanged.)

    Extend the forward-orphan/filtering logic (:283 and the diagnostics assembly around it) so resolve-list members that target a name absent from the active manifest are reported as forward-orphan/stale-target diagnostics (not silently dropped). Reuse `indexes.command`/`indexes.skill` presence sets already threaded through `typedMappings`. Do not weaken 32-01's coverage-audit extension — run `tests/router.coverage-audit.test.mjs` and let it drive the closure.
  </action>
  <verify>
    <automated>rtk node --test tests/router.coverage-audit.test.mjs 2>&1 | grep -E "stale|orphan|forward|pass|fail" | head -25</automated>
  </verify>
  <done>audit.mjs:142's schema_version blanket pass is removed; slash routes and resolve-list members are validated for presence; the 32-01 coverage-audit extension (stale_target for absent resolve member on a schema_version-SET fixture) is GREEN; pre-existing coverage-audit tests remain GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ROUTE-04 generic native-capability fallback in the shipped route decision (mirror)</name>
  <files>tests/router.mjs.snapshot, /Users/guilherme/.claude/hooks/router.mjs, tests/router.schema-v4-routing.test.mjs</files>
  <read_first>tests/router.mjs.snapshot</read_first>
  <behavior>
    - Test 1: a high-confidence intent whose matched entry has an EMPTY resolve set (no mode present, no resolve member present) emits AT MOST ONE generic native-capability fallback line (e.g. "Run a command from your available toolset") and NEVER a fabricated capability/slash name (32-01 ROUTE-04 RED → GREEN).
    - Test 2: when a real resolve member IS present, the generic fallback does NOT fire (the resolve-first suggestion wins).
  </behavior>
  <action>
    In the shipped route-decision path (mirrored snapshot, insertion near the resolve-first evaluation from 32-02 at :850-870), add the high-confidence Rustoute-04 branch: when the resolve-first resolution returns null (empty resolved set, no present member) BUT the intent is high-confidence (confident tier), emit at most one generic native-capability fallback line — a fixed, capability-agnostic string that never names a fabricated command. The generic line must be a single, predefined string (choose a constant) — not derived from the entry id or any resolve candidate — so a fabricated capability name is impossible. Do not fire it when a resolve member is present (the real suggestion always wins). Keep the fail-open guarantee: any exception on this branch → silent pass-through.

    Mirror both copies (snapshot + `~/.claude/hooks/router.mjs`). Do NOT weaken 32-01's ROUTE-04 assertion — it drives this to GREEN. Note the acceptance-criterion phrasing must never itself ship a fabricated name; the generic line references only native capabilities, never a specific framework.
  </action>
  <verify>
    <automated>rtk node --test tests/router.schema-v4-routing.test.mjs 2>&1 | grep -E "generic|fallback|fabricated|pass|fail" | head -25</automated>
  </verify>
  <done>A high-confidence intent with an empty resolve set emits at most one generic native-capability fallback line (constant string, no fabricated name) and the real resolve suggestion wins when present; 32-01 ROUTE-04 assertion GREEN; mirror-lockstep maintained.</done>
</task>

<task type="auto">
  <name>Task 3: Ship the resolve-list tie-lint gate (near-tie → med, stale-target quarantine) + full-suite verify</name>
  <files>scripts/resolve-tie-lint.mjs, tests/router.schema-v4-routing.test.mjs</files>
  <action>
    Create a deterministic resolve-list lint gate as a new script `scripts/resolve-tie-lint.mjs` (Node stdlib only, per the no-dep constraint). It reads a mode-map (path via env/arg, defaulting to `~/.claude/router/mode-map.json`) and emits a report + non-zero exit on violations:
    - NEAR-TIE: for each slash entry's resolve list, if two members' weights (or implied rank when weights absent) are within a tie gap of 0.05, the entry is flagged as a near-tie and its route tier is downgraded to `med` (the route must not emit a confident-position suggestion). Emit a row per offending entry.
    - STALE-TARGET QUARANTINE: for each resolve member whose stripped name is absent from the active manifest's command inventory, flag it and quarantine it (the route is suppressed, not shipped) — aligned with the coverage audit-guard's forward-orphan semantics. GSD-vs-any-framework agnostic; reads only entry data, never hardcodes a framework prefix.

    Add a CI spec (extend `tests/router.schema-v4-routing.test.mjs` or add `tests/router.resolve-tie-lint.test.mjs`) that runs the lint against the 32-01 near-tie and stale-target fixtures and asserts the downgrade/quarantine outcomes — this turns the remaining 32-01 cross-runtime Group C tie-lint RED surface GREEN. Do not weaken the 32-01 assertion.

    Then run the full suite to confirm no regression from audit.mjs, the generic fallback, and the tie-lint. At minimum run resolve/coverage-audit/guard/failopen/snapshot.diff specs.
  </action>
  <verify>
    <automated>rtk node --test tests/router.schema-v4-routing.test.mjs tests/router.resolve-tie-lint.test.mjs tests/router.mjs.snapshot.diff.test.mjs 2>&1 | tail -20</automated>
  </verify>
  <done>A deterministic tie-lint CI gate exists (near-tie → `med`, stale-target quarantine, stdlib-only, framework-neutral), the 32-01 tie-lint/forward-orphan RED surface is GREEN, and the full suite is regression-free for resolve/coverage-audit/guard/failopen with mirror-lockstep intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| resolve list → audit/coverage report | An absent resolve member could slip through the audit uncovered (the audit half of the guard hole) |
| resolve list → emitted route | A near-tie or stale member could ship a weak/fabricated suggestion |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-09 | Repudiation | audit.mjs schemaRoute guard hole (:142) | critical | mitigate | Replace `Boolean(modeMap.schema_version)` with a resolve-aware presence check; 32-01 coverage-audit extension RED → GREEN enforces closure. |
| T-32-10 | Tampering | forward-orphan channel | high | mitigate | Resolve-list members absent from active manifest reported as forward_orphan diagnostics (extend :283); never silently dropped. |
| T-32-11 | Spoofing | generic fallback fabrication | medium | mitigate | Fallback emits a fixed constant string, never derived from entry id/candidates — fabricated names impossible; only fires for high-confidence empty resolve set. |
| T-32-12 | Elevation of Privilege | tie-lint gate | low | mitigate | Lint is a pure read-only gate over mode-map; near-tie deterministically downgrades to med; stale targets quarantined, never shipped. |

## Package legitimacy gate
No package-manager installs — stdlib-only (`scripts/resolve-tie-lint.mjs` uses node:fs, node:path only). Not applicable.
</threat_model>

<verification>
`rtk node --test tests/router.coverage-audit.test.mjs tests/router.schema-v4-routing.test.mjs tests/router.resolve-tie-lint.test.mjs tests/router.mjs.snapshot.diff.test.mjs tests/router.failopen.test.mjs`
Expected: audit guard closure + forward-orphan GREEN; generic fallback GREEN; tie-lint GREEN; snapshot.diff (mirror) GREEN; failopen GREEN. Full suite at wave merge.
</verification>

<success_criteria>
- audit.mjs:142 guard hole closed: schema_version no longer exempts slash routes; resolve-list presence validated. 32-01 coverage-audit extension GREEN.
- ROUTE-04: high-confidence empty-resolve emits at most one generic native line, never a fabricated name. 32-01 assertion GREEN.
- ROUTE-05: deterministic tie-lint (near-tie → med, stale-target quarantine) + forward-orphan coverage. 32-01 Group C + lint spec GREEN.
- Mirror-lockstep maintained (snapshot == live hook).
- PARITY-03/04 cross-runtime + perf budget are NOT in this plan — deferred to 32-04.
</success_criteria>

<output>
Create `.planning/phases/32-intent-first-routing-mode-map-schema-v4-guard-hole-closure/32-03-SUMMARY.md` when done
</output>

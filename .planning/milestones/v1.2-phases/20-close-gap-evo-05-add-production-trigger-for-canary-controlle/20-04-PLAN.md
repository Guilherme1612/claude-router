---
phase: 20-close-gap-evo-05-add-production-trigger-for-canary-controlle
plan: 04
type: execute
wave: 4
depends_on: [20-02]
files_modified:
  - src/registry/watcher.mjs
  - tests/router.watcher-canary-trigger.test.mjs
autonomous: true
gap_closure: true
requirements: [EVO-05]
user_setup: []

must_haves:
  truths:
    - "Watcher invokes the canary path (proposeCandidate -> evaluateCandidate -> applyCanaryDecision) on EVERY eligible reconcile with known-good + sufficient evidence — verified by a multi-reconcile regression test calling reconcile() TWICE and asserting applyCanaryDecision runs on both calls (CR-01 closed)."
    - "src/registry/watcher.mjs no longer declares `let recovered = false;` at factory scope (line 304) — recovered is reset per reconcile call (moved inside reconcile or reset at its top)."
    - "src/registry/watcher.mjs line ~337 no longer contains the both-branches-null ternary `authority_status === 'empty' ? null : null`; knownGood is initialized plainly and set inside the recovery block."
    - "Existing tests/router.watcher-canary-trigger.test.mjs tests still pass (no regression in the single-reconcile cases)."
  artifacts:
    - src/registry/watcher.mjs (recovered reset + knownGood initializer fix)
    - tests/router.watcher-canary-trigger.test.mjs (new multi-reconcile regression test)
  key_links:
    - "watcher.mjs reconcile eligible+recoveryReady branch -> canary-controller applyCanaryDecision (reachable on EVERY eligible reconcile, not just the first)"
---

<objective>
Close CR-01: the watcher's production canary trigger is dead after the first reconcile because `let recovered = false;` (src/registry/watcher.mjs:304) is declared at factory scope and never reset per-call, and line 337's ternary `active.authority_status === 'empty' ? null : null` is a refactoring bug (both branches return null). After the first successful recovery sets `recovered = true`, every subsequent reconcile skips the recovery block (line 338 guard `!recovered`), knownGood stays null, and line 372 `if (knownGood === null)` routes to the bootstrap path — activating the candidate directly with `reason:'watcher'` and bypassing applyCanaryDecision, the 6 REQUIRED_GATES, the evidence sufficiency gate, and the D-05 demonstrated_benefit derivation.

Purpose: Restore EVO-05's production canary trigger so telemetry drives canary promotion + rollback on EVERY eligible reconcile, not just the first. This is the PRIMARY automatic production trigger (20-02's core deliverable) and the root of audit BLOCKER 2's "production trigger wired" claim.

Output: Surgical two-line source fix (reset `recovered` per reconcile; fix the line-337 initializer) plus a multi-reconcile regression test that calls reconcile() TWICE and asserts applyCanaryDecision runs on both calls. No adjacent code is refactored; the hot path src/context/prompt-route.mjs is untouched.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/20-close-gap-evo-05-add-production-trigger-for-canary-controlle/20-VERIFICATION.md
@.planning/phases/20-close-gap-evo-05-add-production-trigger-for-canary-controlle/20-02-SUMMARY.md
@src/registry/watcher.mjs
@src/evolution/canary-controller.mjs
@tests/router.watcher-canary-trigger.test.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reset `recovered` per reconcile + fix line-337 knownGood initializer + add multi-reconcile regression test</name>
  <files>src/registry/watcher.mjs, tests/router.watcher-canary-trigger.test.mjs</files>
  <read_first>
    - src/registry/watcher.mjs (lines 290-485 — the reconcile function and the eligible+recoveryReady branch; confirm the exact current text of line 304 `let recovered = false;`, line 337 `let knownGood = active.authority_status === 'empty' ? null : null;`, line 338 guard `if (!recovered && active.authority_status !== 'empty')`, line 341 `recovered = true;`, line 343 `knownGood = recoveryResult.version_id || null;`, and line 372 `if (knownGood === null)`)
    - .planning/phases/20-close-gap-evo-05-add-production-trigger-for-canary-controlle/20-VERIFICATION.md (the authoritative gap spec — gaps[0] CR-01 with exact line numbers and required fixes)
    - .planning/phases/20-close-gap-evo-05-add-production-trigger-for-canary-controlle/20-02-SUMMARY.md (what was built for the watcher; test conventions + the `createRegistryReconciler` dependency seam keys: applyCanaryDecision/buildCandidateCalibrationRoute/buildKnownGoodCalibrationRoute/measureRoutes/assessCalibration/evaluateCalibrationCorpus/createPersistentEvidenceStore/compatible)
    - src/evolution/canary-controller.mjs (applyCanaryDecision signature lines 148-157; evaluateCandidate line 91-112 — promotable:false when evidence_window.sufficient !== true)
    - tests/router.watcher-canary-trigger.test.mjs (the ANALOG — replicate its `makeCanaryDeps` fixture, `makeEvidenceStore(30)` helper, `createTestRegistryReconciler` invocation, and assertion style; the new test calls reconcile() TWICE on the same reconciler with the same eligible candidate + known-good + sufficient evidence and asserts the canaryDecision spy is invoked on BOTH calls)
  </read_first>
  <behavior>
    - Test (multi-reconcile): given a reconciler with test_mode=false, an eligible candidate, a healthy recovery returning version_id=KNOWN_GOOD, and a sufficient evidence window (30 records), calling reconcile() TWICE in succession invokes the canaryDecision dependency on BOTH calls (canaryCalls.length === 2), and BOTH calls return an activation status other than the bootstrap `reason:'watcher'` path (i.e., the canary path runs, not the bootstrap path).
    - Test (regression guard): the existing 7 single-reconcile tests still pass unchanged.
    - Source assertion: `grep -c "let recovered = false;" src/registry/watcher.mjs` matches the moved/reset form (the declaration is inside reconcile OR a `recovered = false;` reset statement appears at the top of the reconcile function body).
    - Source assertion: the line-337 both-branches-null ternary (both branches return null) is removed from src/registry/watcher.mjs — verified by reading the source (the dead-code ternary is gone, replaced with a plain null initializer).
  </behavior>
  <action>
    RED first — add the multi-reconcile regression test to tests/router.watcher-canary-trigger.test.mjs (new test, e.g. "Test 8: canary path runs on EVERY eligible reconcile (multi-reconcile regression — CR-01)"). Replicate the existing `makeCanaryDeps('promote', makeEvidenceStore(30), ...)` fixture from the promote test, but construct the reconciler once and call `await reconciler.reconcile({ diff })` TWICE with the same diff (same eligible candidate). Assert the `canaryDecision` spy (the `canaryCalls` array from `makeCanaryDeps`) has length 2 — applyCanaryDecision was invoked on BOTH the first AND the second reconcile. Also assert neither reconcile took the bootstrap path (the activation result reason_code is not 'watcher' / the activation_status reflects a canary decision: 'activated' promoted, 'preserved', 'rolled_back', or 'recovery_required' — NOT the bootstrap `reason:'watcher'` activation). The test MUST fail before the source fix (currently the 2nd reconcile skips the recovery block, knownGood stays null, the bootstrap path runs, and canaryDecision is NOT called on the 2nd reconcile, so canaryCalls.length === 1).

    GREEN — surgical two-line fix to src/registry/watcher.mjs:
    1. Reset `recovered` per reconcile call: move the `let recovered = false;` declaration from factory scope (line 304, OUTSIDE the reconcile function at line 306) to the TOP of the reconcile function body (inside reconcile, as the first statement after `const reconcile = async ({ diff }) => {`). Equivalently, leave the declaration at line 304 and add `recovered = false;` as the first line inside reconcile — either form makes each reconcile re-evaluate recovery state from scratch. The intended invariant: on EVERY reconcile, the recovery block at line 338 (`if (!recovered && active.authority_status !== 'empty')`) is entered when active.authority_status !== 'empty', regardless of prior reconcile outcomes. Do NOT change any other use of `recovered`.
    2. Fix line 337: replace the line-337 ternary expression whose both branches evaluate to null (the dead-code form identified in 20-VERIFICATION.md gaps[0]) with `let knownGood = null;`. The both-branches-null ternary is dead code — knownGood is set correctly inside the recovery block (line 343 `knownGood = recoveryResult.version_id || null;` on healthy/recovered, line 348 `knownGood = null;` on no_valid_history). The initializer's only job is to start at null; the recovery block populates it. Do NOT change any other line that sets knownGood.
<!-- planner-discipline-allow: authority_status === 'empty' ? null : null -->

    Do NOT refactor adjacent watcher code. Do NOT touch src/context/prompt-route.mjs (the hot path). Do NOT change the test_mode bypass at line 363. Do NOT change the canary path body (lines 386-480). The fix is exactly: (a) reset `recovered` per reconcile, (b) fix the line-337 initializer, (c) add the multi-reconcile regression test. Nothing else.

    Reference existing code: the `makeCanaryDeps` / `makeEvidenceStore` / `createTestRegistryReconciler` fixtures in tests/router.watcher-canary-trigger.test.mjs (lines 32-120) already inject the canaryDecision spy; the new test reuses them verbatim and only adds a second `await reconciler.reconcile({ diff })` call plus the length-2 assertion.
    Gap reason: CR-01 (20-VERIFICATION.md gaps[0]) — `recovered` flag persists across reconciles; after the first successful recovery sets `recovered=true`, every subsequent reconcile skips the recovery block, knownGood stays null, and the bootstrap path bypasses applyCanaryDecision.
  </action>
  <verify>
    <automated>node --test tests/router.watcher-canary-trigger.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/router.watcher-canary-trigger.test.mjs` exits 0 (all tests pass, including the new multi-reconcile Test 8 and the existing 7 single-reconcile tests).
    - The line-337 dead-code ternary (both branches returning null) is removed from src/registry/watcher.mjs — verified by reading the source (replaced with a plain null initializer).
    - `grep -v '^#' src/registry/watcher.mjs | grep -c "let recovered = false;"` matches the moved/reset form: either the declaration is inside the reconcile function body, OR a `recovered = false;` reset statement appears at the top of reconcile (verified by reading the source — the declaration is no longer at factory scope outside reconcile).
    - The new multi-reconcile test asserts `canaryCalls.length === 2` (applyCanaryDecision invoked on BOTH reconcile calls).
    - The new multi-reconcile test asserts the 2nd reconcile does NOT take the bootstrap path (activation is NOT the `reason:'watcher'` bootstrap activation — it is a canary decision: promoted/preserved/rolled_back/recovery_required).
    - `grep -v '^#' src/context/prompt-route.mjs | grep -c 'canary\|evidence\|telemetry-bridge\|buildCandidateCalibrationRoute'` = 0 (hot path untouched).
    - `node --test tests/router.evolution-canary.test.mjs tests/router.compiled-evolution.test.mjs` exits 0 (no regression in the existing canary/evolution suites).
  </acceptance_criteria>
  <done>
    CR-01 closed: the watcher's canary path runs on EVERY eligible reconcile with known-good + sufficient evidence, not just the first. The multi-reconcile regression test passes, asserting applyCanaryDecision is invoked on both reconcile() calls. The line-304 `recovered` flag is reset per reconcile, and the line-337 both-branches-null ternary is replaced with a plain `let knownGood = null;` initializer. The hot path is untouched and existing single-reconcile tests still pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| watcher reconcile -> activate.mjs (REGISTRY_PUBLICATION) | Publication mutation authority — the canary path delegates exclusively through applyCanaryDecision -> REGISTRY_PUBLICATION -> activate.mjs. No new boundary introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-04-01 | Tampering | src/registry/watcher.mjs `recovered` flag | high | mitigate | Reset `recovered = false` per reconcile call so the recovery block (and thus the canary path with its 6 REQUIRED_GATES + evidence sufficiency gate) runs on EVERY eligible reconcile. The current bug lets the bootstrap path bypass applyCanaryDecision after the first reconcile — a safety-gate bypass. Fixing it RESTORES the safety gates in production. |
| T-20-04-02 | Information Disclosure | src/registry/watcher.mjs knownGood initializer | low | accept | The line-337 both-branches-null ternary is dead code, not an info-disclosure vector; replacing it with `let knownGood = null;` is a clarity fix. No secret/PII surface. |

**Net security impact: THREAT REDUCTION.** This plan closes an existing safety-gate bypass (CR-01): after the fix, the 6 REQUIRED_GATES, the evidence sufficiency gate, the D-05 demonstrated_benefit derivation, and the REGISTRY_PUBLICATION-only mutation constraint are reachable in production on every eligible reconcile, not just the first. No NEW attack surface is introduced — the fix is internal control-flow correctness. The hot path src/context/prompt-route.mjs is not touched.
</threat_model>

## Artifacts this phase produces

- **`recovered` reset (src/registry/watcher.mjs)** — the `let recovered = false;` declaration moves from factory scope (line 304) into the reconcile function body (or a `recovered = false;` reset is added at the top of reconcile), so each reconcile re-evaluates recovery state.
- **knownGood initializer fix (src/registry/watcher.mjs line ~337)** — `let knownGood = active.authority_status === 'empty' ? null : null;` replaced with `let knownGood = null;` (dead-code ternary removed; the recovery block at line ~343 populates knownGood correctly).
- **Multi-reconcile regression test (tests/router.watcher-canary-trigger.test.mjs)** — a new test (e.g. "Test 8") that calls `reconcile()` TWICE on the same reconciler with the same eligible candidate + known-good + sufficient evidence and asserts the `canaryDecision` spy is invoked on BOTH calls (canaryCalls.length === 2) and the 2nd reconcile does not take the bootstrap path.
- No new exports, no new files, no new dependencies. The fix touches exactly 2 lines of production code + 1 new test case.

<verification>
- `node --test tests/router.watcher-canary-trigger.test.mjs` exits 0 (new multi-reconcile Test 8 + existing 7 tests pass).
- `grep -c "authority_status === 'empty' ? null : null" src/registry/watcher.mjs` = 0.
- `grep -v '^#' src/registry/watcher.mjs | grep -c "let recovered = false;"` — the declaration is inside reconcile OR a `recovered = false;` reset appears at the top of reconcile (verified by reading the source — no longer at factory scope).
- `grep -v '^#' src/context/prompt-route.mjs | grep -c 'canary\|evidence\|telemetry-bridge\|buildCandidateCalibrationRoute'` = 0 (hot path untouched).
- `node --test tests/router.evolution-canary.test.mjs tests/router.compiled-evolution.test.mjs` exits 0 (no regression).
</verification>

<success_criteria>
CR-01 closed: the watcher's production canary trigger runs on EVERY eligible reconcile (not just the first), verified by a multi-reconcile regression test asserting applyCanaryDecision is invoked on both reconcile() calls. The `recovered` flag is reset per reconcile and the line-337 knownGood initializer is fixed. No adjacent code is refactored; the hot path is untouched; existing single-reconcile tests still pass.
</success_criteria>

<output>
Create `.planning/phases/20-close-gap-evo-05-add-production-trigger-for-canary-controlle/20-04-SUMMARY.md` when done
</output>
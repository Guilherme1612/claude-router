---
phase: 29-mode-map-curation-and-signal-patterns-expansion
plan: 04
type: execute
wave: 4
depends_on: [29-03]
files_modified:
  - calibration-tasks.json
  - router.calibrate.mjs
  - tests/router.calibration-thresholds.test.mjs
  - /Users/guilherme/.claude/router/mode-map.json
  - coverage-report.json
autonomous: true
requirements: [MAP-01, MAP-02, MAP-03, SIG-04]

must_haves:
  truths:
    - "Threshold selection uses labeled expanded-route positives, negatives, and near-collisions evaluated through production inspection."
    - "No selected threshold set creates a wrong high-confidence route."
    - "The chosen constants and leave-one-out sensitivity are deterministic, evidence-backed, and written to the installed map only after selection."
    - "The complete serial suite, performance gate, strict coverage rebuild, and installed-runtime dry-run pass after calibration."
  artifacts:
    - path: "calibration-tasks.json"
      provides: "Labeled Phase 29 expanded-route fixtures"
    - path: "router.calibrate.mjs"
      provides: "Pure constrained grid selection and sensitivity report"
      exports: ["dryRun", "selectThresholds", "leaveOneOutThresholds"]
    - path: "tests/router.calibration-thresholds.test.mjs"
      provides: "Selection and real-corpus regression evidence"
    - path: "/Users/guilherme/.claude/router/mode-map.json"
      provides: "Evidence-derived T_high, T_low, and M constants"
    - path: "coverage-report.json"
      provides: "Regenerated post-curation coverage evidence"
  key_links:
    - from: "router.calibrate.mjs"
      to: "/Users/guilherme/.claude/hooks/router.mjs"
      via: "dryRun delegates to inspectDecision with supplied fixture objects"
      pattern: "inspectDecision"
    - from: "calibration-tasks.json"
      to: "selectThresholds"
      via: "Phase 29 labeled routing subset only"
      pattern: "phase29"
    - from: "router.calibrate.mjs"
      to: "/Users/guilherme/.claude/router/mode-map.json"
      via: "reported selected constants copied only after deterministic test evidence"
      pattern: "T_high|T_low|M"
---

<objective>
Re-derive and publish routing thresholds from the completed expanded route corpus, then run the phase release gates.

Purpose: Ensure added lexical coverage improves correct suggestions without turning near-collisions into unsafe high-confidence routes.
Output: Labeled fixtures, deterministic grid/sensitivity tooling, evidence-derived constants, regenerated coverage evidence, and full verification.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-mode-map-curation-and-signal-patterns-expansion/29-RESEARCH.md
@.planning/phases/29-mode-map-curation-and-signal-patterns-expansion/29-VALIDATION.md
@.planning/phases/29-mode-map-curation-and-signal-patterns-expansion/29-03-SUMMARY.md
@calibration-tasks.json
@router.calibrate.mjs
@tests/router.calibration-thresholds.test.mjs
@/Users/guilherme/.claude/router/mode-map.json
@src/evolution/perf-measure.mjs
@build-manifest.mjs

<interfaces>
Phase 29 fixtures are distinctly labeled so mapper/evolution/codebase fixtures are not treated as threshold samples.
`dryRun(prompt, manifest, modeMap, cwd, weights)` must actually pass supplied objects through `inspectDecision`.
Candidate values come from observed score/margin breakpoints plus current values; selection never mutates the live map.
The labeled corpus includes deterministic low/high score boundary records and near-tie margin records so the objective identifies T_high, T_low, and M independently. Numerically unchanged values are valid only when the selector chooses them.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add expanded labeled calibration evidence</name>
  <files>calibration-tasks.json, tests/router.calibration-thresholds.test.mjs</files>
  <behavior>
    - Phase 29 fixtures include required-route positives, pass-through negatives, sibling near-collisions, and deterministic boundary records around all three confidence constants.
    - Every fixture has an explicit expected route/status/tier contract and a Phase 29 classification.
    - Fixture prompts use implicit outcome language rather than target names.
    - Existing original, codebase, evolution, coverage, and mapper fixture counts/contracts remain intact.
  </behavior>
  <action>Append the smallest labeled set that covers all 18 curated targets plus representative family-level hard negatives and near-collisions already proven by the curation suite, following `calibration-tasks.json`'s existing explicit `right` records and `tests/router.calibration-coverage.test.mjs`'s cluster accounting. Add deterministic labeled boundary fixtures whose production-path score/margin evidence distinguishes low from medium, medium from high, and tied from decisive, so T_low, T_high, and M are each selectable rather than defaulted. Do not duplicate every unit prompt if one record supplies the needed evidence. Keep Phase 29 records distinct from original/codebase/evolution/mapping classes and expand pass-threshold accounting deliberately.</action>
  <verify>
    <automated>rtk node --test tests/router.calibration-thresholds.test.mjs tests/router.calibrate.test.mjs tests/router.calibration-coverage.test.mjs tests/router.calibration-evolution.test.mjs</automated>
  </verify>
  <done>The expanded corpus has explicit positive/negative/near-collision labels without reclassifying legacy fixtures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Select thresholds through production decisions</name>
  <files>router.calibrate.mjs, tests/router.calibration-thresholds.test.mjs</files>
  <behavior>
    - `dryRun` consumes its supplied manifest and mode map.
    - Grid selection rejects wrong-high candidates and follows the deterministic objective.
    - CLI prints selected values, affected-sample counts, and leave-one-out ranges/frequencies.
    - Repeated runs return identical selection and report.
  </behavior>
  <action>Fix the currently unused `manifest`/`modeMap` dry-run arguments by passing the Plan 02 object seam to `inspectDecision`. Following `router.calibrate.mjs`'s existing `dryRun` adapter/fixture loop and `tests/router.calibrate-importable.test.mjs`'s pure named-export pattern, add small exported candidate enumeration, evaluation, selection, and leave-one-out functions. Evaluate only labeled routing records, not evolution/mapper branches. Use exhaustive observed breakpoints plus current constants and select lexicographically: zero wrong-high routes, maximum exact-route correctness, maximum expected-tier correctness across low/medium/high boundary labels, maximum correct-high routes, minimum misses, then smallest numeric tuple for a deterministic tie. No isotonic, Platt, probability model, dependency, or duplicated confidence/scoring implementation. Require all three selected constants to be supported by at least one boundary-changing labeled record.</action>
  <verify>
    <automated>rtk node --test tests/router.calibration-thresholds.test.mjs tests/router.calibrate-importable.test.mjs && rtk node router.calibrate.mjs</automated>
  </verify>
  <done>The CLI deterministically recommends a zero-wrong-high threshold tuple with sensitivity and affected-sample evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Publish evidence-derived constants and close phase gates</name>
  <files>/Users/guilherme/.claude/router/mode-map.json, coverage-report.json</files>
  <action>Copy only the full T_high/T_low/M tuple selected by Task 2 into the installed map; unchanged numeric values are acceptable only when the boundary fixtures and deterministic objective select them. Regenerate `coverage-report.json` only through `build-manifest.mjs --strict-coverage`; never hand-edit generated evidence. Confirm the 18 newly mapped targets leave the unacknowledged-gap set, unrelated baseline policy is unchanged, the Phase 27 latency/size gates pass, and installed CLI inspection selects representative lifecycle and design routes. Preserve any unrelated dirty-worktree files.</action>
  <verify>
    <automated>rtk node --test tests/router.mode-map-v3.test.mjs tests/router.mode-map-curation.test.mjs tests/router.calibration-thresholds.test.mjs tests/router.route-targets.test.mjs tests/router.coverage-audit.test.mjs && rtk node router.calibrate.mjs && rtk node --test tests/router.performance.test.mjs tests/router.mode-map-size.test.mjs && rtk node build-manifest.mjs --strict-coverage && rtk node --test --test-concurrency=1 tests/*.test.mjs && rtk node /Users/guilherme/.claude/hooks/router.mjs inspect --json \"prepare the branch and pull request for release\" | rtk node -e 'const fs=require(\"node:fs\"),assert=require(\"node:assert/strict\"),j=JSON.parse(fs.readFileSync(0,\"utf8\")); assert.equal(j.selected_route?.id ?? j.selected_route,\"gsd-ship\")' && rtk node /Users/guilherme/.claude/hooks/router.mjs inspect --json \"turn this screenshot into a working responsive page\" | rtk node -e 'const fs=require(\"node:fs\"),assert=require(\"node:assert/strict\"),j=JSON.parse(fs.readFileSync(0,\"utf8\")); assert.equal(j.selected_route?.id ?? j.selected_route,\"image-to-code\")'</automated>
  </verify>
  <done>The installed full tuple matches deterministic boundary evidence, coverage is regenerated, no wrong-high route exists, performance/size gates and serial full suite are green, and installed CLI JSON inspection shows `gsd-ship` plus `image-to-code` as the representative selected routes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| labeled fixtures → threshold selector | Fixture classification and expected outcomes determine confidence policy. |
| selector report → installed mode-map | Offline evidence changes always-on routing constants. |
| generated manifest/report → publication gate | Curated mappings must not hide unrelated coverage failures. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-03 | Tampering | threshold objective | high | mitigate | Reject any wrong-high candidate, deterministic tie-break, leave-one-out sensitivity, immutable input fixtures. |
| T-29-07 | Repudiation | threshold publication | medium | mitigate | CLI prints selected tuple, affected counts, sensitivity, and executor records exact output in summary before copying constants. |
| T-29-08 | Denial of Service | expanded routing | high | mitigate | Run Phase 27 p95/max latency and 30KB size gates after the final tuple/map. |
| T-29-09 | Tampering | generated coverage | high | mitigate | Regenerate through strict builder and retain unrelated forward/reverse diagnostics and baseline policy. |
</threat_model>

<verification>
Quick gate: `rtk node --test tests/router.mode-map-v3.test.mjs tests/router.mode-map-curation.test.mjs tests/router.calibration-thresholds.test.mjs tests/router.route-targets.test.mjs tests/router.coverage-audit.test.mjs`.
Calibration gate: `rtk node router.calibrate.mjs`.
Release gate: `rtk node --test tests/router.performance.test.mjs tests/router.mode-map-size.test.mjs && rtk node build-manifest.mjs --strict-coverage && rtk node --test --test-concurrency=1 tests/*.test.mjs`.
</verification>

<success_criteria>
- SIG-04 thresholds are selected from the final expanded labeled corpus, not assumed or copied from v1.3.
- No candidate with a wrong high-confidence route can be published.
- Sensitivity and zero-affected constants are reported honestly.
- Strict coverage, performance, size, focused, and serial full-suite gates pass.
</success_criteria>

<output>
Create `.planning/phases/29-mode-map-curation-and-signal-patterns-expansion/29-04-SUMMARY.md` when done.
</output>

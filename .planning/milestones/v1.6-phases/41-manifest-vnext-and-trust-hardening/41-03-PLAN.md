---
phase: 41-manifest-vnext-and-trust-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - 41-01
  - 41-02
files_modified:
  - src/registry/eligibility.mjs
  - src/registry/schema.mjs
  - src/adapters/dispatch/contract.mjs
  - tests/router.trust-quarantine.test.mjs
  - tests/router.contract-eligibility.test.mjs
autonomous: true
requirements:
  - TRUST-05
user_setup: []

estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Quarantined capabilities have eligibility.quarantined === true and eligibility.quarantine_reasons is a non-empty array of attributable reason codes"
    - "Independent valid fallback capabilities with the same semantic_type but different stableCapabilityId remain eligible when a sibling is quarantined"
    - "Injection-bearing capabilities (unsafe content in purpose/description) are quarantined"
    - "Scope-escaping capabilities are quarantined"
    - "RECEIPT_STATES includes 'quarantined' and 'blocked' states for blocked receipts"
    - "Quarantine is per-capability (per-record), not per-route or per-semantic-slot"
  artifacts:
    - src/registry/eligibility.mjs (quarantined + quarantine_reasons disposition)
    - src/registry/schema.mjs (validateEligibility extended for quarantine)
    - src/adapters/dispatch/contract.mjs (RECEIPT_STATES extended with quarantined + blocked)
    - tests/router.trust-quarantine.test.mjs
  key_links:
    - "eligibility.mjs::evaluateEligibility ↔ schema.mjs::validateEligibility (gate-set + disposition invariant, atomic)"
    - "eligibility.mjs::quarantined → dispatch/contract.mjs::RECEIPT_STATES (quarantined receipt state)"
    - "contract.mjs::envelope() → eligibility quarantine detection (injection-bearing evidence triggers quarantine)"
---

<objective>
Add a per-capability quarantine disposition (TRUST-05) to the eligibility layer so invalid, ambiguous, stale, unavailable, injection-bearing, or scope-escaping capabilities are blocked with attributable reason codes while independent valid fallbacks remain eligible.

Purpose: Close the trust gap so a single bad capability cannot disable an entire capability class — quarantine is per-record, and separately valid fallbacks with the same semantic_type stay eligible.
Output: Extended eligibility.mjs with quarantined + quarantine_reasons; extended schema.mjs validation; extended RECEIPT_STATES; new quarantine test file.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-manifest-vnext-and-trust-hardening/41-RESEARCH.md
@.planning/phases/41-manifest-vnext-and-trust-hardening/41-PATTERNS.md
@.planning/phases/41-manifest-vnext-and-trust-hardening/41-01-SUMMARY.md
@.planning/phases/41-manifest-vnext-and-trust-hardening/41-02-SUMMARY.md

@src/registry/eligibility.mjs
@src/registry/schema.mjs
@src/registry/contract.mjs
@src/adapters/dispatch/contract.mjs
@tests/router.contract-eligibility.test.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TRUST-05 — add quarantined disposition to eligibility + schema validation + RECEIPT_STATES extension</name>
  <files>
    src/registry/eligibility.mjs
    src/registry/schema.mjs
    src/adapters/dispatch/contract.mjs
    tests/router.trust-quarantine.test.mjs
  </files>
  <read_first>
    src/registry/eligibility.mjs (ELIGIBILITY_GATES at line 5-16, evaluateEligibility at line 145-190, per-gate state functions at line 124-138, return shape at line 182-190, DISPATCH_FIELDS at line 18-30)
    src/registry/schema.mjs (ELIGIBILITY_GATES at line 49, validateEligibility at line 191-225, gate-set check at line 203-208, disposition validation at line 197-201, stableStringify at line 339-341)
    src/adapters/dispatch/contract.mjs (RECEIPT_STATES at line 33-35, buildReceipt at line 50-74, validateInvocation + preDispatchGate added in Plan 02)
    src/registry/contract.mjs (hasUnsafeAuthoredContent at line 237-249 — injection detection pattern, envelope() evidence_class from Plan 01)
    tests/router.contract-eligibility.test.mjs (safeRecord at line 13-23, withField at line 38-49, evaluate harness at line 26-36)
  </read_first>
  <behavior>
    - Test 1: A quarantined record has eligibility.quarantined === true and eligibility.quarantine_reasons is a non-empty array of reason codes
    - Test 2: A quarantined record has eligibility.eligible === false and eligibility.recommendation_only === true
    - Test 3: An independent valid fallback with the same semantic_type but different stableCapabilityId remains eligible: true when its sibling is quarantined
    - Test 4: An injection-bearing capability (unsafe content detected by hasUnsafeAuthoredContent pattern) is quarantined with reason_code 'injection_bearing'
    - Test 5: A scope-escaping capability (runtime mismatch or path escape in contract) is quarantined with reason_code 'scope_escaping'
    - Test 6: A valid non-quarantined record has eligibility.quarantined === false and eligibility.quarantine_reasons === []
    - Test 7: validateEligibility in schema.mjs accepts quarantined: true with quarantine_reasons: string[] without throwing
    - Test 8: RECEIPT_STATES includes 'quarantined' ('blocked' already added by Plan 02)
    - Test 9: Existing eligibility tests remain green (gate set unchanged if quarantine is a disposition, not a gate)
  </behavior>
  <action>
    1. Extend evaluateEligibility in src/registry/eligibility.mjs (line 145-190) to add a quarantined disposition. Per RESEARCH Pattern 5 and Pitfall 5: quarantine is per-capability (per-record), NOT per-route. Add two fields to the return object (line 182-190): `quarantined: boolean` (default false) and `quarantine_reasons: string[]` (default []). A quarantined record has eligible: false, recommendation_only: true, quarantined: true, and quarantine_reasons with reason codes.

    2. Add quarantine detection logic in evaluateEligibility. Check for conditions that trigger quarantine (set quarantined=true and push reason codes):
       a. injection_bearing: if the record's contract has unsafe authored content detected (reuse the hasUnsafeAuthoredContent pattern from contract.mjs:237-249 — check for secrets, paths, control chars in evidence). If detected, push 'injection_bearing' to quarantine_reasons.
       b. scope_escaping: if the record's contract has invocation_kind.value that does not match the record's semantic_type (e.g., invocation_kind='none' but semantic_type='skill', or invocation_kind declares a runtime different from the record's runtime). Concretely: check contract.fields.invocation_kind.value !== record.semantic_type AND contract.fields.invocation_kind.value !== 'none' — a mismatch means the capability declares an invocation shape outside its declared scope. Push 'scope_escaping'.
       c. stale_quarantine: if the record's contract freshness is 'stale' beyond a threshold. Push 'stale_unavailable'.
       Do NOT add a new gate to ELIGIBILITY_GATES unless the quarantine check is structurally a gate. If it is a disposition (computed from existing gate results + contract inspection), no gate-set change is needed (avoids the Pitfall 1 gate-set invariant).

    3. ATOMIC INVARIANT (Pitfall 1): if a new gate IS added to ELIGIBILITY_GATES, update BOTH eligibility.mjs:5-16 AND schema.mjs:49 atomically. If quarantine is a disposition (not a gate), no gate-set change is needed — just extend validateEligibility in schema.mjs to accept the new fields.

    4. Extend validateEligibility in src/registry/schema.mjs (line 191-225) to accept quarantined and quarantine_reasons:
       a. Add validation: quarantined must be a boolean. quarantine_reasons must be an array of strings matching the reason-token pattern /^[a-z0-9][a-z0-9._-]{0,63}$/i.
       b. Adjust the disposition check at line 197-201: a quarantined record has eligible===false and recommendation_only===true. These values are unequal, so the existing check `eligibility.eligible === eligibility.recommendation_only` (which rejects when they are equal) passes for quarantined records — no adjustment to the existing assertion is needed. Just add the new quarantined/quarantine_reasons field validation alongside it.

    5. Extend RECEIPT_STATES in src/adapters/dispatch/contract.mjs (line 33-35) to add 'quarantined'. Note: 'blocked' was already added in Plan 02 Task 1 (step 1b) when validateInvocation first emitted blocked receipts. Per RESEARCH Assumption A5: new states are additive; existing Phase 38 tests check exact states — verify they still pass (the frozen array gains 1 entry here, on top of the 'blocked' entry from Plan 02, existing states unchanged in position 0-5).

    6. Create tests/router.trust-quarantine.test.mjs: import evaluateEligibility from ../src/registry/eligibility.mjs, buildCapabilityContract from ../src/registry/contract.mjs, contractEvidence + buildClaudeHeavyProfile from ./helpers/inventory-fixture.mjs. Copy the safeRecord + withField harness from contract-eligibility.test.mjs:13-49. Write tests matching the behavior block. For the fallback test: create two records with the same semantic_type but different stableCapabilityId (use overrides), quarantine one, and assert the other remains eligible: true.

    NEVER quarantine at the route level (Pitfall 5). Quarantine sets eligibility.quarantined on the specific record. Other records with the same semantic_type remain eligible independently.
  </action>
  <verify>
    <automated>rtk node --test tests/router.trust-quarantine.test.mjs tests/router.contract-eligibility.test.mjs tests/router.contracts.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - src/registry/eligibility.mjs evaluateEligibility return object contains quarantined (boolean) and quarantine_reasons (array) keys
    - A quarantined record has quarantined===true, eligible===false, recommendation_only===true, quarantine_reasons is non-empty
    - A valid non-quarantined record has quarantined===false, quarantine_reasons===[] (or empty array)
    - An injection-bearing capability (unsafe content in contract evidence) triggers quarantined=true with 'injection_bearing' in quarantine_reasons
    - src/registry/schema.mjs validateEligibility accepts quarantined and quarantine_reasons fields without throwing
    - src/adapters/dispatch/contract.mjs RECEIPT_STATES array contains 'quarantined' (grep "'quarantined'" returns a match; 'blocked' already added by Plan 02)
    - rtk node --test tests/router.trust-quarantine.test.mjs exits 0
    - rtk node --test tests/router.contract-eligibility.test.mjs exits 0 (regression)
    - rtk node --test tests/router.contracts.test.mjs exits 0 (regression — field-set invariant holds)
  </acceptance_criteria>
  <done>
    evaluateEligibility returns a quarantined disposition with attributable reason codes. validateEligibility accepts the new fields. RECEIPT_STATES includes quarantined and blocked states. All existing eligibility and contract tests remain green.
  </done>
  <reversibility rating="costly" rationale="Adding a quarantine disposition to eligibility is additive but removing it after deployment breaks persisted eligibility records that contain quarantined fields. Adding is safe; removing post-ship requires a schema migration." />
</task>

<task type="auto" tdd="true">
  <name>Task 2: TRUST-05 — wire fallback eligibility verification and quarantine integration with dispatch gate</name>
  <files>
    src/registry/eligibility.mjs
    tests/router.trust-quarantine.test.mjs
    tests/router.contract-eligibility.test.mjs
  </files>
  <read_first>
    src/registry/eligibility.mjs (evaluateEligibility at line 145-190 — with quarantine disposition from Task 1, per-gate state functions at line 124-138)
    src/adapters/dispatch/claude.mjs (invokeImpl — preDispatchGate from Plan 02, recommendationOnly at line 241-265)
    tests/router.contract-eligibility.test.mjs (evaluate harness at line 26-36, safeRecord at line 13-23, withField at line 38-49)
    src/registry/contract.mjs (envelope() evidence_class from Plan 01, hasUnsafeAuthoredContent at line 237-249)
  </read_first>
  <behavior>
    - Test 1: Two records with the same semantic_type but different stableCapabilityId: quarantine record A, assert record B has eligible: true, quarantined: false (Pitfall 5 backstop)
    - Test 2: Quarantine does not propagate to other records in the same evaluateEligibility call (evaluateEligibility operates per-record)
    - Test 3: A quarantined record's quarantine_reasons are inspectable via the eligibility object (string array of reason tokens matching /^[a-z0-9][a-z0-9._-]{0,63}$/i)
    - Test 4: A capability that was eligible before quarantine but has injection-bearing evidence added via overlay becomes quarantined after re-evaluation
    - Test 5: Existing contract-eligibility tests remain green (no quarantine on safe records)
  </behavior>
  <action>
    1. Verify that evaluateEligibility operates per-record: the quarantine flag on one record does not affect another record's eligibility. If evaluateEligibility takes a single record (not a list), this is structurally guaranteed. If it takes a records array, ensure the quarantine computation only reads the current record's contract, not siblings'. Per Pitfall 5: quarantining one skill must NOT make all skills with the same name ineligible.

    2. Add a helper function in eligibility.mjs (if not already present in Task 1): isQuarantined(record) returning boolean — checks whether the record's contract has injection-bearing content, scope-escaping declarations, or stale-unavailable freshness. This is a pure function that evaluateEligibility calls to set the quarantined field. Export it for testability.

    3. Extend tests/router.trust-quarantine.test.mjs with the fallback eligibility test: create two safeRecord instances with the same semantic_type but different stableCapabilityId (override the name/id). Quarantine one by injecting injection-bearing evidence (use withField to add unsafe content to purpose or description). Call evaluateEligibility on both independently. Assert the quarantined record has eligible: false, quarantined: true, and the non-quarantined sibling has eligible: true, quarantined: false.

    4. Extend tests/router.contract-eligibility.test.mjs: add a test asserting that a safe record (no injection, no scope escape, fresh) has quarantined: false and quarantine_reasons: []. Do NOT modify or weaken existing assertions.

    5. Verify the quarantine reason tokens match the reason-code convention: /^[a-z0-9][a-z0-9._-]{0,63}$/i (per PATTERNS.md Reason-Code Convention). New codes: 'injection_bearing', 'scope_escaping', 'stale_unavailable'. All must pass the reasonToken validator.
  </action>
  <verify>
    <automated>rtk node --test tests/router.trust-quarantine.test.mjs tests/router.contract-eligibility.test.mjs tests/router.contracts.test.mjs tests/router.dispatch-integration.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Two records with the same semantic_type but different stableCapabilityId: one quarantined, the other eligible: true (Pitfall 5 backstop test passes)
    - evaluateEligibility does not propagate quarantine across records (quarantine is per-record)
    - isQuarantined is exported from eligibility.mjs if added (grep "export function isQuarantined" or "export const isQuarantined" returns a match if exported)
    - quarantine_reasons tokens match /^[a-z0-9][a-z0-9._-]{0,63}$/i (all reason codes pass the reason-token pattern)
    - rtk node --test tests/router.trust-quarantine.test.mjs exits 0
    - rtk node --test tests/router.contract-eligibility.test.mjs exits 0 (regression — safe records not quarantined)
    - rtk node --test tests/router.dispatch-integration.test.mjs exits 0 (regression — dispatch path not broken)
  </acceptance_criteria>
  <done>
    Quarantine is per-capability: a quarantined record does not affect sibling fallbacks with the same semantic_type. Quarantine reason codes are attributable and match the reason-token convention. Safe records remain unquarantined. All existing eligibility, contract, and dispatch tests remain green.
  </done>
  <reversibility rating="reversible" rationale="Test additions + helper function. No structural change to the eligibility evaluator beyond Task 1. Removing the tests and helper restores prior state." />
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contract evidence → eligibility quarantine | Injection-bearing or scope-escaping contract evidence triggers quarantine at eligibility evaluation time |
| quarantined record → dispatch | Quarantined records are blocked from dispatch; independent valid fallbacks remain eligible |
| eligibility → receipt | Quarantined/blocked states produce attributable receipts with reason codes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-11 | Elevation of Privilege | eligibility.mjs / scope-escaping capability | high | mitigate | TRUST-05: scope-escaping capabilities (runtime mismatch, path escape in contract) are quarantined with 'scope_escaping' reason code; evaluateEligibility sets quarantined=true, blocking dispatch |
| T-41-12 | Tampering | eligibility.mjs / injection-bearing capability | high | mitigate | TRUST-05: injection-bearing capabilities (unsafe content in purpose/description detected via hasUnsafeAuthoredContent pattern) are quarantined with 'injection_bearing' reason code; quarantined records have eligible=false |
| T-41-13 | Denial of Service | eligibility.mjs / quarantine breaking fallback | medium | mitigate | TRUST-05: quarantine is per-record (per-capability), not per-route or per-semantic-slot (Pitfall 5); independent valid fallbacks with same semantic_type but different stableCapabilityId remain eligible |
| T-41-14 | Repudiation | dispatch/contract.mjs / quarantine receipts | medium | mitigate | TRUST-05: quarantined invocations produce receipts with state 'quarantined' + quarantine_reasons; blocked invocations produce receipts with state 'blocked' + reason_codes; all states are in RECEIPT_STATES and are attributable |
| T-41-SC | Tampering | npm/pip installs | low | accept | Phase 41 installs no external packages — stdlib-only, zero supply-chain surface |
</threat_model>

<verification>
- Quarantine is per-record: a quarantined capability does not affect sibling fallbacks with the same semantic_type (Pitfall 5 backstop)
- Quarantined records have eligible=false, recommendation_only=true, quarantined=true, quarantine_reasons (non-empty array)
- Injection-bearing and scope-escaping capabilities are quarantined with attributable reason codes
- RECEIPT_STATES includes quarantined and blocked states (additive to existing 6 states)
- validateEligibility accepts the new quarantine fields without breaking the gate-set invariant
- All existing eligibility, contract, inspection, and dispatch tests remain green
</verification>

<success_criteria>
- TRUST-05: Invalid, ambiguous, stale, unavailable, injection-bearing, or scope-escaping capabilities are blocked or quarantined with attributable reasons while a separately valid compatible fallback remains eligible
- Quarantine is per-capability, not per-route (Pitfall 5 backstop verified)
- RECEIPT_STATES extended with quarantined + blocked states (additive, existing states unchanged)
- All existing tests remain green (eligibility gate set, contract field-set, dispatch integration)
</success_criteria>

<output>
Create `.planning/phases/41-manifest-vnext-and-trust-hardening/41-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- New eligibility disposition fields on `evaluateEligibility` return: `quarantined` (boolean), `quarantine_reasons` (string[])
- New `RECEIPT_STATES` entry: `quarantined` (1 new entry in the frozen array, additive to existing 6 + 'blocked' from Plan 02)
- New quarantine reason tokens: `injection_bearing`, `scope_escaping`, `stale_unavailable`
- New exported helper (if added): `isQuarantined(record)` from eligibility.mjs
- `validateEligibility` in schema.mjs extended to accept `quarantined` + `quarantine_reasons` fields
- New test file: `tests/router.trust-quarantine.test.mjs`
- Extended test file: `tests/router.contract-eligibility.test.mjs` (quarantine backstop assertions)
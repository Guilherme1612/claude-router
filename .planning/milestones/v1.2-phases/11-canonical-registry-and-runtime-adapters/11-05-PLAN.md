---
phase: 11-canonical-registry-and-runtime-adapters
plan: "05"
type: execute
wave: 4
depends_on:
  - 11-03
files_modified:
  - src/registry/build.mjs
  - tests/router.registry-build.test.mjs
  - src/lifecycle/router-lifecycle.mjs
  - tests/router.lifecycle.test.mjs
autonomous: true
gap_closure: true
requirements:
  - REG-01
  - REG-02
  - ADP-01
  - ADP-02
must_haves:
  truths:
    - Every material disagreement between authoritatively linked runtime variants is represented by a deterministic typed conflict with field, sources, competing values or fingerprints, type, and severity.
    - Authoritative identity remains merged under D-02 while native variant data stays separate and disagreements never silently inherit the first record.
    - Any failure after installation mutation restores settings, replaced owned files, newly created files, bindings, marker, candidate/report, and ownership manifest to their exact pre-install state.
    - Successful installation remains idempotent, one-command, inactive, and byte-preserving for unrelated runtime configuration.
  artifacts:
    - path: src/registry/build.mjs
      provides: Complete deterministic material-field conflict synthesis
    - path: tests/router.registry-build.test.mjs
      provides: Linked-variant conflict matrix for F-003
    - path: src/lifecycle/router-lifecycle.mjs
      provides: Transactional restoration of all installer mutations
    - path: tests/router.lifecycle.test.mjs
      provides: Post-mutation failure injection and exact-state rollback proof
  key_links:
    - from: src/registry/build.mjs
      to: src/registry/schema.mjs
      via: Conflict values and records are canonicalized before deterministic comparison and serialization
    - from: tests/router.registry-build.test.mjs
      to: src/registry/build.mjs
      via: Explicitly linked Claude/Codex variants exercise each material field
    - from: src/lifecycle/router-lifecycle.mjs
      to: tests/router.lifecycle.test.mjs
      via: Injected post-mutation readiness failure proves transaction restoration
---

<objective>
Close F-003 and the remaining installer warning by making linked-variant conflicts complete and installation rollback transactional after mutation begins.

Purpose: Preserve D-02/D-14 diagnostic truth and D-18/D-19 one-command safety without crossing Phase 11's read-only candidate and inactive-routing boundary.
Output: Complete conflict synthesis tests/implementation and exact post-mutation rollback tests/implementation.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-canonical-registry-and-runtime-adapters/11-CONTEXT.md
@.planning/phases/11-canonical-registry-and-runtime-adapters/11-RESEARCH.md
@.planning/phases/11-canonical-registry-and-runtime-adapters/11-PATTERNS.md
@.planning/phases/11-canonical-registry-and-runtime-adapters/11-VERIFICATION.md
@.planning/phases/11-canonical-registry-and-runtime-adapters/11-01-SUMMARY.md
@.planning/phases/11-canonical-registry-and-runtime-adapters/11-03-SUMMARY.md
@src/registry/schema.mjs
@src/registry/identity.mjs
@install-router.mjs
@tests/router.settings-diff.test.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Synthesize complete typed linked-variant conflicts</name>
  <files>tests/router.registry-build.test.mjs, src/registry/build.mjs</files>
  <read_first>
    <file>.planning/phases/11-canonical-registry-and-runtime-adapters/11-VERIFICATION.md</file>
    <file>.planning/phases/11-canonical-registry-and-runtime-adapters/11-CONTEXT.md</file>
    <file>tests/router.registry-build.test.mjs</file>
    <file>tests/router.mjs.snapshot</file>
    <file>tests/router.lifecycle.test.mjs</file>
    <file>src/registry/build.mjs</file>
    <file>src/registry/schema.mjs</file>
    <file>src/registry/identity.mjs</file>
  </read_first>
  <behavior>
    - Linked runtime variants remain one canonical record when authoritative identity evidence matches.
    - Material disagreements in name, type, description, lifecycle, dispatchability, invocation, dependencies, scope-compatible metadata, and other canonical merge-owned fields each produce typed conflicts.
    - Every conflict includes the field, deterministic source identities, competing canonical values or stable fingerprints, conflict type, and one allowed severity.
    - Conflict arrays and nested source/value collections serialize identically regardless of discovery or variant order.
    - Native runtime variants and provenance remain intact; a conflict does not overwrite one variant with another or activate either variant.
  </behavior>
  <action>Expand `tests/router.registry-build.test.mjs` with an authoritative-identity fixture matrix that varies each material merge-owned field independently and together, then implement a declarative material-field comparison in `mergeGroup`. Retain the deterministic aggregation conventions from `tests/router.mjs.snapshot`: normalized batch inputs, stable owning-boundary sorting, and byte-stable output independent of traversal order. Retain the explicit temporary-root, isolated filesystem, before/after snapshot, and try/finally cleanup conventions from `tests/router.lifecycle.test.mjs` for registry-build fixtures. Per D-02, keep one canonical ID and preserve every runtime variant even when native metadata differs. Per D-14, emit a conflict object containing field, deterministic sources, competing canonical values or stable fingerprints, conflict type, and severity (`informational`, `dispatch-blocking`, or `build-blocking`). Define severity by field semantics without making Phase 13 quarantine or Phase 14 activation decisions. Compare canonicalized values, deduplicate equivalent conflicts, and sort conflicts/sources/values at their owning boundary per D-16. Do not compare aggregation-owned fields such as provenance, runtime_variants, or existing conflicts as if they were disagreements, and do not silently retain first-record values without recording the disagreement.</action>
  <acceptance_criteria>
    <criterion>Each material disagreement has an executable assertion for field, sources, values or fingerprints, type, and severity.</criterion>
    <criterion>Equal material fields produce no synthetic conflict and duplicate conflict evidence is deduplicated.</criterion>
    <criterion>Variant-order permutations produce exact canonical bytes.</criterion>
    <criterion>Linked variants retain one ID, all provenance, and all native invocation variants without activation.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/router.registry-schema.test.mjs tests/router.registry-build.test.mjs</automated>
  </verify>
  <done>F-003 is closed and D-02/D-14 disagreements are complete, typed, deterministic, and non-activating.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Restore exact pre-install state after post-mutation failure</name>
  <files>tests/router.lifecycle.test.mjs, src/lifecycle/router-lifecycle.mjs</files>
  <read_first>
    <file>.planning/phases/11-canonical-registry-and-runtime-adapters/11-VERIFICATION.md</file>
    <file>tests/router.lifecycle.test.mjs</file>
    <file>tests/router.settings-diff.test.mjs</file>
    <file>src/lifecycle/router-lifecycle.mjs</file>
    <file>install-router.mjs</file>
  </read_first>
  <behavior>
    - A deterministic test-only failure seam triggers after settings, bindings, and owned artifacts have been mutated but before successful return.
    - Fresh-install failure leaves no newly created router, marker, modules, candidate, report, manifest, binding, or empty installer-owned directory.
    - Repair/upgrade failure restores every replaced owned file and the pre-existing manifest byte-for-byte.
    - Existing settings are restored byte-for-byte, including formatting and unrelated fields; absent settings return to absent.
    - Normal install, dry-run, repair, rerun, and uninstall behavior remains unchanged.
  </behavior>
  <action>Add a narrow dependency-injection seam to `installRouter(options)` for deterministic post-mutation readiness failure; it must be inert unless a test supplies it and must not create a production bypass. Before the first mutation, snapshot the exact bytes/existence state of every path the transaction may change, including settings, router, Codex marker, module files, candidate/report, manifest, and affected directories. On any exception after mutation begins, restore replaced bytes atomically, remove only newly created files, restore or remove settings exactly as they existed, and remove newly created empty owned directories. Test both a fresh install and a repair/upgrade with pre-existing owned artifacts and manifest. Preserve D-18 through D-20: the normal one-command path remains idempotent, concise, standard-library-only, inactive, and does not change unrelated Claude/Codex state.</action>
  <acceptance_criteria>
    <criterion>Post-mutation failure is injected only after at least one settings and owned-file mutation occurred.</criterion>
    <criterion>Fresh and repair failure tests compare a complete before/after filesystem-and-settings snapshot byte-for-byte.</criterion>
    <criterion>No pre-existing owned file, manifest, binding, or unrelated setting is lost or changed.</criterion>
    <criterion>All existing lifecycle and settings-diff tests continue to pass.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/router.lifecycle.test.mjs tests/router.settings-diff.test.mjs</automated>
  </verify>
  <done>The fourth Required Gap Closure item is satisfied by exact transactional restoration after injected post-mutation failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Linked adapter records to canonical merge | Authoritative identity permits divergent untrusted native metadata into one record. |
| Installer transaction to runtime filesystem | Owned and unrelated runtime state coexist across a multi-file mutation boundary. |
| Failure injection seam to production lifecycle | Test control must not become a production bypass or hidden behavior switch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-G05-01 | Spoofing | linked-variant merge | high | mitigate | Require existing authoritative identity evidence and report every material disagreement per D-02/D-14. |
| T-11-G05-02 | Repudiation | conflict report | medium | mitigate | Include deterministic sources, values/fingerprints, field, type, and severity. |
| T-11-G05-03 | Tampering | installer rollback | high | mitigate | Snapshot exact pre-state and atomically restore replaced files while removing only transaction-created paths. |
| T-11-G05-04 | Elevation of Privilege | failure seam | medium | mitigate | Accept only a direct injected callback in the programmatic test path; no CLI flag, environment switch, or production default. |
</threat_model>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | - | Stable runtime-neutral view with complete conflicts and safe read-only build installation | 11-05 | COVERED | Conflicts remain diagnostic; candidate remains inactive.
REQ | REG-01 | One canonical schema and stable linked identity | 11-05 | COVERED | Linked variants retain identity while disagreements are explicit.
REQ | REG-02 | Complete full-build report | 11-05 | COVERED | All material conflicts and transactional installer evidence are reported/tested.
REQ | ADP-01 | Claude variant preservation | 11-05 | COVERED | Claude linked provenance/native invocation remains intact.
REQ | ADP-02 | Codex variant preservation | 11-05 | COVERED | Codex linked provenance/native invocation remains intact.
RESEARCH | - | Evidence-gated merge, typed conflicts, deterministic reports | 11-05 | COVERED | Declarative field matrix and stable sorting.
RESEARCH | - | Preflight, atomic writes, ownership, rollback and usable diagnosis | 11-05 | COVERED | Exact transaction snapshots and restoration.
CONTEXT | D-01..D-04 | Authoritative cross-runtime identity and variant retention | 11-05 | COVERED | Conflict task preserves evidence-gated merge.
CONTEXT | D-05..D-12 | Scope, partial, and dependency semantics | 11-05 | COVERED | Material-field conflicts include these canonical semantics without activation policy.
CONTEXT | D-13..D-16 | Complete typed deterministic provenance/conflicts/report | 11-05 | COVERED | F-003 directly closes D-14/D-16.
CONTEXT | D-17..D-20 | Lightweight safe one-command installation | 11-05 | COVERED | Standard-library transactional rollback and unchanged CLI path.
</source_audit>

<verification>
Run `node --test tests/router.registry-schema.test.mjs tests/router.adapters.test.mjs tests/router.registry-build.test.mjs tests/router.lifecycle.test.mjs tests/router.settings-diff.test.mjs`, then `node --test tests/*.test.mjs`.
</verification>

<success_criteria>
F-003 and Required Gap Closure item 4 are closed: all linked material disagreements are complete deterministic typed conflicts, post-mutation failures restore exact pre-state, and the full suite passes without activating candidate routing.
</success_criteria>

<output>
Create `.planning/phases/11-canonical-registry-and-runtime-adapters/11-05-SUMMARY.md` when done.
</output>

---
phase: 16-workflow-first-orchestration-and-context-budgets
plan: 03
type: execute
wave: 3
depends_on: ["16-02"]
files_modified:
  - src/orchestrator/budget.mjs
  - tests/router.context-budget.test.mjs
autonomous: true
requirements: [TOK-01, TOK-02]
nyquist_compliant: true
must_haves:
  truths:
    - "D-01/D-02/D-03/D-04: context planning accepts only one resolved, gate-safe workflow and never repairs, chooses, or persists a transition."
    - "D-05/D-06/D-07/D-08: required dependency facts come only from the selected workflow closure, with lifecycle hooks represented as bindings rather than tools."
    - "D-09/D-10: each workflow allowlists source classes with per-source and total hard ceilings; required overflow blocks while optional overflow is deterministically omitted in stable priority order."
    - "D-11/D-12/D-13: utf8-bytes-v1-ceil-div-3 accounting reports bytes/tokens/deltas, summary reuse requires exact identity/witness/contract version, forbidden broad sources never load, and planning remains pure."
  artifacts:
    - path: src/orchestrator/budget.mjs
      provides: "Versioned estimator, context-contract validation, source prioritization, summary reuse, overflow policy, and explainable accounting"
    - path: tests/router.context-budget.test.mjs
      provides: "TOK-01/TOK-02 allowlist, boundary, reuse, UTF-8, permutation, privacy, and no-I/O matrix"
  key_links:
    - from: src/orchestrator/budget.mjs
      to: src/orchestrator/select.mjs
      via: "consumes selected workflow identity and required dependency closure facts"
    - from: src/orchestrator/budget.mjs
      to: src/context/capsule.mjs
      via: "uses canonical identity, freshness witnesses, and bounded safe references without filesystem access"
---

<objective>
Implement least-sufficient context contracts for TOK-01/TOK-02 and D-09 through D-13 while requiring the workflow and closure guarantees of D-01 through D-08.

Purpose: Produce a deterministic explainable context-load plan that cannot exceed declared budgets or silently load broad source bodies.
Output: `src/orchestrator/budget.mjs` and its complete context-budget regression matrix.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-workflow-first-orchestration-and-context-budgets/16-CONTEXT.md
@.planning/phases/16-workflow-first-orchestration-and-context-budgets/16-RESEARCH.md
@.planning/phases/16-workflow-first-orchestration-and-context-budgets/16-02-SUMMARY.md
@src/orchestrator/transitions.mjs
@src/orchestrator/select.mjs
@src/context/capsule.mjs
@src/context/sources.mjs
@src/registry/schema.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce workflow source contracts and hard budgets</name>
  <read_first>src/orchestrator/transitions.mjs, src/orchestrator/select.mjs, src/context/capsule.mjs, src/context/sources.mjs, .planning/phases/16-workflow-first-orchestration-and-context-budgets/16-CONTEXT.md, .planning/phases/16-workflow-first-orchestration-and-context-budgets/16-RESEARCH.md</read_first>
  <files>tests/router.context-budget.test.mjs, src/orchestrator/budget.mjs</files>
  <behavior>
    - Missing/non-dispatchable workflow or unsafe/incomplete closure blocks before any source descriptor is considered.
    - Workflow contracts require allowed source classes, per-source byte ceilings, one total hard ceiling, and required/optional priority; undeclared classes are rejected.
    - Stable load order is transition facts, required dependencies, exact artifact summaries/references, then optional diagnostics.
    - Required overflow by one byte blocks without truncation; optional overflow by one byte is omitted deterministically with reason/accounting; input permutations are byte-stable.
  </behavior>
  <action>Write failing contract and boundary fixtures first, then implement exported contract validation and `planContextLoad(workflow, capsule, index, budget)` (or an equivalent documented signature) as a pure transformation over descriptors. Per D-01 through D-08 require selected workflow and safe closure facts rather than deriving them again. Per D-09 enforce exact source-class allowlists, positive integer per-source ceilings, total ceiling, and required/optional declarations. Per D-10 sort by fixed semantic priority and canonical identity; block required overflow without semantic truncation, omit optional overflow with stable reason codes/counts, and never substitute an undeclared source. Explicitly reject default source classes representing a full manifest, planning tree/directory, conversation history, or complete design body to satisfy TOK-01. Use no filesystem, network, cache write, or deployed-hook integration.</action>
  <verify><automated>node --test tests/router.context-budget.test.mjs</automated></verify>
  <acceptance_criteria>Every contract/budget edge has exact output; forbidden broad source descriptors cannot enter a default plan; required overflow never dispatches and optional omission is deterministic.</acceptance_criteria>
  <done>D-09/D-10 and TOK-01 have an executable least-sufficient context-load contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add versioned accounting, exact summary reuse, and regression reports</name>
  <read_first>src/orchestrator/budget.mjs, tests/router.context-budget.test.mjs, src/context/capsule.mjs, src/registry/schema.mjs, .planning/phases/16-workflow-first-orchestration-and-context-budgets/16-RESEARCH.md</read_first>
  <files>tests/router.context-budget.test.mjs, src/orchestrator/budget.mjs</files>
  <behavior>
    - `utf8-bytes-v1-ceil-div-3` estimates tokens as the ceiling of canonical UTF-8 bytes divided by three, including multibyte and exact-ceiling boundary fixtures.
    - Summary reuse succeeds only when canonical source identity, freshness witness, and summary-contract version all match; each mismatch is a stable miss and never falls back to a full body.
    - Reports include per-source and total canonical bytes, estimated tokens, ceilings, included/omitted sources, reuse status, and signed regression deltas against an optional baseline.
    - Planning is byte-stable, side-effect-free, privacy-safe, and performs no file reads/writes, recursive enumeration, history loading, tokenizer/network/model call, or Phase 17 compilation.
  </behavior>
  <action>Extend tests first with ASCII/UTF-8, reuse hit/miss, baseline, permutation, forbidden-fallback, canary, and injected-I/O-spy fixtures. Per D-11 export a named/versioned deterministic estimator using canonical bytes and the conservative `ceil(bytes / 3)` formula; label it as a routing-budget estimate, not billing parity. Include stable per-source/total accounting and signed byte/token regression deltas. Per D-12 reuse a bounded summary only on exact canonical identity, freshness witness, and summary-contract-version equality; on any miss require an allowed exact summary/reference descriptor or block/omit according to required priority, never loading a full planning tree, history, manifest, or design body. Per D-13 keep the module pure and JSON-ready; persistence, compiled indexes, deployed-hook wiring, warm latency measurement, telemetry, canaries, and automatic evolution remain Phase 17.</action>
  <verify><automated>node --test tests/router.context-budget.test.mjs tests/router.context-capsule.test.mjs tests/router.context-sources.test.mjs</automated></verify>
  <acceptance_criteria>Estimator outputs are exact and versioned; all three reuse witnesses are mandatory; regression reports are byte-stable; privacy/I/O spies prove no broad load or side effect.</acceptance_criteria>
  <done>D-11 through D-13 and TOK-02 are complete with exact accounting, safe reuse, and Phase 17-ready explainable outputs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Risk | Mitigation |
|---|---|---|
| Workflow contract to source planner | Malformed ceilings or undeclared sources could bypass limits | Strict schema, positive integer ceilings, allowlists, and hard total gate. |
| Summary index to reused context | Stale or cross-artifact summary could poison routing | Exact identity, freshness witness, and contract-version match. |
| Source descriptors to report | Sensitive bodies or diagnostic data could leak | Canonical bounded metadata only, privacy canaries, and no direct I/O. |
| Required content to overflow handling | Truncation could remove semantic preconditions | Block required overflow; only optional items may be omitted. |
</threat_model>

<verification>
Run `node --test tests/router.context-budget.test.mjs tests/router.workflow-orchestrator.test.mjs tests/router.context-*.test.mjs`, then `node --test tests/*.test.mjs` and `git diff --check`. Inspect the diff to prove no installed hook, lifecycle installer, compiled runtime index, telemetry, evolution, or performance-calibration file changed.
</verification>

<success_criteria>
- TOK-01 forbids full manifests, planning trees, conversation history, and complete design bodies in default context plans.
- TOK-02 enforces declared workflow budgets, exact reusable-summary witnesses, and deterministic token regression reporting.
- All D-01 through D-13 are traceable across the prerequisite workflow token, closure facts, and pure context plan.
- Phase 17 exclusively retains compilation, deployed-hook integration, warm performance measurement, telemetry canaries, and evolution.
</success_criteria>

## Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Least-sufficient compatible context within declared budgets | 16-03 | COVERED | Pure load planner and report. |
| REQ | TOK-01 | No broad default source bodies | 16-03 | COVERED | Forbidden-class and no-fallback tests. |
| REQ | TOK-02 | Declared budgets and unchanged-summary reuse | 16-03 | COVERED | Tasks 1-2. |
| RESEARCH | Contract validation, ceil(bytes/3), overflow boundaries, regression deltas | 16-03 | COVERED | Exact behavioral matrix. |
| CONTEXT | D-01–D-08 | Selected workflow and closure prerequisites | 16-03 | COVERED | Strict input gates; no re-selection. |
| CONTEXT | D-09–D-13 | Budgets, ordering, accounting, reuse, purity | 16-03 | COVERED | Direct implementation. |

<output>Create `.planning/phases/16-workflow-first-orchestration-and-context-budgets/16-03-SUMMARY.md` when done.</output>

---
phase: 44-receipts-and-causal-attribution
plan: 02
type: execute
wave: 2
depends_on: [44-01]
files_modified:
  - src/adapters/dispatch/claude.mjs
  - src/adapters/dispatch/codex.mjs
  - tests/phase-44/receipts.test.mjs
autonomous: true
requirements: [RCPT-01, RCPT-02, RCPT-03, RCPT-04, RCPT-05]
must_haves:
  truths:
    - "Both runtime adapters publish one stable pending receipt before spawn and retain the same receipt id through invoked and terminal updates."
    - "Actual composition, evidence references, bounded resource facts, selection divergence, alternatives, rejections, corrections, and substitutions survive adapter transitions without raw prompts."
    - "A completed process without linked verified postcondition evidence is not credited as Router success."
    - "Existing path, runtime, authority, strategy, invocation, pre-dispatch, and idempotency gates remain authoritative and Phase 38 behavior remains green."
  artifacts:
    - path: src/adapters/dispatch/claude.mjs
      provides: "Claude pending/invoked/terminal attribution transitions"
    - path: src/adapters/dispatch/codex.mjs
      provides: "Codex pending/invoked/terminal attribution transitions"
    - path: tests/phase-44/receipts.test.mjs
      provides: "Cross-runtime pending, terminal, divergence, and no-credit integration coverage"
  key_links:
    - from: adapters invokeImpl
      to: receipt helpers
      via: "stable pending id → transitionReceipt → outcomeCredit"
    - from: adapter exit handler
      to: existing ReceiptStore
      via: "same receipt id is atomically republished and appended"
---

<objective>
Wire the Phase 44 receipt contract into both production dispatch variants without changing native spawn or safety-gate ordering. Every attempted route gets an attributable stable record, and process completion receives credit only from causally linked verified evidence.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/44-receipts-and-causal-attribution/44-CONTEXT.md
@.planning/phases/44-receipts-and-causal-attribution/44-RESEARCH.md
@.planning/phases/44-receipts-and-causal-attribution/44-PATTERNS.md
@.planning/phases/44-receipts-and-causal-attribution/44-01-PLAN.md
@src/adapters/dispatch/claude.mjs
@src/adapters/dispatch/codex.mjs
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add failing cross-runtime pending and causal-transition tests</name>
  <files>tests/phase-44/receipts.test.mjs</files>
  <behavior>
    - Test 1: Claude and Codex publish pending before native spawn and return the same stable receipt id through invoked/completed observation.
    - Test 2: adapter receipts retain structured identity, actual composition, bounded evidence, selected-versus-actual divergence, alternatives, rejection reasons, corrections, substitutions, and Phase 43 strategy/work fields.
    - Test 3: linked verified postcondition evidence produces outcome_credit true; exit zero without it, ignored recommendations, and unrelated receipt evidence produce false.
    - Test 4: invalid or unavailable runtime paths still produce truthful explicit terminal route state and never claim invocation.
  </behavior>
  <action>Extend the Phase 44 suite with temp receipt roots and the existing harmless fixture. Poll the real adapter receipt store for terminal state. Assert the pending file exists before the child exits, stable id continuity, runtime partitioning, bounded fields, and no raw prompt persistence. Keep assertions additive so Phase 38 compatibility remains explicit.</action>
  <verify><automated>node --test tests/phase-44/receipts.test.mjs</automated></verify>
  <done>The integration tests fail on missing pending/attribution behavior while existing adapter tests continue to provide the compatibility baseline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire stable receipt transitions into Claude and Codex adapters</name>
  <files>src/adapters/dispatch/claude.mjs, src/adapters/dispatch/codex.mjs</files>
  <action>At the dispatch boundary, derive only structured identity and bounded attribution fields from the action. Publish a pending receipt before native spawn for dispatchable actions, reuse its stable id for invoked and terminal updates, and map rejected/blocked/recommendation-only paths to explicit route-state metadata while preserving legacy completion state values. Enrich invoked and terminal receipts with actual composition, evidence references, strategy/work facts, bounded time/token/call/retry/failure/coordination facts, selected-versus-actual divergence, alternatives, decisive reasons, corrections, and substitutions. Compute outcome credit with the shared causal predicate; exit zero alone must remain uncredited. Keep `validateStrategyBounds` → `validateInvocation` → `preDispatchGate` → `spawn` contiguous, preserve both runtime partitions, and use the existing store for every write. Do not persist prompt text, secrets, environment, or raw output.

Apply the same transition shape to both variants, branching only at their established runtime seams. Preserve pause/resume receipt identity and existing Phase 38 state assertions.</action>
  <verify><automated>node --test tests/phase-44/receipts.test.mjs tests/phase-38/*.mjs</automated></verify>
  <done>Both runtimes produce stable pending-to-terminal attribution, explicit route states, bounded inspection data, and truthful outcome credit while all prior native dispatch tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Run phase integration, review, and regression gates</name>
  <files>src/adapters/dispatch/contract.mjs, src/adapters/dispatch/receipt.mjs, src/adapters/dispatch/claude.mjs, src/adapters/dispatch/codex.mjs, tests/phase-44/receipts.test.mjs</files>
  <action>Run focused Phase 44 and Phase 38 tests first, inspect persisted receipt JSON for bounded fields and stable id continuity, then run the full repository suite. Correct only owning-seam regressions. Confirm each RCPT requirement has executable assertions and that no prompt/startup code path imports inspection or performs receipt scans.</action>
  <verify><automated>node --test tests/phase-44/receipts.test.mjs tests/phase-38/*.mjs && node --test tests/*.test.mjs</automated></verify>
  <done>Phase 44 focused coverage and the repository regression suite pass, with the receipt contract demonstrably attributable and bounded.</done>
</task>
</tasks>

<verification>
- Focused Phase 44 tests pass before the full suite.
- Phase 38 Claude/Codex native dispatch tests remain green.
- Full `node --test tests/*.test.mjs` result is recorded in the summary, including any pre-existing baseline failures.
</verification>

<success_criteria>
Every route attempted by either installed runtime has a stable, inspectable, bounded causal receipt from pending through explicit terminal state, and only linked verified postconditions receive outcome credit.
</success_criteria>

<output>
Create `.planning/phases/44-receipts-and-causal-attribution/44-02-SUMMARY.md` when done.
</output>

---
phase: 26-coherent-publication-and-dual-runtime-release
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/router.registry-build.test.mjs
  - src/registry/build.mjs
  - tests/router.phase26-tuple.test.mjs
  - tests/router.phase26-hot-path.test.mjs
  - tests/router.phase26-invalidation.test.mjs
  - tests/router.phase26-equivalence.test.mjs
  - tests/router.phase26-lifecycle.test.mjs
  - tests/router.phase26-dual-runtime.test.mjs
  - tests/router.phase26-authority.test.mjs
  - tests/router.phase26-performance.test.mjs
  - tests/router.phase26-release.test.mjs
autonomous: true
requirements: [REL-01, REL-02, REL-03, REL-04, REL-05, REL-06, REL-07, REL-08, REL-09]
must_haves:
  truths:
    - "The pre-existing mode-map regression has a root-cause fix and a clean executable baseline."
    - "Every Phase 26 requirement has a failure-capable behavioral test before production implementation."
  artifacts:
    - "tests/router.registry-build.test.mjs"
    - "tests/router.phase26-tuple.test.mjs"
    - "tests/router.phase26-release.test.mjs"
  key_links:
    - "Mode-map stamping reaches mapCandidateRegistry through the production registry record contract."
    - "Each Wave 0 test exercises a production seam rather than source-text inspection."
---

<objective>
Establish a trustworthy Wave 0 by fixing the existing mode-map regression at its shared root and creating all Phase 26 behavioral test owners.

Purpose: Prevent old failures from being hidden by new work and make every locked release behavior falsifiable before implementation.
Output: Clean registry-build baseline plus nine failing Phase 26 behavioral test files.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-coherent-publication-and-dual-runtime-release/26-CONTEXT.md
@.planning/phases/26-coherent-publication-and-dual-runtime-release/26-RESEARCH.md
@.planning/phases/26-coherent-publication-and-dual-runtime-release/26-VALIDATION.md
@src/registry/build.mjs
@tests/router.registry-build.test.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repair the mode-map mapping regression at the shared registry seam</name>
  <files>tests/router.registry-build.test.mjs, src/registry/build.mjs</files>
  <behavior>
    - A slash, skill, or agent mode-map target retains its explicit subject through normalization and maps to a dispatch route.
    - Artifact-provided subjects remain unioned and warning entries remain non-dispatchable.
    - Missing mode-map input remains a no-op.
  </behavior>
  <action>Trace the failing `mode-map stamping seeds record mapping.explicit_subjects` test through `resolveModeMapTargets`, record assembly, contract eligibility, and `mapCandidateRegistry`. Fix the first shared production seam that discards or disqualifies the stamped subjects; do not weaken contract eligibility, manufacture test-only mappings, or special-case names. Preserve malformed-input failure behavior and add the smallest regression assertion that proves the corrected normalized record is dispatch-eligible.</action>
  <verify>
    <automated>rtk node --test tests/router.registry-build.test.mjs</automated>
  </verify>
  <done>All registry-build tests pass and the mode-map test publishes at least two real mapped subjects without bypassing the mapper.</done>
</task>

<task type="auto">
  <name>Task 2: Create tuple, hot-path, invalidation, and equivalence RED tests</name>
  <files>tests/router.phase26-tuple.test.mjs, tests/router.phase26-hot-path.test.mjs, tests/router.phase26-invalidation.test.mjs, tests/router.phase26-equivalence.test.mjs</files>
  <action>Create failure-capable tests from 26-VALIDATION.md for the complete seven-projection tuple, bounded read-only prompt path, eight invalidation classes, atomic old-or-new visibility, and full/incremental complete-tuple byte equivalence. Use production builders/publisher/loader/router and existing fixture helpers. Each RED file must fail with its named stable reason marker: `PHASE26_MISSING_COMPLETE_TUPLE`, `PHASE26_PROMPT_PATH_NOT_READ_ONLY`, `PHASE26_INVALIDATION_INCOMPLETE`, or `PHASE26_EQUIVALENCE_INCOMPLETE`. Observe each expected behavioral failure; syntax, import, harness, empty-stub, mocks-only, source-text-only, or unrelated failures do not qualify.</action>
  <verify>
    <automated>rtk proxy bash -lc 'set -o pipefail; d=$(mktemp -d); trap '\''rm -rf "$d"'\'' EXIT; for spec in "tests/router.phase26-tuple.test.mjs:PHASE26_MISSING_COMPLETE_TUPLE" "tests/router.phase26-hot-path.test.mjs:PHASE26_PROMPT_PATH_NOT_READ_ONLY" "tests/router.phase26-invalidation.test.mjs:PHASE26_INVALIDATION_INCOMPLETE" "tests/router.phase26-equivalence.test.mjs:PHASE26_EQUIVALENCE_INCOMPLETE"; do f=${spec%%:*}; marker=${spec#*:}; log="$d/$(basename "$f").log"; node --test "$f" 2>&1 | tee "$log"; status=${PIPESTATUS[0]}; test "$status" -ne 0 || { echo "unexpected GREEN: $f"; exit 1; }; ! grep -Eq "SyntaxError|ERR_MODULE_NOT_FOUND|Cannot find (module|package)|Could not find" "$log" || { echo "invalid RED harness failure: $f"; exit 1; }; grep -Fq "$marker" "$log" || { echo "unexpected RED reason: $f (wanted $marker)"; exit 1; }; done'</automated>
  </verify>
  <done>All four files contain mapped behavioral assertions and each has demonstrated a relevant RED failure.</done>
</task>

<task type="auto">
  <name>Task 3: Create lifecycle, runtime, authority, performance, and release RED tests</name>
  <files>tests/router.phase26-lifecycle.test.mjs, tests/router.phase26-dual-runtime.test.mjs, tests/router.phase26-authority.test.mjs, tests/router.phase26-performance.test.mjs, tests/router.phase26-release.test.mjs</files>
  <action>Create failure-capable tests for guarded activation/recovery, actual installed Claude/Codex execution, approval and fail-open authority, a generated mixed registry of at least 300 records with isolated latency/context metrics, and release evidence freshness/completeness. Reuse existing lifecycle/install/release helpers. Each RED file must fail with its named stable reason marker: `PHASE26_LIFECYCLE_INCOMPLETE`, `PHASE26_DUAL_RUNTIME_INCOMPLETE`, `PHASE26_AUTHORITY_INCOMPLETE`, `PHASE26_PERFORMANCE_EVIDENCE_MISSING`, or `PHASE26_RELEASE_EVIDENCE_MISSING`. Observe each expected behavioral failure; syntax, import, harness, or unrelated failures do not qualify.</action>
  <verify>
    <automated>rtk proxy bash -lc 'set -o pipefail; d=$(mktemp -d); trap '\''rm -rf "$d"'\'' EXIT; for spec in "tests/router.phase26-lifecycle.test.mjs:PHASE26_LIFECYCLE_INCOMPLETE" "tests/router.phase26-dual-runtime.test.mjs:PHASE26_DUAL_RUNTIME_INCOMPLETE" "tests/router.phase26-authority.test.mjs:PHASE26_AUTHORITY_INCOMPLETE" "tests/router.phase26-performance.test.mjs:PHASE26_PERFORMANCE_EVIDENCE_MISSING" "tests/router.phase26-release.test.mjs:PHASE26_RELEASE_EVIDENCE_MISSING"; do f=${spec%%:*}; marker=${spec#*:}; log="$d/$(basename "$f").log"; node --test "$f" 2>&1 | tee "$log"; status=${PIPESTATUS[0]}; test "$status" -ne 0 || { echo "unexpected GREEN: $f"; exit 1; }; ! grep -Eq "SyntaxError|ERR_MODULE_NOT_FOUND|Cannot find (module|package)|Could not find" "$log" || { echo "invalid RED harness failure: $f"; exit 1; }; grep -Fq "$marker" "$log" || { echo "unexpected RED reason: $f (wanted $marker)"; exit 1; }; done'</automated>
  </verify>
  <done>All nine Wave 0 owners exist with real behavioral assertions, and `wave_0_complete` can be recorded truthfully.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mode-map -> registry | Local configuration becomes dispatch metadata. |
| test fixture -> production seam | Adversarial inputs must not bypass real validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-01 | Tampering | mode-map stamping | high | mitigate | Keep contract eligibility and mapper validation intact; repair the shared normalization seam only. |
| T-26-02 | Repudiation | Wave 0 evidence | medium | mitigate | Require executable RED observations and a clean baseline command. |
</threat_model>

<verification>
Run `rtk node --test tests/router.registry-build.test.mjs`; confirm every Phase 26 test file exists and fails for its mapped missing behavior before later plans begin.
</verification>

<success_criteria>
The baseline regression is green, all nine requirement owners are behavioral and RED, and no production Phase 26 feature has been implemented prematurely.
</success_criteria>

<output>
Create `.planning/phases/26-coherent-publication-and-dual-runtime-release/26-01-SUMMARY.md` when done.
</output>

---
phase: 25-advisory-stewardship-and-guarded-drafts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/steward/suggestion.mjs
  - src/steward/state.mjs
  - tests/router.steward-suggestion.test.mjs
  - tests/router.steward-state.test.mjs
autonomous: true
requirements: [UX-01, UX-02, UX-03, UX-04, UX-05, UX-06, UX-07, UX-08, UX-09]
must_haves:
  truths:
    - "UX-01/UX-02/UX-03/UX-04: a pure, versioned policy filters fresh, high-confidence, actionable observations, deterministically returns exactly one suggestion, and derives a stable semantic fingerprint."
    - "UX-05: dismiss, bounded snooze, cooldown, and correction proposals are local and keyed by fingerprint; corrections never alter routing."
    - "UX-08/UX-09: steward state is isolated from authoritative capability/routing artifacts and no list, dashboard, timeline, or maintenance surface is introduced."
  artifacts:
    - path: src/steward/suggestion.mjs
      provides: "Bounded eligibility, ranking, projection, overview, and fingerprint policy."
      exports: [selectSuggestion, suggestionFingerprint, startupPointer]
    - path: src/steward/state.mjs
      provides: "Atomic private interaction state and immutable correction proposals."
      exports: [createStewardStore]
    - path: tests/router.steward-suggestion.test.mjs
      provides: "Nyquist coverage for deterministic one-item selection and privacy bounds."
    - path: tests/router.steward-state.test.mjs
      provides: "Nyquist coverage for permissions, suppression, concurrency, and mutation isolation."
  key_links:
    - from: src/steward/suggestion.mjs
      to: src/health/catalog.mjs
      via: "Consumes the bounded deriveObservations result contract; never reads raw outcome history."
      pattern: "observations"
    - from: src/steward/state.mjs
      to: src/steward/suggestion.mjs
      via: "Fingerprint-keyed dismissal, snooze, cooldown, and correction state."
      pattern: "fingerprint"
---

<objective>
Create the deterministic advisory policy and private interaction store that all later Phase 25 surfaces consume.

Purpose: deliver the bounded, local core for UX-01 through UX-05 while establishing UX-08/UX-09 isolation before CLI or draft wiring exists.
Output: two stdlib-only modules and focused node:test coverage.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-advisory-stewardship-and-guarded-drafts/25-CONTEXT.md
@.planning/phases/25-advisory-stewardship-and-guarded-drafts/25-UI-SPEC.md
@.planning/phases/25-advisory-stewardship-and-guarded-drafts/25-RESEARCH.md
@.planning/phases/25-advisory-stewardship-and-guarded-drafts/25-PATTERNS.md

<interfaces>
From src/health/catalog.mjs:
- deriveObservations(...) returns {schema_version, policy_version, observations, reason_codes}; each observation is already bounded and contains observation_kind, reason_code, evidence_window_ms, freshness, affected_capability_ids, confidence_basis_points, remedy.
From src/health/thresholds.mjs:
- POLICY_VERSION and COOLDOWN_MS are the authoritative version/cooldown constants.
From src/registry/schema.mjs:
- stableStringify(value) provides canonical bytes for hashing.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Select one deterministic, privacy-bounded suggestion</name>
  <read_first>src/health/catalog.mjs, src/health/thresholds.mjs, src/registry/schema.mjs, tests/router.health.catalog.test.mjs</read_first>
  <files>src/steward/suggestion.mjs, tests/router.steward-suggestion.test.mjs</files>
  <behavior>
    - Empty, stale, low-confidence, non-actionable, dismissed, snoozed, or cooldown-suppressed inputs return suggestion_none without rejected candidates.
    - Input permutations select the same single fingerprint using an explicit total tie-break order.
    - The selected detail contains compact aggregate health, bounded evidence, integer confidence, sorted affected IDs, fixed expected-benefit and risk tokens, and a safe next action.
    - Fingerprints exclude wall-clock and presentation text; the same semantic observation under different now values retains identity.
  </behavior>
  <action>Write the failing tests first, then implement `src/steward/suggestion.mjs`. Per the locked Suggestion Selection and Suggestion Interaction decisions, filter before ranking and return only index zero; use a frozen policy version, fixed actionability precedence, descending confidence/evidence, then stable reason/affected-ID/fingerprint tie-breaks. Reuse `COOLDOWN_MS`, `stableStringify`, and `node:crypto` SHA-256. Consume Phase 24 observations as the sole evidence input and map reason codes through fixed allowlists for benefit, risk, and safe action. `startupPointer` returns only schema/version/fingerprint/availability/cooldown metadata needed for later compilation, never detail or a candidate list. Reject malformed trust-boundary structures with TypeError and bound all arrays/strings.</action>
  <verify>
    <automated>rtk node --test tests/router.steward-suggestion.test.mjs</automated>
  </verify>
  <acceptance_criteria>Exactly one eligible suggestion or suggestion_none is returned; selection is permutation-stable; required UX-03/UX-04 fields are bounded; no raw prompt, secret, arbitrary evidence text, absolute private path, or rejected list crosses the result.</acceptance_criteria>
  <done>UX-01 through UX-04 and the one-item/no-dump parts of UX-09 have executable policy coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist suppression and correction proposals without routing mutation</name>
  <read_first>src/health/store.mjs, src/registry/activate.mjs, tests/router.health.admin.test.mjs, tests/router.health.privacy.test.mjs</read_first>
  <files>src/steward/state.mjs, tests/router.steward-state.test.mjs</files>
  <behavior>
    - Steward directories are 0700 and state/proposal files are durable atomic 0600 writes.
    - Dismissal is idempotent; snooze requires a safe integer expiry inside the documented bound; cooldown timestamps remain separate from identity.
    - Correction creates a content-addressed, versioned proposal under the steward root and reports routing unchanged.
    - Missing/corrupt optional state fails closed without leaking exceptions; concurrent mutation does not lose updates.
    - Registry, active pointers, mode map, weights, capability definitions, and routing policy remain byte-identical after every interaction.
  </behavior>
  <action>Write tests first, then implement `createStewardStore({root, lock})` under the Router-owned `steward/` sibling. Copy the proven lock, temp-write, fsync, rename, directory-sync, permissions, and stale-lock behavior from `src/health/store.mjs`; do not extract a new abstraction. Store only bounded fingerprints, integer timestamps, reason codes, and versioned structured correction fields. Per the locked correction decision, corrections are immutable proposals and never authoritative edits. Validate containment for every proposal target and do not import activation, publication, lifecycle, installer, settings, or routing mutation modules.</action>
  <verify>
    <automated>rtk node --test tests/router.steward-state.test.mjs tests/router.health.privacy.test.mjs</automated>
  </verify>
  <acceptance_criteria>Dismiss/snooze/cooldown/correct survive atomic persistence with restrictive permissions, corrupt state cannot authorize anything, and protected artifacts remain byte-identical.</acceptance_criteria>
  <done>UX-05 local interactions and correction proposals work without expanding authority; UX-08 isolation is test-enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Phase 24 catalog → steward policy | Bounded but untrusted observation records enter ranking/projection. |
| CLI caller → steward state | Fingerprints, timestamps, and correction fields reach private persistence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-25-01 | Tampering | state.mjs | high | mitigate | Containment, locks, atomic 0600 writes, schema bounds, and protected-artifact hash tests. |
| T-25-02 | Information Disclosure | suggestion.mjs | high | mitigate | Allowlisted bounded projection; prohibit raw prompt/evidence/private-root output. |
| T-25-03 | Denial of Service | suggestion.mjs | medium | mitigate | Bound input and output cardinality and use deterministic total ordering. |
| T-25-04 | Elevation of Privilege | state.mjs | high | mitigate | Correction records are proposal-only and mutation-module imports are structurally denied. |
| T-25-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package install exists; stdlib and current modules only. |
</threat_model>

<verification>
Nyquist: each behavior has a focused node:test created before production code. Run `rtk node --test tests/router.steward-suggestion.test.mjs tests/router.steward-state.test.mjs`, then the Phase 24 privacy regression.
</verification>

<success_criteria>
One deterministic advisory candidate can be selected and locally suppressed/corrected with no routing mutation, no candidate dump, and no new dependency.
</success_criteria>

<output>
Create `.planning/phases/25-advisory-stewardship-and-guarded-drafts/25-01-SUMMARY.md` when done.
</output>

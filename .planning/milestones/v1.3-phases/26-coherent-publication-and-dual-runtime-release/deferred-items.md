# Deferred Items

- `tests/router.safety-release.test.mjs` currently has two live-environment failures outside Plan 26-06 ownership: archived `.planning/phases/10-safety-coexistence-and-release-gates/10-VERIFICATION.md` is absent from the active phase tree, and the local Claude settings do not enable `ralph-loop`. The Phase 26 authority and approval slices pass independently.

---
phase: 32-intent-first-routing-mode-map-schema-v4-guard-hole-closure
plan: 04
type: execute
wave: 3
depends_on: [32-03]
files_modified:
  - tests/router.mjs.snapshot
  - /Users/guilherme/.claude/hooks/router.mjs
  - tests/router.phase32-cross-runtime.test.mjs
  - tests/router.perf.test.mjs
autonomous: true
requirements: [PARITY-03, PARITY-04]
user_setup: []
estimate:
  tokens: 55000
  raw_tokens: 30000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - Resolve evaluation uses ONLY the active runtime's present capabilities, and only the active runtime's suggestion is injected — PARITY-03 (32-01 Group A RED → GREEN).
    - A capability present in one runtime resolves to its local equivalent in the other — PARITY-04 (32-01 Group B RED → GREEN).
    - The resolve-first hot path stays within budget: warm p95 < 40ms and max < 100ms (ROADMAP criterion 5), confirmed by an extended perf gate measuring the resolve path, not just the pass-through path.
  artifacts:
    - tests/router.mjs.snapshot + ~/.claude/hooks/router.mjs (runtime-conditional resolve)
    - tests/router.phase32-cross-runtime.test.mjs (GREEN)
    - tests/router.perf.test.mjs (extended with a resolve-first budget gate)
  key_links:
    - resolve-evaluation must consume `RUNTIME`/runtime-conditional dirs (from phase 31/26) so claude and codex runtimes select their own present capability sets — 32-01 Group A/B stay RED until wired.
    - The resolve-first hot path added in 32-02 must not blow the <100ms budget; perf gate measures the resolve-heavy path.
    - Only the active runtime's suggestion is injected — no cross-runtime cache/telemetry reuse (PARITY-02 already established).
---

<objective>
Finalize Phase 32 by making resolve evaluation runtime-conditional (PARITY-03/04, turning the cross-runtime RED specs GREEN) and proving the new resolve-first hot path still meets the latency budget.

Purpose: Close the phase's cross-runtime requirement pair — resolve must evaluate and inject only against the ACTIVE runtime's present capabilities, and a capability present in one runtime must resolve to its local equivalent in the other — then verify the aggregate resolve-first path in 32-02/32-03 stays within the ROADMAP criterion-5 budget (warm p95 < 40ms, max < 100ms). This is the last wave; after it Phase 32 requirements are complete.

Output: runtime-conditional resolve evaluation in the shipped hook (mirror); cross-runtime test GREEN; perf gate extended with a resolve-heavy budget assertion.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-intent-first-routing-mode-map-schema-v4-guard-hole-closure/32-CONTEXT.md

# Reference files (read once, extract exact localities):
@tests/router.mjs.snapshot  # RUNTIME/RUNTIME_CONFIG_DIR (phase 31:104); buildKnownNames:407; resolveSlashRoute + resolve-first eval (32-02); generic fallback (32-03)
@tests/router.phase32-cross-runtime.test.mjs  # 32-01 RED spec driving PARITY-03/04 (do NOT weaken)
@tests/router.phase26-dual-runtime.test.mjs  # PROFILES + provenance[{runtime}] shape to reuse
@tests/router.perf.test.mjs  # HOOK, NODE, BUDGET_MS, runOnce pattern to extend
@tests/router.phase26-performance.test.mjs  # any existing cross-runtime perf baseline to reuse
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Runtime-conditional resolve evaluation — active-runtime-only capabilities (PARITY-03)</name>
  <files>tests/router.mjs.snapshot, /Users/guilherme/.claude/hooks/router.mjs, tests/router.phase32-cross-runtime.test.mjs</files>
  <read_first>tests/router.mjs.snapshot</read_first>
  <behavior>
    - Test 1: same intent driven with `ROUTER_RUNTIME=claude` vs `ROUTER_RUNTIME=codex` where `.claude` has `gsd-debug` but `.codex` does NOT → the claude runtime injects the `gsd-debug` slash suggestion and the codex runtime does NOT (only the active runtime's present capabilities evaluated and injected) — 32-01 Group A RED → GREEN.
    - Test 2: the suggestion injected always matches the active runtime's present set — never the other runtime's candidate (no cross-runtime leakage).
  </behavior>
  <action>
    In the shipped resolve-first evaluation (mirrored snapshot), make the presence check runtime-conditional: `resolveSlashRoute` must evaluate candidates against the ACTIVE runtime's present command inventory only. The active runtime is the module `RUNTIME` constant / runtime-conditional dir (established in phase 31 at snapshot:104). Ensure the `indexes.commands` set fed to resolve is derived from the active runtime's manifest presence, so a capability present only in `.claude` is invisible to a `.codex` runtime and vice-versa.

    Wire the cross-runtime spec drive: `tests/router.phase32-cross-runtime.test.mjs` already drives ROUTER_RUNTIME via spawnSync (32-01 Group A). Do not weaken that spec — make the hook honor it so the group turns GREEN. Ensure fail-open holds: an ambiguous/unset runtime defaults to claude (phase 31 enum clamp) and never selects a foreign capability set. Mirror both copies (snapshot + `~/.claude/hooks/router.mjs`).
  </action>
  <verify>
    <automated>rtk node --test tests/router.phase32-cross-runtime.test.mjs 2>&1 | grep -E "runtime|claude|codex|pass|fail" | head -20</automated>
  </verify>
  <done>Resolve evaluation consumes only the active runtime's present capabilities and injects only the active runtime's suggestion; 32-01 Group A (PARITY-03) is GREEN; fail-open default (claude) preserved; mirror-lockstep maintained.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cross-runtime equivalent resolution (PARITY-04)</name>
  <files>tests/router.mjs.snapshot, /Users/guilherme/.claude/hooks/router.mjs, tests/router.phase32-cross-runtime.test.mjs</files>
  <read_first>tests/router.mjs.snapshot</read_first>
  <behavior>
    - Test 1: a capability role whose resolve list carries framework-neutral members (e.g. claude's `gsd-debug`, codex's `systematic-debugging`) resolves to whichever is present in the ACTIVE runtime: claude → `gsd-debug`, codex → the codex-local present equivalent — 32-01 Group B RED → GREEN.
    - Test 2: provenance carries `{ runtime }` (phase-26 shape) so the resolved suggestion is attributable to the active runtime alone.
  </behavior>
  <action>
    Extend the resolve evaluation (mirrored snapshot) so a framework-neutral resolve list resolves a capability present in one runtime to its local equivalent present in the other: because the candidate set is framework-neutral and evaluated against the ACTIVE runtime's presence, the same capability role picks the runtime-local present member. Verify the resolved route suggestion carries the phase-26 `provenance: [{ runtime }]` shape so cross-runtime attribution holds (only the active runtime's suggestion is injected, per PARITY-03).

    Drive the cross-runtime spec `tests/router.phase32-cross-runtime.test.mjs` Group B to GREEN (do not weaken the spec). Mirror both copies. No hardcoded framework names in the evaluator — the equivalent mapping falls out of the framework-neutral ranked list, not a claude↔codex translation table.
  </action>
  <verify>
    <automated>rtk node --test tests/router.phase32-cross-runtime.test.mjs 2>&1 | grep -E "equivalent|provenance|pass|fail" | head -20</automated>
  </verify>
  <done>A capability present in one runtime resolves to its local present equivalent in the other via the framework-neutral resolve list; the resolved suggestion carries `provenance: [{ runtime }]`; 32-01 Group B (PARITY-04) is GREEN; mirror-lockstep maintained.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the perf gate to a resolve-heavy budget (warm p95 < 40ms / max < 100ms) + full-suite verify</name>
  <files>tests/router.perf.test.mjs, tests/router.mjs.snapshot, /Users/guilherme/.claude/hooks/router.mjs</files>
  <action>
    Extend `tests/router.perf.test.mjs` with a resolve-heavy budget gate that exercises the resolve-first hot path (not just the trivial pass-through path the existing gate measures). Seed a fixture prompt that actually drives a resolve decision (a confident intent hitting a slash entry plus a resolve list, per the 32-02/32-03 route), run it repeatedly (>= 20 warm iterations), and assert the warm p95 (sorted 95th-percentile wall-clock) is < 40ms and every individual run's max < 100ms, with exit 0 and no stdout on each run (same runOnce + BUDGET_MS patterns the existing perf spec uses; add a p95 assertion and a resolve-heavy fixture input). Keep the existing trivial pass-through gate intact.

    Then run the full Phase-32 suite to confirm all requirements are GREEN and no regression from the runtime-conditional resolve, generic fallback, tie-lint, and audit closure. At minimum run: schema-v4 routing, cross-runtime, coverage-audit, resolve-tie-lint, perf, failopen, guards, snapshot.diff. Mirror-lockstep must hold (snapshot == live hook).
  </action>
  <verify>
    <automated>rtk node --test tests/router.perf.test.mjs tests/router.phase32-cross-runtime.test.mjs tests/router.mjs.snapshot.diff.test.mjs 2>&1 | tail -20</automated>
  </verify>
  <done>A resolve-heavy perf gate asserts warm p95 < 40ms and max < 100ms on the resolve-first path (exit 0, no dead stdout); the Phase-32 full suite is GREEN with mirror-lockstep intact — PARITY-03/04 complete and Phase 32 requirements ROUTE-01..05 + PARITY-03/04 all satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process ROUTER_RUNTIME → active capability set | A spoofed runtime tag could select the wrong runtime's capabilities, leaking a foreign suggestion |
| resolve-first hot path → injection | New resolve logic must stay within latency budget and never block/dead-inject |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-13 | Spoofing | runtime detection → resolve eval | medium | mitigate | Only the ACTIVE runtime's present capabilities are evaluated/injected (PARITY-03); ambiguous/unset runtime defaults to claude enum clamp (phase 31) — never a foreign capability set. |
| T-32-14 | Tampering | cross-runtime equivalent mapping | medium | mitigate | Equivalent resolution falls out of the framework-neutral ranked list against active presence — no claude↔codex translation table to tamper with; provenance[{runtime}] attributes the active runtime. |
| T-32-15 | Denial of Service | resolve-first hot path | medium | mitigate | Perf gate asserts warm p95 < 40ms / max < 100ms on a resolve-heavy fixture; resolveSlashRoute is pure/fail-open so latency and throw behavior are bounded. |
| T-32-16 | Elevation of Privilege | injected suggestion cross-runtime leak | low | mitigate | Only the active runtime's suggestion is injected; cross-runtime cache/telemetry reuse already blocked (PARITY-02). |

## Package legitimacy gate
No package-manager installs — stdlib-only per project CLAUDE.md. Not applicable.
</threat_model>

<verification>
`rtk node --test tests/router.phase32-cross-runtime.test.mjs tests/router.perf.test.mjs tests/router.schema-v4-routing.test.mjs tests/router.coverage-audit.test.mjs tests/router.resolve-tie-lint.test.mjs tests/router.mjs.snapshot.diff.test.mjs tests/router.failopen.test.mjs tests/router.guards.test.mjs`
Expected: cross-runtime GREEN (PARITY-03/04); perf resolve-heavy p95<40/max<100 GREEN; resolve/coverage/tie-lint GREEN; failopen + guards GREEN; snapshot.diff (mirror) GREEN. Full suite at wave merge.
</verification>

<success_criteria>
- PARITY-03 GREEN: resolve evaluation uses only active runtime's present capabilities; only active runtime's suggestion injected.
- PARITY-04 GREEN: capability present in one runtime resolves to its local equivalent in the other; provenance[{runtime}] attached.
- Resolve-first hot path within budget: warm p95 < 40ms, max < 100ms (ROADMAP criterion 5) via extended perf gate.
- Mirror-lockstep intact (snapshot == live hook); full suite regression-free.
- Phase 32 requirements ROUTE-01..05 + PARITY-03/04 all satisfied and verifiable.
</success_criteria>

<output>
Create `.planning/phases/32-intent-first-routing-mode-map-schema-v4-guard-hole-closure/32-04-SUMMARY.md` when done
</output>

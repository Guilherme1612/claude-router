---
phase: 32.1-phase-32-review-closure
plan: 04
subsystem: testing
tags: [performance, hot-path, resolve-first, cache, node-test]

# Dependency graph
requires:
  - phase: 32.1-02
    provides: resolver wiring on the live inspectDecision production hot path
provides:
  - End-to-end warm performance coverage for route scoring, cache, guards, resolve, and render
  - Independently named pure resolveSlashRoute helper benchmark
affects: [ROUTE-05, PARITY-03, phase-32.1 release verification]

# Tech tracking
tech-stack:
  added: []
  patterns: [stdlib subprocess perf fixture, temporary cache isolation, render-reach assertion]

key-files:
  created: []
  modified:
    - tests/router.perf.test.mjs

key-decisions:
  - "Use the live hook in a stdlib-only subprocess with in-memory manifest/mode-map fixtures so the tight loop measures shipped decision logic without re-reading the 208KB manifest each iteration."
  - "Use a fresh temporary cache path: the first warm call proves scoring, guards, render, and cache population; 30 measured warm calls prove cached render latency without home-directory writes."
  - "Keep the pure resolver benchmark separate and unchanged apart from its helper-level title."

patterns-established:
  - "Hot-path perf fixtures must assert non-empty final_injected_context and resolved route identity before accepting timing data."
  - "Runtime-split fixtures keep Claude's gsd-debug and Codex's systematic-debugging candidates isolated through runtime_commands."

requirements-completed: [ROUTE-05, PARITY-03]

coverage:
  - id: D1
    description: "The live inspectDecision resolve-first path reaches route selection and render, proves cache warmup, and sustains warm p95 below 40ms with max below 100ms across 30 measured iterations."
    requirement: PARITY-03
    verification:
      - kind: e2e
        ref: "node --test tests/router.perf.test.mjs#e2e: inspectDecision resolve-first hot path reaches render within budget"
        status: pass
    human_judgment: false
  - id: D2
    description: "The pure resolveSlashRoute helper benchmark remains a separate budget gate, alongside the existing trivial pass-through gate."
    requirement: ROUTE-05
    verification:
      - kind: unit
        ref: "node --test tests/router.perf.test.mjs#helper: pure resolveSlashRoute stays within budget"
        status: pass
    human_judgment: false

# Metrics
duration: 2min
completed: 2026-08-01
status: complete
---

# Phase 32.1 Plan 04: Real hot-path performance gate Summary

**Live resolve-first performance coverage now measures route scoring, cache, guards, runtime resolution, and rendered injection, while retaining a distinct resolver microbenchmark.**

## Performance

- **Duration:** 2 min
- **Started:** 2026-08-01T19:00:06+01:00
- **Completed:** 2026-08-01T19:01:59+01:00
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments

- Added a live-hook subprocess fixture with a runtime-split manifest, schema-v4 mode-map, temporary cache, warmup assertions, render proof, and 30 measured iterations.
- Gated the real hot path at warm p95 < 40ms and max < 100ms while keeping stdout pure timing JSON.
- Relabeled the direct `resolveSlashRoute` test as a helper-level gate without changing its timing math; trivial pass-through gates remain intact.

## Task Commits

Each task was committed atomically:

1. **Task 1: End-to-end hot-path perf fixture** - RED `e5fcefb`, GREEN `bf52146`
2. **Task 2: Retain the cheap resolver-helper benchmark as a separate gate** - `af061ed`

**Plan metadata:** skipped (commit_docs disabled in `.planning/config.json`).

## Files Created/Modified

- `tests/router.perf.test.mjs` - End-to-end hot-path timing fixture and separately named helper benchmark.

## Decisions Made

- The fixture imports the live hook and passes in-memory manifest/mode-map objects, which keeps the measured loop focused on the shipped route path while still exercising scoring, cache lookup, guards, resolution, and render.
- A fresh temporary cache path prevents home-directory writes and proves the initial cache population before measuring warm cache-backed render latency.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None. No package installs or authentication gates were needed.

## User Setup Required

None - no external service configuration required.

## Verification

- `node --test tests/router.perf.test.mjs` — 5/5 passing.
- `node --test tests/router.hot-path-resolve.integration.test.mjs tests/router.mjs.snapshot.diff.test.mjs` — 8/8 passing.
- `git diff --check` — clean.

## Next Phase Readiness

Phase 32.1 plan 04 is complete; the phase's real hot-path perf closure is ready for phase-level verification and audit.

---
*Phase: 32.1-phase-32-review-closure*
*Completed: 2026-08-01*

## Self-Check: PASSED

- Summary file exists at the required phase path.
- Task commits `e5fcefb`, `bf52146`, and `af061ed` exist in git history.
- Perf, hot-path integration, snapshot parity, and diff-check verification passed.

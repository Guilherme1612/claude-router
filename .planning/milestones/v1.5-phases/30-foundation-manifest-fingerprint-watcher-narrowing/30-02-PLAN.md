---
phase: 30-foundation-manifest-fingerprint-watcher-narrowing
plan: 2
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - src/lifecycle/router-lifecycle.mjs
  - tests/router.registry-watcher.test.mjs
  - tests/router.plugins-fingerprint.test.mjs
autonomous: true
requirements: [INVC-04]
estimate:
  tokens: 45000
  raw_tokens: 22500
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Watcher scans ignore noise files (sqlite/WAL content+session DBs, plugin-catalog caches): a change under an ignored prefix never marks a root dirty"
    - "plugins/installed_plugins.json is the authoritative plugin add/remove signal: adding or removing a plugin entry changes manifest_fingerprint"
    - "Plugin-only churn (plugin-catalog cache, marketplace cache edits) never changes the fingerprint and never dirties watcher roots"
    - "installed_plugins timestamps (installed_at/lastUpdated/installPath) are excluded — a timestamp-only edit leaves the fingerprint unchanged"
    - "The ignore list stays prefix-specific: installed_plugins.json itself is never ignored"
  artifacts:
    - src/lifecycle/router-lifecycle.mjs (noise ignore prefixes on claude_global + codex_home roots)
    - tests/router.registry-watcher.test.mjs (noise-event and installed_plugins-event dirty-roots tests)
    - tests/router.plugins-fingerprint.test.mjs (plugin add/remove fingerprint sensitivity + timestamp-stability)
  key_links:
    - "router-lifecycle.mjs roots.ignoredRelativePaths → watcher.mjs matched filter (L318-329) → markDirty(matched)"
    - "plugins/installed_plugins.json → build-manifest.mjs parse (L267-281) → computeCompositeEpoch → manifest_fingerprint"
  prohibitions:
    - statement: "The ignore list must never cover plugins/installed_plugins.json — it is the authoritative plugin signal"
      status: unverified
      flagged: true
    - statement: "installed_plugins installed_at, lastUpdated, and installPath must never be fingerprint inputs"
      status: unverified
      flagged: true
    - statement: "No watcher core change is allowed — narrowing is a roots-config change only"
      status: unverified
      flagged: true
---

<objective>
Narrow watcher scan noise and establish `installed_plugins.json` as the authoritative plugin add/remove signal. INVC-04: sqlite/WAL and plugin-catalog cache churn must never dirty roots or change the fingerprint, while a real plugin add/remove bumps the fingerprint.

Purpose: The fingerprint epoch from Plan 30-01 only earns its keep if the watcher stops reconciling on noise — measured reconcile churn is the reason this phase exists. The `ignoredRelativePaths` prefix filter is already live in the watcher (watcher.mjs:318-329); work is purely a roots-config change in router-lifecycle.mjs:430-436 plus tests.
Output: Noise ignore prefixes on both watch roots; watcher dirty-roots tests; a plugin-fingerprint sensitivity test proving installed_plugins.json add/remove changes the fingerprint while churn and timestamps do not.
</objective>

<execution_context>
@/Users/guilherme/.claude/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-foundation-manifest-fingerprint-watcher-narrowing/30-PATTERNS.md (router-lifecycle.mjs analog L425-437; watcher matched filter L318-329; build-manifest installed_plugins parse L267-281)
@.planning/phases/30-foundation-manifest-fingerprint-watcher-narrowing/30-CONTEXT.md

# Real noise paths observed on the live setup (2026-07-31):
#   ~/.claude/context-mode/content/*.db, ~/.claude/context-mode/sessions/*.db (+WAL/SHM)
#   ~/.claude/plugins/plugin-catalog-cache.json, ~/.claude/plugins/cache/, ~/.claude/plugins/data/,
#   ~/.claude/plugins/marketplaces/, ~/.claude/plugins/known_marketplaces.json
# installed_plugins.json stays visible (authoritative signal) — the ignore list must be prefix-specific.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add noise ignore prefixes to watcher roots — sqlite/WAL + plugin-catalog caches</name>
  <files>src/lifecycle/router-lifecycle.mjs, tests/router.registry-watcher.test.mjs</files>
  <read_first>
    src/lifecycle/router-lifecycle.mjs — controllerConfig roots array L425-437
    src/registry/watcher.mjs — matched/ignored prefix filter L318-329 (read-only reference, NOT modified)
    tests/router.registry-watcher.test.mjs — dirty-roots test seams L69-86 and L223-238
  </read_first>
  <action>
    1. In src/lifecycle/router-lifecycle.mjs, extend BOTH the `claude_global` and `codex_home` root entries' `ignoredRelativePaths` (currently `['router']` at L430-431) with these prefix values — keep the existing `'router'` prefix:
       - `'context-mode'` (sqlite content/session DBs + WAL/SHM companions)
       - `'plugins/plugin-catalog-cache.json'`
       - `'plugins/known_marketplaces.json'`
       - `'plugins/cache'`
       - `'plugins/data'`
       - `'plugins/marketplaces'`
       Do NOT add a bare `'plugins'` prefix — `plugins/installed_plugins.json` must remain visible to the watcher. Do NOT touch project roots (they carry includeRelativePaths only).

    2. In tests/router.registry-watcher.test.mjs, add a test that constructs the same roots shape as router-lifecycle.mjs (claude_global with the full new ignoredRelativePaths list) and drives the matched filter: an event on `context-mode/content/foo.db`, `context-mode/sessions/x.db-wal`, `plugins/plugin-catalog-cache.json`, `plugins/cache/context-mode/x/y/1.0.0/file`, and `plugins/data/whatever` must NOT mark claude_global dirty; an event on `plugins/installed_plugins.json` MUST mark claude_global dirty. Follow the existing dirty-roots test seams (mocked scan + assert on the dirty/roots result).

    3. Assert in the test that the claude_global ignoredRelativePaths exactly equals the required prefix list (no accidental coverage of `plugins/installed_plugins.json`).
  </action>
  <verify>
    node --test tests/router.registry-watcher.test.mjs
  </verify>
  <acceptance_criteria>
    - claude_global and codex_home ignoredRelativePaths each contain 'context-mode', 'plugins/plugin-catalog-cache.json', 'plugins/known_marketplaces.json', 'plugins/cache', 'plugins/data', 'plugins/marketplaces' plus the existing 'router'
    - Noise events (context-mode DBs/WAL, plugin-catalog cache, plugins/cache, plugins/data, plugins/marketplaces) do not mark claude_global dirty
    - An event on plugins/installed_plugins.json DOES mark claude_global dirty
  </acceptance_criteria>
  <done>
    Watcher roots ignore every observed sqlite/WAL and plugin-catalog noise path while keeping installed_plugins.json visible; dirty-roots tests prove both halves.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: installed_plugins.json is the authoritative plugin signal — add/remove bumps fingerprint, churn and timestamps do not</name>
  <files>tests/router.plugins-fingerprint.test.mjs</files>
  <read_first>
    build-manifest.mjs — installed_plugins parse L267-281, atomic write L541-545 (read-only reference)
    tests/router.build-manifest.test.mjs — runBuilder spawn + env-var HOME override seam L13-45 (replicate the helper locally in the new test file; do NOT import across test files)
    src/registry/fingerprint.mjs — computeCompositeEpoch (from Plan 30-01)
  </read_first>
  <behavior>
    - Test 1: building a temp home twice with identical installed_plugins.json emits an identical manifest_fingerprint
    - Test 2: appending a new plugin entry to installed_plugins.json then rebuilding emits a DIFFERENT manifest_fingerprint (plugin add bumps the epoch)
    - Test 3: removing that plugin entry then rebuilding emits a fingerprint different from the added state (plugin remove bumps the epoch)
    - Test 4: editing only installed_at/lastUpdated/installPath of an existing plugin then rebuilding emits the SAME manifest_fingerprint (timestamps excluded)
  </behavior>
  <action>
    Create tests/router.plugins-fingerprint.test.mjs with a local `runBuilder(root, extraEnv)` helper replicating the spawn seam from tests/router.build-manifest.test.mjs L13-45 (NODE = process.execPath, BUILDER = ../build-manifest.mjs, ROUTER_CLAUDE_HOME + ROUTER_AGENTS_SKILLS_DIR env overrides). Seed a temp HOME with `.claude/plugins/installed_plugins.json` containing one plugin record shaped like the real schema (`{ plugins: { 'context-mode@context-mode': [{ scope, installPath, version, installedAt, lastUpdated }] } }`).

    Test 2 add path: append a second key `{ 'caveman@caveman': [...] }` and rebuild — assert `manifest_fingerprint` changed.
    Test 3 remove path: delete the second key and rebuild — assert the fingerprint equals neither the added state nor a hypothetical unchanged baseline (strict: differs from the added fingerprint).
    Test 4 timestamp-exclusion path: keep the same plugin set but change `installedAt`/`lastUpdated`/`installPath` values on the existing record and rebuild — assert `manifest_fingerprint` is byte-identical to the pre-edit fingerprint.

    Read `manifest_fingerprint` from the emitted `OUT` file (join(root, '.claude', 'router', 'claude-inventory-manifest.json')) after each build. Use a fresh temp dir per test (withTempDir pattern) so no cross-test state leaks.
  </action>
  <verify>
    node --test tests/router.plugins-fingerprint.test.mjs
  </verify>
  <acceptance_criteria>
    - Plugin add and plugin remove each change manifest_fingerprint
    - A timestamp-only (installedAt/lastUpdated/installPath) edit leaves manifest_fingerprint byte-identical
    - Identical installed_plugins.json → identical fingerprint (determinism holds for the plugin dimension)
  </acceptance_criteria>
  <done>
    installed_plugins.json add/remove drives the fingerprint epoch and plugin metadata churn does not — INVC-04's authoritative-signal property proven at the builder level.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → watcher | FS events under ~/.claude are untrusted churn; the roots filter decides what reconciles |
| installed_plugins.json → manifest | plugin identity is the authoritative add/remove signal feeding the fingerprint |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-05 | Denial of Service | watcher reconcile / markDirty | high | mitigate | sqlite/WAL + plugin-catalog prefixes added to ignoredRelativePaths; noise events never dirty roots (test-verified); no watcher core change |
| T-30-06 | Tampering | plugins/installed_plugins.json | medium | accept | Local user-owned file; a malicious edit only triggers a deterministic rebuild + fingerprint change, never a hot-path write |
| T-30-07 | Denial of Service | future unknown noise file | low | accept | Ignore-list is additive prefix config; new noise patterns are config edits, fail-open, no core change |
| T-30-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase — stdlib-only |
</threat_model>

<verification>
- `node --test tests/router.registry-watcher.test.mjs` — noise events do not dirty roots; installed_plugins.json does
- `node --test tests/router.plugins-fingerprint.test.mjs` — plugin add/remove bumps fingerprint; timestamp-only edits do not
- grep gate: no occurrence of `ignoredRelativePaths: ['router']` without the new noise prefixes in router-lifecycle.mjs roots
</verification>

<success_criteria>
- INVC-04: watcher scans exclude noise files (sqlite/WAL, plugin-catalog caches); installed_plugins.json is the authoritative plugin add/remove signal; plugin add/remove changes the fingerprint; plugin-only churn never dirties roots
</success_criteria>

## Artifacts this phase produces

- `ignoredRelativePaths` noise prefixes on `claude_global` + `codex_home` roots in `src/lifecycle/router-lifecycle.mjs` (context-mode, plugins/plugin-catalog-cache.json, plugins/known_marketplaces.json, plugins/cache, plugins/data, plugins/marketplaces; `plugins/installed_plugins.json` deliberately excluded)
- `tests/router.plugins-fingerprint.test.mjs` — new test file (local runBuilder helper; plugin add/remove/timestamp-exclusion fingerprint tests)
- Watcher dirty-roots tests in `tests/router.registry-watcher.test.mjs` (noise-event + installed_plugins-event assertions)

<output>
Create `.planning/phases/30-foundation-manifest-fingerprint-watcher-narrowing/30-02-SUMMARY.md` when done
</output>

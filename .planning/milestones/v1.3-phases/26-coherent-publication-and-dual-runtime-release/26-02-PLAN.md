---
phase: 26-coherent-publication-and-dual-runtime-release
plan: 02
type: execute
wave: 1
depends_on: [26-01]
files_modified:
  - src/registry/build.mjs
  - src/prompt/publish-index.mjs
  - src/prompt/compile-index.mjs
  - src/context/prompt-route.mjs
  - tests/router.phase26-tuple.test.mjs
  - tests/router.phase26-hot-path.test.mjs
autonomous: true
requirements: [REL-01, REL-02, REL-03]
must_haves:
  truths:
    - "One immutable tuple contains registry, contracts, relationships, intent policy, workflow routes, health policy, and suggestion reference."
    - "Prompt submission reads only a bounded precompiled projection and performs no mutation or analysis."
    - "Incomplete, mixed, oversized, symlinked, or hash-invalid tuple members cannot gain dispatch authority."
  artifacts:
    - "src/prompt/publish-index.mjs"
    - "src/prompt/compile-index.mjs"
    - "src/context/prompt-route.mjs"
  key_links:
    - "buildFullRegistry output feeds publishCompiledIndex complete member bytes."
    - "loadCompiledIndex exposes one bounded route projection to routeContextPrompt."
---

<objective>
Extend the existing compiled-index publisher and loader into the complete v1.3 tuple and make the real prompt path a bounded read-only consumer.

Purpose: Activate every decision sibling under one identity without introducing a second publisher or prompt router.
Output: Complete tuple publication/validation plus a pure prompt-time projection.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-coherent-publication-and-dual-runtime-release/26-CONTEXT.md
@.planning/phases/26-coherent-publication-and-dual-runtime-release/26-PATTERNS.md
@.planning/phases/26-coherent-publication-and-dual-runtime-release/26-VALIDATION.md
@src/prompt/publish-index.mjs
@src/prompt/compile-index.mjs
@src/context/prompt-route.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Publish and validate every tuple sibling under one content identity</name>
  <files>src/registry/build.mjs, src/prompt/publish-index.mjs, src/prompt/compile-index.mjs, tests/router.phase26-tuple.test.mjs</files>
  <behavior>
    - Every required projection is bounded, canonical, hash-listed, immutable, and included in tuple identity.
    - Intent policy, health policy, and suggestion reference changes change tuple identity.
    - Partial, mixed, unknown, symlinked, oversized, substituted, or hash-invalid members are rejected.
  </behavior>
  <action>Extend `publishCompiledIndex()` and `loadCompiledIndex()` only. Have the background build provide canonical bounded projections for registry/contracts, relationships, intent policy, workflow routes and local equivalents, active health policy, and a bounded suggestion reference. Derive the tuple ID from every member hash; write all immutable siblings and the manifest before replacing the existing active pointer. Validate exact membership, schemas, hashes, bounds, and same tuple ID before exposing dispatch. Optional suggestion content may project as absent only when the verified tuple explicitly records that bounded absence; it must not become an independently versioned startup pointer.</action>
  <verify>
    <automated>rtk node --test tests/router.phase26-tuple.test.mjs</automated>
  </verify>
  <done>The production publish/load round trip accepts only a complete same-version tuple and all tuple RED cases are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reduce the real prompt route to one bounded tuple projection</name>
  <files>src/context/prompt-route.mjs, src/prompt/compile-index.mjs, tests/router.phase26-hot-path.test.mjs</files>
  <behavior>
    - Real prompt routing performs bounded reads only from the active compiled tuple.
    - It performs no capsule write, startup-pointer read, discovery, manifest scan, health/history calculation, graph traversal, network call, or model call.
    - Corrupt optional recommendation data stays silent while verified dispatch or known-good routing continues.
  </behavior>
  <action>Refactor `routeContextPrompt` to consume the loader's already-bounded projection. Remove prompt-time capsule mutation and the separate startup-pointer read; keep any refresh or suggestion compilation in background/admin callers. Preserve the existing 2 KiB injection contract and fail-open optional recommendation behavior while dispatch integrity remains fail-closed. Test the deployed-hook integration and trap forbidden calls rather than relying only on import greps.</action>
  <verify>
    <automated>rtk node --test tests/router.phase26-hot-path.test.mjs tests/router.context-prompt-integration.test.mjs</automated>
  </verify>
  <done>The real prompt path is read-only, bounded, and routes through verified active/known-good tuple data with optional advice failing open.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| background builder -> immutable tuple | Derived decision artifacts gain routing authority only after verification. |
| tuple files -> prompt route | Local files are untrusted until exact membership, hash, schema, and bounds checks pass. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-03 | Tampering | tuple siblings/manifest | high | mitigate | Content-derived identity, exact manifest, hash/bounds/symlink validation, pointer replacement last. |
| T-26-04 | Denial of Service | prompt projection | high | mitigate | Fixed read and context bounds; optional projection failure stays silent. |
| T-26-05 | Elevation of Privilege | unverified tuple | critical | mitigate | Expose dispatch only from complete verified active or known-good tuple. |
</threat_model>

<verification>
Run the tuple and hot-path gates serially, then the existing compiled-index schema tests.
</verification>

<success_criteria>
REL-01 through REL-03 behavioral tests pass using the existing publisher/loader/router, with no duplicate publication or prompt path.
</success_criteria>

<output>
Create `.planning/phases/26-coherent-publication-and-dual-runtime-release/26-02-SUMMARY.md` when done.
</output>

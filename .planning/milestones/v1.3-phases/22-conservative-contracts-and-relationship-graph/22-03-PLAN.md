---
phase: 22-conservative-contracts-and-relationship-graph
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/router.relationships.test.mjs
  - src/registry/relationships.mjs
  - src/registry/reconcile.mjs
autonomous: true
requirements: [CONT-07]
nyquist_compliant: true
must_haves:
  truths:
    - "Users can distinguish exactly eight relationship types without lexical similarity becoming authority."
    - "Every active edge has typed evidence, provenance, confidence, freshness, endpoint validation, and deterministic state."
    - "Ambiguous, conflicting, dangling, and invalidated candidates remain inspectable but inactive."
  artifacts:
    - path: "src/registry/relationships.mjs"
      provides: "Strict typed relationship derivation, validation, canonicalization, and invalidation"
    - path: "tests/router.relationships.test.mjs"
      provides: "Eight-type, adversarial, permutation, cycle, and lifecycle oracle"
  key_links:
    - from: "src/registry/relationships.mjs"
      to: "src/registry/reconcile.mjs"
      via: "relationship endpoint references enter existing transitive invalidation closure"
---

<objective>
Create a strict, deterministic relationship graph for CONT-07.

Purpose: Users need meaningful typed relationships while similarity-only, ambiguous, and stale links remain visibly inactive.
Output: One flat relationship module, reconciliation integration, and a complete Wave 0 edge-state oracle.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-conservative-contracts-and-relationship-graph/22-CONTEXT.md
@.planning/phases/22-conservative-contracts-and-relationship-graph/22-RESEARCH.md
@.planning/phases/22-conservative-contracts-and-relationship-graph/22-PATTERNS.md
@.planning/phases/21-authoritative-personalized-inventory/21-03-SUMMARY.md

<interfaces>
Use Phase 21 stable capability IDs as edge endpoints.
Reuse `stableStringify` from `src/registry/schema.mjs` and the generic canonical reference/invalidation closure in `src/registry/reconcile.mjs`.
Publish plain ESM functions and data; do not add a graph library, class hierarchy, fuzzy matcher, or model call.
</interfaces>
</context>

## Artifacts this plan creates/modifies

- `src/registry/relationships.mjs` — strict edge schema and typed-evidence validation for substitute, variant, prerequisite, composition, conflict, fallback, implementation, and alias.
- `src/registry/reconcile.mjs` — relationship references participate in endpoint and transitive invalidation.
- `tests/router.relationships.test.mjs` — type, evidence, inactive-candidate, canonical order, cycle, dangling endpoint, and mutation coverage.

<tasks>

<task type="auto">
  <name>Task 1: Establish the Wave 0 typed-relationship oracle</name>
  <files>tests/router.relationships.test.mjs</files>
  <read_first>
    - .planning/phases/22-conservative-contracts-and-relationship-graph/22-CONTEXT.md (relationship classification decisions)
    - .planning/phases/22-conservative-contracts-and-relationship-graph/22-VALIDATION.md (CONT-07 matrices)
    - tests/helpers/inventory-fixture.mjs (four profiles and mutations)
    - tests/router.registry-reconcile.test.mjs (generic reference graph and callback ordering)
    - src/registry/map.mjs (evidence and confidence conventions)
    - src/registry/reconcile.mjs (canonical edge/reference invalidation)
  </read_first>
  <action>Create tests for only the eight CONT-07 edge types. For each type, cover sufficient type-specific evidence, insufficient evidence, conflicting evidence, stale evidence, missing source/target, disabled/removed/replaced endpoint, and deterministic confidence/freshness/validation state. Assert name or lexical similarity can create only an inactive candidate and can never establish alias, substitute, or equivalence authority. Keep ambiguous/conflicting candidates inspectable with stable reasons. Cover malformed cycles and dangling endpoints without recursion failure, bounded collections, transitive invalidation, and byte equality under record/evidence/edge/reason permutations.</action>
  <acceptance_criteria>
    - Type assertion: the active schema accepts exactly the eight required relationship types.
    - Authority assertion: lexical similarity alone never activates any edge.
    - Lifecycle assertion: changed or missing endpoints invalidate direct and dependent edges before consumers observe them.
    - RED assertion marker: intentionally failing names contain `[phase22-red:relationships]` and no unrelated failure is accepted.
  </acceptance_criteria>
  <verify>
    <automated>rtk node -e "const {spawnSync}=require('node:child_process');const r=spawnSync(process.execPath,['--test','tests/router.relationships.test.mjs'],{encoding:'utf8'});const out=(r.stdout||'')+(r.stderr||'');const failed=[...out.matchAll(/^\s*not ok \d+ - (.+)$/gm)].map(m=&gt;m[1]);process.exit(r.status!==0&amp;&amp;failed.length&gt;0&amp;&amp;failed.every(name=&gt;name.includes('[phase22-red:relationships]'))?0:1)"</automated>
  </verify>
  <done>The eight-type graph, evidence thresholds, inactive candidates, and invalidation semantics are fully specified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement strict typed edges and invalidation</name>
  <files>src/registry/relationships.mjs, src/registry/reconcile.mjs, tests/router.relationships.test.mjs</files>
  <read_first>
    - tests/router.relationships.test.mjs (Task 1 oracle)
    - src/registry/reconcile.mjs (canonicalizeReferences and invalidation closure)
    - src/registry/map.mjs (canonical collections, evidence, policy versions, reason codes)
    - src/registry/schema.mjs (stableStringify and portable validation conventions)
    - .planning/phases/22-conservative-contracts-and-relationship-graph/22-PATTERNS.md (`relationships.mjs` analogs)
  </read_first>
  <behavior>
    - Active edges have valid live endpoints and sufficient type-specific evidence.
    - Invalid or ambiguous candidates stay inspectable and inactive.
    - Edge output and invalidation order are byte deterministic.
  </behavior>
  <action>Create `src/registry/relationships.mjs` with a closed eight-value type set, bounded strict edge validation, type-specific evidence requirements, integer confidence, freshness, validation state, stable reason codes, and canonical source/target ordering. Separate active edges from inspectable inactive candidates. Treat lexical similarity only as candidate evidence and never as sufficient authority. Reject self-edges where the type cannot meaningfully support them, detect malformed cycles iteratively or with a bounded visited set, and fail closed on dangling endpoints. Feed active and candidate endpoint references into the existing reconciliation closure so direct and transitive edges invalidate before callbacks when endpoints change, disable, replace, or disappear. Keep the implementation flat and functional; no general graph engine.</action>
  <acceptance_criteria>
    - Schema assertion: unknown relationship types and malformed endpoints are inactive with stable reasons.
    - Evidence assertion: each active type passes its own evidence rule; similarity-only cases remain inactive.
    - Safety assertion: malformed cycles and dangling endpoints terminate safely and cannot authorize an edge.
    - Determinism assertion: canonical graph bytes and invalidation results ignore input ordering.
  </acceptance_criteria>
  <verify>
    <automated>rtk node --test tests/router.relationships.test.mjs tests/router.registry-reconcile.test.mjs tests/router.inventory-mutations.test.mjs</automated>
  </verify>
  <done>Router has a deterministic, conservative eight-type relationship graph integrated with existing lifecycle invalidation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| relationship evidence → active edge | Weak or malicious evidence attempts to establish semantic authority |
| endpoint lifecycle → graph state | Removed/replaced capabilities may leave stale direct or transitive edges |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-09 | Spoofing | alias/substitute inference | high | mitigate | Type-specific evidence rules and tests make lexical similarity insufficient for activation. |
| T-22-10 | Tampering | edge endpoint state | high | mitigate | Exact stable-ID endpoint validation plus direct/transitive reconciliation invalidation. |
| T-22-11 | Denial of Service | cycles/oversized graphs | medium | mitigate | Bounded collections, deterministic traversal, visited-set cycle handling, and adversarial tests. |
| T-22-12 | Repudiation | inactive relationship decisions | medium | mitigate | Preserve evidence, provenance, confidence, freshness, validation state, and stable reason codes. |
| T-22-SC | Tampering | package installation | high | mitigate | No package installation task exists. |
</threat_model>

<verification>
- `rtk node --test tests/router.relationships.test.mjs tests/router.registry-reconcile.test.mjs tests/router.inventory-mutations.test.mjs`
</verification>

<success_criteria>
- CONT-07 passes for every required type and adversarial state.
- Similarity never becomes relationship authority.
- Endpoint invalidation and canonical graph bytes remain deterministic.
</success_criteria>

<output>
Create `.planning/phases/22-conservative-contracts-and-relationship-graph/22-03-SUMMARY.md` when done.
</output>

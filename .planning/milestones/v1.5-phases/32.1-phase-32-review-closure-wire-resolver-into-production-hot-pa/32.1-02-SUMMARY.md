---
phase: 32.1-phase-32-review-closure
plan: 02
subsystem: routing
tags: [hot-path, resolve-first, runtime-parity, stable-sort, node-test]

# Dependency graph
requires:
  - phase: 32.1-01
    provides: runtime-aware command indexes and active-runtime attribution
  - phase: 32-intent-first-routing-mode-map-schema-v4-guard-hole-closure
    provides: resolveSlashRoute and resolve-aware route validation
provides:
  - Resolver-sourced slash emission on the production inspectDecision path
  - Stable declared-order tie handling for equal-weight resolve candidates
  - Runtime-isolated hot-path integration coverage for emitted additionalContext
affects: [phase-32.1, release-gate-verification, ROUTE-01, ROUTE-03, PARITY-04]

# Tech tracking
tech-stack:
  added: []
  patterns: [raw-manifest runtime resolution, bounded native fallback rendering, stable weight-only sorting]

key-files:
  created:
    - tests/router.hot-path-resolve.integration.test.mjs
  modified:
    - tests/router.mjs.snapshot
    - /Users/guilherme/.claude/hooks/router.mjs

key-decisions:
  - "Resolve slash routes from the raw manifest immediately before guards so runtime-specific presence controls the emitted mode without changing route identity or recommendations."
  - "Propagate the resolver tier to the emitted route so near-ties cannot remain high-confidence or enter the high-tier cache path."
  - "Use stable weight-only sorting because the insertion-ordered candidate Map already carries the authored mode and resolve-list order."
  - "Render only the resolver's single fixed generic native fallback line when a high-tier route has no resolvable slash candidate."

patterns-established:
  - "Cross-runtime hot-path tests use fresh Node subprocesses because ROUTER_RUNTIME is fixed at module load."
  - "Quarantined candidates are observable in decision state/telemetry but are never copied into injected context."

requirements-completed: [ROUTE-01, ROUTE-03, PARITY-04]

coverage:
  - id: D1
    description: "inspectDecision emits the resolver-selected local slash for Claude and Codex, never a foreign or dead candidate."
    requirement: ROUTE-01
    verification:
      - kind: integration
        ref: "node --test tests/router.hot-path-resolve.integration.test.mjs"
        status: pass
      - kind: integration
        ref: "node --test tests/router.phase32-cross-runtime.test.mjs"
        status: pass
    human_judgment: false
  - id: D2
    description: "Equal-weight resolve candidates retain declared order while differing weights and mode priority remain intact."
    requirement: ROUTE-01
    verification:
      - kind: unit
        ref: "tests/router.hot-path-resolve.integration.test.mjs#resolver keeps declared order for equal-weight resolve members"
        status: pass
      - kind: unit
        ref: "node --test tests/router.resolve-tie-lint.test.mjs"
        status: pass
    human_judgment: false
  - id: D3
    description: "Unresolvable slash routes pass through, near-ties render at medium tier, and generic fallback output is bounded and non-fabricated."
    requirement: ROUTE-03
    verification:
      - kind: integration
        ref: "node --test tests/router.hot-path-resolve.integration.test.mjs tests/router.schema-v4-routing.test.mjs"
        status: pass
      - kind: integration
        ref: "node --test tests/router.failopen.test.mjs"
        status: pass
    human_judgment: false
  - id: D4
    description: "Snapshot and live production hook remain byte-identical after both snapshot edits."
    requirement: PARITY-04
    verification:
      - kind: other
        ref: "cmp tests/router.mjs.snapshot /Users/guilherme/.claude/hooks/router.mjs"
        status: pass
      - kind: integration
        ref: "node --test tests/router.mjs.snapshot.diff.test.mjs"
        status: pass
    human_judgment: false

# Metrics
duration: 10min
completed: 2026-08-01
status: complete
---

# Phase 32.1 Plan 02: Production hot-path resolver wiring Summary

**Production slash injection now emits the active runtime's resolved capability, preserves authored tie order, and fails open on dead candidates.**

## Performance

- **Duration:** 10 min
- **Started:** 2026-08-01T17:40:01Z
- **Completed:** 2026-08-01T17:50:08Z
- **Tasks:** 3
- **Files modified:** 3 planned artifacts

## Accomplishments

- Routed slash routes in `inspectDecision()` through `resolveSlashRoute()` before guards and render, including runtime-specific resolution, quarantine attribution, tier propagation, dead-route suppression, and fixed native fallback rendering.
- Replaced lexicographic tie-breaking with stable weight-only sorting so equal-weight resolve candidates follow declared order.
- Added fresh-subprocess integration coverage for Claude/Codex emitted `final_injected_context`, quarantine non-injection, near-tie medium downgrade, silent unresolved routes, fallback safety, and mirror lockstep.

## Task Commits

Each task was committed atomically, including TDD RED/GREEN commits:

1. **Task 1: Wire resolveSlashRoute into inspectDecision production hot path** - RED `8e77f2b`, GREEN `c29cd3f`
2. **Task 2: Preserve declared resolve-list order for equal weights** - RED `d50f5bc`, GREEN `aa5a2d1`
3. **Task 3: Integration test — real inspectDecision hot path** - `027ab47`

**Plan metadata:** skipped (commit_docs disabled in `.planning/config.json`; `.planning/` is gitignored).

## Files Created/Modified

- `tests/router.mjs.snapshot` - Resolver-backed hot-path route construction, fallback rendering, state/telemetry attribution, and stable candidate ordering.
- `/Users/guilherme/.claude/hooks/router.mjs` - Byte-identical live production mirror.
- `tests/router.hot-path-resolve.integration.test.mjs` - Runtime-isolated real `inspectDecision` and resolver ordering integration coverage.

## Decisions Made

- Raw manifests are passed to the resolver at the hot-path seam so `runtime_commands[RUNTIME]` remains authoritative for the emitted slash.
- Resolver tier is copied onto the route; this makes the near-tie `med` result visible to `formatInjection()` and prevents high-tier caching.
- Generic fallback rendering is capped to one fixed line and carries no slash mode, preventing fabricated framework commands.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Added the missing generic fallback render seam**
- **Found during:** Task 1 (hot-path resolver wiring)
- **Issue:** `resolveSlashRoute()` already returned `fallback_lines`, but `formatInjection()` had no path to render them; high-tier empty resolution would otherwise silently drop the required fallback.
- **Fix:** Render at most the resolver's first fixed fallback line with an empty mode and native-capability reasoning.
- **Files modified:** `tests/router.mjs.snapshot`, `/Users/guilherme/.claude/hooks/router.mjs`
- **Verification:** Hot-path integration plus schema-v4 and fail-open suites passed.
- **Committed in:** `c29cd3f`

**Total deviations:** 1 auto-fixed (Rule 2 missing critical functionality).
**Impact on plan:** Required fallback behavior is now reachable without adding dependencies or changing the route architecture.

## Issues Encountered

- The second live-hook mirror copy was blocked by the sandbox and was completed through the explicitly scoped approved file-sync command; `cmp` and the snapshot-diff test both pass afterward.
- No package installs, authentication gates, or unrelated source changes were needed.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- ROUTE-01, ROUTE-03, and PARITY-04 hot-path closure is implemented and verified.
- The remaining incomplete phase plan is `32.1-04-PLAN.md`; no blocker was found for continuing phase execution.

---
*Phase: 32.1-phase-32-review-closure*
*Completed: 2026-08-01*

## Self-Check: PASSED

- Summary file exists at the required phase path.
- Task commits `8e77f2b`, `c29cd3f`, `d50f5bc`, `aa5a2d1`, and `027ab47` exist in git history.
- Snapshot and live hook are byte-identical; the plan-scoped 69-test regression sweep passed.

---
phase: 21-authoritative-personalized-inventory
plan: 03
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - tests/router.inventory-mutations.test.mjs
  - tests/router.registry-diff.test.mjs
  - tests/router.registry-reconcile.test.mjs
  - src/registry/identity.mjs
  - src/registry/diff.mjs
  - src/registry/reconcile.mjs
autonomous: true
requirements: [DISC-03, DISC-05]
must_haves:
  truths:
    - "Add, edit, rename, move, disable, replace, dependency-loss, and removal mutations produce deterministic identity and lifecycle outcomes."
    - "Rename/move continuity occurs only through a validated stable ID or one unique removed-to-added exact fingerprint pair."
    - "Removed, disabled, replaced, or dependency-unhealthy targets invalidate every transitive reference before mapping or activation callbacks."
  artifacts:
    - path: "src/registry/identity.mjs"
      provides: "Path-aware scope-separated identity and exact continuity evidence"
    - path: "src/registry/diff.mjs"
      provides: "Deterministic mutation classification and unique exact-fingerprint pairing"
    - path: "src/registry/reconcile.mjs"
      provides: "Versioned reference graph and candidate-local invalidation closure"
    - path: "tests/router.inventory-mutations.test.mjs"
      provides: "D-19 mutation and invalidation oracle"
  key_links:
    - from: "src/registry/diff.mjs"
      to: "src/registry/reconcile.mjs"
      via: "lifecycle seeds removal/replacement/disablement invalidation"
    - from: "src/registry/reconcile.mjs"
      to: "src/registry/watcher.mjs"
      via: "fully invalidated candidate is returned before mapper callback"
---

<objective>
Make identity continuity and downstream invalidation authoritative inside one candidate transaction, implementing D-09 through D-13 and DISC-03/DISC-05.

Purpose: Prevent moves from losing valid identity while ensuring replacements, removals, disablement, and dependency loss cannot leave stale trusted relationships or compiled routes.
Output: Exact continuity rules, deterministic reference closure, and complete mutation-sequence tests.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-authoritative-personalized-inventory/21-CONTEXT.md
@.planning/phases/21-authoritative-personalized-inventory/21-RESEARCH.md
@.planning/phases/21-authoritative-personalized-inventory/21-PATTERNS.md
@.planning/phases/21-authoritative-personalized-inventory/21-AI-SPEC.md
@.planning/phases/21-authoritative-personalized-inventory/21-01-SUMMARY.md
@src/registry/identity.mjs
@src/registry/diff.mjs
@src/registry/reconcile.mjs
@tests/helpers/inventory-fixture.mjs

<interfaces>
`stableCapabilityId(record)` and `contentFingerprint(value)` own identity evidence.
`diffFingerprintTrees(previous, current)` returns ordered lifecycle events and diagnostics.
`reconcileCandidate(options)` canonicalizes candidates, aliases, lifecycle, and verdicts before mapper/publication consumers.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Specify mutation identity and transitive invalidation outcomes</name>
  <files>tests/router.inventory-mutations.test.mjs, tests/router.registry-diff.test.mjs, tests/router.registry-reconcile.test.mjs</files>
  <read_first>
    - tests/helpers/inventory-fixture.mjs (mutation playback and semantic-byte helpers)
    - tests/router.registry-diff.test.mjs (lifecycle ordering, uncertainty, advisory similarity)
    - tests/router.registry-reconcile.test.mjs (candidate callback ordering and alias invalidation)
    - src/registry/identity.mjs (current fallback identity)
    - src/registry/diff.mjs (continuity and lifecycle event construction)
    - src/registry/reconcile.mjs (canonical candidate, alias input, verdict and callback order)
  </read_first>
  <action>Create `tests/router.inventory-mutations.test.mjs` replaying every D-19 mutation after each of the four synthetic base profiles. Assert D-09 stable-ID continuity and unique one-to-one exact-fingerprint continuity, D-10 simultaneous duplicate separation, D-12 disabled-record retention with dispatch reference invalidation, and D-13 replacement as new identity absent valid proof. Extend diff tests for ambiguous N-to-M exact fingerprints, path-aware fallback identity, and partial-scan removal suppression. Extend reconcile tests with a versioned generic reference input covering aliases, equivalence, workflow, correction, mapping, and compiled-route edges; seed multi-hop graphs from removal/replacement/disablement/dependency-loss and assert the mapper/evaluator callback observes only the fully invalidated candidate per D-11. Permute record and edge input order and require identical result bytes.</action>
  <acceptance_criteria>
    - Behavior assertion: all eight mutation types have expected ID sets, events, invalidated IDs, and retained evidence.
    - Behavior assertion: two live equal-content paths remain distinct, while one removed/one added exact pair preserves continuity.
    - Ordering assertion: callbacks cannot observe any live reference to an invalid seed.
    - RED assertion marker: name every intentionally failing Phase 21 mutation contract with `[phase21-red:mutation]`; the TAP-aware automated harness exits 0 only when at least one exact failing `not ok` test name carries that marker and every reported test failure carries the same marker.
  </acceptance_criteria>
  <verify>
    <automated>rtk node -e "const {spawnSync}=require('node:child_process');const r=spawnSync(process.execPath,['--test','tests/router.registry-diff.test.mjs','tests/router.registry-reconcile.test.mjs','tests/router.inventory-mutations.test.mjs'],{encoding:'utf8'});const out=(r.stdout||'')+(r.stderr||'');const failed=[...out.matchAll(/^\\s*not ok \\d+ - (.+)$/gm)].map(m=&gt;m[1]);process.exit(r.status!==0&amp;&amp;failed.length&gt;0&amp;&amp;failed.every(name=&gt;name.includes('[phase21-red:mutation]'))?0:1)"</automated>
  </verify>
  <done>The mutation corpus completely specifies identity and invalidation before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement exact continuity and candidate-local invalidation closure</name>
  <files>src/registry/identity.mjs, src/registry/diff.mjs, src/registry/reconcile.mjs</files>
  <read_first>
    - src/registry/schema.mjs (Plan 01 canonical record contract)
    - src/registry/identity.mjs (stableCapabilityId and contentFingerprint)
    - src/registry/diff.mjs (ORDER, dimensionChanges, lifecycleEvent, weaklySimilar)
    - src/registry/reconcile.mjs (canonicalCandidate, canonicalAliases, wholeCandidateVerdicts, reconcileCandidate)
    - tests/router.inventory-mutations.test.mjs (Task 1 oracle)
  </read_first>
  <behavior>
    - Path identity separates simultaneously live records across paths/scopes.
    - Stable declared identity wins; otherwise only unique exact fingerprint removal/addition establishes continuity.
    - Similarity remains diagnostic evidence and never transfers identity, trust, correction, or reference ownership.
    - Invalidation closure is deterministic, transitive, and complete before downstream callbacks.
  </behavior>
  <action>Update the fallback in `stableCapabilityId` to include portable source-path identity and scope; preserve schema-validated declared stable identity as the first continuity proof. In `diffFingerprintTrees`, build sorted removed/added fingerprint buckets and pair only buckets with exactly one record on each side; keep weak similarity advisory and never merge simultaneously live duplicates per D-09/D-10. In `reconcileCandidate`, generalize aliases into a versioned canonical `references` graph whose typed edges cover the D-11 families without inferring Phase 22 semantics. Construct deterministic reverse edges, seed invalidation from lifecycle removal/replacement, `enabled: false`, and unavailable required dependencies, traverse to a fixed point, emit sorted invalidation evidence, and set affected dispatch targets/references ineligible before mapper/evaluation callbacks. Preserve exact active bytes on malformed graphs, dangling unsafe edges, or callback failure. Do not transfer corrections or trust to D-13 replacements without D-09 proof.</action>
  <acceptance_criteria>
    - Source assertion: continuity uses stable ID then unique exact fingerprint; no similarity-based authoritative pairing exists.
    - Source assertion: one generic typed reference graph handles every D-11 reference family and is sorted canonically.
    - Behavior assertion: mutation and permutation matrices pass with identical bytes.
    - Test command: `rtk node --test tests/router.registry-diff.test.mjs tests/router.registry-reconcile.test.mjs tests/router.inventory-mutations.test.mjs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>rtk node --test tests/router.registry-diff.test.mjs tests/router.registry-reconcile.test.mjs tests/router.inventory-mutations.test.mjs</automated>
  </verify>
  <done>Every required mutation produces safe, deterministic identity and invalidation state before mapping or activation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lifecycle evidence → identity continuity | Filesystem changes may be ambiguous or attacker-controlled. |
| candidate references → mapper/activation | Stale edges could retain dispatch authority after a target changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-08 | Spoofing | rename/move identity pairing | high | mitigate | Accept only validated stable ID or unique exact fingerprint removal/addition; ambiguous/similar matches remain advisory. |
| T-21-09 | Tampering | reference invalidation | high | mitigate | Candidate-local reverse-edge closure invalidates all reachable typed references before callbacks. |
| T-21-10 | Elevation of Privilege | replacement trust transfer | high | mitigate | Replacement receives new identity and no inherited correction/trust unless D-09 proof succeeds. |
| T-21-11 | Denial of Service | cyclic reference graph | medium | mitigate | Sorted visited-set fixed-point traversal bounds each node/edge to deterministic processing. |
| T-21-SC | Tampering | package installation | high | mitigate | No package installation task exists. |
</threat_model>

<verification>
- `rtk node --test tests/router.registry-diff.test.mjs tests/router.registry-reconcile.test.mjs tests/router.inventory-mutations.test.mjs`
- `rtk node --test tests/router.route-targets.test.mjs`
</verification>

<success_criteria>
- DISC-03 and DISC-05 pass for every required mutation and reference family.
- Identity continuity never merges simultaneous duplicates or merely similar replacements.
- No downstream callback can observe stale eligible references.
</success_criteria>

<output>
Create `.planning/phases/21-authoritative-personalized-inventory/21-03-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- Path-aware `stableCapabilityId` fallback and unique exact-fingerprint continuity evidence.
- Versioned `references` input/output fields with typed alias/equivalence/workflow/correction/mapping/compiled-route edges.
- Sorted invalidation IDs/evidence in `reconcileCandidate`.
- `tests/router.inventory-mutations.test.mjs` — eight-mutation identity and invalidation matrix.

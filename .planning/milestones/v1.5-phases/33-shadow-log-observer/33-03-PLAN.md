---
phase: 33-shadow-log-observer
plan: 03
type: execute
wave: 3
depends_on:
  - 33-02
files_modified:
  - tests/router.mjs.snapshot
  - tests/router.shadow-log.e2e.test.mjs
  - tests/fixtures/graphify-out/graph.json
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - CALIB-01
  - CALIB-02
  - CALIB-05
user_setup: []
estimate:
  tokens: 22000
  raw_tokens: 11000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - "A real installed-hook-style sequence produces accepted, rejected, and no_signal rows, excludes the cache-hit row, and leaves the existing routing/telemetry path intact."
    - "The divergence report is available before Phase 34 and explicitly states calibration is disabled; no threshold or calibration file is written."
    - "The graphify integration fixture is deterministic and does not require a live graphify installation or external API key."
  artifacts:
    - tests/router.shadow-log.e2e.test.mjs
    - tests/fixtures/graphify-out/graph.json
    - tests/router.mjs.snapshot (fresh/cached telemetry and report seam)
  key_links:
    - "installed hook event sequence -> shadow-log.jsonl -> buildShadowDivergenceReport"
    - "report calibration_enabled:false -> Phase 34 threshold derivation remains disabled"

<objective>
Prove the deployed-shaped end-to-end observer sequence and leave a deterministic report/fixture seam for the next calibration phase. This plan closes the phase with measure-only evidence and does not activate calibration.
</objective>

<context>
@.planning/phases/33-shadow-log-observer/33-01-PLAN.md
@.planning/phases/33-shadow-log-observer/33-02-PLAN.md
@tests/router.mjs.snapshot
@tests/router.lifecycle.test.mjs
@tests/router-graphify-integration.test.mjs

Graphify integration is currently environment-gated in the repository. The fixture should be the smallest checked-in graph.json that exercises the existing import/query shape; it must not rebuild the repository graph or add a dependency.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Installed-shaped observer sequence and report gate</name>
  <files>tests/router.shadow-log.e2e.test.mjs, tests/router.mjs.snapshot</files>
  <read_first>
    - tests/router.shadow-log.test.mjs and tests/router.shadow-log.lifecycle.test.mjs
    - tests/router.telemetry-evolved.test.mjs: temporary-home subprocess conventions
    - tests/router-graphify-integration.test.mjs: environment-gated fixture conventions
  </read_first>
  <action>
    Drive a temporary HOME with telemetry rows for one fresh accepted suggestion, one fresh rejected suggestion, one fresh no-signal suggestion, and one cache-hit suggestion. Feed matching UserPromptExpansion/PostToolUse/Failure/Stop payloads through the installed-hook-shaped subprocess and assert one outcome per fresh suggestion, zero for cache-hit, deterministic by-mode/by-skill counts, runtime tags, and calibration_enabled:false. Assert no calibration.json, weights mutation, prompt text, command_args, tool_input, tool_response, transcript path, or user command is persisted.
  </action>
  <acceptance_criteria>
    - End-to-end counts are accepted=1, rejected=1, no_signal=1 for fresh suggestions and cache-hit contributes zero.
    - Report output is stable and threshold-free.
    - Existing router.mjs snapshot mirror test stays green.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/router.shadow-log.e2e.test.mjs</automated>
    <automated>node --test tests/router.mjs.snapshot.diff.test.mjs tests/router.telemetry.test.mjs tests/router.runtime-tagging.test.mjs</automated>
  </verify>
  <done>The phase has a real installed-shaped evidence trail and an explicit measure-only report gate.</done>
</task>

<task type="auto">
  <name>Task 2: Deterministic graphify fixture and phase evidence</name>
  <files>tests/fixtures/graphify-out/graph.json, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - tests/router-graphify-integration.test.mjs
    - graphify-out/graph.json: existing node/edge schema only; do not copy the full generated graph
    - .planning/ROADMAP.md and .planning/STATE.md
  </read_first>
  <action>
    Add only the minimal graphify-out fixture required to make the integration seam deterministic, or document the existing fixture as the source if the test already accepts it. Update ROADMAP/STATE only through the normal phase completion transition after all tests pass; record CALIB-01, CALIB-02, and CALIB-05 evidence and the deliberate calibration-disabled boundary. Do not mark the phase complete while review, security, Nyquist validation, or verification artifacts are missing.
  </action>
  <acceptance_criteria>
    - Fixture/test is deterministic without network or external API keys.
    - ROADMAP/STATE describe the actual verified evidence, not intended behavior.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/router-graphify-integration.test.mjs</automated>
    <automated>node --test tests/router.shadow-log.e2e.test.mjs</automated>
  </verify>
  <done>Phase evidence is complete and truthful; Phase 34 can consume the report without calibration being enabled early.</done>
</task>

</tasks>

<verification>
- `node --test tests/router.shadow-log.e2e.test.mjs` passes.
- `node --test tests/router-graphify-integration.test.mjs` passes or remains explicitly skipped only by its documented environment gate, with the deterministic fixture committed.
- Full focused Phase 33 suite and mirror-lockstep checks pass.
</verification>

<success_criteria>
- CALIB-01, CALIB-02, and CALIB-05 are proven end-to-end; the measure-only divergence report is ready for Phase 34 and calibration remains disabled.
</success_criteria>

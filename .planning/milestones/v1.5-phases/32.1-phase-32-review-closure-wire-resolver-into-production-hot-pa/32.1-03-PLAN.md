---
phase: 32.1-phase-32-review-closure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - build-manifest.mjs
  - src/coverage/audit.mjs
  - tests/router.build-gate.test.mjs
  - tests/router.coverage-audit.test.mjs
autonomous: true
requirements:
  - ROUTE-05
user_setup: []
estimate:
  tokens: 36000
  raw_tokens: 18000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "build-manifest.mjs --strict-coverage is a hard gate: a mode-map with a NEAR-TIE or an unresolvable/stale resolve list exits non-zero, and a clean mode-map exits zero (ROUTE-05 tie-lint CI gate)."
    - "A fixture proves the gate runs (a crafted near-tie mode-map fails strict coverage even though validateRouteTargets/auditCoverage alone would pass it)."
    - "Absent resolve members are defined and encoded with a severity: a route that otherwise resolves flags absent members as non-blocking quarantine diagnostics; a route with NO resolvable primary/member stays a blocking stale_target (ROUTE-05 forward-orphan)."
  artifacts:
    - build-manifest.mjs (lintModeMap wired into strict-coverage gate)
    - src/coverage/audit.mjs (absent-fallback severity classification)
    - tests/router.build-gate.test.mjs (gate fixture)
    - tests/router.coverage-audit.test.mjs (severity regression fixtures)
  key_links:
    - "build-manifest.mjs auditCoverage block -> lintModeMap(coverageModeMap, manifest) -> STRICT_COVERAGE exitCode=1 on violations"
    - "src/coverage/audit.mjs stale_target -> quarantined/non-blocking when another member resolves"
---

<objective>
Address review closure item 3 (HIGH): the tie-lint is NOT wired as a CI/build gate. `scripts/resolve-tie-lint.mjs` exists and is unit-tested, but `build-manifest.mjs` runs `auditCoverage()` with no tie-lint invocation, so the "CI gate" requirement (ROUTE-05) is unmet. This plan wires `lintModeMap()` into `build-manifest.mjs --strict-coverage` with deterministic non-zero failure behavior and adds a fixture proving the gate runs. It also folds in review MEDIUM item 5: define + encode the severity of absent resolve members (non-blocking quarantine when another candidate resolves; blocking stale_target when the route is otherwise unresolvable).

Purpose: Make resolve-list hygiene a hard, reproducible build/release gate rather than a read-only diagnostic, and make absent-fallback severity explicit so a valid route that merely loses an optional fallback does not fail strict coverage while a genuinely broken route still blocks.

Output: tie-lint wired into the strict build gate with a fixture, plus audit severity classification + regression tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-intent-first-routing-mode-map-schema-v4-guard-hole-closure/32-REVIEWS.md
@.planning/phases/32-intent-first-routing-mode-map-schema-v4-guard-hole-closure/32-VERIFICATION.md

# Source of truth for this plan (read_first targets):
@build-manifest.mjs                # strict-coverage block (~597 auditCoverage, ~603-609 exitCode=1), env overrides (ROUTER_MODE_MAP_PATH, ROUTER_MANIFEST_OUT, ROUTER_CLAUDE_HOME, ROUTER_COVERAGE_BASELINE_PATH, ROUTER_HOOK_PATH)
@scripts/resolve-tie-lint.mjs      # lintModeMap / commandInventory / TIE_GAP / GENERIC_FALLBACK_TIER / main() exports to wire in
@src/coverage/audit.mjs            # schemaRoute resolve-member diagnostics (~142-166) — severity classification target
@tests/router.build-gate.test.mjs  # (to create) gate fixture
@tests/router.coverage-audit.test.mjs  # existing forward-orphan expectations to extend, not break
@tests/router.resolve-tie-lint.test.mjs # existing lintModeMap unit coverage to keep green
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire lintModeMap into build-manifest strict-coverage gate</name>
  <files>build-manifest.mjs</files>
  <read_first>
    - build-manifest.mjs: the env-var constants block (~20-60) — ROUTER_MODE_MAP_PATH, ROUTER_MANIFEST_OUT, ROUTER_CLAUDE_HOME, ROUTER_COVERAGE_BASELINE_PATH, ROUTER_HOOK_PATH, STRICT_COVERAGE flag, CONFIG_HOME runtime pin.
    - build-manifest.mjs: the coverage/strict block (~590-610) where `const coverage = auditCoverage({... routeDiagnostics})` and `if (STRICT_COVERAGE && (coverage.unacknowledged_gaps.length || coverage.forward_diagnostics.length)) process.exitCode = 1;`.
    - scripts/resolve-tie-lint.mjs: `export function lintModeMap(modeMap, manifest, opts)` returning `{violations, downgradedTiers, quarantined}` and `main()` (CLI exit 0/1); the `modeMap` and `manifest` shape it consumes (mode-map `entries[]`, manifest `commands[]`/`runtime_commands`).
  </read_first>
  <behavior>
    - Test 1: a crafted mode-map with a NEAR-TIE resolve list (two members within 0.05) makes `build-manifest.mjs --strict-coverage` exit 1, even though validateRouteTargets/auditCoverage would pass it (tie-lint is the only flagging source — proving the gate truly runs).
    - Test 2: a clean mode-map (no near-tie, all resolve members present) exits 0 under --strict-coverage.
    - Test 3: a stale_target resolve member (absent from the manifest) also exits 1 under strict coverage.
  </behavior>
  <action>
    In `build-manifest.mjs`, import `lintModeMap` (and `commandInventory` for resolvability) from `./scripts/resolve-tie-lint.mjs` (stdlib, no install). In the strict-coverage region (near the auditCoverage call ~597), run the tie-lint over the same mode-map (`coverageModeMap`) and the freshly-built `manifest`:
    - `const tieLint = lintModeMap(coverageModeMap, manifest);`
    - Emit a deterministic report to stderr for any `violations` (near_tie + stale_target lines) regardless of strictness (so the gate is visible in normal builds too).
    - RECONCILE with the audit's severity model (review MEDIUM #5 / plan Task 3): `lintModeMap` flags EVERY absent resolve member as `type:'stale_target'` with NO resolvability context, so do NOT gate on `tieLint.violations.length` directly. Compute `blockingViolations` that EXCLUDES members the audit classifies as non-blocking `quarantined_fallback` on a still-resolvable route:
      - `near_tie` violations always block (ROUTE-05 CI gate).
      - `stale_target` violations block ONLY when the entry is unresolvable — mode absent AND no resolve member present in the active command inventory. Resolvability is decided with `commandInventory(manifest)` (`commands` set), mirroring audit.mjs `resolveRoute` (`mode` present OR any member present). A `stale_target` on an entry that otherwise resolves (mode or >=1 member present) is non-blocking — the absent member is reclassified `quarantined_fallback` per Task 3.
      - `const commands = commandInventory(manifest); const blockingViolations = tieLint.violations.filter(v => v.type === 'near_tie' || !(v.type === 'stale_target' && entryResolvable(v.entry, commands, coverageModeMap)));` where `entryResolvable` replicates `mode in commands || resolve.some(m => commands.has(stripLeadingSlash(m?.name)))`.
    - Under `STRICT_COVERAGE`, treat `blockingViolations.length > 0` as a gate failure: `process.exitCode = 1` alongside the existing audit coverage failure. Keep the existing audit-coverage failure branch intact (frequency of a resolvable route's `quarantined_fallback` in strict coverage is Task 3's responsibility) and fold the tie-lint check into the same `if (STRICT_COVERAGE && (...))` condition so a single strict gate covers both.
    - Do NOT auto-mutate the mode-map; the gate is read-only diagnostics + non-zero exit (deterministic, never rewrites user data).
  </action>
  <acceptance_criteria>
    - Near-tie and UNRESOLVABLE stale_target resolve lists fail `--strict-coverage` (exit 1) with a stderr report.
    - A resolvable route's absent optional resolve member (audit non-blocking `quarantined_fallback`) does NOT fail strict coverage (exit 0) — reconciled with Task 3.
    - Clean mode-map passes `--strict-coverage` (exit 0) with a normal build side effect.
    - Non-strict builds still emit the tie-lint report but do not fail.
    - Sanity: an empty/default mode-map never regresses the normal (non-strict) builder.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/router.build-gate.test.mjs</automated>
    <automated>node --test tests/router.resolve-tie-lint.test.mjs tests/router.coverage-audit.test.mjs</automated>
  </verify>
  <done>The strict build gate fails on near-tie and UNRESOLVABLE stale resolve lists with a reproducible report, passes on clean lists AND on a resolvable route with an absent optional member (non-blocking quarantine); existing tie-lint unit + coverage-audit suites stay green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate fixture — craft a near-tie mode-map and drive build-manifest --strict-coverage</name>
  <files>tests/router.build-gate.test.mjs</files>
  <read_first>
    - build-manifest.mjs: command discovery (~303-318, walks `plugins/**/commands/*.md` + `gsd-core/**/commands/*.md` under ROUTER_CLAUDE_HOME) so the fixture can create a temp home whose manifest carries the resolve-listed commands.
    - build-manifest.mjs: env overrides (~20-60) to sandbox every path (ROUTER_CLAUDE_HOME, ROUTER_MODE_MAP_PATH, ROUTER_MANIFEST_OUT, ROUTER_COVERAGE_BASELINE_PATH, ROUTER_WEIGHTS_PATH, ROUTER_CLAUDE_JSON, ROUTER_HOOK_PATH default = live hook).
    - tests/router.perf.test.mjs: spawnSync-with-env-override pattern to reuse for subprocess driving.
  </read_first>
  <action>
    Create `tests/router.build-gate.test.mjs` that proves the gate runs end-to-end in a fully-sandboxed temp dir (never touching the real ~/.claude router):
    - Build a temp `ROUTER_CLAUDE_HOME` containing `plugins/gsp/commands/gsd-debug.md` and `plugins/gsp/commands/systematic-debugging.md` (frontmatter name = stem) so the builder's command walk emits both commands.
    - Craft `ROUTER_MODE_MAP_PATH` mode-maps: (a) NEAR-TIE — slash entry `mode: gsd-debug`, `resolve:[{name:'gsd-debug',weight:1.0},{name:'systematic-debugging',weight:0.98}]` (gap 0.02 <= 0.05); (b) WIDE-GAP — same members with weight 0.9 (no near-tie); (c) STALE-UNRESOLVABLE — a slash entry whose `mode` AND its only resolve member (`ghost-capability`) are both ABSENT from the manifest command inventory (no resolvable primary) -> blocking; (d) RESOLVABLE-STALE — a slash entry whose `mode: gsd-debug` IS present in the manifest while an additional resolve member (`ghost-capability`) is absent, so the route still resolves -> non-blocking.
    - Provide temp `ROUTER_COVERAGE_BASELINE_PATH` (empty gaps), `ROUTER_WEIGHTS_PATH` (defaults), `ROUTER_CLAUDE_JSON` (empty object), `ROUTER_MANIFEST_OUT` (temp).
    - Spawn `node build-manifest.mjs --strict-coverage` with `ROUTER_HOOK_PATH` set to the live hook (so validateRouteTargets runs on the fixture) and assert: (a) near-tie -> exit 1 + stderr contains `near_tie`; (b) wide-gap -> exit 0; (c) stale-unresolvable -> exit 1 (blocking `stale_target`); (d) resolvable-stale -> exit 0 (absent optional member is non-blocking `quarantined_fallback`, NOT a strict failure). Case (d) LOCKS the Warning-2 reconcile: a resolvable route that lost an optional fallback must never fail strict coverage. Because validateRouteTargets does NOT flag the near-tie (mode is present, members present), the near-tie failure can only come from the wired tie-lint — this is the proof the gate runs.
  </action>
  <acceptance_criteria>
    - Four subprocess assertions (near-tie exit 1, wide-gap exit 0, stale-unresolvable exit 1, resolvable-stale exit 0) all pass deterministically.
    - All fixture writes happen under a temp dir created and removed by the test (no external home-dir mutation).
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/router.build-gate.test.mjs</automated>
  </verify>
  <done>End-to-end gate fixture proves strict coverage fails on near-tie and unresolvable-stale resolve lists, passes on clean ones AND on a resolvable route with an absent optional member (exit 0), in a sandboxed temp dir — locking the Warning-2 reconcile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Classify absent resolve-member severity in audit (non-blocking quarantine vs blocking stale_target)</name>
  <files>src/coverage/audit.mjs, tests/router.coverage-audit.test.mjs</files>
  <read_first>
    - src/coverage/audit.mjs: resolve-member diagnostics loop (~157-166) — currently reports EVERY absent resolve member as `stale_target` even when another member resolves.
    - src/coverage/audit.mjs: the `schemaRoute`/`resolveRoute` block (~142-160) showing how a route's resolvability is decided (indexes.command.has(mode) or member).
    - tests/router.coverage-audit.test.mjs: existing forward-orphan / resolve-member expectations (to extend, not break) — read the resolve-list test section before editing.
  </read_first>
  <behavior>
    - Test 1: a route that still resolves (mode or ≥1 member present) with an absent optional member flags that member as a NON-blocking quarantine diagnosis (not a coverage failure).
    - Test 2: a route with NO resolvable primary or member stays a blocking stale_target (coverage failure) — unchanged behavior.
    - Test 3: existing forward-orphan sentiments (absent resolve members never silently dropped; always reported) still hold — only the blocking severity of a resolvable route's optional fallback changes.
  </behavior>
  <action>
    Per review MEDIUM item 5, define and encode absent-fallback severity in `src/coverage/audit.mjs`:
    - When a slash route's mode and/or at least one resolve member is PRESENT in the active command inventory (i.e., the route resolves), report each absent remaining resolve member as a NON-blocking quarantine diagnosis (e.g. code `quarantined_fallback`, category commands, reason 'optional resolve member absent — quarantined non-blocking') rather than a coverage-failing `stale_target`.
    - When the route has NO resolvable primary or member (mode absent and no member present), keep the blocking `stale_target` behavior so a genuinely broken route still fails strict coverage.
    - Keep reporting every absent member (never silently dropped — forward-orphan semantics preserved); only the blocking severity of a resolvable route's optional fallback changes.
    - Update `tests/router.coverage-audit.test.mjs` fixtures: assert the new `quarantined_fallback` code for resolvable routes and that fully-unresolvable routes still fail. Ensure `auditCoverage`'s `forward_diagnostics` (which `build-manifest.mjs` strict coverage checks via `coverage.forward_diagnostics.length`) carries ONLY the blocking class — resolvable routes' `quarantined_fallback` members stay in the report for visibility but are EXCLUDED from the strict-failure consumption, so they never trip the audit branch. This makes the audit gate and Task 1's reconciled `lintModeMap` gate agree: both fail only on near-tie + genuinely unresolvable routes.
  </action>
  <acceptance_criteria>
    - Absent members on a resolvable route are reported but non-blocking in strict coverage; fully-unresolvable routes remain blocking.
    - No member ever silently dropped (report completeness preserved).
    - Existing + new coverage-audit assertions pass.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/router.coverage-audit.test.mjs tests/router.build-gate.test.mjs</automated>
  </verify>
  <done>Absent-fallback severity is explicit and encoded (non-blocking quarantine when the route resolves; blocking stale_target when it does not), covered by coverage-audit fixtures and honored by the strict gate.</done>
</task>

</tasks>

## Artifacts this phase produces

- Modified `build-manifest.mjs` — `lintModeMap()` wired into the strict-coverage gate; `blockingViolations` derived to exclude resolvable-route absent members (non-blocking `quarantined_fallback`), deterministic stderr report + exitCode=1 on near_tie / unresolvable-stale under `--strict-coverage`.
- Modified `src/coverage/audit.mjs` — absent resolve-member severity classification (`quarantined_fallback` non-blocking vs `stale_target` blocking).
- New `tests/router.build-gate.test.mjs` — end-to-end gate fixture (sandboxed temp dir; near-tie/stale fail, clean passes).
- Modified `tests/router.coverage-audit.test.mjs` — severity regression fixtures.

## Dependency note

This plan touches neither `tests/router.mjs.snapshot` nor live router code and therefore does NOT add a mirror step. It is independent of plans 32.1-01/02 (different files) and runs in parallel as wave 1.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build gate input (mode-map/manifest) -> strict-coverage verdict | untrusted resolve-list data crosses here; a bad list could ship or, conversely, a healthy list could be falsely rejected |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32.1-08 | Tampering | build-manifest strict gate | high | mitigate | Deterministic non-zero exit on near-tie/stale; fixture proves the gate genuinely runs; read-only (never mutates mode-map) |
| T-32.1-09 | Tampering | audit severity classification | medium | mitigate | Only the blocking class feeds forward_diagnostics/strict failure; non-blocking quarantined_fallback never fails a resolvable route |
| T-32.1-10 | DoS | gate false-positive on clean lists | low | mitigate | WIDE-GAP fixture asserts clean lists still exit 0; strict failure requires an actual violation |
| T-32.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs (lintModeMap is stdlib-only); slopcheck + blocking human checkpoint only if an install is ever introduced |
</threat_model>

<verification>
- `node --test tests/router.build-gate.test.mjs` passes (near-tie exit 1, wide-gap exit 0, stale-unresolvable exit 1, resolvable-stale exit 0).
- `node --test tests/router.coverage-audit.test.mjs tests/router.resolve-tie-lint.test.mjs` pass (severity + tie-lint regressions).
- Non-strict builder still emits a non-fatal tie-lint report on a normal run.
</verification>

<success_criteria>
- Tie-lint is a hard build/release gate (closure item 3 closed) with a proven fixture.
- Absent resolve-member severity is explicit and encoded (MEDIUM #5 closed).
</success_criteria>

<output>
Create `.planning/phases/32.1-phase-32-review-closure-wire-resolver-into-production-hot-pa/32.1-03-SUMMARY.md` when done.
</output>

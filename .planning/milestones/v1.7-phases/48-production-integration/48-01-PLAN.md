---
phase: 48-production-integration
plan: 01
wave: 1
depends_on: []
files_modified:
  - src/orchestrator/strategy.mjs
  - src/adapters/dispatch/claude.mjs
  - src/adapters/dispatch/codex.mjs
  - src/evolution/local-learning.mjs
  - tests/router.production-integration.test.mjs
autonomous: true
requirements: [PROD-01, PROD-03]
---

# Plan 48-01: Production Dispatch and Learning

## Objective

Put proportional strategy selection on the native worker path and make learning evidence unique and protected.

## Tasks

### Task 1: Prepare and apply production strategy

Add the smallest strategy-preparation seam to the existing strategy module. Have both native workers prepare a plan from the marker’s workflow inputs (with a safe compatibility baseline when those inputs are absent) before invoking the adapter, and pass the plan through the existing dispatch validator.

<verify>rtk node --test tests/router.production-integration.test.mjs tests/phase-43/strategy.test.mjs tests/router.dispatch-safety.test.mjs</verify>
<done>Claude and Codex native workers carry a planned, validated proportional strategy before native invocation.</done>

### Task 2: Reject duplicate learning evidence

Make `collectLearningEvidence` reject repeated receipt identities before partitioning and retain the existing causal, partition, and protected-field gates.

<verify>rtk node --test tests/router.production-integration.test.mjs tests/phase-45/local-learning.test.mjs</verify>
<done>Duplicate receipts cannot inflate sample counts or satisfy learning thresholds.</done>

## Success Criteria

- PROD-01 and PROD-03 are directly evidenced by fresh tests.


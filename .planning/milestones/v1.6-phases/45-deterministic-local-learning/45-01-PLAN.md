---
phase: 45-deterministic-local-learning
plan: 01
wave: 1
---

# Plan 45-01

## Goal

Create the smallest cold-path learning boundary that accepts only credited receipts and explicit corrections, partitions evidence deterministically, and routes candidates through shadow, canary, promotion, or complete rollback states.

## Tasks

1. Add `src/evolution/local-learning.mjs` with the exact thresholds from `45-RESEARCH.md`, receipt/correction validation, stable partition and candidate identities, deterministic gate evaluation, and pure decision output that preserves the full known-good tuple.
2. Add `tests/phase-45/local-learning.test.mjs` covering causal/privacy rejection, partition isolation, insufficient/contradictory evidence, threshold boundaries, shadow-to-canary-to-promotion, canary failure rollback, and deterministic output.

## Verification

`rtk node --test tests/phase-45/local-learning.test.mjs tests/router.evolution-canary.test.mjs tests/router.auto-calibration.lifecycle.test.mjs`

---
phase: 19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - src/prompt/compile-index.mjs
  - src/prompt/publish-index.mjs
  - src/orchestrator/workflow-declarations.json
autonomous: true
requirements:
  - ORC-01
  - TOK-02
must_haves:
  truths:
    - "selectCapabilities, selectWorkflow/nextValidTransitions, and planContextLoad run inside publishCompiledIndex at publish time and their output is frozen into the immutable compiled tuple as sibling files (per D-01)."
    - "The route path stays a read-only projection — it reads baked siblings via loadCompiledIndex and never imports or calls an orchestrator function (per D-01, D-02, D-08)."
    - "COMPILED_INDEX_SCHEMA_VERSION is bumped 1→2 and COMPILED_INDEX_COMPATIBILITY gains orchestrator_contract_version + context_contract_version; compatible() checks both new members, so prior schema-1 tuples are rejected and the watcher re-publishes (per D-04)."
    - "closure.json, budget.json, and summary-index.json are written as sibling files in the tuple version directory via durableWrite, with per-workflow-keyed by_workflow maps mirroring routes?.[workflowId] (per D-05)."
    - "The manifest at publish-index.mjs:85-91 gains closure/budget/summary_index payload_sha256 fields and verifyTuple at compile-index.mjs:121-132 hash-checks each sibling, so a tampered sibling causes the tuple to be rejected and blocked() returned (closes T-19-01, satisfies Phase 17 D-02 fail-closed integrity)."
    - "The blanket fallback at publish-index.mjs:63-67 is removed entirely; empty mapping → :68 throw → publish fails closed → no route (per D-06, closes ORC-01)."
    - "Per-prompt budget estimation is NOT added to the route path; required-overflow is baked as dispatch_eligible:false at publish via planContextLoad (per D-03, closes TOK-02)."
    - "D-09 backstop: the extended Phase 18 E2E (Plan 04) proves Flow 11 dispatch_eligible flips to PASS via the baked budget flag — { statement, verification: backstop }."
  artifacts:
    - "src/prompt/compile-index.mjs (modified: schema 1→2, compatible() +2 members, COMPILED_INDEX_LIMITS +3 limits, verifyTuple +3 sibling hash checks, loadCompiledIndex return +3 keys, pointer schema_version 1→2 check)"
    - "src/prompt/publish-index.mjs (modified: orchestrator imports + calls per mapped subject, sibling durableWrite calls, manifest +3 payload_sha256 fields, fallback :63-67 deleted, pointer schema_version 1→2)"
    - "src/orchestrator/workflow-declarations.json (new static declarations file consumed by publishCompiledIndex)"
  key_links:
    - "publish-index.mjs imports selectCapabilities/selectWorkflow/nextValidTransitions/planContextLoad/DEFAULT_CONTEXT_CONTRACT/CONTEXT_CONTRACT_VERSION/ESTIMATOR_VERSION/WORKFLOW_TRANSITIONS from src/orchestrator/{select,transitions,budget}.mjs"
    - "publish-index.mjs imports CONTEXT_CONTRACT_VERSION from src/orchestrator/budget.mjs and uses it in COMPILED_INDEX_COMPATIBILITY — wait, COMPILED_INDEX_COMPATIBILITY lives in compile-index.mjs; compile-index.mjs imports CONTEXT_CONTRACT_VERSION from src/orchestrator/budget.mjs"
    - "compile-index.mjs verifyTuple hash-checks siblings against manifest.closure/budget/summary_index payload_sha256 written by publish-index.mjs"
    - "publish-index.mjs reads workflow-declarations.json via relative path ../orchestrator/workflow-declarations.json (resolves in both source and deployed modules/ layouts)"
---

<objective>
Wire the Phase 16 orchestrator modules (selectCapabilities / selectWorkflow / nextValidTransitions / planContextLoad) into the live publish path inside `publishCompiledIndex`, freeze their output into sibling tuple files (closure.json / budget.json / summary-index.json), bump the compiled-index schema 1→2 with the extended compatibility gate, extend the manifest + verifyTuple with sibling hash verification (T-19-01 mitigation), and remove the ORC-01 blanket fallback. This plan closes the live-path gaps for ORC-01 (no capability dispatch without workflow-rooted mapping) and TOK-02 (declared context budget baked and enforced at publish via dispatch_eligible).

Purpose: This is the core wiring plan. compile-index.mjs and publish-index.mjs are tightly coupled by the manifest hash contract (publish writes sibling hashes, compile verifies them) and must land atomically. The Q1 design decision (Plan 01) locks the sibling shape, manifest extension, and compatibility members this plan implements verbatim.

Output: Modified `src/prompt/compile-index.mjs` and `src/prompt/publish-index.mjs`, new `src/orchestrator/workflow-declarations.json`. Prior on-disk schema-1 tuples are intentionally invalidated (D-04); the watcher re-publishes from authoritative disk state on the next reconciliation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-CONTEXT.md
@.planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-RESEARCH.md
@.planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-PATTERNS.md
@.planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-ORCHESTRATOR-INPUT-DECISION.md
@.planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-VALIDATION.md
@src/prompt/publish-index.mjs
@src/prompt/compile-index.mjs
@src/orchestrator/select.mjs
@src/orchestrator/transitions.mjs
@src/orchestrator/budget.mjs
@src/registry/watcher.mjs
@tests/router.workflow-orchestrator.test.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: compile-index.mjs schema bump 1→2, compatible() +2 members, COMPILED_INDEX_LIMITS +3 sibling limits, verifyTuple +3 sibling hash checks, loadCompiledIndex +3 return keys, pointer schema_version 1→2</name>
  <files>src/prompt/compile-index.mjs</files>
  <read_first>
    - .planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-ORCHESTRATOR-INPUT-DECISION.md (LOCKED: items #6 compatible() members, #7 COMPILED_INDEX_LIMITS values, #8 pointer schema_version bump, #9 loadCompiledIndex return keys, #5 manifest extension for sibling hashes — implement verbatim)
    - .planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-PATTERNS.md (compile-index.mjs analog :105-158: schema constants :5-10, compatible() :72-76, boundedJson :34-52, verifyTuple :118-132, loadCompiledIndex return :137-152, validRoutes :54-70 UNCHANGED)
    - .planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-RESEARCH.md (Pitfall #1 schema-bump churn, Pitfall #5 route-path read surface bloat, Security V6 sibling hash requirement)
    - src/prompt/compile-index.mjs (current full file — focus :5-10 constants, :11-18 LIMITS, :72-76 compatible(), :106-177 loadCompiledIndex, :118-132 verifyTuple)
    - src/orchestrator/budget.mjs (CONTEXT_CONTRACT_VERSION export at :4 — import it for the new compatibility member)
  </read_first>
  <behavior>
    - Test: `COMPILED_INDEX_SCHEMA_VERSION === 2` (was 1)
    - Test: `COMPILED_INDEX_COMPATIBILITY.orchestrator_contract_version === 'workflow-first-v1'`
    - Test: `COMPILED_INDEX_COMPATIBILITY.context_contract_version === CONTEXT_CONTRACT_VERSION`
    - Test: `compatible({ router_contract: 'prompt-route-v1', policy_version: 'workflow-transitions-v1', capsule_schema_version: 1, orchestrator_contract_version: 'workflow-first-v1', context_contract_version: CONTEXT_CONTRACT_VERSION })` returns true
    - Test: `compatible({ router_contract: 'prompt-route-v1', policy_version: 'workflow-transitions-v1', capsule_schema_version: 1 })` returns false (old schema-1 tuple rejected — missing new members)
    - Test: `COMPILED_INDEX_LIMITS.closure_bytes === 64 * 1024`, `COMPILED_INDEX_LIMITS.budget_bytes === 32 * 1024`, `COMPILED_INDEX_LIMITS.summary_index_bytes === 16 * 1024`
    - Test: a tuple whose manifest has `closure.payload_sha256` mismatched against the actual closure.json bytes causes `verifyTuple` to return null → `loadCompiledIndex` returns `blocked()`
    - Test: a tuple whose manifest lacks `closure.payload_sha256` entirely causes `verifyTuple` to return null → `loadCompiledIndex` returns `blocked()`
    - Test: `loadCompiledIndex` ready return includes `closure`, `budget`, `summaryIndex` keys (the parsed sibling JSON) when the tuple is fully valid
    - Test: `validRoutes()` behavior is unchanged — sibling reads do NOT affect route validation (routes[] remains the dispatch surface per D-05)
    - Test: pointer with `schema_version: 1` is rejected by verifyTuple (pointer schema bumped to 2)
    - Test: pointer with `schema_version: 2` is accepted (all else valid)
  </behavior>
  <action>
    Edit `src/prompt/compile-index.mjs` per the locked contract in 19-ORCHESTRATOR-INPUT-DECISION.md. Concrete changes:

    1. **Add import** at top: `import { CONTEXT_CONTRACT_VERSION } from '../orchestrator/budget.mjs';` (compile-index.mjs currently imports nothing from orchestrator — this is the one additive import; it does NOT enter the hook import graph because the hook imports prompt-route.mjs, not compile-index.mjs directly... wait, prompt-route.mjs imports compile-index.mjs. CHECK D-08: does this transitively pull orchestrator/budget.mjs into the hook? YES — `import { CONTEXT_CONTRACT_VERSION }` is a value import, so it executes budget.mjs module body at import time. This is a D-08 concern. MITIGATION: import `CONTEXT_CONTRACT_VERSION` as a frozen-string constant duplicated in compile-index.mjs instead of importing from budget.mjs, OR inline the literal value. DECISION: inline the literal — read CONTEXT_CONTRACT_VERSION from src/orchestrator/budget.mjs:4 (it is a string constant) and hardcode the same literal in COMPILED_INDEX_COMPATIBILITY. Add a comment: `// CONTEXT_CONTRACT_VERSION inlined to avoid pulling src/orchestrator/* into the hook import graph (D-08). Keep in sync with src/orchestrator/budget.mjs:4.` Do NOT add the import. This keeps D-08 intact.)

    2. **Bump** `COMPILED_INDEX_SCHEMA_VERSION` from `1` to `2` (line 5).

    3. **Extend** `COMPILED_INDEX_COMPATIBILITY` (lines 6-10) with two new members: `orchestrator_contract_version: 'workflow-first-v1'` and `context_contract_version: <inlined CONTEXT_CONTRACT_VERSION literal>`. Object remains `Object.freeze`.

    4. **Extend** `COMPILED_INDEX_LIMITS` (lines 11-18) with three new size limits: `closure_bytes: 64 * 1024`, `budget_bytes: 32 * 1024`, `summary_index_bytes: 16 * 1024`. Object remains `Object.freeze`.

    5. **Extend** `compatible()` (lines 72-76) to check the two new members: add `&& value?.orchestrator_contract_version === COMPILED_INDEX_COMPATIBILITY.orchestrator_contract_version && value?.context_contract_version === COMPILED_INDEX_COMPATIBILITY.context_contract_version`.

    6. **Update verifyTuple pointer check** (line 121): change `pointer?.schema_version !== 1` to `pointer?.schema_version !== 2` (pointer schema bumped alongside tuple schema per decision #8).

    7. **Extend verifyTuple** (lines 118-132) to read and hash-verify the three sibling files. After the existing `indexRead` hash check, add: read `closureRead = boundedJson(resolve(versionRoot, 'closure.json'), COMPILED_INDEX_LIMITS.closure_bytes, io)`, `budgetRead = boundedJson(resolve(versionRoot, 'budget.json'), COMPILED_INDEX_LIMITS.budget_bytes, io)`, `summaryIndexRead = boundedJson(resolve(versionRoot, 'summary-index.json'), COMPILED_INDEX_LIMITS.summary_index_bytes, io)`. For each: if the read is null OR `sha256(read.bytes) !== manifest.<field>?.payload_sha256` OR the manifest field is missing → return null (tuple rejected). This is the T-19-01 mitigation. Then include the parsed values in the returned object: `closure: closureRead.value, budget: budgetRead.value, summaryIndex: summaryIndexRead.value`.

    8. **Extend the three `ready` return statements** in `loadCompiledIndex` (the `release_tuple_recovery_candidate`, `release_tuple_active`, `release_tuple_known_good` branches at lines ~137-152) to include the three new keys `closure`, `budget`, `summaryIndex` from the verified tuple. The `compiled_index_active` and `compiled_index_known_good` branches (legacy compiled-index path) do NOT get sibling keys (siblings only exist in release-tuples schema-2) — those branches return only `index` as before. This is acceptable because the schema bump invalidates old tuples and the watcher re-publishes into release-tuples.

    9. **validRoutes() is UNCHANGED** (lines 54-70) — routes[] remains the dispatch surface per D-05. Sibling reads do NOT participate in route validation.

    Do NOT modify `boundedJson`, `contained`, `blocked()`, `verifyVersion` (legacy compiled-index path), or the LKG iteration logic — only the targeted extensions above.

    Run `node --test tests/router.compiled-index.test.mjs` after editing — expect failures from schema-1 fixtures (Pitfall #1); the fixture-update wave in Plan 04 fixes those. The new behavior tests (schema 2, compatible() new members, sibling hash checks, loadCompiledIndex return keys) MUST pass. If no test file exists for the new behavior, the executor creates a focused test in `tests/router.compiled-index.schema2.test.mjs` covering the behavior assertions above (Wave 0 gap — this is the Wave 0 scaffold the Nyquist gate requires).
  </action>
  <verify>
    <automated>node --test tests/router.compiled-index.test.mjs tests/router.compiled-index.schema2.test.mjs 2>&1 | tail -20; grep -c "COMPILED_INDEX_SCHEMA_VERSION = 2" src/prompt/compile-index.mjs | grep -q '^1$'; grep -c "orchestrator_contract_version" src/prompt/compile-index.mjs | grep -qv '^0$'; grep -c "closure_bytes" src/prompt/compile-index.mjs | grep -qv '^0$'; grep -c "summary_index" src/prompt/compile-index.mjs | grep -qv '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "COMPILED_INDEX_SCHEMA_VERSION = 2" src/prompt/compile-index.mjs` returns 1 (was `= 1`)
    - `grep -c "orchestrator_contract_version" src/prompt/compile-index.mjs` returns ≥2 (constant + compatible() check)
    - `grep -c "context_contract_version" src/prompt/compile-index.mjs` returns ≥2 (constant + compatible() check)
    - `grep -c "closure_bytes\|budget_bytes\|summary_index_bytes" src/prompt/compile-index.mjs` returns ≥3 (COMPILED_INDEX_LIMITS entries)
    - `grep -c "closure.json" src/prompt/compile-index.mjs` returns ≥1 (verifyTuple sibling read)
    - `grep -c "manifest.closure?.payload_sha256\|manifest?.closure?.payload_sha256" src/prompt/compile-index.mjs` returns ≥1 (sibling hash verification)
    - `grep -c "pointer?.schema_version !== 2" src/prompt/compile-index.mjs` returns 1 (pointer schema bump)
    - `grep -c "closure:" src/prompt/compile-index.mjs` returns ≥1 in the loadCompiledIndex ready-return (additive return keys)
    - `node --test tests/router.compiled-index.schema2.test.mjs` exits 0 (the new-behavior test passes)
    - No `import` from `src/orchestrator/*` added to compile-index.mjs (D-08 preserved — `grep -c "from '../orchestrator" src/prompt/compile-index.mjs` returns 0)
    - `validRoutes()` function body is byte-identical to the pre-edit version (verify via `git diff src/prompt/compile-index.mjs` — the validRoutes hunk is empty)
  </acceptance_criteria>
  <done>
    compile-index.mjs schema is bumped to 2 with two new compatibility members, three new size limits, three sibling hash checks in verifyTuple (T-19-01 mitigated), three new return keys in loadCompiledIndex, and the pointer schema_version check updated to 2. The D-08 hook-import-graph lock is preserved (no orchestrator import added; CONTEXT_CONTRACT_VERSION inlined as a literal). validRoutes() is untouched. New-behavior tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: publish-index.mjs orchestrator wiring (D-01), sibling durableWrite (D-05), manifest +3 payload_sha256 (T-19-01), fallback :63-67 delete (D-06), workflow-declarations.json static file</name>
  <files>src/prompt/publish-index.mjs, src/orchestrator/workflow-declarations.json</files>
  <read_first>
    - .planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-ORCHESTRATOR-INPUT-DECISION.md (LOCKED: items #1 declarations source + relative path, #2 synthetic evidence shape, #3 candidate+selected bake, #4 sibling file by_workflow shape, #5 manifest extension, #10 dispatch_eligible flag)
    - .planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-PATTERNS.md (publish-index.mjs analog :24-101: imports :1-5, durableWrite :10-14, publishCompiledIndex :54-99, route loop :59-62, fallback :63-67, manifest :85-91, tuple write :81-91; orchestrator call pattern :82-90; sibling hash registration :255-263)
    - .planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-RESEARCH.md (Code Examples: orchestrator signatures :317-336, publish call site :303-315; Pitfalls #2, #3, #4)
    - src/prompt/publish-index.mjs (current full file — focus :1-5 imports, :10-14 durableWrite, :54-99 publishCompiledIndex body, :63-67 fallback to delete, :85-91 manifest)
    - src/orchestrator/select.mjs (selectCapabilities :160 — signature `{ workflow, workflowDeclarations, registry, requestedScope, explicitCapability }`; return :128-139 closure/invokable_capabilities/required_models/required_permissions/lifecycle_bindings; blocked shape :9-11)
    - src/orchestrator/transitions.mjs (nextValidTransitions :69 signature `(evidence, policy = WORKFLOW_TRANSITIONS)`; selectWorkflow :143 signature `(transitionResult, explicitIntent)`; WORKFLOW_TRANSITIONS :9-19)
    - src/orchestrator/budget.mjs (planContextLoad :131 signature `{ workflow, closure, contract, sources, summaryIndex, baseline }`; DEFAULT_CONTEXT_CONTRACT :21-30; report shape :200-209)
    - tests/router.workflow-orchestrator.test.mjs (workflowDeclaration() :36-43 + evidence() :12-21 fixtures — the only existing templates for declarations/evidence shape)
  </read_first>
  <behavior>
    - Test: `publishCompiledIndex` with a mapping containing at least one `disposition: 'mapped'` subject produces a tuple whose `closure.json`, `budget.json`, `summary-index.json` siblings exist and parse as the locked `{ schema_version: 1, by_workflow: { ... } }` shape
    - Test: the published manifest contains `closure.payload_sha256`, `budget.payload_sha256`, `summary_index.payload_sha256` fields whose values equal `sha256(<sibling bytes>)`
    - Test: `loadCompiledIndex({ ownedRoot })` after publish returns an object with `closure`, `budget`, `summaryIndex` keys whose `by_workflow` map contains the mapped workflow_id
    - Test: `publishCompiledIndex` with `mapping.subjects: []` (empty mapping) throws `TypeError('compiled index requires at least one dispatch route')` at :68 — NO `canonical_record` route is produced (D-06 ORC-01 closure)
    - Test: `grep -c "canonical_record" src/prompt/publish-index.mjs` returns 0 (fallback removed)
    - Test: when `planContextLoad` returns `dispatch_eligible: false` (required-overflow), the per-workflow `budget.json` entry has `dispatch_eligible: false` (D-03 TOK-02 closure)
    - Test: the published `index.json` `routes[]` shape is unchanged — `validRoutes()` still passes (D-05 dispatch surface preserved)
  </behavior>
  <action>
    Edit `src/prompt/publish-index.mjs` and create `src/orchestrator/workflow-declarations.json` per the locked contract in 19-ORCHESTRATOR-INPUT-DECISION.md. Concrete changes:

    1. **Add orchestrator imports** at top (alongside existing :1-5):
       `import { selectCapabilities } from '../orchestrator/select.mjs';`
       `import { selectWorkflow, nextValidTransitions, WORKFLOW_TRANSITIONS } from '../orchestrator/transitions.mjs';`
       `import { planContextLoad, DEFAULT_CONTEXT_CONTRACT } from '../orchestrator/budget.mjs';`
       (D-08 is preserved — the hook imports `prompt-route.mjs`, NOT `publish-index.mjs`. publish-index.mjs is controller-side; adding orchestrator imports here does NOT enter the hook import graph. This is the D-07/D-08 payoff: only the controller/watcher needs the orchestrator.)

    2. **Load workflow-declarations.json** inside `publishCompiledIndex` via the relative path `join(dirname(fileURLToPath(import.meta.url)), '..', 'orchestrator', 'workflow-declarations.json')` using `readFileSync`. Wrap in try/catch — if the file is missing, throw `TypeError('workflow declarations source missing')` (fail-closed, not silent). Parse as JSON. Cache the parse outside the route loop (read once per publish). Add `import { readFileSync } from 'node:fs'` if not already imported (it is — :2).
       Also add `import { fileURLToPath } from 'node:url'` if not already imported (check :1-5; add if missing).

    3. **Delete the blanket fallback at :63-67** — remove the entire `if (!Object.keys(routes).length) { for (const record of registry.records.filter(...)) ... }` block. The `:68` throw remains the only empty-mapping path. This is D-06 (ORC-01 closure). After deletion, `grep -c "canonical_record" src/prompt/publish-index.mjs` returns 0.

    4. **After the routes loop (:59-62) and before the tuple write block (:74-89), insert the orchestrator wiring block** — for each `workflow_id` in `Object.keys(routes)` (the mapped subjects' subject_ids), run:
       a. Construct synthetic `evidence = { status: 'active', freshness: 'fresh', position: { family: <family derived from workflow_id — prefix before first '-' or full workflow_id>, state: 'planned' }, gates: { plan_approved: true }, dependencies_safe: true }`.
       b. `const transitionResult = nextValidTransitions(evidence, WORKFLOW_TRANSITIONS);` — if `transitionResult.status !== 'candidates_available'`, bake `closure_by_workflow[workflow_id] = { dispatch_eligible: false, reason_code: transitionResult.reason_code, selected_transition: null, candidates: [], closure: [], invokable_capabilities: [], required_models: [], required_permissions: [], lifecycle_bindings: [] }` and skip to the next workflow_id.
       c. `const selected = selectWorkflow(transitionResult, undefined);` — if `selected.status !== 'selected'`, bake `dispatch_eligible: false` with `selected.reason_code` and skip.
       d. `const closureResult = selectCapabilities({ workflow: selected, workflowDeclarations, registry, requestedScope: undefined, explicitCapability: undefined });` — if `closureResult.status !== 'resolved'`, bake `dispatch_eligible: false` with `closureResult.reason_code` and skip.
       e. `const budgetResult = planContextLoad({ workflow: selected, closure: closureResult.closure, contract: DEFAULT_CONTEXT_CONTRACT, sources: [], summaryIndex: null, baseline: undefined });` — sources/summaryIndex can be empty for v1 (the budget contract is still evaluated over the closure; per-prompt source budget is v2 per D-03). If `budgetResult.status !== 'planned'`, bake `dispatch_eligible: false` with `budgetResult.reason_code`.
       f. Bake the per-workflow entry into the three by_workflow maps:
          - `closure_by_workflow[workflow_id] = { selected_transition: selected.selection, candidates: transitionResult.candidates, closure: closureResult.closure, invokable_capabilities: closureResult.invokable_capabilities, required_models: closureResult.required_models, required_permissions: closureResult.required_permissions, lifecycle_bindings: closureResult.lifecycle_bindings, dispatch_eligible: closureResult.dispatch_eligible && budgetResult.dispatch_eligible, reason_code: closureResult.dispatch_eligible ? (budgetResult.dispatch_eligible ? 'resolved' : budgetResult.reason_code) : closureResult.reason_code }`
          - `budget_by_workflow[workflow_id] = { report: budgetResult.report, dispatch_eligible: budgetResult.dispatch_eligible, reason_code: budgetResult.reason_code }`
          - `summary_index_by_workflow[workflow_id] = null` (v1 — summary-index reuse is wired when summaries exist; the sibling file is present with the by_workflow map keyed, values null. This satisfies D-05 "summary-index ref" — the ref shape exists, the value is null until summaries are produced. Document this in a comment.)
       g. The `routes[workflow_id]` entry itself is UNCHANGED (D-05 — routes[] stays the compact dispatch contract). The orchestrator output goes ONLY into siblings.

    5. **Write the three sibling files** inside the existing `if (!existsSync(tupleRoot))` block (:81-91) alongside the existing `durableWrite(join(tupleRoot, 'registry.json'), ...)` and `durableWrite(join(tupleRoot, 'index.json'), compiledBytes)`:
       - `const closureBytes = json({ schema_version: 1, by_workflow: closure_by_workflow });`
       - `const budgetBytes = json({ schema_version: 1, by_workflow: budget_by_workflow });`
       - `const summaryIndexBytes = json({ schema_version: 1, by_workflow: summary_index_by_workflow });`
       - `durableWrite(join(tupleRoot, 'closure.json'), closureBytes);`
       - `durableWrite(join(tupleRoot, 'budget.json'), budgetBytes);`
       - `durableWrite(join(tupleRoot, 'summary-index.json'), summaryIndexBytes);`
       Use the existing `json()` helper (stableStringify-based, already imported via `:3`).

    6. **Extend the manifest** at :85-91 with three new payload_sha256 fields alongside the existing `registry` and `compiled` entries:
       - `closure: { payload_sha256: sha256(closureBytes) },`
       - `budget: { payload_sha256: sha256(budgetBytes) },`
       - `summary_index: { payload_sha256: sha256(summaryIndexBytes) },`
       The existing `sha256` helper (already defined in publish-index.mjs) is reused. The manifest `schema_version` at :85 stays `1` (the manifest schema_version is the manifest's own schema, NOT the compiled-index schema — do NOT bump it). Add the three new fields inside the existing manifest object literal.

    7. **Bump the pointer schema_version** at :97 from `1` to `2`: `const pointer = { schema_version: 2, tuple_version_id: tupleVersionId };` (matches the compile-index.mjs verifyTuple pointer check update from Task 1).

    8. **Create `src/orchestrator/workflow-declarations.json`** — a JSON array of workflow declaration records, one per workflow family in the v1.2 workflow set. Each record shape: `{ "workflow_id": "<id>", "owners": ["<owner_id>", ...], "requirements": ["<req_id>", ...], "compatible": ["<compatible_workflow_id>", ...] }`. Populate with at least the workflow families referenced by the existing tests and the v1.2 workflow set (gsd-execute-phase, gsd-debug, gsd-quick, etc. — consult `tests/router.workflow-orchestrator.test.mjs:workflowDeclaration()` for the template and the v1.2 release matrix for the authoritative workflow list). For workflows where owners/requirements are unknown, use empty arrays — the orchestrator `declarationFor` lookup (`select.mjs`) handles the match by `workflow_id` and tolerates empty owners/requirements. Add a comment header in the file (JSON does not support comments — instead add a `_meta` field: `{ "_meta": { "purpose": "Publish-time orchestrator workflow declarations (Phase 19 D-01). Consumed by publishCompiledIndex via relative path. Keep in sync with the v1.2 workflow set.", "schema_version": 1 } }` alongside the array, OR structure as `{ "schema_version": 1, "declarations": [ ... ] }` — pick the latter for parseability and consistency with the sibling file shape).

    Run `node --test tests/router.workflow-orchestrator.test.mjs` (orchestrator unit tests must still pass — the modules are unchanged) and the publish-index tests. Create `tests/router.publish-index.orchestrator.test.mjs` if no test covers the new publish-path behavior (Wave 0 gap).
  </action>
  <verify>
    <automated>node --test tests/router.publish-index.orchestrator.test.mjs tests/router.workflow-orchestrator.test.mjs 2>&1 | tail -20; grep -c "canonical_record" src/prompt/publish-index.mjs | grep -q '^0$'; grep -c "selectCapabilities" src/prompt/publish-index.mjs | grep -qv '^0$'; grep -c "nextValidTransitions" src/prompt/publish-index.mjs | grep -qv '^0$'; grep -c "planContextLoad" src/prompt/publish-index.mjs | grep -qv '^0$'; grep -c "closure.json" src/prompt/publish-index.mjs | grep -qv '^0$'; grep -c "manifest.closure\|closure: { payload_sha256" src/prompt/publish-index.mjs | grep -qv '^0$'; test -f src/orchestrator/workflow-declarations.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "canonical_record" src/prompt/publish-index.mjs` returns 0 (D-06 fallback removed)
    - `grep -c "selectCapabilities\|nextValidTransitions\|planContextLoad" src/prompt/publish-index.mjs` returns ≥3 (orchestrator wired per D-01)
    - `grep -c "closure.json" src/prompt/publish-index.mjs` returns ≥1 (sibling durableWrite per D-05)
    - `grep -c "budget.json" src/prompt/publish-index.mjs` returns ≥1
    - `grep -c "summary-index.json" src/prompt/publish-index.mjs` returns ≥1
    - `grep -c "payload_sha256: sha256(closureBytes)\|closure: { payload_sha256" src/prompt/publish-index.mjs` returns ≥1 (T-19-01 manifest hash extension)
    - `grep -c "schema_version: 2" src/prompt/publish-index.mjs` returns ≥1 (pointer schema bump)
    - `src/orchestrator/workflow-declarations.json` exists and `JSON.parse(readFileSync(...))` succeeds with a `declarations` array
    - `node --test tests/router.publish-index.orchestrator.test.mjs` exits 0 (new publish-path behavior verified)
    - `node --test tests/router.workflow-orchestrator.test.mjs` exits 0 (orchestrator modules unchanged — no regression)
    - The `routes[]` building loop at :59-62 is byte-identical to the pre-edit version (only the fallback :63-67 and the post-loop orchestrator block are added/removed)
  </acceptance_criteria>
  <done>
    publish-index.mjs calls the three orchestrator functions per mapped subject, writes three sibling tuple files via durableWrite, extends the manifest with three payload_sha256 fields (T-19-01 mitigated), bumps the pointer schema_version to 2, and has the :63-67 blanket fallback deleted (D-06). workflow-declarations.json exists with at least one declaration record. New publish-path tests pass; orchestrator unit tests show no regression.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| controller→tuple-disk | Publish-time orchestrator output frozen into immutable sibling files on disk |
| disk→route-path | Route path reads baked siblings via loadCompiledIndex; untrusted disk bytes cross into the hot path through the bounded reader |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-01 | Tampering | sibling tuple files closure.json/budget.json/summary-index.json — swapped or missing on disk | high | mitigate | Manifest gains closure/budget/summary_index payload_sha256 fields (Task 2 step 6); verifyTuple hash-checks each sibling against the manifest (Task 1 step 7); missing/mismatched hash → tuple rejected → loadCompiledIndex returns blocked(). This is the Phase 17 D-02 fail-closed integrity gate extended to siblings. |
| T-19-02 | Tampering | src/orchestrator/workflow-declarations.json — malicious declarations injected pre-publish | medium | mitigate | Declarations file is controller-owned (deployed via moduleNames into ownedRoot/modules/); publishCompiledIndex reads it via relative path. Tampering requires controller compromise. The bundled file is verified by the existing installRouter moduleValues deployment (readFileSync from sourceRoot). Orchestrator functions validate all declaration inputs (`select.mjs:5-7, :64-72` — `validId`, `declarationFor` rejects ambiguous matches); a malformed declaration produces `workflow_declaration_invalid` blocked, never dispatch. |
| T-19-03 | Spoofing | schema-downgrade attack — old schema-1 tuple re-injected | high | mitigate | compatible() now requires the two new members (Task 1 step 5); schema-1 tuples missing them are rejected. loadCompiledIndex falls through to blocked(). The watcher re-publishes a schema-2 tuple from authoritative disk state (Phase 14/17 recovery). |
| T-19-04 | Information disclosure | secrets/PII in sibling tuple files | low | accept | Sibling files contain only registry-derived closure/budget — no prompt text, no secrets. Phase 17 D-06 privacy constraints are inherited; the orchestrator functions do not accept prompt text as input. |
| T-19-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this phase (stdlib-only wiring per CLAUDE.md). N/A — satisfied trivially. |
</threat_model>

<verification>
- `node --test tests/router.compiled-index.test.mjs tests/router.compiled-index.schema2.test.mjs tests/router.publish-index.orchestrator.test.mjs tests/router.workflow-orchestrator.test.mjs` passes (new-behavior tests green; orchestrator unit tests show no regression).
- `grep -c "canonical_record" src/prompt/publish-index.mjs` returns 0 (D-06).
- `grep -c "COMPILED_INDEX_SCHEMA_VERSION = 2" src/prompt/compile-index.mjs` returns 1 (D-04).
- `grep -c "from '../orchestrator" src/prompt/compile-index.mjs` returns 0 (D-08 preserved — CONTEXT_CONTRACT_VERSION inlined).
- `grep -c "from '../orchestrator" src/prompt/publish-index.mjs` returns ≥3 (D-01 wiring; publish-index is controller-side, not hook-side).
- Prior schema-1 tuple fixtures in `tests/router.compiled-index.test.mjs` are now failing — this is expected; Plan 04 updates them to schema 2.
- Full suite `node --test tests/*.test.mjs` is NOT expected green yet (fixture churn in Plan 04); only the directly-touched + orchestrator suites must pass.
</verification>

<success_criteria>
- ORC-01 closed: no `canonical_record` route is produced; empty mapping → publish fails at :68 → no dispatch.
- TOK-02 closed: each workflow carries a baked budget report; required-overflow → dispatch_eligible:false at publish; route path observes the flag (implemented in Plan 03).
- T-19-01 mitigated: sibling tampering is detected via manifest hash verification → blocked().
- D-08 preserved: compile-index.mjs (transitively imported by the hook via prompt-route.mjs) does NOT import from src/orchestrator/* — CONTEXT_CONTRACT_VERSION is inlined as a literal.
- D-04/D-05/D-06 implemented verbatim per the locked contract in 19-ORCHESTRATOR-INPUT-DECISION.md.
</success_criteria>

<output>
Create `.planning/phases/19-close-gap-tok-02-orc-01-wire-orchestrator-select-transitions/19-02-SUMMARY.md` when done.

## Artifacts this phase produces

**New exported symbols:** none (no new exports added; existing orchestrator exports consumed, existing compile-index/publish-index functions extended in-place).

**New files created:**
- `src/orchestrator/workflow-declarations.json` — static workflow declarations source consumed by publishCompiledIndex (Plan 03 deploys it via moduleNames extension)
- `tests/router.compiled-index.schema2.test.mjs` — new-behavior test for schema 2 / compatible() / sibling hash checks / loadCompiledIndex return keys (Wave 0 scaffold, created by Task 1)
- `tests/router.publish-index.orchestrator.test.mjs` — new-behavior test for publish-path orchestrator wiring + sibling writes + fallback removal (Wave 0 scaffold, created by Task 2)

**Modified files:**
- `src/prompt/compile-index.mjs` — `COMPILED_INDEX_SCHEMA_VERSION: 2` (was 1), `COMPILED_INDEX_COMPATIBILITY` +2 members (`orchestrator_contract_version: 'workflow-first-v1'`, `context_contract_version: <inlined literal>`), `COMPILED_INDEX_LIMITS` +3 members (`closure_bytes`, `budget_bytes`, `summary_index_bytes`), `compatible()` +2 checks, `verifyTuple` +3 sibling hash checks + pointer schema_version 2 check, `loadCompiledIndex` ready-returns +3 keys (`closure`, `budget`, `summaryIndex`)
- `src/prompt/publish-index.mjs` — +3 orchestrator imports (select.mjs, transitions.mjs, budget.mjs), workflow-declarations.json read, per-mapped-subject orchestrator calls (nextValidTransitions/selectWorkflow/selectCapabilities/planContextLoad), 3 sibling durableWrite calls (closure.json/budget.json/summary-index.json), manifest +3 payload_sha256 fields (`closure`/`budget`/`summary_index`), pointer `schema_version: 2`, blanket fallback :63-67 deleted (removed symbol: `reason_code: 'canonical_record'`)

**New sibling tuple file names** (written into release-tuples/versions/<id>/):
- `closure.json` — `{ schema_version: 1, by_workflow: { "<workflow_id>": { selected_transition, candidates, closure, invokable_capabilities, required_models, required_permissions, lifecycle_bindings, dispatch_eligible, reason_code } } }`
- `budget.json` — `{ schema_version: 1, by_workflow: { "<workflow_id>": { report, dispatch_eligible, reason_code } } }`
- `summary-index.json` — `{ schema_version: 1, by_workflow: { "<workflow_id>": null } }` (v1 — values null until summaries are produced)

**New manifest fields:** `closure.payload_sha256`, `budget.payload_sha256`, `summary_index.payload_sha256` (alongside existing `registry.payload_sha256` and `compiled.payload_sha256`)

**New `dispatch_eligible` flag field:** baked per-workflow in closure.json + budget.json entries (from selectCapabilities + planContextLoad results; required-overflow → false per D-03)

**Removed symbols:** `reason_code: 'canonical_record'` and the blanket fallback block at publish-index.mjs:63-67 (D-06)

The newly-created symbols that downstream plan-review-convergence source-grounding must exclude from drift verification: `src/orchestrator/workflow-declarations.json`, `closure.json`/`budget.json`/`summary-index.json` sibling file names, `orchestrator_contract_version`/`context_contract_version` compatibility members, `closure_bytes`/`budget_bytes`/`summary_index_bytes` limits, `closure`/`budget`/`summaryIndex` loadCompiledIndex return keys, `closure`/`budget`/`summary_index` manifest fields, per-workflow `dispatch_eligible` flag in siblings, the two new test files listed above.
</output>
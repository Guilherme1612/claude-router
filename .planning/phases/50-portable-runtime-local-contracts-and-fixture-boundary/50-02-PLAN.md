---
phase: 50-portable-runtime-local-contracts-and-fixture-boundary
plan: 02
type: execute
wave: 2
depends_on: [50-01]
files_modified:
  - src/adapters/claude.mjs
  - src/adapters/codex.mjs
  - src/registry/schema.mjs
  - src/registry/contract.mjs
  - src/registry/eligibility.mjs
  - src/registry/build.mjs
  - src/coverage/audit.mjs
  - tests/helpers/inventory-fixture.mjs
  - tests/router.v18-contracts.test.mjs
autonomous: true
requirements: [CVRG-02, CVRG-03, CVRG-04]
must_haves:
  truths:
    - "Every assembled record has stable semantic identity, portable runtime locator, provenance, availability, source freshness, and exactly one class from the eight-class coverage set."
    - "Routable and composable records expose bounded typed intents, inputs, outputs, effects, risk, authority, composition, cost, evidence, and alias metadata."
    - "Unknown effects, authority, dependencies, risk, invalid metadata, and unavailable targets remain inspectable with independent deterministic reasons and cannot be executable selections."
    - "Coverage record count and unique classified ID count equal assembled registry record count; unclassified is empty."
    - "Existing record fields, report fields, canonical ordering, and existing callers remain compatible."
  prohibitions:
    - "Names, aliases, descriptions, and paths MUST NOT infer executable effects, authority, risk, dependencies, or permissions."
    - "Invalid or unavailable records MUST NOT be dropped to make coverage appear complete."
    - "Phase 50 MUST NOT add a second registry, routing owner, dependency, package, service, or database migration."
  artifacts:
    - src/registry/schema.mjs
    - src/registry/contract.mjs
    - src/registry/eligibility.mjs
    - src/registry/build.mjs
    - src/coverage/audit.mjs
    - tests/router.v18-contracts.test.mjs
  key_links:
    - "adapter observation -> canonicalizeCapability -> buildCapabilityContract -> evaluateEligibility -> classifyCoverage"
    - "assembled records -> auditCoverage record output -> exactly-once completeness assertion"
---

<objective>
Extend the existing registry pipeline with portable typed capability truth, fail-closed execution eligibility, and exactly-once record coverage for Claude and Codex.

Purpose: Make semantic routing inspectable and safe before later phases add retrieval and composition.
Output: Additive canonical metadata, versioned contract semantics, exhaustive classification/reporting, and focused plus full regression evidence.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/50-portable-runtime-local-contracts-and-fixture-boundary/50-CONTEXT.md
@.planning/phases/50-portable-runtime-local-contracts-and-fixture-boundary/50-RESEARCH.md
@.planning/phases/50-portable-runtime-local-contracts-and-fixture-boundary/50-PATTERNS.md
@.planning/phases/50-portable-runtime-local-contracts-and-fixture-boundary/50-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify typed metadata, fail-closed unknowns, and exactly-once coverage</name>
  <files>tests/router.v18-contracts.test.mjs, tests/helpers/inventory-fixture.mjs</files>
  <read_first>
    - tests/router.v18-contracts.test.mjs (Plan 01 portable boundary; extend rather than replace)
    - tests/helpers/inventory-fixture.mjs (existing record(), contractEvidence(), and materialized scenario helpers)
    - tests/router.contract-inspection.test.mjs (contract envelope and stable snapshot assertions)
    - tests/router.contracts.test.mjs (primary complete-field contract loop and unknown-future profile analog)
    - tests/router.phase26-dual-runtime.test.mjs (per-runtime parity analog)
    - tests/router.context-sources.test.mjs (private-path negative assertion analog)
    - tests/router.coverage.test.mjs (auditCoverage report compatibility expectations)
    - tests/router.registry-build.test.mjs (assembleRegistry and acquisition patterns)
    - src/registry/schema.mjs (current canonical defaults and validation)
    - src/registry/eligibility.mjs (existing independent gate result shape)
  </read_first>
  <action>
    Extend `tests/router.v18-contracts.test.mjs` before source changes, retaining the mapped top-level contract-loop, dual-runtime, and privacy-negative test styles. Define the exact allowed set `routable`, `composable`, `direct-only`, `hook-owned`, `project-scoped`, `unavailable`, `invalid`, `excluded`. Build registry cases from the Plan 01 scenarios and existing in-memory helper records, including stale and unknown-future profiles. Assert every assembled record contains the bounded metadata shape from 50-RESEARCH.md plus typed `inputs`; record count equals coverage-report record count; classified IDs are unique; every class belongs to the set; `unclassified` is empty; and Claude/Codex completeness is calculated independently.

    Add separate records with unknown effects, authority, dependencies, and risk. Assert each remains present, has `eligible: false`, has a field-specific deterministic reason, receives the non-executable `invalid` coverage class, and cannot be `routable` or `composable`. Add declared-metadata precedence assertions and a negative case proving a capability name such as `dangerous-admin-delete` cannot infer execution authority or effects. Add stable serialization and private-path rejection assertions. Extend `contractEvidence()` only with the minimum variants needed by these tests; preserve all current variants.

    Run the new test alone and record the expected RED result in the execution summary before implementing source changes.
  </action>
  <acceptance_criteria>
    - The test names map explicitly to CVRG-02, CVRG-03, and CVRG-04.
    - The pre-implementation run fails because typed metadata and exhaustive classes are absent, not because of fixture or syntax errors.
    - Typed intents, inputs, outputs, effects, risk, authority, composition, cost, evidence, and aliases are asserted explicitly.
    - Unknown effects, authority, dependencies, and risk are asserted independently, and stale/unknown-future records remain inspectable.
    - The test rejects duplicate, missing, or unknown classifications and any serialized private absolute path.
  </acceptance_criteria>
  <verify>
    <automated>rtk node --test tests/router.v18-contracts.test.mjs</automated>
  </verify>
  <done>The v1.8 contract suite is a valid RED specification for all remaining Phase 50 behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize bounded semantic contracts without widening authority</name>
  <files>src/adapters/claude.mjs, src/adapters/codex.mjs, src/registry/schema.mjs, src/registry/contract.mjs, src/registry/eligibility.mjs</files>
  <read_first>
    - src/adapters/claude.mjs (shared adapter factory, portable provenance, diagnostics, inferred metadata)
    - src/adapters/codex.mjs (Codex layout and factory reuse)
    - src/registry/schema.mjs (normalizeRecord, canonicalizeCapability, validateCapability, stableStringify)
    - src/registry/contract.mjs (CONTRACT_FIELDS, DISPATCH_FIELDS, evidence classes, policy version, overlays)
    - src/registry/eligibility.mjs (fieldState and independent gates)
    - .planning/phases/50-portable-runtime-local-contracts-and-fixture-boundary/50-RESEARCH.md (exact bounded metadata shape and inference rules)
    - tests/router.v18-contracts.test.mjs (RED acceptance contract)
  </read_first>
  <action>
    Extend the existing canonicalization path so each record deterministically receives: `semantic` with sorted unique intents, subjects, operations, outputs, evidence, aliases; typed `inputs`; `effects`; `risk {level, source}`; `authority {ceiling, source}`; `composition {roles, requires, conflicts, exclusive}`; `cost {latency, context_bytes, tool_calls}`; `coverage {classification, reasons}`; and `source_freshness {fingerprint, observed_at}`. Keep existing fields and outer schema compatibility. Validate bounded enums, booleans, non-negative nullable counts, arrays, portable provenance, and freshness fingerprints at the existing schema trust boundary.

    In `contract.mjs`, expose the new semantic data through existing evidence/envelope semantics and bump only the contract policy version whose semantics changed. Declared evidence wins; safe inference may populate retrieval-only semantic arrays and aliases. It must leave executable effects, risk, authority, dependencies, permissions, and composition requirements unknown unless declared or structurally proven. Preserve deterministic sorting and overlay compatibility.

    Reuse `evaluateEligibility()` as the execution authority. Add only missing independent gates/reason codes required for unknown authority and semantic dependencies; do not collapse existing effects, risk, dependency, permission, scope, or confidence gates. Any unknown execution-critical value sets `eligible: false` and `recommendation_only: true` while retaining the record and all reasons.
  </action>
  <acceptance_criteria>
    - `canonicalizeCapability()` emits typed intents, inputs, outputs, effects, risk, authority, composition, cost, evidence, aliases, and the complete bounded metadata shape with deterministic ordering.
    - Existing valid v1 records canonicalize without caller changes.
    - Declared values override inference; name-only inference affects retrieval semantic arrays/aliases only.
    - Unknown effects, authority, dependencies, and risk each produce a distinct failing eligibility reason.
    - Adapter provenance remains logical-root/relative-path portable for both runtimes.
  </acceptance_criteria>
  <verify>
    <automated>rtk node --test tests/router.v18-contracts.test.mjs tests/router.contract-inspection.test.mjs tests/router.contract-eligibility.test.mjs</automated>
  </verify>
  <done>Typed records and contracts pass their focused tests without making unknown critical metadata executable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Classify every retained record once and report exhaustive coverage</name>
  <files>src/registry/build.mjs, src/coverage/audit.mjs, tests/router.v18-contracts.test.mjs</files>
  <read_first>
    - src/registry/build.mjs (assembleRegistry grouping, contract build, eligibility loop, deterministic output)
    - src/coverage/audit.mjs (current report schema, records, diagnostics, counts, and caller compatibility)
    - src/registry/schema.mjs (canonical coverage defaults and stableStringify)
    - src/registry/eligibility.mjs (eligible, recommendation_only, gates, reason_codes, quarantine)
    - tests/router.coverage.test.mjs (existing report assertions)
    - tests/router.registry-build.test.mjs (existing retained-record and deterministic-build assertions)
    - tests/router.v18-contracts.test.mjs (exactly-once RED assertions)
  </read_first>
  <action>
    Add one exported frozen `COVERAGE_CLASSES` set/array and one pure `classifyCoverage(record, eligibility)` owner in `src/registry/build.mjs` or the smallest existing single-owner seam. Use deterministic ordered precedence: `invalid` for invalid metadata, quarantine, or `recommendation_only` caused by unknown/failed execution-critical effects, authority, dependencies, risk, permission, or confidence gates; `unavailable` for a missing/unavailable target or invocation; `excluded` for explicit exclusion; then hook ownership, project scope, direct-only invocation, composable role, and finally `routable` only for fully eligible records. Return exactly `{classification, reasons}` and attach it to each retained assembled record after contract and eligibility evaluation. Never filter a record to improve coverage.

    Extend `auditCoverage()` additively to accept or derive assembled record classifications and emit record-level `{id, runtime, kind, classification, reasons}` entries plus `unclassified`. Preserve all existing report fields, classifications, diagnostics, and counts used by current callers. Add per-runtime discovered/classified counts without asserting cross-runtime equality. Sort record output by stable ID and reject duplicate IDs or missing/unknown classifications as coverage failures.

    Make the v1.8 tests GREEN, then run the focused registry/coverage/contract suite followed by the serial full repository suite. Fix shared-root regressions only; do not weaken new safety assertions or rewrite unrelated modules.
  </action>
  <acceptance_criteria>
    - Every assembled record is retained and has exactly one allowed coverage class and deterministic reasons.
    - `report.records.length === registry.records.length`, classified IDs are unique, and `report.unclassified.length === 0` for valid fixture builds.
    - Invalid, unavailable, excluded, and unknown-critical records remain visible and are not executable; unknown-critical `recommendation_only` records deterministically classify as `invalid` with their gate reasons.
    - Existing coverage-report consumers and focused regressions pass unchanged or with additive assertions only.
    - Claude and Codex each report 100% classification independently on asymmetric fixtures.
  </acceptance_criteria>
  <verify>
    <automated>rtk node --test tests/router.v18-contracts.test.mjs tests/router.registry-build.test.mjs tests/router.coverage.test.mjs tests/router.contract-inspection.test.mjs</automated>
  </verify>
  <done>The focused suite is green with exhaustive portable classification and no private-path leakage.</done>
</task>

</tasks>

<threat_model>
## Phase 50 Plan 02 Threat Model — ASVS Level 1

| Threat ID | Severity | Threat | Mitigation | Automated proof |
|---|---|---|---|---|
| T-50-01 | high | Portable output leaks a live absolute path | Validate symbolic logical roots and relative paths; serialize fixture output and reject live home/workspace strings | v1.8 private-path assertions |
| T-50-02 | high | Name or alias inference widens effects, authority, risk, dependencies, or executable eligibility | Restrict inference to retrieval semantics; unknown critical fields independently fail eligibility | adversarial name and unknown-field tests |
| T-50-03 | high | Invalid records are dropped or multiply classified, creating false 100% coverage | Retain all assembled records; one ordered classifier; compare counts and unique IDs; report unclassified | exhaustive coverage assertions |

No packages are installed and no database schema is changed. High-severity threats block phase completion.
</threat_model>

<verification>
- `rtk node --test tests/router.v18-contracts.test.mjs tests/router.registry-build.test.mjs tests/router.coverage.test.mjs tests/router.contract-inspection.test.mjs` exits 0.
- `rtk node --test --test-concurrency=1 tests/*.test.mjs` exits 0.
- `rtk git diff --check` exits 0.
- Serialized v1.8 fixture registry and coverage output contains no live home or workspace path.
</verification>

<success_criteria>
- CVRG-02 through CVRG-04 are automated and green.
- Every Claude/Codex discovered record is portable, typed, inspectable, and classified exactly once.
- Unknown or invalid execution-critical metadata fails closed without disappearing.
- Focused and full serial regression suites pass with no new dependency or routing path.
</success_criteria>

<output>
Create `.planning/phases/50-portable-runtime-local-contracts-and-fixture-boundary/50-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `src/registry/schema.mjs`: canonical typed `semantic`, `inputs`, `effects`, `risk`, `authority`, `composition`, `cost`, `coverage`, and `source_freshness` record fields.
- `src/registry/contract.mjs`: versioned typed semantic contract evidence/envelopes.
- `src/registry/eligibility.mjs`: independent authority and semantic-dependency fail-closed reasons where absent.
- `src/registry/build.mjs`: `COVERAGE_CLASSES` and `classifyCoverage(record, eligibility)` plus classified retained records.
- `src/coverage/audit.mjs`: additive record-level coverage and `unclassified` output with per-runtime completeness counts.
- `tests/router.v18-contracts.test.mjs`: exhaustive typed-contract, inference-safety, classification, parity, and privacy suite.

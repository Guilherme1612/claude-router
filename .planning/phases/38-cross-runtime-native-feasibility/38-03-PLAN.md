---
phase: 38-cross-runtime-native-feasibility
plan: 03
type: execute
wave: 3
depends_on: [38-02]
files_modified:
  - tests/phase-38/budget.test.mjs
  - src/lifecycle/router-lifecycle.mjs
autonomous: true
requirements: [HOST-04]
must_haves:
  truths:
    # --- HOST-04 covered acceptance criteria (exact thresholds) ---
    - "Warm prompt path latency: p95 <= 25ms, p99 <= 50ms, hard max < 100ms (measured via process.hrtime.bigint() in the hook with ROUTER_DEBUG_LATENCY=1, across >= 50 invocations)."
    - "Startup briefing latency: p95 <= 50ms (measured across >= 20 cold-start invocations)."
    - "Normal injection size: <= 120 tokens (measured via tokenCount(), Math.ceil(len/4))."
    - "The prompt path performs NO spawn/spawnSync/execSync, NO file scanning, NO hashing of prompt-derived content, NO network/API/LLM calls, NO mutation of user code, and NO learning/weight updates."
    - "The dispatch path (off the hot path) does not count against the prompt-path hrtime budget (fire-and-forget unref)."
    - "On any exception in the dispatch trigger, the hook passes through the original prompt unchanged, exits 0, and never emits decision:'block' (fail-open)."
    # --- Edge-probe coverage (HOST-04 boundary/precision/concurrency) ---
    # boundary
    - "Latency/token budgets hold exactly at and one step beyond thresholds: a run whose p95 is > 25ms or whose max is >= 100ms fails the budget test; an injection of > 120 tokens fails the budget test."
    # precision
    - "Latency is measured via process.hrtime.bigint() (nanosecond, no overflow for any realistic run); receipt wall_ms is Number(ns_delta)/1e6 (millisecond, nanosecond-precision source); token count is Math.ceil(String(text).length/4) (ceiling, no rounding/tie-breaking ambiguity)."
    # concurrency
    - "Dispatch spawn is fire-and-forget unref(); concurrent prompt-path invocations share no mutable dispatch state; receipts are append-only via appendFileSync (macOS PIPE_BUF 4096, lines < 4KB) so concurrent sessions do not interleave (existing telemetry.jsonl discipline)."
  prohibitions:
    - statement: "The prompt path must NEVER spawn a child process (spawnSync/execSync/spawn) — dispatch is off the hot path only (fire-and-forget unref)."
      status: unverified
      flagged: true
    - statement: "The hook must NEVER exit 2 or emit decision:'block' on any dispatch-path failure — fail-open with pass-through, exit 0."
      status: unverified
      flagged: true
    - statement: "The prompt path must NEVER perform scanning, hashing of prompt-derived content, network/API/LLM calls, mutation, or learning (HOST-04 invariants)."
      status: unverified
      flagged: true
  artifacts:
    - tests/phase-38/budget.test.mjs
    - src/lifecycle/router-lifecycle.mjs (modified — moduleNames + gateFixtureNames additions)
  key_links:
    - "router.perf.test.mjs hrtime pattern → budget.test.mjs prompt-path latency assertion"
    - "router-lifecycle.mjs moduleNames bundle → deployed adapters/dispatch/* + tests/phase-38/fixtures/harmless.mjs"
---

<objective>
Prove the prompt and startup paths still meet their latency and token budgets and remain read-only/fail-open with the new dispatch surface wired (HOST-04), and ship the new adapters + fixture into the deployed tree via the lifecycle bundle. This is the final feasibility gate: the dispatch layer exists, but if it bends the prompt budget or fails to deploy, the feasibility proof is incomplete.

Purpose: HOST-04 is the invariant that protects everything else. Phase 38 adds a real spawn off the hot path; the budget test proves the prompt path is untouched (read-only, no scan/hash/network/LLM/mutation/learning) and the latency/token budgets hold exactly at and beyond thresholds. The deploy bundle update ensures the new dispatch layer reaches ~/.claude/router/modules/ and ~/.codex/router/modules/ at install/upgrade.

Output: budget test (HOST-04), updated router-lifecycle.mjs bundle list.
</objective>

<execution_context>
@/Users/guilherme/.claude/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/38-cross-runtime-native-feasibility/38-RESEARCH.md
@.planning/phases/38-cross-runtime-native-feasibility/38-PATTERNS.md
@.planning/phases/38-cross-runtime-native-feasibility/38-VALIDATION.md
@.claude/CLAUDE.md

# Genuine dependency: needs both adapters from Plans 01+02 in the bundle, and the
# budget test asserts the prompt path stays clean WITH the dispatch trigger wired.
@.planning/phases/38-cross-runtime-native-feasibility/38-01-PLAN.md
@.planning/phases/38-cross-runtime-native-feasibility/38-02-PLAN.md

# Primary analogs:
@src/lifecycle/router-lifecycle.mjs
@tests/router.perf.test.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (tdd): Budget + invariants test (HOST-04)</name>
  <files>tests/phase-38/budget.test.mjs</files>
  <read_first>
    - tests/router.perf.test.mjs (spawnSync hook driver, ROUTER_DEBUG_LATENCY=1, TEST_HOME isolation, hrtime parse :1-40)
    - src/runtime/router.mjs (the hook under test — the off-hot-path dispatch trigger from Plan 01, detectRuntime, tokenCount :2755-2758, emit)
    - src/adapters/dispatch/claude.mjs and codex.mjs (from Plans 01/02 — to confirm the dispatch path does not run on the prompt path)
    - 38-PATTERNS.md §"Token budget (≤120 tokens for HOST-04)", §"Latency measurement", §"Test isolation"
    - 38-RESEARCH.md §Architecture Patterns (hot path vs dispatch path separation), §Validation Architecture (HOST-04 perf test), §Assumption A1 (unref'd child does not count against prompt latency)
  </read_first>
  <behavior>
    - Test 1 (warm prompt latency): spawnSync the hook with a trivial prompt and ROUTER_DEBUG_LATENCY=1 over >= 50 iterations (warm cache); parse the hook's hrtime debug line; assert p95 <= 25ms, p99 <= 50ms, max < 100ms.
    - Test 2 (startup latency): spawnSync the hook's SessionStart/startup path over >= 20 cold-start iterations (fresh temp HOME each run); assert p95 <= 50ms.
    - Test 3 (injection token budget): for a representative trivial prompt, parse the hook's stdout additionalContext and assert tokenCount(additionalContext) <= 120.
    - Test 4 (hot-path invariants — no spawn/scan/hash/network/LLM/mutation/learning): assert that grep -n "spawnSync\|execSync" src/runtime/router.mjs finds NO new prompt-path spawn (only the unref'd fire-and-forget in the off-hot-path trigger from Plan 01); assert the hook, when run with a dispatch-lease marker present, still returns < 100ms and does NOT hash prompt-derived content on the prompt path (the dispatch path's sha256 is over fixture stdout, not the prompt).
    - Test 5 (fail-open): run the hook with a broken dispatch adapter (e.g., point the fixture path at a missing file via env override); assert the hook exits 0, does not emit decision:'block', and passes the original prompt through unchanged.
    - Test 6 (boundary): explicitly construct an injection string of exactly 120 tokens (tokenCount == 120) and one of 121 tokens; assert the budget gate accepts 120 and rejects 121 (the gate is enforced).
  </behavior>
  <action>
    Write tests/phase-38/budget.test.mjs using node:test + assert/strict + spawnSync hook driver (tests/router.perf.test.mjs:30-40). Set ROUTER_DEBUG_LATENCY=1 and a temp HOME for isolation. For warm latency, run >= 50 iterations after a warmup; compute p95/p99/max from the parsed hrtime line (the hook prints an in-process latency to stderr when ROUTER_DEBUG_LATENCY=1 — reuse the existing parse). For startup latency, use the existing startup entrypoint with a fresh temp HOME per run. For Test 3, import tokenCount from src/runtime/router.mjs (router.mjs:2755-2758) and assert tokenCount(additionalContext) <= 120. For Test 4, use fs.readFileSync + a regex scan of router.mjs source to assert no new spawnSync/execSync on the prompt path (the only spawn is the unref'd fire-and-forget, identifiable by .unref() on the same line/region). For Test 5, set an env var that breaks the adapter (e.g., ROUTER_DISPATCH_FIXTURE=/nonexistent) and assert the hook still exits 0 with no decision:'block' in stdout. For Test 6, build strings of length 480 (120 tokens at chars/4) and 484 (121 tokens) and assert the cap. RED first against an unbounded baseline, GREEN against the Plan 01 trigger.
  </action>
  <verify>
    <automated>node --test tests/phase-38/budget.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/phase-38/budget.test.mjs` exits 0 with all 6 behavior cases green.
    - Warm prompt p95 <= 25ms, p99 <= 50ms, max < 100ms over >= 50 iterations (exact thresholds).
    - Startup p95 <= 50ms over >= 20 cold-start iterations.
    - tokenCount(additionalContext) <= 120 for a trivial prompt (exact threshold).
    - `grep -n "spawnSync\|execSync" src/runtime/router.mjs` finds no new prompt-path spawn (only the unref'd fire-and-forget).
    - Fail-open: broken adapter → hook exits 0, no decision:'block', prompt passed through.
    - Boundary: 120-token injection accepted, 121-token injection rejected by the cap.
  </acceptance_criteria>
  <done>HOST-04 is satisfied: the prompt and startup paths meet their exact latency/token budgets, the prompt path is read-only/fail-open with no spawn/scan/hash/network/LLM/mutation/learning, and the dispatch path (off the hot path) does not count against the prompt budget.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy bundle update — ship dispatch adapters + fixture (closes Plan 01 Assumption A4)</name>
  <files>src/lifecycle/router-lifecycle.mjs</files>
  <read_first>
    - src/lifecycle/router-lifecycle.mjs (moduleNames bundle :384-409, moduleValues flatMap deploys to ownedRoot + codexOwnedRoot, gateFixtureNames :436-438)
    - src/adapters/dispatch/contract.mjs, claude.mjs, codex.mjs, receipt.mjs (from Plans 01/02 — the files to add to moduleNames)
    - tests/phase-38/fixtures/harmless.mjs (from Plan 01 — the fixture to add to gateFixtureNames)
    - 38-RESEARCH.md §Assumption A4 (deploy bundle is the mechanism that copies dispatch/* into ~/.claude/router/modules/ and ~/.codex/router/modules/), §Runtime State Inventory
  </read_first>
  <action>
    In src/lifecycle/router-lifecycle.mjs, add to the moduleNames array (lines 384-409): 'adapters/dispatch/contract.mjs', 'adapters/dispatch/receipt.mjs', 'adapters/dispatch/claude.mjs', 'adapters/dispatch/codex.mjs'. Add 'tests/phase-38/fixtures/harmless.mjs' to gateFixtureNames (lines 436-438) alongside the existing gate test files. Add the four new tests (tests/phase-38/native-dispatch.test.mjs, claude-adapter.test.mjs, codex-adapter.test.mjs, recommendation-only.test.mjs, budget.test.mjs) to gateFixtureNames so the deploy gate runs them. Do NOT change the moduleValues flatMap — it already deploys every moduleName to BOTH ownedRoot and codexOwnedRoot (a single deployed module set serves both runtimes; receipts partition by runtime at read time per RESEARCH §Pitfall 3 / PARITY-02). Add a comment line citing Phase 38 + Assumption A4. Run the build/lifecycle gate to confirm the deployed tree now contains adapters/dispatch/*.mjs under both ~/.claude/router/modules/ and ~/.codex/router/modules/.
  </action>
  <verify>
    <automated>node --test tests/router.build-gate.test.mjs tests/router.adapters.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "adapters/dispatch/contract.mjs\|adapters/dispatch/receipt.mjs\|adapters/dispatch/claude.mjs\|adapters/dispatch/codex.mjs" src/lifecycle/router-lifecycle.mjs` returns 4 matches in moduleNames.
    - `grep -n "tests/phase-38/fixtures/harmless.mjs\|tests/phase-38/native-dispatch.test.mjs\|tests/phase-38/budget.test.mjs" src/lifecycle/router-lifecycle.mjs` returns matches in gateFixtureNames.
    - Running the lifecycle build produces deployed files at <ownedRoot>/modules/adapters/dispatch/contract.mjs (and receipt/claude/codex) and <codexOwnedRoot>/modules/adapters/dispatch/contract.mjs (etc.).
    - `node --test tests/router.build-gate.test.mjs` exits 0 (existing gate stays green).
    - The deployed harmless fixture is executable by the deployed adapter (path resolves inside the deployed tree, no path escape).
  </acceptance_criteria>
  <done>The new dispatch adapters + receipt module + harmless fixture + five phase-38 tests are in the deploy bundle; running the lifecycle build ships them to both ~/.claude/router/modules/ and ~/.codex/router/modules/, and the build gate stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prompt path → dispatch trigger | The off-hot-path trigger must not let dispatch failures affect the prompt path's latency/exit behavior. |
| deployed tree → host | The deployed adapters/dispatch/* must be the validated source copies; a stale or tampered deployed tree must not be invokable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-12 | Denial | dispatch trigger blows prompt latency budget | high | mitigate | Spawn is fire-and-forget unref (Plan 01); budget Test 1 asserts p95<=25ms, p99<=50ms, max<100ms with the trigger wired. |
| T-38-13 | Tampering | prompt-path mutation via spawn/scan/hash on hot path | high | mitigate | Test 4 asserts no new spawnSync/execSync on the prompt path and no prompt-derived hashing on the hot path (dispatch sha256 is over fixture stdout only). |
| T-38-14 | Denial | unbounded injection overflows the 10,000-char additionalContext cap | medium | mitigate | tokenCount cap <= 120 tokens (Test 3, Test 6 boundary); existing INJ-06 cap also enforces. |
| T-38-15 | Tampering | stale/tampered deployed adapter invoked instead of validated source | medium | mitigate | The lifecycle build copies source files to both ownedRoot and codexOwnedRoot; build-gate test confirms the deployed tree matches source. Deployed fixture path validated by within() containment. |
| T-38-16 | Denial | fail-open broken by dispatch exception | high | mitigate | Test 5 asserts broken adapter → exit 0, no decision:'block', prompt passed through. The dispatch trigger in router.mjs is wrapped in try/catch (fail-open) per Plan 01. |
| T-38-SC | Tampering | (none — no package installs) | low | accept | Stdlib only; no npm/pip/cargo installs this plan. |
</threat_model>

<verification>
- `node --test tests/phase-38/budget.test.mjs` is green (all 6 cases).
- `node --test tests/router.build-gate.test.mjs tests/router.adapters.test.mjs` is green (bundle update did not break existing gates).
- Deployed tree contains adapters/dispatch/{contract,receipt,claude,codex}.mjs under both ~/.claude/router/modules/ and ~/.codex/router/modules/.
</verification>

<success_criteria>
- HOST-04 is satisfied: warm prompt p95<=25ms, p99<=50ms, max<100ms; startup p95<=50ms; injection <=120 tokens; no scan/hash/network/LLM/mutation/learning on the prompt path; fail-open preserved; budget test green at and beyond exact thresholds.
- The dispatch adapters + receipt module + harmless fixture + all five phase-38 tests are in the deploy bundle and ship to both runtime trees; build gate stays green.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces (Plan 03)

| Symbol / File | Kind | Purpose |
|---------------|------|---------|
| `tests/phase-38/budget.test.mjs` | test | HOST-04 latency/token budget + read-only/fail-open invariants (boundary, precision, concurrency edges) |
| `src/lifecycle/router-lifecycle.mjs` (modified) | config/build | moduleNames + gateFixtureNames additions to ship dispatch adapters + fixture + tests to both runtime trees |

New symbols: none (reuses NativeDispatchAdapter, Receipt, ReceiptStore, adapters, fixture from Plans 01/02). This plan ships and budgets them.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/38-cross-runtime-native-feasibility/38-03-SUMMARY.md` when done
</output>
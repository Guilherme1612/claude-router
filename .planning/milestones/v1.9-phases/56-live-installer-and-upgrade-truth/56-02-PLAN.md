---
phase: 56-live-installer-and-upgrade-truth
plan: 02
type: execute
wave: 2
depends_on: [56-01]
files_modified:
  - .planning/evidence/v1.9/live-upgrade.json
  - .planning/evidence/v1.9/live-recovery.json
  - .planning/evidence/v1.9/live-after.json
autonomous: true
requirements: [LIVE-02, LIVE-03, LIVE-04]
must_haves:
  truths:
    - "The live installer dry-run completes before mutation, and the real install upgrades both supported runtime homes using the current source bundle while retaining the pre-upgrade snapshot."
    - "The Claude and Codex ownership manifests, managed hooks, source/deployed hashes, controller readiness, active tuple, and generation/lifecycle pointers agree after upgrade."
    - "Unrelated user configuration, coexisting hooks, plugins, and files are byte-identical before and after the owned upgrade; an ownership conflict fails before the first mutation."
    - "Existing isolated crash, rollback, uninstall, reinstall, and last-known-good tests pass, and the live owned installation can be recovered or removed only through validated ownership operations without orphaned Router roots."
  prohibitions:
    - "Do not edit live settings, hooks, manifests, or owned files directly with ad-hoc shell writes."
    - "Do not run live uninstall/reinstall until the before snapshot, dry-run, ownership validation, and isolated recovery checks have passed."
    - "Do not delete, reset, clean, stash, or overwrite unrelated workspace changes."
    - "Do not claim rollback, uninstall, or recovery from a green unit suite alone; record the observable live/isolated evidence used for each claim."
  artifacts:
    - .planning/evidence/v1.9/live-upgrade.json
    - .planning/evidence/v1.9/live-recovery.json
    - .planning/evidence/v1.9/live-after.json
  key_links:
    - "live-before snapshot -> installer dry-run -> owned install -> live-after snapshot"
    - "ownership manifest -> lifecycle validation -> preserved user state and bounded removal"
    - "generation/known-good pointers -> crash recovery -> current active installation"

<objective>
Run the existing installer against the current Claude/Codex homes, prove preservation and exact installed identity, and close the upgrade/recovery evidence loop before later health and observability phases.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/56-live-installer-and-upgrade-truth/56-CONTEXT.md
@.planning/phases/56-live-installer-and-upgrade-truth/56-01-PLAN.md
@install-router.mjs
@src/lifecycle/router-lifecycle.mjs
@tests/router.installer-coexistence.test.mjs
@tests/router.lifecycle-recovery.test.mjs
@tests/router.phase26-lifecycle.test.mjs
</context>

<tasks>

<task type="checkpoint:human-verify" tdd="false">
  <name>Task 1: Capture and validate the live pre-upgrade boundary</name>
  <files>.planning/evidence/v1.9/live-before.json</files>
  <read_first>
    - scripts/v19-live-snapshot.mjs (snapshot options and privacy contract)
    - install-router.mjs (production path defaults and dry-run behavior)
    - .planning/phases/56-live-installer-and-upgrade-truth/56-CONTEXT.md (live mutation gate)
  </read_first>
  <action>
    Create `.planning/evidence/v1.9/` and run the snapshot CLI with explicit roots `/Users/guilherme/.claude` and `/Users/guilherme/.codex`, current source paths `/Users/guilherme/Desktop/ClaudeCode/Router-build/src/runtime/router.mjs` and `/Users/guilherme/Desktop/ClaudeCode/Router-build/src/runtime/router.evolve.mjs`, and output `.planning/evidence/v1.9/live-before.json`. Inspect the snapshot for complete owned manifests, matching declared roots, non-stale controller metadata, and no raw-content fields. Then run `rtk node install-router.mjs --claude-root /Users/guilherme/.claude --codex-root /Users/guilherme/.codex --source-router /Users/guilherme/Desktop/ClaudeCode/Router-build/src/runtime/router.mjs --source-evolve /Users/guilherme/Desktop/ClaudeCode/Router-build/src/runtime/router.evolve.mjs --dry-run`; record stdout, exit code, and a second read-only state check in `live-upgrade.json`. If the manifest is invalid, roots mismatch, or dry-run would overwrite an unowned artifact, stop before any live write and report the exact blocker.
  </action>
  <acceptance_criteria>
    - `live-before.json` exists and passes JSON parsing with both runtime blocks and source fingerprints.
    - The dry-run exits 0 and reports candidate changes without changing any settings, hook, manifest, controller, or active-tuple bytes.
    - The snapshot contains no raw prompt/session/telemetry/audit bodies.
    - Any ownership conflict causes the task to stop before the real installer command.
  </acceptance_criteria>
  <verify>
    <automated>rtk node --check scripts/v19-live-snapshot.mjs && rtk node scripts/v19-live-snapshot.mjs --claude-root /Users/guilherme/.claude --codex-root /Users/guilherme/.codex --source-router src/runtime/router.mjs --source-evolve src/runtime/router.evolve.mjs --output .planning/evidence/v1.9/live-before.json</automated>
  </verify>
  <done>The exact live pre-upgrade state is captured and the installer has passed a no-write preflight.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Upgrade through the owned installer and verify preservation</name>
  <files>.planning/evidence/v1.9/live-upgrade.json</files>
  <read_first>
    - .planning/evidence/v1.9/live-before.json (operator-approved pre-upgrade state)
    - install-router.mjs (only supported live mutation entrypoint)
    - src/lifecycle/router-lifecycle.mjs (transaction restore, manifest, binding, and readiness behavior)
    - scripts/v19-live-snapshot.mjs (post-upgrade evidence shape)
  </read_first>
  <action>
    Run `rtk node install-router.mjs --claude-root /Users/guilherme/.claude --codex-root /Users/guilherme/.codex --source-router /Users/guilherme/Desktop/ClaudeCode/Router-build/src/runtime/router.mjs --source-evolve /Users/guilherme/Desktop/ClaudeCode/Router-build/src/runtime/router.evolve.mjs` after the preflight gate passes. Capture only bounded command metadata and exit status in `live-upgrade.json`, then take the post-upgrade snapshot. Compare source router/evolve hashes to Claude and Codex hook targets and manifest entries; compare pre/post allowlisted user-state fingerprints, non-router hook identities, plugin/user-file hashes, controller configuration fingerprints, active tuple identifiers, and generation/lifecycle pointers. If any conflict or verification failure occurs, rely on the installer's transaction restore and record the restored state; do not repair by hand.
  </action>
  <acceptance_criteria>
    - The installer exits 0 with `INSTALL OK` or `ALREADY INSTALLED` and both runtime paths are reported.
    - Claude and Codex managed hook targets and manifest-listed source fingerprints equal the current source fingerprints.
    - Controller state is `ready`, its configuration fingerprint matches the installed config, and the active tuple/generation pointer is valid.
    - Pre/post allowlisted unrelated user-state fingerprints and non-router hook identities are unchanged.
    - A forced preflight conflict in an isolated fixture exits non-zero with no changed user bytes.
  </acceptance_criteria>
  <verify>
    <automated>rtk node --test tests/router.installer-coexistence.test.mjs tests/router.lifecycle-recovery.test.mjs tests/router.phase26-lifecycle.test.mjs</automated>
  </verify>
  <done>The current live bundle is upgraded through the owned transaction and preservation/identity evidence is recorded.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Exercise bounded recovery and finalize live lifecycle evidence</name>
  <files>.planning/evidence/v1.9/live-recovery.json, .planning/evidence/v1.9/live-after.json</files>
  <read_first>
    - .planning/evidence/v1.9/live-before.json (recovery comparison baseline)
    - .planning/evidence/v1.9/live-upgrade.json (upgrade result and current identity)
    - src/lifecycle/router-lifecycle.mjs (uninstall validation and generation recovery)
    - tests/router.lifecycle-recovery.test.mjs (corrupt pointer, crash, watcher/controller recovery cases)
    - tests/router.installer-coexistence.test.mjs (uninstall/reinstall preservation matrix)
  </read_first>
  <action>
    Run the focused lifecycle matrix serially and record pass/fail plus commit/test identifiers in `live-recovery.json`. For the current owned installation, use only the validated lifecycle CLI/function boundary to verify controller restart, active-pointer repair, and last-known-good selection. If the pre-upgrade snapshot proves complete and the isolated recovery matrix is green, perform the bounded live uninstall/reinstall smoke only if the live manifest still matches the recorded roots; verify the user-state fingerprint and non-router hooks after reinstall. Take `live-after.json` and assert no owned Router root, generation tree, manifest, or managed binding remains after a live uninstall, or that all are restored with current hashes after reinstall. On any failure, stop, preserve artifacts, and report the exact lifecycle state rather than deleting residual files manually.
  </action>
  <acceptance_criteria>
    - Focused serial lifecycle/recovery tests pass with zero failures.
    - Restart and pointer-repair evidence identifies a ready controller and a valid current/known-good tuple.
    - If live uninstall/reinstall is performed, only installer-owned files/bindings are removed and restored; user-owned files and hooks remain byte-identical.
    - Final live evidence contains no raw prompt/session/telemetry/audit bodies and records either a clean uninstalled state or a current ready installed state.
    - No orphaned Router-owned manifest, generation tree, managed hook, or owned runtime root remains contrary to the recorded final state.
  </acceptance_criteria>
  <verify>
    <automated>rtk node --test --test-concurrency=1 tests/router.installer-coexistence.test.mjs tests/router.lifecycle-recovery.test.mjs tests/router.phase26-lifecycle.test.mjs</automated>
  </verify>
  <done>LIVE-02 through LIVE-04 have bounded live/isolated evidence and the workspace is ready for Phase 57 health verification.</done>
</task>

</tasks>

<threat_model>
## Phase 56 Plan 02 Threat Model — ASVS Level 1

| Threat ID | Severity | Threat | Mitigation | Automated proof |
|---|---|---|---|---|
| T-56-04 | critical | Live upgrade overwrites user-owned config or a coexisting hook | Complete dry-run and ownership manifest validation before install; compare allowlisted pre/post state | Dry-run and preservation matrix |
| T-56-05 | high | Interrupted upgrade leaves a partial generation or invalid active pointer | Existing staging/atomic pointer/known-good recovery path; crash tests before live recovery | Lifecycle recovery suite |
| T-56-06 | high | Uninstall deletes modified user content or leaves an orphaned Router tree | Manifest root/path/fingerprint validation; remove only proven owned bytes and report retained conflicts | Coexistence uninstall test and final snapshot |
</threat_model>

<verification>
- `rtk node --test --test-concurrency=1 tests/router.installer-coexistence.test.mjs tests/router.lifecycle-recovery.test.mjs tests/router.phase26-lifecycle.test.mjs` exits 0.
- The production installer dry-run exits 0 before any live write.
- Source/deployed/manifest hashes, controller readiness, active tuple, and final ownership state reconcile in the v1.9 evidence files.
- `rtk git diff --check` exits 0 and unrelated pre-existing dirty files remain untouched.
</verification>

<success_criteria>
- The current live Claude/Codex installation is upgraded by the owned installer, not hand-edited.
- User-owned state is preserved and conflicts fail closed.
- Rollback, uninstall, interruption recovery, and last-known-good behavior are evidenced without orphaned Router state.
</success_criteria>

## Artifacts this phase produces

- `.planning/evidence/v1.9/live-upgrade.json`: bounded installer dry-run/upgrade and identity comparison.
- `.planning/evidence/v1.9/live-recovery.json`: focused and live lifecycle recovery results.
- `.planning/evidence/v1.9/live-after.json`: final allowlisted live-state snapshot.
</artifacts>

---
phase: 11-canonical-registry-and-runtime-adapters
plan: "03"
type: execute
wave: 3
depends_on:
  - 11-01
  - 11-02
files_modified:
  - src/registry/build.mjs
  - tests/router.registry-build.test.mjs
  - install-router.mjs
  - tests/router.lifecycle.test.mjs
  - tests/router.settings-diff.test.mjs
autonomous: true
requirements:
  - REG-01
  - REG-02
  - ADP-01
  - ADP-02
must_haves:
  truths:
    - One full build deterministically combines all supported Claude and Codex observations into a candidate registry, diagnostics, and machine-readable summary.
    - Cross-runtime variants merge only on authoritative identity evidence; scope collisions, malformed records, dependencies, and conflicts remain explicit.
    - Per D-06, within a matching workspace a valid applicable project-scoped record is reported as preferred, and the global record is reported as fallback only when no applicable project-scoped record exists.
    - Per D-07, an unusable preferred project record remains preserved and non-dispatchable with its reason while the global record is identified as an available fallback; Phase 11 reports this metadata without activating or mutating routing.
    - Candidate construction and installation never activate the candidate or alter active routing state or unrelated runtime configuration.
    - One idempotent installer command deploys owned modules, builds the initial candidate, reports exact changes, and leaves no partial install on failure.
  artifacts:
    - src/registry/build.mjs
    - tests/router.registry-build.test.mjs
    - install-router.mjs
  key_links:
    - buildFullRegistry calls both adapters then schema-validates, evidence-groups, canonicalizes, and sorts their observations.
    - Installer lifecycle deploys registry modules and candidate/report artifacts through existing preflight, atomic write, ownership, and readiness patterns.
    - Full-build tests compare portable bytes across traversal permutations and snapshot current Claude category parity.
---

<objective>
Assemble the complete read-only dual-runtime candidate registry and integrate its initial build into the existing safe one-command installer.

Purpose: Deliver REG-02 and phase-wide parity while preserving last-known-good routing, privacy, ownership, and deterministic output.
Output: Full registry builder, integration tests, and idempotent installer deployment/readiness wiring.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-canonical-registry-and-runtime-adapters/11-CONTEXT.md
@.planning/phases/11-canonical-registry-and-runtime-adapters/11-RESEARCH.md
@.planning/phases/11-canonical-registry-and-runtime-adapters/11-PATTERNS.md
@.planning/phases/11-canonical-registry-and-runtime-adapters/11-VALIDATION.md
@.planning/phases/11-canonical-registry-and-runtime-adapters/11-01-SUMMARY.md
@.planning/phases/11-canonical-registry-and-runtime-adapters/11-02-SUMMARY.md
@src/lifecycle/router-lifecycle.mjs
@install-router.mjs
@tests/router.settings-diff.test.mjs
@tests/router.mjs.snapshot
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify deterministic full-build parity and read-only guarantees</name>
  <files>tests/router.registry-build.test.mjs</files>
  <read_first>
    <file>.planning/phases/11-canonical-registry-and-runtime-adapters/11-VALIDATION.md</file>
    <file>.planning/phases/11-canonical-registry-and-runtime-adapters/11-CONTEXT.md</file>
    <file>src/adapters/claude.mjs</file>
    <file>src/adapters/codex.mjs</file>
    <file>tests/router.mjs.snapshot</file>
  </read_first>
  <behavior>
    - Full builds cover all REG-02/ADP-01/ADP-02 categories with the same current Claude manifest-category parity plus Codex coverage.
    - Shared authoritative origin merges variants; equal name/content without evidence stays separate.
    - Per D-06, global/project/worktree records remain distinct; in a matching workspace a valid applicable project record is marked preferred and global is marked fallback only when no applicable project record exists.
    - Per D-07, an unusable preferred project record is preserved as non-dispatchable with a deterministic reason and global is identified as an available fallback, but neither record is activated and routing is not mutated.
    - Malformed recognizable artifacts, unsupported versions, unavailable dependencies, typed conflicts, and source provenance appear in deterministic outputs.
    - Reordered roots/traversals and repeated clean builds produce identical portable registry/report bytes.
    - Building does not change fixture runtime files, mode maps, hooks, active pointers, or unrelated configuration; portable bytes contain no absolute paths or raw prompt/environment content.
  </behavior>
  <action>Create the Wave 0 REG-02 integration suite around `buildFullRegistry(options)`. Follow the mapped `tests/router.lifecycle.test.mjs` analog for isolated temporary-root fixtures, complete cleanup, and before/after mutation checks, and `tests/router.mjs.snapshot` for batch inventory/report shape. Use explicit temporary Claude/Codex/project roots and snapshot every in-scope file before and after builds. Assert `{ registry, diagnostics, summary }`, complete category counts, evidence-gated grouping, variant preservation, deterministic conflict ordering, portable provenance, traversal permutation equivalence, and zero active-state mutations. For D-06, cover a matching workspace where a valid applicable project record is preferred and a case with no applicable project record where global alone is reported as fallback. For D-07, cover an unusable preferred project record that is retained as non-dispatchable with its reason while global is identified as an available fallback. Assert that all precedence/fallback results are report metadata only: no selection is activated and no routing state is written or mutated. Import the missing builder so the first focused run is intentionally RED.</action>
  <acceptance_criteria>
    <criterion>The suite maps every Phase 11 requirement to at least one executable assertion.</criterion>
    <criterion>Two logically equivalent fixture traversals compare exact portable registry and report bytes.</criterion>
    <criterion>Before/after byte snapshots prove buildFullRegistry is read-only.</criterion>
    <criterion>D-06 and D-07 fixtures prove valid-project precedence, absence-only global fallback, and unusable-project diagnostics with global availability without activating either record.</criterion>
    <criterion>The initial run fails because build.mjs or build behavior is absent.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/router.registry-build.test.mjs; test $? -ne 0</automated>
  </verify>
  <done>Wave 0 REG-02 parity/read-only coverage exists and is intentionally RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the deterministic read-only full registry builder</name>
  <files>src/registry/build.mjs</files>
  <read_first>
    <file>tests/router.registry-build.test.mjs</file>
    <file>src/registry/schema.mjs</file>
    <file>src/registry/identity.mjs</file>
    <file>src/adapters/claude.mjs</file>
    <file>src/adapters/codex.mjs</file>
  </read_first>
  <behavior>
    - buildFullRegistry(options) returns stable registry, diagnostics, and summary without writes.
    - Every observation is classified, schema-validated, evidence-grouped, canonicalized, and deterministically ordered.
    - Typed conflicts and runtime variants retain sufficient evidence for later safety phases without making activation decisions.
    - D-06 and D-07 precedence/fallback fields are descriptive candidate metadata only and never select, activate, or mutate a route.
  </behavior>
  <action>Implement `buildFullRegistry(options)` as a pure orchestration boundary over both adapters. Follow the mapped `tests/router.mjs.snapshot` batch/transform analog for deterministic aggregation and output shaping, and the `src/lifecycle/router-lifecycle.mjs` hashing pattern wherever report fingerprints are emitted. Per D-01 through D-04, group only on explicit canonical/shared-origin evidence and retain each runtime variant. Preserve separate scope identities per D-05 and D-08. Per D-06, report a valid applicable project record as preferred within its matching workspace and report global as fallback only when no applicable project record exists. Per D-07, preserve an unusable preferred project record, mark it non-dispatchable with the reason, and identify global as an available fallback for later safety phases. These fields are diagnostics/candidate metadata only: do not activate, select, or mutate routing. Per D-09 through D-12, retain recognizable partial records, route unknown schemas to build diagnostics, represent optional/dependency unknowns, and block declared unavailable dependencies. Per D-13 through D-16, produce compact logical provenance, typed conflicts, a complete machine-readable report, and deterministic sorting by canonical identity then logical source. Return `{ registry, diagnostics, summary }`; do not write active state, mode maps, hooks, pointers, runtime sources, telemetry, or raw prompts. Use only existing local/Node imports per D-17.</action>
  <acceptance_criteria>
    <criterion>buildFullRegistry returns all three contract outputs and no supported observation disappears silently.</criterion>
    <criterion>All merge, scope, diagnostic, provenance, conflict, and determinism assertions pass.</criterion>
    <criterion>Portable registry/report bytes are machine-independent and sorted.</criterion>
    <criterion>Builder execution produces no filesystem mutations.</criterion>
    <criterion>D-06/D-07 precedence and fallback metadata distinguishes no-applicable-project from unusable-preferred-project cases while deferring activation to later safety phases.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/router.registry-schema.test.mjs tests/router.adapters.test.mjs tests/router.registry-build.test.mjs</automated>
  </verify>
  <done>REG-02 full builds are deterministic, complete, diagnostic, privacy-safe, and read-only.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate owned candidate deployment into the one-command installer</name>
  <files>install-router.mjs, tests/router.lifecycle.test.mjs, tests/router.settings-diff.test.mjs</files>
  <read_first>
    <file>install-router.mjs</file>
    <file>src/lifecycle/router-lifecycle.mjs</file>
    <file>src/registry/build.mjs</file>
    <file>tests/router.lifecycle.test.mjs</file>
    <file>tests/router.settings-diff.test.mjs</file>
    <file>.planning/phases/11-canonical-registry-and-runtime-adapters/11-CONTEXT.md</file>
  </read_first>
  <action>Extend the existing installer/lifecycle path, not a second installer, to deploy owned registry/adapter modules and build the initial candidate plus machine-readable diagnostic report under a router-owned location distinct from active state. Use the mapped `install-router.mjs` extension point and `src/lifecycle/router-lifecycle.mjs` preflight/atomic-write/ownership boundary; follow `tests/router.lifecycle.test.mjs` for temporary-root lifecycle fixtures and `tests/router.settings-diff.test.mjs` for byte-preserving unrelated-settings assertions. Per D-18 and D-20, the existing single documented command detects default Claude/Codex locations with explicit override options, deploys, builds, wires only supported owned integration, verifies readiness, and prints concise exact changes without manual path editing. Per D-19, complete all validation/readability/build preflight before the first mutation; use existing atomic rename, ownership fingerprint, idempotency, exact-change reporting, and rollback patterns. Preserve unrelated settings bytes and pre-existing unowned files, never update an active candidate pointer, and on failure remove newly created owned artifacts while returning a usable diagnosis. Extend lifecycle standard-library import enforcement and temporary-root tests for success, dry-run, rerun, collision, build failure, and settings preservation.</action>
  <acceptance_criteria>
    <criterion>A fresh temporary-home install deploys owned modules, creates the initial candidate/report, and passes readiness in one command.</criterion>
    <criterion>Dry-run and rerun are deterministic/idempotent and report exact additions or no-op state.</criterion>
    <criterion>Unrelated Claude/Codex configuration remains byte-identical and unowned collisions fail before mutation.</criterion>
    <criterion>Injected build failure leaves no partial owned additions and does not change active routing state.</criterion>
    <criterion>No external package, daemon, database, container, or manual bootstrap step is introduced.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/router.registry-build.test.mjs tests/router.lifecycle.test.mjs tests/router.settings-diff.test.mjs</automated>
  </verify>
  <done>D-18 through D-20 are implemented through the existing idempotent installer while candidate state remains inactive and unrelated configuration is preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Adapter observations to canonical aggregation | Conflicting or spoofed records cross into shared candidate state. |
| Candidate builder to installed filesystem | Generated bytes enter router-owned locations near live runtime config. |
| Installer ownership manifest to mutation | Ownership evidence authorizes creation/replacement and rollback. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-10 | Spoofing | cross-runtime grouping | high | mitigate | Evidence-gated grouping and typed conflicts; names/content never grant identity. |
| T-11-11 | Tampering | installer deployment | high | mitigate | Complete preflight, ownership fingerprints, atomic writes, exact-change tests, and rollback of newly created files. |
| T-11-12 | Information Disclosure | portable candidate/report | high | mitigate | Logical provenance only; exclude absolute roots, raw prompts, and environment contents. |
| T-11-13 | Denial of Service | failed candidate build | high | mitigate | Keep active state untouched and return stable diagnostics with no partial install. |
| T-11-14 | Elevation of Privilege | dependency handling | high | mitigate | Record availability only; never install or execute discovered capabilities. |
</threat_model>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | - | Stable runtime-neutral Claude/Codex view with native invocation and scope | 11-01, 11-02, 11-03 | COVERED | Schema, adapters, and full build compose the outcome.
REQ | REG-01 | Canonical schema and stable identities | 11-01, 11-03 | COVERED | Unit contract plus integration parity.
REQ | REG-02 | Full supported inventory rebuild | 11-03 | COVERED | Full builder and installer candidate build.
REQ | ADP-01 | Claude global/plugin/agents-store/project coverage | 11-02, 11-03 | COVERED | Temporary-home matrix plus full build.
REQ | ADP-02 | Codex skills/plugins/agents/hooks/config/project coverage | 11-02, 11-03 | COVERED | Temporary-home matrix plus full build.
RESEARCH | - | Zero-dependency ESM, pure adapters, canonical bytes, evidence-gated merge, first-class diagnostics | 11-01, 11-02, 11-03 | COVERED | Each recommended pattern has implementation and tests.
RESEARCH | - | Read-only candidate and existing installer integration | 11-03 | COVERED | No activation; ownership-scoped atomic deployment.
CONTEXT | D-01..D-04 | Cross-runtime identity | 11-01, 11-03 | COVERED | Evidence ladder, variants, rename/move, readable fallback.
CONTEXT | D-05..D-08 | Scope collisions | 11-01, 11-02, 11-03 | COVERED | Separate identities, precedence diagnostics, stable project/worktree identity.
CONTEXT | D-09..D-12 | Incomplete artifacts and dependencies | 11-01, 11-02, 11-03 | COVERED | Partial records, unknowns, unsupported diagnostics, unavailable blocking.
CONTEXT | D-13..D-16 | Provenance and conflicts | 11-01, 11-02, 11-03 | COVERED | Logical provenance, typed conflicts, deterministic reports.
CONTEXT | D-17..D-20 | Lightweight installation and operation | 11-01, 11-02, 11-03 | COVERED | Standard library and one safe idempotent command.
</source_audit>

<verification>
Run `node --test tests/router.registry-schema.test.mjs tests/router.adapters.test.mjs tests/router.registry-build.test.mjs tests/router.settings-diff.test.mjs`, then `node --test tests/*.test.mjs`. Both commands must pass; rerun the focused command once to confirm deterministic output.
</verification>

<success_criteria>
All REG-01, REG-02, ADP-01, and ADP-02 tests pass; the one-command installer produces an inactive deterministic candidate safely; no active state, unrelated configuration, privacy boundary, or package footprint regresses.
</success_criteria>

<output>
Create `.planning/phases/11-canonical-registry-and-runtime-adapters/11-03-SUMMARY.md` when done.
</output>

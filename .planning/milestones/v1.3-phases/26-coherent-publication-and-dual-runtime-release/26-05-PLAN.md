---
phase: 26-coherent-publication-and-dual-runtime-release
plan: 05
type: execute
wave: 4
depends_on: [26-04]
files_modified:
  - src/lifecycle/router-lifecycle.mjs
  - install-router.mjs
  - tests/router.phase26-dual-runtime.test.mjs
autonomous: true
requirements: [REL-05, REL-06]
must_haves:
  truths:
    - "Command, skill, agent, workflow, MCP, and tool recommendations route in Claude-only, Codex-only, and combined installs."
    - "Fresh install, repair, upgrade, rollback, recovery, and actual installed activation preserve the complete tuple."
    - "Unrelated user files and configuration remain byte-identical."
  artifacts:
    - "src/lifecycle/router-lifecycle.mjs"
    - "install-router.mjs"
    - "tests/router.phase26-dual-runtime.test.mjs"
  key_links:
    - "Installer-owned generation manifest includes every runtime dependency of the deployed hook/controller."
    - "Tests execute installed bytes from temporary Claude and Codex roots."
---

<objective>
Prove the existing installer and lifecycle preserve all recommendation kinds through actual Claude and Codex runtime activation.

Purpose: Replace source-only confidence with installed dual-runtime compatibility evidence.
Output: Complete deployment manifest/wiring and a six-kind by three-profile lifecycle matrix.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-coherent-publication-and-dual-runtime-release/26-CONTEXT.md
@.planning/phases/26-coherent-publication-and-dual-runtime-release/26-PATTERNS.md
@.planning/phases/26-coherent-publication-and-dual-runtime-release/26-04-SUMMARY.md
@src/lifecycle/router-lifecycle.mjs
@install-router.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deploy the complete v1.3 dependency closure through the existing installer</name>
  <files>src/lifecycle/router-lifecycle.mjs, install-router.mjs, tests/router.phase26-dual-runtime.test.mjs</files>
  <behavior>
    - Fresh Claude, Codex, and combined installs contain every dependency needed by deployed controllers/hooks.
    - Repair and upgrade preserve owned generation integrity and unrelated user bytes.
    - Rollback and recovery reactivate a complete verified tuple.
  </behavior>
  <action>Extend the existing lifecycle module manifest and installer-owned generation only where the complete tuple introduced a real runtime dependency. Keep install/repair/upgrade/rollback/recovery in `installRouter` and existing lifecycle functions; do not add an installer. Exercise generated installed files, not source imports, from isolated runtime roots.</action>
  <verify>
    <automated>rtk node --test --test-concurrency=1 tests/router.phase26-dual-runtime.test.mjs tests/router.installer-coexistence.test.mjs tests/router.autonomous-lifecycle.test.mjs</automated>
  </verify>
  <done>All three install profiles complete every lifecycle operation without missing modules or user-file drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route all six recommendation kinds through installed runtimes</name>
  <files>tests/router.phase26-dual-runtime.test.mjs</files>
  <behavior>
    - Command, skill, agent, workflow, MCP, and tool recommendations survive compile, install, load, and prompt route.
    - Claude-only and Codex-only roots route locally; combined roots route both installed entry points.
    - Existing recommendation contracts remain backward compatible.
  </behavior>
  <action>Build a table-driven six-kind by three-profile matrix using existing inventory fixtures and adapters. Publish a complete tuple, install it, invoke each deployed controller/hook file, and assert the expected recommendation kind/target. Snapshot unrelated files before and after repair/upgrade/rollback/recovery.</action>
  <verify>
    <automated>rtk node --test --test-concurrency=1 tests/router.phase26-dual-runtime.test.mjs</automated>
  </verify>
  <done>Every recommendation kind routes through actual installed Claude and Codex bytes across all lifecycle operations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source tree -> installed generation | Only owned verified files may be deployed. |
| installer -> user runtime roots | Router must not overwrite unrelated user state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-11 | Tampering | installed generation | high | mitigate | Existing owned manifest, hashes, atomic generations, and installed execution. |
| T-26-12 | Elevation of Privilege | user runtime files | critical | mitigate | Ownership proof and byte snapshots across all lifecycle operations. |
</threat_model>

<verification>
Run the serial dual-runtime, coexistence, and autonomous lifecycle gate.
</verification>

<success_criteria>
REL-06 is proven with installed execution for all six kinds and all three runtime profiles.
</success_criteria>

<output>
Create `.planning/phases/26-coherent-publication-and-dual-runtime-release/26-05-SUMMARY.md` when done.
</output>

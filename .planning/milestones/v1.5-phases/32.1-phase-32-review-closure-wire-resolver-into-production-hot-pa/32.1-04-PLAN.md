---
phase: 32.1-phase-32-review-closure
plan: 04
type: execute
wave: 3
depends_on:
  - 32.1-02
files_modified:
  - tests/router.perf.test.mjs
autonomous: true
requirements:
  - ROUTE-05
  - PARITY-03
user_setup: []
estimate:
  tokens: 26000
  raw_tokens: 13000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A real end-to-end hot-path perf fixture (manifest load + scoring + cache + guards + route render on the LIVE hook hot path) sustains warm p95 < 40ms and max < 100ms (ROUTE-05 / PARITY-03 budget on the real path)."
    - "The cheap resolveSlashRoute helper benchmark is retained separately and still within budget."
    - "The trivial pass-through latency gate (< 100ms) remains intact."
  artifacts:
    - tests/router.perf.test.mjs (augmented with the e2e hot-path fixture)
  key_links:
    - "e2e fixture -> inspectDecision(prompt, {manifest, modeMap}) -> scoring + cache + guards + render -> measured latency"
    - "helper benchmark (resolveSlashRoute direct) retained as a separate cheap gate"
---

<objective>
Address review closure item 4 (MEDIUM): the 32-04 perf gate measures the helper, not the router hot path. `tests/router.perf.test.mjs:82-108` calls `m.resolveSlashRoute()` directly, not manifest load + scoring + cache + guards + render, so it cannot substantiate the end-to-end <40ms p95 / <100ms max claim. This plan replaces/augments that gate with an end-to-end subprocess fixture that reaches route selection AND rendering on the real hot path, while retaining the cheap helper benchmark separately.

Purpose: Substantiate the warm p95 < 40ms / max < 100ms budget on the path that actually ships suggestions (the resolve-first hot path wired by 32.1-02), not just on the pure resolver helper.

Output: augmented `tests/router.perf.test.mjs` with an e2e hot-path fixture + a separate retained helper benchmark.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-intent-first-routing-mode-map-schema-v4-guard-hole-closure/32-REVIEWS.md
@.planning/phases/32-intent-first-routing-mode-map-schema-v4-guard-hole-closure/32-VERIFICATION.md

# Source of truth for this plan (read_first targets):
@tests/router.perf.test.mjs           # the helper-only resolve gate at :82-108 this plan augments; existing spawnSync + ROUTER_DEBUG_LATENCY seams
@tests/router.mjs.snapshot            # inspectDecision opts injection (~2870-2871) + finish()/final_injected_context render path (~2960-3080); reuse in the e2e fixture
@tests/router.hot-path-resolve.integration.test.mjs  # created by 32.1-02 — real-hot-path driving pattern to reuse for the perf fixture
@tests/router.phase32-cross-runtime.test.mjs  # runtime_commands fixture + per-runtime subprocess convention
# NOTE: depends on 32.1-02 (hot-path resolve wiring) so the e2e fixture measures the resolve-first path it ships.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: End-to-end hot-path perf fixture (route selection + rendering)</name>
  <files>tests/router.perf.test.mjs</files>
  <read_first>
    - tests/router.perf.test.mjs: existing gates (:1-79 trivial pass-through, :82-108 resolver-helper gate) and helpers (runOnce, HOOK, NODE, spawnSync env override).
    - tests/router.mjs.snapshot: `inspectDecision(prompt, options)` opts injection (~2870-2871, accepts `manifest`/`modeMap` in-memory) and the render path where `state.finalInjectedContext = composeWithCap(routeBlock, graphBlock)` (~3077) — the code the fixture must reach.
    - tests/router.hot-path-resolve.integration.test.mjs (from 32.1-02): the runtime-split manifest + mode-map fixture + per-runtime subprocess pattern to reuse for the perf scenario.
  </read_first>
  <behavior>
    - Test 1: an e2e warm loop driving the real hot path (manifest/mode-map load + scoring + cache + guards + route render) sustains warm p95 < 40ms and every-run max < 100ms across 20+ warm iterations.
    - Test 2: the fixture asserts the measured path actually reached route selection + render (e.g. a non-empty final_injected_context at least once / ROUTER_DEBUG_LATENCY line), so the number is not a vestigial helper-only timing.
  </behavior>
  <action>
    Augment `tests/router.perf.test.mjs` with a new end-to-end (e2e) hot-path fixture that PROVES the resolve-first path within budget:
    - In a spawned node subprocess (stdlib `--input-type=module -e`, same NODE binary + ROUTER_RUNTIME env technique as the existing resolve gate), import the LIVE hook and drive `inspectDecision(prompt, { manifest: runtimeManifest, modeMap, mutateCache:true, logTelemetry:false, emitInjection:false, includePrompt:false })` for 20+ warm iterations after 5+ warmup.
    - Use the runtime-split fixture (claude owns gsd-debug, codex owns systematic-debugging) + a mode-map whose slash entry has the near-tie resolve list, so each iteration exercises: manifest/modeMap load (or a single pre-loaded in-memory manifest for the tight loop — the requirement is reaching scoring + cache + guards + render, not re-reading 208KB per iteration), BM25 scoring, cache lookup (mutateCache writes/reads the in-memory cache), guards, and route render into `final_injected_context`.
    - Assert, per iteration, that the path reached render (final_injected_context non-empty for the confident route), then collect `performance.now()` deltas and gate warm p95 < 40ms and max < 100ms (reusing the existing p95/max math at :123-128). Keep stdout pure timing JSON (assert no dead stdout, per the existing gate).
    - Keep this as a SEPARATE test from the helper benchmark so the two budgets are independently attributable.
  </action>
  <acceptance_criteria>
    - 20+ warm e2e iterations reach route render, warm p95 < 40ms, max < 100ms.
    - The measured number is demonstrably the hot path (render reached), not the helper.
    - Deterministic in CI; no external home-dir writes (live hook imported/read only).
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/router.perf.test.mjs</automated>
  </verify>
  <done>An end-to-end hot-path fixture (scoring + cache + guards + render) substantiates warm p95 < 40ms and max < 100ms on the resolve-first path.</done>
</task>

<task type="auto">
  <name>Task 2: Retain the cheap resolver-helper benchmark as a separate gate</name>
  <files>tests/router.perf.test.mjs</files>
  <read_first>
    - tests/router.perf.test.mjs: the existing `resolve-heavy` helper benchmark (:82-108) — keep it, do not delete; just mark it as the helper-level counterpart to the new e2e fixture.
  </read_first>
  <action>
    Keep the existing direct-`resolveSlashRoute` benchmark as the CHEAP helper-level gate (it validates the resolver's own microsecond cost and catches pathological resolver regressions in isolation), relabeling it so its purpose is distinct from the new e2e hot-path fixture (e.g. rename test title to make clear it measures the pure resolver helper, not the hot path). Ensure both the helper benchmark and the new e2e fixture coexist under `node --test tests/router.perf.test.mjs`, each asserting its own budget. No behavior change to the helper benchmark's timing math.
  </action>
  <acceptance_criteria>
    - Helper benchmark still passes its budget.
    - Test titles uniquely identify helper vs e2e so CI output is unambiguous.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/router.perf.test.mjs</automated>
  </verify>
  <done>Both the helper benchmark and the e2e hot-path fixture gate independently under the same perf suite.</done>
</task>

</tasks>

## Artifacts this phase produces

- Augmented `tests/router.perf.test.mjs` — new end-to-end hot-path perf fixture (real inspectDecision: manifest/modeMap + scoring + cache + guards + route render; warm p95 <40ms / max <100ms) plus the retained, relabeled helper benchmark.

## Dependency note

This plan touches only `tests/router.perf.test.mjs` (no snapshot, no production source, no mirror step). It depends on 32.1-02 (hot-path resolve wiring) so the e2e fixture measures the resolve-first path the phase ships; runs wave 3.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI perf subprocess -> timing verdict | a mis-measured path (helper instead of hot path) would falsely certify the latency budget |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32.1-11 | Spoofing | perf e2e fixture scope | high | mitigate | Assert render is actually reached (non-empty final_injected_context) so the timing is the hot path, not the helper |
| T-32.1-12 | DoS | flaky CI latency | low | mitigate | Warmup + warm p95 (not cold) statistics; generous max budget; whole-suite timeout retained |
| T-32.1-13 | Elevation of Privilege | spawned subprocess env | low | accept | Subprocess is stdlib-only and reads the live hook; no privilege gain; temp/read-only by construction |
| T-32.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs (stdlib-only timing); slopcheck + blocking human checkpoint only if an install is ever introduced |
</threat_model>

<verification>
- `node --test tests/router.perf.test.mjs` passes — both the e2e hot-path fixture (warm p95 <40ms / max <100ms, render reached) and the retained helper benchmark green.
- Full phase-32 perf + hot-path integration suites remain green: `node --test tests/router.hot-path-resolve.integration.test.mjs tests/router.mjs.snapshot.diff.test.mjs`.
</verification>

<success_criteria>
- Review closure item 4 closed: the hot-path perf budget is substantiated on the REAL path (route selection + render), not the helper.
- Helper benchmark retained separately; no regressions.
</success_criteria>

<output>
Create `.planning/phases/32.1-phase-32-review-closure-wire-resolver-into-production-hot-pa/32.1-04-SUMMARY.md` when done.
</output>

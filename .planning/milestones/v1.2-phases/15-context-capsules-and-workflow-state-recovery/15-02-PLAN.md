---
phase: 15-context-capsules-and-workflow-state-recovery
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - src/context/sources.mjs
  - tests/router.context-sources.test.mjs
autonomous: true
requirements: [CTX-01, CTX-02]
nyquist_compliant: true
must_haves:
  truths:
    - "D-10: live execution and authoritative project/phase sources outrank capsule hints."
    - "D-11: refresh reads only fields needed for the next action and degrades optional failures through structured diagnostics."
    - "D-12: recovery is local and targeted, with no recursive planning scan, full design load, history replay, network classifier, or model call."
  artifacts:
    - path: src/context/sources.mjs
      provides: "Exact-path bounded authoritative source adapters and freshness witnesses"
    - path: tests/router.context-sources.test.mjs
      provides: "Missing, malformed, oversized, symlink, changed-witness, git branch/dirty-summary, and read-budget matrix"
  key_links:
    - from: src/context/sources.mjs
      to: .planning/STATE.md
      via: "exact-path bounded heading/key extraction"
    - from: src/context/sources.mjs
      to: src/context/capsule.mjs
      via: "validated artifact references and canonical freshness witnesses"
    - from: src/context/sources.mjs
      to: .git/HEAD
      via: "bounded local git branch and porcelain dirty-summary adapter with injectable process execution"
---

<objective>
Build the authoritative, bounded recovery inputs for CTX-01/CTX-02 and decisions D-02, D-09, D-10, D-11, and D-12.

Purpose: Reconcile capsules against current truth without loading whole planning trees or documents.
Output: `src/context/sources.mjs` and source-budget/freshness integration tests.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-context-capsules-and-workflow-state-recovery/15-CONTEXT.md
@.planning/phases/15-context-capsules-and-workflow-state-recovery/15-RESEARCH.md
@.planning/phases/15-context-capsules-and-workflow-state-recovery/15-PATTERNS.md
@.planning/phases/15-context-capsules-and-workflow-state-recovery/15-01-SUMMARY.md
@src/context/capsule.mjs
@src/registry/activate.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement exact-path bounded authoritative readers</name>
  <read_first>src/context/capsule.mjs, src/registry/activate.mjs, .planning/phases/15-context-capsules-and-workflow-state-recovery/15-PATTERNS.md, .planning/STATE.md, .planning/ROADMAP.md</read_first>
  <files>tests/router.context-sources.test.mjs, src/context/sources.mjs</files>
  <behavior>
    - Exact STATE, ROADMAP, active phase artifact, design reference, execution/checkpoint paths, and local git branch/dirty summary return compact approved facts plus witnesses under explicit byte/entry budgets.
    - Missing optional sources return structured degraded results; missing/malformed identity-critical sources return reason-coded unresolved results.
    - Symlinks, non-files, escaping references, and oversized inputs fail closed without reading or reflecting bodies.
    - Instrumented tests prove no recursive planning enumeration and no unrelated or complete design document read.
  </behavior>
  <action>Create the Wave 0 source fixtures/tests first. Implement separate exported targeted readers and a bounded aggregate snapshot API accepting explicit workspace root, known workflow/phase identity, exact references, injectable filesystem primitives, and an injectable bounded command runner for budget assertions. Per D-02 emit compact values plus mtime/size or canonical SHA-256/version/generation witnesses. Per D-12 use lstat before bounded reads, never directory recursion or basename search, and extract only approved headings/keys from STATE, ROADMAP, the exact active artifact, the exact checkpoint/execution state, and one uniquely referenced design artifact. Add a local git adapter that runs only `git symbolic-ref --short -q HEAD` (with detached-HEAD reason coding) and `git status --porcelain=v1 -uno` under an explicit output/time ceiling, returning branch identity plus a bounded dirty count/category summary and witness rather than filenames or diff bodies. Reject a non-repository, timeout, oversized output, or command failure through stable structured results. Return `{status, reason_code, value?, witness?}`-shaped serializable results without parser internals.</action>
  <verify>
    <automated>node --test tests/router.context-sources.test.mjs</automated>
  </verify>
  <acceptance_criteria>All approved source kinds, including git branch/dirty summary, obey byte, entry, output, and time ceilings; unsafe/oversized/malformed/detached/non-repository cases are reason-coded; git results expose no filenames or diff bodies; instrumentation proves no recursive scan, unrelated read, full design load, network, or model path.</acceptance_criteria>
  <done>Authoritative inputs are targeted, local, bounded, witnessed, and private as D-02 and D-12 require.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Classify freshness and construct minimal refresh evidence</name>
  <read_first>src/context/sources.mjs, src/context/capsule.mjs, tests/router.context-capsule.test.mjs</read_first>
  <files>tests/router.context-sources.test.mjs, src/context/sources.mjs</files>
  <behavior>
    - Matching witnesses classify capsule evidence fresh; changed authoritative identity/status/artifact witnesses classify it stale, not corrupt.
    - Refresh uses precedence explicit instruction, live execution, authoritative phase/project and exact artifacts, then valid capsule hint.
    - Only next-action identity fields are rebuilt; optional failures remain bounded diagnostics while critical conflicts stay unresolved.
  </behavior>
  <action>Add freshness/precedence tests first, then implement exported witness comparison and minimal refresh-evidence assembly. Per D-09 separate schema corruption from witness staleness. Per D-10 encode source precedence explicitly and never let capsule values replace newer authoritative values. Per D-11 return only facts needed to identify workflow identity, status, exact next position, linked artifact, and blockers, with bounded diagnostics for optional omissions and a focused-question-ready unresolved classification for critical conflicts. Do not persist here; Plan 15-03 owns resolver-directed atomic rewrite.</action>
  <verify>
    <automated>node --test tests/router.context-sources.test.mjs tests/router.context-capsule.test.mjs</automated>
  </verify>
  <acceptance_criteria>Fresh/stale/corrupt are distinct; source precedence is deterministic under permutations; minimal refresh excludes unrelated document content; critical conflicts cannot be represented as dispatchable evidence.</acceptance_criteria>
  <done>D-09 through D-12 yield deterministic, bounded evidence suitable for the resume resolver.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Artifact reference to filesystem | Capsule-controlled references select local authoritative inputs. |
| Authoritative bytes to compact facts | Potentially malformed or sensitive files cross a parser boundary. |
| Witness comparison to dispatch evidence | Stale or spoofed metadata could be mistaken for truth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-15-05 | Elevation of Privilege | artifact reader | high | mitigate | Root containment, exact allowlisted paths, lstat symlink rejection, and no basename/discovery search. |
| T-15-06 | Information Disclosure | source adapters | high | mitigate | Bounded extraction of approved facts; canary tests over values, diagnostics, and errors. |
| T-15-07 | Tampering | freshness witnesses | high | mitigate | Canonical fingerprints for identity-critical sources plus explicit precedence and mismatch classification. |
| T-15-08 | Denial of Service | source reads | medium | mitigate | Pre-read stat ceilings, entry budgets, and no recursion/network/model calls. |
</threat_model>

<verification>
Run `node --test tests/router.context-sources.test.mjs tests/router.context-capsule.test.mjs` and the full `node --test tests/*.test.mjs`; confirm instrumented fixtures record only exact expected paths and bounded read lengths.
</verification>

<success_criteria>
- Authoritative readers expose only compact next-action evidence and stable witnesses.
- Fresh, stale, corrupt, missing-optional, and identity-conflict outcomes are distinct and reason-coded.
- Recovery performs no broad scan, full-document load, conversation replay, network classification, or background model call.
</success_criteria>

## Artifacts this phase produces

This plan produces `src/context/sources.mjs`; exact STATE/ROADMAP/phase-artifact/design/execution readers; byte and entry budget constants; freshness-witness comparison and minimal refresh-evidence APIs; structured source status/reason-code fields; and `tests/router.context-sources.test.mjs`.

## Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Resume from current authoritative state | 15-02 | COVERED | Supplies bounded current truth. |
| REQ | CTX-01, CTX-02 | Freshness and minimal-prompt evidence | 15-02 | COVERED | Tasks 1-2. |
| RESEARCH | Targeted source adapters, including git branch/dirty summary, and witness architecture | 15-02 | COVERED | Exact paths, bounded local git commands, budgets, structured failures. |
| CONTEXT | D-02, D-09–D-12 | Source precedence and bounded refresh | 15-02 | COVERED | Explicit in tasks and criteria. |

<output>
Create `.planning/phases/15-context-capsules-and-workflow-state-recovery/15-02-SUMMARY.md` when done.
</output>

---
phase: 58-outcome-and-graph-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/v19-observability-report.mjs
  - src/runtime/router.mjs
  - tests/router.v19-observability.test.mjs
  - tests/router.telemetry.test.mjs
autonomous: true
requirements: [OBS-01, OBS-02, OBS-03, OBS-04]
must_haves:
  truths:
    - "New telemetry carries an explicit bounded route anchor without reading receipts or raw downstream content."
    - "A post-run report correlates telemetry with shadow outcomes, native receipt state, verification state, and health outcomes using bounded identifiers only."
    - "Selected, ignored, rejected, substituted, completed, failed, and accepted states are represented as derived classifications even when legacy telemetry fields are null."
    - "Every graph-missing record is counted under a bounded reason and remediation state, and malformed JSONL is counted without leaking its contents."
  prohibitions:
    - "The prompt path MUST NOT read receipts, health stores, or perform correlation I/O."
    - "The report MUST NOT persist raw prompts, cwd values, commands, stdout, stderr, audit bodies, or raw JSONL lines."
    - "No dependency, service, database, graph rebuild, or second router is introduced."
  artifacts:
    - scripts/v19-observability-report.mjs
    - tests/router.v19-observability.test.mjs

<objective>
Implement the smallest off-hot-path evidence command and route anchor required to close the live observability gaps.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the privacy-safe correlation report</name>
  <files>tests/router.v19-observability.test.mjs</files>
  <action>
  Add anonymous temporary dual-runtime JSONL fixtures. Assert the CLI emits bounded counts and classifications, links a telemetry route to a receipt, records native/verification state, classifies graph_missing with actionable remediation, counts malformed lines, and excludes prompt-like, command-like, stdout-like, cwd-like, and raw-line sentinel values.
  </action>
  <verify><automated>rtk node --test tests/router.v19-observability.test.mjs</automated></verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement correlation and route anchoring</name>
  <files>scripts/v19-observability-report.mjs, src/runtime/router.mjs, tests/router.telemetry.test.mjs</files>
  <action>
  Add the standard-library report CLI and add `route_id` to `telemetryEntryFromState` as the selected route's bounded identity. Keep receipt/health parsing in the report only. Update telemetry coverage for the new anchor while retaining the historical null fields as raw compatibility seams.
  </action>
  <verify><automated>rtk node --test tests/router.v19-observability.test.mjs tests/router.telemetry.test.mjs</automated></verify>
</task>

</tasks>

<verification>
- `rtk node --test tests/router.v19-observability.test.mjs tests/router.telemetry.test.mjs`
- `rtk node --test --test-concurrency=1`
- `rtk git diff --check`
</verification>

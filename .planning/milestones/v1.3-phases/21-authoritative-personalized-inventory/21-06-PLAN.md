---
phase: 21-authoritative-personalized-inventory
plan: 06
type: execute
wave: 5
depends_on: ["21-05"]
files_modified:
  - tests/router.registry-schema.test.mjs
autonomous: true
gap_closure: true
requirements: [DISC-02, DISC-03]
must_haves:
  truths:
    - "The legacy schema regression contract accepts portable source-path fallback identities while still proving equal names and equal live content do not merge."
    - "Canonical semantic bytes remain sensitive to invocation and precedence ordering, while exact continuity fingerprints remain derived only from authored content or sorted source fingerprints."
    - "The exact 116-test Phase 21 slice contains no schema-contract failure; any remaining focused or repository failure is rerun and reported separately with Phase 21 ownership evidence."
  artifacts:
    - path: "tests/router.registry-schema.test.mjs"
      provides: "D-09-aligned fallback identity and exact-source-fingerprint regression assertions"
  key_links:
    - from: "tests/router.registry-schema.test.mjs"
      to: "src/registry/identity.mjs"
      via: "asserts stableCapabilityId path/type/runtime/scope/root/source behavior and contentFingerprint exact-pair behavior"
      pattern: "stableCapabilityId|contentFingerprint"
---

<objective>
Close GAP-21-01 by reconciling the two stale Phase 11 schema assertions with the approved Phase 21 D-09 identity contract, without changing production behavior.

Purpose: Restore a green focused Phase 21 regression gate while preserving portable path-separated identity and exact fingerprint continuity semantics.
Output: Corrected schema assertions plus focused-slice and repository-suite evidence.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-authoritative-personalized-inventory/21-CONTEXT.md
@.planning/phases/21-authoritative-personalized-inventory/21-VERIFICATION.md
@.planning/phases/21-authoritative-personalized-inventory/21-03-SUMMARY.md
@.planning/phases/21-authoritative-personalized-inventory/21-05-SUMMARY.md
@src/registry/identity.mjs
@tests/router.registry-schema.test.mjs

<interfaces>
`stableCapabilityId(record)` uses declared canonical identity, then shared origin, then a portable source identity containing type plus sorted runtime/scope/logical-root/relative-path provenance and the record scope suffix.
`contentFingerprint(record)` hashes authored `content` when present; otherwise it hashes the normalized type plus sorted provenance `source_fingerprint` values so a unique removed/added exact pair can prove D-09 continuity.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reconcile stale schema assertions with D-09 identity semantics</name>
  <files>tests/router.registry-schema.test.mjs</files>
  <read_first>
    - .planning/phases/21-authoritative-personalized-inventory/21-VERIFICATION.md (GAP-21-01 failure ownership and required reruns)
    - .planning/phases/21-authoritative-personalized-inventory/21-CONTEXT.md (D-09, D-10, and D-15 locked identity rules)
    - src/registry/identity.mjs (live fallback-ID and exact fingerprint definitions)
    - tests/router.registry-schema.test.mjs (the two stale assertions at the equal-name and semantic-ordering tests)
    - tests/router.registry-diff.test.mjs (existing unique exact-fingerprint continuity and ambiguous duplicate coverage)
  </read_first>
  <behavior>
    - Equal-name Claude and Codex records receive distinct deterministic IDs derived from their portable source provenance, not their authored name.
    - Reordering invocation arguments or precedence changes canonical semantic bytes.
    - Reordering invocation arguments or precedence alone does not change the exact continuity fingerprint when source fingerprints are unchanged.
    - Changing an exact source fingerprint changes the continuity fingerprint.
  </behavior>
  <action>Update only `tests/router.registry-schema.test.mjs`. In the equal-name test, replace the two Phase 11 name-derived literal expectations with the exact portable source-path fallback IDs produced by the fixture and keep the explicit non-equality assertion, implementing D-09/D-10/D-15 without weakening the test to a prefix-only check. In the semantic-ordering test, retain the assertion that canonical bytes differ, change the continuity assertion to require equal `contentFingerprint` values for unchanged exact source evidence, and add a record whose provenance has a different `source_fingerprint` to prove that exact source changes still change the fingerprint. Do not modify `src/registry/identity.mjs`, schema canonicalization, fixture provenance, or unrelated assertions: the verified Phase 21 production behavior is authoritative and this plan corrects only stale regression expectations.</action>
  <acceptance_criteria>
    - The equal-name test asserts the full portable Claude and Codex fallback IDs and that they remain distinct.
    - The semantic-ordering test separately proves canonical-byte sensitivity and continuity-fingerprint stability.
    - A changed provenance source fingerprint produces a different continuity fingerprint.
    - `rtk node --test tests/router.registry-schema.test.mjs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>rtk node --test tests/router.registry-schema.test.mjs</automated>
  </verify>
  <done>Both GAP-21-01 assertion mismatches are corrected without changing production identity or fingerprint semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run the focused Phase 21 gate and classify repository regressions</name>
  <files>tests/router.registry-schema.test.mjs</files>
  <read_first>
    - .planning/phases/21-authoritative-personalized-inventory/21-VERIFICATION.md (116-test baseline and unrelated tuple-publication failure)
    - .planning/phases/21-authoritative-personalized-inventory/21-05-SUMMARY.md (prior repository-suite baseline and ownership boundary)
    - tests/router.registry-build.test.mjs (the pre-existing tuple-publication failure included in the focused slice)
  </read_first>
  <action>Run the exact live-reproduced 116-test Phase 21 command from this plan's verification block after Task 1, then run the full repository suite once. Record exact pass/fail/skip counts and every remaining failing test in `21-06-SUMMARY.md`. The focused slice must no longer contain either schema assertion failure; if it remains red only at the previously documented registry-build tuple-publication seam, classify that failure separately rather than changing out-of-scope implementation. For the full suite, distinguish failures touching the sole owned file from failures in untouched lifecycle, live-hook, recovery/controller, ancestor-watch, registry-build, release, install, or test-mode seams. Do not edit additional tests or production files under this gap plan.</action>
  <acceptance_criteria>
    - The exact focused command no longer reports either `router.registry-schema.test.mjs` failure.
    - Focused and full-suite counts and failing test names are captured in the plan summary.
    - Any remaining failure is explicitly classified by owned-file evidence and is not mislabeled as a Phase 21 schema regression.
    - `git diff --name-only -- tests src` lists only `tests/router.registry-schema.test.mjs` for this plan's changes.
  </acceptance_criteria>
  <verify>
    <automated>rtk node -e "const{spawnSync}=require('node:child_process'),{readdirSync}=require('node:fs');const focused=['tests/router.adapters.test.mjs','tests/router.registry-schema.test.mjs','tests/router.registry-diff.test.mjs','tests/router.registry-reconcile.test.mjs','tests/router.registry-build.test.mjs','tests/router.registry-watcher.test.mjs','tests/router.registry-activate.test.mjs','tests/router.control-cli.test.mjs',...readdirSync('tests').filter(x=&gt;x.startsWith('router.inventory-')&amp;&amp;x.endsWith('.test.mjs')).map(x=&gt;'tests/'+x)];const run=a=&gt;spawnSync(process.execPath,['--test',...a],{encoding:'utf8'});const f=run(focused),all=run(readdirSync('tests').filter(x=&gt;x.endsWith('.test.mjs')).map(x=&gt;'tests/'+x)),out=(f.stdout||'')+(f.stderr||'')+(all.stdout||'')+(all.stderr||'');process.stdout.write(out);const failed=[...out.matchAll(/^\\s*not ok \\d+ - (.+)$/gm)].map(m=&gt;m[1]);const stale=new Set(['equal names or content do not merge without declared evidence','semantic invocation argument and precedence order changes canonical bytes']);process.exit(failed.some(name=&gt;stale.has(name))?1:0)"</automated>
  </verify>
  <done>The Phase 21 focused gate is rerun after assertion reconciliation, and repository-wide failures are reported with an explicit owned-versus-pre-existing boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| legacy test expectation → current identity contract | A stale assertion could pressure production code back toward name-derived identity and merge unrelated installed capabilities. |
| focused phase evidence → repository-wide status | Unrelated failures could be hidden or incorrectly attributed to the Phase 21 correction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-20 | Tampering | fallback identity regression assertion | high | mitigate | Assert full portable source-path IDs and distinct simultaneous live records per D-09/D-10/D-15. |
| T-21-21 | Tampering | exact continuity fingerprint assertion | high | mitigate | Separate canonical semantic-byte sensitivity from exact-source continuity and prove changed source evidence changes the fingerprint. |
| T-21-22 | Repudiation | suite result classification | medium | mitigate | Capture exact commands, counts, failing test names, and owned-file boundaries in the summary. |
| T-21-SC | Tampering | package installation | high | mitigate | No package installation task exists. |
</threat_model>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Current safe framework-neutral installed inventory | 21-01..21-06 | COVERED | Plans 01-05 implement the goal; Plan 06 restores its required regression gate.
REQ | DISC-01..DISC-08 | Phase 21 discovery requirements | 21-01..21-06 | COVERED | All eight remain implemented; DISC-02/DISC-03 receive direct corrective coverage here.
RESEARCH | — | Portable path identity and unique exact-fingerprint continuity | 21-03, 21-06 | COVERED | Plan 06 aligns the legacy oracle without production changes.
CONTEXT | D-01..D-21 | Locked Phase 21 decisions | 21-01..21-06 | COVERED | D-09/D-10/D-15 are cited directly; no deferred ideas exist.
</source_audit>

<verification>
- `rtk node --test tests/router.registry-schema.test.mjs`
- `rtk node --test tests/router.adapters.test.mjs tests/router.registry-schema.test.mjs tests/router.registry-diff.test.mjs tests/router.registry-reconcile.test.mjs tests/router.registry-build.test.mjs tests/router.registry-watcher.test.mjs tests/router.registry-activate.test.mjs tests/router.control-cli.test.mjs tests/router.inventory-*.test.mjs`
- `rtk node --test tests/*.test.mjs`
- Review `git diff -- tests/router.registry-schema.test.mjs` and confirm no production file changed.
</verification>

<success_criteria>
- Both stale schema assertions express the approved D-09 portable identity and exact-source-fingerprint contracts.
- The focused Phase 21 slice contains no schema-contract failure; any unrelated failure remains explicitly classified.
- Full-suite evidence is current and separates owned regressions from unrelated pre-existing failures.
- Plans 21-01 through 21-05 and all production implementation remain untouched.
</success_criteria>

<output>
Create `.planning/phases/21-authoritative-personalized-inventory/21-06-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- Reconciled portable fallback-ID assertions in `tests/router.registry-schema.test.mjs`.
- Reconciled canonical-byte versus exact-continuity-fingerprint assertions in `tests/router.registry-schema.test.mjs`.
- Current focused Phase 21 and full repository suite evidence in `21-06-SUMMARY.md`.

---
phase: 32.1-phase-32-review-closure
plan: 02
type: execute
wave: 2
depends_on:
  - 32.1-01
files_modified:
  - tests/router.mjs.snapshot
  - ~/.claude/hooks/router.mjs
  - tests/router.hot-path-resolve.integration.test.mjs
autonomous: true
requirements:
  - ROUTE-01
  - ROUTE-03
  - PARITY-04
user_setup: []
estimate:
  tokens: 42000
  raw_tokens: 21000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The emitted additionalContext slash suggestion on the PRODUCTION hot path (inspectDecision -> guards -> render) is the resolver's resolved present candidate, not a direct mmEntry build (ROUTE-01/ROUTE-03)."
    - "A quarantined candidate (present only under the other runtime) is never emitted; the active runtime's local equivalent is (PARITY-04)."
    - "Nothing resolvable -> the slash is suppressed/pass-through (never a dead injection), fail-open preserved on any throw (ROUTE-03)."
    - "Equal-weight resolve candidates retain their DECLARED resolve-list order (stable), not rewritten lexicographically (ROUTE-01 ranked-list semantics)."
  artifacts:
    - tests/router.mjs.snapshot (inspectDecision resolve wiring, resolveSlashRoute stable-order tie-break)
    - ~/.claude/hooks/router.mjs (byte-identical mirror)
    - tests/router.hot-path-resolve.integration.test.mjs
  key_links:
    - "inspectDecision buildTargetIndexes/mmEntry -> resolveSlashRoute(mmEntry, manifest, {tier}).suggested_slash -> route.mode -> formatInjection 'Run /{{mode}}'"
    - "resolveSlashRoute present[] sort -> declared order for equal weights"
    - "snapshot <-> ~/.claude/hooks/router.mjs byte-identical"
---

<objective>
Address review closure item 1 (HIGH): the production route path does NOT call `resolveSlashRoute`. `inspectDecision()` builds a route directly from `mmEntry` (tests/router.mjs.snapshot:3022-3033) then applies guards — the resolver only feeds validation/exported probes, not the emitted suggestion. This plan wires the resolver into the actual production hot path so the emitted `suggested_slash` comes from `resolveSlashRoute`, locks equal-weight ranked-list order (review MEDIUM #6), and adds an integration test that drives the real `inspectDecision()` entrypoint with runtime-specific manifests and verifies the emitted `additionalContext`.

Purpose: Prove the architecture end-to-end on the real hot path — the resolved, runtime-correct suggestion is what actually ships as the slash line — closing the review's highest-priority finding. Stable declared order makes the ranked list behave as authored.

Output: hot-path resolve wiring + stable-order resolver + `tests/router.hot-path-resolve.integration.test.mjs` (real inspectDecision, per-runtime subprocess, emitted additionalContext assertions) + mirror.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-intent-first-routing-mode-map-schema-v4-guard-hole-closure/32-REVIEWS.md
@.planning/phases/32-intent-first-routing-mode-map-schema-v4-guard-hole-closure/32-VERIFICATION.md

# Source of truth for this plan (read_first targets):
@tests/router.mjs.snapshot        # inspectDecision (~3021-3042), resolveSlashRoute (~727-809 incl. present sort 778-781), formatInjection (~2479+), applyGuards
@tests/router.runtime-index-path.test.mjs  # created by 32.1-01 — runtime-aware indexes this plan's hot path uses on the raw-manifest side
@tests/router.hot-path-resolve.integration.test.mjs  # (to create)
@tests/router.schema-v4-routing.test.mjs  # established HOOK-import + inspectDecision-options conventions
# NOTE: depends on 32.1-01 (runtime-aware buildTargetIndexes) being complete; snapshot is shared so this runs wave 2.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire resolveSlashRoute into inspectDecision production hot path</name>
  <files>tests/router.mjs.snapshot, ~/.claude/hooks/router.mjs</files>
  <read_first>
    - tests/router.mjs.snapshot: `inspectDecision` route construction (~3021-3042) — where `const mmEntry = modeMapEntryForName(modeMap, topName)` yields a `route` built directly from `mmEntry`, then `applyGuards(route, ...)` at ~3035.
    - tests/router.mjs.snapshot: `resolveSlashRoute(entry, manifestOrIndexes, opts)` (~727-809) — returns `{suggested_slash, tier, weight, quarantined, provenance}` or null or the high empty-resolve `{suggested_slash:null, fallback_lines}` object; pure and fail-open.
    - tests/router.mjs.snapshot: `inspectDecision` opts injection (~2870-2871) — `Object.hasOwn(opts,'manifest') / 'modeMap'` confirm the in-memory seam the integration test will use.
    - tests/router.mjs.snapshot: `formatInjection`/`slashLine` (~2479+, `Run /{{mode}}`) — how `route.mode` becomes the emitted line.
  </read_first>
  <behavior>
    - Test 1: driving the real inspectDecision hot path under ROUTER_RUNTIME=claude with a runtime-split manifest emits final_injected_context containing the claude-local resolved slash (e.g. `Run /gsd-debug`), and does NOT emit the codex-only capability.
    - Test 2: under ROUTER_RUNTIME=codex the same prompt/routine emits the codex local equivalent slash and quarantines the claude-only one.
    - Test 3: when nothing resolves, the slash suggestion is suppressed/pass-through (no dead slash line).
  </behavior>
  <action>
    In `tests/router.mjs.snapshot`, in `inspectDecision()` after the `route` object is built from `mmEntry` and BEFORE `applyGuards(route, ...)`, for slash routes route the emitted mode through the resolver:
    - For `route.invoke_kind === 'slash'`, call `resolveSlashRoute(mmEntry, manifest, { tier })` (pass the raw manifest so PARITY-03 runtime-slice selection applies; plan 01 already made the index path runtime-aware for validate/routeTargetsExist).
    - When the result has a non-null `suggested_slash`: set `route.mode = resolved.suggested_slash` (the emitted suggestion now comes from the resolver, not the direct mmEntry build). Keep `route.id`/`invoke_kind`/`recommended_*` from mmEntry; add telemetry/state fields for `resolved_slash` and `resolve_quarantined` (e.g. record on `state` and push a `resolve:slash:<name>` decision trace) so the resolved path is attributable.
    - When invoked at a high tier and the resolver returns the empty-resolve fallback object (`suggested_slash:null` with `fallback_lines:[GENERIC_NATIVE_FALLBACK]`), surface the generic fallback exactly once (never a fabricated name) — route mode stays null and the fallback line is injected.
    - When the resolver returns null (nothing resolves, no fallback): do NOT ship the dead slash — suppress (drop `route.mode`/route so the path renders no slash, mirroring ROUTE-03 silent). This must be inside the existing try/catch so any resolver throw still fails open (outer catch already sets tier=error/pass-through).
    - Do not alter non-slash kinds (skill/agent/warn) — only the slash suggestion source changes.
  </action>
  <acceptance_criteria>
    - The emitted slash line's command is `resolveSlashRoute(...).suggested_slash` for resolvable slash routes (verified by the integration test).
    - Fail-open preserved: a resolver throw produces a pass-through, never a hard error and never a fabricated slash.
    - Mirror byte-identical (cmp snapshot live-hook = 0; router.mjs.snapshot.diff.test.mjs green).
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/router.hot-path-resolve.integration.test.mjs tests/router.schema-v4-routing.test.mjs tests/router.mjs.snapshot.diff.test.mjs</automated>
  </verify>
  <done>Hot-path emitted suggestion is resolver-sourced, per-runtime correct, and the phase-32 slash suite still passes with the mirror intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve declared resolve-list order for equal weights (stable sort)</name>
  <files>tests/router.mjs.snapshot, ~/.claude/hooks/router.mjs</files>
  <read_first>
    - tests/router.mjs.snapshot: `resolveSlashRoute` present-filter sort (~778-781): `.sort((a,b) => b.weight - a.weight || String(a.name).localeCompare(String(b.name)))` — the lexicographic tie-break that breaks declared order.
    - tests/router.mjs.snapshot: `addCandidate` (~766-772) — `candidates` is a Map keyed by stripped name, so Map iteration preserves declaration order (mode first, then resolve members in declaration order).
    - tests/router.resolve-tie-lint.test.mjs: ranked-list ordering expectations to stay consistent with.
  </read_first>
  <behavior>
    - Test 1: two equal-weight resolve members `[b, a]` resolve to `b` (declaration order), not `a` (lexicographic order).
    - Test 2: differing-weight members still sort weight-descending first (declaration order only breaks ties).
    - Test 3: mode (weight 1.0) remains the primary candidate over resolve members.
  </behavior>
  <action>
    In `resolveSlashRoute`, replace the lexicographic name tie-break with a STABLE sort on weight only (ES2019+ `Array.prototype.sort` is stable). Because `candidates` is an insertion-ordered Map (mode added first, then resolve members in authoring order), a weight-only descending sort preserves the DECLARED resolve-list order for equal weights. Remove `String(a.name).localeCompare(...)` so equal weights keep declaration order; document this as the ROUTE-01 ranked-list ordering contract (weight desc, then authoring order). Keep `addCandidate`'s higher-weight-wins dedup so a duplicate name upgrades rather than reorders.

    This task edits `tests/router.mjs.snapshot` a SECOND time (Task 1 already mirrored after its edit), so re-run the mirror-lockstep here or the byte-identity diff gate fails: after the snapshot edit, run `cp tests/router.mjs.snapshot ~/.claude/hooks/router.mjs` to copy the snapshot to the live hook, then confirm byte-identity by running `cmp tests/router.mjs.snapshot ~/.claude/hooks/router.mjs` (must exit 0). The snapshot's sort change and the live-hook copy must land together so `router.mjs.snapshot.diff.test.mjs` (byte-identity gate) stays green and the integration/unit tests exercise the live, mirrored hook.
  </action>
  <acceptance_criteria>
    - Equal-weight candidates emit in declaration order (not alphabetical).
    - Weight-descending remains primary; mode stays primary.
    - Additive unit assertions in the new integration spec (or the resolver spec) prove the ordering contract.
    - Mirror byte-identical after this second snapshot edit: `cmp tests/router.mjs.snapshot ~/.claude/hooks/router.mjs` exits 0 and `router.mjs.snapshot.diff.test.mjs` is green.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/router.hot-path-resolve.integration.test.mjs tests/router.schema-v4-routing.test.mjs tests/router.mjs.snapshot.diff.test.mjs</automated>
  </verify>
  <done>Equal-weight ranked lists preserve authored order; weight ordering unchanged; reveal order never regresses the phase-32 suite.</done>
</task>

<task type="auto">
  <name>Task 3: Integration test — real inspectDecision hot path, verified emitted additionalContext</name>
  <files>tests/router.hot-path-resolve.integration.test.mjs</files>
  <read_first>
    - tests/router.hot-path-resolve.integration.test.mjs (to create): new spec following the HOOK-import + spawnSync-per-runtime convention of tests/router.perf.test.mjs and tests/router.phase32-cross-runtime.test.mjs.
    - tests/router.mjs.snapshot: `inspectDecision(prompt, options)` opts seam (~2870-2871) allowing `manifest`/`modeMap` injection in-memory; `state.finalInjectedContext` carries the composed additionalContext; formatInjection output shape ("Run /{{mode}}", "<!-- router-inject ... -->" sentinel).
  </read_first>
  <action>
    Create `tests/router.hot-path-resolve.integration.test.mjs`. It must drive the REAL hot path — not just the resolver — for runtime-specific manifests:
    - Import the live hook and call `inspectDecision(prompt, { manifest: runtimeManifest, modeMap, mutateCache:false, logTelemetry:false, emitInjection:false, includePrompt:false, ... })` in a fresh node subprocess per ROUTER_RUNTIME (claude and codex).
    - Use a mode-map whose gsd-debug slash entry carries a `resolve` list `[{name:'gsd-debug',weight:1.0},{name:'systematic-debugging',weight:0.98}]` and a runtime-split manifest where claude owns gsd-debug and codex owns systematic-debugging.
    - Assert on the EMITTED `final_injected_context` / route block: under claude it contains the claude-local resolved slash line and does NOT contain the codex-only candidate; under codex the reverse (PARITY-04 emission, closure item 1). Assert nothing is emitted when the prompt resolves to nothing (ROUTE-03 silent).
    - Assert the near-tie (0.02 gap <= 0.05) downgrades the emitted tier to `med` (tie-lint hot-path behavior), and the quarantined candidate is recorded but never injected.
    - Each subprocess must exit 0 and produce no dead/foreign stdout beyond the asserted context.
  </action>
  <acceptance_criteria>
    - Covers claude + codex runtimes, real inspectDecision (scoring + guards + render), emitted additionalContext assertions, near-tie med downgrade, quarantine non-injection, and silent no-resolve.
    - No direct external home-dir writes from the test (live hook is read/imported only; mirror is the deploy step).
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/router.hot-path-resolve.integration.test.mjs</automated>
    <automated>node --test tests/router.mjs.snapshot.diff.test.mjs tests/router.schema-v4-routing.test.mjs tests/router.phase32-cross-runtime.test.mjs</automated>
  </verify>
  <done>Integration spec proves the production hot path emits the resolver-selected, runtime-correct suggestion (and suppresses quarantined/dead ones) in both runtimes; full phase-32 baseline green.</done>
</task>

</tasks>

## Artifacts this phase produces

- Modified `inspectDecision()` in `tests/router.mjs.snapshot` — slash suggestion routed through `resolveSlashRoute` (emitted `suggested_slash` + provenance/quarantine attribution, fail-open preserved).
- Modified `resolveSlashRoute` present-sort in `tests/router.mjs.snapshot` — stable weight-only tie-break preserving declared resolve-list order.
- Byte-identical `~/.claude/hooks/router.mjs` mirror.
- New `tests/router.hot-path-resolve.integration.test.mjs` — real-hot-path integration spec (claude + codex subprocesses, emitted additionalContext, tie-lint med downgrade, quarantine, silent-no-resolve).

## Mirror-lockstep requirement (this plan edits the snapshot)

Snapshot edits land first, then copy `tests/router.mjs.snapshot` to `~/.claude/hooks/router.mjs` and verify byte-identity via `cmp` + `router.mjs.snapshot.diff.test.mjs`. Tests use the live-hook read-only import seam.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mode-map resolve list -> hot-path injection | untrusted/absent candidates cross here; a dead or foreign-capability slash would ship as a suggestion |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32.1-04 | Spoofing | `inspectDecision` slash emission | high | mitigate | Emit only `resolveSlashRoute(...).suggested_slash`; quarantined candidates are recorded but never injected |
| T-32.1-05 | Tampering | `resolveSlashRoute` present-sort order | medium | mitigate | Stable weight-only sort preserves declared order; ordering-contract unit assertions lock it |
| T-32.1-06 | DoS | fault injection into resolver on hot path | high | mitigate | Resolver already try/catch fail-open->null; inspectDecision outer catch degrades to error/pass-through, never a throw |
| T-32.1-07 | Information Disclosure | quarantined names in rendered context | low | accept | Quarantined names go to telemetry/state only, not the injected block (asserted by integration test) |
| T-32.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs (stdlib-only); slopcheck + blocking human checkpoint only if an install is ever introduced |
</threat_model>

<verification>
- `node --test tests/router.hot-path-resolve.integration.test.mjs` passes (claude + codex).
- `node --test tests/router.mjs.snapshot.diff.test.mjs` passes — snapshot byte-identical to live hook.
- Phase-32 baseline still green (schema-v4-routing, phase32-cross-runtime, resolve-tie-lint, coverage-audit, failopen).
- `cmp tests/router.mjs.snapshot ~/.claude/hooks/router.mjs` returns 0.
</verification>

<success_criteria>
- Production hot path emits the resolver-selected, runtime-correct suggestion (closure item 1 closed).
- Equal-weight ranked lists preserve declared order (MEDIUM #6 closed).
- Never a dead/foreign slash; fail-open verified; mirror intact.
</success_criteria>

<output>
Create `.planning/phases/32.1-phase-32-review-closure-wire-resolver-into-production-hot-pa/32.1-02-SUMMARY.md` when done.
</output>

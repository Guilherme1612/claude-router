---
phase: 32.1-phase-32-review-closure-wire-resolver-into-production-hot-pa
verified: 2026-08-01T18:17:38Z
status: passed
score: 12/13 must-haves verified
behavior_unverified: 1
overrides_applied: 0
re_verification: false
deferred:

  - truth: "The broader 1240/1262 suite remains green across installed-controller lifecycle, installed mode-map parity, route-target calibration, and release baselines."
    addressed_in: "Phase 34 and Phase 36"
    evidence: "Phase 34 covers per-install auto-calibration; Phase 36 success criteria explicitly cover live-install readiness, the existing baseline, reverse-gap reconciliation, and release activation."
behavior_unverified_items:

  - truth: "When the production hot path encounters a resolver throw, it remains fail-open and does not emit a fabricated slash suggestion."
    test: "Exercise inspectDecision with a fault injected specifically inside resolveSlashRoute on a slash-scoring prompt."
    expected: "The decision exits safely with no fabricated slash injection, preserving pass-through/error handling rather than crashing or emitting a stale candidate."
    why_human: "The source has resolver and outer-pipeline catches, and the generic fail-open test passes, but no existing test injects a throw specifically inside the resolver while running the production inspectDecision path."
human_verification:

  - test: "Fault-inject resolveSlashRoute during the production inspectDecision hot path."
    expected: "The hook remains fail-open: no stale or fabricated slash is emitted, and the decision is returned as safe pass-through/error handling."
    why_human: "Presence of catch blocks and a generic forced-pipeline-throw test cannot prove the resolver-specific exception path."
---

# Phase 32.1: Phase 32 Review Closure — Wire Resolver into Production Hot Path Verification Report

**Phase Goal:** Close the four Codex review findings from Phase 32: (1) emitted production slash suggestion comes from `resolveSlashRoute` on hot path; (2) runtime parity cannot be bypassed via index path (`buildTargetIndexes` runtime-aware); (3) tie-lint hard `--strict-coverage` build gate; (4) real end-to-end hot-path perf fixture substantiates warm p95 < 40ms / max < 100ms.

**Verified:** 2026-08-01T18:17:38Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | `routeTargetsExist` and `validateRouteTargets` evaluate slash/resolve members against the active runtime inventory supplied by `buildTargetIndexes`. | ✓ VERIFIED | `tests/router.mjs.snapshot:710-719,837,956`; `tests/router.runtime-index-path.test.mjs` passed 2/2. |
| 2 | A capability present only in Codex is not treated as present under Claude, and vice versa, on the index path. | ✓ VERIFIED | Runtime-isolation fixtures exercise both runtime directions; `router.runtime-index-path.test.mjs` passed 2/2 and cross-runtime tests passed. |
| 3 | A manifest without a usable `runtime_commands` slice retains the flat `commands` fallback. | ✓ VERIFIED | `buildTargetIndexes` explicitly selects a non-empty runtime slice and otherwise falls back to `manifest.commands`; runtime-index tests cover the legacy fallback. |
| 4 | The production `inspectDecision` hot path emits the resolver-selected slash candidate rather than directly emitting `mmEntry.mode`. | ✓ VERIFIED | `tests/router.mjs.snapshot:3058-3076` calls `resolveSlashRoute` before route rendering; `router.hot-path-resolve.integration.mjs` passed 7/7, including the emitted-context assertions. |
| 5 | A foreign-runtime candidate is quarantined and never emitted, while an active local equivalent is emitted. | ✓ VERIFIED | Claude and Codex hot-path subtests assert local `final_injected_context`, reject the foreign command, and assert `resolve_quarantined`; 7/7 hot-path tests passed. |
| 6 | No resolvable candidate suppresses the slash injection, and resolver failure remains fail-open. | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | The no-resolvable branch passes in the hot-path suite and the resolver/outer path has catches (`tests/router.mjs.snapshot:815,3139`); `router.failopen.test.mjs` passes the generic forced-pipeline-throw case. No test injects a throw specifically inside `resolveSlashRoute` on `inspectDecision`, so this invariant requires human verification. |
| 7 | Equal-weight resolve candidates retain declared order while differing weights still sort by weight. | ✓ VERIFIED | `router.hot-path-resolve.integration.mjs` and the tie-order tests passed; equal-weight `b,a` remains `b`, while the higher-weight candidate wins. |
| 8 | `build-manifest --strict-coverage` is a hard gate: near-tie/unresolvable stale cases exit nonzero and a clean case exits zero. | ✓ VERIFIED | `router.build-gate.test.mjs` passed 5/5, covering strict exit behavior and clean output. |
| 9 | The strict gate actually runs even when the ordinary audit would otherwise pass. | ✓ VERIFIED | The build-gate fixture asserts zero forward audit diagnostics while the near-tie strict gate still exits nonzero; the 5/5 focused gate suite passed. |
| 10 | Absent resolve members are classified as nonblocking quarantine when the route remains resolvable, and as blocking `stale_target` when fully unresolvable. | ✓ VERIFIED | `src/coverage/audit.mjs:140-168` implements the split; `router.coverage-audit.test.mjs` and tie-lint/build-gate checks passed in the 54-test integration group. |
| 11 | The real end-to-end hot path, including manifest/scoring/cache/guards/route/render, satisfies warm p95 < 40ms and max < 100ms. | ✓ VERIFIED | `router.perf.test.mjs` passed 5/5; the e2e fixture reaches render (`__router_e2e_rendered=1`) and asserts both budgets. |
| 12 | The pure resolver helper has a separate benchmark and remains within the same budget. | ✓ VERIFIED | `router.perf.test.mjs` contains a distinct helper test and passed 5/5. |
| 13 | Trivial pass-through remains below the 100ms ceiling. | ✓ VERIFIED | The three trivial/pass-through performance checks in `router.perf.test.mjs` passed as part of 5/5. |

**Score:** 12/13 truths verified (12 present and behavior verified; 1 present and wired but behavior-unverified).

## Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `tests/router.mjs.snapshot` | Runtime-aware indexes, resolver, and production hot-path wiring | ✓ VERIFIED | 3,404-line substantive implementation; exports and internal callers are exercised by focused suites. |
| `/Users/guilherme/.claude/hooks/router.mjs` | Deployed production hook mirrors the verified snapshot | ✓ VERIFIED | `rtk cmp tests/router.mjs.snapshot /Users/guilherme/.claude/hooks/router.mjs` exited 0; snapshot-diff test passed 1/1. |
| `tests/router.runtime-index-path.test.mjs` | Runtime-isolation regression coverage | ✓ VERIFIED | Exists, substantive, and executed 2/2. |
| `tests/router.hot-path-resolve.integration.test.mjs` | Production inspectDecision resolver integration coverage | ✓ VERIFIED | Exists, substantive, and executed 7/7. |
| `build-manifest.mjs` | Strict coverage build gate wired to audit and tie lint | ✓ VERIFIED | Imports audit/lint helpers, computes blocking violations, and exits nonzero for strict violations; build-gate tests passed 5/5. |
| `src/coverage/audit.mjs` | Quarantine versus stale-target severity classification | ✓ VERIFIED | Contains real route-resolution classification and returns forward/quarantined diagnostics; coverage tests passed. |
| `tests/router.build-gate.test.mjs` | Gate behavior fixtures | ✓ VERIFIED | Executed 5/5, including audit-pass/strict-fail coverage. |
| `tests/router.coverage-audit.test.mjs` | Severity and coverage regression tests | ✓ VERIFIED | Executed in the focused integration group; all 18 coverage-audit subtests passed. |
| `tests/router.perf.test.mjs` | Real e2e and helper performance fixtures | ✓ VERIFIED | Executed 5/5, including render reachability and p95/max assertions. |

The GSD artifact query returned zero parsed inline artifacts/links for these plans because their `must_haves` are embedded in task XML rather than the query parser’s expected frontmatter shape. Manual three-level verification above is therefore authoritative: all listed artifacts exist, are substantive, and are wired into tests or production code.

## Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| `buildTargetIndexes` | active runtime manifest slice | `runtime_commands[RUNTIME]` with flat fallback | WIRED | Runtime selection is explicit at `tests/router.mjs.snapshot:710-727`; runtime-index tests pass. |
| `routeTargetsExist` | resolver candidate availability | index-aware `resolveSlashRoute` | WIRED | `routeTargetsExist` uses the supplied/build indexes and resolver path at line 956. |
| `inspectDecision` | `resolveSlashRoute` | resolver call before route render | WIRED | `tests/router.mjs.snapshot:3058`; hot-path integration proves output uses the resolved command. |
| resolver result | injected route text | `route.mode`/`resolved_slash` → `formatInjection` | WIRED | Fresh and cache-hit render paths populate final injected context; hot-path tests assert emitted text. |
| foreign runtime candidate | quarantine state | active-runtime command set | WIRED | Codex/Claude integration assertions prove quarantine and non-emission. |
| audit severity | strict build exit | `auditCoverage` + `lintModeMap` → blocking violations | WIRED | Build-gate fixtures prove audit-pass/strict-fail and clean-pass outcomes. |
| absent resolve member | quarantine or `stale_target` | route resolvability branch in `src/coverage/audit.mjs` | WIRED | Coverage-audit tests pass the two severity branches. |
| perf fixture | production render | spawned live `inspectDecision` path | WIRED | The e2e fixture proves `__router_e2e_rendered=1` and enforces latency budgets. |

## Data-Flow Trace (Level 4)

| Artifact | Data variable | Source | Produces real data | Status |
| --- | --- | --- | --- | --- |
| `inspectDecision` | selected route / final injected context | manifest + mode-map scoring → runtime resolver → `formatInjection` | Yes; exercised with Claude and Codex fixtures | ✓ FLOWING |
| `buildTargetIndexes` | active command `Set` | `runtime_commands[RUNTIME]`, or legacy flat manifest | Yes; runtime-isolation tests distinguish members | ✓ FLOWING |
| strict build gate | blocking diagnostics and exit status | generated manifest → audit/tie lint → strict decision | Yes; fixtures produce both zero and nonzero outcomes | ✓ FLOWING |
| `auditCoverage` | forward/quarantined diagnostics | route target and resolve-member inspection | Yes; coverage tests assert classification | ✓ FLOWING |

## Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| Runtime index isolation | `rtk node --test tests/router.runtime-index-path.test.mjs` | 2/2 passed | ✓ PASS |
| Production hot-path resolver emission, quarantine, suppression, and tie behavior | `rtk node --test tests/router.hot-path-resolve.integration.test.mjs` | 7/7 passed | ✓ PASS |
| Strict tie/stale build gate | `rtk node --test tests/router.build-gate.test.mjs` | 5/5 passed | ✓ PASS |
| E2E and helper performance | `rtk node --test tests/router.perf.test.mjs` | 5/5 passed | ✓ PASS |
| Snapshot/deployed-hook parity | `rtk cmp tests/router.mjs.snapshot /Users/guilherme/.claude/hooks/router.mjs`; `rtk node --test tests/router.mjs.snapshot.diff.test.mjs` | byte-identical; 1/1 passed | ✓ PASS |
| Cross-runtime, schema, tie-lint, coverage, and generic fail-open integration | `rtk node --test tests/router.phase32-cross-runtime.test.mjs tests/router.schema-v4-routing.test.mjs tests/router.resolve-tie-lint.test.mjs tests/router.coverage-audit.test.mjs tests/router.failopen.test.mjs` | 54/54 passed | ✓ PASS |

The full workspace command `rtk node --test tests/*.test.mjs` returned nonzero. The phase summary reported 1,240/1,262 passing with 22 failures; the individual file reruns below independently account for all 22 named failures. This is not hidden or treated as a green release baseline.

## Broader Suite Failure Classification (22 failures)

| Count | Test file and failing subtests | Classification | Disposition |
| ---: | --- | --- | --- |
| 1 | `router.installer-coexistence.test.mjs`: fresh install onboarding consumes deployed baseline | Installed baseline/onboarding lifecycle mismatch | Outside Phase 32.1’s resolver/gate/perf contract; grouped with Phase 36 live-install and baseline work. |
| 5 | `router.lifecycle-recovery.test.mjs`: five D-04 unsafe/corrupt registry, index, tuple-manifest/schema, and tuple-hash recovery cases | Installed controller did not publish within 5s | Deferred to Phase 36 live-install/readiness and release-baseline work; not a hot-path resolver failure. |
| 3 | `router.autonomous-lifecycle.test.mjs`: Claude runtime lifecycle, Codex runtime lifecycle, and fourteen-cell operation coverage | Installed controller readiness timeout; coverage test is downstream (`[]` vs 14) | Deferred to Phase 36 success criteria for lifecycle readiness and baseline coverage. |
| 1 | `router.test-mode-seam.test.mjs`: opt-in test-mode installed watcher → controller → compiled-index seam | Candidate publication timeout within 2s | Deferred to lifecycle/test-seam work in Phase 36. |
| 2 | `router.lifecycle.test.mjs`: live mutation reconciliation and installed-project ancestor inventory repair | Reconciliation condition not met within 2s | Deferred to Phase 36 live-install/readiness criteria. |
| 1 | `router.inspect.test.mjs`: inspect hit explanation full prompt-level contract | Installed/current route shape lacks expected `selected_route` for this legacy assertion | Broader contract/parity regression; not one of the Phase 32.1 must-haves; included in Phase 36 baseline reconciliation. |
| 1 | `router.mode-map-curation.test.mjs`: installed mode map matches repository canonical source | Deployed schema v4/resolve map differs from repository schema v3 expectation | Installed canonical parity baseline; deferred to Phase 36 baseline/reverse-gap work. |
| 1 | `router.route-targets.test.mjs`: real mode-map route targets resolve to known safe manifest entries | Current live map contains stale `gsd-debug` target under this legacy assertion | Same installed mode-map/manifest parity family; deferred to Phase 36 release cleanup. |
| 1 | `router.context-prompt-integration.test.mjs`: startup pointer/cooldown output regex | Assertion expects legacy `/router-inject`; actual message is `Router suggestion available — inspect with /router suggestion` | Legacy output-contract mismatch, separate from resolver selection; deferred to baseline reconciliation. |
| 3 | `router.perf-evolved.test.mjs`: weights file, evolve trigger, and file-hook cases | EPERM writing `/Users/guilherme/.claude/router/{weights.json,.evolve-trigger}` in this verification environment | Environment/test-isolation failure; not evidence against Phase 32.1’s read-only hot-path perf fixture. Requires an owning environment/release test fix. |
| 1 | `router.safety-release.test.mjs`: calibration fixtures preserve subset-specific thresholds | Expected status 0, actual 1; calibration output reports 47/58 and route-target misses | Existing calibration/release baseline mismatch; deferred to Phase 34 calibration and Phase 36 reverse-gap/release work. |
| 1 | `router.calibration-codebase.test.mjs`: calibration CLI codebase target/miss taxonomy | Expected status 0, actual 1; current mode-map/threshold misses | Calibration baseline issue; addressed by Phase 34 calibration work and Phase 36 baseline reconciliation. |
| 1 | `router.calibration-evolution.test.mjs`: dry-run weights blend produces a route | No route produced under current map/threshold state | Calibration/evolution baseline issue, not the Phase 32.1 resolver hot-path gate; deferred to Phase 34/36. |

**Classification total:** 1 + 5 + 3 + 1 + 2 + 1 + 1 + 1 + 1 + 3 + 1 + 1 + 1 = 22.

## Probe Execution

No phase-declared or conventional `scripts/*/tests/probe-*.sh` probe was found. Probe execution is therefore not applicable; the focused Node test commands above are the phase’s runnable checks.

## Requirements Coverage

| Requirement | Source plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| ROUTE-01 | 32.1-01/02 | Intent resolves to a ranked framework-neutral locally-present candidate | ✓ SATISFIED | Runtime-aware resolver and production hot-path integration pass; local slash is emitted. |
| ROUTE-03 | 32.1-02 | Absent top candidate falls back to next present; none becomes silent low | ✓ SATISFIED | Claude/Codex local-equivalent and no-resolvable suppression tests pass. |
| ROUTE-05 | 32.1-03 | Tie/near-tie downgrade, stale quarantine, and audit guard | ✓ SATISFIED | Stable tie tests, coverage severity tests, and strict build-gate tests pass. |
| PARITY-03 | 32.1-01/02 | Active runtime is the only suggestion source | ✓ SATISFIED | `buildTargetIndexes` and resolver use `runtime_commands[RUNTIME]`; cross-runtime tests pass. |
| PARITY-04 | 32.1-02 | Capability maps to a local equivalent | ✓ SATISFIED | Hot-path Claude/Codex fixtures emit the local equivalent and quarantine the foreign candidate. |

No additional Phase 32.1 requirement ID is orphaned: the phase roadmap contract declares exactly these five IDs, and each has implementation evidence above. The general traceability table still points these IDs at Phase 32; the inserted 32.1 closure phase explicitly carries and verifies the same contract.

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | ---: | --- | --- | --- |
| Phase implementation/test artifacts | — | No `TBD`, `FIXME`, or `XXX` markers found | None | No debt-marker blocker. |
| `tests/router.mjs.snapshot` | 815, related no-route branches | `return null`/empty injection paths | Info, legitimate | These are explicit no-resolve/fail-open outcomes exercised by tests, not placeholder implementations. |
| Phase implementation/test artifacts | — | No placeholder/TODO/HACK-only implementation found | None | Artifacts are substantive and wired. |

`rtk git diff --check` was clean. Existing unrelated worktree changes were preserved; this report is the only file created by this verification.

## Human Verification Required

### 1. Resolver-specific fail-open fault injection

**Test:** Run the deployed hook’s `inspectDecision` hot path with a controlled exception injected specifically inside `resolveSlashRoute` for a prompt that would otherwise score a slash route.

**Expected:** The hook returns safe pass-through/error handling, emits no stale or fabricated slash suggestion, and does not crash.

**Why human:** The current code has the relevant catches and the generic forced-pipeline-throw test passes, but static inspection and that generic test do not exercise the resolver-specific throw boundary.

## Gaps Summary

The four Phase 32 review closures are present, substantive, wired, and supported by passing focused/integration evidence: runtime-aware index isolation, production resolver emission, strict tie/stale coverage gating, and real e2e hot-path performance. No phase-contract truth failed, no required artifact is missing or stubbed, and no key link is broken.

The phase is routed to `human_needed` rather than `passed` because the resolver-specific fail-open exception path has not been behaviorally exercised. Separately, the broader suite is genuinely non-green at 1,240/1,262; all 22 failures are enumerated and classified above, with lifecycle, baseline, calibration, and environment ownership deferred to later roadmap work rather than silently absorbed into this phase’s score.

---

_Verified: 2026-08-01T18:17:38Z_  
_Verifier: the agent (gsd-verifier)_

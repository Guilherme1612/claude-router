---
phase: 21-authoritative-personalized-inventory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/helpers/inventory-fixture.mjs
  - tests/router.registry-schema.test.mjs
  - src/registry/schema.mjs
autonomous: true
requirements: [DISC-02]
must_haves:
  truths:
    - "Every installed artifact can be represented by one framework-neutral record with explicit native_type, semantic_type, lifecycle_role, enabled, dispatchable, invocation availability, dependencies, scope, and portable provenance."
    - "Configuration, instruction, container, disabled, and unknown records remain inspectable without receiving fabricated invocation authority."
    - "Canonical semantic bytes exclude volatile processing metadata and remain stable across record and set ordering."
  artifacts:
    - path: "src/registry/schema.mjs"
      provides: "Phase 21 normalized inventory record validation and canonicalization"
      exports: ["validateCapability", "canonicalizeCapability", "stableStringify"]
    - path: "tests/helpers/inventory-fixture.mjs"
      provides: "Isolated multi-profile inventory fixture and mutation builder"
    - path: "tests/router.registry-schema.test.mjs"
      provides: "Normalized-record and canonical-byte contract tests"
  key_links:
    - from: "tests/helpers/inventory-fixture.mjs"
      to: "src/registry/schema.mjs"
      via: "fixture records pass validateCapability and stableStringify"
    - from: "src/registry/schema.mjs"
      to: "src/registry/build.mjs"
      via: "existing assembly canonicalizes every normalized observation"
---

<objective>
Define the authoritative normalized inventory contract and the reusable isolated fixture boundary required by all later Phase 21 plans, implementing D-01 through D-04 and D-15 through D-17 at the schema seam.

Purpose: Make inert, disabled, compound, and unknown installed artifacts first-class inspectable records without granting them invocation or policy authority.
Output: Extended canonical schema plus deterministic synthetic-profile helpers and focused contract tests.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-authoritative-personalized-inventory/21-CONTEXT.md
@.planning/phases/21-authoritative-personalized-inventory/21-RESEARCH.md
@.planning/phases/21-authoritative-personalized-inventory/21-PATTERNS.md
@.planning/phases/21-authoritative-personalized-inventory/21-AI-SPEC.md
@.planning/phases/21-authoritative-personalized-inventory/21-VALIDATION.md
@src/registry/schema.mjs
@tests/router.registry-schema.test.mjs

<interfaces>
Existing schema exports: validateCapability(record), stableStringify(value), canonicalizeCapability(record).
Existing identity consumers require portable scope and provenance fields; set-like arrays are schema-sorted while invocation order remains semantic.
The Phase 21 record contract must expose native_type, semantic_type, lifecycle_role, enabled, dispatchable, invocation availability, dependencies, provenance, adapter/parser evidence, and diagnostics.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish synthetic inventory profiles and normalized-record RED contracts</name>
  <files>tests/helpers/inventory-fixture.mjs, tests/router.registry-schema.test.mjs</files>
  <read_first>
    - .planning/phases/21-authoritative-personalized-inventory/21-CONTEXT.md (D-01 through D-04, D-15 through D-21)
    - .planning/phases/21-authoritative-personalized-inventory/21-AI-SPEC.md (reference dataset and installed-state fidelity rubric)
    - tests/router.adapters.test.mjs (mkdtempSync, put, portable-output, and finally-cleanup patterns)
    - tests/router.registry-schema.test.mjs (current capability fixture and stable-byte assertions)
    - src/registry/schema.mjs (current required fields and canonical set handling)
  </read_first>
  <action>Create `tests/helpers/inventory-fixture.mjs` exporting deterministic builders for Claude-heavy, Codex-heavy, mixed/custom, and unknown-future profiles, separate synthetic home/project/worktree roots, mutation playback names for add/edit/rename/move/disable/replace/dependency-loss/removal, and semantic-byte assertions. Profiles must vary names, counts, missing families, scopes, and runtime mixes per D-18 through D-21 and must never read ambient user roots. Extend `tests/router.registry-schema.test.mjs` with failing contracts for D-01 through D-04: explicit `native_type`, small core `semantic_type` plus namespaced adapter evidence per D-03, `lifecycle_role`, `enabled`, `dispatchable`, optional/unavailable invocation for inert records, compound `container_id`/member provenance, and opaque unknown records forced non-dispatchable per D-02. Add D-15 scope-separation and D-16 authored-prose inertness cases. Assert canonical bytes ignore operational generation/timestamp/event metadata and deterministically sort schema-owned sets per D-07.</action>
  <acceptance_criteria>
    - Source assertion: the helper exports four profile builders and mutation playback without developer-home paths or live capability names.
    - Behavior assertion: tests reject a dispatchable unknown/config/instruction/container record and accept the same record when non-dispatchable with invocation unavailable.
    - Behavior assertion: same-named global, user, project, and worktree fixtures remain distinct.
    - RED assertion marker: name every intentionally failing Phase 21 schema contract with `[phase21-red:schema]`; the TAP-aware automated harness exits 0 only when at least one exact failing `not ok` test name carries that marker and every reported test failure carries the same marker.
  </acceptance_criteria>
  <verify>
    <automated>rtk node -e "const {spawnSync}=require('node:child_process');const r=spawnSync(process.execPath,['--test','tests/router.registry-schema.test.mjs'],{encoding:'utf8'});const out=(r.stdout||'')+(r.stderr||'');const failed=[...out.matchAll(/^\\s*not ok \\d+ - (.+)$/gm)].map(m=&gt;m[1]);process.exit(r.status!==0&amp;&amp;failed.length&gt;0&amp;&amp;failed.every(name=&gt;name.includes('[phase21-red:schema]'))?0:1)"</automated>
  </verify>
  <done>Fixture builders are reusable by downstream plans and the schema test names every new invariant before production edits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the framework-neutral inventory schema</name>
  <files>src/registry/schema.mjs, tests/router.registry-schema.test.mjs</files>
  <read_first>
    - src/registry/schema.mjs (validateCapability, canonicalizeCapability, stableStringify)
    - tests/router.registry-schema.test.mjs (Task 1 contract)
    - src/registry/identity.mjs (scopeSuffix and stableCapabilityId consumers)
    - src/registry/build.mjs (assembleRegistry validation/canonicalization consumer)
    - .planning/phases/21-authoritative-personalized-inventory/21-UI-SPEC.md (inventory-record field order and unknown/disabled presentation semantics)
  </read_first>
  <behavior>
    - Dispatchable records require a schema-validated invocation; inert, disabled, and unknown records represent invocation as unavailable.
    - Unknown semantic types preserve namespaced native type, provenance, fingerprint, and adapter evidence while remaining non-dispatchable.
    - Compound containers and members retain explicit provenance links without making the container dispatchable.
    - Semantic canonicalization excludes operational timestamps, triggers, scan IDs, event order, and generation IDs.
  </behavior>
  <action>Extend `validateCapability` and `canonicalizeCapability` additively for the Task 1 contract. Use a small runtime-neutral semantic enum and accept adapter extensions only as namespaced evidence, not Router-core branches, per D-03/D-07. Enforce `enabled: false` implies `dispatchable: false` per D-12; require invocation only when dispatchable is true; require portable logical-root-relative provenance and bounded adapter/parser evidence; validate container/member provenance and required-dependency state. Preserve existing v1 records through a deterministic compatibility normalization where their current invocation/lifecycle values map unambiguously; do not introduce an AI framework, provider SDK, network call, semantic cache, or model-based classification per the AI-SPEC. Keep `stableStringify` as the sole canonical byte boundary and ensure operational inspection state is outside the semantic capability value.</action>
  <acceptance_criteria>
    - Source assertion: `validateCapability`, `canonicalizeCapability`, and `stableStringify` remain exported and existing consumers need no ecosystem-name conditionals.
    - Behavior assertion: all Task 1 record, scope, invocation, compound, authored-prose, and byte-stability cases pass.
    - Compatibility assertion: existing canonical records still validate and serialize deterministically.
    - Test command: `rtk node --test tests/router.registry-schema.test.mjs tests/router.registry-build.test.mjs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>rtk node --test tests/router.registry-schema.test.mjs tests/router.registry-build.test.mjs</automated>
  </verify>
  <done>The canonical schema represents every Phase 21 artifact family safely and supplies stable semantic bytes to later plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adapter observation → canonical schema | Untrusted filesystem-derived metadata enters authoritative candidate representation. |
| semantic snapshot → downstream mapping | Record fields may later influence eligibility and routing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-01 | Elevation of Privilege | `validateCapability` | high | mitigate | Schema requires explicit enabled/dispatchable/lifecycle invariants and validated invocation; authored prose and unknown types cannot self-authorize. |
| T-21-02 | Spoofing | scope/provenance identity fields | high | mitigate | Require portable logical root, relative path, runtime, scope, adapter/parser evidence; keep cross-scope records distinct. |
| T-21-03 | Tampering | canonical semantic bytes | medium | mitigate | Schema-owned canonicalization and `stableStringify` exclude volatile operational metadata and sort set-like values. |
| T-21-SC | Tampering | package installation | high | mitigate | No package install exists; Phase 21 uses Node stdlib and repository-local modules only. |
</threat_model>

<verification>
- `rtk node --test tests/router.registry-schema.test.mjs tests/router.registry-build.test.mjs`
- `rtk node --test tests/router.adapters.test.mjs`
</verification>

<success_criteria>
- DISC-02 is executable: every record exposes the required runtime-neutral fields and safe invocation invariant.
- D-01 through D-04 and D-15 through D-17 have a canonical schema representation.
- The shared fixture boundary is ready for portability, mutation, convergence, and security plans.
</success_criteria>

<output>
Create `.planning/phases/21-authoritative-personalized-inventory/21-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `tests/helpers/inventory-fixture.mjs` — exports synthetic profile builders, mutation playback, and canonical-byte assertions.
- `src/registry/schema.mjs` — extended normalized fields `native_type`, `semantic_type`, `lifecycle_role`, `enabled`, `dispatchable`, invocation availability, adapter/parser evidence, container/member provenance, and dependency state.
- `tests/router.registry-schema.test.mjs` — Phase 21 schema and canonical-byte contracts.

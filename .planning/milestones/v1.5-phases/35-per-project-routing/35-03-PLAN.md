---
phase: 35-per-project-routing
plan: 03
type: execute
wave: 3
depends_on:
  - 35-02
files_modified:
  - tests/router.project-routing.e2e.test.mjs
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - PROJ-01
  - PROJ-02
  - PROJ-03
must_haves:
  truths:
    - "A temporary install discovers a project skill from ~/.claude.json, routes it inside the project, and excludes it outside."
    - "Adding or removing the project root changes the manifest fingerprint while an unchanged rebuild remains deterministic."
    - "Existing router, lifecycle, calibration, and snapshot gates remain green except tracked baseline debt."
  artifacts:
    - tests/router.project-routing.e2e.test.mjs
    - .planning/phases/35-per-project-routing/35-VERIFICATION.md
tasks:
  - task: "Add the temporary-home builder plus routing-corpus e2e scenario and fingerprint assertions."
    files: tests/router.project-routing.e2e.test.mjs
    verify: "node --test tests/router.project-routing.e2e.test.mjs"
  - task: "Run focused regressions and write review, security, Nyquist, verification, roadmap, and state evidence."
    files: .planning/ROADMAP.md .planning/STATE.md
    verify: "node --test tests/router.build-manifest.test.mjs tests/router.project-routing*.test.mjs tests/router.mjs.snapshot.diff.test.mjs"
verification:
  - "node --test tests/router.build-manifest.test.mjs tests/router.project-routing.test.mjs tests/router.project-routing.e2e.test.mjs tests/router.mjs.snapshot.diff.test.mjs"
success_criteria:
  - "PROJ-01, PROJ-02, and PROJ-03 are proven end-to-end and Phase 36 is the only remaining milestone phase."

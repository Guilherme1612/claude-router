---
phase: 17-compiled-prompt-routing-and-safe-evolution
plan: 05
type: execute
wave: 5
depends_on: ["17-04"]
gap_closure: true
files_modified:
  - tests/router.compiled-evolution.test.mjs
autonomous: true
requirements:
  - REL-01

must_haves:
  truths:
    - "F-05/REL-01/D-14: measureRoutes times the real routeContextPrompt compiled-routing callable across the fixed corpus, with warm p95 below 25 ms and every measured route below 100 ms."
    - "F-06/REL-01/D-13: every fixed-corpus quality result comes from routeContextPrompt rather than fixture.expected, including minimal prompt, explicit override, stale context, ambiguity, terminal state, dependency, and context-budget cases."
    - "F-06/D-13 through D-15: context-budget evaluation measures the exact UTF-8 bytes of routeContextPrompt additional_context against each fixture max_context_bytes ceiling."
  artifacts:
    - path: tests/router.compiled-evolution.test.mjs
      provides: "End-to-end fixed-corpus routing, emitted-context byte, and latency proof for the compiled prompt path"
  key_links:
    - from: tests/router.compiled-evolution.test.mjs
      to: src/context/prompt-route.mjs
      via: "fixture adapter invokes routeContextPrompt against real compiled-index and capsule state"
    - from: tests/router.compiled-evolution.test.mjs
      to: src/evolution/perf-measure.mjs
      via: "the same real routing callable feeds evaluateCalibrationCorpus and measureRoutes"
---

<objective>
Close F-05 and F-06 by replacing the Phase 17 integration-test shortcuts with end-to-end calibration over the actual compiled prompt router and its emitted context bytes.

Purpose: REL-01 needs direct evidence that the real prompt-time route stays within its latency ceilings and preserves fixed-corpus routing quality/context budgets; unit coverage of the measurement primitives is necessary but not sufficient.
Output: A hermetic integration corpus in `tests/router.compiled-evolution.test.mjs` that constructs compiled indexes and capsules, routes every fixture through `routeContextPrompt`, evaluates emitted UTF-8 bytes, and measures the real callable.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-compiled-prompt-routing-and-safe-evolution/17-CONTEXT.md
@.planning/phases/17-compiled-prompt-routing-and-safe-evolution/17-VERIFICATION.md
@.planning/phases/17-compiled-prompt-routing-and-safe-evolution/17-04-SUMMARY.md
@src/context/prompt-route.mjs
@src/evolution/perf-measure.mjs
@tests/router.context-prompt-integration.test.mjs
@tests/router.perf-calibration.test.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build a real compiled-routing adapter for the fixed corpus</name>
  <files>tests/router.compiled-evolution.test.mjs</files>
  <behavior>
    - Each `CALIBRATION_CORPUS` fixture owns hermetic temporary compiled-index and capsule state; no live `~/.claude` path is read or written.
    - Minimal-prompt, explicit-override, stale-context, ambiguity, terminal-state, dependency, and context-budget fixtures invoke `routeContextPrompt` with deterministic `now` and the fixture-specific authoritative/capsule state needed to produce their expected semantics.
    - The adapter's enumerable fields contain only observed `result.resolution.outcome` and `result.resolution.dispatch_eligible`, plus `context_within_budget` derived from the actual emitted bytes for context-budget fixtures; the real `result.additional_context` is exposed as a non-enumerable `additional_context` property for byte measurement without contaminating exact quality JSON comparison.
  </behavior>
  <action>Extend the existing end-to-end test with the fixture-building pattern already proven in `tests/router.context-prompt-integration.test.mjs`: import `saveCapsule`, `routeContextPrompt`, and only the Node filesystem helpers needed to publish a valid versioned compiled index under a temporary owned root. Build a table-driven adapter for every class in `CALIBRATION_CORPUS`. Translate corpus controls such as forced staleness, tied/ambiguous authority, terminal status, and dependency state into real capsule/index/authoritative inputs, then call `routeContextPrompt({ prompt, ownedRoot, projectRoot, now, ... })`. Construct a fresh normalized object whose enumerable fields are the observed `resolution.outcome` and `resolution.dispatch_eligible`; for a context-budget fixture, also set enumerable `context_within_budget` from `Buffer.byteLength(result.additional_context, 'utf8') <= fixture.max_context_bytes`. Attach the exact emitted string with `Object.defineProperty(normalized, 'additional_context', { value: result.additional_context, enumerable: false })` so `evaluateCalibrationCorpus` can measure real bytes while its exact `JSON.stringify(actual) === JSON.stringify(fixture.expected)` quality comparison sees only the expected quality fields. Never spread, return, or clone `fixture.expected`. Register cleanup for every temporary root. Do not change production routing or measurement code and do not remove the isolated unit tests in `tests/router.perf-calibration.test.mjs` that intentionally use synthetic callbacks to verify percentile and gate mechanics.</action>
  <verify>
    <automated>node --test tests/router.compiled-evolution.test.mjs</automated>
  </verify>
  <done>All seven versioned fixture classes execute through real compiled-index/capsule state and return observed router output suitable for quality, byte, and timing evaluation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove routing quality and exact emitted-context byte ceilings</name>
  <files>tests/router.compiled-evolution.test.mjs</files>
  <behavior>
    - `evaluateCalibrationCorpus` receives the real routing adapter for every fixed fixture instead of a callback that returns or clones `fixture.expected`.
    - Each fixture's `measured_context_bytes` equals `Buffer.byteLength(actual.additional_context, 'utf8')` and is compared with that fixture's `max_context_bytes`.
    - The integration assertion fails if routing outcome, dispatch eligibility, or emitted context budget regresses for any fixture class.
    - Assertions prove enumerable quality values originate from `routeContextPrompt` resolution fields and the context-budget boolean is derived from actual emitted UTF-8 bytes, while `additional_context` remains non-enumerable but directly readable by the evaluator.
  </behavior>
  <action>Replace the end-to-end quality shortcut with `evaluateCalibrationCorpus({ corpus: CALIBRATION_CORPUS, route: realRoute, versions })`. Before evaluation, call the adapter directly for representative fixtures and assert `Object.keys(actual)` contains only the observed quality fields required by that fixture, `Object.prototype.propertyIsEnumerable.call(actual, 'additional_context')` is false, and `actual.additional_context` is exactly the emitted string captured from the real router call. Assert outcome and dispatch eligibility equal the captured `routeContextPrompt(...).resolution` values; for the context-budget fixture, assert `context_within_budget` equals the result of comparing `Buffer.byteLength(captured.additional_context, 'utf8')` with `fixture.max_context_bytes`. Add a mutation-sentinel test showing that changing a fixture's expected object/value does not change the adapter result, proving no `fixture.expected` object or value is copied into the route output. Then assert overall quality and context-budget gates pass, every fixture reports its required outcome/eligibility, and each reported byte count equals `Buffer.byteLength` of the captured emitted string and does not exceed `max_context_bytes`. Include at least one non-ASCII emitted-context assertion so UTF-8 byte accounting cannot collapse to JavaScript string length. Keep the version tuple and corpus fingerprint assertions intact so D-15 evidence stays bound to the evaluated candidate/index/policy/corpus.</action>
  <verify>
    <automated>node --test tests/router.compiled-evolution.test.mjs tests/router.perf-calibration.test.mjs</automated>
  </verify>
  <done>The integration suite contains no expected-result routing shortcut: exact quality JSON contains only observed/derived enumerable fields, real emitted context remains non-enumerable and measurable, and quality plus exact UTF-8 context ceilings pass across the complete fixed corpus.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Measure real warm compiled-routing latency and enforce both hard ceilings</name>
  <files>tests/router.compiled-evolution.test.mjs</files>
  <behavior>
    - `measureRoutes` invokes the same real compiled-routing adapter used by quality evaluation rather than an empty callback.
    - The recorded warm p95 is strictly below 25 ms and the recorded maximum route duration is strictly below 100 ms.
    - The assessment records candidate, compiled-index, policy, and corpus versions alongside the measured values and keeps latency independent from quality.
  </behavior>
  <action>Replace the empty timing callback in the end-to-end calibration test with the real routing adapter. Perform setup before measurement so warm samples time prompt routing rather than fixture publication; retain explicit warmup and measured-run counts large enough to cover every fixture repeatedly without introducing sleeps or network work. Assert the raw `measured.warm.p95_ms < 25` and `measured.warm.max_ms < 100` values before passing them to `assessCalibration`, assert the assessment's latency and overall gates pass, and assert the result preserves the exact version tuple plus numeric measured values/baseline deltas. Keep the existing synthetic-clock percentile tests as unit coverage; this task supplies the missing real-path REL-01 evidence.</action>
  <verify>
    <automated>node --test tests/router.compiled-evolution.test.mjs tests/router.perf-calibration.test.mjs tests/router.context-prompt-integration.test.mjs</automated>
  </verify>
  <done>The real compiled prompt router is the measured callback, its recorded warm p95 is below 25 ms, every recorded route is below 100 ms, and independent quality/latency gates pass together.</done>
</task>

</tasks>

<verification>
Run `node --test tests/router.compiled-evolution.test.mjs tests/router.perf-calibration.test.mjs tests/router.context-prompt-integration.test.mjs`, then `node --test tests/*.test.mjs` at the wave boundary. Confirm the integration test no longer contains the two verifier-identified shortcut callbacks and that all temporary roots are removed.
</verification>

<success_criteria>
- F-05 is closed with recorded real-route warm p95 below 25 ms and max below 100 ms.
- F-06 is closed because every fixed-corpus outcome and dispatch decision comes from `routeContextPrompt`, and every context ceiling uses exact emitted `additional_context` UTF-8 bytes.
- REL-01 has end-to-end evidence while the production router and the synthetic unit tests for measurement mechanics remain unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/17-compiled-prompt-routing-and-safe-evolution/17-05-SUMMARY.md`.
</output>

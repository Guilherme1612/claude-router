---
phase: 17-compiled-prompt-routing-and-safe-evolution
plan: 04
type: execute
wave: 4
depends_on: ["17-01", "17-02", "17-03"]
gap_closure: true
files_modified:
  - src/prompt/compile-index.mjs
  - src/context/prompt-route.mjs
  - src/evolution/evidence.mjs
  - src/evolution/canary-controller.mjs
  - src/evolution/perf-measure.mjs
  - tests/router.compiled-index.test.mjs
  - tests/router.context-prompt-integration.test.mjs
  - tests/router.evolution-canary.test.mjs
  - tests/router.perf-calibration.test.mjs
  - tests/router.compiled-evolution.test.mjs
autonomous: true
requirements: [EVO-05, REL-01]
nyquist_compliant: true
must_haves:
  truths:
    - "F-01/D-01 through D-04: only strictly validated workflow route records from a compatible compact index can authorize dispatch, and deterministic capsule writes use the validated injected clock."
    - "F-04/D-01 through D-04: every compiled pointer, metadata, and payload read is bound to one regular-file descriptor inside the owned immutable publication boundary, including under path-swap attacks."
    - "F-02/EVO-05/D-07 through D-10: aggregate evidence requires explicit eligibility at read and write, and only an authentic frozen evidence window bound to the candidate source fingerprint can authorize promotion."
    - "F-03/D-13 through D-16: the context-budget gate measures exact serialized or injected output bytes against each fixture limit rather than trusting candidate-reported status."
  artifacts:
    - path: src/prompt/compile-index.mjs
      provides: "Strict compiled projection schemas and descriptor-bound bounded JSON reads"
    - path: src/evolution/evidence.mjs
      provides: "Eligible, scoped, content-addressed, immutable evidence-window envelopes"
    - path: src/evolution/canary-controller.mjs
      provides: "Candidate-bound evidence provenance and invariant validation before promotion"
    - path: src/evolution/perf-measure.mjs
      provides: "Measured context-byte evaluation for every calibration fixture"
    - path: tests/router.compiled-evolution.test.mjs
      provides: "End-to-end regression evidence for hardened compiled routing and evolution"
  key_links:
    - from: src/prompt/compile-index.mjs
      to: src/context/prompt-route.mjs
      via: "validated route projection keyed by matching workflow_id before dispatch"
    - from: src/evolution/evidence.mjs
      to: src/evolution/canary-controller.mjs
      via: "frozen evidence-window fingerprint bound to candidate source_evidence_fingerprint"
    - from: src/evolution/perf-measure.mjs
      to: tests/router.perf-calibration.test.mjs
      via: "byte measurement compared with fixture max_context_bytes"
---

<objective>
Close every Phase 17 verifier gap by making compiled route authority inode-bound and schema-strict, making canary evidence eligible and provenance-bound, and measuring actual context bytes while preserving deterministic clock behavior.

Purpose: Restore the phase goal that compiled routing and privacy-safe evolution cannot silently authorize malformed state, escaped files, fabricated evidence, or oversized context.
Output: Hardened routing/evolution seams plus adversarial regression coverage for F-01 through F-04 and the clock-injection warning.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-compiled-prompt-routing-and-safe-evolution/17-CONTEXT.md
@.planning/phases/17-compiled-prompt-routing-and-safe-evolution/17-RESEARCH.md
@.planning/phases/17-compiled-prompt-routing-and-safe-evolution/17-VERIFICATION.md
@.planning/phases/17-compiled-prompt-routing-and-safe-evolution/17-01-SUMMARY.md
@.planning/phases/17-compiled-prompt-routing-and-safe-evolution/17-02-SUMMARY.md
@.planning/phases/17-compiled-prompt-routing-and-safe-evolution/17-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bind compiled reads to owned regular files and validate every route projection</name>
  <files>src/prompt/compile-index.mjs, src/context/prompt-route.mjs, tests/router.compiled-index.test.mjs, tests/router.context-prompt-integration.test.mjs</files>
  <read_first>
    <file>src/prompt/compile-index.mjs</file>
    <file>src/context/prompt-route.mjs</file>
    <file>tests/router.compiled-index.test.mjs</file>
    <file>tests/router.context-prompt-integration.test.mjs</file>
    <file>.planning/phases/17-compiled-prompt-routing-and-safe-evolution/17-VERIFICATION.md</file>
  </read_first>
  <behavior>
    - A valid-hash index containing an empty, non-object, unknown-field, unbounded, missing-field, or key/workflow mismatch route record is rejected before it becomes dispatch authority.
    - Pointer, known-good, metadata, and payload reads open once with no-follow semantics, verify a bounded regular file with fstat, read that same descriptor, and close it in finally; a path swapped to a symlink cannot escape the owned root.
    - Refresh and explicit-override capsule persistence use the validated routeContextPrompt now value and reject non-finite or unsafe timestamps consistently.
  </behavior>
  <action>Write the malformed-projection matrix, descriptor/symlink-swap adversarial cases, and deterministic clock assertions first. Replace the separate path inspection/read sequence with a bounded descriptor-based helper using Node standard-library open/read/fstat/close primitives and no-follow support; keep injected filesystem operations testable and preserve the existing byte limits and bounded known-good fallback. Define an explicit allowlist schema for every route entry, require bounded workflow_id, transition_id, and reason_code tokens, require the map key to equal workflow_id, reject arrays and prototype-sensitive/unknown fields, and validate all entries before returning ready. Thread the already validated now argument into refreshedCapsule and overrideCapsule so persisted freshness is reproducible per D-01 through D-04; retain fail-closed diagnostics and do not add inventory, registry-build, directory-enumeration, or external-model work to the hot path.</action>
  <verify>
    <automated>node --test tests/router.compiled-index.test.mjs tests/router.context-prompt-integration.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - F-01 reproductions return non-dispatchable results for every malformed route-record class, including an empty object with a correct payload hash.
    - F-04 path-swap and symlink fixtures cannot make bytes outside ownedRoot eligible, and every opened descriptor is closed on success and failure.
    - Refresh and override fixtures persist captured_at equal to the injected now value.
    - Existing active/known-good recovery, compact-read limits, and forbidden slow-path tests remain green.
  </acceptance_criteria>
  <done>Compiled dispatch authority comes only from strict route schemas read from the same verified in-boundary inode, and capsule timestamps are deterministic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Authorize aggregate reads and bind immutable evidence windows to candidates</name>
  <files>src/evolution/evidence.mjs, src/evolution/canary-controller.mjs, tests/router.evolution-canary.test.mjs</files>
  <read_first>
    <file>src/evolution/evidence.mjs</file>
    <file>src/evolution/canary-controller.mjs</file>
    <file>tests/router.evolution-canary.test.mjs</file>
    <file>.planning/phases/17-compiled-prompt-routing-and-safe-evolution/17-CONTEXT.md</file>
    <file>.planning/phases/17-compiled-prompt-routing-and-safe-evolution/17-VERIFICATION.md</file>
  </read_first>
  <behavior>
    - Aggregate windows require aggregate_eligible true on read as well as append; project windows remain isolated by project_id.
    - The store emits a deeply frozen content-addressed window whose fingerprint covers scope, eligible observation fingerprints, counts, weighting policy, minimum threshold, and source evidence identity.
    - Candidate evaluation rejects fabricated, mutated, unrelated, zero-count, negative/non-finite weighted, count-inconsistent, scope-invalid, or fingerprint-mismatched windows before evaluating hard gates.
  </behavior>
  <action>Write authorization, cross-project/cross-scope, fabricated-window, mutation, count-boundary, numeric-invariant, and candidate-fingerprint mismatch tests first. Reuse the same scopeFor authorization path for append and window so D-07 aggregate eligibility is symmetric. Produce a canonical frozen evidence-window envelope from validated retained records, include a stable SHA-256 fingerprint and an explicit source-evidence fingerprint suitable for proposeCandidate, and bound all values and arrays. Harden evaluateCandidate to recompute and verify window integrity, require source_evidence_fingerprint equality with the immutable candidate, require sample_count to equal observation count, require finite non-negative weighted_samples within the sample bound, and require sufficient to agree with minimum_samples before D-10 hard gates run. Preserve D-05/D-06 deny-before-hash behavior, retention/decay, the 30-sample floor, independent hard gates, and existing journaled promotion/rollback authority.</action>
  <verify>
    <automated>node --test tests/router.evolution-canary.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - F-02 aggregate reads without explicit eligibility are denied and never expose stored observations.
    - The verifier's fabricated window with sample_count zero and negative weighted_samples is rejected and cannot be promotable.
    - A valid authentic window promotes only when its fingerprint equals candidate.source_evidence_fingerprint and every independent gate passes.
    - Content-free envelope validation, privacy-denied zero-write behavior, scope isolation, decay, and rollback tests remain green.
  </acceptance_criteria>
  <done>Only eligible, authentic, internally consistent evidence from the candidate's declared source can reach promotion evaluation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Measure exact context bytes and prove all gap closures end to end</name>
  <files>src/evolution/perf-measure.mjs, tests/router.perf-calibration.test.mjs, tests/router.compiled-evolution.test.mjs</files>
  <read_first>
    <file>src/evolution/perf-measure.mjs</file>
    <file>tests/router.perf-calibration.test.mjs</file>
    <file>tests/router.compiled-evolution.test.mjs</file>
    <file>tests/router.workflow-orchestrator.test.mjs</file>
    <file>.planning/phases/17-compiled-prompt-routing-and-safe-evolution/17-VERIFICATION.md</file>
  </read_first>
  <behavior>
    - Calibration derives exact UTF-8 bytes from the routed serialized or injected context and compares that measurement with each fixture max_context_bytes.
    - A candidate-returned context_within_budget flag cannot override measured bytes; a 100,000-character output fails the context-budget gate.
    - End-to-end fixtures exercise malformed compiled records, descriptor-safe loading, eligible candidate-bound evidence, actual-byte budget failure, deterministic clocks, and successful known-good rollback without weakening REL-01 ceilings.
  </behavior>
  <action>Write the oversized self-attestation adversarial test and boundary/encoding cases first. Extend evaluateCalibrationCorpus to extract the actual routed context from the established route result contract, serialize deterministically where needed, measure Buffer.byteLength UTF-8 bytes, record measured and maximum bytes per fixture, and derive the independent context_budget verdict only from that comparison; malformed or missing measurable output must fail closed. Update the integrated lifecycle fixture to consume the authentic evidence-window contract from Task 2 and cover all F-01 through F-04 repairs plus deterministic now propagation. Rerun the focused Phase 17 suites and the entire workspace suite, preserving the fixed corpus fingerprint or deliberately updating its version and locked fingerprint only if fixture bytes themselves change; keep quality, budget, p95 under 25 ms, and maximum under 100 ms as independent hard gates per D-13 through D-16 and REL-01.</action>
  <verify>
    <automated>node --test tests/router.perf-calibration.test.mjs tests/router.compiled-evolution.test.mjs tests/router.compiled-index.test.mjs tests/router.evolution-canary.test.mjs tests/router.context-prompt-integration.test.mjs tests/router.workflow-orchestrator.test.mjs &amp;&amp; node --test tests/*.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - F-03's 100,000-character output fails context_budget even when candidate metadata claims it is within budget.
    - Exact-boundary and multi-byte UTF-8 fixtures report reproducible measured_context_bytes and enforce their declared max_context_bytes.
    - The end-to-end suite proves all four critical verifier gaps and the clock warning are closed through public seams.
    - EVO-05 and REL-01 focused suites plus the complete workspace test suite pass without a compensating score or relaxed latency ceiling.
  </acceptance_criteria>
  <done>Context budgets are measured rather than self-attested, and integrated regression evidence proves every Phase 17 verification gap is closed.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- `src/prompt/compile-index.mjs`: descriptor-bound bounded reads and strict route projection validation.
- `src/context/prompt-route.mjs`: deterministic validated-clock persistence for refresh and override capsules.
- `src/evolution/evidence.mjs`: symmetric aggregate authorization and immutable content-addressed evidence windows.
- `src/evolution/canary-controller.mjs`: evidence provenance, schema, scope, count, and numeric-integrity enforcement.
- `src/evolution/perf-measure.mjs`: exact UTF-8 context-byte measurement and independent budget verdicts.
- Phase 17 test suites: adversarial regression coverage for F-01, F-02, F-03, F-04, and clock injection drift.
</artifacts_this_phase_produces>

<source_coverage_audit>

| Source | ID | Required outcome | Coverage | Status / justification |
|---|---|---|---|---|
| GOAL | Phase 17 | Fast compiled routing improves from privacy-safe telemetry without latency or silent-regression risk | 17-01 through 17-03 plus Tasks 1-3 | COVERED — completed plans provide the architecture; this gap plan repairs every verifier-disproved authority boundary. |
| REQ | EVO-05 | Privacy-safe canary evolution with automatic rollback | Tasks 2-3 | COVERED — authentic eligible evidence is required before the already-shipped promotion/rollback path. |
| REQ | REL-01 | Warm p95 below 25 ms and every route below 100 ms | Task 3 | COVERED — existing ceilings remain independent and are rerun with measured budget gates. |
| RESEARCH | Compiled hot path | Compact exact-address state, verified known-good fallback, no slow-path rebuild | Task 1 | COVERED — descriptor-safe reads and strict schemas harden rather than replace the researched approach. |
| RESEARCH | Privacy/evolution | Content-free scoped evidence, immutable candidates, independent hard gates | Task 2 | COVERED — symmetric authorization and candidate binding close the missing provenance boundary. |
| RESEARCH | Calibration | Fixed seven-class corpus, reproducible versions, independent quality/budget/latency gates | Task 3 | COVERED — actual UTF-8 byte measurement replaces self-attestation. |
| CONTEXT | D-01–D-04 | Compact compatible compiled-index authority and fail-closed behavior | Task 1 | COVERED. |
| CONTEXT | D-05–D-06 | Content-free privacy boundary | Task 2 regression suite | COVERED — shipped validation is preserved and rerun; no verifier gap requires redesign. |
| CONTEXT | D-07–D-10 | Eligible scoped evidence, bounded windows, immutable bound candidates, all hard gates | Task 2 | COVERED. |
| CONTEXT | D-11–D-12 | Immediate rejection/rollback through durable atomic publication | Tasks 2-3 regression suite | COVERED — shipped publication authority is preserved and exercised with authentic evidence. |
| CONTEXT | D-13–D-16 | Seven-class correctness, actual budgets, independent latency, demonstrated benefit | Task 3 | COVERED. |
| VERIFICATION | F-01 | Malformed route records authorize dispatch | Task 1 | COVERED. |
| VERIFICATION | F-02 | Unauthorized/fabricated evidence authorizes promotion | Task 2 | COVERED. |
| VERIFICATION | F-03 | Candidate self-attests context budget | Task 3 | COVERED. |
| VERIFICATION | F-04 | Path swap escapes publication boundary | Task 1 | COVERED. |
| VERIFICATION | Clock drift | Refresh/override ignores injected now | Task 1 | COVERED. |

No source item is deferred or omitted. Existing completed Plans 17-01 through 17-03 remain the implementation record for source items the verifier confirmed; 17-04 changes only the uncovered behavior.

</source_coverage_audit>

<verification>
Run each task's focused command, then the combined Phase 17 command and `node --test tests/*.test.mjs`. Passing execution evidence must be followed by the independent phase verifier; this plan does not itself declare Phase 17 complete.
</verification>

<success_criteria>
- All four critical entries in `17-VERIFICATION.md` have deterministic adversarial regression tests and passing implementations.
- The non-critical injected-clock gap is closed for refresh and explicit override persistence.
- EVO-05 permits promotion only from explicitly eligible, authentic candidate-bound evidence and retains automatic rollback.
- REL-01 remains satisfied with independent semantic, measured-context-budget, warm-p95, and maximum-route gates.
- D-01 through D-16 remain honored, and completed Plans 17-01 through 17-03 are unchanged.
</success_criteria>

---
phase: 50-portable-runtime-local-contracts-and-fixture-boundary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/helpers/inventory-fixture.mjs
  - tests/fixtures/v1.8/empty-claude.json
  - tests/fixtures/v1.8/minimal-codex.json
  - tests/fixtures/v1.8/asymmetric-runtimes.json
  - tests/fixtures/v1.8/conflicting-invalid.json
  - tests/router.v18-contracts.test.mjs
autonomous: true
requirements: [CVRG-01, CVRG-05, CVRG-06]
must_haves:
  truths:
    - "Synthetic Claude and Codex roots are materialized under a test-owned temporary directory and passed explicitly to runtime discovery."
    - "Skills, agents, commands, tools, hooks, and workflows plus empty, minimal, asymmetric, conflicting, invalid, stale, renamed, project-scoped, plugin-heavy, and unknown-future inventory shapes are representable without reading a live runtime home."
    - "Claude and Codex fixture inventories are asserted independently; their record counts and native mechanics are not required to match."
    - "Fixture output and adapter provenance use symbolic logical roots plus relative paths and contain no private absolute path."
  prohibitions:
    - "Default tests MUST NOT read HOME, CODEX_HOME, ~/.claude, ~/.codex, or serialize live installed inventory content."
    - "Fixtures MUST NOT encode the maintainer username, workspace path, or real capability names."
  artifacts:
    - tests/helpers/inventory-fixture.mjs
    - tests/fixtures/v1.8/empty-claude.json
    - tests/fixtures/v1.8/minimal-codex.json
    - tests/fixtures/v1.8/asymmetric-runtimes.json
    - tests/fixtures/v1.8/conflicting-invalid.json
    - tests/router.v18-contracts.test.mjs
  key_links:
    - "JSON scenario -> materializeRuntimeInventoryScenario() -> explicit Claude/Codex roots -> adapter discovery"
    - "Discovered provenance -> logical_root + relative_path -> privacy assertions"
---

<objective>
Create the portable test boundary for v1.8: anonymous declarative inventory scenarios, one reusable temporary-root materializer, and passing runtime-local discovery/isolation assertions.

Purpose: Make all later routing, parity, and evaluation work reproducible without depending on the maintainer's installed Claude or Codex homes.
Output: Four JSON scenarios, one additive fixture helper export, and the initial portable contract test file.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/50-portable-runtime-local-contracts-and-fixture-boundary/50-CONTEXT.md
@.planning/phases/50-portable-runtime-local-contracts-and-fixture-boundary/50-RESEARCH.md
@.planning/phases/50-portable-runtime-local-contracts-and-fixture-boundary/50-PATTERNS.md
@.planning/phases/50-portable-runtime-local-contracts-and-fixture-boundary/50-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add anonymous declarative runtime inventory scenarios</name>
  <files>tests/fixtures/v1.8/empty-claude.json, tests/fixtures/v1.8/minimal-codex.json, tests/fixtures/v1.8/asymmetric-runtimes.json, tests/fixtures/v1.8/conflicting-invalid.json</files>
  <read_first>
    - tests/helpers/inventory-fixture.mjs (existing anonymous record conventions and synthetic root names)
    - src/adapters/claude.mjs (supported Claude layouts and artifact formats)
    - src/adapters/codex.mjs (supported Codex layouts and reused adapter factory)
    - tests/router.registry-build.test.mjs (existing temporary inventory setup and assertions)
  </read_first>
  <action>
    Create four UTF-8 JSON scenarios with a shared bounded shape: top-level `claude` and `codex` objects, each containing a `files` array of `{path, content}` entries and optional project-root entries. Use anonymous capability IDs only. Cover: no Claude files; one Codex skill; asymmetric runtime counts with renamed and runtime-only artifacts; and conflicting/invalid metadata with stale evidence, a project-scoped artifact, plugin-heavy artifacts, and an unknown-future artifact/layout. Across the four scenarios include discoverable examples of every required kind: skill, agent, command, tool, hook, and workflow. All paths must be relative, use `/`, remain under the synthetic root, and match layouts already accepted by the adapters except the intentional unknown-future case. Do not include absolute paths, environment variables, real usernames, or installed capability content.
  </action>
  <acceptance_criteria>
    - All four files parse with `JSON.parse`.
    - Every scenario has `claude.files` and `codex.files` arrays.
    - Every file path is relative, contains no `..`, and contains no `/Users/` or maintainer name.
    - The scenarios collectively contain skill, agent, command, tool, hook, workflow, empty, minimal, renamed, asymmetric, conflicting, invalid, stale, project-scoped, plugin-heavy, and unknown-future cases.
  </acceptance_criteria>
  <verify>
    <automated>rtk node -e "const fs=require('node:fs'); for (const f of fs.readdirSync('tests/fixtures/v1.8')) JSON.parse(fs.readFileSync('tests/fixtures/v1.8/'+f,'utf8')); console.log('v1.8 fixtures valid')"</automated>
  </verify>
  <done>All declarative scenarios parse and contain only anonymous portable paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Materialize isolated roots and prove runtime-local discovery</name>
  <files>tests/helpers/inventory-fixture.mjs, tests/router.v18-contracts.test.mjs</files>
  <read_first>
    - tests/helpers/inventory-fixture.mjs (extend this helper; do not create a second fixture framework)
    - tests/fixtures/v1.8/empty-claude.json (shared scenario shape)
    - tests/fixtures/v1.8/minimal-codex.json (minimal discovery case)
    - tests/fixtures/v1.8/asymmetric-runtimes.json (independent runtime parity case)
    - tests/fixtures/v1.8/conflicting-invalid.json (malformed and project/plugin layouts)
    - src/adapters/claude.mjs (discoverRoots option names and portable provenance output)
    - src/adapters/codex.mjs (discoverRoots option names and Codex logical roots)
    - tests/router.registry-build.test.mjs (node:test temporary-directory cleanup pattern)
    - tests/router.contracts.test.mjs (primary top-level node:test contract-loop analog)
    - tests/router.phase26-dual-runtime.test.mjs (isolated dual-runtime root and cleanup analog)
    - tests/router.context-sources.test.mjs (privacy-negative assertion analog)
  </read_first>
  <action>
    Add one exported async helper `materializeRuntimeInventoryScenario(scenarioOrPath)` to `tests/helpers/inventory-fixture.mjs` using Node standard-library `mkdtemp`, `mkdir`, `writeFile`, `readFile`, and `rm`. Validate the scenario trust boundary before writing: objects only, arrays only, relative normalized paths only, no traversal, and string content only. Return explicit `claudeRoot`, `codexRoot`, optional project roots, and an idempotent `cleanup()` function. Keep existing exports unchanged.

    Create `tests/router.v18-contracts.test.mjs` using the top-level `test()` and strict-assertion style from `tests/router.contracts.test.mjs`, the isolated dual-root setup from `tests/router.phase26-dual-runtime.test.mjs`, and privacy-negative assertions from `tests/router.context-sources.test.mjs`. Load each JSON scenario, materialize it, call each adapter with explicit roots, and clean up in `t.after()`. Assert the empty Claude case discovers zero Claude artifacts while the minimal Codex case discovers its anonymous skill; the union of scenarios exercises skill, agent, command, tool, hook, and workflow records; stale and unknown-future records remain visible diagnostics/records rather than disappearing; asymmetric inventories are checked independently rather than for equal counts; every emitted provenance locator has a symbolic `logical_root`, non-absolute `relative_path`, and no traversal; serialized observations contain neither the process home nor the real workspace path. Add a poisoned-home guard by setting adapter options explicitly and placing a recognizable outside artifact in a separate directory that must never be discovered. Do not mutate process-global HOME/CODEX_HOME if explicit adapter options can prove the boundary.
  </action>
  <acceptance_criteria>
    - `materializeRuntimeInventoryScenario` rejects absolute and traversal paths before writing.
    - Every scenario is created below one test-owned temporary parent and cleanup removes it safely.
    - Claude and Codex discovery receive explicit synthetic roots and never discover the outside sentinel artifact.
    - The scenario suite observes all six required capability kinds and retains stale and unknown-future surfaces for later classification.
    - Provenance is portable and serialized test output contains no live home or workspace path.
    - Existing `tests/helpers/inventory-fixture.mjs` exports and callers remain compatible.
  </acceptance_criteria>
  <verify>
    <automated>rtk node --test tests/router.v18-contracts.test.mjs tests/router.registry-build.test.mjs</automated>
  </verify>
  <done>The portable discovery/isolation suite passes for all four anonymous scenarios without live-home reads.</done>
</task>

</tasks>

<threat_model>
## Phase 50 Plan 01 Threat Model — ASVS Level 1

| Threat ID | Severity | Threat | Mitigation | Automated proof |
|---|---|---|---|---|
| T-50-01 | high | A fixture path escapes its temporary root or adapter discovery falls through to a live home, leaking private inventory | Validate and resolve every relative path within the temporary root; pass roots explicitly; assert an outside sentinel is absent | `tests/router.v18-contracts.test.mjs` traversal rejection, sentinel exclusion, and serialized-path checks |
| T-50-03 | medium | Runtime parity is incorrectly implemented as equal inventory counts | Assert each runtime's discovered set against its own scenario declaration | asymmetric runtime test |

No packages are installed and no database schema is changed.
</threat_model>

<verification>
- `rtk node --test tests/router.v18-contracts.test.mjs tests/router.registry-build.test.mjs` exits 0.
- `rtk git diff --check` exits 0.
- `rtk rg -n '/Users/|guilherme|\.claude|\.codex' tests/fixtures/v1.8` returns no private-home match.
</verification>

<success_criteria>
- Anonymous fixture scenarios cover all six capability kinds and every locked CVRG-06 installation shape, including stale and unknown-future.
- Explicit-root Claude and Codex discovery is independently verified.
- No live runtime home content or private absolute path enters committed fixtures or test output.
</success_criteria>

<output>
Create `.planning/phases/50-portable-runtime-local-contracts-and-fixture-boundary/50-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `tests/helpers/inventory-fixture.mjs`: `materializeRuntimeInventoryScenario(scenarioOrPath)` returning explicit runtime roots and `cleanup()`.
- `tests/fixtures/v1.8/empty-claude.json`.
- `tests/fixtures/v1.8/minimal-codex.json`.
- `tests/fixtures/v1.8/asymmetric-runtimes.json`.
- `tests/fixtures/v1.8/conflicting-invalid.json`.
- `tests/router.v18-contracts.test.mjs`: portable runtime-local discovery and privacy contract suite.

---
phase: 44-receipts-and-causal-attribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/adapters/dispatch/contract.mjs
  - src/adapters/dispatch/receipt.mjs
  - tests/phase-44/receipts.test.mjs
autonomous: true
requirements: [RCPT-01, RCPT-02, RCPT-03, RCPT-04, RCPT-05]
must_haves:
  truths:
    - "A stable structured identity can be durably published as pending before native invocation and reused for later receipt transitions."
    - "Receipt states cover ignored, rejected, substituted, blocked, partial, failed, completed, and preserved-unknown outcomes without accepting arbitrary states."
    - "Compact inspection exposes bounded attribution fields and never returns raw prompt text, secrets, environment, or file contents."
    - "Outcome credit is true only when invocation evidence and verified postcondition evidence reference the same receipt."
  artifacts:
    - path: src/adapters/dispatch/receipt.mjs
      provides: "Stable receipt identity, bounded attribution helpers, causal outcome-credit predicate, and compact inspection"
      exports: [receiptIdentityId, buildPendingReceipt, transitionReceipt, outcomeCredit, inspectReceipt]
    - path: src/adapters/dispatch/contract.mjs
      provides: "Complete shared receipt state vocabulary"
    - path: tests/phase-44/receipts.test.mjs
      provides: "Failing-first contract coverage for RCPT-01..05"
  key_links:
    - from: src/adapters/dispatch/receipt.mjs
      to: existing ReceiptStore
      via: "pending and transition records use existing atomic publish/append persistence"
---

<objective>
Extend the existing stdlib-only receipt seam into a stable, bounded attribution contract. Keep the existing store and adapter compatibility while making pre-invocation identity, terminal-state completeness, compact inspection, and causal outcome credit executable.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/44-receipts-and-causal-attribution/44-CONTEXT.md
@.planning/phases/44-receipts-and-causal-attribution/44-RESEARCH.md
@.planning/phases/44-receipts-and-causal-attribution/44-PATTERNS.md
@src/adapters/dispatch/receipt.mjs
@src/adapters/dispatch/contract.mjs
</execution_context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define the Phase 44 receipt contract through failing tests</name>
  <files>tests/phase-44/receipts.test.mjs</files>
  <behavior>
    - Test 1: stable structured identity produces the same receipt id before and after PID assignment.
    - Test 2: pending receipt contains project, goal, route, action, mapping generation, capability fingerprint, authority, risk, and idempotency identities.
    - Test 3: valid transitions preserve identity and reject arbitrary states.
    - Test 4: compact inspection includes selected/actual divergence, alternatives, rejection reasons, corrections, substitutions, bounded evidence, and resource facts without raw prompt text.
    - Test 5: only matching invocation plus verified postcondition evidence receives outcome credit; exit zero, ignored, unrelated, or unverified evidence does not.
  </behavior>
  <action>Write the smallest built-in `node:test` suite against the planned receipt exports. Use structured fixtures and assert the missing exports/behavior fail for the intended reason before production changes. Do not place secrets or raw prompt samples in persisted fixtures.</action>
  <verify><automated>node --test tests/phase-44/receipts.test.mjs</automated></verify>
  <done>The new tests fail because the Phase 44 receipt contract is not implemented, not because of test syntax or fixture errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement stable identity, complete states, inspection, and causal credit</name>
  <files>src/adapters/dispatch/contract.mjs, src/adapters/dispatch/receipt.mjs</files>
  <action>Use the existing `ReceiptStore`, atomic publish, redaction, and hashing helpers. Add the required terminal route states to the shared vocabulary. Implement deterministic structured identity hashing independent of PID, a pending-receipt constructor with bounded identity/selection/evidence fields, a transition helper that preserves the receipt id and rejects unknown states, a causal outcome-credit predicate requiring matching receipt ids plus verified postcondition evidence, and a compact inspection projection that whitelists identifiers, selected-versus-actual divergence, alternatives, decisive reasons, bounded evidence, timings, token classes, calls, retries, failures, and coordination cost. Sanitize all string fields through the existing redaction boundary and omit raw prompt/environment/content fields. Preserve existing schema fields and APIs so Phase 38 tests continue to pass.</action>
  <verify><automated>node --test tests/phase-44/receipts.test.mjs</automated></verify>
  <done>RCPT-01 through RCPT-05 contract tests pass and the helper layer remains stdlib-only, deterministic, bounded, and fail-open for persistence.</done>
</task>
</tasks>

<verification>
- `node --test tests/phase-44/receipts.test.mjs`
- Existing Phase 38 adapter tests remain green after the additive schema changes.
</verification>

<success_criteria>
The receipt layer can durably anchor a route before invocation, represent every terminal route state, expose a bounded compact view, and deny causal credit without linked verified postconditions.
</success_criteria>

<output>
Create `.planning/phases/44-receipts-and-causal-attribution/44-01-SUMMARY.md` when done.
</output>

---
phase: 31-runtime-tagging
plan: 03
type: tdd
wave: 2
depends_on: [31-01, 31-02]
files_modified:
  - tests/router.mjs.snapshot
  - ~/.claude/hooks/router.mjs
  - src/health/outcome-schema.mjs
  - src/health/observe.mjs
  - src/registry/watcher.mjs
autonomous: true
requirements: [PARITY-02]
user_setup: []
estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - Every cache key folds the runtime as key IDENTITY so a Claude-served route is never returned to a Codex session and vice versa (D-05, PARITY-02).
    - Every telemetry record carries a runtime field (D-06), appended to the existing record, and telemetry remains append-only JSONL.
    - The runtime/epoch fields land in the evidence schema via a deliberate OUTCOME_FIELDS policy-version bump (size 14 → 16) that updates BOTH outcome-schema.mjs AND its 16-field enforcement test in the same change — never a silent schema add (ROADMAP criterion 3).
  artifacts:
    - tests/router.mjs.snapshot (cacheKey runtime slot; telemetryEntryFromState runtime field)
    - ~/.claude/hooks/router.mjs (lockstep mirror, byte-identical)
    - src/health/outcome-schema.mjs (OUTCOME_FIELDS 14 → 16: +runtime, +epoch)
    - src/registry/watcher.mjs (runtime/epoch threaded through telemetryRecordToEvidence if it allowlists/maps raw fields)
  key_links:
    - cacheKey runtime param lands in the key tuple (identity), not the route metadata — otherwise the LRU still cross-mixes runtimes.
    - OUTCOME_FIELDS bump is atomic with its enforcement test — alone either side is RED.
    - Snapshot and live hook stay byte-identical (mirror guard stays GREEN).
---

<objective>
Deepen the probe: fold the runtime into cache-key identity and telemetry records, and land the runtime/epoch fields in the evidence schema through a deliberate policy-version bump. This completes PARITY-02 (cache isolation + telemetry tagging) and satisfies ROADMAP success criterion 3 (runtime/epoch via intentional bump).

Purpose: Prevent cross-runtime cache reuse and enable shadow-log/per-install correlation. The 31-01 RED tests for cacheKey divergence, telemetry runtime, and the 16-field schema are turned GREEN here.

Output: Runtime is cache-key identity, a telemetry field, and a deliberately-bumped evidence-schema field.

Deferred note (decision-coverage): D-07 (resolve evaluation considers only active runtime's capabilities; only active runtime's suggestion injected into additionalContext) and D-08 (cross-runtime capability-equivalent fixture reusing phase-26 provenance[{runtime}]) are DEFERRED to Phase 32 — the resolve-first capability-resolution path does not exist until Phase 32 (PARITY-03/04 → ROUTE-*). This plan ships the cache/telemetry tagging foundation (PARITY-02) only; capability-resolution per runtime and the cross-runtime fixture land with the resolve layer. Referenced for coverage, not dropped.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-runtime-tagging/31-CONTEXT.md
@.planning/phases/31-runtime-tagging/31-RESEARCH.md

# Reference files (read once):
@tests/router.mjs.snapshot  # cacheKey():1692-1700; call site:2788; telemetryEntryFromState():2554-2585; telemetry write:2664
@src/health/outcome-schema.mjs  # OUTCOME_FIELDS:33-38 (14 now)
@tests/router.health.outcome-schema.test.mjs  # size/membership test:68-74 (already bumped to 16 in 31-01 — correct RED target)
@tests/router.cache.test.mjs  # cross-runtime divergence test (added in 31-01)
@src/registry/watcher.mjs  # telemetryRecordToEvidence mapping site (documented in 31-02 SUMMARY)
</context>

## Artifacts this phase produces

| Kind | Artifact | What it is |
|------|----------|------------|
| Updated canonical hook | `tests/router.mjs.snapshot` | `cacheKey` runtime slot (D-05 key identity, pushed into the hashed `parts` tuple); `telemetryEntryFromState` `runtime` field (D-06); production cacheKey call site (:2788) passes the module `RUNTIME` constant. |
| Updated live mirror | `~/.claude/hooks/router.mjs` | Byte-identical lockstep copy of the canonical hook (mirror guard stays GREEN). |
| Updated schema | `src/health/outcome-schema.mjs` | Frozen `OUTCOME_FIELDS` 14 → 16 (+`runtime`, +`epoch`) with an intentional policy-version bump comment (ROADMAP criterion 3). |
| Updated policy version | `src/health/observe.mjs` | `HEALTH_POLICY_VERSION` `'health-policy-v1'` → `'health-policy-v2'` at :40 (evidence emission sites :86/:232). |
| Updated ingest | `src/registry/watcher.mjs` | `runtime`/`epoch` threaded through `telemetryRecordToEvidence` at the site documented in 31-02, so the runtime tag survives the 16-field allowlist. |
| Updated test (conditional) | `tests/router.health.canary.test.mjs` | `POLICY_VERSION` assertion (:49) updated in lockstep IF the version string surfaces there (see Task 3 action); it is now in the Task 3 verify. |
| Tests turned GREEN | `tests/router.health.outcome-schema.test.mjs`, `tests/router.cache.test.mjs`, `tests/router.runtime-tagging.test.mjs` | The 31-01 RED surfaces (16-field schema, cross-runtime cacheKey divergence, telemetry runtime field) all pass. |

<tasks>

<task type="tdd">
  <name>Task 1: Fold runtime into the cache key as identity</name>
  <files>tests/router.mjs.snapshot, ~/.claude/hooks/router.mjs</files>
  <read_first>tests/router.mjs.snapshot</read_first>
  <behavior>
    - Test 1: cacheKey('fix bug', ['fix'], 'fp', '', '', '', 'claude') !== cacheKey('fix bug', ['fix'], 'fp', '', '', '', 'codex') — runtime is key identity.
    - Test 2: cacheKey('fix bug', ['fix'], 'fp', '', '', '', 'claude') === cacheKey('fix bug', ['fix'], 'fp', '', '', '', 'claude') — deterministic within a runtime.
    - Test 3: existing 6-arg and fewer-arg cacheKey calls (tests/router.cache.test.mjs:23-56) still produce the same keys as before the runtime slot (default handled) — no regression.
  </behavior>
  <action>
    In tests/router.mjs.snapshot, extend `cacheKey(normalizedPrompt, intentKeywords, manifestFingerprint = '0', modeMapHash = '', weightsHash = '', graphHash = '')` (snapshot:1692-1700) to fold the runtime as KEY IDENTITY, per D-05 and RESEARCH Pitfall 3:

    - Add a `runtime` parameter (append it as the last param, e.g. `runtime = ''` or defaulting to the module `RUNTIME` constant for the production call site). Per Claude's Discretion the placement is documented and consistent; the RESEARCH example names a `runtime` slot appended to the `parts` array (tuple becomes `[np, ik, manifestFingerprint, runtime, live]`).
    - Push `String(runtime || '')` into the `parts` array ALWAYS (not conditionally), so runtime is part of the hashed identity and two runtimes can never hash to the same key even when `live` is empty. This is the anti-pattern guard from RESEARCH Pitfall 3 / 173-175: runtime MUST be in the hashed input tuple, not route metadata.
    - Do not change the existing `parts`/`live` composition for the non-runtime inputs (manifestFingerprint epoch, live hashes) — the Phase-30 fingerprint epoch slot stays.
    - Update the production call site (snapshot:2788) `cacheKey(state.normalizedPrompt, [], manifestFingerprint, modeMapHash, weightsHash, graphHash)` to pass the module `RUNTIME` constant as the runtime argument.

    After editing, mirror the SAME bytes into ~/.claude/hooks/router.mjs (byte-identical; 31-01 mirror guard must stay GREEN; tests import the live hook).
  </action>
  <verify>
    <automated>rtk node --test tests/router.cache.test.mjs tests/router.runtime-tagging.test.mjs 2>&1 | grep -E "cache|runtime|pass|fail" | head -20</automated>
  </verify>
  <acceptance_criteria>
    The 31-01 cross-runtime divergence test is GREEN: same prompt+manifest under 'codex' and 'claude' produce different cacheKey hashes; same runtime is deterministic. The existing 6-arg cacheKey tests (router.cache.test.mjs:23-56) still pass — the runtime parameter is added without breaking positional-call compatibility (the runtime slot only participates when the production call site passes RUNTIME or a user passes a runtime string). Mirror guard GREEN.
  </acceptance_criteria>
  <done>cacheKey folds runtime into the hashed identity tuple; claude vs codex keys differ (31-01 divergence test GREEN); existing cacheKey tests pass; production call site passes RUNTIME; snapshot == live hook.</done>
</task>

<task type="tdd">
  <name>Task 2: Add runtime field to every telemetry record</name>
  <files>tests/router.mjs.snapshot, ~/.claude/hooks/router.mjs</files>
  <read_first>tests/router.mjs.snapshot</read_first>
  <behavior>
    - Test 1: telemetryEntryFromState(decisionFixture, startNs).runtime is a non-empty string in {"claude","codex"}.
    - Test 2: the runtime field value equals the module RUNTIME constant (the field tags the active runtime correctly).
    - Test 3: all pre-existing telemetry fields (ts, prompt_signature, suggested_mode, confidence_tier, latency_ms, cwd, routing_version, ...) remain present and unchanged in shape.
  </behavior>
  <action>
    In tests/router.mjs.snapshot, add a `runtime` field to `telemetryEntryFromState(decision, startNs)` return object (snapshot:2560-2585) so every telemetry record carries its active runtime, per D-06 and PARITY-02. Ensure `telemetryEntryFromState` is exported (`export function telemetryEntryFromState(...)` — 31-01 telemetry-runtime tests import it directly). Append `runtime: RUNTIME` as a field on the returned object (additive — RESEARCH: existing named-field parsers unaffected). Place it consistently with the other observability fields (e.g. alongside `routing_version`), per Claude's Discretion on field order — document the chosen placement in a code comment so it is consistent and discoverable. The value comes from the module-level `RUNTIME` constant (already resolved in 31-02).

    This is the "telemetry RECORD carries a runtime field" half of D-06; the CACHE KEY identity half was Task 1. Telemetry stays append-only JSONL via logTelemetry (snapshot:1806) — no new writer, no raw prompt text (the record already only carries promptSignature sha256).

    After editing, mirror the SAME bytes into ~/.claude/hooks/router.mjs (byte-identical; mirror guard GREEN).
  </action>
  <verify>
    <automated>rtk node --test tests/router.runtime-tagging.test.mjs tests/router.prompt-route.baked-sibling.test.mjs 2>&1 | grep -E "runtime|pass|fail" | head</automated>
  </verify>
  <acceptance_criteria>
    The 31-01 telemetry-runtime test group is GREEN: telemetryEntryFromState (or a driven decision's telemetry_entry) carries a runtime field equal to the module RUNTIME constant, and the existing record shape is preserved. The mirror guard stays GREEN. Telemetry file format remains append-only JSONL written only by logTelemetry.
  </acceptance_criteria>
  <done>telemetryEntryFromState returns a runtime field equal to the module RUNTIME (31-01 telemetry test GREEN); existing record fields preserved; append-only JSONL via logTelemetry; snapshot == live hook.</done>
</task>

<task type="tdd">
  <name>Task 3: Deliberate OUTCOME_FIELDS policy-version bump (runtime/epoch, 14 → 16)</name>
  <files>src/health/outcome-schema.mjs, src/registry/watcher.mjs</files>
  <read_first>src/health/outcome-schema.mjs</read_first>
  <behavior>
    - Test 1: OUTCOME_FIELDS.size === 16 and Object.isFrozen(OUTCOME_FIELDS) (the 31-01 updated enforcement test).
    - Test 2: OUTCOME_FIELDS.has('runtime') and OUTCOME_FIELDS.has('epoch') (membership loop updated in 31-01).
    - Test 3: a 14-field record (no runtime) or a record with runtime/epoch still validates via validateOutcomeEnvelope; existing health tests stay GREEN.
  </behavior>
  <action>
    In src/health/outcome-schema.mjs, perform the deliberate schema policy-version bump, per ROADMAP criterion 3 and D-06 consumer (RESEARCH Pitfall 2): add `'runtime'` and `'epoch'` to the frozen `OUTCOME_FIELDS` Set (snapshot:33-38), changing it from 14 to 16 members. Keep `Object.freeze(new Set(...))`. Add a code comment noting this is the intentional Phase-31 policy-version bump (HLTH-04 comment at :31-32 already documents that adding a field requires a policy_version bump — honor it; do not just extend the set silently).

    Also bump whatever `policy_version` value is surfaced by the schema if one is defined locally. The evidence records write `policy_version: HEALTH_POLICY_VERSION` from `src/health/observe.mjs:40` (emission sites :86 and :232) — bump it from `'health-policy-v1'` to `'health-policy-v2'` to reflect the new allowlist. NO canary-bridge guard change is needed: `canary-bridge.mjs:73` already validates candidates with `/^health-policy-v\d+$/`, which accepts v2 unchanged. Note `src/health/thresholds.mjs:34` exports a SEPARATE `POLICY_VERSION = 'health-policy-v1'` (imported by `tests/router.health.canary.test.mjs:49`) — bump it ONLY if the evidence schema shares that constant, and if bumped, update the canary test's assertion in the SAME change so the verify (which now includes it) stays GREEN. Inspect the exact policy_version emission path and bump the version string that the env/evidence schema writes — a silent add WITHOUT the version bump defeats ROADMAP criterion 3.

    Then, in src/registry/watcher.mjs, wire `runtime`/`epoch` through `telemetryRecordToEvidence` at the mapping site documented in 31-02 Task 3: ensure the hook's `runtime` field (and the epoch indicator) are carried into the evidence record so a runtime-tagged telemetry line survives ingest into the OUTCOME_FIELDS allowlist (per RESEARCH A2 — if the ingest allowlists raw fields, the allowlist is now 16 so runtime/epoch pass; if it forward-maps, add the mapping). Do this in the SAME change as the OUTCOME_FIELDS bump so the schema and its consumer move together.
  </action>
  <verify>
    <automated>set -o pipefail; rtk node --test tests/router.health.outcome-schema.test.mjs tests/router.health.canary.test.mjs tests/router.registry-diff.test.mjs tests/router.health.privacy.test.mjs 2>&1 | grep -E "pass|fail" | head -20</automated>
  </verify>
  <acceptance_criteria>
    The 31-01 outcome-schema test (size 16 + runtime/epoch membership + frozen) is now GREEN against the updated 14→16 allowlist and policy-version. Health/outcome/canary/privacy/registry tests all GREEN (the canary test's `POLICY_VERSION` assertion at :49 is updated in the same change IF the version string surfaces there). The runtime/epoch fields are reachable from the hook's telemetry through the watcher ingest into the 16-field evidence schema. No unrelated schema fields were added or removed.
  </acceptance_criteria>
  <done>OUTCOME_FIELDS is 14→16 (runtime/epoch) with policy-version bumped and wired through telemetryRecordToEvidence; the 31-01 16-field enforcement test GREEN; health/outcome/canary/privacy/registry tests all pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cache key parts → LRU store | Runtime identity in the key partitions the cache; wrong partition = cross-runtime stale route served |
| telemetry record → telemetry.jsonl / evidence schema | The runtime field must survive ingest through the allowlist; a dropped field silently loses runtime correlation |
| OUTCOME_FIELDS → ingest allowlist | A non-bumped schema silently accepts a new field (ROADMAP criterion 3 violation) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-05 | Tampering | cacheKey runtime slot | high | mitigate | runtime folded into the hashed parts tuple (identity), never route metadata; 31-01 divergence test proves claude/codex keys differ. |
| T-31-06 | Information Disclosure | telemetry runtime + prompt | high | mitigate | only promptSignature sha256 + runtime tag recorded; append-only JSONL 0600 via logTelemetry; no raw prompt text. |
| T-31-07 | Tampering | OUTCOME_FIELDS silent add | medium | mitigate | policy-version bump + size-16 enforcement test are atomic (31-01 test goes RED if schema unmoved); canary-bridge regex accepts the bumped v2. |
</threat_model>

<verification>
Full suite at wave merge: `rtk node --test tests/*.test.mjs`.
</verification>

<success_criteria>
- Cache keys are runtime-isolated (no cross-runtime reuse); 31-01 cache divergence test GREEN.
- Every telemetry record carries the active runtime field; append-only JSONL preserved.
- runtime/epoch land in evidence via a deliberate 14→16 OUTCOME_FIELDS policy-version bump wired through the watcher ingest — never a silent add; the 31-01 16-field enforcement test GREEN.
- Mirror guard stays GREEN (snapshot == live hook).
- Full suite GREEN before /gsd-verify-work.
</success_criteria>

<output>
Create `.planning/phases/31-runtime-tagging/31-03-SUMMARY.md` when done
</output>

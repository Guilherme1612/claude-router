---
phase: 39
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/intent/authority.mjs
  - tests/router.authority.test.mjs
  - tests/router.authority-policy.test.mjs
  - src/lifecycle/router-lifecycle.mjs
  - tests/router.lifecycle.test.mjs
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
estimate:
  tokens: 52000
  raw_tokens: 26000
  tasks: 2
  confidence: low

must_haves:
  truths:
    # Goal-backward truths (AUTH-01/02/03)
    - "classifyAuthority(prompt, { intent }) returns exactly one of advice, inspection, one_turn_action, persistent_goal_action, non_authorizing_discussion for every string input"
    - "Abstaining dispositions (hypothetical, quoted, negated, prohibited, preview, ambiguous) always map to non_authorizing_discussion regardless of any autonomous wording in the text"
    - "evaluateAuthorityPolicy returns a decision in {proceed, pause, ask, block} with a distinct snake_case reason_code per leg"
    - "A low confidence value with full authority + reversible + local + fit yields proceed; a high confidence value with no authority yields block"
    - "Flipping a weights.score field in the evaluator input does not change the decision (AUTH-03 independence invariant)"
    - "The 19 existing intent tests (10 in router.intent.test.mjs + 9 in router.intent-adversarial.test.mjs) remain green — classify.mjs is not edited"
    # Edge-probe covered rows (AUTH-02 empty/encoding/idempotency/concurrency)
    - "An empty or whitespace-only prompt classifies to non_authorizing_discussion with reason_code no_authority_marker (or the underlying ambiguous disposition), never to an executing class"
    - "Classification operates on JS string UTF-16 code units via regex; the same prompt string yields the same authority class on every call (pure function, no shared mutable state, safe under concurrent invocation)"
    - "Running classifyAuthority twice on the same prompt + intent produces identical results (idempotent pure function)"
  prohibitions:
    # Descriptor-less, flagged unverified. Domain-specific (survive canon-referral drop).
    - statement: "Confidence or historical-success weight must never upgrade a block or pause to a proceed (AUTH-03 elevation-of-privilege)"
    - statement: "A quoted, example, retrospective-audit, or policy-discussion framing must never create or widen execution authority even when it contains autonomous wording such as 'autonomously' or 'finish it' (AUTH-02 spoofing)"
    - statement: "The hook must never erase a prompt (exit 2) to enforce confirmation — pause/ask are surfaced as additionalContext suggestions only (fail-open)"
  flagged_assumptions:
    # Edge-probe unresolved/unclassified rows — never silently dropped
    - requirement_id: AUTH-01
      probe: "unclassified — review manually"
      assumption: "The 5-class taxonomy names (advice, inspection, one_turn_action, persistent_goal_action, non_authorizing_discussion) are the correct AUTH-01 interpretation per ROADMAP success criterion 1; RESEARCH A1 cites ROADMAP.md:64."
      status: unverified
      flagged: true
    - requirement_id: AUTH-03
      probe: "unclassified — review manually"
      assumption: "evaluateAuthorityPolicy runs both on the route-suggestion path (hint) and the action-mapper path (enforceable) per RESEARCH Open Question 1 recommendation; Phase 39 Plan 02 wires the enforceable gate, Plan 01 ships the pure evaluator + unit tests."
      status: unverified
      flagged: true
    # Edge-probe AUTH-02 rows — purity-covered by truths (lines above) but surfaced here so no edge is silently dropped
    - requirement_id: AUTH-02
      probe: "empty prompt — purity-covered by truth"
      assumption: "An empty or whitespace-only prompt classifies to non_authorizing_discussion (never an executing class); covered by must_haves.truths AUTH-02 empty row + a dedicated test case in router.authority.test.mjs."
      status: unverified
      flagged: true
    - requirement_id: AUTH-02
      probe: "encoding — UTF-16 code units via regex"
      assumption: "Classification operates on JS string UTF-16 code units via regex; non-ASCII prompts are handled by the existing classify.mjs regex set (MULTILINGUAL abstains). classifyAuthority inherits the same string-input contract. Covered by must_haves.truths AUTH-02 encoding row."
      status: unverified
      flagged: true
    - requirement_id: AUTH-02
      probe: "idempotency — same prompt twice → same class"
      assumption: "classifyAuthority and evaluateAuthorityPolicy are pure functions with no shared mutable state; running twice on the same prompt + intent yields identical results. Covered by must_haves.truths AUTH-02 idempotency row."
      status: unverified
      flagged: true
    - requirement_id: AUTH-02
      probe: "concurrency — safe under parallel invocation"
      assumption: "classifyAuthority/evaluateAuthorityPolicy hold no mutable state; safe under concurrent invocation. Covered by must_haves.truths AUTH-02 concurrency row."
      status: unverified
      flagged: true
  artifacts:
    - path: src/intent/authority.mjs
      provides: AUTHORITY_POLICY_VERSION, AUTHORITY_CLASSES, PROTECTED_EFFECT_TOKENS, classifyAuthority, autonomousWordingIsText, evaluateAuthorityPolicy
      min_lines: 80
    - path: tests/router.authority.test.mjs
      provides: AUTH-01 5-class taxonomy + AUTH-02 framing guard tests
      min_lines: 60
    - path: tests/router.authority-policy.test.mjs
      provides: AUTH-03 independence + weights-ignored invariant tests
      min_lines: 40
    - path: src/lifecycle/router-lifecycle.mjs
      provides: moduleNames entry for intent/authority.mjs (deploy bundle to both runtimes)
      min_lines: 1
    - path: tests/router.lifecycle.test.mjs
      provides: manifest.files.length === 261 assertion (259 + 2 = 1 new module × 2 roots)
      min_lines: 1
  key_links:
    - from: src/intent/authority.mjs
      to: src/lifecycle/router-lifecycle.mjs moduleNames
      via: deploy bundle flatMap (ownedRoot + codexOwnedRoot)
    - from: src/intent/authority.mjs classifyAuthority
      to: src/intent/classify.mjs classifyIntent output (intent.disposition)
      via: parameter passing (NOT import — authority.mjs is self-contained for deploy)
    - from: src/orchestrator/approval.mjs (Plan 02)
      to: src/intent/authority.mjs PROTECTED_EFFECT_TOKENS
      via: ES import (single source of truth for AUTH-05 protected class)
---

<objective>
Ship the deterministic authority taxonomy (AUTH-01/02) and the independent-input authority-policy evaluator (AUTH-03) as a single stdlib-only pure-function module `src/intent/authority.mjs`, layered over the existing 8-disposition `classifyIntent` without editing it, and deploy it to both runtimes via the lifecycle bundle.

Purpose: Establish the authority classification + policy primitives that AUTH-04/05 (Plan 02) wire into the gate and hot path. Pure functions with sealed inputs enforce the AUTH-03 independence invariant (confidence/weights cannot grant authority) and the AUTH-02 abstention invariant (framing-with-autonomous-wording never authorizes) at the type level.

Output: `src/intent/authority.mjs` (new), `tests/router.authority.test.mjs` (new), `tests/router.authority-policy.test.mjs` (new), lifecycle deploy bundle + count bump.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-intent-authority-risk-and-invocation-policy/39-RESEARCH.md
@.planning/phases/39-intent-authority-risk-and-invocation-policy/39-PATTERNS.md
@.planning/phases/39-intent-authority-risk-and-invocation-policy/39-VALIDATION.md
@src/intent/classify.mjs
@src/orchestrator/approval.mjs
@src/registry/contract.mjs
@src/lifecycle/router-lifecycle.mjs
@tests/router.intent.test.mjs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: authority taxonomy module + AUTH-01/02 tests + deploy bundle (tracer)</name>
  <files>src/intent/authority.mjs, tests/router.authority.test.mjs, src/lifecycle/router-lifecycle.mjs, tests/router.lifecycle.test.mjs</files>
  <read_first>
    - src/intent/classify.mjs (full, 108 lines) — copy the module-header + version-constant + Object.freeze vocabulary + regex + outcome() + classifyIntent precedence-chain shape; do NOT edit this file
    - src/orchestrator/approval.mjs (lines 1-19) — copy the Set-of-tokens vocabulary shape for PROTECTED_EFFECT_TOKENS
    - src/lifecycle/router-lifecycle.mjs (lines 384-420) — moduleNames array + moduleValues flatMap pattern
    - tests/router.lifecycle.test.mjs (lines 140-155) — manifest.files.length === 259 assertion + explanatory comment block
    - tests/router.intent.test.mjs (lines 1-34) — test scaffold + test-case-array loop pattern to mirror in router.authority.test.mjs
    - .planning/phases/39-intent-authority-risk-and-invocation-policy/39-PATTERNS.md (sections: authority.mjs, router.authority.test.mjs, router.lifecycle.test.mjs)
  </read_first>
  <behavior>
    - AUTH-01: an "execute" disposition with no persistent-goal marker → authority_class one_turn_action, reason_code one_turn_action
    - AUTH-01: an "execute" disposition WITH a persistent-goal marker ("until done", "keep going", "finish it all", "autonomously ... until", "end-to-end", "don't stop") → authority_class persistent_goal_action, reason_code persistent_goal_marker
    - AUTH-01: an "explain" disposition → authority_class advice, reason_code explain_marker
    - AUTH-01: INSPECTION_ONLY match ("inspect", "show", "list", "what does/do/is", "status", "audit", "diagnose", "inventory", "coverage", "health") AND no execute verb ("run","execute","start","create","fix","ship","deploy","plan") → authority_class inspection, reason_code inspection_marker
    - AUTH-01: abstaining dispositions (hypothetical, quoted, negated, prohibited, preview, ambiguous) → authority_class non_authorizing_discussion, reason_code abstaining_disposition
    - AUTH-02: autonomous wording ("autonomously", "without asking", "just do it", "don't ask", "no confirmation", "unattended") inside EXAMPLE_FRAMING ("e.g.", "for example", "such as", "like when", "suppose you", "imagine you") → autonomousWordingIsText returns true; classifyAuthority returns non_authorizing_discussion even if disposition is execute
    - AUTH-02: autonomous wording inside RETROSPECTIVE_FRAMING ("earlier", "previously", "last time", "before you", "yesterday", "in the past", "you already/just") → autonomousWordingIsText returns true
    - AUTH-02: autonomous wording inside POLICY_DISCUSSION ("the policy", "policy says", "rule says", "per the rules", "according to", "should you/the router") → autonomousWordingIsText returns true
    - AUTH-02: empty/whitespace-only prompt → authority_class non_authorizing_discussion (never an executing class)
    - AUTH-02: the 19 existing intent tests (10 + 9) remain green (classify.mjs is not edited)
    - Lifecycle: manifest.files.length === 261 after adding intent/authority.mjs to moduleNames (one module × two runtime roots via flatMap)
  </behavior>
  <action>
    Create src/intent/authority.mjs following the Pure-function policy module shape (PATTERNS §Shared Patterns). Export: AUTHORITY_POLICY_VERSION = 'authority-policy-v1'; AUTHORITY_CLASSES = Object.freeze(['advice','inspection','one_turn_action','persistent_goal_action','non_authorizing_discussion']); PROTECTED_EFFECT_TOKENS = Object.freeze(['destructive','unbounded','external','privileged','difficult-to-recover','credentialed','billing','publication','published','deploy','deployed','deployment','push','pr','costly','scope-expanding']); and the pure functions classifyAuthority(prompt, { intent }) and autonomousWordingIsText(text, disposition). classifyAuthority takes the intent.disposition from classifyIntent output (it does NOT import classifyIntent — it receives the disposition as a parameter, keeping the module self-contained for deploy), short-circuits abstaining dispositions first per AUTH-02, then disambiguates execute via PERSISTENT_GOAL_MARKERS, then falls back to INSPECTION_ONLY only when no execute verb matches, else non_authorizing_discussion. Every return value carries policy_version: AUTHORITY_POLICY_VERSION via an outcome() helper mirroring classify.mjs:53-60. No eval/Function, no prompt retention, no disk I/O, no spawn. Add a header comment naming Phase 39 + AUTH-01/02 + the purity invariant, mirroring classify.mjs:1-4 style.

    Create tests/router.authority.test.mjs mirroring tests/router.intent.test.mjs:1-34 scaffold (import('../src/intent/authority.mjs'), async authority helper, test-case-array loop with per-prompt failure localization). Tag new tests [phase39:authority]. Cover every AUTH-01 class + every AUTH-02 framing (example/retrospective/policy) using the behavior cases above. Include adversarial cases where classifyIntent would return execute but classifyAuthority must still abstain (e.g. "e.g. autonomously finish it"). Use the for-of loop with assert.equal(..., `expected ... for: ${prompt}`) shape for per-case localization.

    Add 'intent/authority.mjs' to the moduleNames array in src/lifecycle/router-lifecycle.mjs (after the registry/* block near line 389, with a "// Phase 39: AUTH-01..05 authority taxonomy + policy evaluator" comment matching the Phase 38 comment style at line 392). The existing moduleValues flatMap at line 418-420 deploys every entry to BOTH ownedRoot and codexOwnedRoot — adding one line is sufficient. Update the manifest.files.length assertion in tests/router.lifecycle.test.mjs from 259 to 261 and add a "+ 2 = 1 new module (src/intent/authority.mjs) × 2 roots (Phase 39 AUTH-01..05)" line to the explanatory comment block (lines 140-154).
  </action>
  <verify>
    <automated>rtk node --test tests/router.authority.test.mjs tests/router.intent.test.mjs tests/router.intent-adversarial.test.mjs tests/router.lifecycle.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - tests/router.authority.test.mjs exists and is green; it asserts authority_class + reason_code for every AUTH-01 class and every AUTH-02 framing
    - src/intent/authority.mjs exports AUTHORITY_POLICY_VERSION, AUTHORITY_CLASSES, PROTECTED_EFFECT_TOKENS, classifyAuthority, autonomousWordingIsText
    - classifyAuthority never imports classifyIntent (grep "import.*classify" src/intent/authority.mjs returns no match) — the module is self-contained for deploy
    - "e.g. autonomously finish it" classifies as non_authorizing_discussion (not persistent_goal_action)
    - "show me the routes" classifies as inspection; "run the deploy" classifies as one_turn_action
    - tests/router.intent.test.mjs (10 tests) and tests/router.intent-adversarial.test.mjs (9 tests) remain green with zero edits to src/intent/classify.mjs (git diff --stat src/intent/classify.mjs is empty)
    - tests/router.lifecycle.test.mjs assertion manifest.files.length === 261 is green
    - grep -c "intent/authority.mjs" src/lifecycle/router-lifecycle.mjs returns 1
  </acceptance_criteria>
  <done>New authority module ships, deploys to both runtimes, and AUTH-01/02 unit tests pass while all 19 existing intent tests (10 + 9) stay green.</done>
  <reversibility rating="reversible">New additive module + test file + one-line deploy list entry; no existing contract changed, no migration needed.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AUTH-03 independent-input authority-policy evaluator + tests</name>
  <files>src/intent/authority.mjs, tests/router.authority-policy.test.mjs</files>
  <read_first>
    - src/intent/authority.mjs (as created in Task 1) — append evaluateAuthorityPolicy to this module
    - src/orchestrator/approval.mjs (lines 120-151) — verifyApproval sealed-input + reason_code return shape to mirror
    - src/registry/contract.mjs (lines 56-59) — reversibility/risk enum values the evaluator reads
    - .planning/phases/39-intent-authority-risk-and-invocation-policy/39-PATTERNS.md (sections: authority.mjs Policy-evaluator shape, router.authority-policy.test.mjs)
    - .planning/phases/39-intent-authority-risk-and-invocation-policy/39-RESEARCH.md (Pattern 3, Pitfall 1)
  </read_first>
  <behavior>
    - AUTH-03: compatibility unfit (eligible=false or disposition != dispatch-candidate) → decision block, reason_code compatibility_unfit
    - AUTH-05 precursor: protected_ === true → decision pause, reason_code protected_effect_requires_confirmation (regardless of confidence or authority source)
    - AUTH-03: authGranted === false → decision block, reason_code authority_not_granted (regardless of confidence)
    - AUTH-04: reversible && local && (confidence high or medium) && authGranted → decision proceed, reason_code reversible_local_authorized
    - AUTH-05: !reversible || !local with authGranted → decision pause, reason_code non_reversible_or_external_requires_confirmation
    - AUTH-04: low confidence + authGranted + reversible + local → decision ask, reason_code low_confidence_clarify
    - AUTH-03 invariant: calling evaluateAuthorityPolicy with weights: { score: 999 } vs weights: { score: 0 } yields the SAME decision (weights is not read)
    - AUTH-03 invariant: low confidence + full authority + reversible + local → proceed; high confidence + no authority → block
    - Every return value carries decision, reason_code, confidence, and policy_version: AUTHORITY_POLICY_VERSION
  </behavior>
  <action>
    Append the pure function evaluateAuthorityPolicy({ confidence, authority, risk, compatibility } = {}) to src/intent/authority.mjs. The signature takes a sealed object; the confidence field is the tier STRING ('high'|'medium'|'low'), never the numeric confidenceTier score and never weights. The authority and risk legs cannot read confidence or weights — only the confidence string is passed, and only to modulate the proceed/ask branch, never to permit. Add a header comment on the function stating the AUTH-03 invariant (mirroring src/orchestrator/approval.mjs:1-5 style). Return { decision, reason_code, confidence, policy_version, ...facts } using the same field vocabulary as verifyApproval so downstream gateAction composes without shape translation. Check legs in order: compatibility_unfit → protected pause → authority_not_granted → reversible_local_authorized proceed → non_reversible_or_external pause → low_confidence_clarify ask. Use the exact reason_code strings listed in behavior above (PATTERNS §Reason-code vocabulary).

    Create tests/router.authority-policy.test.mjs mirroring tests/router.approval.test.mjs:1-18 scaffold. Tag tests [phase39:authority-policy]. Assert the two load-bearing independence invariants (low+auth+reversible+local → proceed; high+no-auth → block) and the sealed-input weights-ignored invariant (call evaluateAuthorityPolicy twice with identical confidence/authority/risk/compatibility but weights.score 999 vs 0; assert deepEqual decisions). Cover every behavior case above as a distinct test() with a descriptive name.
  </action>
  <verify>
    <automated>rtk node --test tests/router.authority-policy.test.mjs tests/router.authority.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - src/intent/authority.mjs exports evaluateAuthorityPolicy
    - evaluateAuthorityPolicy signature destructures { confidence, authority, risk, compatibility } — grep for "weights" in the function body returns 0 matches (AUTH-03 invariant enforced at code level)
    - tests/router.authority-policy.test.mjs is green; it contains a test asserting weights.score 999 vs 0 yields identical decisions
    - It contains a test asserting low+auth+reversible+local → proceed and high+no-auth → block
    - Every behavior case (compatibility_unfit, protected pause, authority_not_granted, reversible_local_authorized, non_reversible pause, low_confidence ask) has a passing test
    - rtk node --test tests/router.authority.test.mjs tests/router.intent.test.mjs tests/router.intent-adversarial.test.mjs tests/router.lifecycle.test.mjs all remain green (no regression from appending to authority.mjs)
  </acceptance_criteria>
  <done>AUTH-03 evaluator ships with independence-invariant tests; confidence and weights are provably unable to grant authority.</done>
  <reversibility rating="reversible">Appending a pure function + new test file to an additive module; no existing contract changed.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted prompt text → classifyAuthority | Untrusted operator prompt text crosses into the authority classifier; regex-deterministic, no eval/Function, no prompt retention |
| sealed evaluator input → authority/risk legs | The { confidence, authority, risk, compatibility } object is the trust boundary enforcing AUTH-03 independence — confidence/weights cannot cross into the authority/risk decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-01 | Spoofing | classifyAuthority — prompt injection via quoted/example/retrospective/policy framing containing autonomous wording | high | mitigate | Abstaining dispositions short-circuit before execute; autonomousWordingIsText detects EXAMPLE_FRAMING/RETROSPECTIVE_FRAMING/POLICY_DISCUSSION and demotes to non_authorizing_discussion even when classifyIntent returned execute. Unit test "e.g. autonomously finish it" → non_authorizing_discussion. |
| T-39-02 | Elevation of privilege | evaluateAuthorityPolicy — confidence or weights granting authority | high | mitigate | Sealed-input signature destructures { confidence, authority, risk, compatibility }; weights is not a parameter; confidence is the tier string only, used solely in the proceed/ask branch, never to permit. Unit test: weights.score 999 vs 0 yields identical decisions. |
| T-39-03 | Tampering | moduleNames deploy list — new module missing from one runtime | medium | mitigate | Append 'intent/authority.mjs' to moduleNames; the existing moduleValues flatMap deploys to both ownedRoot and codexOwnedRoot; lifecycle test count bump 259→261 is the regression backstop. |
| T-39-SC | Tampering | npm/pip/cargo installs | high | mitigate | No packages installed — stdlib-only module; no supply-chain surface. No legitimacy checkpoint needed. |
</threat_model>

<verification>
- rtk node --test tests/router.authority.test.mjs tests/router.authority-policy.test.mjs — phase unit tests green
- rtk node --test tests/router.intent.test.mjs tests/router.intent-adversarial.test.mjs — 19 existing intent tests (10 + 9) remain green (no regression)
- rtk node --test tests/router.lifecycle.test.mjs — manifest.files.length === 261, new module deploys to both runtimes
- grep -c "import.*classify" src/intent/authority.mjs returns 0 — authority.mjs is self-contained (does not import classifyIntent, receives disposition as a parameter)
</verification>

<success_criteria>
- AUTH-01: 5-class taxonomy distinguishes advice/inspection/one_turn_action/persistent_goal_action/non_authorizing_discussion before any capability executes
- AUTH-02: quotations, examples, negations, hypotheticals, audits, and policy discussion never create authority, including autonomous wording as text
- AUTH-03: confidence and historical-success weight never grant permission; independence invariant enforced by sealed input and verified by weights-ignored test
- No regression to the 19 existing intent tests (10 + 9); classify.mjs untouched
- New module deploys to both Claude and Codex runtime roots (HOST-03 parity preserved)
</success_criteria>

<output>
Create .planning/phases/39-intent-authority-risk-and-invocation-policy/39-01-SUMMARY.md when done
</output>

## Artifacts this phase produces

Plan 01 creates these symbols (consumed by Plan 02 and by plan-review-convergence drift verification):
- `src/intent/authority.mjs` (new file path)
- `AUTHORITY_POLICY_VERSION` (exported const, value 'authority-policy-v1')
- `AUTHORITY_CLASSES` (exported frozen array of 5 strings)
- `PROTECTED_EFFECT_TOKENS` (exported frozen array of protected-effect tokens — single source of truth for the AUTH-05 protected class, imported by approval.mjs in Plan 02)
- `classifyAuthority` (exported pure function — takes prompt + { intent }, returns { authority_class, disposition, reason_code, policy_version })
- `autonomousWordingIsText` (exported pure function — AUTH-02 framing guard)
- `evaluateAuthorityPolicy` (exported pure function — sealed-input evaluator, AUTH-03 independence invariant)
- Reason codes added: abstaining_disposition, explain_marker, persistent_goal_marker, one_turn_action, inspection_marker, no_authority_marker, compatibility_unfit, protected_effect_requires_confirmation, authority_not_granted, reversible_local_authorized, non_reversible_or_external_requires_confirmation, low_confidence_clarify
- Lifecycle: 'intent/authority.mjs' entry appended to moduleNames; manifest.files.length assertion 259 → 261
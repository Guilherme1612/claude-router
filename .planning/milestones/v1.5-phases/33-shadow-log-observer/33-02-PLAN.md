---
phase: 33-shadow-log-observer
plan: 02
type: execute
wave: 2
depends_on:
  - 33-01
files_modified:
  - tests/router.mjs.snapshot
  - src/lifecycle/router-lifecycle.mjs
  - tests/router.lifecycle.test.mjs
  - tests/router.shadow-log.lifecycle.test.mjs
autonomous: true
requirements:
  - CALIB-01
  - CALIB-02
  - CALIB-05
user_setup: []
estimate:
  tokens: 28000
  raw_tokens: 14000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - "Claude installs the observer as additive UserPromptExpansion, PostToolUse/PostToolUseFailure, and Stop bindings while the existing UserPromptSubmit router binding remains intact."
    - "Existing gsd, ralph-loop, caveman, and context-monitor groups survive install, repair, enable, disable, upgrade, and uninstall unchanged."
    - "Observer hook events are side-effect-only telemetry and never inject, block, or rewrite tool/prompt results."
  artifacts:
    - tests/router.mjs.snapshot (event dispatch branch)
    - src/lifecycle/router-lifecycle.mjs (binding reconciliation)
    - tests/router.shadow-log.lifecycle.test.mjs
  key_links:
    - "lifecycle updateManagedBinding -> additive observer event groups -> installed router.mjs"
    - "UserPromptExpansion command_name / PostToolUse tool_input / Stop -> observeShadowEvent"
    - "existing groups preserved by path-scoped filtering and matcher-aware binding reconciliation"

<objective>
Wire the observer into the deployed lifecycle without taking ownership of existing hooks. Direct slash commands use UserPromptExpansion, tool-based Skill/Agent/Task invocations use PostToolUse and PostToolUseFailure, and Stop settles no_signal. The same router source remains fail-open and the hook returns no output for observer events.
</objective>

<context>
@.planning/phases/33-shadow-log-observer/33-01-PLAN.md
@src/lifecycle/router-lifecycle.mjs
@tests/router.mjs.snapshot
@tests/router.lifecycle.test.mjs
@.claude/CLAUDE.md

The current installer binds only UserPromptSubmit and uses a path-aware updateBindingAt helper. Extend that helper minimally with optional matcher/event handling, and remove only groups whose command points at the owned router path. Do not replace the settings object or reorder unrelated groups.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dispatch observer events from the production hook</name>
  <files>tests/router.mjs.snapshot</files>
  <read_first>
    - tests/router.mjs.snapshot: isMain, main, logTelemetry, runtime paths, and fail-open top-level execution guard
    - tests/router.shadow-log.test.mjs: observer helper contract from Plan 01
  </read_first>
  <action>
    Add an early main(payload) branch for hook_event_name in UserPromptExpansion, PostToolUse, PostToolUseFailure, and Stop. Call observeShadowEvent with the event payload and return without routing, stdout injection, decision/block fields, or additionalContext. Keep UserPromptSubmit behavior byte-for-byte equivalent. The event branch must catch all observer errors and exit 0 so existing prompt/tool execution cannot be blocked.
  </action>
  <acceptance_criteria>
    - Spawned hook subprocesses for all four observer events exit 0 with empty stdout and do not mutate telemetry.
    - UserPromptSubmit still emits the existing route context and telemetry.
    - Observer event payload fields are not copied to persisted output.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/router.shadow-log.test.mjs tests/router.mjs.snapshot.diff.test.mjs</automated>
  </verify>
  <done>The deployed hook can observe invocation lifecycle events without changing the routing hook contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconcile additive lifecycle bindings and coexistence</name>
  <files>src/lifecycle/router-lifecycle.mjs, tests/router.lifecycle.test.mjs, tests/router.shadow-log.lifecycle.test.mjs</files>
  <read_first>
    - src/lifecycle/router-lifecycle.mjs: routerEntry, isRouterEntry, updateManagedBinding, updateBindingAt, install/uninstall transaction file lists
    - tests/router.lifecycle.test.mjs: fixture settings, install/repair/uninstall assertions, and snapshot/ownership checks
    - .claude/CLAUDE.md: verified hook coexistence constraints
  </read_first>
  <action>
    Extend Claude-side binding reconciliation with four observer events: UserPromptExpansion (no matcher), PostToolUse and PostToolUseFailure (matcher Skill|Agent|Task), and Stop (no matcher). Keep the existing UserPromptSubmit and Codex UserPromptSubmit bindings as-is. Make routerEntry accept an optional matcher while preserving the existing command/timeout shape. On disable/uninstall remove only owned router groups from each observer event; preserve every unrelated group, matcher, ordering, and plugin/context-monitor/ralph-loop entry. Add install, repair, disable, and uninstall tests proving the additive groups and byte-equivalent preservation of unrelated settings. Do not add a new dependency or alter hot-path lifecycle controller behavior.
  </action>
  <acceptance_criteria>
    - Fresh fixture settings contain the original UserPromptSubmit binding plus the four observer bindings.
    - Pre-existing same-event groups remain byte-for-byte equivalent apart from the expected append/remove of the owned observer group.
    - Uninstall leaves non-router bindings and unrelated settings intact; repeated install is idempotent.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/router.shadow-log.lifecycle.test.mjs</automated>
    <automated>node --test tests/router.lifecycle.test.mjs</automated>
  </verify>
  <done>CALIB-05 coexistence is proven at install and teardown, with observer bindings additive and fail-open.</done>
</task>

</tasks>

<verification>
- `node --test tests/router.shadow-log.lifecycle.test.mjs tests/router.lifecycle.test.mjs` passes.
- `node --test tests/router.mjs.snapshot.diff.test.mjs` passes after the live mirror is refreshed.
- A fixture containing ralph-loop, gsd, caveman, and context-monitor groups retains all four after install and uninstall.
</verification>

<success_criteria>
- CALIB-05 is satisfied without changing existing hook ownership or routing behavior.
</success_criteria>

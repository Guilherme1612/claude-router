---
phase: 34-per-install-auto-calibration
plan: 02
type: execute
wave: 2
depends_on:
  - 34-01
files_modified:
  - tests/router.mjs.snapshot
  - tests/router.auto-calibration.lifecycle.test.mjs
  - tests/router.mjs.snapshot.diff.test.mjs
autonomous: true
requirements:
  - CALIB-03
  - CALIB-04
must_haves:
  truths:
    - "A fresh accepted-outcome corpus automatically publishes one epoch-matched calibration file after the floor is reached."
    - "A fingerprint or mode-map epoch change prevents the old calibration from affecting routing while the existing cache key forces recomputation."
    - "Publication is atomic and retains a known-good rollback baseline; failed derivation leaves the current calibration unchanged."
  artifacts:
    - tests/router.mjs.snapshot
    - tests/router.auto-calibration.lifecycle.test.mjs
tasks:
  - task: "Run calibration after shadow settlement, using the existing observer lifecycle and preserving fail-open behavior."
    files: tests/router.mjs.snapshot
    verify: "node --test tests/router.auto-calibration.lifecycle.test.mjs"
  - task: "Prove epoch fallback, atomic publication, rollback baseline retention, and snapshot/live-hook parity."
    files: tests/router.auto-calibration.lifecycle.test.mjs tests/router.mjs.snapshot.diff.test.mjs
    verify: "node --test tests/router.auto-calibration.lifecycle.test.mjs tests/router.mjs.snapshot.diff.test.mjs"
verification:
  - "node --test tests/router.auto-calibration.lifecycle.test.mjs tests/router.calibration-epoch.test.mjs"
success_criteria:
  - "Calibration activation is per-install and epoch-gated; curated mode-map data is untouched."

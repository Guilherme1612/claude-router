---
phase: 41-manifest-vnext-and-trust-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 41-01
files_modified:
  - src/adapters/dispatch/contract.mjs
  - src/adapters/dispatch/claude.mjs
  - src/adapters/dispatch/codex.mjs
  - tests/router.trust-invocation.test.mjs
  - tests/router.trust-pregate.test.mjs
autonomous: true
requirements:
  - TRUST-03
  - TRUST-04
user_setup: []

estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "validateInvocation checks typed arguments, entrypoint identity, path containment, working directory, wrappers, quoting, destructive targets, and runtime scope before spawn"
    - "preDispatchGate validates dependency availability, permission/effect class, timeout, retry policy, output bounds, and completion contract before the adapter receives the invocation"
    - "Blocked invocations return a receipt with no spawn — the recommendationOnly no-spawn pattern is reused"
    - "Validation runs inside invokeImpl at dispatch time, never on the prompt hot path (router.mjs)"
    - "Both claude.mjs and codex.mjs invokeImpl call validateInvocation then preDispatchGate before spawn"
  artifacts:
    - src/adapters/dispatch/contract.mjs (validateInvocation + preDispatchGate exports)
    - src/adapters/dispatch/claude.mjs (invokeImpl wiring)
    - src/adapters/dispatch/codex.mjs (invokeImpl wiring)
    - tests/router.trust-invocation.test.mjs
    - tests/router.trust-pregate.test.mjs
  key_links:
    - "dispatch/contract.mjs::validateInvocation → claude.mjs::invokeImpl (called before spawn)"
    - "dispatch/contract.mjs::preDispatchGate → claude.mjs::invokeImpl (called after validateInvocation, before spawn)"
    - "codex.mjs::invokeImpl mirrors claude.mjs::invokeImpl (same insertion point)"
---

<objective>
Add a typed-argument-contract invocation validator (TRUST-03) and a pre-dispatch contract gate (TRUST-04) to the dispatch adapter contract. Both run inside invokeImpl before spawn, closing the trust gap between build-time eligibility and dispatch-time execution.

Purpose: Ensure every native invocation validates typed arguments, entrypoint, containment, cwd, wrappers, quoting, destructive targets, runtime scope (TRUST-03) and dependency, permission/effect, timeout, retry, output, and completion contracts (TRUST-04) before reaching an adapter.
Output: validateInvocation and preDispatchGate exported from dispatch/contract.mjs; wired into claude.mjs and codex.mjs invokeImpl; two new test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-manifest-vnext-and-trust-hardening/41-RESEARCH.md
@.planning/phases/41-manifest-vnext-and-trust-hardening/41-PATTERNS.md
@.planning/phases/41-manifest-vnext-and-trust-hardening/41-01-SUMMARY.md

@src/adapters/dispatch/contract.mjs
@src/adapters/dispatch/claude.mjs
@src/adapters/dispatch/codex.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TRUST-03 — validateInvocation typed-args + entrypoint/containment/cwd/wrapper/quoting/target/runtime-scope validation</name>
  <files>
    src/adapters/dispatch/contract.mjs
    src/adapters/dispatch/claude.mjs
    src/adapters/dispatch/codex.mjs
    tests/router.trust-invocation.test.mjs
  </files>
  <read_first>
    src/adapters/dispatch/contract.mjs (createDispatchAdapter at line 89-117, RECEIPT_STATES at line 33-35, buildReceipt at line 50-74, DISPATCH_CONTRACT_VERSION at line 19)
    src/adapters/dispatch/claude.mjs (validateFixturePath at line 111-132, within() at line 108-110, invokeImpl at line 267-288, recommendationOnly at line 241-265, canDispatchImpl, spawn call)
    src/adapters/dispatch/codex.mjs (validateFixturePath at line 74, invokeImpl at line 177, spawn at line 194 — mirrors claude.mjs)
    src/registry/fingerprint.mjs (portablePath at line 58-63, contained — path containment patterns)
  </read_first>
  <behavior>
    - Test 1: validateInvocation rejects a path with '..' → { ok: false, reason: 'path_escape' }
    - Test 2: validateInvocation rejects a fixture path outside allowed roots → { ok: false, reason: 'path_escape' }
    - Test 3: validateInvocation rejects a non-existent fixture → { ok: false, reason: 'fixture_not_found' }
    - Test 4: validateInvocation rejects a non-file path → { ok: false, reason: 'not_a_file' }
    - Test 5: validateInvocation rejects a cwd outside allowed roots → { ok: false, reason: 'cwd_escape' }
    - Test 6: validateInvocation rejects args with shell metacharacters → { ok: false, reason: 'unquoted_metachar' }
    - Test 7: validateInvocation rejects destructive target patterns (rm -rf /, > /dev/sda) → { ok: false, reason: 'destructive_target' }
    - Test 8: validateInvocation rejects runtime mismatch (invocation.runtime !== adapter.runtime) → { ok: false, reason: 'runtime_scope_mismatch' }
    - Test 9: validateInvocation rejects args with wrong types → { ok: false, reason: 'arg_type_invalid' }
    - Test 10: validateInvocation passes a valid invocation (typed args, contained paths, safe targets, matching runtime) → { ok: true }
    - Test 11: claude.mjs invokeImpl calls validateInvocation before spawn — a failed validation returns a receipt with no spawn
  </behavior>
  <action>
    1. Export validateInvocation(action, adapter) from src/adapters/dispatch/contract.mjs. This is a pure function (no spawn, no I/O beyond realpathSync/statSync for path validation). It reuses the existing { ok: false, reason: '<reason_code>' } return shape from validateFixturePath (claude.mjs:111-132).

    2. validateInvocation checks, in order:
       a. Typed argument contract: verify action.args is an array of strings (or typed per a declared schema if present). Reject non-string args with reason 'arg_type_invalid'.
       b. Entrypoint identity: reuse the validateFixturePath logic (path includes '..' → 'path_escape'; realpath resolves; containment via within(); isFile check). Import or inline the within() helper from claude.mjs:108-110.
       c. Working directory (cwd): if action.cwd is provided, verify it is within adapter allowed roots using the same realpath + within() pattern. Reject with 'cwd_escape'.
       d. Wrapper injection: verify shell:false is enforced (it already is in spawn calls — assert no shell wrapper string is present in action). Reject with 'wrapper_injection' if action.shell === true or a shell wrapper string is detected.
       e. Quoting: scan args for unescaped shell metacharacters (| ; & $ ` ! < > ( ) \n). Reject with 'unquoted_metachar'. Note: shell:false already prevents shell interpretation, but this is defense-in-depth per TRUST-03.
       f. Destructive targets: scan args for destructive patterns: 'rm -rf /', 'rm -rf ~', '> /dev/sd', 'mkfs', 'dd if= of=/dev/', 'shutdown', 'reboot'. Reject with 'destructive_target'.
       g. Runtime scope: verify action.runtime (or invocation.runtime) === adapter.runtime. Reject with 'runtime_scope_mismatch'.

    3. In claude.mjs invokeImpl (line 267-288): insert a call to validateInvocation(action, adapter) after canDispatchImpl check and before the spawn() call. On failure ({ ok: false, reason }), return a no-spawn receipt using the recommendationOnly pattern (claude.mjs:241-265) but with completion_evidence.state = 'blocked' and the reason in reason_codes. Import validateInvocation from './contract.mjs'.

    4. In codex.mjs invokeImpl (line 177): apply the identical validateInvocation insertion before spawn (line 194). codex.mjs mirrors claude.mjs (PATTERNS.md: codex.mjs is the variant, claude.mjs is canonical). Import validateInvocation from './contract.mjs'.

    5. Create tests/router.trust-invocation.test.mjs: import validateInvocation from ../src/adapters/dispatch/contract.mjs. Write pure-function tests (no real spawn) matching the behavior block. Use test fixtures with path strings, arg arrays, and a mock adapter object with { runtime: 'claude', allowedRoots: [...] }. Use the existing tests/phase-38/fixtures/harmless.mjs pattern if a real path fixture is needed for the pass case.

    NEVER place validateInvocation on the prompt hot path (Pitfall 4). It runs inside invokeImpl (dispatch time), never in router.mjs.
  </action>
  <verify>
    <automated>rtk node --test tests/router.trust-invocation.test.mjs tests/router.dispatch-integration.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - src/adapters/dispatch/contract.mjs exports validateInvocation (grep "export function validateInvocation" returns a match)
    - validateInvocation returns { ok: false, reason: 'path_escape' } for input containing '..'
    - validateInvocation returns { ok: false, reason: 'cwd_escape' } for cwd outside allowed roots
    - validateInvocation returns { ok: false, reason: 'destructive_target' } for args containing 'rm -rf /'
    - validateInvocation returns { ok: false, reason: 'runtime_scope_mismatch' } for runtime !== adapter.runtime
    - src/adapters/dispatch/claude.mjs invokeImpl calls validateInvocation before spawn (grep "validateInvocation" in claude.mjs returns a match)
    - src/adapters/dispatch/codex.mjs invokeImpl calls validateInvocation before spawn (grep "validateInvocation" in codex.mjs returns a match)
    - A failed validateInvocation returns a receipt with state 'blocked' and no spawn
    - rtk node --test tests/router.trust-invocation.test.mjs exits 0
    - rtk node --test tests/router.dispatch-integration.test.mjs exits 0 (regression)
  </acceptance_criteria>
  <done>
    validateInvocation is exported from dispatch/contract.mjs and called by both claude.mjs and codex.mjs invokeImpl before spawn. It rejects path escape, cwd escape, shell metacharacters, destructive targets, runtime mismatches, and invalid arg types. Blocked invocations produce a no-spawn receipt with reason codes.
  </done>
  <reversibility rating="reversible" rationale="Pure function + two call-site insertions. Removing the calls and the export restores prior behavior." />
</task>

<task type="auto" tdd="true">
  <name>Task 2: TRUST-04 — preDispatchGate validates dependency/permission/timeout/retry/output/completion before dispatch</name>
  <files>
    src/adapters/dispatch/contract.mjs
    src/adapters/dispatch/claude.mjs
    src/adapters/dispatch/codex.mjs
    tests/router.trust-pregate.test.mjs
  </files>
  <read_first>
    src/adapters/dispatch/contract.mjs (createDispatchAdapter at line 89-117, validateInvocation added in Task 1, buildReceipt at line 50-74)
    src/adapters/dispatch/claude.mjs (invokeImpl at line 267-288 — insertion point after validateInvocation, recommendationOnly at line 241-265)
    src/adapters/dispatch/codex.mjs (invokeImpl at line 177 — same insertion point)
    src/registry/eligibility.mjs (evaluateEligibility at line 145-190 — build-time baseline for comparison)
  </read_first>
  <behavior>
    - Test 1: preDispatchGate blocks an invocation with no timeout declared → { ok: false, reason: 'missing_timeout' }
    - Test 2: preDispatchGate blocks an invocation with unbounded retry (retry: -1 or retry: Infinity) → { ok: false, reason: 'unbounded_retry' }
    - Test 3: preDispatchGate blocks an invocation with no output bounds declared → { ok: false, reason: 'missing_output_bounds' }
    - Test 4: preDispatchGate blocks an invocation with no completion contract declared → { ok: false, reason: 'missing_completion_contract' }
    - Test 5: preDispatchGate blocks an invocation with missing dependency → { ok: false, reason: 'dependency_missing' }
    - Test 6: preDispatchGate blocks an invocation with disallowed permission/effect class → { ok: false, reason: 'permission_effect_disallowed' }
    - Test 7: preDispatchGate passes a valid invocation with bounded timeout, bounded retry, declared output bounds, completion contract, dependencies met, and allowed permission/effect → { ok: true }
    - Test 8: claude.mjs invokeImpl calls preDispatchGate after validateInvocation and before spawn — a failed gate returns a receipt with no spawn
  </behavior>
  <action>
    1. Export preDispatchGate(action, adapter, context) from src/adapters/dispatch/contract.mjs. This is a pure function validating the invocation contract (not the capability record). The context argument provides dependency availability and permission/effect state from the build-time eligibility evaluation (if available; default to permissive if context is absent — fail-open for the gate itself, but the gate still checks declared contracts).

    2. preDispatchGate checks, in order:
       a. Dependency availability: if action.dependencies is declared and context.dependencies are provided, verify each declared dependency is available. Reject with 'dependency_missing'. If no context, skip this check (build-time eligibility already validated dependencies).
       b. Permission/effect class: if action.permission_effect is declared and context.permission_effect is provided, verify it is in the allowed set. Reject with 'permission_effect_disallowed'. If no context, skip.
       c. Timeout contract: verify action.timeout is a positive integer (milliseconds). Reject with 'missing_timeout' if absent or not a positive number.
       d. Retry policy: verify action.retry is a non-negative integer (bounded). Reject with 'unbounded_retry' if retry is -1, Infinity, or not a finite non-negative integer.
       e. Output bounds: verify action.output_bounds is declared (max bytes or max lines). Reject with 'missing_output_bounds' if absent.
       f. Completion contract: verify action.completion_contract is declared (evidence type/schema). Reject with 'missing_completion_contract' if absent.

    3. In claude.mjs invokeImpl: insert a call to preDispatchGate(action, adapter) after validateInvocation and before spawn. On failure ({ ok: false, reason }), return a no-spawn receipt using the recommendationOnly pattern with completion_evidence.state = 'blocked' and the reason in reason_codes. Import preDispatchGate from './contract.mjs'.

    4. In codex.mjs invokeImpl: apply the identical preDispatchGate insertion after validateInvocation and before spawn. Import preDispatchGate from './contract.mjs'.

    5. Create tests/router.trust-pregate.test.mjs: import preDispatchGate from ../src/adapters/dispatch/contract.mjs. Write pure-function tests matching the behavior block. Fixtures are invocation objects (not full capability records) — per RESEARCH Pattern 4, these are dispatch-time gates validating the invocation contract. Include a valid invocation fixture with bounded timeout (e.g., 30000), bounded retry (e.g., 2), output_bounds (e.g., { max_bytes: 1048576 }), and completion_contract (e.g., { evidence_type: 'exit_code' }).
  </action>
  <verify>
    <automated>rtk node --test tests/router.trust-pregate.test.mjs tests/router.trust-invocation.test.mjs tests/router.dispatch-integration.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - src/adapters/dispatch/contract.mjs exports preDispatchGate (grep "export function preDispatchGate" returns a match)
    - preDispatchGate returns { ok: false, reason: 'missing_timeout' } for invocation without timeout
    - preDispatchGate returns { ok: false, reason: 'unbounded_retry' } for retry: Infinity
    - preDispatchGate returns { ok: false, reason: 'missing_output_bounds' } for invocation without output_bounds
    - preDispatchGate returns { ok: false, reason: 'missing_completion_contract' } for invocation without completion_contract
    - preDispatchGate returns { ok: true } for a valid invocation with all contracts declared
    - src/adapters/dispatch/claude.mjs invokeImpl calls preDispatchGate after validateInvocation (grep "preDispatchGate" in claude.mjs returns a match)
    - src/adapters/dispatch/codex.mjs invokeImpl calls preDispatchGate after validateInvocation (grep "preDispatchGate" in codex.mjs returns a match)
    - rtk node --test tests/router.trust-pregate.test.mjs exits 0
    - rtk node --test tests/router.dispatch-integration.test.mjs exits 0 (regression)
  </acceptance_criteria>
  <done>
    preDispatchGate is exported from dispatch/contract.mjs and called by both claude.mjs and codex.mjs invokeImpl after validateInvocation and before spawn. It validates timeout, retry, output bounds, completion contract, dependency availability, and permission/effect class. Failed gates produce a no-spawn blocked receipt with reason codes.
  </done>
  <reversibility rating="reversible" rationale="Pure function + two call-site insertions. Removing the calls and the export restores prior behavior." />
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| invokeImpl → spawn() | Untrusted invocation arguments cross into the host subprocess spawn boundary; validateInvocation + preDispatchGate form the gate |
| build-time eligibility → dispatch-time gate | Build-time eligibility provides baseline; dispatch-time gate validates the specific invocation contract before spawn |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-05 | Tampering | dispatch/claude.mjs + codex.mjs invokeImpl / path traversal | high | mitigate | TRUST-03: validateInvocation rejects '..' in paths, validates realpath containment via within(), rejects cwd outside allowed roots with 'cwd_escape' and 'path_escape' reason codes |
| T-41-06 | Tampering | dispatch invokeImpl / destructive targets | high | mitigate | TRUST-03: validateInvocation scans args for destructive patterns (rm -rf /, > /dev/sd, mkfs, dd, shutdown, reboot) and rejects with 'destructive_target' before spawn |
| T-41-07 | Tampering | dispatch invokeImpl / shell metacharacter injection | high | mitigate | TRUST-03: validateInvocation scans args for unescaped shell metacharacters and rejects with 'unquoted_metachar'; shell:false is already enforced in spawn — this is defense-in-depth |
| T-41-08 | Elevation of Privilege | dispatch invokeImpl / runtime scope | medium | mitigate | TRUST-03: validateInvocation verifies invocation.runtime === adapter.runtime and rejects with 'runtime_scope_mismatch'; a codex invocation cannot run in the claude adapter and vice versa |
| T-41-09 | Denial of Service | dispatch invokeImpl / unbounded execution | medium | mitigate | TRUST-04: preDispatchGate rejects missing timeout ('missing_timeout'), unbounded retry ('unbounded_retry'), and missing output bounds ('missing_output_bounds') before spawn |
| T-41-10 | Repudiation | dispatch invokeImpl / completion evidence | medium | mitigate | TRUST-04: preDispatchGate requires a declared completion contract ('missing_completion_contract'); blocked invocations produce attributable receipts with reason codes (no silent spawn failure) |
| T-41-SC | Tampering | npm/pip installs | low | accept | Phase 41 installs no external packages — stdlib-only, zero supply-chain surface |
</threat_model>

<verification>
- validateInvocation rejects path escape, cwd escape, shell metacharacters, destructive targets, runtime mismatches, and invalid arg types before spawn
- preDispatchGate rejects missing timeout, unbounded retry, missing output bounds, and missing completion contract before spawn
- Both gates are called inside invokeImpl (dispatch time), not on the prompt hot path
- Both claude.mjs and codex.mjs wire the gates in the same insertion point (after canDispatch, before spawn)
- Blocked invocations return no-spawn receipts with attributable reason codes
- Existing dispatch integration tests remain green
</verification>

<success_criteria>
- TRUST-03: Every native invocation uses a typed argument contract and validates entrypoint identity, path containment, working directory, wrappers, quoting, destructive targets, and runtime scope before execution
- TRUST-04: Dispatch validates dependency availability, permission/effect class, timeout, retry policy, output bounds, and completion contract before the host adapter receives an invocation
- All existing dispatch integration tests remain green
- No hot-path impact (validateInvocation + preDispatchGate run at dispatch time only)
</success_criteria>

<output>
Create `.planning/phases/41-manifest-vnext-and-trust-hardening/41-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- New exports from `src/adapters/dispatch/contract.mjs`: `validateInvocation(action, adapter)`, `preDispatchGate(action, adapter, context)`
- New reason tokens: `path_escape`, `fixture_not_found`, `not_a_file`, `cwd_escape`, `wrapper_injection`, `unquoted_metachar`, `destructive_target`, `runtime_scope_mismatch`, `arg_type_invalid`, `missing_timeout`, `unbounded_retry`, `missing_output_bounds`, `missing_completion_contract`, `dependency_missing`, `permission_effect_disallowed`
- `invokeImpl` in claude.mjs and codex.mjs gains: `validateInvocation` call + `preDispatchGate` call before `spawn()`
- Blocked receipt state: `completion_evidence.state = 'blocked'` with reason_codes (no-spawn receipt via recommendationOnly pattern)
- New test files: `tests/router.trust-invocation.test.mjs`, `tests/router.trust-pregate.test.mjs`
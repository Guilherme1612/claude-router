---
phase: 14-deterministic-mapping-activation-and-rollback
plan: "04"
type: execute
wave: 4
depends_on: ["14-03"]
files_modified:
  - src/lifecycle/router-lifecycle.mjs
  - src/registry/watcher.mjs
  - src/registry/validate.mjs
  - tests/router.lifecycle.test.mjs
  - tests/router.registry-watcher.test.mjs
autonomous: true
gap_closure: true
requirements: [MAP-01, ACT-01]
must_haves:
  truths:
    - Installed Claude and Codex controllers enter the reconcile-map-verify-activate pipeline without test-only injected paths.
    - Canonical ambiguous mapping results stop before production verification or activation.
    - Blocked or uncertain recovery preserves prior authority and remains retryable.
  artifacts:
    - path: src/lifecycle/router-lifecycle.mjs
      provides: Owned activation store and active pointer configuration for installed controllers
    - path: src/registry/watcher.mjs
      provides: Canonical mapping-shape and startup-recovery fail-closed guards
    - path: tests/router.registry-watcher.test.mjs
      provides: Installed-path activation, real ambiguity, and blocked-recovery regression evidence
  key_links:
    - from: src/lifecycle/router-lifecycle.mjs
      to: src/registry/watcher.mjs
      via: controllerConfig activation_root and active_path
    - from: src/registry/watcher.mjs
      to: src/registry/validate.mjs
      via: mapping.summary.disposition and mapping.subjects
---

<objective>
Close the installed-controller, ambiguity-boundary, and blocked-recovery gaps while preserving the existing deterministic mapper and immutable activation implementation.

Purpose: Make the normal dual-runtime installation actually reach Phase 14 automation and ensure every unsafe startup or mapping state fails closed per D-01 through D-14.
Output: Owned activation configuration, canonical schema guards, retryable recovery behavior, and integration regressions.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-deterministic-mapping-activation-and-rollback/14-CONTEXT.md
@.planning/phases/14-deterministic-mapping-activation-and-rollback/14-VERIFICATION.md
@.planning/phases/14-deterministic-mapping-activation-and-rollback/14-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enable owned activation paths in installed controllers</name>
  <files>src/lifecycle/router-lifecycle.mjs, tests/router.lifecycle.test.mjs, tests/router.registry-watcher.test.mjs</files>
  <behavior>
    - An isolated Claude install supplies controller-owned activation_root and active_path values.
    - An isolated Codex install supplies the equivalent paths without creating a second mutable authority.
    - Eligible reconciliation through the installed controller writes one immutable version and replaces active.json.
  </behavior>
  <action>Add failing installer/controller integration coverage first, then extend controllerConfig with deterministic controller-owned activation storage and active pointer paths for both deployed runtimes. Reuse the ownership manifest and shared controller root established by 14-03; do not give runtime mirrors independent mutation authority. Prove the real installed configuration reaches watcher activation rather than relying on injected test configuration, satisfying MAP-01 and ACT-01.</action>
  <verify><automated>node --test tests/router.lifecycle.test.mjs tests/router.registry-watcher.test.mjs</automated></verify>
  <done>Both runtime installations configure the same owned activation authority, and an eligible installed reconciliation publishes active.json.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce canonical ambiguity and recovery guards</name>
  <files>src/registry/watcher.mjs, src/registry/validate.mjs, tests/router.registry-watcher.test.mjs</files>
  <behavior>
    - A real mapCandidateRegistry result with summary.disposition ambiguous is rejected.
    - Any ambiguous subject is rejected even if the summary is malformed or optimistic.
    - recovery_status blocked never verifies or activates and is retried on a later reconciliation.
  </behavior>
  <action>Write regressions using the actual mapper output, then replace legacy top-level mapping checks with validation of the canonical summary and subjects schema in watcher and mapping_integrity. Treat malformed canonical output as unsafe. Change watcher startup state so only healthy or successfully recovered states can proceed; blocked recovery must retain prior authority and remain retryable rather than permanently setting recovered. Preserve deterministic mapper precedence from D-01 through D-09 and guarded activation from D-10 through D-14.</action>
  <verify><automated>node --test tests/router.registry-map.test.mjs tests/router.registry-watcher.test.mjs tests/router.registry-activate.test.mjs</automated></verify>
  <done>Real ambiguous mappings and blocked recovery cannot cross the verification boundary or mutate active authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| installed runtime -> controller config | Runtime-local deployment selects controller-owned mutable registry authority. |
| mapper -> verifier | Candidate-derived mapping evidence crosses into the production activation gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-14-04-01 | Tampering | controllerConfig | high | mitigate | Resolve only installer-owned activation paths and prove dual-runtime ownership in isolated integration tests. |
| T-14-04-02 | Elevation of Privilege | watcher ambiguity guard | high | mitigate | Validate canonical summary and every subject before invoking verifier or activator. |
| T-14-04-03 | Denial of Service | recovery retry state | medium | mitigate | Preserve prior authority and retry deterministically without advancing activation. |
</threat_model>

<verification>
Run `node --test tests/router.registry-map.test.mjs tests/router.registry-watcher.test.mjs tests/router.lifecycle.test.mjs tests/router.registry-activate.test.mjs` and confirm installed activation, ambiguity, and blocked recovery cases pass.
</verification>

<success_criteria>
- The installed controller uses owned activation_root and active_path configuration for both runtimes.
- Canonical ambiguous mapping evidence cannot reach production verification.
- Blocked recovery never activates and remains retryable with prior authority intact.
- MAP-01 and the installed-automation portion of ACT-01 have direct integration evidence.
</success_criteria>

<output>
Create `.planning/phases/14-deterministic-mapping-activation-and-rollback/14-04-SUMMARY.md` when done.
</output>

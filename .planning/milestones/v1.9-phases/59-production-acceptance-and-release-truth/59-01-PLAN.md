---
phase: 59-production-acceptance-and-release-truth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/release/preflight.mjs
  - tests/router.release-preflight.test.mjs
  - .planning/evidence/v1.9/RELEASE-EVIDENCE.json
  - .planning/phases/59-production-acceptance-and-release-truth/59-01-SUMMARY.md
  - .planning/phases/59-production-acceptance-and-release-truth/59-VERIFICATION.md
autonomous: true
requirements: [ACC-01, ACC-02, ACC-03, ACC-04]
must_haves:
  truths:
    - "Preflight accepts a verified safe-empty active state only when candidate eligibility, verification, and zero dispatchable routes are all explicit."
    - "Live installer snapshots, native smoke evidence, observability evidence, focused safety checks, full serial tests, evaluation, security, and planning projections are represented without raw logs."
    - "The final release gate checks archive invariant and peeled tag equality after archival, while the pre-archive run proves every other lane first."
    - "Prompt-path privacy and latency remain covered by the independent evaluation and full serial performance tests."
  prohibitions:
    - "Do not fabricate an active tuple for a zero-dispatchable inventory."
    - "Do not use an ordinary unstructured log or a composite score as release truth."
    - "Do not reset, clean, or overwrite unrelated working-tree changes."

<objective>
Run production acceptance and leave one independently inspectable v1.9 release evidence record ready for archival gates.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make preflight truthful for safe empty authority</name>
  <files>tests/router.release-preflight.test.mjs, src/release/preflight.mjs</files>
  <action>
  Add a test for a candidate with eligible/passing verification, absent active tuple, and dispatchable_count=0. Permit that state as installed semantic evidence while retaining the existing blocker for any incomplete safe-empty claim.
  </action>
  <verify><automated>rtk node --test tests/router.release-preflight.test.mjs</automated></verify>
</task>

<task type="auto">
  <name>Task 2: Run final acceptance and preflight evidence</name>
  <files>.planning/evidence/v1.9/RELEASE-EVIDENCE.json, 59-01-SUMMARY.md, 59-VERIFICATION.md</files>
  <action>
  Run focused safety/native checks, full serial tests, both evaluation runtimes, final live snapshot/report, planning validation, and pre-archive preflight. Record only bounded command results and evidence paths. Archive and tag only after the non-archive preflight is clean, then rerun final preflight against archive invariant and peeled tag equality.
  </action>
  <verify><automated>rtk node scripts/v18-release-preflight.mjs .planning/evidence/v1.9/RELEASE-EVIDENCE.json</automated></verify>
</task>

</tasks>

<verification>
- `rtk node --test tests/router.release-preflight.test.mjs`
- focused safety/native suite
- `rtk node --test --test-concurrency=1`
- Claude and Codex evaluation
- pre-archive and final release preflight
- `rtk git diff --check`
</verification>

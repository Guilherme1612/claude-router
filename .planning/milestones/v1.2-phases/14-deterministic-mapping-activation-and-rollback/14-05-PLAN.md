---
phase: 14-deterministic-mapping-activation-and-rollback
plan: "05"
type: execute
wave: 5
depends_on: ["14-04"]
files_modified:
  - src/registry/validate.mjs
  - src/registry/activate.mjs
  - router.calibrate.mjs
  - tests/router.registry-activate.test.mjs
  - tests/router.registry-watcher.test.mjs
autonomous: true
gap_closure: true
requirements: [MAP-01, ACT-01]
must_haves:
  truths:
    - Production activation independently authenticates fresh evidence bound to every exact input.
    - Incremental/full equivalence compares deterministic registry bytes produced from the candidate input.
    - Every Phase 14 calibration fixture is evaluated by the deterministic mapper and contributes to the required threshold.
  artifacts:
    - path: src/registry/activate.mjs
      provides: Independent production verification and input-fingerprint trust boundary
    - path: src/registry/validate.mjs
      provides: Behavioral incremental/full equivalence gate
    - path: router.calibrate.mjs
      provides: Phase 14 mapping-fixture evaluator and threshold accounting
  key_links:
    - from: src/registry/validate.mjs
      to: src/registry/activate.mjs
      via: canonical verification fingerprints, runner identities, freshness, and production disposition
    - from: router.calibrate.mjs
      to: src/registry/map.mjs
      via: mapping_fixture evaluation
---

<objective>
Replace boolean-shaped trust and nominal gates with exact-input production authentication and behavioral equivalence/calibration evidence.

Purpose: Ensure Phase 14 activation accepts only authentic production verification and that its mandatory gates test the behaviors their names promise.
Output: Hardened evidence binding, real registry equivalence, mapping calibration evaluation, and substitution regressions.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-deterministic-mapping-activation-and-rollback/14-CONTEXT.md
@.planning/phases/14-deterministic-mapping-activation-and-rollback/14-VERIFICATION.md
@.planning/phases/14-deterministic-mapping-activation-and-rollback/14-02-SUMMARY.md
@.planning/phases/14-deterministic-mapping-activation-and-rollback/14-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Authenticate production verification at activation</name>
  <files>src/registry/activate.mjs, tests/router.registry-activate.test.mjs, tests/router.registry-watcher.test.mjs</files>
  <behavior>
    - test_only or substituted evidence is rejected before version creation.
    - Candidate, reconciliation, mapping, policy, evidence, and verification fingerprint mismatches each fail closed.
    - Expired, incomplete, unknown-runner, or non-passing production verification cannot replace active.json.
  </behavior>
  <action>Create negative tests for each substitution dimension before hardening activation. Make activateCandidate recompute canonical fingerprints from its exact inputs and verification payload, compare every binding, require production provenance, known frozen runner identities, complete required gate IDs, passing disposition, and freshness per D-10 through D-14. Stop using test-only verifier output in success fixtures; construct production-valid evidence through the authoritative producer. Perform all checks before immutable version creation and again where the pointer publication contract requires freshness.</action>
  <verify><automated>node --test tests/router.registry-activate.test.mjs tests/router.registry-watcher.test.mjs</automated></verify>
  <done>Activation rejects test-only, stale, incomplete, substituted, or fingerprint-mismatched evidence without creating a version or changing active.json.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make equivalence and calibration gates behavioral</name>
  <files>src/registry/validate.mjs, router.calibrate.mjs, tests/router.registry-watcher.test.mjs</files>
  <behavior>
    - incremental_full_equivalence builds incremental and clean full registries from the exact candidate inputs and byte-compares canonical output.
    - phase14_mapping fixtures evaluate expected_target and expected_disposition through mapCandidateRegistry.
    - Every Phase 14 fixture affects mappingCount and the required pass threshold; no fixture falls through prompt-routing evaluation.
  </behavior>
  <action>Add failing tests that distinguish schema equality from registry-byte equivalence and that fail when one Phase 14 fixture is wrong. Replace the schema-only equivalence runner with bounded deterministic incremental and clean-full builds using established Phase 11/12 registry APIs, comparing canonical portable bytes for the exact candidate. Add an explicit phase14_mapping calibration branch that invokes the deterministic mapper, evaluates mapping_fixture, expected_target, and expected_disposition, and includes every such fixture in threshold totals. Preserve D-01 through D-09 precedence; do not reinterpret mapping fixtures as prompt routes.</action>
  <verify><automated>node --test tests/router.registry-watcher.test.mjs tests/router.registry-map.test.mjs tests/router.calibrate.test.mjs</automated></verify>
  <done>The production evidence bundle contains passing behavioral equivalence and complete Phase 14 calibration results, and deliberate mismatches fail their named gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| verifier -> activator | Verification evidence claims authority over immutable version publication. |
| calibration fixtures -> release gate | Local fixtures influence whether deterministic mapping is considered safe. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-14-05-01 | Spoofing | verification.json | high | mitigate | Recompute all exact-input and verification fingerprints and require production runner identities. |
| T-14-05-02 | Tampering | equivalence gate | high | mitigate | Build both paths from exact candidate inputs and compare canonical bytes. |
| T-14-05-03 | Repudiation | calibration accounting | medium | mitigate | Count every Phase 14 fixture and emit deterministic per-fixture outcomes. |
</threat_model>

<verification>
Run `node --test tests/router.registry-map.test.mjs tests/router.registry-activate.test.mjs tests/router.registry-watcher.test.mjs tests/router.calibrate.test.mjs` and exercise negative fingerprint, equivalence, and mapping-fixture cases.
</verification>

<success_criteria>
- Only complete fresh production verification bound to exact inputs can activate.
- Equivalence proves canonical incremental/full byte equality, not schema presence.
- All Phase 14 mapping fixtures are deterministically evaluated and thresholded.
- MAP-01 calibration and ACT-01 verification claims have disconfirming regression coverage.
</success_criteria>

<output>
Create `.planning/phases/14-deterministic-mapping-activation-and-rollback/14-05-SUMMARY.md` when done.
</output>

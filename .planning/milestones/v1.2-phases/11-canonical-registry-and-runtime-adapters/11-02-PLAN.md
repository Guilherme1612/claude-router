---
phase: 11-canonical-registry-and-runtime-adapters
plan: "02"
type: tdd
wave: 2
depends_on:
  - 11-01
files_modified:
  - src/adapters/claude.mjs
  - src/adapters/codex.mjs
  - tests/router.adapters.test.mjs
autonomous: true
requirements:
  - ADP-01
  - ADP-02
must_haves:
  truths:
    - Claude discovery covers global, plugin, agents-store, agent, command, hook/binding, dependency, and project inventory from explicit roots.
    - Codex discovery covers skills, plugins, agents, hooks, config metadata, MCP/tools/models/permissions, dependencies, and project inventory from explicit roots.
    - Recognizable malformed artifacts remain diagnostic non-dispatchable observations and unsupported formats become build diagnostics.
    - Native invocation data remains separated by runtime and discovery never reads outside supplied roots.
  artifacts:
    - src/adapters/claude.mjs
    - src/adapters/codex.mjs
    - tests/router.adapters.test.mjs
  key_links:
    - Both adapters expose discoverRoots, parseArtifact, normalizeArtifact, and compileInvocation.
    - Adapter normalization targets the schema and identity exports delivered by 11-01.
    - Realpath containment checks precede all artifact reads and portable provenance uses logical roots.
---

<objective>
Implement explicit-root Claude and Codex adapters that preserve native invocation and complete diagnostic evidence while producing the shared canonical observation contract.

Purpose: Satisfy ADP-01 and ADP-02 without ambient-home reads, heuristic merging, execution, or configuration mutation.
Output: Tested runtime-specific ESM adapters and temporary-home fixture matrix.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-canonical-registry-and-runtime-adapters/11-CONTEXT.md
@.planning/phases/11-canonical-registry-and-runtime-adapters/11-RESEARCH.md
@.planning/phases/11-canonical-registry-and-runtime-adapters/11-PATTERNS.md
@.planning/phases/11-canonical-registry-and-runtime-adapters/11-VALIDATION.md
@.planning/phases/11-canonical-registry-and-runtime-adapters/11-01-SUMMARY.md
@tests/router.mjs.snapshot
@tests/router.lifecycle.test.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify complete isolated runtime adapter matrices</name>
  <files>tests/router.adapters.test.mjs</files>
  <read_first>
    <file>.planning/phases/11-canonical-registry-and-runtime-adapters/11-CONTEXT.md</file>
    <file>.planning/phases/11-canonical-registry-and-runtime-adapters/11-VALIDATION.md</file>
    <file>tests/router.lifecycle.test.mjs</file>
    <file>tests/router.mjs.snapshot</file>
    <file>src/registry/schema.mjs</file>
    <file>src/registry/identity.mjs</file>
  </read_first>
  <behavior>
    - Claude fixtures enumerate global skills, plugin skills, agents-store skills, agents, commands, hooks/bindings, declared dependencies, and project `.claude` records.
    - Codex fixtures enumerate skills, plugins, agents, hooks, config metadata, MCP/tools/models/permissions, declared dependencies, and project `.codex` records.
    - Each adapter exposes and exercises the same four public operations while preserving runtime-specific invocation data.
    - Malformed recognizable artifacts produce partial non-dispatchable observations per D-09; unknown versions/types produce build diagnostics per D-11.
    - Missing optional values remain unknown per D-10 and declared unavailable dependencies are blocking per D-12.
    - Symlink escapes and canary files outside fixture roots are not read.
    - Traversal-order permutations yield identical normalized observations.
  </behavior>
  <action>Create the Wave 0 ADP-01/ADP-02 node:test suite using temporary roots, explicit `claudeRoot`, `codexRoot`, and project root options, and unconditional try/finally cleanup. Follow the mapped `tests/router.lifecycle.test.mjs` analog for temporary-home fixture ownership and cleanup, and `tests/router.mjs.snapshot` for native category shapes and fail-open inventory expectations. Include every required native category, global/project collision, repository/worktree scope, malformed/unsupported files, symlink escape, optional metadata, unavailable dependency, invocation preservation, logical provenance, sorted traversal, and outside-root canary cases. Import the planned adapter interfaces so the initial focused run is intentionally RED.</action>
  <acceptance_criteria>
    <criterion>Both runtime category matrices cover every inventory surface named by ADP-01 and ADP-02.</criterion>
    <criterion>Tests create no fixtures in and perform no reads from live Claude or Codex homes.</criterion>
    <criterion>Canary and symlink-escape assertions prove containment before reads.</criterion>
    <criterion>The initial run fails for missing adapter behavior, not fixture syntax or cleanup errors.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/router.adapters.test.mjs; test $? -ne 0</automated>
  </verify>
  <done>Wave 0 adapter coverage is complete and RED for both runtime contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Claude and Codex native adapters</name>
  <files>src/adapters/claude.mjs, src/adapters/codex.mjs</files>
  <read_first>
    <file>tests/router.adapters.test.mjs</file>
    <file>.planning/phases/11-canonical-registry-and-runtime-adapters/11-PATTERNS.md</file>
    <file>src/registry/schema.mjs</file>
    <file>src/registry/identity.mjs</file>
    <file>tests/router.mjs.snapshot</file>
  </read_first>
  <behavior>
    - Both modules implement discoverRoots(options), parseArtifact(path, options), normalizeArtifact(nativeRecord), and compileInvocation(record).
    - Results are deterministically sorted and contain compact logical provenance plus runtime-native variants.
    - Discovery treats content as data only and never executes capability files or installs dependencies.
  </behavior>
  <action>Implement both standard-library-only adapters behind the identical public interface. Follow the mapped `tests/router.mjs.snapshot` analog for file-I/O-to-transform flow and native invocation preservation, and the `src/lifecycle/router-lifecycle.mjs` path-validation pattern for resolved-root containment before reads. Require explicit roots in core APIs; canonicalize and realpath roots, reject path/symlink escapes before reads, and sort roots, directory entries, and observations. Claude must cover the ADP-01 matrix including commands and hook bindings; Codex must cover the ADP-02 matrix including configuration, MCP/tools/models/permissions metadata. Normalize through the 11-01 schema while preserving separate native invocation fields per D-02. Apply D-05 through D-08 scope precedence metadata without activating fallback. Apply D-09 through D-12 diagnostic/unknown/dependency behavior and D-13 through D-15 logical provenance. Unknown formats/versions return deterministic build diagnostics. Do not consult ambient homes, execute discovered content, write runtime state, infer missing declarations, or add packages per D-17.</action>
  <acceptance_criteria>
    <criterion>All four contract exports exist in each adapter and produce schema-valid observations.</criterion>
    <criterion>Every required Claude and Codex category is discovered with native invocation and logical provenance intact.</criterion>
    <criterion>Malformed/unsupported inputs remain visible but cannot dispatch.</criterion>
    <criterion>No read crosses supplied roots and no adapter operation mutates fixture or active state.</criterion>
    <criterion>Repeated and permuted traversals produce byte-equivalent normalized results.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/router.registry-schema.test.mjs tests/router.adapters.test.mjs</automated>
  </verify>
  <done>ADP-01 and ADP-02 are green with isolated deterministic discovery and complete diagnostics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supplied root to filesystem traversal | Adapter walks attacker-controlled local directory layouts. |
| Artifact bytes to parser | Capability metadata may contain malformed or prompt-like content. |
| Native dependency declarations to dispatchability | A file may claim unavailable or privileged dependencies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-05 | Elevation of Privilege | discoverRoots/parseArtifact | high | mitigate | Realpath and containment-check every root and artifact; reject symlink escapes before reading. |
| T-11-06 | Information Disclosure | provenance normalization | high | mitigate | Emit logical roots/relative paths portably; keep absolute paths only in local diagnostics. |
| T-11-07 | Tampering | native parser | high | mitigate | Parse known fields as inert data, validate shapes, never import or execute discovered files. |
| T-11-08 | Spoofing | origin/dependency metadata | high | mitigate | Preserve claims as evidence, use canonical identity rules, and block declared unavailable dependencies. |
| T-11-09 | Denial of Service | filesystem traversal | medium | mitigate | Explicit roots, deterministic bounded traversal, unsupported-format diagnostics, and no recursive escape. |
</threat_model>

<verification>
Run `node --test tests/router.registry-schema.test.mjs tests/router.adapters.test.mjs`; confirm temporary fixtures clean up and outside-root canaries remain untouched.
</verification>

<success_criteria>
ADP-01 and ADP-02 pass complete isolated fixture matrices and preserve D-02 plus D-05 through D-17 without package additions or runtime mutation.
</success_criteria>

<output>
Create `.planning/phases/11-canonical-registry-and-runtime-adapters/11-02-SUMMARY.md` when done.
</output>

---
phase: 20-close-gap-evo-05-add-production-trigger-for-canary-controlle
plan: 02
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - src/registry/watcher.mjs
  - tests/router.watcher-canary-trigger.test.mjs
autonomous: true
requirements: [EVO-05]
user_setup: []

must_haves:
  truths:
    - "When the watcher reconciles an eligible+recoveryReady candidate AND a known-good version exists AND the project evidence window is sufficient (≥30 samples), the watcher invokes proposeCandidate → evaluateCandidate → applyCanaryDecision instead of calling activateCandidate directly."
    - "When the evidence window is insufficient (<30 samples or empty), the watcher preserves (writes candidate.json, does not promote) with reason_code='insufficient_evidence_samples' — fail closed, never invokes uncompiled slow path."
    - "When recoverActiveVersion returns no_valid_history (first-ever activation, no active.json), the watcher activates directly via the existing activateCandidate path (bootstrap) — the first activation becomes the known-good with no canary rollback target."
    - "applyCanaryDecision delegates all publication mutation exclusively through REGISTRY_PUBLICATION (activate.mjs activateCandidate/previewRollback/executeRollback) — the watcher never writes active.json directly."
    - "The 6 REQUIRED_GATES (safety, privacy, quality, context_budget, compatibility, latency) are constructed from the reconciliation report, evidence window, perf-measure assessCalibration, and compile-index COMPILED_INDEX_COMPATIBILITY before evaluateCandidate is called."
    - "The watcher hot path (the reconcile function) runs in the controller child process, NOT in the prompt-routing hot path; src/context/prompt-route.mjs is unchanged and imports none of this wiring."
  artifacts:
    - "src/registry/watcher.mjs (modified reconcile at lines 328-345 to route through canary when conditions met)"
    - "tests/router.watcher-canary-trigger.test.mjs (new integration test covering promote/preserve/bootstrap edges)"
  key_links:
    - "watcher.mjs reconcile → canary-controller.mjs proposeCandidate/evaluateCandidate/applyCanaryDecision"
    - "watcher.mjs reconcile → evidence.mjs createPersistentEvidenceStore.window (sufficiency gate)"
    - "watcher.mjs reconcile → activate.mjs recoverActiveVersion (known-good + bootstrap branch)"
    - "watcher.mjs reconcile → perf-measure.mjs assessCalibration (3 of 6 gates) + compile-index.mjs COMPILED_INDEX_COMPATIBILITY (compatibility gate)"

# Specless probe fallback SKIPPED (visible, recorded choice) — see 20-01-PLAN.md frontmatter
# note for the full rationale. No probe-derived predicates; must_haves.truths derived from
# phase goal + RESEARCH.md + Phase 17 success criterion #4 + audit BLOCKER 2.
---

<objective>
Wire the watcher's eligible-activation branch (src/registry/watcher.mjs:328-345) to route eligible+recoveryReady candidates through the canary-controller (proposeCandidate → evaluateCandidate → applyCanaryDecision) instead of calling activateCandidate directly. This is the PRIMARY automatic production trigger that closes audit BLOCKER 2 (lines 177-180): telemetry-driven canary promotion + automatic rollback becomes reachable from the watcher's background control plane.

Purpose: The canary-controller library (Plan 01 deployed it) has no production caller. The watcher is the natural trigger — it already builds candidates and holds the activation context. This plan adds the evidence-sufficiency gate, the 6-gate construction, and the promote/rollback/preserve decision routing.
Output: Modified src/registry/watcher.mjs reconcile (lines 328-345) + new tests/router.watcher-canary-trigger.test.mjs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-close-gap-evo-05-add-production-trigger-for-canary-controlle/20-RESEARCH.md
@.planning/phases/20-close-gap-evo-05-add-production-trigger-for-canary-controlle/20-01-SUMMARY.md
@.planning/v1.2-MILESTONE-AUDIT.md
@.claude/CLAUDE.md
@src/registry/watcher.mjs
@src/evolution/canary-controller.mjs
@src/evolution/evidence.mjs
@src/evolution/perf-measure.mjs
@src/registry/activate.mjs
@src/prompt/compile-index.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Watcher canary trigger integration test (promote/preserve/bootstrap edges)</name>
  <files>tests/router.watcher-canary-trigger.test.mjs</files>
  <read_first>
    - src/registry/watcher.mjs lines 246-365 (createRegistryReconciler signature, dependencies injection at 258 activator/recovery/mapper/verifier, reconcile function 284-363, eligible+recoveryReady branch 328-348)
    - .planning/phases/20-close-gap-evo-05-add-production-trigger-for-canary-controlle/20-RESEARCH.md (Pattern 3 "Watcher canary trigger integration" lines 284-320 — the integration sketch + gate construction; Pitfall 5 "Insufficient evidence must preserve" lines 407-411; Runtime State "active.json bootstrapping" lines 370-379)
    - src/evolution/canary-controller.mjs (proposeCandidate line 54, evaluateCandidate line 91, REQUIRED_GATES line 11, applyCanaryDecision line 148, REGISTRY_PUBLICATION line 135)
    - src/registry/activate.mjs (recoverActiveVersion line 190 — returns recovery_status:'blocked', reason_code:'no_valid_history' when no versions exist; activateCandidate line 179)
    - tests/router.evolution-canary.test.mjs lines 194-270 (D-10/D-11/D-12 test patterns — validSignal helper, gate construction, applyCanaryDecision promote/rollback assertions — replicate the test harness style)
    - tests/router.autonomous-lifecycle.test.mjs (grep for createTestRegistryReconciler / dependencies injection patterns — the existing watcher integration test style)
  </read_first>
  <behavior>
    - Test 1 (promote path): given a createTestRegistryReconciler with an eligible candidate, a known-good version present (recoverActiveVersion returns recovery_status:'healthy'), a sufficient evidence window (createPersistentEvidenceStore.window returns sufficient:true with ≥30 samples), all 6 gates pass, and demonstrated_benefit.status='demonstrated' → reconcile returns activation.activation_status='activated' AND applyCanaryDecision was invoked (assert the injected canary decision spy was called with known_good_version equal to the recovered version).
    - Test 2 (preserve on insufficient evidence): eligible candidate + known-good present + evidence window sufficient:false (injected store returns sample_count < 30) → reconcile returns activation.activation_status='preserved', reason_code='insufficient_evidence_samples' AND activateCandidate was NOT called (assert the activator spy call count is 0) AND applyCanaryDecision was NOT called.
    - Test 3 (bootstrap direct activation): eligible candidate + recoverActiveVersion returns recovery_status:'blocked', reason_code:'no_valid_history' (no active.json, first-ever) → reconcile calls activator directly (existing path), activation.activation_status='activated', applyCanaryDecision NOT called (no canary rollback possible when nothing to roll back to).
    - Test 4 (rollback on gate failure): eligible candidate + known-good present + sufficient evidence + one gate fails (e.g. quality.pass=false) + published_version exists → applyCanaryDecision returns status='rolled_back' (rollback branch to known_good_version via previewRollback/executeRollback) AND activateCandidate promote branch NOT called.
    - Test 5 (no-known-good + gate failure): eligible candidate + recoverActiveVersion returns no_valid_history + a gate would fail → bootstrap path still activates directly (canaries protect existing quality; first activation has no prior quality to protect — RESEARCH.md line 376).
  </behavior>
  <action>
    Create tests/router.watcher-canary-trigger.test.mjs using node:test + assert/strict + dynamic import via URL (replicate tests/router.evolution-canary.test.mjs lines 1-25 style). Use createTestRegistryReconciler (src/registry/watcher.mjs line 367) with injected dependencies: activator (spy), recovery (stub returning the desired recovery_status), mapper, verifier, and the new canary decision function (spy/stub returning the desired applyCanaryDecision status). Inject a stub persistent evidence store (or use createPersistentEvidenceStore with a tmpdir root seeded with ≥30 or <30 records). Construct the 6 gates per RESEARCH.md Pattern 3 lines 290-296: safety from report.disposition==='eligible'; privacy from evidence window (no privacy-denied records); quality/context_budget/latency from perf-measure assessCalibration (or stub the gate outcomes directly for unit-test speed); compatibility from COMPILED_INDEX_COMPATIBILITY.compatible(metadata.compatibility). Implement the 5 behavior tests above. Use mkdtempSync for any tmpdirs; clean up in try/finally.
    The test must verify the DECISION routing, not just the activation result — assert which spies were called and with what arguments (known_good_version, published_version, candidate fingerprint binding).
  </action>
  <verify>
    <automated>node --test tests/router.watcher-canary-trigger.test.mjs</automated>
  </verify>
  <done>
    - tests/router.watcher-canary-trigger.test.mjs exists and exits 0 with 5 passing tests.
    - Test 2 asserts activateCandidate was NOT called when evidence insufficient (fail-closed).
    - Test 3 asserts the bootstrap path activates directly when no known-good exists.
    - Test 4 asserts applyCanaryDecision rollback branch fires on gate failure with published_version present.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire watcher.mjs reconcile to route eligible activations through canary-controller</name>
  <files>src/registry/watcher.mjs</files>
  <read_first>
    - src/registry/watcher.mjs lines 1-17 (imports — add canary-controller, evidence, perf-measure, compile-index imports), lines 246-365 (createRegistryReconciler + reconcile), lines 328-348 (the eligible+recoveryReady branch to modify)
    - .planning/phases/20-close-gap-evo-05-add-production-trigger-for-canary-controlle/20-RESEARCH.md (Pattern 3 lines 284-320 — the integration sketch; Pitfall 5 lines 407-411; bootstrapping 370-379)
    - src/evolution/canary-controller.mjs (proposeCandidate signature line 54, evaluateCandidate line 91, applyCanaryDecision line 148 — the three functions to call in sequence)
    - src/evolution/evidence.mjs (createPersistentEvidenceStore — added in Plan 01 Task 2; .window(options) returns { sufficient, sample_count, weighted_samples, source_evidence_fingerprint, records })
    - src/evolution/perf-measure.mjs (assessCalibration line 86, evaluateCalibrationCorpus line 34, measureRoutes line 62 — build 3 of 6 gates)
    - src/prompt/compile-index.mjs (COMPILED_INDEX_COMPATIBILITY line 6, compatible() line 72 — compatibility gate)
    - src/registry/activate.mjs (recoverActiveVersion line 190 — no_valid_history bootstrap signal)
    - tests/router.watcher-canary-trigger.test.mjs (Task 1 — the tests this wiring must pass)
  </read_first>
  <behavior>
    - Behavior 1 (promote): when report.disposition==='eligible' && recoveryReady && knownGood exists && window.sufficient===true && all 6 gates pass && demonstrated_benefit.status in ['demonstrated','safety_correction'] → applyCanaryDecision promote branch runs, activation.activation_status='activated'.
    - Behavior 2 (preserve on insufficient): when window.sufficient===false → activation={ activation_status:'preserved', reason_code:'insufficient_evidence_samples' }; activator (activateCandidate) NOT called; applyCanaryDecision NOT called.
    - Behavior 3 (bootstrap): when recoverActiveVersion returns recovery_status:'blocked', reason_code:'no_valid_history' → the existing activator({ ownedRoot, candidate, ... reason:'watcher', test_mode }) path runs directly (no canary eval).
    - Behavior 4 (rollback on gate failure): when a gate fails && published_version exists → applyCanaryDecision rollback branch fires (delegates to previewRollback/executeRollback via REGISTRY_PUBLICATION).
    - Behavior 5 (published_version passed): applyCanaryDecision receives published_version = the current active.version_id (or null for first activation in the non-bootstrap branch).
  </behavior>
  <action>
    Add imports at the top of src/registry/watcher.mjs (after existing imports lines 1-16): `import { proposeCandidate, evaluateCandidate, applyCanaryDecision } from '../evolution/canary-controller.mjs';` `import { createPersistentEvidenceStore } from '../evolution/evidence.mjs';` `import { assessCalibration, evaluateCalibrationCorpus, measureRoutes, CALIBRATION_CORPUS } from '../evolution/perf-measure.mjs';` `import { COMPILED_INDEX_COMPATIBILITY } from '../prompt/compile-index.mjs';`. Add `recoverActiveVersion` to the existing activate.mjs import (line 14 already imports activateCandidate; add recoverActiveVersion if not present — it is at activate.mjs:190).
    Modify the `if (report.disposition === 'eligible' && recoveryReady)` block at lines 328-348. After the existing `verification.disposition === 'passing' && verification.complete === true` check (line 335), BEFORE the current `activation = await activator(...)` call at line 336, insert the canary routing:
    1. Call `recoverActiveVersion({ ownedRoot: config.activation_root, test_mode: config.test_mode === true })`. If result.recovery_status === 'blocked' && result.reason_code === 'no_valid_history' → fall through to the existing `activator({...})` call (bootstrap path — no canary rollback possible). Set knownGood = null.
    2. Else set knownGood = result.version_id (or if recovery_status in ['healthy','recovered'], use result.version_id).
    3. If knownGood !== null: open the persistent evidence store via `createPersistentEvidenceStore({ root: join(config.activation_root, 'evidence') })`. Call `window = store.window({ project_id: config.scope_id || config.scope || 'global' })`. If `window.sufficient !== true` → set `activation = { activation_status: 'preserved', reason_code: 'insufficient_evidence_samples' }` and skip the activator call (Pitfall 5 — fail closed). Do NOT call applyCanaryDecision.
    4. If sufficient: build the 6 gates per RESEARCH.md Pattern 3 lines 290-296: safety={pass: report.disposition==='eligible', reason_code: report.disposition==='eligible'?'safety_passed':'safety_uncertain'}; privacy={pass: !window.records.some(r => r.signal.confidence_band==='deny_filtered' || r.signal.guard_codes.some(c => ['privacy_guard','deny_filtered','secret_detected','content_detected'].includes(c))), reason_code:'privacy_passed' or 'privacy_regression'}; quality/context_budget/latency from assessCalibration({ evaluation: evaluateCalibrationCorpus({ route: <candidate route fn>, versions: {...} }), performance: measureRoutes({ fixtures: CALIBRATION_CORPUS, route: <candidate route fn> }) }); compatibility={pass: COMPILED_INDEX_COMPATIBILITY.compatible(<candidate metadata compatibility>), reason_code:'compatibility_passed' or 'compatibility_uncertain'}.
    5. Call `candidate = proposeCandidate({ source_evidence_fingerprint: window.source_evidence_fingerprint, policy_version: COMPILED_INDEX_COMPATIBILITY.policy_version, compiled_index_version: <candidate version id>, evaluation_inputs: {...}, proposal: {...} })` (bind evidence fingerprint + compiled_index_version + policy_version per the phase directive).
    6. Call `evaluation = evaluateCandidate({ candidate, evidence_window: window, gates, known_good_version: knownGood })`.
    7. Derive demonstrated_benefit: if all gates pass AND candidate performance is at least as good as known-good → {status:'demonstrated', reason_code:'...'}; if neutral → {status:'neutral'} (preserve); if safety-only change → {status:'safety_correction'}.
    8. Call `decision = applyCanaryDecision({ evaluation, demonstrated_benefit, activation: { ownedRoot: config.activation_root, candidate: built.registry, reconciliation: report, mapping, policy: config.activation_policy || {}, verification, reason: 'canary', test_mode: config.test_mode === true }, ownedRoot: config.activation_root, known_good_version: knownGood, published_version: active.version_id || null })`.
    9. Map decision.status to activation: 'promoted' → keep the existing post-activation publishIndex call (lines 337-344); 'rolled_back' → { activation_status:'rolled_back', reason_code: decision.reason_code }; 'preserved' → { activation_status:'preserved', reason_code: decision.reason_code || 'canary_preserved' }; 'recovery_required' → { activation_status:'recovery_required', reason_code: decision.reason_code }; 'rejected' → { activation_status:'preserved', reason_code: decision.reason_code || 'canary_rejected' }.
    Preserve all existing behavior for the non-eligible, non-recoveryReady, and bootstrap branches. The existing activator call at line 336 remains for the bootstrap case (knownGood === null) and MUST run with reason:'watcher' (not 'canary') for the bootstrap first-activation.
    Preserve Phase 17 D-decisions (STATE.md lines 136-141): canary never mutates publication authority directly (only via applyCanaryDecision → REGISTRY_PUBLICATION → activate.mjs); every mapping target revalidated against exact supplied candidate (evaluateCandidate enforces candidate hash integrity); evidence rejects forbidden fields before persistence (Plan 01); project+aggregate evidence isolated (Plan 01); 7d/24h/30-sample retention (Plan 01); fails closed when compatible compiled state missing (loadCompiledIndex already non-dispatchable — canary does not change this).
    Do NOT touch src/context/prompt-route.mjs (hot path stays read-only).
  </action>
  <verify>
    <automated>node --test tests/router.watcher-canary-trigger.test.mjs tests/router.evolution-canary.test.mjs</automated>
  </verify>
  <done>
    - src/registry/watcher.mjs reconcile calls applyCanaryDecision when eligible+recoveryReady+sufficient+knownGood present.
    - Insufficient evidence (window.sufficient===false) → preserve with reason_code='insufficient_evidence_samples', activator NOT called.
    - Bootstrap (recoverActiveVersion returns no_valid_history) → existing activator path runs directly with reason:'watcher'.
    - Gate failure + published_version present → applyCanaryDecision rollback branch fires.
    - node --test tests/router.watcher-canary-trigger.test.mjs exits 0 (5 tests from Task 1 pass).
    - node --test tests/router.evolution-canary.test.mjs exits 0 (existing canary tests still pass — no regression in the library).
    - grep -v '^#' src/context/prompt-route.mjs | grep -c "canary\|evidence\|telemetry-bridge" returns 0 (hot path untouched).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evidence window → canary decision | Persisted evidence (Plan 01) drives promotion/rollback of the compiled index — a poisoned or insufficient window could promote a bad candidate or block a good one. |
| watcher reconcile → publication authority | The watcher mutates active.json via applyCanaryDecision → REGISTRY_PUBLICATION → activate.mjs; bypassing this chain breaks CAS + fsync + TOCTOU invariants. |
| telemetry → evidence → canary | The full chain: telemetry.jsonl → bridge → evidence store → window → evaluateCandidate → applyCanaryDecision. A break anywhere cascades. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-08 | Tampering | watcher canary trigger (insufficient evidence promotion) | high | mitigate | Window.sufficient gate checked BEFORE evaluateCandidate (Pitfall 5 line 410); if insufficient → preserve with reason_code='insufficient_evidence_samples', activator NOT called (Test 2). 30-sample floor (Phase 17 D-08) prevents low-volume promotion. |
| T-20-09 | Tampering | watcher bypasses REGISTRY_PUBLICATION | high | mitigate | applyCanaryDecision delegates exclusively through REGISTRY_PUBLICATION (canary-controller.mjs:135-141); the watcher never calls writeFileSync(active.json) directly; activateCandidate/previewRollback/executeRollback are the only mutation paths (Phase 17 D-decision STATE.md:140). |
| T-20-10 | Denial of Service | bootstrap chicken-and-egg (no known-good) | high | mitigate | Bootstrap branch: recoverActiveVersion returns no_valid_history → activate directly via existing path (first activation becomes known-good; no canary rollback possible when nothing to roll back to — RESEARCH.md line 376). Test 3 + Test 5 verify. |
| T-20-11 | Tampering | weighted score compensates for hard-gate failure | high | mitigate | evaluateCandidate requires ALL 6 REQUIRED_GATES pass independently (canary-controller.mjs:115-120); no weighted compensation (Phase 17 D-decision STATE.md:140). Test 4 verifies rollback on single-gate failure. |
| T-20-12 | Tampering | stale evidence dominates promotion | medium | mitigate | 7d MAX_RETENTION_MS + 24h HALF_LIFE_MS decay enforced on window read (Plan 01 Task 2 + evidence.mjs:15-16); evidenceWindowFingerprint binds window to records. |
| T-20-13 | Tampering | canary trigger runs on hot path | high | mitigate | The reconcile function runs in the controller child process (watcher.mjs createRegistryReconciler), NOT in the prompt-routing hot path. src/context/prompt-route.mjs is unchanged (grep assertion in done criteria). CLAUDE.md <100ms hot-path constraint preserved. |
| T-20-14 | Elevation of Privilege | test_mode leaks into production trigger | medium | mitigate | test_mode is threaded through as config.test_mode === true (existing pattern line 336); production config.test_mode is absent/false; applyCanaryDecision/activateCandidate respect test_mode (activate.mjs:79-87). Phase 20 adds SEPARATE production wiring, does not reuse test_mode as a trigger (RESEARCH.md Anti-Pattern line 336). |
| T-20-SC | Tampering | npm/pip/cargo installs | high | mitigate | N/A — zero-dependency phase (no new packages). |
</threat_model>

<verification>
- node --test tests/router.watcher-canary-trigger.test.mjs exits 0 (5 tests).
- node --test tests/router.evolution-canary.test.mjs exits 0 (existing library unbroken).
- node --test tests/*.test.mjs | tail -5 shows no new failures introduced (regression baseline).
- grep -c "applyCanaryDecision" src/registry/watcher.mjs returns ≥1 (wiring present).
- grep -v '^#' src/context/prompt-route.mjs | grep -c "canary\|evidence\|telemetry-bridge" returns 0 (hot path untouched).
</verification>

<success_criteria>
- EVO-05 production trigger (watcher surface) wired: eligible+recoveryReady+sufficient → canary decision routing.
- Fail-closed on insufficient evidence (preserve, no promotion, no uncompiled slow path).
- Bootstrap path for first-ever activation (no canary rollback possible).
- Phase 17 D-decision invariants preserved (REGISTRY_PUBLICATION-only mutation, 6 independent gates, project+aggregate isolation).
- Hot path unchanged (prompt-route.mjs imports none of this wiring).
</success_criteria>

<output>
Create `.planning/phases/20-close-gap-evo-05-add-production-trigger-for-canary-controlle/20-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- Modified `src/registry/watcher.mjs` (reconcile function lines 328-348 — new canary routing branch; new imports of canary-controller, evidence, perf-measure, compile-index).
- `tests/router.watcher-canary-trigger.test.mjs` (new integration test file).
- No new exported symbols from watcher.mjs (the change is internal to reconcile; createRegistryReconciler/createTestRegistryReconciler signatures unchanged).
---
phase: 35-per-project-routing
plan: 02
type: execute
wave: 2
depends_on:
  - 35-01
files_modified:
  - tests/router.mjs.snapshot
  - tests/router.project-routing.test.mjs
autonomous: true
requirements:
  - PROJ-02
must_haves:
  truths:
    - "A project skill enters the routing corpus only when cwd equals the project root or is a child path."
    - "Sibling and prefix-collision paths cannot see the project skill."
    - "The prompt path performs no filesystem discovery for this scope decision."
  artifacts:
    - tests/router.mjs.snapshot
    - tests/router.project-routing.test.mjs
tasks:
  - task: "Include project-scoped entries through a pure cwd-prefix gate in buildCorpus and pass inspectDecision cwd through."
    files: tests/router.mjs.snapshot
    verify: "node --test tests/router.project-routing.test.mjs"
  - task: "Cover root, child, sibling, and prefix-collision paths."
    files: tests/router.project-routing.test.mjs
    verify: "node --test tests/router.project-routing.test.mjs"
verification:
  - "node --test tests/router.project-routing.test.mjs"
  - "node --test tests/router.mjs.snapshot.diff.test.mjs"
success_criteria:
  - "PROJ-02 is test-proven with a constant-time string gate and no route leakage outside the project."

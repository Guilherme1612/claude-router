---
phase: 48-production-integration
plan: 02
wave: 2
depends_on: [48-01]
files_modified:
  - src/lifecycle/router-lifecycle.mjs
  - src/lifecycle/migration.mjs
  - tests/router.production-integration.test.mjs
autonomous: true
requirements: [PROD-02, PROD-04]
---

# Plan 48-02: Installed Closure and Migration Recovery

## Objective

Deploy every production dependency to both runtimes and make crash recovery repeatable.

## Tasks

### Task 1: Expand the installed module closure

Add the existing strategy, learning, and migration modules to the installer’s shared module list so Claude and Codex receive identical runtime dependencies.

<verify>rtk node --test tests/router.production-integration.test.mjs tests/router.phase26-dual-runtime.test.mjs tests/router.lifecycle.test.mjs</verify>
<done>Fresh temporary installs contain and can import the strategy, learning, migration, and dispatch closure under both runtime roots.</done>

### Task 2: Make migration recovery idempotent

Accept completed recovery journal states and return the already selected old or new generation without rewriting an inconsistent pointer.

<verify>rtk node --test tests/router.production-integration.test.mjs tests/phase-46/migration.test.mjs tests/router.lifecycle-recovery.test.mjs</verify>
<done>Repeated recovery after missing, corrupt, before-pointer, and after-pointer interruption remains on one complete generation.</done>

## Success Criteria

- PROD-02 and PROD-04 are directly evidenced by fresh tests.


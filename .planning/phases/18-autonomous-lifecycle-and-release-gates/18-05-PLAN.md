---
phase: 18-autonomous-lifecycle-and-release-gates
plan: 05
type: execute
wave: 4
depends_on: ["18-01", "18-02", "18-03"]
files_modified:
  - tests/router.coexistence.test.mjs
  - tests/router.installer-coexistence.test.mjs
  - tests/router.v12-release.test.mjs
  - tests/router.compiled-evolution.test.mjs
  - src/release/run-release.mjs
autonomous: true
requirements:
  - SAF-09
  - SAF-10
  - ACT-01
  - MAP-02
  - REL-01
  - EVO-05
  - CHG-01
  - CHG-02
gap_closure: true
closes_gaps:
  - "verification gap 3: coexistence 5-verb matrix (install/upgrade/reinstall/disable/uninstall independently and together with unrelated state preservation, binding restoration, together-mode isolation, byte snapshots, post-pointer crash sampling)"
  - "verification gap 4: full workspace 1/607 failing + gate_results synthesized from child exit success (replace with real behavioral coverage parsing)"

must_haves:
  truths:
    - "All five lifecycle verbs (install, upgrade, reinstall, disable, uninstall) execute independently and together for Claude, Codex, and together fixtures with byte-identical preservation of unrelated plugins, skills, and user files, and binding restoration leaves non-router hooks unchanged (closes gap 3, D-07/D-08/D-09). Verified by: `node --test tests/router.installer-coexistence.test.mjs` exits 0 and `node --test tests/router.coexistence.test.mjs` exits 0."
    - "Complete old-or-new installation generation byte snapshots are sampled at every documented crash boundary (pre-pointer and post-pointer) for each of the five verbs, and every sample is either the complete prior generation or the complete new generation, never mixed or partial (closes gap 3, D-08). Verified by: grep for the crash-boundary sampling assertions in tests/router.installer-coexistence.test.mjs returns a positive count and the test command exits 0."
    - "Together-mode isolation proves the Claude and Codex installations do not share owned roots, controllers, pointers, or generation state, and the together fixture exercises all five verbs without cross-runtime interference (closes gap 3, D-09). Verified by: grep for together-mode isolation assertions in tests/router.installer-coexistence.test.mjs returns a positive count and the test command exits 0."
    - "The previously-failing 1 of 607 workspace test (tests/router.compiled-evolution.test.mjs:179 'D-13 through D-16 fixed corpus passes independent semantic budget and REL-01 gates') passes under full workspace load (`node --test tests/*.test.mjs` exits 0 with 607/607 pass) (closes gap 4). Verified by: `node --test tests/*.test.mjs 2>&1 | tail -10` reports `# pass 607` and `# fail 0`."
    - "src/release/run-release.mjs executeChild parses real behavioral coverage from child test stdout (TAP pass/fail counts and per-gate RELEASE_METRICS evidence) instead of synthesizing gate_results from exit-code 0; gate_results reflect actual test outcomes and fail closed when coverage is absent (closes gap 4, REL-01). Verified by: `node --test tests/router.v12-release.test.mjs` exits 0 and the new tests assert gate_results reflect parsed TAP outcomes, not synthesized pass entries."
  artifacts:
    - path: tests/router.installer-coexistence.test.mjs
      provides: "Complete five-verb coexistence matrix (install, upgrade, reinstall, disable+enable, uninstall) for Claude, Codex, and together fixtures with unrelated plugins/skills/user files, byte-identical unrelated-state assertions, binding restoration, post-pointer crash sampling, and together-mode isolation"
    - path: tests/router.coexistence.test.mjs
      provides: "Coexistence assertions for unrelated-state preservation across the five verbs and router-sentinel lexical distinctness from caveman output under each verb"
    - path: tests/router.compiled-evolution.test.mjs
      provides: "The previously-flaky D-13 through D-16 latency gate test, made deterministic under full workspace concurrency load"
    - path: tests/router.v12-release.test.mjs
      provides: "Release runner tests asserting executeChild parses real TAP/RELEASE_METRICS evidence from child stdout and fails closed on missing or failing behavioral coverage"
    - path: src/release/run-release.mjs
      provides: "executeChild implementation that parses child test stdout for TAP pass/fail counts and per-gate RELEASE_METRICS evidence, returning gate_results that reflect real behavioral coverage rather than synthesized pass entries"
  key_links:
    - from: tests/router.installer-coexistence.test.mjs
      to: src/lifecycle/router-lifecycle.mjs
      via: "installRouter, upgradeRouter, disableRouter, enableRouter, uninstallRouter, resolveInstallGeneration exercised across all five verbs and three fixture variants"
      pattern: "installRouter|upgradeRouter|disableRouter|enableRouter|uninstallRouter"
    - from: src/release/run-release.mjs
      to: tests/router.v12-release.test.mjs
      via: "executeChild returns gate_results parsed from child TAP stdout and RELEASE_METRICS evidence, not synthesized from exit code"
      pattern: "executeChild|gate_results"
---

<objective>
Close verification gaps 3 and 4 by extending the coexistence suite to exercise the full five-verb lifecycle matrix (install, upgrade, reinstall, disable, uninstall) independently and together with unrelated-state preservation, binding restoration, together-mode isolation, and post-pointer crash sampling; and by making the release runner's gate_results reflect real parsed behavioral coverage from child test stdout instead of synthesized pass entries, plus fixing the one flaky latency test that fails under full workspace load.

Purpose: The verifier found that tests/router.installer-coexistence.test.mjs covers only upgrade, disable/enable, and one pre-pointer failure, omitting install, reinstall, uninstall, together-mode isolation, complete unrelated-state byte snapshots, post-pointer crash sampling, and binding restoration; and that src/release/run-release.mjs lines 96-108 synthesize passing gate_results from child exit success, which cannot compensate for missing behavioral coverage, while the full workspace run is 606/607 with the D-13 through D-16 latency test failing under concurrent load.

Output: Five-verb coexistence matrix across three fixture variants with byte-identical unrelated-state assertions; deterministic release runner gate_results parsed from real TAP/RELEASE_METRICS evidence; the previously-flaky latency test made deterministic under full workspace load; full workspace regression green at 607/607.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-autonomous-lifecycle-and-release-gates/18-CONTEXT.md
@.planning/phases/18-autonomous-lifecycle-and-release-gates/18-RESEARCH.md
@.planning/phases/18-autonomous-lifecycle-and-release-gates/18-VERIFICATION.md
@.planning/phases/18-autonomous-lifecycle-and-release-gates/18-01-SUMMARY.md
@.planning/phases/18-autonomous-lifecycle-and-release-gates/18-02-SUMMARY.md
@.planning/phases/18-autonomous-lifecycle-and-release-gates/18-03-SUMMARY.md
@src/lifecycle/router-lifecycle.mjs
@src/release/run-release.mjs
@tests/router.coexistence.test.mjs
@tests/router.installer-coexistence.test.mjs
@tests/router.v12-release.test.mjs
@tests/router.compiled-evolution.test.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Five-verb coexistence matrix across Claude, Codex, and together fixtures with unrelated-state preservation, binding restoration, and post-pointer crash sampling</name>
  <files>tests/router.installer-coexistence.test.mjs, tests/router.coexistence.test.mjs</files>
  <read_first>
    - tests/router.installer-coexistence.test.mjs (the file being extended — currently 3 tests: upgrade, disable/enable, failed pre-pointer + startup repair; uses fixture() builder with claudeRoot, codexRoot, sourceRouter, settingsPath, routerPath, ownedRoot)
    - tests/router.coexistence.test.mjs (sentinel distinctness and override tests; extend with unrelated-state preservation assertions across the five verbs)
    - src/lifecycle/router-lifecycle.mjs (installRouter, upgradeRouter, disableRouter, enableRouter, uninstallRouter, resolveInstallGeneration exports; installRouter at line 270, uninstallRouter at line 464, restartController at line 523; updateManagedBinding at line 70 manages settings.json binding transitions)
    - src/lifecycle/router-lifecycle.mjs lines 234-268 (validatedSettings, fileMatches, transactionSnapshot, restoreTransaction — the transaction snapshot/restore machinery used for byte-identical rollback)
  </read_first>
  <behavior>
    - Five verbs exercised independently: installRouter (fresh install into empty homes), upgradeRouter (generation N to N+1), reinstallRouter (uninstallRouter followed by installRouter with same source generation), disableRouter+enableRouter (binding transitions), uninstallRouter (complete removal). Each verb is its own test.
    - Five verbs exercised together: one test runs install → upgrade → reinstall → disable → enable → uninstall in sequence on the same fixture, asserting state transitions at every step.
    - Three fixture variants: Claude-only (claudeRoot populated, codexRoot absent), Codex-only (codexRoot populated, claudeRoot absent), together (both populated with distinct unrelated state).
    - Unrelated state preserved byte-identically: each fixture seeds unrelated plugins (e.g., .claude/plugins/marketplace/other/plugin.json with distinct content), skills (e.g., .claude/skills/other/SKILL.md), and user files (e.g., .claude/user-notes.md, .codex/user-config.toml). After each verb, assert these unrelated files are byte-identical to their pre-verb snapshots (readFileSync deepEqual).
    - Binding restoration: after disableRouter, the settings.json UserPromptSubmit entry for router is removed but all non-router hooks (Stop, caveman UserPromptSubmit, etc.) are byte-identical to pre-install. After enableRouter, the router binding is restored and non-router hooks remain byte-identical. After uninstallRouter, settings.json is byte-identical to the pre-install snapshot (no router binding, all user hooks preserved).
    - Post-pointer crash sampling: for upgradeRouter with crashAt: 'after-active-pointer', assert resolveInstallGeneration returns the new generation (pointer committed) and unrelated state is preserved. For crashAt: 'before-active-pointer', assert resolveInstallGeneration returns the prior generation and unrelated state is preserved. Sample at both boundaries for install, upgrade, and reinstall.
    - Together-mode isolation: the together fixture asserts Claude and Codex owned roots are distinct directories, their active.json pointers reference different generation ids, and disabling the Claude installation does not affect the Codex installation's binding or owned root.
    - Existing three tests (upgrade, disable/enable, failed pre-pointer + startup repair) are retained and extended with unrelated-state byte assertions where they currently omit them.
  </behavior>
  <action>
    Extend tests/router.installer-coexistence.test.mjs with the five-verb matrix. Add a richer fixture builder (extend the existing fixture() function) that seeds unrelated plugins, skills, and user files in both claudeRoot and codexRoot, and returns pre-snapshot byte buffers for each unrelated file. Add a helper assertUnrelatedStateUnchanged(f, snapshot) that deepEquals every unrelated file against its pre-snapshot. Add five independent tests, one per verb: (1) installRouter into empty Claude and Codex homes, assert generation 1 is selected, assert unrelated files (seeded before install where applicable) are byte-identical, assert settings.json router binding is present and non-router hooks are preserved; (2) upgradeRouter from generation 1 to 2, assert new generation selected, unrelated state byte-identical, non-router hooks preserved; (3) reinstall via uninstallRouter then installRouter with the same source, assert generation id is fresh, unrelated state byte-identical, non-router hooks preserved; (4) disableRouter then enableRouter, assert binding transitions are exact and non-router hooks are byte-identical at every transition; (5) uninstallRouter, assert owned root is removed, settings.json is byte-identical to pre-install snapshot (no router binding, all user hooks preserved), unrelated files byte-identical. Add one together-mode test that runs all five verbs in sequence on a together fixture and asserts Claude and Codex owned roots remain distinct, their active.json pointers reference independent generation ids, and disabling Claude does not affect Codex bindings. Add post-pointer crash sampling tests for install, upgrade, and reinstall using crashAt: 'before-active-pointer' and 'after-active-pointer', asserting resolveInstallGeneration returns the correct generation and unrelated state is byte-identical at each boundary. Use transactionSnapshot/restoreTransaction from src/lifecycle/router-lifecycle.mjs if available, otherwise readFileSync-based byte comparison. Extend tests/router.coexistence.test.mjs with assertions that the router sentinel remains lexically distinct from caveman output after each of the five verbs (re-import the hook after each verb and re-run the sentinel distinctness assertion). Keep all existing tests passing. Do not touch real user homes — all tests use mkdtempSync temporary roots.
  </action>
  <verify>
    <automated>node --test tests/router.installer-coexistence.test.mjs tests/router.coexistence.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - node --test tests/router.installer-coexistence.test.mjs exits 0.
    - node --test tests/router.coexistence.test.mjs exits 0.
    - grep -c "installRouter" tests/router.installer-coexistence.test.mjs returns a positive count (install verb exercised).
    - grep -c "uninstallRouter" tests/router.installer-coexistence.test.mjs returns a positive count (uninstall verb exercised).
    - grep -c "reinstall" tests/router.installer-coexistence.test.mjs returns a positive count (reinstall verb exercised).
    - grep -c "together" tests/router.installer-coexistence.test.mjs returns a positive count (together-mode isolation exercised).
    - grep -c "after-active-pointer" tests/router.installer-coexistence.test.mjs returns a positive count (post-pointer crash sampling exercised).
    - grep -c "before-active-pointer" tests/router.installer-coexistence.test.mjs returns a positive count (pre-pointer crash sampling exercised).
    - The test file contains assertions that unrelated plugin, skill, and user files are byte-identical after each verb (grep for "deepEqual" and "readFileSync" patterns returns a positive count).
    - The test file contains assertions that non-router hooks in settings.json are preserved after disable/enable/uninstall (grep for "user-hook" or equivalent non-router hook assertion returns a positive count).
  </acceptance_criteria>
  <done>All five lifecycle verbs execute independently and together across Claude, Codex, and together fixtures with byte-identical unrelated-state preservation, binding restoration, post-pointer crash sampling, and together-mode isolation; both coexistence test files pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace synthesized gate_results with real parsed TAP/RELEASE_METRICS evidence and fix the flaky D-13 through D-16 latency test under full workspace load</name>
  <files>src/release/run-release.mjs, tests/router.v12-release.test.mjs, tests/router.compiled-evolution.test.mjs</files>
  <read_first>
    - src/release/run-release.mjs (the file being modified — executeChild at lines 96-113 synthesizes gate_results as gate_ids.map(id => ({ id, pass: true, reason_code: '${id}_pass' })) whenever exit code is 0 and not skipped; assertStageResult at lines 115-126 validates gate_results completeness)
    - src/release/run-release.mjs lines 86-94 (STAGES array — each stage has id, files, gate_ids; the latency stage has isolated: true and emits RELEASE_METRICS via stdout)
    - tests/router.v12-release.test.mjs (the test file being extended — currently uses a mock execute function that returns synthesized gate_results; the new tests must assert executeChild parses real TAP stdout)
    - tests/router.compiled-evolution.test.mjs lines 179-203 (the failing test 'D-13 through D-16 fixed corpus passes independent semantic budget and REL-01 gates' — asserts measured.warm.p95_ms < 25 and measured.warm.max_ms < 100; fails under full workspace load with warm p95 ~70ms due to concurrent test execution slowing the route)
    - tests/router.compiled-evolution.test.mjs (measureRoutes implementation and buildRealCalibrationRoute — the latency measurement harness)
  </read_first>
  <behavior>
    - executeChild parses child stdout for TAP pass/fail counts: extract `# pass N`, `# fail M`, `# skipped K` from the TAP summary. If `# fail` is non-zero or `# pass` does not match the expected count, gate_results fail closed.
    - executeChild parses per-gate RELEASE_METRICS evidence from stdout: lines matching `RELEASE_METRICS {json}` are parsed and their fields mapped to the corresponding gate_ids (e.g., warm_p95_ms and max_route_ms map to the 'warm-p95' and 'hard-route-ceiling' gates for the latency stage).
    - When stdout contains no TAP summary (e.g., child crashed before producing TAP), gate_results is empty and assertStageResult fails closed with 'structured-evidence-missing'.
    - When stdout contains TAP but no RELEASE_METRICS line for a latency stage, the latency gate fails closed with 'metrics-missing'.
    - The existing synthesized pass entries (gate_ids.map(id => ({ id, pass: true, reason_code: '${id}_pass' }))) are removed; gate_results are populated only from parsed stdout evidence.
    - The D-13 through D-16 latency test (tests/router.compiled-evolution.test.mjs:179) passes deterministically under full workspace load: either the measurement harness isolates the route from concurrent load (e.g., by using a dedicated worker/subprocess, or by increasing warmup_runs to stabilize the p95, or by making the assertion tolerant of documented workspace concurrency overhead while preserving the <25ms threshold for the isolated latency stage), or the test is marked with a concurrency isolation hint (node:test's `concurrency: 1` isolation) so it runs without competing test files.
    - The full workspace `node --test tests/*.test.mjs` reports 607 pass, 0 fail.
    - The release runner `node src/release/run-release.mjs` still exits 0 with the new parsing logic.
  </behavior>
  <action>
    Modify src/release/run-release.mjs executeChild (lines 96-113) to parse child stdout for TAP summary lines and RELEASE_METRICS evidence. Replace the synthesized gate_results line (line 107) with a realEvidence extraction: parse stdout for `# pass (\d+)`, `# fail (\d+)`, `# skipped (\d+)` and `RELEASE_METRICS (\{[^\n]+\})`; if `# fail` is non-zero or absent, return empty gate_results (fail closed); if RELEASE_METRICS is present, map its fields to the stage's gate_ids (warm_p95_ms -> 'warm-p95', max_route_ms -> 'hard-route-ceiling'); for non-latency stages, gate pass requires `# fail` to be 0 and `# pass` to be greater than 0. Keep the existing status/exit_code/skipped logic. Update assertStageResult if needed to handle the new reason codes ('tap-fail', 'metrics-missing', 'no-tap-summary'). Then update tests/router.v12-release.test.mjs to add tests that pass a mock child stdout containing TAP summary and RELEASE_METRICS lines and assert executeChild returns gate_results populated from the parsed evidence (not synthesized); add negative tests for missing TAP summary, non-zero fail count, and missing RELEASE_METRICS on the latency stage, asserting the runner fails closed. For the flaky D-13 through D-16 test: modify tests/router.compiled-evolution.test.mjs line 179 test to isolate the latency measurement from concurrent workspace load. The preferred fix is to run the measurement in a dedicated subprocess (spawnSync a single node process that imports the route and runs measureRoutes in isolation) so the p95 reflects the route's actual cost, not concurrent test scheduling overhead. If a subprocess is impractical, use node:test's `t.concurrent(false)` or break the test into a separate file that runs with `--test-concurrency=1`. Do not relax the <25ms threshold — the route's real p95 in isolation is well under 25ms (the test passes in isolation at ~452ms total). Document the chosen isolation approach in the test's leading comment.
  </action>
  <verify>
    <automated>node --test tests/router.v12-release.test.mjs tests/router.compiled-evolution.test.mjs && node --test tests/*.test.mjs 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - node --test tests/router.v12-release.test.mjs exits 0.
    - node --test tests/router.compiled-evolution.test.mjs exits 0 (in isolation).
    - node --test tests/*.test.mjs 2>&1 | tail -10 reports `# pass 607` and `# fail 0` (full workspace green).
    - grep -c "RELEASE_METRICS" src/release/run-release.mjs returns a positive count (real evidence parsing).
    - grep -c "gate_ids.map" src/release/run-release.mjs returns 0 (synthesis removed) — verify with `grep -v '^#' src/release/run-release.mjs | grep -c "gate_ids.map"`.
    - grep -c "# fail" src/release/run-release.mjs returns a positive count (TAP fail-count parsing).
    - tests/router.v12-release.test.mjs contains tests asserting executeChild fails closed on missing TAP summary, non-zero fail count, and missing RELEASE_METRICS on the latency stage (grep for 'no-tap-summary', 'tap-fail', 'metrics-missing' or equivalent reason codes returns a positive count).
    - node src/release/run-release.mjs exits 0 (release runner still passes with real evidence parsing).
  </acceptance_criteria>
  <done>The release runner's gate_results reflect real parsed TAP and RELEASE_METRICS evidence from child test stdout (not synthesized pass entries), the D-13 through D-16 latency test passes deterministically under full workspace load, and the full workspace regression is green at 607/607.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No new attack surface | This plan modifies test files and one production module (src/release/run-release.mjs). The production change replaces synthesized gate pass entries with parsed TAP evidence, which is a security improvement: the release runner can no longer report false-positive gate passes when child tests fail silently. Test files run in temporary directories and never touch real user homes. |
| Release runner evidence parsing | src/release/run-release.mjs executeChild now parses child stdout. Malformed stdout could cause the parser to miss evidence — but the fail-closed behavior (empty gate_results on missing TAP) is safer than the prior synthesized pass entries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-05-G3A | Tampering | tests/router.installer-coexistence.test.mjs | low | accept | Test-only additions in temporary directories; existing temp-root containment and cleanup patterns preserved. |
| T-18-05-G3B | Information Disclosure | tests/router.installer-coexistence.test.mjs | low | accept | Tests use mkdtempSync temporary roots; no real user home data is read or written. |
| T-18-05-G4A | Tampering | src/release/run-release.mjs executeChild | medium | mitigate | Replace synthesized gate_results with parsed TAP evidence; fail closed on missing or malformed stdout. Add tests asserting fail-closed behavior for missing TAP, non-zero fail count, and missing RELEASE_METRICS. |
| T-18-05-G4B | Denial | tests/router.compiled-evolution.test.mjs latency test | low | mitigate | Isolate the latency measurement from concurrent workspace load via dedicated subprocess or concurrency=1 so the p95 reflects the route's real cost, not test scheduling overhead. Do not relax the <25ms threshold. |
| T-18-05-SC | Tampering | npm/pip/cargo installs | n/a | n/a | No package installs in this plan — stdlib only, consistent with project constraints. |
</threat_model>

<verification>
Run `node --test tests/router.installer-coexistence.test.mjs tests/router.coexistence.test.mjs tests/router.v12-release.test.mjs tests/router.compiled-evolution.test.mjs` after both tasks. Then run `node --test tests/*.test.mjs 2>&1 | tail -10` and confirm `# pass 607` and `# fail 0`. Then run `node src/release/run-release.mjs` and confirm exit 0.
</verification>

<success_criteria>
- All five lifecycle verbs (install, upgrade, reinstall, disable, uninstall) execute independently and together across Claude, Codex, and together fixtures with byte-identical unrelated-state preservation and binding restoration.
- Post-pointer crash sampling covers install, upgrade, and reinstall at both before-active-pointer and after-active-pointer boundaries.
- Together-mode isolation proves Claude and Codex installations remain independent across all five verbs.
- src/release/run-release.mjs executeChild parses real TAP and RELEASE_METRICS evidence from child stdout; synthesized gate_results are removed.
- The D-13 through D-16 latency test passes deterministically under full workspace load.
- Full workspace regression is green at 607/607.
- The release runner exits 0 with the new evidence parsing logic.
</success_criteria>

<output>
Create `.planning/phases/18-autonomous-lifecycle-and-release-gates/18-05-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `tests/router.installer-coexistence.test.mjs` (extended): adds install, reinstall, uninstall, together-mode, and post-pointer crash sampling tests; extends existing tests with unrelated-state byte-identical assertions; covers the complete five-verb matrix across Claude, Codex, and together fixtures with binding restoration.
- `tests/router.coexistence.test.mjs` (extended): adds sentinel-distinctness assertions after each of the five verbs; retains existing override and caveman coexistence tests.
- `src/release/run-release.mjs` (modified): executeChild parses real TAP pass/fail counts and RELEASE_METRICS evidence from child stdout; removes synthesized gate_results; fails closed on missing or malformed evidence.
- `tests/router.v12-release.test.mjs` (extended): adds tests asserting executeChild returns parsed gate_results from TAP/RELEASE_METRICS stdout and fails closed on missing TAP, non-zero fail count, and missing RELEASE_METRICS on the latency stage.
- `tests/router.compiled-evolution.test.mjs` (modified): the D-13 through D-16 latency test is isolated from concurrent workspace load so the p95 measurement reflects the route's real cost, not test scheduling overhead; the <25ms threshold is preserved.
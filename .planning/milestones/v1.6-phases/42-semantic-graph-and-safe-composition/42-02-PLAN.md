---
phase: 42-semantic-graph-and-safe-composition
plan: 02
type: execute
wave: 2
depends_on: ["42-01"]
files_modified:
  - src/registry/substitute.mjs
  - src/cli/router-control.mjs
  - src/lifecycle/router-lifecycle.mjs
  - tests/router.semantic-substitution.test.mjs
  - tests/router.semantic-inspection.test.mjs
autonomous: true
requirements: [SEM-02, SEM-04]

estimate:
  tokens: 48000
  raw_tokens: 24000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "resolveSubstitution traverses substitute/fallback edges from a failed record and returns a contract-compatible candidate within unchanged authority, risk, scope, and resource bounds (SEM-04)"
    - "resolveSubstitution validates each candidate via evaluateEligibility and checks AUTHORITY_CRITICAL_FIELDS (permissions, side_effects, risk, reversibility, invocation_kind) — substitute must be within original's bounds (SEM-04, Pitfall 1)"
    - "resolveSubstitution retains both original_route and substitute_route in the result for attribution (SEM-04)"
    - "resolveSubstitution returns status: 'blocked' when zero candidates pass and status: 'ambiguous' when multiple candidates pass — never silently picks one (SEM-04, Pitfall 3)"
    - "RECEIPT_STATES in src/adapters/dispatch/contract.mjs is NOT extended with 'substituted' — Phase 44 (RCPT-02) owns that state (scope boundary)"
    - "semanticProjection combines contract fields (requires/produces), relationship edges (conflicts/substitutions/compositions), and lifecycle evidence into a unified inspectable view (SEM-02)"
    - "semanticProjection uses safeToken/safeIdentifier/safeTokenList on all surfaced strings — never echoes raw record text (privacy)"
    - "registry/substitute.mjs is deployed to BOTH ownedRoot and codexOwnedRoot via the moduleNames flatMap (HOST-03 parity)"
  artifacts:
    - "src/registry/substitute.mjs exports resolveSubstitution and SUBSTITUTION_REASONS"
    - "src/cli/router-control.mjs exports semanticProjection and wires a 'contract semantic' command path"
    - "src/lifecycle/router-lifecycle.mjs moduleNames includes registry/substitute.mjs"
    - "tests/router.semantic-substitution.test.mjs covers SEM-04 all substitution scenarios + bounds violations + RECEIPT_STATES unchanged"
    - "tests/router.semantic-inspection.test.mjs covers SEM-02 projection shape, pagination, safe tokens, TypeError"
  key_links:
    - "resolveSubstitution traverses edges from relationships.edges where type === 'substitute' or 'fallback' and source_id or target_id matches the failed record's stableCapabilityId"
    - "resolveSubstitution calls evaluateEligibility with the destructure pattern from build.mjs (strip authored eligibility/dispatch_eligible)"
    - "computeBoundsViolations checks each AUTHORITY_CRITICAL_FIELDS entry: risk ordering unknown < low < medium < high < critical < unacceptable, scope via stableStringify equality, permissions via subset, reversibility via safety ordering"
    - "semanticProjection filters relationship edges to those where source_id or target_id matches the record's stable_id"
    - "contract semantic command path in router-control.mjs dispatches to semanticProjection with --id, --limit, --offset options"
  prohibitions:
    - "Substitution must not select a candidate whose AUTHORITY_CRITICAL_FIELDS exceed the original's bounds — permissions must be a subset, risk must be equal or lower, scope must be identical, reversibility must be at least as safe (permission laundering prohibition, SEM-04 security-critical)"
    - "Substitution must not add 'substituted' to RECEIPT_STATES — Phase 44 RCPT-02 owns that receipt state"
    - "Substitution must not produce a result without both original_route and substitute_route retained — attribution loss is prohibited"
    - "semanticProjection must not echo raw contract field values or raw record text — safeToken/safeIdentifier sanitization is mandatory on all surfaced strings"
  flagged_assumptions:
    - "SEM-02 (unclassified edge): semanticProjection assumes the existing boundedResult/safeToken/safeIdentifier helpers provide sufficient sanitization for the semantic projection surface. The projection reuses fieldProjection (which already strips raw values from evidence) and relationshipItemProjection (which already sanitizes edge fields). [FLAGGED — specless probe fallback, manual review required]"
    - "SEM-04 (unclassified edge): resolveSubstitution assumes the relationship graph's substitute/fallback edges are the correct traversal surface for finding contract-compatible alternatives. It assumes evaluateEligibility on the candidate (not the failed record) is the authoritative eligibility check. It assumes AUTHORITY_CRITICAL_FIELDS (permissions, side_effects, risk, reversibility, invocation_kind) is the complete set of fields that must not expand. Resource bounds are checked via the cost field if present. [FLAGGED — specless probe fallback, manual review required]"
---

<objective>
SEM-04 contract-compatible substitution resolver + SEM-02 semantic inspection projection — the dispatch-time substitution path that finds a contract-compatible fallback within unchanged authority bounds, and the CLI inspection surface that shows why a capability fits.

Purpose: A failed selected capability can be substituted only by a contract-compatible candidate that stays inside unchanged authority, risk, scope, and resource bounds, with both routes retained for attribution. Operators can inspect versioned requires, produces, conflicts, substitutions, compositions, and lifecycle evidence explaining why a capability fits.
Output: `src/registry/substitute.mjs` (new), `src/cli/router-control.mjs` (extended), `src/lifecycle/router-lifecycle.mjs` (deploy list), 2 test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-semantic-graph-and-safe-composition/42-RESEARCH.md
@.planning/phases/42-semantic-graph-and-safe-composition/42-PATTERNS.md
@.planning/phases/42-semantic-graph-and-safe-composition/42-01-SUMMARY.md
@.planning/phases/41-manifest-vnext-and-trust-hardening/41-03-SUMMARY.md
@src/registry/eligibility.mjs
@src/registry/contract.mjs
@src/registry/trust.mjs
@src/registry/relationships.mjs
@src/registry/identity.mjs
@src/registry/schema.mjs
@src/adapters/dispatch/contract.mjs
@src/cli/router-control.mjs
@src/lifecycle/router-lifecycle.mjs
@tests/router.contract-eligibility.test.mjs
@tests/router.relationships.test.mjs
@tests/helpers/inventory-fixture.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>resolveSubstitution — contract-compatible fallback within unchanged bounds (SEM-04)</name>
  <files>src/registry/substitute.mjs, src/lifecycle/router-lifecycle.mjs, tests/router.semantic-substitution.test.mjs</files>
  <read_first>
    - src/registry/eligibility.mjs (lines 1-42 — imports, field/fieldState helpers, unsafeValue pattern; lines 140-143 — unsafeValue lexical check; lines 158-196 — computeQuarantineReasons per-record reasons pattern; lines 198-248 — evaluateEligibility shape, recordsById try/catch, destructure pattern, stableStringify scope equality at line 215-217)
    - src/registry/trust.mjs (lines 3-9 — AUTHORITY_CRITICAL_FIELDS: permissions, side_effects, risk, reversibility, invocation_kind)
    - src/registry/contract.mjs (lines 5-24 — CONTRACT_FIELDS; lines 67-70 — ENUM_FIELDS risk ordering: unknown < low < medium < high < critical < unacceptable, reversibility: unknown < reversible < irreversible; lines 83-103 — validateContractFieldValue)
    - src/registry/relationships.mjs (lines 7-16 — RULES, substitute and fallback edge types; full compileRelationshipGraph from Plan 01)
    - src/registry/identity.mjs (stableCapabilityId signature)
    - src/registry/schema.mjs (stableStringify export)
    - src/adapters/dispatch/contract.mjs (lines 36-40 — RECEIPT_STATES current 8 states: pending, invoked, paused, completed, failed, recommendation_only, blocked, quarantined — do NOT extend)
    - src/lifecycle/router-lifecycle.mjs (lines 384-431 — moduleNames array, existing semantic.mjs entry from Plan 01, moduleValues flatMap)
    - tests/router.contract-eligibility.test.mjs (lines 1-50 — safeRecord fixture, lazy import, test naming)
  </read_first>
  <behavior>
    - Test 1: Zero substitute/fallback edges from failed record → status: 'blocked', reason_code: 'no_compatible_substitute'
    - Test 2: One compatible candidate within bounds → status: 'substituted', bounds_unchanged: true, original_route and substitute_route both present
    - Test 3: Multiple compatible candidates → status: 'ambiguous', reason_code: 'ambiguous_substitute', candidates array sorted
    - Test 4: Substitute with broader permissions than original → not selected (substitution_permissions_expanded in violations)
    - Test 5: Substitute with higher risk than original (e.g., original risk='low', substitute risk='high') → not selected (substitution_risk_escalation in violations)
    - Test 6: Substitute with different scope than original → not selected (substitution_scope_expansion in violations)
    - Test 7: Substitute with less-safe reversibility (original 'reversible', substitute 'irreversible') → not selected (substitution_reversibility_escalation in violations)
    - Test 8: Both routes retained in result — original_route and substitute_route are both stableCapabilityId strings
    - Test 9: RECEIPT_STATES in dispatch/contract.mjs is unchanged — does NOT include 'substituted' (scope boundary with Phase 44)
    - Test 10: resolveSubstitution is non-throwing for invalid/missing failedRecord — returns status: 'blocked'
  </behavior>
  <action>
    Create `src/registry/substitute.mjs` with `resolveSubstitution({ failedRecord, records, relationships })`:
    - Import stableCapabilityId from ./identity.mjs, stableStringify from ./schema.mjs, evaluateEligibility from ./eligibility.mjs, AUTHORITY_CRITICAL_FIELDS from ./trust.mjs, CONTRACT_FIELDS and validateContractFieldValue from ./contract.mjs.
    - Export frozen `SUBSTITUTION_REASONS` array: ['ambiguous_substitute', 'no_compatible_substitute', 'substitution_permissions_expanded', 'substitution_risk_escalation', 'substitution_reversibility_escalation', 'substitution_scope_expansion', 'substitution_within_bounds'].
    - Build recordsById Map with try/catch around stableCapabilityId (copy eligibility.mjs:199-206).
    - Compute subjectId = stableCapabilityId(failedRecord). If this throws, return { schema_version: 1, policy_version: 'substitution-v1', status: 'blocked', reason_code: 'no_compatible_substitute' }.
    - Traverse edges: filter relationships.edges (and candidates) where edge.type === 'substitute' or edge.type === 'fallback' and (edge.source_id === subjectId or edge.target_id === subjectId). For each edge, the candidate is the other endpoint.
    - For each candidate: look up in recordsById. If not found, skip. Run evaluateEligibility with the destructure pattern (strip authored eligibility/dispatch_eligible). If not eligible, skip. Call computeBoundsViolations(failedRecord, candidate) — if violations array is non-empty, skip.
    - computeBoundsViolations(original, substitute): iterate AUTHORITY_CRITICAL_FIELDS. For 'permissions': substitute must be a subset of original (string array). For 'risk': use ordering unknown < low < medium < high < critical < unacceptable (from contract.mjs ENUM_FIELDS.risk Set order) — substitute risk must be <= original risk. For 'reversibility': use ordering unknown < reversible < irreversible — substitute must be at least as safe (<= in safety ordering where 'reversible' is safest). For 'side_effects': substitute side_effects must not contain tokens that original doesn't have. For 'invocation_kind': substitute invocation_kind must equal original. Also check scope: stableStringify(substitute.scope) === stableStringify(original.scope) (copy eligibility.mjs:215-217). Push reason codes for each violation. Return empty array = bounds unchanged.
    - Use the field() and fieldState() helpers from eligibility.mjs:32-42 to read contract field envelope state. Only compare fields with state === 'known'. If a field is 'unknown' on either side, treat as a violation (cannot verify bounds with unknown fields — conservative).
    - If zero candidates pass: return { schema_version: 1, policy_version: 'substitution-v1', status: 'blocked', reason_code: 'no_compatible_substitute' }.
    - If multiple candidates pass: return { schema_version: 1, policy_version: 'substitution-v1', status: 'ambiguous', reason_code: 'ambiguous_substitute', candidates: [ids sorted] }.
    - If exactly one candidate passes: return { schema_version: 1, policy_version: 'substitution-v1', status: 'substituted', original_route: subjectId, substitute_route: stableCapabilityId(candidate), bounds_unchanged: true, reason_codes: ['substitution_within_bounds'] }.
    - Non-throwing: all operational failures produce reason codes, not exceptions. TypeError only for structural shape violations (e.g., relationships not an object).

    Add `registry/substitute.mjs` to `src/lifecycle/router-lifecycle.mjs` moduleNames array:
    - After the 'registry/semantic.mjs' entry added by Plan 01, add:
      // Phase 42 SEM-04: substitution resolver deployed to BOTH ownedRoot
      // and codexOwnedRoot via the moduleValues flatMap below (HOST-03
      // parity; single-runtime deploy would ENOENT in Codex).
      'registry/substitute.mjs',
    - Copy the comment style from the Phase 42 semantic.mjs comment and the Phase 40 lease comment at line 399-404.

    Create `tests/router.semantic-substitution.test.mjs`:
    - Import buildClaudeHeavyProfile + contractEvidence from helpers/inventory-fixture.mjs, lazy import substitute module and dispatch/contract module.
    - safeRecord helper (copy contract-eligibility.test.mjs:13-22). Create a substituteRecord helper that builds a second record with a substitute edge to the first.
    - Test naming: '[42-red:semantic-substitution]'.
    - Include a test asserting RECEIPT_STATES (imported from src/adapters/dispatch/contract.mjs) does NOT include 'substituted' — assert.deepEqual(RECEIPT_STATES, ['pending', 'invoked', 'paused', 'completed', 'failed', 'recommendation_only', 'blocked', 'quarantined']).
  </action>
  <verify>
    <automated>node --test tests/router.semantic-substitution.test.mjs tests/router.deployed-bundle.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - resolveSubstitution returns status: 'substituted' with bounds_unchanged: true when exactly one contract-compatible candidate within bounds exists
    - resolveSubstitution returns status: 'blocked' with reason_code: 'no_compatible_substitute' when zero candidates pass
    - resolveSubstitution returns status: 'ambiguous' with reason_code: 'ambiguous_substitute' and sorted candidates array when multiple candidates pass
    - resolveSubstitution does not select a candidate whose permissions are not a subset of the original's permissions
    - resolveSubstitution does not select a candidate whose risk level exceeds the original's risk level (low original, high substitute → rejected)
    - resolveSubstitution does not select a candidate whose scope differs from the original's scope (stableStringify inequality → rejected)
    - resolveSubstitution does not select a candidate whose reversibility is less safe than the original (reversible original, irreversible substitute → rejected)
    - resolveSubstitution result with status: 'substituted' includes both original_route and substitute_route as stableCapabilityId strings
    - RECEIPT_STATES in src/adapters/dispatch/contract.mjs does NOT include 'substituted' — the array equals ['pending', 'invoked', 'paused', 'completed', 'failed', 'recommendation_only', 'blocked', 'quarantined']
    - grep -c "registry/substitute.mjs" src/lifecycle/router-lifecycle.mjs returns 1
    - tests/router.deployed-bundle.test.mjs passes with substitute.mjs in the moduleNames list
    - resolveSubstitution does not throw for a missing or invalid failedRecord — returns status: 'blocked'
  </acceptance_criteria>
  <done>Substitution resolver finds contract-compatible fallbacks within unchanged authority bounds, rejects permission laundering, retains both routes, RECEIPT_STATES unchanged, substitute.mjs deployed to both runtimes — all tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>semanticProjection — unified "why this fits" inspection (SEM-02)</name>
  <files>src/cli/router-control.mjs, tests/router.semantic-inspection.test.mjs</files>
  <read_first>
    - src/cli/router-control.mjs (lines 25-26 — MAX_VALUE, MAX_DIFF; lines 149-165 — boundedResult; lines 167-176 — safeToken, safeIdentifier, safeTokenList; lines 396-414 — fieldProjection; lines 439-467 — contractDetailProjection shape with TypeError; lines 469-491 — relationshipItemProjection; lines 493-509 — relationshipProjection shape; lines 953-1010 — contractCommand function, relationshipView dispatch, --id/--limit/--offset handling)
    - src/registry/contract.mjs (lines 5-24 — CONTRACT_FIELDS for requires/produces mapping)
    - src/registry/eligibility.mjs (lines 5-16 — ELIGIBILITY_GATES for lifecycle eligibility_gates projection)
    - tests/router.contract-eligibility.test.mjs (lines 1-50 — fixture patterns for CLI tests)
    - tests/router.relationships.test.mjs (lines 1-50 — edge/record helpers for relationship fixture)
  </read_first>
  <behavior>
    - Test 1: semanticProjection returns requires, produces, and relationship sections for a record with contract fields
    - Test 2: semanticProjection returns lifecycle section with enabled, lifecycle, eligible, and eligibility_gates fields
    - Test 3: semanticProjection respects limit and offset — total, returned, truncated, next_offset match boundedResult convention
    - Test 4: semanticProjection filters relationship edges to those where source_id or target_id matches the record's stable_id
    - Test 5: semanticProjection throws TypeError('invalid_contract_record') for a record without a contract object
    - Test 6: semanticProjection uses safeToken fallback 'unknown' for unsafe tokens — no raw record text echoed
    - Test 7: contract semantic command path returns canonical result with 'semantic_detail_ready' reason and exitCode 0
  </behavior>
  <action>
    Add `semanticProjection` export to `src/cli/router-control.mjs`:
    - Function signature: `export function semanticProjection({ record, relationships = {}, limit = MAX_DIFF, offset = 0 } = {})`.
    - If `!record?.contract || typeof record.contract !== 'object'`, throw `new TypeError('invalid_contract_record')` (copy contractDetailProjection pattern at line 440).
    - Build `requires`: map over ['inputs', 'dependencies'] — for each, call fieldProjection(record.contract.fields?.[f]) and tag with kind: 'requires'.
    - Build `produces`: map over ['outputs'] — call fieldProjection and tag with kind: 'produces'.
    - Compute subjectId = safeIdentifier(record?.stable_id || record?.id || record?.name).
    - Build edges: filter relationship edges (from relationships.edges and relationships.candidates) where edge.source_id === subjectId or edge.target_id === subjectId, map via relationshipItemProjection, tag with kind: 'relationship'.
    - Combine values: [...requires, ...produces, ...edges], sort by stableStringify(left).localeCompare(stableStringify(right)).
    - Call boundedResult(values, { limit, offset }) for pagination (copy relationshipProjection pattern at line 499).
    - Build lifecycle section: { enabled: record?.enabled !== false, lifecycle: safeToken(record?.lifecycle), eligible: record?.eligibility?.eligible === true, eligibility_gates: Object.fromEntries(Object.entries(record.eligibility?.gates || {}).sort(([l],[r]) => l.localeCompare(r)).map(([gate, state]) => [safeToken(gate), safeToken(state)])) }.
    - Return { total, returned, truncated, limit, offset, next_offset, semantic: bounded.values, lifecycle }.
    - Use safeToken/safeIdentifier on ALL surfaced strings — never echo raw record text.

    Wire `semanticProjection` into the contract command dispatch in `contractCommand` (around line 953-1010):
    - Add a `semanticView = positional[1] === 'semantic'` check alongside the existing `relationshipView = positional[1] === 'relationships'` at line 954.
    - Update the positional.length check to account for semanticView (same as relationshipView: positional.length > (semanticView ? 2 : 1)).
    - Update the invalid-arguments guard: semanticView should not coexist with relationshipView, and semanticView requires options.id (a specific record to inspect semantically).
    - After the relationshipView return block (around line 985), add a semanticView block:
      - Require options.id (semantic projection is per-record). If no options.id, return { result: canonical('contract', false, 'invalid_arguments'), exitCode: EXIT.usage }.
      - Find the record by safeIdentifier match (copy the record-finding logic from lines 991-992).
      - If record not found or no contract, return { result: canonical('contract', false, 'contract_not_found'), exitCode: EXIT.invalid }.
      - Return { result: canonical('contract semantic', true, 'semantic_detail_ready', semanticProjection({ record, relationships: source.relationships, limit, offset })), exitCode: 0 }.
    - Wrap in the existing try/catch that returns 'unsafe_contract_projection' on error (line 1008-1010).

    Create `tests/router.semantic-inspection.test.mjs`:
    - Import buildClaudeHeavyProfile + contractEvidence from helpers/inventory-fixture.mjs, lazy import router-control module.
    - safeRecord helper with contract + eligibility fields (copy contract-eligibility.test.mjs pattern, add eligibility: { eligible: true, gates: { ... } }).
    - Test naming: '[42-red:semantic-inspection]'.
    - Test the semanticProjection function directly (unit) and the contract semantic command path (integration if feasible).
  </action>
  <verify>
    <automated>node --test tests/router.semantic-inspection.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - semanticProjection returns an object with keys: total, returned, truncated, limit, offset, next_offset, semantic (array), lifecycle (object)
    - semantic array entries have kind: 'requires' | 'produces' | 'relationship'
    - lifecycle object has keys: enabled, lifecycle, eligible, eligibility_gates
    - semanticProjection respects limit and offset — total equals full count, returned equals sliced count, truncated is true when total > limit
    - semanticProjection filters relationship edges to those involving the record's stable_id — edges for other records are excluded
    - semanticProjection throws TypeError with message 'invalid_contract_record' when called with a record lacking a contract object
    - semanticProjection does not echo raw record text — all surfaced strings pass through safeToken/safeIdentifier
    - contract semantic command path (positional[1] === 'semantic' with --id) returns exitCode 0 and result.reason === 'semantic_detail_ready'
    - contract semantic command without --id returns exitCode usage (EXIT.usage) and invalid_arguments
    - All existing router-control tests remain green (no regression from the new command path)
  </acceptance_criteria>
  <done>semanticProjection combines requires/produces/relationships/lifecycle into a unified inspectable view, wired into the contract semantic CLI command, all surfaced strings sanitized — all tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Failed route → substitute candidate (NEW) | resolveSubstitution traverses the relationship graph to find a fallback. The substitute's AUTHORITY_CRITICAL_FIELDS must not exceed the original's bounds. This is the security-critical boundary for SEM-04. |
| Registry records → CLI projection (NEW) | semanticProjection surfaces contract fields and relationship edges. All strings pass through safeToken/safeIdentifier. Raw record text is never echoed. |
| Substitution result → attribution evidence (NEW) | Both original_route and substitute_route are retained. Phase 44 formalizes the 'substituted' receipt state; this plan produces the attribution record only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-07 | Elevation of Privilege | resolveSubstitution (substitute.mjs) | high | mitigate | computeBoundsViolations checks every AUTHORITY_CRITICAL_FIELDS entry: permissions subset, risk <= original, scope identical (stableStringify equality), reversibility at least as safe, invocation_kind identical, side_effects subset. Unknown-state fields are conservative violations (cannot verify bounds). A candidate with any violation is rejected. |
| T-42-08 | Repudiation | resolveSubstitution attribution | high | mitigate | Both original_route and substitute_route are retained in the substitution record. The result includes bounds_unchanged: true confirming bounds were checked. Phase 44 formalizes the receipt state; this plan produces the attribution evidence. |
| T-42-09 | Information Disclosure | semanticProjection (router-control.mjs) | medium | mitigate | All surfaced strings pass through safeToken (with 'unknown' fallback) and safeIdentifier. fieldProjection already strips raw values from evidence. relationshipItemProjection already sanitizes edge fields. No raw record text is echoed. |
| T-42-10 | Tampering | RECEIPT_STATES extension | medium | mitigate | RECEIPT_STATES is NOT extended with 'substituted' — Phase 44 (RCPT-02) owns that state. A test asserts the array equals the 8-state frozen list. This prevents scope creep into Phase 44's receipt contract. |
| T-42-SC | Tampering | npm/pip/cargo installs | n/a | accept | This phase installs no external packages. All code is stdlib-only Node.js ESM .mjs. No supply-chain surface. |
</threat_model>

<verification>
- node --test tests/router.semantic-substitution.test.mjs tests/router.semantic-inspection.test.mjs passes
- node --test tests/router.deployed-bundle.test.mjs passes (substitute.mjs in deploy list)
- node --test tests/router.contract-inspection.test.mjs passes (no regression from semanticProjection addition to router-control.mjs)
- grep -c "'substituted'" src/adapters/dispatch/contract.mjs returns 0 (RECEIPT_STATES unchanged)
- Full suite: rtk node --test tests/*.test.mjs passes with no new failures
</verification>

<success_criteria>
- SEM-04: resolveSubstitution finds a contract-compatible substitute within unchanged authority, risk, scope, and resource bounds, with both routes retained for attribution
- SEM-04: Permission laundering is rejected — substitute with broader permissions, higher risk, wider scope, or less-safe reversibility is not selected
- SEM-04: RECEIPT_STATES is NOT extended with 'substituted' (Phase 44 scope boundary)
- SEM-02: semanticProjection combines requires/produces/relationships/lifecycle into a unified inspectable view with safe token sanitization
- SEM-02: contract semantic CLI command path is wired with --id, --limit, --offset options
- substitute.mjs deployed to both Claude and Codex runtime roots via moduleNames flatMap
- No existing tests regress
</success_criteria>

<output>
Create `.planning/phases/42-semantic-graph-and-safe-composition/42-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

### New modules
- `src/registry/substitute.mjs` — new module exporting `resolveSubstitution({ failedRecord, records, relationships })`, `computeBoundsViolations` (internal), and `SUBSTITUTION_REASONS` frozen array

### New symbols
- `resolveSubstitution` — pure function, returns `{ schema_version, policy_version, status, original_route?, substitute_route?, bounds_unchanged?, candidates?, reason_code?, reason_codes }` where status is 'substituted' | 'blocked' | 'ambiguous'
- `SUBSTITUTION_REASONS` — frozen array of 7 reason codes: ambiguous_substitute, no_compatible_substitute, substitution_permissions_expanded, substitution_risk_escalation, substitution_reversibility_escalation, substitution_scope_expansion, substitution_within_bounds
- `computeBoundsViolations` — internal function returning array of violation reason codes (empty = bounds unchanged)
- `semanticProjection` — exported from `src/cli/router-control.mjs`, returns `{ total, returned, truncated, limit, offset, next_offset, semantic, lifecycle }`

### Modified symbols
- `contractCommand` in `src/cli/router-control.mjs` — gains `semanticView` branch dispatching to semanticProjection when `positional[1] === 'semantic'` with `--id`

### New CLI / deploy artifacts
- `src/lifecycle/router-lifecycle.mjs` moduleNames array — gains 'registry/substitute.mjs' entry
- `contract semantic` CLI subcommand — new inspection path with --id, --limit, --offset options

### New test files
- `tests/router.semantic-substitution.test.mjs` — SEM-04 coverage (10+ tests)
- `tests/router.semantic-inspection.test.mjs` — SEM-02 coverage (7+ tests)

### NOT created (scope boundary)
- `'substituted'` is NOT added to `RECEIPT_STATES` in `src/adapters/dispatch/contract.mjs` — Phase 44 (RCPT-02) owns that state. A test explicitly asserts the 8-state frozen array is unchanged.
- `src/registry/semantic.mjs` and `compileRelationshipGraph` are NOT created in this plan — Plan 01 creates them.
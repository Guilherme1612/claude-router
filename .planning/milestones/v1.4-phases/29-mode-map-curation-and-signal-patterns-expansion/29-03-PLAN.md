---
phase: 29-mode-map-curation-and-signal-patterns-expansion
plan: 03
type: execute
wave: 3
depends_on: [29-02]
files_modified:
  - /Users/guilherme/.claude/router/mode-map.json
  - tests/router.mode-map-curation.test.mjs
  - tests/router.route-targets.test.mjs
autonomous: true
requirements: [MAP-01, MAP-02, MAP-03, SIG-01, SIG-03]

must_haves:
  truths:
    - "Implicit lifecycle intents route to all eight required gsd-* skills without spelling their names."
    - "Implicit design intents route to all ten required design skills using artifact/outcome language."
    - "Every old and new entry has at most six patterns and every duplicate is either removed or explicitly grouped."
    - "All targets validate against both the live inventory and the neutral synthetic manifest; missing-MCP agents stay warning-only."
  artifacts:
    - path: "/Users/guilherme/.claude/router/mode-map.json"
      provides: "Schema v3 curated lifecycle/design routing data"
      contains: "\"schema_version\": 3"
    - path: "tests/router.mode-map-curation.test.mjs"
      provides: "Portable 18-route and hard-negative evidence"
    - path: "tests/router.route-targets.test.mjs"
      provides: "Live target and blocked-agent validation"
  key_links:
    - from: "/Users/guilherme/.claude/router/mode-map.json"
      to: "tests/router.mode-map-curation.test.mjs"
      via: "same curated entries evaluated against synthetic manifest"
      pattern: "gsd-ship|brandkit|excalidraw-diagram"
    - from: "/Users/guilherme/.claude/router/mode-map.json"
      to: "validateRouteTargets"
      via: "typed skill targets"
      pattern: "\"invoke_kind\": \"skill\""
---

<objective>
Curate the installed schema-v3 mode map for the required lifecycle and design routes.

Purpose: Improve auto-suggestion coverage with short, output-anchored evidence while preserving target safety and portability.
Output: A pruned v3 mode map and green synthetic/live route validation.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/29-mode-map-curation-and-signal-patterns-expansion/29-RESEARCH.md
@.planning/phases/29-mode-map-curation-and-signal-patterns-expansion/29-PATTERNS.md
@.planning/phases/29-mode-map-curation-and-signal-patterns-expansion/29-02-SUMMARY.md
@/Users/guilherme/.claude/router/mode-map.json
@tests/router.mode-map-curation.test.mjs
@tests/router.route-targets.test.mjs
@coverage-report.json

<interfaces>
Lifecycle targets: gsd-ship, gsd-new-project, gsd-execute-phase, gsd-quick, gsd-validate-phase, gsd-verify-work, gsd-resume-work, gsd-complete-milestone.
Design targets: brandkit, minimalist-ui, industrial-brutalist-ui, image-to-code, imagegen-frontend-web, imagegen-frontend-mobile, redesign-existing-projects, stitch-design-taste, excalidraw-diagram, gpt-taste.
Each is `invoke_kind: "skill"` with itself as the typed recommended skill because the live manifest exposes skills rather than matching commands.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Curate lifecycle routes and prune the existing map</name>
  <files>/Users/guilherme/.claude/router/mode-map.json, tests/router.mode-map-curation.test.mjs</files>
  <behavior>
    - Eight lifecycle positives select their expected skill.
    - Lifecycle near-collisions do not select a sibling workflow.
    - No entry in the complete map exceeds six patterns.
    - Patterns describe requested outputs/state transitions rather than skill names or broad action verbs.
  </behavior>
  <action>Bump the installed map to schema version 3. First prune every existing entry to its strongest one-to-six artifact/outcome phrases using the generated coverage and Wave 0 cases as evidence; preserve behavior required by existing calibration fixtures. Add exactly the eight MAP-01 skill-target entries because these are the requirement's enumerated unacknowledged manifest gaps and together cover ship/create/execute/quick/validate/verify/resume/complete outputs; the Phase 28 ranking provides no distinct extra lifecycle output with fixture evidence. Keep contains patterns as strings and use object form only when collision metadata is required.</action>
  <verify>
    <automated>rtk node --test tests/router.mode-map-curation.test.mjs tests/router.mode-map-v3.test.mjs tests/router.calibration-coverage.test.mjs</automated>
  </verify>
  <done>All eight lifecycle cases and existing route-coverage contracts pass with no entry above six patterns.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Curate design routes and declare the real collision</name>
  <files>/Users/guilherme/.claude/router/mode-map.json, tests/router.mode-map-curation.test.mjs</files>
  <behavior>
    - Ten design positives select their expected skill.
    - Minimalist, brutalist, redesign, image generation, image-to-code, taste, brand, and diagram siblings remain distinguishable.
    - The existing `"redesign the ui"` duplicate is either made unique or both occurrences explicitly declare one shared collision group.
    - No other undeclared canonical duplicate remains.
  </behavior>
  <action>Add exactly the ten MAP-02 skill-target entries using at most six output-specific phrases each. Preserve existing contains-only BM25 semantics. For the current duplicate, prefer unique output phrases; if both routes genuinely require `"redesign the ui"`, represent both as `contains` objects with the identical non-empty collision group. Do not use other kinds, bare collision booleans, skill-name signals, regex, or priority as a substitute for disambiguation.</action>
  <verify>
    <automated>rtk node --test tests/router.mode-map-curation.test.mjs tests/router.mode-map-v3.test.mjs</automated>
  </verify>
  <done>All ten design routes and hard negatives pass, with only explicitly grouped semantic duplicates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Validate live and synthetic target safety</name>
  <files>tests/router.mode-map-curation.test.mjs, tests/router.route-targets.test.mjs</files>
  <behavior>
    - All curated entries resolve against the live global skill inventory.
    - The same entries resolve and route against the neutral synthetic inventory.
    - No fixture relies on live capability descriptions.
    - Missing-MCP agents remain excluded from candidates and dispatch targets.
  </behavior>
  <action>Complete the Wave 0 integration matrix using the production map entries with a synthetic manifest whose descriptions are deliberately generic. Assert all 18 typed targets exist in the fixture, positive and hard-negative decisions use only injected objects, live target validation remains green, and a blocked synthetic agent cannot be selected even when its description strongly overlaps a prompt.</action>
  <verify>
    <automated>rtk node --test tests/router.mode-map-curation.test.mjs tests/router.route-targets.test.mjs tests/router.coverage-audit.test.mjs</automated>
  </verify>
  <done>MAP-03 portability and blocked-agent safety pass for both installed and fixture inventories.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curated mode-map → scorer | Added lexical evidence can inflate or collide with sibling candidates. |
| installed targets → manifest inventory | Stale or blocked targets must not become dispatchable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-03 | Tampering | curated signals | high | mitigate | Six-pattern cap, hard negatives, canonical lint, explicit same-group exception only. |
| T-29-05 | Spoofing | skill-name signals | medium | mitigate | Author only implicit output/artifact phrases and exercise prompts without capability names. |
| T-29-06 | Elevation of Privilege | missing-MCP agents | critical | mitigate | Live and synthetic target validation plus adversarial blocked-agent route assertion. |
</threat_model>

<verification>
Run `rtk node --test tests/router.mode-map-v3.test.mjs tests/router.mode-map-curation.test.mjs tests/router.route-targets.test.mjs tests/router.coverage-audit.test.mjs tests/router.calibration-coverage.test.mjs`, then `rtk node router.calibrate.mjs` to record the pre-recalibration expanded-corpus evidence without changing thresholds yet.
</verification>

<success_criteria>
- MAP-01 and MAP-02 all 18 named targets route from implicit intent.
- SIG-01 and SIG-03 hold across the entire installed map.
- MAP-03 passes against live and non-GSD synthetic inventories.
- No new dependency, extra route, parser, or engine is introduced.
</success_criteria>

<output>
Create `.planning/phases/29-mode-map-curation-and-signal-patterns-expansion/29-03-SUMMARY.md` when done.
</output>

---
phase: 40-project-identity-leases-continuity-and-safe-resume
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lease/identity.mjs
  - src/lease/store.mjs
  - src/lease/policy.mjs
  - tests/router.lease-identity.test.mjs
  - tests/router.lease-creation.test.mjs
  - tests/router.lease-inspect.test.mjs
autonomous: true
requirements: [LEASE-01, LEASE-02, LEASE-03]
user_setup: []
deferred:
  - "max_wall_ms/max_tokens enforcement — resource_bounds fields are inspectable per LEASE-03 (buildLeaseRecord emits { max_wall_ms, max_invocations, max_tokens }); isExpired enforces expiry.deterministic_at_ms + max_invocations only for v1. max_wall_ms/max_tokens enforcement deferred to Phase 41+. Conscious deferral — fields present, enforcement partial."
estimate:
  tokens: 58000
  raw_tokens: 29000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A lease created under runtime=claude with repo A / worktree W / schema-gen G yields a computeLeaseFingerprint hash that differs from one under runtime=codex, or repo B, or worktree X, or schema-gen H, or a different goal; findByFingerprint matches only the exact six-axis fingerprint (LEASE-01 identity binding)."
    - "Only a prompt classified as persistent_goal_action AND carrying an explicit operator instruction produces a lease record via shouldCreateLease; one_turn_action, advice, inspection, and non_authorizing_discussion never create a lease (LEASE-02)."
    - "inspect(lease_id) returns all 9 fields — goal, scope, allowed_effects, confirmation_effects, resource_bounds, status, expiry, authority_source, last_safe_checkpoint, freshness_evidence — and reflects durable status transitions active/paused/completed/expired/revoked (LEASE-03)."
    - "No lease record stores raw prompt text; the goal field is a short structured operator-declared label and any prompt-derived value uses hashPromptDerived redact-then-hash (privacy contract, Pitfall 5)."
    - { statement: "LEASE-01 'unclassified' edge — the six-axis fingerprint (repo, worktree, runtime, goal, schema_generation, project_fingerprint) is sufficient for v1; no additional binding axis (e.g. branch, user identity) is required.", verification: backstop }
    - { statement: "LEASE-02 concurrency edge — lease creation under two concurrent prompts for the same fingerprint: the second createLease is an idempotent no-op returning the existing lease_id, not an error or a second record.", verification: backstop }
    - { statement: "LEASE-03 adjacency edge — two leases with identical six-axis fingerprints are the SAME lease (one record per fingerprint); a second createLease returns the existing lease_id rather than creating a duplicate.", verification: backstop }
    - { statement: "LEASE-03 empty edge — empty goal or empty scope axes produce a fingerprint over the empty-string axis values (no special-case); a null projectFingerprint is rejected with an invalid-input reason rather than hashed.", verification: backstop }
    - { statement: "LEASE-03 ordering edge — claimed_actions is insertion-order stable; inspect output field order follows the 9-field schema declaration order (goal, scope, allowed_effects, confirmation_effects, resource_bounds, status, expiry, authority_source, last_safe_checkpoint, freshness_evidence).", verification: backstop }
  artifacts:
    - src/lease/identity.mjs
    - src/lease/store.mjs
    - src/lease/policy.mjs
    - tests/router.lease-identity.test.mjs
    - tests/router.lease-creation.test.mjs
    - tests/router.lease-inspect.test.mjs
  key_links:
    - "computeLeaseFingerprint({ repo, worktree, runtime, goal, schemaGeneration, projectFingerprint }) → sha256 hex over stableStringify of the six axes"
    - "defaultLeaseRoot(runtime) → ~/.<runtime>/router/leases (mirrors receipt.mjs defaultReceiptRoot, LEASE-01 cross-runtime isolation)"
    - "createLeaseStore({ root, runtime, lock }) → Object.freeze({ createLease, readLease, findByFingerprint, mutate, setStatus, isExpired, inspect })"
    - "shouldCreateLease({ authority_class, explicitInstruction }) → boolean (LEASE-02 gate)"
  prohibitions:
    - { statement: "Lease records MUST NOT store raw prompt text; goal is a short structured operator-declared label and any prompt-derived field uses hashPromptDerived (redact-then-hash) imported from receipt.mjs.", status: resolved, verification: backstop }
    - { statement: "A lease created under one runtime/repo/worktree/schema-gen MUST NOT authorize work for a different project fingerprint (LEASE-01 isolation).", status: resolved, verification: backstop }
    - { statement: "one_turn_action, advice, inspection, and non_authorizing_discussion MUST NOT create a lease (LEASE-02); only persistent_goal_action + explicit instruction creates one.", status: resolved, verification: backstop }
    - { statement: "Lease store MUST NOT hardcode /Users/guilherme; use os.homedir() (CLAUDE.md constraint).", status: resolved, verification: backstop }
---

<objective>
Build the lease identity + durable store + creation-gate + inspection foundation (LEASE-01, LEASE-02, LEASE-03) as a new `src/lease/` module family that reuses the authority taxonomy, atomic-write, mutation-lock, and per-runtime partition patterns proven in Phases 38/39. This is the tracer slice for Phase 40: it proves the lease storage layer end-to-end (fingerprint → create → read → inspect) before any hot-path wiring, durable resume, or briefing work.

Purpose: Establish the inspectable, revocable, expiry-bounded lease record that downstream plans (02: authority precedence + resume, 03: briefing + deploy) build on. Without a durable, schema'd, identity-bound store, LEASE-04/05/06 have nothing to act on.

Output: Three new stdlib-only modules (`identity.mjs`, `store.mjs`, `policy.mjs`) and three new test files proving cross-axis identity rejection, the creation gate, and 9-field inspection with status transitions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-project-identity-leases-continuity-and-safe-resume/40-RESEARCH.md
@.planning/phases/40-project-identity-leases-continuity-and-safe-resume/40-PATTERNS.md
@src/registry/fingerprint.mjs
@src/registry/schema.mjs
@src/steward/state.mjs
@src/adapters/dispatch/receipt.mjs
@src/intent/authority.mjs
@tests/router.steward-state.test.mjs
@tests/router.authority.test.mjs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Lease identity + durable store tracer (LEASE-01)</name>
  <files>src/lease/identity.mjs, src/lease/store.mjs, tests/router.lease-identity.test.mjs</files>
  <read_first>
    - src/registry/fingerprint.mjs (computeCompositeEpoch pattern: stableStringify + createHash, order-canonicalized, volatile fields excluded)
    - src/registry/schema.mjs (stableStringify, SCOPES, validateScope)
    - src/steward/state.mjs (durableWrite lines 81-89, mutationLock lines 47-79, createStewardStore factory lines 109-130)
    - src/adapters/dispatch/receipt.mjs (defaultReceiptRoot lines 66-69 per-runtime partition, redact/hashPromptDerived lines 31-41)
    - src/intent/authority.mjs (AUTHORITY_CLASSES, PERSISTENT_GOAL_MARKERS)
    - tests/router.steward-state.test.mjs (mkdtempSync fixture + cleanup + privacy/idempotency assertions)
  </read_first>
  <behavior>
    - computeLeaseFingerprint over {repo:'A',worktree:'W',runtime:'claude',goal:'g',schemaGeneration:1,projectFingerprint:'pf1'} differs from runtime='codex', repo='B', worktree='X', schemaGeneration:2, goal:'h', projectFingerprint:'pf2' (six axes each independently change the hash)
    - defaultLeaseRoot('claude') ends with '.claude/router/leases'; defaultLeaseRoot('codex') ends with '.codex/router/leases'
    - createLeaseStore({root}) writes <lease_id>.json with mode 0o600 under a 0o700 dir; createLease returns {status:'stored', lease_id}; readLease(lease_id) returns the record; readLease on a corrupt file returns null (fail-closed)
    - findByFingerprint(fp) returns the lease only on exact six-axis match; a different runtime/repo/worktree/schema-gen/goal fingerprint returns null
    - No lease field stores unredacted operator-prompt content
  </behavior>
  <action>
    Create src/lease/identity.mjs: export computeLeaseFingerprint({ repo, worktree, runtime, goal, schemaGeneration, projectFingerprint }) returning createHash('sha256').update(stableStringify({ repo, worktree, runtime, goal, schema_generation: schemaGeneration, project_fingerprint: projectFingerprint }), 'utf8').digest('hex'). Import createHash from node:crypto and stableStringify from ../registry/schema.mjs. Pure function — no fs/os import, receives all axes as args (mirrors computeCompositeEpoch's pure shape). Reject null/undefined projectFingerprint by throwing an Error('invalid_project_fingerprint') rather than hashing a null axis.

    Create src/lease/store.mjs: export defaultLeaseRoot(runtime) mirroring receipt.mjs defaultReceiptRoot (runtime === 'codex' ? '.codex' : '.claude' joined under homedir()/router/leases). Export createLeaseStore({ root, runtime, lock } = {}) factory: resolve root via defaultLeaseRoot(runtime) when not provided; mkdirSync root recursive mode 0o700. Reuse the mutationLock (mkdir-based, stale-PID recovery via process.kill(pid,0)) and durableWrite (temp+fsync+rename+dir-fsync) patterns VERBATIM from src/steward/state.mjs — copy the helpers into store.mjs (do not import state.mjs internals; the lease store is self-contained for deploy like authority.mjs). Implement one-lease-per-file at <root>/<lease_id>.json. Public surface via Object.freeze: createLease(record) writes the full record under lease_id (durableWrite), returns {status:'stored', lease_id}; readLease(leaseId) readFileSync try/catch → null on missing/corrupt (fail-closed, mirrors readState); findByFingerprint(fp) scans readdirSync for *.json, readLease each, return first whose project_fingerprint === fp (no lock — read-only); mutate(leaseId, cb) wraps readLease → cb → durableWrite under mutationLock, returns {status:'stored'|'unchanged'|'blocked', ...result.data}. The lease_id is record.lease_id (a sha256 from computeLeaseFingerprint). createLease is idempotent: if readLease(lease_id) exists and fingerprint matches, return {status:'unchanged', lease_id} (no duplicate, per LEASE-03 adjacency assumption).

    Write tests/router.lease-identity.test.mjs using the mkdtempSync(tmpdir()) fixture pattern from router.steward-state.test.mjs. Cover LEASE-01: assert computeLeaseFingerprint changes when each of the six axes changes independently (six cases); assert defaultLeaseRoot('claude') and defaultLeaseRoot('codex') paths; assert createLeaseStore creates a 0o700 root + 0o600 file; assert createLease → readLease round-trip; assert readLease on a corrupt file (writeFileSync '{bad') returns null; assert findByFingerprint matches only the exact fingerprint (create lease under runtime=claude/repoA, then findByFingerprint with a codex-fingerprinted lease's fp returns null). No unredacted operator-prompt content in any field.
  </action>
  <verify>
    <automated>rtk node --test tests/router.lease-identity.test.mjs</automated>
  </verify>
  <done>lease-identity test green: six-axis fingerprint independence proven, per-runtime partition paths correct, create/read round-trip works, corrupt-file read fails closed, cross-fingerprint findByFingerprint returns null.</done>
  <acceptance_criteria>
    - src/lease/identity.mjs contains `export function computeLeaseFingerprint`
    - src/lease/store.mjs contains `export function defaultLeaseRoot` and `export function createLeaseStore`
    - tests/router.lease-identity.test.mjs exits 0 via `rtk node --test`
    - computeLeaseFingerprint produces distinct hashes for all six single-axis perturbations
    - readLease on a corrupt JSON file returns null (no throw)
    - No test fixture stores unredacted operator-prompt content (grep -c 'unredactedOperatorPrompt' on test file == 0)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lease creation gate + 9-field record builder (LEASE-02)</name>
  <files>src/lease/policy.mjs, tests/router.lease-creation.test.mjs</files>
  <read_first>
    - src/intent/authority.mjs (AUTHORITY_CLASSES lines 13-19 frozen, PERSISTENT_GOAL_MARKERS lines 35-39, classifyAuthority lines 110-155 returning authority_class)
    - src/lease/identity.mjs (computeLeaseFingerprint signature from Task 1)
    - src/lease/store.mjs (createLeaseStore, createLease from Task 1)
    - src/adapters/dispatch/receipt.mjs (redact/hashPromptDerived for the no-raw-prompt rule)
    - tests/router.authority.test.mjs (authority classification + reason_code assertion pattern)
  </read_first>
  <behavior>
    - shouldCreateLease({authority_class:'persistent_goal_action', explicitInstruction:true}) → true
    - shouldCreateLease({authority_class:'one_turn_action', explicitInstruction:true}) → false
    - shouldCreateLease({authority_class:'advice', explicitInstruction:true}) → false
    - shouldCreateLease({authority_class:'inspection', explicitInstruction:true}) → false
    - shouldCreateLease({authority_class:'non_authorizing_discussion', explicitInstruction:true}) → false
    - shouldCreateLease({authority_class:'persistent_goal_action', explicitInstruction:false}) → false
    - buildLeaseRecord produces an object with exactly the 9 inspection fields + schema_version + policy_version + lease_id + claimed_actions:[]
    - goal field is a short structured label, never the raw prompt
  </behavior>
  <action>
    Create src/lease/policy.mjs. Export LEASE_POLICY_VERSION = 'lease-policy-v1' (frozen string). Import AUTHORITY_CLASSES from ../intent/authority.mjs (do NOT redefine the enum or the PERSISTENT_GOAL_MARKERS regex — frozen-vocabulary rule). Export shouldCreateLease({ authority_class, explicitInstruction } = {}) returning true ONLY when authority_class === 'persistent_goal_action' && explicitInstruction === true; all other AUTHORITY_CLASSES and explicitInstruction:false → false. Validate authority_class against AUTHORITY_CLASSES (unknown class → false, not a throw — fail-closed gate).

    Export buildLeaseRecord({ fingerprint, goal, scope, allowedEffects, confirmationEffects, resourceBounds, expiryMs, authoritySource, checkpoint }) assembling the lease record with EXACTLY these keys: schema_version:1, policy_version:LEASE_POLICY_VERSION, lease_id: fingerprint, project_fingerprint: fingerprint, goal (short structured label — NOT raw prompt), scope { repo, worktree, runtime, schema_generation }, allowed_effects (array), confirmation_effects (array from PROTECTED_EFFECT_TOKENS class), resource_bounds { max_wall_ms, max_invocations, max_tokens }, status:'active', expiry { deterministic_at_ms: expiryMs, tz:'UTC' }, authority_source { kind, instruction, class }, last_safe_checkpoint { receipt_id, action_id, state, at_ms } | null, freshness_evidence { lease_mtime_ms: Date.now(), fingerprint_match: true }, claimed_actions: []. If goal contains natural-language prompt text longer than a short label, the caller is misusing the API — do not sanitize here, but the tests assert a structured goal is passed. Do NOT import or call hashPromptDerived inside buildLeaseRecord (the goal is structured, not prompt-derived); document in a leading comment that prompt-derived fields, if ever needed, must use hashPromptDerived from receipt.mjs.

    Write tests/router.lease-creation.test.mjs: assert shouldCreateLease truth table (persistent_goal_action+explicit=true; all four other classes=false; explicit=false=false). Assert buildLeaseRecord returns all 9 inspection fields with status 'active' and claimed_actions []. Assert LEASE_POLICY_VERSION === 'lease-policy-v1'. Assert AUTHORITY_CLASSES is imported (not redefined) — `authority.mjs.AUTHORITY_CLASSES === policy.mjs` re-export if any. Use the tmpdir fixture pattern; create a lease via createLeaseStore + buildLeaseRecord + createLease and readLease it back — assert the stored record round-trips all 9 fields.
  </action>
  <verify>
    <automated>rtk node --test tests/router.lease-creation.test.mjs</automated>
  </verify>
  <done>Creation gate rejects all non-persistent-goal classes and missing explicit instruction; buildLeaseRecord produces the full 9-field record that round-trips through the store.</done>
  <acceptance_criteria>
    - src/lease/policy.mjs contains `export const LEASE_POLICY_VERSION` and `export function shouldCreateLease` and `export function buildLeaseRecord`
    - policy.mjs does NOT redefine AUTHORITY_CLASSES or PERSISTENT_GOAL_MARKERS (imports from ../intent/authority.mjs)
    - tests/router.lease-creation.test.mjs exits 0
    - shouldCreateLease returns false for all four non-persistent classes and for explicitInstruction:false
    - buildLeaseRecord output contains all 9 keys: goal, scope, allowed_effects, confirmation_effects, resource_bounds, status, expiry, authority_source, last_safe_checkpoint, freshness_evidence
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 9-field inspection + status/expiry transitions (LEASE-03)</name>
  <files>src/lease/store.mjs, tests/router.lease-inspect.test.mjs</files>
  <read_first>
    - src/lease/store.mjs (createLeaseStore, mutate from Task 1)
    - src/lease/policy.mjs (buildLeaseRecord, LEASE_POLICY_VERSION from Task 2)
    - src/steward/state.mjs (mutate callback returns {changed, data} shape)
    - tests/router.steward-state.test.mjs (atomic write + idempotency + fail-closed assertion pattern)
  </read_first>
  <behavior>
    - inspect(lease_id) returns an object with all 9 fields plus computed is_expired and is_revoked booleans
    - setStatus(lease_id, 'revoked') → readLease(lease_id).status === 'revoked' (durable)
    - setStatus(lease_id, 'completed') → status === 'completed' (durable)
    - setStatus is idempotent (second call with same status → {status:'unchanged'})
    - isExpired(lease, now) returns true when expiry.deterministic_at_ms <= now; false otherwise
    - inspect on a missing lease_id returns null
  </behavior>
  <action>
    Add to createLeaseStore (src/lease/store.mjs): setStatus(leaseId, status) via mutate — cb reads lease, if lease.status === status return {changed:false}, else set lease.status = status and return {changed:true}; durableWrite under mutationLock. Valid status enum: active|paused|completed|blocked|expired|revoked (validate against this set; unknown status → {status:'blocked', reason_code:'invalid_status'}). Add isExpired(lease, now = Date.now()) pure predicate: true when lease.expiry.deterministic_at_ms <= now OR any resource_bound exceeded (max_invocations when claimed_actions.length >= max_invocations, max_wall_ms when last_safe_checkpoint.at_ms + max_wall_ms <= now — keep the bound check minimal: max_invocations only for v1, document max_wall_ms/max_tokens as future). Add inspect(leaseId) returning readLease plus computed { is_expired: isExpired(lease), is_revoked: lease.status === 'revoked' } or null when readLease returns null. Keep the read-only methods (readLease, findByFingerprint, inspect) lock-free; only mutate/setStatus acquire the mutation lock.

    Write tests/router.lease-inspect.test.mjs: create a lease (buildLeaseRecord with expiryMs = NOW + 60_000), createLease, inspect → assert all 9 fields present with status 'active', is_expired false, is_revoked false. setStatus('paused') → inspect.status === 'paused'. setStatus('completed') → status 'completed'. setStatus('completed') again → {status:'unchanged'} (idempotent). setStatus('revoked') → status 'revoked', inspect.is_revoked true. Set expiryMs = NOW - 1 → isExpired true, inspect.is_expired true. inspect('nonexistent') → null. Assert durableWrite leaves no .tmp- files in the root (readdirSync filter, mirrors steward-state test).
  </action>
  <verify>
    <automated>rtk node --test tests/router.lease-inspect.test.mjs</automated>
  </verify>
  <done>inspect surfaces all 9 fields plus is_expired/is_revoked; setStatus transitions are durable and idempotent; isExpired enforced at the deterministic wall-clock deadline; missing lease inspects to null.</done>
  <acceptance_criteria>
    - src/lease/store.mjs contains `setStatus` and `isExpired` and `inspect` on the createLeaseStore return object
    - tests/router.lease-inspect.test.mjs exits 0
    - inspect output contains all 9 schema fields + is_expired + is_revoked
    - setStatus('revoked') persisted across a fresh readLease (durable)
    - setStatus idempotent (second call returns status:'unchanged')
    - readdirSync(root).filter(name => name.includes('.tmp-')).length === 0 after mutations
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lease record → authority resolution | A lease record is the authority source for persistent-goal work; a foreign or tampered record crossing this boundary could authorize unintended work. |
| disk → lease store read | Corrupt or tampered lease files on disk are read back; fail-closed null on parse error prevents garbage authorization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-01 | Spoofing | computeLeaseFingerprint / findByFingerprint | high | mitigate | Six-axis sha256 fingerprint; mismatch → lease not found → no authority (LEASE-01). Test asserts each axis independently changes the hash. |
| T-40-02 | Information disclosure | lease record goal field | high | mitigate | Goal is a short structured label, never raw prompt; documented + tested. Prompt-derived fields, if ever added, use hashPromptDerived from receipt.mjs (Pitfall 5). |
| T-40-03 | Tampering | lease store read path | medium | mitigate | readLease fail-closed: try/catch returns null on missing/corrupt JSON; no garbage record ever authorizes work. |
| T-40-04 | Elevation of privilege | shouldCreateLease gate | high | mitigate | Only persistent_goal_action + explicit instruction creates a lease; one_turn_action and the three non-authorizing classes are rejected; unknown class → false (fail-closed). |
| T-40-SC | Tampering | npm/pip/cargo installs | high | accept | Stdlib-only phase — no packages installed (CLAUDE.md hard constraint). No supply-chain surface. |
</threat_model>

<verification>
- `rtk node --test tests/router.lease-identity.test.mjs tests/router.lease-creation.test.mjs tests/router.lease-inspect.test.mjs` all green
- `rtk node --test tests/router.authority.test.mjs tests/router.authority-policy.test.mjs` unchanged (Phase 39 sealed evaluator not modified — regression backstop)
- No lease module imports `node:http`, `node:net`, or any npm package (grep gate)
- `src/lease/store.mjs` does not hardcode `/Users/guilherme` (uses os.homedir())
</verification>

<success_criteria>
- LEASE-01: six-axis fingerprint independence + cross-runtime partition proven by test
- LEASE-02: only persistent_goal_action + explicit instruction creates a lease
- LEASE-03: inspect returns all 9 fields; status transitions durable and idempotent; expiry enforced
- Phase 39 authority tests remain green (sealed signature untouched)
- No raw prompt text in any lease record or test fixture
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

**New modules (src/lease/):**
- `computeLeaseFingerprint({ repo, worktree, runtime, goal, schemaGeneration, projectFingerprint })` → string (identity.mjs)
- `defaultLeaseRoot(runtime)` → string (store.mjs)
- `createLeaseStore({ root, runtime, lock })` → Object.freeze({ createLease, readLease, findByFingerprint, mutate, setStatus, isExpired, inspect }) (store.mjs)
- `LEASE_POLICY_VERSION = 'lease-policy-v1'` (policy.mjs, frozen string)
- `shouldCreateLease({ authority_class, explicitInstruction })` → boolean (policy.mjs)
- `buildLeaseRecord({ fingerprint, goal, scope, allowedEffects, confirmationEffects, resourceBounds, expiryMs, authoritySource, checkpoint })` → lease record (policy.mjs)

**New files:**
- src/lease/identity.mjs
- src/lease/store.mjs
- src/lease/policy.mjs
- tests/router.lease-identity.test.mjs
- tests/router.lease-creation.test.mjs
- tests/router.lease-inspect.test.mjs

**Reused (not redefined):** AUTHORITY_CLASSES, PERSISTENT_GOAL_MARKERS (imported from src/intent/authority.mjs); stableStringify (src/registry/schema.mjs); durableWrite + mutationLock patterns (copied from src/steward/state.mjs, kept self-contained for deploy).
</artifacts_produced>

<output>
Create `.planning/phases/40-project-identity-leases-continuity-and-safe-resume/40-01-SUMMARY.md` when done
</output>
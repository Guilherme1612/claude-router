---
phase: 30-foundation-manifest-fingerprint-watcher-narrowing
plan: 3
type: execute
wave: 3
depends_on: ["30-01", "30-02"]
files_modified:
  - tests/router.mjs.snapshot
  - tests/router.calibration-epoch.test.mjs
  - docs/inventory-lifecycle.md
  - tests/router.lifecycle-invc.test.mjs
autonomous: true
requirements: [INVC-03, INVC-05]
estimate:
  tokens: 50000
  raw_tokens: 25000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Calibration is epoch-keyed: a calibration file whose manifest_fingerprint matches the current manifest wins; a mismatch means mode-map default thresholds (0.591/0.291/0.191) win"
    - "An absent or corrupt calibration file falls back to mode-map defaults without throwing (fail-open on the hot path)"
    - "The add/update/remove capability lifecycle (watcher → rebuild → coverage audit → recompute → re-calibrate) is documented in docs/inventory-lifecycle.md"
    - "An end-to-end lifecycle test proves a capability add/remove propagates: fingerprint change → cache key change → stale route recomputed"
  artifacts:
    - tests/router.mjs.snapshot (CALIBRATION_PATH constant; loadEpochCalibration function; epoch-guarded threshold assignment)
    - tests/router.calibration-epoch.test.mjs (matching/mismatch/absent/corrupt calibration fixture tests)
    - docs/inventory-lifecycle.md (capability add/update/remove lifecycle documentation)
    - tests/router.lifecycle-invc.test.mjs (end-to-end add→rebuild→fingerprint→cache-recompute chain)
  key_links:
    - "snapshot loadEpochCalibration → state.thresholds assignment → mode-map defaults 0.591/0.291/0.191 on mismatch"
    - "build-manifest rebuild → manifest_fingerprint → cacheKey → inspectDecision cache miss (lifecycle e2e)"
  prohibitions:
    - statement: "Calibration must never mutate the curated mode-map — epoch-gated read only"
      status: unverified
      flagged: true
    - statement: "A fingerprint mismatch must never serve stale per-install thresholds — defaults win"
      status: unverified
      flagged: true
    - statement: "The hot path must never throw on calibration file read/parse errors — fail-open"
      status: unverified
      flagged: true
---

<objective>
Close the calibration epoch-keying (INVC-03) and document/test-verify the full add/update/remove capability lifecycle (INVC-05). Phase 34 consumes this epoch gate when it derives per-install thresholds; Phase 30 ships the epoch-guarded read mechanism with fixture-proven fallback semantics.

Purpose: Success criteria 4 and 5 of the phase. The threshold read must consult fingerprint-matched calibration and let mode-map defaults (0.591/0.291/0.191) win on any mismatch — never stale per-install thresholds. And the whole invalidation loop must be documented and proven end-to-end so v1.5 features have a trusted spine.
Output: `loadEpochCalibration()` + epoch-guarded threshold assignment in tests/router.mjs.snapshot; `tests/router.calibration-epoch.test.mjs`; `docs/inventory-lifecycle.md`; `tests/router.lifecycle-invc.test.mjs`.
</objective>

<execution_context>
@/Users/guilherme/.claude/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-foundation-manifest-fingerprint-watcher-narrowing/30-PATTERNS.md (threshold read gap note; snapshot threshold assignment L2684; readWeightsMtime try/catch shape L1683-1690; hook import seam)
@.planning/phases/30-foundation-manifest-fingerprint-watcher-narrowing/30-CONTEXT.md

# INVC-03 grounding: mode-map.json carries thresholds {T_high:0.591, T_low:0.291, M:0.191}.
# Snapshot currently assigns state.thresholds = modeMap.thresholds || {T_high:0.6, T_low:0.3, M:0.2}
# at L2684 — that assignment gains an epoch consult ahead of it. The calibration FILE is created by
# Phase 34; this phase ships the epoch-gated READ + fallback semantics, fixture-tested.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Epoch-guarded threshold read — fingerprint match wins, mismatch/absent/corrupt → mode-map defaults</name>
  <files>tests/router.mjs.snapshot, tests/router.calibration-epoch.test.mjs</files>
  <read_first>
    tests/router.mjs.snapshot — threshold assignment L2684, ROUTER_DIR/constants near L76, readWeightsMtime try/catch shape L1683-1690
    tests/router.mutation-safety.test.mjs — withTempDir + inspectDecision fixture seam (reuse pattern for the new test)
  </read_first>
  <behavior>
    - Test 1 (match): a calibration file whose manifest_fingerprint equals the manifest's fingerprint yields per-install thresholds (score_debug thresholds reflect them)
    - Test 2 (mismatch): a calibration file with a different manifest_fingerprint → mode-map defaults 0.591/0.291/0.191 win, calibration thresholds ignored
    - Test 3 (absent): no calibration file → mode-map defaults win, no throw
    - Test 4 (corrupt): a malformed calibration file (invalid JSON) → mode-map defaults win, no throw
  </behavior>
  <action>
    1. In tests/router.mjs.snapshot, add a constant `CALIBRATION_PATH = join(ROUTER_DIR, 'calibration.json')` near the other path constants (MANIFEST at L76). Add a best-effort loader shaped like `readWeightsMtime` (L1683-1690): `function loadEpochCalibration(manifestFingerprint, { calibrationPath = CALIBRATION_PATH } = {})` that reads and parses the file inside try/catch, and returns `{ matched: true, thresholds: parsed.thresholds }` only when `parsed.manifest_fingerprint === manifestFingerprint` and `parsed.thresholds` is an object; otherwise returns `{ matched: false, thresholds: null }`. Never throws.

    2. Replace the threshold assignment at L2684 (`state.thresholds = modeMap.thresholds || { T_high: 0.6, T_low: 0.3, M: 0.2 };`) with an epoch-guarded consult that runs AFTER `manifest` is loaded and the fingerprint is known: `const epochCal = loadEpochCalibration(manifestFingerprint); state.thresholds = epochCal.matched ? epochCal.thresholds : (modeMap.thresholds || { T_high: 0.591, T_low: 0.291, M: 0.191 });`. Update the hardcoded legacy fallback to 0.591/0.291/0.191 so the named roadmap defaults are the literal defaults. Keep `setModeMapForBlend(modeMap)` and the score_debug assignment intact.

    3. Create tests/router.calibration-epoch.test.mjs importing the INSTALLED hook (homedir join seam). For each of the four behaviors, use a withTempDir fixture: a temp manifest carrying `manifest_fingerprint: 'fp-1'` (and mode-map defaults 0.591/0.291/0.191), plus `{ calibrationPath }` pointing at a temp calibration.json. Run `inspectDecision('calibrate me', { manifest, modeMap, calibrationPath, mutateCache: false, logTelemetry: false })` and assert `out.score_debug.T_high/T_low/M` (or the thresholds the decision used). Match fixture: calibration.json `{ manifest_fingerprint: 'fp-1', thresholds: { T_high: 0.8, T_low: 0.4, M: 0.25 } }` → thresholds 0.8/0.4/0.25. Mismatch fixture: `manifest_fingerprint: 'fp-2'` → 0.591/0.291/0.191. Absent: no file → 0.591/0.291/0.191. Corrupt: `not json{` → 0.591/0.291/0.191 and no throw.

    4. Ensure `inspectDecision` forwards `opts.calibrationPath` to `loadEpochCalibration`; stage the hook (`node install-router.mjs`) before running the suite.
  </action>
  <verify>
    node install-router.mjs && node --test tests/router.calibration-epoch.test.mjs
  </verify>
  <acceptance_criteria>
    - Matching fingerprint → per-install thresholds used (score_debug reflects 0.8/0.4/0.25 in the fixture)
    - Mismatch, absent, and corrupt cases all resolve to mode-map defaults 0.591/0.291/0.191 and never throw
    - Calibration read never mutates mode-map.json or the calibration file
  </acceptance_criteria>
  <done>
    The epoch-guarded threshold read is live in the installed hook: matched calibration wins, everything else falls back to mode-map defaults — INVC-03 satisfied.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Document the add/update/remove lifecycle and prove it end-to-end</name>
  <files>docs/inventory-lifecycle.md, tests/router.lifecycle-invc.test.mjs</files>
  <read_first>
    build-manifest.mjs — build flow + coverage audit import L27, atomic write L541-545 (read-only reference)
    tests/router.build-manifest.test.mjs — runBuilder spawn seam L13-45 (replicate locally)
    tests/router.mutation-safety.test.mjs — inspectDecision fixture seam for the cache-recompute half
    src/coverage/audit.mjs — auditCoverage signature (referenced in docs, not modified)
  </read_first>
  <behavior>
    - Test 1: adding a skill to a temp home → rebuild → manifest_fingerprint changes → a cache entry written under the old fingerprint is a MISS on the next decision (route recomputed)
    - Test 2: removing a plugin from installed_plugins.json → rebuild → manifest_fingerprint changes again (removal bumps the epoch)
  </behavior>
  <action>
    1. Create docs/inventory-lifecycle.md documenting the full add/update/remove capability lifecycle with concrete steps and the files each stage touches:
       - Watcher: FS event → matched filter (ignoredRelativePaths noise narrowing, router-lifecycle.mjs roots) → markDirty
       - Rebuild: build-manifest.mjs → computeCompositeEpoch → manifest.manifest_fingerprint (timestamps/paths/counts excluded)
       - Coverage audit: auditCoverage (src/coverage/audit.mjs) runs on the rebuilt manifest
       - Recompute: cacheKey folds manifest_fingerprint → stale routes miss → recomputed
       - Re-calibrate: loadEpochCalibration gates thresholds; Phase 34 derives per-install values keyed by {manifest_fingerprint, mode_map_version, corpus_hash}; mismatch → 0.591/0.291/0.191 defaults
       Include the plugin-noise narrowing rule (installed_plugins.json authoritative; sqlite/WAL + plugin-catalog caches ignored).

    2. Create tests/router.lifecycle-invc.test.mjs (local runBuilder helper replicating router.build-manifest.test.mjs L13-45) proving the chain end-to-end:
       - Seed a temp home: one skill file under `.claude/skills/`, installed_plugins.json with one plugin. Build → read `manifest_fingerprint` F0 from the OUT manifest. Write a cache entry under `cacheKey('help me', [], F0)`.
       - Add a second skill file, rebuild → F1. Assert F1 !== F0. Run inspectDecision with the new manifest (manifest_fingerprint F1) and the seeded cache; assert `out.cache.status === 'miss'` (the F0-keyed entry is not served).
       - Append a plugin to installed_plugins.json, rebuild → F2. Assert F2 !== F1 and F2 !== F0 (remove direction is covered by 30-02 Task 2; this test proves the add direction end-to-end).
    3. Assert docs/inventory-lifecycle.md covers all five stages by grepping for the stage keywords in the doc.
  </action>
  <verify>
    node --test tests/router.lifecycle-invc.test.mjs
    grep -c 'watcher\|rebuild\|coverage audit\|recompute\|re-calibrate' docs/inventory-lifecycle.md
  </verify>
  <acceptance_criteria>
    - Skill add bumps F0 → F1 and the F0-keyed cache entry is a MISS (recomputed, not stale)
    - Plugin add bumps F1 → F2 (F2 differs from both F0 and F1)
    - docs/inventory-lifecycle.md names all five lifecycle stages (watcher, rebuild, coverage audit, recompute, re-calibrate)
  </acceptance_criteria>
  <done>
    The capability lifecycle is documented and the add-direction end-to-end chain is test-verified — INVC-05 satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| calibration.json → hook | untrusted local calibration input gating hot-path thresholds |
| manifest_fingerprint → thresholds | the epoch is the trust anchor deciding defaults vs per-install thresholds |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-08 | Tampering | calibration.json | low | accept | Local user-owned file; a forged file with a matching fingerprint could set thresholds, but its writer already owns mode-map.json/settings.json — no privilege escalation; on any mismatch defaults win |
| T-30-09 | Denial of Service | loadEpochCalibration | medium | mitigate | try/catch around read+parse; corrupt file → { matched:false } → defaults, never a throw on the hot path |
| T-30-10 | Tampering | docs/inventory-lifecycle.md | low | accept | Documentation only; no runtime effect |
| T-30-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase — stdlib-only |
</threat_model>

<verification>
- `node install-router.mjs && node --test tests/router.calibration-epoch.test.mjs` — match/mismatch/absent/corrupt threshold behaviors green
- `node --test tests/router.lifecycle-invc.test.mjs` — skill-add cache-recompute + plugin-add fingerprint bump chain green
- grep gate: docs/inventory-lifecycle.md contains all five lifecycle stage keywords
- Full-phase regression: `node --test tests/router.calibration-thresholds.test.mjs` still green (defaults unchanged elsewhere)
</verification>

<success_criteria>
- INVC-03: calibration is epoch-keyed by the fingerprint; mismatch → mode-map default thresholds (0.591/0.291/0.191) win, never stale per-install thresholds
- INVC-05: the add/update/remove capability lifecycle (watcher → rebuild → coverage audit → recompute → re-calibrate) is documented in docs/inventory-lifecycle.md and test-verified end-to-end
</success_criteria>

## Artifacts this phase produces

- `CALIBRATION_PATH` constant + `loadEpochCalibration(manifestFingerprint, { calibrationPath })` function + epoch-guarded threshold assignment in `tests/router.mjs.snapshot` (defaults 0.591/0.291/0.191)
- `opts.calibrationPath` — inspectDecision option in `tests/router.mjs.snapshot`
- `tests/router.calibration-epoch.test.mjs` — new test file (four fixture behaviors: match/mismatch/absent/corrupt)
- `docs/inventory-lifecycle.md` — new lifecycle documentation (watcher → rebuild → coverage audit → recompute → re-calibrate; plugin noise narrowing rule)
- `tests/router.lifecycle-invc.test.mjs` — new end-to-end test file (skill-add → fingerprint bump → cache miss; plugin-add → fingerprint bump)

<output>
Create `.planning/phases/30-foundation-manifest-fingerprint-watcher-narrowing/30-03-SUMMARY.md` when done
</output>

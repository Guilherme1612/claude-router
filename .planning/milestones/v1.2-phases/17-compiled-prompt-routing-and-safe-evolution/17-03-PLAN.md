---
phase: 17-compiled-prompt-routing-and-safe-evolution
plan: 03
type: execute
wave: 3
depends_on: ["17-01", "17-02"]
files_modified:
  - src/evolution/canary-controller.mjs
  - src/evolution/perf-measure.mjs
  - tests/router.evolution-canary.test.mjs
  - tests/router.perf-calibration.test.mjs
  - tests/router.compiled-evolution.test.mjs
autonomous: true
requirements: [EVO-05, REL-01]
nyquist_compliant: true
must_haves:
  truths:
    - "D-11/D-12: hard failures reject or roll back immediately, quality regression rolls back automatically, and publication reuses the durable journal, immutable versions, atomic pointer, known-good state, and recovery path."
    - "D-13/D-15: all seven route/context fixture classes use a fixed versioned corpus with deterministic expected outcomes and exact evaluated versions."
    - "D-14/REL-01: warm p95 is below 25 ms and every measured route is below 100 ms as an independent hard gate."
    - "D-16: faster or smaller candidates promote only after correctness and only with demonstrated benefit or a separately justified safety/reliability correction."
  artifacts:
    - path: src/evolution/canary-controller.mjs
      provides: "Automatic promotion, rejection, and rollback through existing registry publication primitives"
    - path: src/evolution/perf-measure.mjs
      provides: "Deterministic cold/warm measurement and percentile summaries"
    - path: tests/router.perf-calibration.test.mjs
      provides: "Fixed-corpus quality, context-budget, and REL-01 latency gates"
    - path: tests/router.compiled-evolution.test.mjs
      provides: "End-to-end compiled routing, canary, promotion, and rollback verification"
  key_links:
    - from: src/evolution/canary-controller.mjs
      to: src/registry/activate.mjs
      via: "existing journaled atomic activation and rollback API"
    - from: src/evolution/perf-measure.mjs
      to: tests/router.perf-calibration.test.mjs
      via: "fixed corpus cold/warm measurements and versioned deltas"
    - from: tests/router.compiled-evolution.test.mjs
      to: src/context/prompt-route.mjs
      via: "end-to-end live routing lifecycle"
---

<objective>
Complete safe evolution with automatic rollback and prove routing quality, context budgets, and REL-01 latency on a fixed corpus under D-11 through D-16.

Purpose: Prevent silent regressions by making correctness and latency independent hard gates and by publishing only through the existing atomic recovery mechanism.
Output: Publication/rollback wiring, deterministic performance harness, and end-to-end phase verification.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-compiled-prompt-routing-and-safe-evolution/17-CONTEXT.md
@.planning/phases/17-compiled-prompt-routing-and-safe-evolution/17-RESEARCH.md
@src/evolution/canary-controller.mjs
@src/registry/activate.mjs
@router.calibrate.mjs
@tests/router.registry-activation.test.mjs
@tests/router.context-budget.test.mjs
@tests/router.workflow-orchestrator.test.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire promotion and automatic rollback through existing publication primitives</name>
  <files>src/evolution/canary-controller.mjs, tests/router.evolution-canary.test.mjs</files>
  <behavior>
    - Safety, privacy, corruption, compatibility, or latency-ceiling failure immediately rejects an unpublished candidate or rolls back a published canary.
    - Quality regression across the evidence window rolls back automatically; uncertainty preserves current known-good state.
    - Readers observe either the previous or new immutable version, never partial publication.</behavior>
  <action>Write rejection, post-promotion regression, crash-boundary, and recovery tests first. Extend the controller to call the existing `src/registry/activate.mjs` durable journal, immutable-version, atomic-pointer, known-good, and rollback primitives per D-12; do not introduce a second publication format. Enforce D-11 immediate hard-failure behavior and automatic evidence-window quality rollback, recording stable verdict/reason codes.</action>
  <verify>
    <automated>node --test tests/router.evolution-canary.test.mjs tests/router.registry-activation.test.mjs</automated>
  </verify>
  <done>Every failure class deterministically rejects or rolls back, crash recovery preserves the last verified version, and readers never observe partial publication.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Establish the fixed calibration corpus and independent latency harness</name>
  <files>src/evolution/perf-measure.mjs, tests/router.perf-calibration.test.mjs</files>
  <behavior>
    - A byte-locked versioned corpus covers minimal-prompt, explicit-override, stale-context, ambiguity, terminal-state, dependency, and context-budget fixtures with deterministic expected outcomes.
    - Representative cold and warm runs record baseline delta plus exact candidate, compiled-index, policy, and corpus versions.
    - Warm p95 is below 25 ms and every measured route is below 100 ms; quality and latency failures remain independent.</behavior>
  <action>Write corpus-integrity, expected-outcome, context-budget, percentile, and ceiling tests first. Implement a standard-library measurement helper using monotonic time, warmup exclusion, deterministic percentile calculation, and bounded output. Per D-13/D-15, lock all seven fixture classes and exact evaluated versions. Per D-14 and REL-01, assert p95 and per-route ceilings independently of quality; avoid a compensating weighted score.</action>
  <verify>
    <automated>node --test tests/router.perf-calibration.test.mjs</automated>
  </verify>
  <done>The versioned corpus deterministically protects every required route/context class, and measured warm routing satisfies p95 under 25 ms with every route under 100 ms.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove the compiled evolution lifecycle end to end</name>
  <files>tests/router.compiled-evolution.test.mjs, tests/router.evolution-canary.test.mjs, tests/router.perf-calibration.test.mjs</files>
  <behavior>
    - Valid compiled routing emits privacy-safe evidence, produces an immutable candidate, passes canary gates, and atomically promotes only when demonstrated benefit exists.
    - A faster neutral candidate does not auto-promote; a quality or latency regression automatically restores known-good state.
    - Phase 16 selection, dispatch eligibility, explicit overrides, ambiguity handling, and context budgets remain unchanged.</behavior>
  <action>Create an end-to-end temporary-project fixture that exercises Plans 01–03 through public seams. Assert D-16 benefit semantics, including the separately justified safety/reliability correction path, and automatic rollback on both semantic and latency regressions. Cross-check the seven fixed fixture classes against Phase 16 outputs and ensure the recorded telemetry remains D-05/D-06 compliant.</action>
  <verify>
    <automated>node --test tests/router.compiled-evolution.test.mjs tests/router.evolution-canary.test.mjs tests/router.perf-calibration.test.mjs tests/router.workflow-orchestrator.test.mjs</automated>
  </verify>
  <done>EVO-05 and REL-01 pass together: beneficial safe candidates promote, regressions roll back automatically, neutral speedups do not promote, and inherited routing semantics remain intact.</done>
</task>

</tasks>

<verification>
Run the focused commands above, then `node --test tests/*.test.mjs`. Passing tests are execution evidence; the phase still requires the independent phase verifier before completion.
</verification>

<success_criteria>
- D-11 through D-16 are executable and covered.
- EVO-05 canaries promote safely and roll back regressions automatically.
- REL-01 latency gates pass without masking any quality failure.
- All Phase 17 roadmap fixture and context-budget criteria remain deterministic.</success_criteria>


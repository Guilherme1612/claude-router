---
phase: 25-advisory-stewardship-and-guarded-drafts
plan: 04
type: execute
wave: 4
depends_on: [25-03]
files_modified:
  - src/steward/refresh.mjs
  - src/steward/startup-pointer.mjs
  - src/health/observe.mjs
  - src/health/admin.mjs
  - src/cli/router-control.mjs
  - src/context/prompt-route.mjs
  - tests/router.steward-startup.test.mjs
  - tests/router.health.privacy.test.mjs
autonomous: true
requirements: [UX-01, UX-02, UX-03, UX-04, UX-05, UX-06, UX-07, UX-08, UX-09]
must_haves:
  truths:
    - "UX-01/UX-02: the concrete off-hot-path refreshSuggestionPointer producer atomically recomputes one bounded pointer after every accepted evidence append, health reset/dispose/recover, and suggestion interaction; startup reads only that pointer."
    - "Startup performs no health derivation, outcome/history read, filesystem discovery/enumeration, network request, model call, or steward mutation."
    - "Dismissal, snooze, cooldown, no qualifying item, missing pointer, and corrupt pointer all produce silence."
    - "UX-08/UX-09: startup never mutates capability/routing state and never exposes evidence, lists, dashboards, timelines, or summaries."
  artifacts:
    - path: src/steward/refresh.mjs
      provides: "Concrete off-hot-path producer that loads fixed authoritative inputs, derives the current catalog, applies advisory state, and atomically publishes availability."
      exports: [refreshSuggestionPointer]
    - path: src/steward/startup-pointer.mjs
      provides: "Atomic off-path pointer compiler and bounded read-only startup loader."
      exports: [compileStartupPointer, loadStartupPointer]
    - path: src/context/prompt-route.mjs
      provides: "Startup surface consuming only the bounded pointer."
    - path: tests/router.steward-startup.test.mjs
      provides: "Nyquist coverage for silence, exact line, corruption, I/O bounds, and hot-path isolation."
  key_links:
    - from: src/health/observe.mjs
      to: src/steward/refresh.mjs
      via: "A successfully stored outcome invokes refreshSuggestionPointer after persistence, never during UserPromptSubmit."
      pattern: "refreshSuggestionPointer"
    - from: src/health/admin.mjs
      to: src/steward/refresh.mjs
      via: "Reset, dispose, and recover refresh or clear availability after their atomic state change."
      pattern: "refreshSuggestionPointer"
    - from: src/cli/router-control.mjs
      to: src/steward/refresh.mjs
      via: "Suggestion inspect and interaction actions refresh pointer availability after advisory-state evaluation."
      pattern: "refreshSuggestionPointer"
    - from: src/context/prompt-route.mjs
      to: src/steward/startup-pointer.mjs
      via: "Prompt/startup surface calls only loadStartupPointer; no health/steward policy calculation."
      pattern: "loadStartupPointer"
---

<objective>
Resolve the startup seam conservatively with a precomputed bounded pointer and a read-only one-line consumer.

Purpose: satisfy UX-01/UX-02 without regressing the existing UserPromptSubmit hot-path isolation contract.
Output: one concrete off-path refresh producer, one tiny pointer module, mutation-caller wiring, startup rendering, and structural/I/O/latency regressions.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-advisory-stewardship-and-guarded-drafts/25-CONTEXT.md
@.planning/phases/25-advisory-stewardship-and-guarded-drafts/25-UI-SPEC.md
@.planning/phases/25-advisory-stewardship-and-guarded-drafts/25-RESEARCH.md
@.planning/phases/25-advisory-stewardship-and-guarded-drafts/25-PATTERNS.md
@.planning/phases/25-advisory-stewardship-and-guarded-drafts/25-03-SUMMARY.md

<interfaces>
From src/prompt/compile-index.mjs:
- boundedJson/open→fstat→read→close and fail-open optional sibling semantics establish the hot-path file-read pattern.
From src/context/prompt-route.mjs:
- routeContextPrompt returns handled/resolution/additional_context and must retain existing compiled-index routing behavior.
From src/steward/suggestion.mjs:
- startupPointer(selected,state,now) produces the complete bounded semantic pointer input off-path.
From src/health/observe.mjs:
- ingestTelemetryEvidence(...) calls store.append; only appended.status === 'stored' is a successful evidence-change seam.
From src/health/admin.mjs:
- reset, dispose, and recover are the complete health-state mutation seams; inspect remains read-only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refresh availability at every off-path health and advisory mutation seam</name>
  <read_first>src/health/observe.mjs, src/health/admin.mjs, src/health/catalog.mjs, src/steward/suggestion.mjs, src/steward/state.mjs, src/cli/router-control.mjs, tests/router.health.observe.test.mjs, tests/router.health.admin.test.mjs</read_first>
  <files>src/steward/refresh.mjs, src/health/observe.mjs, src/health/admin.mjs, src/cli/router-control.mjs, tests/router.steward-startup.test.mjs</files>
  <behavior>
    - refreshSuggestionPointer reads only fixed authoritative health/registry/relationship/contract and steward-state paths, derives the current catalog, selects one suggestion, and publishes available/unavailable state.
    - Every successful ingestTelemetryEvidence append invokes the refresher after the outcome is durable; rejected or denied evidence does not.
    - Health reset, dispose, and recover invoke the refresher after their atomic mutation; unavailable state replaces stale availability.
    - Suggestion inspect, dismiss, snooze, correct, and draft invoke the refresher after current advisory state is known.
    - Injected-call tests prove exactly one post-commit refresh per successful change, zero on failed/no-write paths, and no producer invocation from UserPromptSubmit.
  </behavior>
  <action>Write caller-contract tests first, then create `src/steward/refresh.mjs` with `refreshSuggestionPointer({ownedRoot, now, dependencies})`. This is the sole producer: load the current bounded health window plus immutable active registry/relationship/contract inputs from explicit known paths, call existing `deriveObservations`, apply Plan 25-01 selection and interaction state, then call `compileStartupPointer` with available or unavailable state. Wire it after successful persistence in `ingestTelemetryEvidence`, after reset/dispose/recover mutations, and after suggestion actions in `runRouterControl`; inject the function for tests. Refresh failure must not roll back durable health state or leak details, but operator paths receive a stable warning. Never call the producer from `routeContextPrompt`, a hook adapter, or any UserPromptSubmit path.</action>
  <verify>
    <automated>rtk node --test tests/router.steward-startup.test.mjs tests/router.health.observe.test.mjs tests/router.health.admin.test.mjs tests/router.steward-cli.test.mjs</automated>
  </verify>
  <acceptance_criteria>Every successful evidence, health-state, or advisory-state change has one named post-commit refresh call; failed/no-write paths have none; stale availability is replaced; no prompt-submit path references the producer.</acceptance_criteria>
  <done>The bounded pointer is recomputed by concrete off-hot-path callers whenever its source state changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Atomically compile and bounded-read the startup pointer</name>
  <read_first>src/prompt/compile-index.mjs, src/prompt/publish-index.mjs, src/steward/refresh.mjs, tests/router.compiled-index.test.mjs</read_first>
  <files>src/steward/startup-pointer.mjs, tests/router.steward-startup.test.mjs</files>
  <behavior>
    - Producer atomically writes a 0600 pointer containing only schema, policy, fingerprint, availability, and cooldown/expiry metadata.
    - Ineligible or suppressed state replaces the pointer with an unavailable record so stale availability cannot linger.
    - Loader performs one explicit bounded file read, validates exact keys/types/size, and returns unavailable on missing, corrupt, or expired input.
    - Loader never enumerates directories and imports no health, catalog, scoring, history, discovery, network, or model module.
  </behavior>
  <action>Implement `compileStartupPointer` as the atomic sink used only by Task 1's refresher and `loadStartupPointer` as a separate minimal reader using explicit path/open/fstat/bounded read/close and exact validation. Use a fixed file below the steward root, 0600 mode, temp+fsync+rename, and a fixed schema. The loader performs no calculation or discovery and treats missing, corrupt, or expired content as unavailable.</action>
  <verify>
    <automated>rtk node --test tests/router.steward-startup.test.mjs --test-name-pattern="compile|load|bounded|corrupt|expired"</automated>
  </verify>
  <acceptance_criteria>The producer atomically replaces availability and the loader returns only a validated compact decision from one fixed bounded read.</acceptance_criteria>
  <done>The startup consumer has a safe precomputed input format with stale/corrupt fail-silent behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render exact startup silence or one-line pointer from the loader only</name>
  <read_first>src/context/prompt-route.mjs, src/steward/startup-pointer.mjs, src/prompt/compile-index.mjs, tests/router.compiled-index.test.mjs, tests/router.context-prompt-integration.test.mjs, tests/router.health.privacy.test.mjs, .planning/phases/25-advisory-stewardship-and-guarded-drafts/25-UI-SPEC.md</read_first>
  <files>src/context/prompt-route.mjs, tests/router.steward-startup.test.mjs, tests/router.health.privacy.test.mjs</files>
  <behavior>
    - Available validated pointer adds exactly `Router suggestion available — inspect with /router suggestion` and no evidence/details.
    - Unavailable, missing, corrupt, expired, dismissed, snoozed, or cooldown pointer produces no suggestion bytes and does not disturb normal routing.
    - Instrumented filesystem proves only the fixed pointer path is read for this feature and directory enumeration throws if attempted.
    - Source gates prove prompt-route imports no src/health module and calls no derivation/ranking/history/discovery/network/model function.
    - Existing compiled routing and isolated latency suites remain green.
  </behavior>
  <action>Integrate `loadStartupPointer` at the existing `routeContextPrompt` startup projection boundary. The surface consumes the loader result only: append the exact approved one-line copy when available and otherwise preserve current output byte-for-byte. It must not import or call `refreshSuggestionPointer`. Do not expose fingerprint or pointer metadata. Keep the pointer optional/fail-open for routing so corruption yields silence, not a blocked prompt. Extend Phase 24's hot-path isolation suite with structural import/call gates and an injected-filesystem test that forbids discovery. Preserve the existing context byte cap and all normal routing semantics.</action>
  <verify>
    <automated>rtk node --test tests/router.steward-startup.test.mjs tests/router.health.privacy.test.mjs tests/router.compiled-index.test.mjs tests/router.context-prompt-integration.test.mjs</automated>
  </verify>
  <acceptance_criteria>Startup emits exactly zero or one approved line based solely on a validated pointer, with no health calculation/discovery and no regression to compiled routing.</acceptance_criteria>
  <done>UX-01/UX-02 are delivered through the conservative precomputed seam; UX-08/UX-09 remain intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Off-path policy → pointer file | Advisory eligibility is reduced to a compact startup decision. |
| Pointer file → UserPromptSubmit | Local bytes enter a latency-sensitive prompt surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-25-13 | Tampering | startup pointer | high | mitigate | Atomic 0600 replacement, exact schema/keys/types, size bound, expiry, fail-silent corruption. |
| T-25-14 | Information Disclosure | prompt-route | high | mitigate | Emit one fixed literal only; never render pointer metadata or evidence. |
| T-25-15 | Denial of Service | hot-path loader | high | mitigate | One fixed bounded read, no enumeration/analysis, and isolated latency regression. |
| T-25-16 | Elevation of Privilege | prompt-route | high | mitigate | Read-only optional signal; no mutation or dispatch authority derives from availability. |
| T-25-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package install exists; stdlib and current modules only. |
</threat_model>

<verification>
Nyquist: test each producer caller, then the compiler/loader, then the startup consumer. Run focused observe/admin/CLI/startup/privacy/compiled-index/integration suites. End-of-phase run: `rtk node --test --test-concurrency=1 tests/*.test.mjs`.
</verification>

<success_criteria>
The startup notice is exact, silent by default, deduplicated/cooldown-aware through precomputed state, and adds no hot-path health work.
</success_criteria>

<output>
Create `.planning/phases/25-advisory-stewardship-and-guarded-drafts/25-04-SUMMARY.md` when done.
</output>

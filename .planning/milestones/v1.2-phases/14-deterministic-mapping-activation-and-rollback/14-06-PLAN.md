---
phase: 14-deterministic-mapping-activation-and-rollback
plan: "06"
type: execute
wave: 6
depends_on: ["14-05"]
files_modified:
  - src/registry/activate.mjs
  - tests/router.registry-activate.test.mjs
autonomous: true
gap_closure: true
requirements: [ACT-01]
must_haves:
  truths:
    - Activation and rollback share one cross-process compare-and-swap pointer transition.
    - Recovery and rollback select only production-verified policy-compatible versions in deterministic immutable order.
    - Rollback publication and privacy-safe audit evidence recover to one truthful durable outcome after interruption.
  artifacts:
    - path: src/registry/activate.mjs
      provides: Mutation lock, known-good verifier, deterministic recovery, and rollback journal
    - path: tests/router.registry-activate.test.mjs
      provides: Multi-process race, compatibility, recovery-order, and audit-failure evidence
  key_links:
    - from: replaceActivePointer
      to: active.json
      via: atomically acquired owned mutation lock spanning reread, compare, rename, and directory fsync
    - from: executeRollback
      to: rollback journal
      via: fsynced pending/completed records and deterministic interrupted-operation recovery
---

<objective>
Make pointer mutation a true cross-process CAS and make recovery/rollback known-good selection and audit outcomes crash-consistent.

Purpose: Close the remaining ACT-01 authority, concurrency, compatibility, and durability gaps without changing immutable version contents.
Output: One owned mutation protocol, semantic known-good verification, deterministic recovery ordering, durable rollback journaling, and failure-injection tests.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-deterministic-mapping-activation-and-rollback/14-CONTEXT.md
@.planning/phases/14-deterministic-mapping-activation-and-rollback/14-VERIFICATION.md
@.planning/phases/14-deterministic-mapping-activation-and-rollback/14-02-SUMMARY.md
@.planning/phases/14-deterministic-mapping-activation-and-rollback/14-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Serialize pointer publication as cross-process CAS</name>
  <files>src/registry/activate.mjs, tests/router.registry-activate.test.mjs</files>
  <behavior>
    - Two separate processes racing with the same expected sequence yield exactly one successful pointer replacement.
    - The loser receives a stable conflict result and never overwrites the winner.
    - Activation and rollback use the same lock, reread, compare, rename, and directory-fsync authority.
  </behavior>
  <action>Write a real multi-process race test, then add an atomically acquired controller-owned mutation lock with bounded stale-owner recovery. Hold it across active.json reread, expected-sequence comparison, same-directory temporary write, atomic rename, and parent directory fsync; release it safely on every path. Route both activation and executeRollback through this single mutation primitive per D-14, D-17, and D-18. Preserve stable conflict reason codes and never weaken exact destination confirmation.</action>
  <verify><automated>node --test tests/router.registry-activate.test.mjs</automated></verify>
  <done>Only one cross-process writer can commit a given sequence and both activation and rollback obey the same CAS protocol.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce deterministic known-good recovery and rollback</name>
  <files>src/registry/activate.mjs, tests/router.registry-activate.test.mjs</files>
  <behavior>
    - Integrity-valid but test-only, expired, failed, unknown-runner, or policy-incompatible versions are not known-good.
    - Recovery order uses immutable manifest/version metadata with deterministic tie-breaking, never directory mtime.
    - Rollback preview and execution apply the same semantic compatibility verdict.
  </behavior>
  <action>Extend verifyVersion into a semantic known-good verifier that composes file integrity with verification.json production provenance, required gates, passing disposition, exact runner identities, and current compatible policy identity from Plan 14-05. Define deterministic ordering from immutable version metadata and stable version ID tie-breakers; remove mutable directory mtime from authority decisions. Reuse this verdict in recovery, previewRollback, and executeRollback per D-12, D-13, and D-17. Add fixtures that are integrity-valid but semantically unsafe.</action>
  <verify><automated>node --test tests/router.registry-activate.test.mjs</automated></verify>
  <done>Recovery and rollback can select only verified compatible known-good versions, in deterministic immutable order.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Journal rollback to a truthful durable outcome</name>
  <files>src/registry/activate.mjs, tests/router.registry-activate.test.mjs</files>
  <behavior>
    - A fsynced privacy-safe pending record exists before pointer publication and a completed record is durable afterward.
    - Restart deterministically resolves interruption before or after pointer publication without duplicating or lying about outcome.
    - An audit write failure cannot return an unchanged-state failure after authority changed.
  </behavior>
  <action>Add failure-injection tests around journal write/fsync, CAS publication, completion write/fsync, and restart. Implement a local append-only or operation-record journal containing only source, destination, time, outcome, reason, operation ID, and expected sequence—never prompts or secrets—per D-19. Fsync pending intent before publication, commit through the shared CAS, fsync completed outcome, and reconcile interrupted operations by comparing durable journal state with active.json. Return results that truthfully distinguish not-committed, committed, and recovered outcomes.</action>
  <verify><automated>node --test tests/router.registry-activate.test.mjs</automated></verify>
  <done>Rollback failures before publication leave authority unchanged; committed rollbacks always have recoverable durable privacy-safe audit evidence and truthful results.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| process -> active pointer | Independent activation and rollback processes contend for one mutable authority. |
| immutable history -> recovery | Historical bundles request restoration as known-good authority. |
| rollback -> audit journal | Authority mutation must be durably and privately attributable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-14-06-01 | Tampering | active.json CAS | high | mitigate | Hold an atomic owned lock across reread, compare, rename, and directory fsync. |
| T-14-06-02 | Elevation of Privilege | historical versions | high | mitigate | Require production verification and current policy/runner compatibility in known-good checks. |
| T-14-06-03 | Repudiation | rollback audit | high | mitigate | Fsync privacy-safe intent and completion records and reconcile interrupted operations deterministically. |
</threat_model>

<verification>
Run `node --test tests/router.registry-activate.test.mjs tests/router.registry-watcher.test.mjs tests/router.control-cli.test.mjs` including child-process races and journal failure injection.
</verification>

<success_criteria>
- Pointer publication is a cross-process CAS with one winner per sequence.
- Recovery and rollback share semantic known-good compatibility and immutable ordering.
- Rollback outcome and durable audit evidence remain truthful across every injected crash boundary.
- ACT-01 atomic activation and rollback behavior is directly proven.
</success_criteria>

<output>
Create `.planning/phases/14-deterministic-mapping-activation-and-rollback/14-06-SUMMARY.md` when done.
</output>

---
phase: 47-dispatch-and-storage-safety
plan: 03
type: execute
wave: 3
depends_on: [47-02]
files_modified:
  - src/adapters/dispatch/claude.mjs
  - src/adapters/dispatch/codex.mjs
  - src/runtime/router.mjs
  - tests/router.dispatch-safety.test.mjs
  - tests/phase-38/native-dispatch.test.mjs
  - tests/phase-38/claude-adapter.test.mjs
  - tests/phase-38/codex-adapter.test.mjs
  - tests/phase-44/receipts.test.mjs
autonomous: true
requirements: [SAFE-01, SAFE-02, SAFE-05]
must_haves:
  truths:
    - "Claude and Codex use the same durable initial and resume claim mechanism; process-local Sets are never the authority for at-most-once execution."
    - "Repeated prompt markers, recreated workers, compaction, and process restarts produce one native side effect per durable identity in both runtimes."
    - "The prompt hook remains read-only, detached, fail-open, and latency-bounded; durable claim and child control happen in the worker."
  artifacts:
    - "src/adapters/dispatch/claude.mjs and codex.mjs — thin runtime variants using shared claim, runner, and receipt contract."
    - "src/runtime/router.mjs — marker detection remains hot-path-safe and only starts the detached worker."
    - "tests/router.dispatch-safety.test.mjs and phase-38/phase-44 regressions — parity and restart evidence."
  key_links:
    - "router marker -> detached worker -> shared durable claim -> runtime adapter -> shared bounded runner"
    - "initial claim and resume claim -> same receipt identity partitioned by runtime/stage"
---

<objective>
Wire both installed runtime variants to the shared Phase 47 authority and repair the current duplicate-dispatch and resume regressions without expanding the prompt hot path.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/47-dispatch-and-storage-safety/47-CONTEXT.md
@.planning/phases/47-dispatch-and-storage-safety/47-RESEARCH.md
@.planning/phases/47-dispatch-and-storage-safety/47-PATTERNS.md
@src/adapters/dispatch/contract.mjs
@src/adapters/dispatch/receipt.mjs
@src/adapters/dispatch/claude.mjs
@src/adapters/dispatch/codex.mjs
@src/runtime/router.mjs
</execution_context>

<tasks>

<task type="auto">
  <name>Replace runtime-local idempotency authority with shared durable initial/resume claims</name>
  <files>src/adapters/dispatch/claude.mjs, src/adapters/dispatch/codex.mjs</files>
  <action>
    In both adapters, call the shared claim immediately before pending publication and child spawn for initial dispatch, and use the corresponding resume-stage claim before rehydrating a checkpoint. A successful claim is permanent for that stage; a losing/restarted worker returns or publishes a truthful already_claimed/preserved result and never spawns. Keep the existing in-memory Set only as an optional performance cache after the durable decision, or remove it if no caller needs it. Do not release a durable claim on timeout, process exit, compaction, or worker shutdown.

    Reconstruct timeout/output/retry/completion settings from the persisted execution_contract, invoke the shared bounded runner, and map its terminal evidence into the existing receipt state machine without raw output or prompt leakage. Keep Claude/Codex differences limited to runtime roots, installed-runtime probes, and adapter identity.
  </action>
  <verify>
    <automated>rtk node --test tests/router.dispatch-safety.test.mjs tests/phase-38/claude-adapter.test.mjs tests/phase-38/codex-adapter.test.mjs</automated>
  </verify>
  <done>Direct duplicate invokes, fresh workers, recreated markers, and resumed receipts cannot spawn the same durable identity twice in either runtime; terminal receipt evidence remains truthful and bounded.</done>
</task>

<task type="auto">
  <name>Preserve the detached hot-path contract and add Claude/Codex parity coverage</name>
  <files>src/runtime/router.mjs, tests/router.dispatch-safety.test.mjs, tests/phase-38/native-dispatch.test.mjs, tests/phase-44/receipts.test.mjs</files>
  <action>
    Keep triggerNativeDispatch() limited to marker detection, bounded marker parsing, and detached worker spawn. It must not claim, read/write receipt state, or wait for child completion on UserPromptSubmit. Remove the reusable marker only after the worker has obtained the durable claim; repeated prompt triggers must observe the same identity without reopening execution. Preserve fail-open behavior when the worker or marker is unavailable.

    Add one table-driven parity matrix for Claude and Codex covering initial dispatch, duplicate prompt, recreated marker after restart, durable resume, cross-runtime partitioning, and bounded child outcomes. Assert one side effect, stable already_claimed evidence, persisted contract reuse, and no regression in existing recommendation-only and receipt attribution behavior. Repair the known red lease-resume assertions through the shared root cause; do not weaken expectations or count approved summaries as verification.
  </action>
  <verify>
    <automated>rtk node --test tests/router.dispatch-safety.test.mjs tests/phase-38/*.test.mjs tests/phase-44/receipts.test.mjs tests/router.lease-resume.test.mjs</automated>
  </verify>
  <done>SAFE-05 parity and restart tests pass for both runtimes, the marker remains off-hot-path and fail-open, the focused Phase 38/44 regressions are green, and exactly one native side effect is observed per durable work identity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Risk | Mitigation |
|----------|------|------------|
| Prompt hook -> worker | Hot-path latency or duplicate worker authority | Marker-only hook; durable claim and bounded runner stay in detached worker. |
| Runtime variant -> shared contract | Claude/Codex behavioral drift | Same claim/runner/receipt seam and one parity matrix; runtime-specific differences remain explicit. |
| Restarted worker -> durable state | Re-execution after uncertain exit | Stage-specific permanent claim and persisted execution contract. |
</threat_model>

<verification>
- `rtk node --test tests/router.dispatch-safety.test.mjs tests/phase-38/*.test.mjs tests/phase-44/receipts.test.mjs tests/router.lease-resume.test.mjs`
- `rtk node --test tests/phase-38/budget.test.mjs` confirms the hook latency/side-effect invariant.
- The same durable identity has at most one native side effect per runtime partition.
</verification>

<success_criteria>
- SAFE-01/02 behavior is live in both runtime adapters.
- SAFE-05 Claude/Codex initial dispatch and resume behavior is equivalent and restart-safe.
- Phase 47's four roadmap success criteria are demonstrated by focused and regression tests.
</success_criteria>

<output>
Create `.planning/phases/47-dispatch-and-storage-safety/47-03-SUMMARY.md` when done.
</output>

---
phase: 11-canonical-registry-and-runtime-adapters
plan: "04"
type: execute
wave: 4
depends_on:
  - 11-03
files_modified:
  - src/adapters/claude.mjs
  - src/adapters/codex.mjs
  - tests/router.adapters.test.mjs
autonomous: true
gap_closure: true
requirements:
  - REG-02
  - ADP-01
  - ADP-02
must_haves:
  truths:
    - Claude discovery recognizes actual global, plugin, agents-store, and project-scoped skill, agent, command, hook, binding, scope, and dependency layouts rather than requiring synthetic JSON descriptors.
    - Codex discovery recognizes actual skill, plugin, agent, hook, configuration, project-scope, and dependency layouts, including SKILL.md and config.toml.
    - Recognizable malformed or unsupported native artifacts produce deterministic diagnostics or diagnostic records instead of disappearing.
    - Native metadata, invocation, provenance, dependency unknowns, and dispatchability remain explicit and portable under D-09 through D-16.
  artifacts:
    - path: tests/router.adapters.test.mjs
      provides: Representative Claude and Codex native-layout fixture matrix
    - path: src/adapters/claude.mjs
      provides: Versioned Claude native-layout discovery and parsers
    - path: src/adapters/codex.mjs
      provides: Versioned Codex native-layout discovery and parsers
  key_links:
    - from: src/adapters/claude.mjs
      to: src/registry/schema.mjs
      via: Native Claude observations normalize through the existing canonical capability contract
    - from: src/adapters/codex.mjs
      to: src/adapters/claude.mjs
      via: Shared inert parsing helpers retain runtime-specific discovery tables and invocation compilation
    - from: tests/router.adapters.test.mjs
      to: src/adapters/claude.mjs
      via: Temporary native Claude homes prove all ADP-01 scopes and categories
    - from: tests/router.adapters.test.mjs
      to: src/adapters/codex.mjs
      via: Temporary native Codex homes prove ADP-02 configuration and project coverage
---

<objective>
Close F-001 and F-002 by replacing the synthetic JSON-only adapter boundary with explicit, versioned parsing of supported Claude and Codex native layouts.

Purpose: Make full registry builds observe the real runtime artifacts required by REG-02, ADP-01, and ADP-02 while preserving the deterministic, read-only, portable contracts already verified.
Output: Representative native-layout tests plus Claude and Codex native discovery/parsing support.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-canonical-registry-and-runtime-adapters/11-CONTEXT.md
@.planning/phases/11-canonical-registry-and-runtime-adapters/11-RESEARCH.md
@.planning/phases/11-canonical-registry-and-runtime-adapters/11-PATTERNS.md
@.planning/phases/11-canonical-registry-and-runtime-adapters/11-VERIFICATION.md
@.planning/phases/11-canonical-registry-and-runtime-adapters/11-02-SUMMARY.md
@.planning/phases/11-canonical-registry-and-runtime-adapters/11-03-SUMMARY.md
@docs/superpowers/specs/2026-07-14-dual-runtime-auto-updating-router-design.md
@docs/superpowers/plans/2026-07-14-dual-runtime-auto-updating-router-implementation.md
@src/registry/schema.mjs
@src/registry/identity.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace synthetic-only fixtures with native runtime layout contracts</name>
  <files>tests/router.adapters.test.mjs</files>
  <read_first>
    <file>.planning/phases/11-canonical-registry-and-runtime-adapters/11-VERIFICATION.md</file>
    <file>.planning/phases/11-canonical-registry-and-runtime-adapters/11-CONTEXT.md</file>
    <file>docs/superpowers/specs/2026-07-14-dual-runtime-auto-updating-router-design.md</file>
    <file>docs/superpowers/plans/2026-07-14-dual-runtime-auto-updating-router-implementation.md</file>
    <file>tests/router.adapters.test.mjs</file>
    <file>tests/router.mjs.snapshot</file>
    <file>tests/router.lifecycle.test.mjs</file>
    <file>src/adapters/claude.mjs</file>
    <file>src/adapters/codex.mjs</file>
  </read_first>
  <behavior>
    - Claude fixtures use real SKILL.md files, agent and command markdown, plugin metadata and plugin skill directories, agents-store layouts, hook files, settings.json hook bindings, and matching project .claude equivalents.
    - Codex fixtures use real SKILL.md files, plugin metadata, agent and hook artifacts, config.toml configuration including dependency metadata, and matching project .codex equivalents.
    - Each required category and scope produces an observation with runtime-native invocation and portable logical provenance.
    - Recognizable malformed markdown frontmatter, JSON metadata, TOML/configuration, hook bindings, and unsupported versions produce deterministic diagnostics or D-09 non-dispatchable records.
    - Arbitrary unsupported files remain inert, symlink escapes are rejected, and repeated discovery stays byte-identical.
  </behavior>
  <action>Rewrite the adapter fixture matrix around representative native Claude and Codex homes instead of `schema_version: 1` JSON descriptors. Preserve the deterministic filesystem inventory conventions in `tests/router.mjs.snapshot` and the containment, explicit-root, try/finally cleanup, outside-canary, and temporary-fixture conventions in `tests/router.lifecycle.test.mjs`. Keep explicit temporary roots and outside-root canaries. Cover every category named by REG-02, ADP-01, and ADP-02, including project equivalents. Assert D-05 scope separation; D-09 partial diagnostic records; D-10 unknown optional metadata without invention; D-11 deterministic diagnostics for recognizable unsupported native formats or versions; D-12 declared-unavailable versus undeclared dependency states; D-13/D-15 logical portable provenance; and preserved native invocation. Include direct regression assertions that a Claude SKILL.md and Codex SKILL.md plus config.toml never yield empty observations and diagnostics together. The focused test must be RED against the current JSON filter.</action>
  <acceptance_criteria>
    <criterion>The fixture matrix contains representative native artifacts for every Claude and Codex category and required scope.</criterion>
    <criterion>Tests explicitly reject silent disappearance of recognizable unsupported native files.</criterion>
    <criterion>Native invocation, dependency state, logical provenance, containment, and deterministic ordering are asserted.</criterion>
    <criterion>The initial focused run fails because current discovery ignores native markdown and TOML/configuration.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/router.adapters.test.mjs; test $? -ne 0</automated>
  </verify>
  <done>F-001 and F-002 have representative failing native-layout contracts spanning REG-02, ADP-01, and ADP-02.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement explicit versioned Claude native discovery and parsing</name>
  <files>src/adapters/claude.mjs</files>
  <read_first>
    <file>tests/router.adapters.test.mjs</file>
    <file>tests/router.mjs.snapshot</file>
    <file>tests/router.lifecycle.test.mjs</file>
    <file>src/adapters/claude.mjs</file>
    <file>src/registry/schema.mjs</file>
    <file>src/registry/identity.mjs</file>
  </read_first>
  <behavior>
    - discoverRoots reads supported Claude native layouts under only the supplied global and project roots.
    - parseArtifact handles SKILL.md, agent/command markdown, plugin and agents-store metadata, hook files, and settings hook bindings as inert data.
    - Parsed observations preserve native invocation and identity evidence without using name, description, or content similarity to merge records.
  </behavior>
  <action>Refactor the shared adapter mechanics so discovery is driven by explicit native-layout descriptors rather than category-directory plus `.json` filtering. Implement Claude adapter versioned recognizers/parsers for the native files in Task 1, parsing markdown frontmatter/body metadata, supported JSON plugin/settings metadata, and hook/binding declarations without importing or executing discovered content. Preserve the deterministic filesystem inventory ordering from `tests/router.mjs.snapshot` and the realpath containment, explicit-root, temporary-fixture, outside-canary, and cleanup patterns from `tests/router.lifecycle.test.mjs`. Preserve D-01 through D-04 identity evidence only when explicitly declared, D-05/D-08 scope identities from the supplied root specs, D-09 through D-12 partial/unknown/dependency semantics, and D-13 through D-16 portable deterministic output. Keep realpath containment before reads, stable traversal, standard-library-only production code per D-17, and existing JSON descriptor compatibility only if it is an explicitly supported versioned layout rather than the sole discovery path.</action>
  <acceptance_criteria>
    <criterion>All native Claude fixtures yield the expected category, scope, invocation, provenance, and dependency observations.</criterion>
    <criterion>Malformed recognizable Claude artifacts yield deterministic diagnostics or non-dispatchable records.</criterion>
    <criterion>No file read crosses a supplied root and no discovered content is executed.</criterion>
    <criterion>Repeated Claude discovery produces byte-identical results.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/router.adapters.test.mjs --test-name-pattern="Claude|parse|discovery|explicit runtime roots"</automated>
  </verify>
  <done>ADP-01 covers supported native Claude global, plugin, agents-store, and project layouts without silent omissions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement explicit versioned Codex native discovery and parsing</name>
  <files>src/adapters/codex.mjs</files>
  <read_first>
    <file>tests/router.adapters.test.mjs</file>
    <file>tests/router.mjs.snapshot</file>
    <file>tests/router.lifecycle.test.mjs</file>
    <file>src/adapters/claude.mjs</file>
    <file>src/adapters/codex.mjs</file>
    <file>src/registry/schema.mjs</file>
  </read_first>
  <behavior>
    - discoverRoots reads supported Codex native layouts under only the supplied home and project roots.
    - parseArtifact handles SKILL.md, plugin/agent/hook metadata, config.toml, and project equivalents as inert data.
    - Configured MCP, tool, model, permission, hook, plugin, and dependency metadata remains explicit without inventing absent declarations.
  </behavior>
  <action>Give the Codex adapter its explicit native-layout table and parsers while reusing only runtime-neutral containment, diagnostic, canonical-normalization, and ordering helpers from the Claude module. Parse the Task 1 Codex markdown, JSON metadata, and bounded TOML/configuration structures needed for ADP-02; do not add an external TOML package or execute configuration. Preserve the deterministic filesystem inventory ordering from `tests/router.mjs.snapshot` and the realpath containment, explicit-root, temporary-fixture, outside-canary, and cleanup patterns from `tests/router.lifecycle.test.mjs`. Preserve exact native invocation data, D-09/D-11 diagnostics, D-10/D-12 unknown-versus-declared metadata, project scope, logical provenance, and deterministic adapter/version evidence. Ensure recognizable unsupported configuration keys or schema versions are reported and arbitrary unrelated files remain inert.</action>
  <acceptance_criteria>
    <criterion>All native Codex fixtures yield the expected category, scope, invocation, provenance, configuration, and dependency observations.</criterion>
    <criterion>Malformed or unsupported recognizable Codex files never vanish silently.</criterion>
    <criterion>No external dependency, ambient-home lookup, mutation, or content execution is introduced.</criterion>
    <criterion>The complete adapter suite passes twice with identical normalized output.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/router.adapters.test.mjs && node --test tests/router.adapters.test.mjs</automated>
  </verify>
  <done>REG-02 and ADP-02 cover supported native Codex home and project layouts, including SKILL.md and config.toml.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supplied runtime roots to traversal | Attacker-controlled local directory entries and symlinks enter discovery. |
| Native markdown/JSON/TOML bytes to parser | Malformed or prompt-like content enters inert metadata parsing. |
| Native identity/dependency claims to canonical observations | Untrusted declarations can affect grouping and dispatchability evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-G04-01 | Elevation of Privilege | native discovery | high | mitigate | Realpath-contain every candidate before reading and never import or execute discovered files. |
| T-11-G04-02 | Spoofing | identity metadata | high | mitigate | Preserve only explicit authoritative evidence per D-01 through D-04; names/content never establish identity. |
| T-11-G04-03 | Information Disclosure | provenance | high | mitigate | Emit logical roots and relative paths only in portable observations per D-13/D-15. |
| T-11-G04-04 | Denial of Service | native parsers | medium | mitigate | Use bounded supported structures, deterministic rejection diagnostics, and no recursive parser dependencies. |
</threat_model>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | - | Stable runtime-neutral view across supported native layouts | 11-04 | COVERED | Native Claude and Codex layouts normalize through the existing schema.
REQ | REG-02 | Full rebuild discovers supported native categories | 11-04 | COVERED | Native fixture matrix and discovery parsers.
REQ | ADP-01 | Claude global/plugin/agents-store/project coverage | 11-04 | COVERED | Explicit Claude layout table and parser tests.
REQ | ADP-02 | Codex skills/plugins/agents/hooks/config/project coverage | 11-04 | COVERED | Explicit Codex layout table including config.toml.
RESEARCH | - | Explicit versioned native layouts; unknown schema diagnostics | 11-04 | COVERED | Recognizers are versioned and diagnostic-first.
RESEARCH | - | Zero-dependency ESM, explicit roots, read-only adapters | 11-04 | COVERED | Standard-library inert parsers and temporary homes.
CONTEXT | D-01..D-08 | Evidence-gated identity and separate scopes | 11-04 | COVERED | Native metadata preserves declared identity evidence and scope.
CONTEXT | D-09..D-12 | Partial, optional, unsupported, and dependency behavior | 11-04 | COVERED | Each behavior has native fixtures and parser rules.
CONTEXT | D-13..D-17 | Portable provenance, deterministic diagnostics, lightweight runtime | 11-04 | COVERED | Logical paths, stable ordering, no packages.
CONTEXT | D-18..D-20 | One-command operation | 11-04 | COVERED | Existing installer consumes adapters through buildFullRegistry; no new bootstrap.
</source_audit>

<verification>
Run `node --test tests/router.registry-schema.test.mjs tests/router.adapters.test.mjs tests/router.registry-build.test.mjs tests/router.settings-diff.test.mjs`, then rerun `node --test tests/router.adapters.test.mjs` to confirm deterministic native discovery.
</verification>

<success_criteria>
F-001 and F-002 close: representative native Claude and Codex layouts produce complete portable observations or explicit diagnostics, all focused tests pass, and no external dependency or runtime mutation is introduced.
</success_criteria>

<output>
Create `.planning/phases/11-canonical-registry-and-runtime-adapters/11-04-SUMMARY.md` when done.
</output>

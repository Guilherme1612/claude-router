---
phase: 29-mode-map-curation-and-signal-patterns-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/router.mode-map-v3.test.mjs
  - tests/router.mode-map-curation.test.mjs
  - tests/router.calibration-thresholds.test.mjs
autonomous: true
requirements: [MAP-01, MAP-02, MAP-03, SIG-01, SIG-02, SIG-03, SIG-04]

must_haves:
  truths:
    - "Wave 0 contains failing executable contracts for every Phase 29 requirement before production or data changes begin."
    - "The routing contract uses a supplied synthetic manifest and mode map rather than the user's live inventory."
    - "Threshold tests require evidence-driven selection and reject any candidate producing a wrong high-confidence route."
  artifacts:
    - path: "tests/router.mode-map-v3.test.mjs"
      provides: "Mixed v2/v3 normalization, cap, malformed-pattern, and collision contracts"
    - path: "tests/router.mode-map-curation.test.mjs"
      provides: "Eighteen portable positive routes, hard negatives, and blocked-agent safety contracts"
    - path: "tests/router.calibration-thresholds.test.mjs"
      provides: "Deterministic grid objective and leave-one-out sensitivity contracts"
  key_links:
    - from: "tests/router.mode-map-curation.test.mjs"
      to: "/Users/guilherme/.claude/hooks/router.mjs"
      via: "inspectDecision with explicit fixture objects"
      pattern: "inspectDecision"
    - from: "tests/router.calibration-thresholds.test.mjs"
      to: "router.calibrate.mjs"
      via: "imported pure threshold helpers"
      pattern: "selectThresholds|evaluateThreshold"
---

<objective>
Create the failing Wave 0 contract suite for MAP-01..03 and SIG-01..04 before implementation.

Purpose: Lock portability, safety, schema, collision, and evidence-derived calibration behavior so later waves cannot pass by relying on the live GSD manifest or assumed thresholds.
Output: Three focused node:test files that fail only because Phase 29 production behavior is not implemented yet.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-mode-map-curation-and-signal-patterns-expansion/29-CONTEXT.md
@.planning/phases/29-mode-map-curation-and-signal-patterns-expansion/29-RESEARCH.md
@.planning/phases/29-mode-map-curation-and-signal-patterns-expansion/29-VALIDATION.md
@.planning/phases/29-mode-map-curation-and-signal-patterns-expansion/29-PATTERNS.md
@tests/router.route-targets.test.mjs
@tests/router.calibrate-importable.test.mjs
@/Users/guilherme/.claude/hooks/router.mjs
@router.calibrate.mjs

<interfaces>
The only supported Phase 29 pattern kind is `contains`; a v2 string is semantically identical to a v3 `contains` object and no direct-match semantics change.
Collision identity is normalized kind plus normalized value. A duplicate is legal only when every occurrence declares the same non-empty `collision_group`.
The synthetic manifest contains the eight MAP-01 lifecycle skill IDs and ten MAP-02 design skill IDs with neutral descriptions, plus safe and missing-MCP agents.
Threshold candidate order is safety-first: zero wrong-high routes, maximum correct routes, maximum correct-high routes, minimum misses, then nearest current stable value.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify v2/v3 normalization, cap, and collision behavior</name>
  <files>tests/router.mode-map-v3.test.mjs</files>
  <behavior>
    - Schema v2 strings and schema v3 strings normalize to `contains`.
    - Schema v3 accepts only `contains` objects with non-empty normalized values.
    - Nulls, arrays, unknown kinds, empty values, and v3 objects under schema v2 produce deterministic invalid-pattern diagnostics.
    - Every entry must contain one through six patterns.
    - A string and an equivalent `contains` object collide.
    - Duplicate canonical patterns fail unless every occurrence has the same non-empty collision group.
    - Corpus-visible values never contain object stringification artifacts.
  </behavior>
  <action>Create table-driven tests against exported router validation/normalization behavior, following `tests/router.route-targets.test.mjs`'s direct hook import, local fixture builder, and diagnostic assertions. Include same-entry and cross-entry duplicates, case/whitespace canonicalization, mismatched/partial collision groups, unsupported kind rejection, and the legitimate current `"redesign the ui"` collision declared under one shared group. Assert diagnostic codes and normalized semantic values, not private helper layout. Keep the file self-contained with minimal fixture entries and no new test helper module.</action>
  <verify>
    <automated>rtk node --test tests/router.mode-map-v3.test.mjs</automated>
  </verify>
  <done>The test file exists, runs, and fails at the missing Phase 29 normalization/collision contract rather than syntax or fixture errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Specify portable lifecycle and design routing</name>
  <files>tests/router.mode-map-curation.test.mjs</files>
  <behavior>
    - Eight implicit lifecycle prompts select gsd-ship, gsd-new-project, gsd-execute-phase, gsd-quick, gsd-validate-phase, gsd-verify-work, gsd-resume-work, and gsd-complete-milestone.
    - Ten implicit design prompts select brandkit, minimalist-ui, industrial-brutalist-ui, image-to-code, imagegen-frontend-web, imagegen-frontend-mobile, redesign-existing-projects, stitch-design-taste, excalidraw-diagram, and gpt-taste.
    - Sibling hard negatives and near-collisions do not select the wrong route.
    - Routing succeeds using only supplied neutral fixture objects and does not consult the live manifest or mode-map.
    - Missing-MCP agents are absent from scored candidates and dispatch targets and remain warning-only.
  </behavior>
  <action>Create one local synthetic manifest builder and one local mode-map fixture builder using generic capability descriptions, following the synthetic typed manifest in `tests/router.route-targets.test.mjs` and isolation assertions in `tests/router.coverage-audit.test.mjs`. Exercise the real `inspectDecision` path with explicit object injection expected from Wave 2. Use output/artifact phrases rather than route or skill names in prompts. Cover all 18 required targets, at least one sibling hard negative per design family/lifecycle family, target validation, live-path isolation, and the existing blocked-agent invariant. Do not create a second scorer or parser in the test.</action>
  <verify>
    <automated>rtk node --test tests/router.mode-map-curation.test.mjs tests/router.route-targets.test.mjs</automated>
  </verify>
  <done>The synthetic suite enumerates all 18 required targets and fails because direct fixture routing and curated data are not yet implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Specify deterministic threshold selection</name>
  <files>tests/router.calibration-thresholds.test.mjs</files>
  <behavior>
    - Candidate enumeration uses observed score and margin breakpoints plus current constants.
    - Any candidate producing a wrong high-confidence route is rejected.
    - Remaining candidates use the documented deterministic lexicographic objective and stable tie-break.
    - Leave-one-out selection reports ranges and frequency without mutating fixtures or live mode-map data.
    - Labeled boundary records independently identify T_high, T_low, and M; unchanged numeric results are permitted only when selected by the objective.
  </behavior>
  <action>Add small fabricated score records following `tests/router.calibrate-importable.test.mjs`'s named-export and purity pattern. Independently prove the safety constraint, ordering, tie-break, all-three-boundary identification, and leave-one-out result. Import the intended pure helpers from `router.calibrate.mjs`; do not encode BM25 or production tier logic in this test. Keep CLI and real expanded-corpus assertions for Wave 4 after labeled fixtures exist.</action>
  <verify>
    <automated>rtk node --test tests/router.calibration-thresholds.test.mjs</automated>
  </verify>
  <done>The pure selection contract is executable and fails only because the calibration helpers do not exist yet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON-like mode-map fixtures → router | Malformed or ambiguous user-reviewed patterns enter routing validation. |
| synthetic inventory → decision path | Fixture-owned capability data must not fall through to live private inventory. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-01 | Tampering | mixed pattern DSL | high | mitigate | Contract invalid kinds/values and v2 object rejection with deterministic diagnostics. |
| T-29-03 | Tampering | collision and threshold fixtures | high | mitigate | Require explicit collision groups and reject all wrong-high threshold candidates. |
| T-29-04 | Information Disclosure | synthetic route fixture | high | mitigate | Supply neutral in-memory objects and assert live inventory paths are not read. |
| T-29-06 | Elevation of Privilege | missing-MCP agent | critical | mitigate | Assert blocked agents never enter candidates or dispatch target arrays. |
</threat_model>

<verification>
Run each new file independently. RED is expected in this Wave 0 plan; failures must identify missing Phase 29 exports/behavior, not broken imports, syntax, or malformed fixtures.
</verification>

<success_criteria>
- All three VALIDATION.md Wave 0 test files exist before production changes.
- Tests cover all seven Phase 29 requirements and the smallest injectable object seam.
- No new dependency, parser module, scorer, or shared fixture abstraction is introduced.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Portable curated routing with collision and calibration evidence | 29-01..04 | COVERED | Wave 0 contracts precede implementation
REQ | MAP-01 | Eight lifecycle routes | 29-01,29-03 | COVERED | Task 2 plus curation
REQ | MAP-02 | Ten design routes | 29-01,29-03 | COVERED | Task 2 plus curation
REQ | MAP-03 | Validation, synthetic manifest, blocked agents | 29-01,29-02,29-03 | COVERED | Tasks 1-2
REQ | SIG-01 | One-to-six output patterns | 29-01,29-02,29-03 | COVERED | Task 1
REQ | SIG-02 | v3 mixed DSL and v2 compatibility | 29-01,29-02 | COVERED | Task 1
REQ | SIG-03 | Explicit collision declaration | 29-01,29-02,29-03 | COVERED | Task 1
REQ | SIG-04 | Re-derived thresholds | 29-01,29-04 | COVERED | Task 3
RESEARCH | — | Contains-only vocabulary and shared normalizer | 29-01,29-02 | COVERED | Task 1 contract
RESEARCH | — | Constrained grid plus sensitivity | 29-01,29-04 | COVERED | Task 3 contract
CONTEXT | discretion | Minimal implementation following existing router patterns | 29-01..04 | COVERED | No locked/deferred decisions
</source_audit>

<output>
Create `.planning/phases/29-mode-map-curation-and-signal-patterns-expansion/29-01-SUMMARY.md` when done.
</output>

---
phase: 22-conservative-contracts-and-relationship-graph
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/helpers/inventory-fixture.mjs
  - tests/router.contracts.test.mjs
  - src/registry/contract.mjs
  - src/registry/schema.mjs
autonomous: true
requirements: [CONT-01, CONT-02, CONT-03]
nyquist_compliant: true
must_haves:
  truths:
    - "Every authoritative Phase 21 capability has a normalized contract covering every CONT-01 field."
    - "Each inferred field independently exposes state, evidence, provenance, inference-rule version, freshness, and integer-basis-point confidence."
    - "Missing, conflicting, stale, or below-threshold dispatch fields remain visibly unknown and recommendation-only."
  artifacts:
    - path: "src/registry/contract.mjs"
      provides: "Deterministic field-envelope inference and canonical contract construction"
    - path: "src/registry/schema.mjs"
      provides: "Canonical validation and serialization of inline contracts"
    - path: "tests/router.contracts.test.mjs"
      provides: "Four-profile contract, uncertainty, and permutation oracle"
  key_links:
    - from: "src/registry/contract.mjs"
      to: "src/registry/schema.mjs"
      via: "contract construction uses the canonical schema boundary and stableStringify"
    - from: "tests/helpers/inventory-fixture.mjs"
      to: "tests/router.contracts.test.mjs"
      via: "all four Phase 21 installation profiles exercise contract inference"
---

<objective>
Create the canonical, conservative contract model for CONT-01 through CONT-03.

Purpose: Users must see what each installed capability can safely do and why, without optimistic defaults or whole-record confidence shortcuts.
Output: A field-envelope contract module, schema integration, and Wave 0 tests covering all normalized fields and uncertainty states.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-conservative-contracts-and-relationship-graph/22-CONTEXT.md
@.planning/phases/22-conservative-contracts-and-relationship-graph/22-RESEARCH.md
@.planning/phases/22-conservative-contracts-and-relationship-graph/22-PATTERNS.md
@.planning/phases/21-authoritative-personalized-inventory/21-01-SUMMARY.md

<interfaces>
Reuse `validateCapability(record)`, `canonicalizeCapability(record)`, and `stableStringify(value)` from `src/registry/schema.mjs`.
Reuse the four synthetic installation profiles and semantic-byte helpers from `tests/helpers/inventory-fixture.mjs`.
Publish pure ESM functions from `src/registry/contract.mjs`; do not introduce classes, services, dependencies, network calls, or a parallel registry.
</interfaces>
</context>

## Artifacts this plan creates/modifies

- `src/registry/contract.mjs` — versioned field envelopes, inference policy, rejected evidence, and normalized contract construction.
- `src/registry/schema.mjs` — validates and canonically orders inline contract data without changing stable Phase 21 identity.
- `tests/router.contracts.test.mjs` — CONT-01/02/03 contract oracle.
- `tests/helpers/inventory-fixture.mjs` — contract evidence/conflict/staleness variants layered onto existing profiles.

<tasks>

<task type="auto">
  <name>Task 1: Establish the Wave 0 canonical contract oracle</name>
  <files>tests/helpers/inventory-fixture.mjs, tests/router.contracts.test.mjs</files>
  <read_first>
    - .planning/phases/22-conservative-contracts-and-relationship-graph/22-CONTEXT.md (contract evidence and confidence decisions)
    - .planning/phases/22-conservative-contracts-and-relationship-graph/22-VALIDATION.md (CONT-01/02/03 matrices)
    - tests/helpers/inventory-fixture.mjs (four profiles and mutation helpers)
    - tests/router.registry-schema.test.mjs (canonical schema and byte-permutation conventions)
    - src/registry/schema.mjs (current normalized record and set-like canonicalization)
    - src/registry/map.mjs (existing integer-basis-point evidence and policy conventions)
  </read_first>
  <action>Extend the existing fixture helper with deterministic evidence variants for accepted, missing, conflicting, stale, rejected, and below-threshold field inference, without ambient filesystem reads. Create `tests/router.contracts.test.mjs` and require every record in the Claude-heavy, Codex-heavy, mixed/custom, and unknown-future profiles to receive purpose, triggers, inputs, outputs, preconditions, dependencies, permissions, side effects, reversibility, risk, invocation kind, lifecycle role, scope, and workflow transitions per CONT-01. Specify one uniform envelope for each field with explicit state plus independently recorded evidence, provenance, rule version, freshness, confidence basis points, and deterministic reason codes per CONT-02. Assert dispatch-relevant uncertainty stays `unknown`, never receives an optimistic value, and makes the record recommendation-only per CONT-03. Permute records, evidence, and set-like values and require byte-identical canonical output. Use conservative `contract-policy-v1`: inferred dispatch-relevant facts require at least 8500 basis points, while structural adapter facts require exact 10000 evidence.</action>
  <acceptance_criteria>
    - Coverage assertion: all CONT-01 fields exist for every record across all four profiles.
    - Evidence assertion: no whole-contract confidence field substitutes for independent per-field envelopes.
    - Safety assertion: missing, conflicting, stale, and below-8500 inferred dispatch facts are unknown and recommendation-only.
    - RED assertion marker: every intentionally failing contract name contains `[phase22-red:contracts]`, and the TAP-aware harness accepts only those expected failures.
  </acceptance_criteria>
  <verify>
    <automated>rtk node -e "const {spawnSync}=require('node:child_process');const r=spawnSync(process.execPath,['--test','tests/router.contracts.test.mjs'],{encoding:'utf8'});const out=(r.stdout||'')+(r.stderr||'');const failed=[...out.matchAll(/^\s*not ok \d+ - (.+)$/gm)].map(m=&gt;m[1]);process.exit(r.status!==0&amp;&amp;failed.length&gt;0&amp;&amp;failed.every(name=&gt;name.includes('[phase22-red:contracts]'))?0:1)"</automated>
  </verify>
  <done>The test suite fully specifies normalized contracts, field-level evidence, conservative thresholds, and canonical bytes before production edits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic field-level contracts</name>
  <files>src/registry/contract.mjs, src/registry/schema.mjs, tests/router.contracts.test.mjs</files>
  <read_first>
    - tests/router.contracts.test.mjs (Task 1 executable contract)
    - src/registry/schema.mjs (validation, normalizeRecord, SET_LIKE_FIELDS, canonicalizeCapability, stableStringify)
    - src/registry/map.mjs (evidence records, policy versioning, integer confidence, stable reasons)
    - src/registry/identity.mjs (stable identity and exact-source fingerprint fields)
    - .planning/phases/22-conservative-contracts-and-relationship-graph/22-PATTERNS.md (`contract.mjs` closest analogs)
  </read_first>
  <behavior>
    - Equal authoritative inputs always produce equal canonical contract bytes.
    - Every field is independently explainable and rejected evidence stays inspectable without authored bodies or secrets.
    - Any uncertain dispatch-relevant field remains unknown and cannot authorize dispatch.
  </behavior>
  <action>Create a small pure-function `src/registry/contract.mjs` implementing `contract-policy-v1`, uniform field envelopes, bounded evidence/rejection collections, stable reason codes, and normalized contract construction from an already-authoritative Phase 21 record. Use integer basis points only. Extend `src/registry/schema.mjs` to validate the inline `contract`, its complete field set, bounded portable evidence, freshness/state enums, version strings, and recommendation-only consequence; add only semantically set-like paths to canonical sorting. Preserve `validateCapability`, `canonicalizeCapability`, and `stableStringify` exports and legacy Phase 21 compatibility. Treat authored prose as untrusted evidence, never as authority, and exclude raw bodies, secrets, absolute roots, timestamps, and operational metadata from semantic bytes. Do not add filesystem discovery, overlays, relationship inference, dispatch execution, or packages in this plan.</action>
  <acceptance_criteria>
    - API assertion: the contract module exports pure builders/validators used by tests without side effects.
    - Compatibility assertion: existing Phase 21 records still validate and retain stable identity behavior.
    - Determinism assertion: every record/evidence permutation produces byte-identical contracts.
    - Security assertion: rejected evidence exposes safe reason/provenance tokens but not authored content, secrets, or absolute paths.
  </acceptance_criteria>
  <verify>
    <automated>rtk node --test tests/router.contracts.test.mjs tests/router.registry-schema.test.mjs</automated>
  </verify>
  <done>All authoritative records carry complete, independently evidenced, conservatively inferred canonical contracts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| discovered record → contract inference | Untrusted authored metadata and adapter evidence enter safety-relevant inference |
| contract → canonical registry | Derived fields become inspectable authority inputs for later eligibility |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-01 | Elevation of Privilege | field inference | high | mitigate | Unknown-by-default envelopes, 8500/10000 versioned thresholds, exact structural evidence, and recommendation-only tests prevent fabricated authority. |
| T-22-02 | Tampering | canonical contract bytes | high | mitigate | Schema validation, integer confidence, canonical set ordering, and input-permutation tests enforce deterministic artifacts. |
| T-22-03 | Information Disclosure | rejected evidence | high | mitigate | Strict portable evidence allowlist and seeded secret/authored-body/absolute-path negative assertions. |
| T-22-04 | Repudiation | inference decisions | medium | mitigate | Every field records rule version, evidence, provenance, freshness, confidence, state, and stable reason codes. |
| T-22-SC | Tampering | package installation | high | mitigate | No package installation task exists. |
</threat_model>

<verification>
- `rtk node --test tests/router.contracts.test.mjs tests/router.registry-schema.test.mjs`
- Confirm no DB/ORM schema file or package manifest changed; the schema work is the existing registry JSON contract only.
</verification>

<success_criteria>
- CONT-01, CONT-02, and CONT-03 pass across all four installation profiles.
- Contract bytes are deterministic and uncertain safety facts remain recommendation-only.
- Existing Phase 21 schema behavior remains green.
</success_criteria>

<output>
Create `.planning/phases/22-conservative-contracts-and-relationship-graph/22-01-SUMMARY.md` when done.
</output>

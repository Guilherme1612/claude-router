---
phase: 31-runtime-tagging
plan: 02
type: tracer
wave: 1
depends_on: [31-01]
files_modified:
  - tests/router.mjs.snapshot
  - ~/.claude/hooks/router.mjs
  - build-manifest.mjs
  - src/registry/watcher.mjs
autonomous: true
requirements: [PARITY-01, PARITY-02]
user_setup: []
estimate:
  tokens: 65000
  raw_tokens: 33000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - The router detects its active runtime once at module load (D-01, zero hot-path IO) and caches it in a module-level constant.
    - The hardcoded RUNTIME_CONFIG_DIR is gone; RUNTIME_CONFIG_DIR, ROUTER_DIR, HOOKS_DIR, and all child constants resolve to the active runtime's profile dir (D-03), verified by the 31-01 detection tests going GREEN.
    - The runtime value is the enum "claude"|"codex" (D-02) and ROUTER_RUNTIME env override wins over autodetection (D-04).
  artifacts:
    - tests/router.mjs.snapshot (RUNTIME constant, detectRuntime(), runtime-conditional base)
    - ~/.claude/hooks/router.mjs (lockstep mirror, byte-identical)
    - src/health/outcome-schema.mjs (epoch indicator wiring, if ingest needs it)
    - src/registry/watcher.mjs (telemetryRecordToEvidence forward-maps runtime, if allowlisted)
  key_links:
    - detectRuntime() → RUNTIME constant → runtime-conditional base → all ROUTER_DIR/HOOKS_DIR descendants.
    - The snapshot and the live ~/.claude/hooks/router.mjs must remain byte-identical (31-01 mirror guard stays GREEN).
---

<objective>
Build the tracer-first vertical slice: deterministic runtime detection wired end-to-end through the config-dir resolution so a Claude session and a Codex session each write cache/telemetry under their own profile root.

Purpose: Prove the full runtime-detection → path-isolation path on a single thin slice before deepening into cache-key and telemetry tagging (plan 31-03). Parities breached: PARITY-01 (detection) fully; PARITY-02 partially (path isolation, tagging completes in 31-03).

Output: The hook resolves its runtime once at module load and resolves every data path under the active runtime's home.

Deferred note (decision-coverage): D-07 (resolve evaluation uses only the active runtime's capabilities; only the active runtime's suggestion injected) and D-08 (cross-runtime capability-equivalent fixture) are DEFERRED to Phase 32 — the resolve-first capability-resolution hot path does not exist until Phase 32 ships PARITY-03/04 (mapped to ROUTE-*-phase-32 and PARITY-03/04). This plan implements PARITY-01 + the per-runtime foundation of PARITY-02 only. D-08's minimal cross-runtime fixture (reusing phase-26 provenance[{runtime}]) is not a deliverable here; it is deferred with the resolve layer. These decisions are referenced, not dropped.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-runtime-tagging/31-CONTEXT.md
@.planning/phases/31-runtime-tagging/31-RESEARCH.md

# Reference files (read once):
@tests/router.mjs.snapshot  # isMain():54-61 reuses process.argv[1]; constant block:74-106 (ROUTER_DIR:75, HOOKS_DIR:90, RUNTIME_CONFIG_DIR:104, SURFACE_FILE:105, GSD_CORE_DIR:106); children:76-92
@build-manifest.mjs  # ROUTER_HOOK_PATH:49; CLAUDE env-override:31-33
@src/health/outcome-schema.mjs  # OUTCOME_FIELDS:33-38 (leave 14 in this plan; bump is 31-03)
@src/registry/watcher.mjs  # telemetryRecordToEvidence forward-map (:114 area) — only inspect now, no behavior change unless ingest allowlists runtime
@tests/router.phase26-dual-runtime.test.mjs  # PROFILES:10; provenance[{runtime}]:120 — shape for D-08 deferral note
</context>

## Artifacts this phase produces

| Kind | Artifact | What it is |
|------|----------|------------|
| Updated canonical hook | `tests/router.mjs.snapshot` | `detectRuntime()` (D-01/D-02/D-04 precedence: `ROUTER_RUNTIME` → codex marker → default `claude`; enum-clamped, fail-open), `export const RUNTIME`, `runtimeBaseDir(runtime)` helper, runtime-conditional `RUNTIME_CONFIG_DIR`/`ROUTER_DIR`/`HOOKS_DIR` + all child constants (:76-92, SURFACE_FILE :105, GSD_CORE_DIR :106) rewired off the `.claude` hardcode. |
| Updated live mirror | `~/.claude/hooks/router.mjs` | Byte-identical lockstep copy of the canonical hook (31-01 mirror guard stays GREEN). |
| Updated builder | `build-manifest.mjs` | `ROUTER_RUNTIME`-aware home derivation: `codex` pins the codex home (`ROUTER_CODEX_HOME || ~/.codex`) for manifest/mode-map/hook path; `claude` default byte-for-byte unchanged. |
| Inspect-only | `src/registry/watcher.mjs` | `telemetryRecordToEvidence` (:114 area) inspected; the exact `runtime`/`epoch` threading site is documented in the SUMMARY for 31-03. No behavior change this plan. |
| Test-status note (no file change this plan) | `tests/router.health.outcome-schema.test.mjs` | EXPECTED RED at this wave: 31-01 bumped it to expect 16 fields; the schema bump is the deliberate 31-03 deliverable. Do NOT edit the schema or the test here. |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Wire runtime detection + runtime-conditional dir resolution end-to-end</name>
  <files>tests/router.mjs.snapshot, ~/.claude/hooks/router.mjs</files>
  <read_first>tests/router.mjs.snapshot</read_first>
  <behavior>
    - Test 1 (detection precedence): with ROUTER_RUNTIME=codex, detectRuntime() returns "codex" (D-04 override wins).
    - Test 2 (default/fail-open): with ROUTER_RUNTIME unset and no codex marker in env or process.argv, detectRuntime() returns "claude".
    - Test 3 (enum clamp): with ROUTER_RUNTIME=bogus, detectRuntime() returns "claude", never an unbounded string (V5 boundary).
    - Test 4 (dir resolution): runtime-conditional base resolves "codex" -> ~/.codex and "claude" -> ~/.claude for RUNTIME_CONFIG_DIR, ROUTER_DIR, HOOKS_DIR.
  </behavior>
  <action>
    In tests/router.mjs.snapshot (canonical hook), add a module-load runtime detection that runs ONCE, not per prompt:

    - Add a pure, exportable `detectRuntime()` function that returns `"claude" | "codex"`. Precedence, per D-01/D-04: (1) if `process.env.ROUTER_RUNTIME` is present and is exactly `"claude"` or `"codex"`, return that (override wins); (2) else if a codex marker is present (e.g. `process.env.CODEX_HOME` set, or `process.argv[1]` path contains `.codex/`), return `"codex"`; (3) else return `"claude"` (deterministic default, fail-open). Wrap the whole body in try/catch and return `"claude"` on any throw (never block, project fail-open rule). Clamp: any out-of-enum env value falls through to the default `"claude"` — never let an unbounded string reach path construction. Detection must use only `process.env`/`process.argv` string checks already in-scope at module load — NO IO, NO file reads, NO existsSync.
    - `export const RUNTIME = detectRuntime()` — module-level constant, computed once at load (D-01 cache-into-constant, zero hot-path cost).
    - Replace the hardcoded `const RUNTIME_CONFIG_DIR = join(homedir(), '.claude')` (snapshot:104) with a runtime-conditional resolution: introduce a helper (e.g. `runtimeBaseDir(runtime)`) returning `join(homedir(), '.' + runtime)` for the two enum values, and set `export const RUNTIME_CONFIG_DIR = runtimeBaseDir(RUNTIME)` (exported — 31-01 detection/dir tests import it directly). Per D-03, this ONE constant is the single source of truth for the profile root.
    - CRITICAL (RESEARCH correction): TODAY `ROUTER_DIR` (snapshot:75) and `HOOKS_DIR` (snapshot:90) call `join(homedir(), '.claude', ...)` DIRECTLY, bypassing RUNTIME_CONFIG_DIR. Rewire BOTH to derive from the runtime-conditional root: `const ROUTER_DIR = join(RUNTIME_CONFIG_DIR, 'router')` and `const HOOKS_DIR = join(RUNTIME_CONFIG_DIR, 'hooks')`. Confirm all ROUTER_DIR children (MANIFEST, BUILD_SCRIPT, COVERAGE_REPORT, CACHE, TELEMETRY, MODE_MAP, WEIGHTS, CALIBRATION_PATH, EVOLUTION_STATE, TRIGGER at :76-89) and HOOKS_DIR child WORKER_PATH (:91) now flow through the runtime-conditional root. SURFACE_FILE (:105) and GSD_CORE_DIR (:106) already derive from RUNTIME_CONFIG_DIR — they now inherit the runtime switch automatically. If any other constant in the file still calls `join(homedir(), '.claude', ...)` directly, rewire it to the runtime-conditional base — do not leave a `.claude` hardcode behind (the goal "the hardcoded RUNTIME_CONFIG_DIR is gone" includes its bypassing siblings).
    - The detection branch must not touch the `_require`/lazy-getter or any hot-path decision logic — it is module-load setup only. Verify `isMain()` (snapshot:54-61) behavior is unchanged.

    After editing tests/router.mjs.snapshot, open ~/.claude/hooks/router.mjs and apply the EXACT same bytes. Both files must remain byte-identical (31-01 mirror guard stays GREEN). Because tests import the live hook (~/.claude/hooks/router.mjs per cache-test:13), the install copy MUST carry the change for 31-01's tests to load it.
  </action>
  <verify>
    <automated>rtk node --test tests/router.runtime-tagging.test.mjs tests/router.mjs.snapshot.diff.test.mjs 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    The 31-01 runtime-tagging detection group is GREEN: RUNTIME constant equals "claude" in this repo shell (default/fail-open), ROUTER_RUNTIME=codex override flips it, out-of-enum stays "claude", and RUNTIME_CONFIG_DIR/ROUTER_DIR/HOOKS_DIR resolve under the active runtime's home. The mirror-desync guard stays GREEN (snapshot == live hook). No per-prompt IO was added.
  </acceptance_criteria>
  <done>Runtime detection (D-01/D-02/D-04) and the runtime-conditional ROUTER_DIR/HOOKS_DIR/children rewire (D-03) are green in the 31-01 detection/dir tests; snapshot == live hook byte-identical; the hardcoded .claude base is gone from the hook constant block.</done>
</task>

<task type="auto">
  <name>Task 2: Make the install/builder honor the runtime pin</name>
  <files>build-manifest.mjs</files>
  <read_first>build-manifest.mjs</read_first>
  <action>
    Per D-04, the install stage must be able to pin the runtime so a Codex install resolves ~/.codex paths. build-manifest.mjs already exposes env-overridable HOMEs (CLAUDE = ROUTER_CLAUDE_HOME || join(HOME,'.claude') at :31-33, and ROUTER_HOOK_PATH at :49). This task makes the builder runtime-aware:

    - Read `ROUTER_RUNTIME` (default `"claude"`). When it is `"codex"`, derive the builder's config/home root from `~/.codex` (via `ROUTER_CODEX_HOME || join(HOME, '.codex')`) instead of `~/.claude` so the manifest, mode-map, and hook path resolve under the codex home.
    - Keep the change additive and fail-open: if ROUTER_RUNTIME is unset or `"claude"`, behavior is byte-for-byte unchanged from today.
    - Do NOT implement the full Codex inventory walk (deferred: REQUIREMENTS line 73 note — `.codex` not in use; parity ships runtime-tagged shared telemetry + presence via canonical registry runtime variants). This task only makes the EXISTING install writer target the pinned runtime's home.

    The installed hook copy is written to `ROUTER_HOOK_PATH` (or the runtime home's hooks path) — this is where the deployable mirror must go, keeping lockstep with tests/router.mjs.snapshot.
  </action>
  <verify>
    <automated>rtk node --test tests/router.fresh-onboarding.test.mjs || exit 1; node -e "process.env.ROUTER_RUNTIME='claude'; await import('./build-manifest.mjs').catch(e=>{if(!/Missing/.test(String(e))) throw e})"</automated>
  </verify>
  <acceptance_criteria>
    build-manifest.mjs loads cleanly with ROUTER_RUNTIME=claude (unchanged default path). With ROUTER_RUNTIME=codex, the builder's config root/hook path target the codex home. Existing onboarding tests pass. The full Codex inventory completeness walk is NOT attempted (deferred).
  </acceptance_criteria>
  <done>build-manifest.mjs honors ROUTER_RUNTIME (codex pins the codex home for manifest/mode-map/hook path; claude default unchanged); onboarding tests pass; no full codex inventory walk.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm watcher ingest forwards/allowlists the runtime field (inspect-only or minimal wire)</name>
  <files>src/registry/watcher.mjs</files>
  <read_first>src/registry/watcher.mjs</read_first>
  <action>
    Inspect `telemetryRecordToEvidence` (src/registry/watcher.mjs ~:114) to determine whether it forward-maps the hook's raw telemetry fields or allowlists them against OUTCOME_FIELDS. Per RESEARCH A2, if the ingest allowlists the hook's raw record, the `runtime` field must be added to OUTCOME_FIELDS (this happens in plan 31-03, not this plan) so a runtime-tagged record survives ingest. In THIS plan, make no behavior change to OUTCOME_FIELDS (still 14 fields — the deliberate bump is 31-03). If ingest maps fields explicitly and would drop `runtime`, note in the SUMMARY exactly where the `runtime` (and `epoch`) fields must be threaded in 31-03 so the runtime tag appears in evidence. Do not add the runtime field to the schema here — only map the wiring point and document it for 31-03.
  </action>
  <verify>
    <automated>rtk node --test tests/router.registry-diff.test.mjs tests/router.registry-watcher.test.mjs || exit 1; rtk node --test tests/router.health.outcome-schema.test.mjs 2>&1 | grep -E "size|expected|actual|fail" | head -5 || true</automated>
  </verify>
  <acceptance_criteria>
    The exact field-mapping for `runtime`/`epoch` through telemetryRecordToEvidence is identified and documented in the SUMMARY (the precise OUTCOME_FIELDS bump site for 31-03). No schema change is made here — `src/health/outcome-schema.mjs` stays at 14 fields. NOTE: the 31-01 outcome-schema enforcement test (which now expects 16) is EXPECTED RED at this wave — it is the deliberate 31-03 bump target, NOT a failure of this task; do NOT bump the schema or edit that test here. Only the registry/ingest tests are expected GREEN.
  </acceptance_criteria>
  <done>The runtime/epoch wiring point through telemetryRecordToEvidence is documented for 31-03 in the SUMMARY; src/health/outcome-schema.mjs stays at 14 fields and the outcome-schema test is EXPECTED RED (16-field expectation — the deliberate 31-03 bump target); registry tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process env / argv → detectRuntime() | Untrusted env/argv could spoof ROUTER_RUNTIME or a codex marker at module load; the returned string flows into path construction |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-01 | Spoofing | detectRuntime() | medium | mitigate | RUNTIME clamped to enum `"claude"\|"codex"`; out-of-enum ROUTER_RUNTIME → default `"claude"`. 31-01 test group 3 asserts fail-open. |
| T-31-02 | Tampering | RUNTIME_CONFIG_DIR / ROUTER_DIR / HOOKS_DIR | medium | mitigate | Path root = `join(homedir(), '.' + <enum-clamped runtime>)`; runtime value can never be attacker-controlled beyond the enum, so the path segment is bounded. |
| T-31-04 | Tampering | mirror trust (snapshot ↔ live hook) | medium | mitigate | detectRuntime wired in tests/router.mjs.snapshot AND ~/.claude/hooks/router.mjs in the same commit; 31-01 snapshot.diff guard stays GREEN. |
</threat_model>

<verification>
Run full suite at wave merge: `rtk node --test tests/*.test.mjs`.
</verification>

<success_criteria>
- The hardcoded .claude config-dir is gone: RUNTIME_CONFIG_DIR, ROUTER_DIR, HOOKS_DIR and all descendants resolve to the active runtime's home.
- Detection is deterministic, enum-clamped, fail-open, and cached once at module load with zero per-prompt IO.
- 31-01 detection/dir groups GREEN; mirror guard GREEN; full suite GREEN.
- D-07/D-08 resolve-layer behavior explicitly deferred to Phase 32 (resolve hot path absent until Phase 32).
</success_criteria>

<output>
Create `.planning/phases/31-runtime-tagging/31-02-SUMMARY.md` when done
</output>

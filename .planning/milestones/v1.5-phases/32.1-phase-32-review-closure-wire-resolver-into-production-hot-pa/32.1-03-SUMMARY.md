---
phase: 32.1-phase-32-review-closure
plan: 03
subsystem: infra
tags: [build-manifest, strict-coverage, resolve-list, tie-lint, coverage-audit]

# Dependency graph
requires:
  - phase: 32-intent-first-routing-mode-map-schema-v4-guard-hole-closure
    provides: schema-v4 resolve lists, forward-orphan diagnostics, and ROUTE-05 tie-lint
provides:
  - strict build-manifest enforcement for near-ties and unresolvable stale resolve lists
  - explicit non-blocking quarantined_fallback coverage diagnostics
  - sandboxed end-to-end regression coverage for strict and non-strict builds
affects: [32.1 review closure, release gates, coverage reports]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - stdlib-only tie-lint import with deterministic stderr diagnostics
    - resolvability-aware split between blocking forward diagnostics and quarantined fallbacks

key-files:
  created:
    - tests/router.build-gate.test.mjs
  modified:
    - build-manifest.mjs
    - src/coverage/audit.mjs
    - tests/router.coverage-audit.test.mjs

key-decisions:
  - "Near-tie violations always fail strict coverage; stale resolve members fail only when no mode or resolve candidate is present in the active command inventory."
  - "Resolvable routes retain absent optional members in quarantined_diagnostics while excluding them from forward_diagnostics and strict failure."
  - "Task 3 severity implementation was completed before Task 2's final subprocess verification because the planned resolvable-stale fixture depends on that classification."

patterns-established:
  - "Build gates report every tie-lint violation in deterministic order without mutating mode-map input."
  - "Coverage reports distinguish blocking forward diagnostics from visible non-blocking quarantine diagnostics."

requirements-completed: [ROUTE-05]

coverage:
  - id: D1
    description: "Strict build-manifest gate rejects near-ties and unresolvable stale routes while accepting clean and resolvable-stale routes."
    requirement: ROUTE-05
    verification:
      - kind: integration
        ref: "node --test tests/router.build-gate.test.mjs"
        status: pass
    human_judgment: false
  - id: D2
    description: "Coverage audit classifies absent optional resolve members as quarantined_fallback and keeps fully stale routes blocking."
    requirement: ROUTE-05
    verification:
      - kind: unit
        ref: "node --test tests/router.coverage-audit.test.mjs tests/router.resolve-tie-lint.test.mjs"
        status: pass
    human_judgment: false

# Metrics
duration: 10min
completed: 2026-08-01
status: complete
---

# Phase 32.1 Plan 03: Strict resolve-list build gate and fallback severity Summary

**Strict coverage now enforces resolve-list hygiene while preserving resolvable routes with quarantined optional fallbacks.**

## Performance

- **Duration:** 10 min
- **Started:** 2026-08-01T17:27:46Z
- **Completed:** 2026-08-01T17:37:07Z
- **Tasks:** 3
- **Files modified:** 4

## Accomplishments

- Wired `lintModeMap()` and `commandInventory()` into `build-manifest.mjs`, with deterministic stderr diagnostics and strict-only failure for near-ties or genuinely unresolvable stale routes.
- Added `quarantined_diagnostics` to coverage reports; absent optional resolve members no longer inflate blocking `forward_diagnostics` when another candidate resolves.
- Added sandboxed subprocess coverage for near-tie, wide-gap, stale-unresolvable, resolvable-stale, and non-strict behavior.

## Task Commits

Each task was committed atomically, including TDD RED/GREEN commits:

1. **Task 1: Wire lintModeMap into build-manifest strict-coverage gate** - RED `b62c094`, GREEN `8863354`
2. **Task 2: Gate fixture — craft a near-tie mode-map and drive build-manifest --strict-coverage** - `548c9c5`
3. **Task 3: Classify absent resolve-member severity in audit** - RED `fea6c88`, GREEN `d4c92d0`

**Plan metadata:** intentionally not committed by the SDK because `.planning/config.json` sets `commit_docs: false`.

## Files Created/Modified

- `build-manifest.mjs` - Runs tie-lint against the freshly built manifest and gates only blocking violations under `--strict-coverage`.
- `src/coverage/audit.mjs` - Separates non-blocking `quarantined_fallback` diagnostics from blocking stale targets.
- `tests/router.build-gate.test.mjs` - Fully sandboxed end-to-end builder gate fixture.
- `tests/router.coverage-audit.test.mjs` - Regression coverage for mode/member resolvability and severity output.

## Decisions Made

- Near-ties are always blocking under strict coverage; absent members are blocking only when no active command candidate resolves the route.
- Quarantine diagnostics remain visible in the report and fingerprint but do not feed the strict forward-diagnostic failure branch.
- The fixture maps both present commands through typed routes so clean strict cases prove tie-lint, not reverse-gap accounting, controls the near-tie verdict.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking test fixture] Added a present command to the existing audit builder helper**
- **Found during:** Task 3 (severity regression tests)
- **Issue:** The existing helper created an empty command inventory, making its intended resolvable-stale case fully unresolvable.
- **Fix:** Added an optional `commandName` fixture input and used it to create `resolve-mode` for the resolvable builder assertion.
- **Files modified:** `tests/router.coverage-audit.test.mjs`
- **Verification:** `node --test tests/router.coverage-audit.test.mjs` passed 18/18.
- **Committed in:** `d4c92d0`

**Total deviations:** 1 auto-fixed (Rule 3 test-fixture blocker).
**Impact on plan:** No production scope expansion; the fixture now tests the specified resolvable route semantics.

## Authentication Gates

None.

## Issues Encountered

- Focused verification passed: build gate 5/5, coverage+tielint 26/26, and existing builder regressions 15/15.
- The project-wide sweep (`rtk node --test tests/*.test.mjs`) reported 1240/1262 passing and 22 failures in unrelated installed-controller lifecycle timing, test-mode seam, and canonical installed mode-map parity areas. These are recorded in `deferred-items.md` and were not changed because they are outside this plan's four source/test files.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- ROUTE-05 strict resolve-list gating and absent-fallback severity are ready for the remaining Phase 32.1 closure plans.
- The live manifest/mode-map stale-target and lifecycle parity findings remain deferred to their owning release-gate work.

---
*Phase: 32.1-phase-32-review-closure*
*Completed: 2026-08-01*

## Self-Check: PASSED

- All four scoped source/test files and the summary exist.
- All five task/TDD commit hashes are present in git history.

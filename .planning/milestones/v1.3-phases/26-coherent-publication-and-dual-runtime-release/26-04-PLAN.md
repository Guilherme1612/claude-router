---
phase: 26-coherent-publication-and-dual-runtime-release
plan: 04
type: execute
wave: 3
depends_on: [26-03]
files_modified:
  - src/registry/watcher.mjs
  - src/prompt/publish-index.mjs
  - src/lifecycle/router-lifecycle.mjs
  - tests/router.phase26-lifecycle.test.mjs
autonomous: true
requirements: [REL-05, REL-08]
must_haves:
  truths:
    - "Verifier and canary success precede complete tuple activation."
    - "Build, write, verify, canary, pointer, or reload failure preserves or restores last-known-good."
    - "Rollback and crash recovery restore the complete tuple, not only the registry."
  artifacts:
    - "src/registry/watcher.mjs"
    - "src/lifecycle/router-lifecycle.mjs"
    - "tests/router.phase26-lifecycle.test.mjs"
  key_links:
    - "Watcher candidate flows through existing verifier/canary before publisher pointer replacement."
    - "Lifecycle recovery invokes the existing complete tuple recovery seam."
---

<objective>
Carry the complete tuple through the existing guarded activation, rollback, and recovery lifecycle.

Purpose: Prove failures cannot strand a mixed or unverified installed routing state.
Output: Extended watcher/lifecycle wiring and an adversarial lifecycle matrix.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-coherent-publication-and-dual-runtime-release/26-CONTEXT.md
@.planning/phases/26-coherent-publication-and-dual-runtime-release/26-03-SUMMARY.md
@src/registry/watcher.mjs
@src/lifecycle/router-lifecycle.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gate complete tuple activation through existing verifier and canary</name>
  <files>src/registry/watcher.mjs, src/prompt/publish-index.mjs, tests/router.phase26-lifecycle.test.mjs</files>
  <behavior>
    - Candidate tuple activation occurs only after existing verifier and canary success.
    - Failure at build, each member write, manifest write, verification, canary, or pointer replacement leaves active bytes unchanged.
    - Post-activation reload failure restores verified known-good.
  </behavior>
  <action>Wire the complete candidate tuple through the existing watcher verifier, canary, activation, and known-good order. Extend existing crash injection points to tuple members without adding another gate or controller. Assert exact active pointer and routed-byte snapshots at every failure point.</action>
  <verify>
    <automated>rtk node --test --test-concurrency=1 tests/router.phase26-lifecycle.test.mjs tests/router.registry-watcher.test.mjs tests/router.registry-activate.test.mjs</automated>
  </verify>
  <done>No failure point exposes mixed or unverified routing; successful activation exposes the complete tuple once.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve complete tuple rollback and crash recovery</name>
  <files>src/lifecycle/router-lifecycle.mjs, src/prompt/publish-index.mjs, tests/router.phase26-lifecycle.test.mjs</files>
  <behavior>
    - Rollback restores one verified complete known-good tuple.
    - Restart recovery repairs interrupted pointer state without reconstructing authority from mutable candidates.
    - Recovered prompt routing matches the known-good bytes.
  </behavior>
  <action>Extend the existing lifecycle recovery path to preserve and restore the complete tuple pointer and immutable siblings. Reuse `recoverReleaseTuple`, owned-generation manifests, rollback, and recovery logic; do not introduce a parallel recovery store. Cover crash-before/after pointer transitions and installed restart.</action>
  <verify>
    <automated>rtk node --test --test-concurrency=1 tests/router.phase26-lifecycle.test.mjs tests/router.lifecycle-recovery.test.mjs</automated>
  </verify>
  <done>Rollback and recovery restore verified complete known-good routing after every injected interruption.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candidate -> active pointer | Candidate gains installed routing authority. |
| interrupted process -> restart recovery | Mutable crash residue must not supersede verified state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-09 | Elevation of Privilege | activation | critical | mitigate | Existing verifier/canary gates precede pointer replacement. |
| T-26-10 | Tampering | crash recovery | high | mitigate | Recover only hash-verified immutable known-good tuple. |
</threat_model>

<verification>
Run the lifecycle gate serially with watcher, activation, and recovery regressions.
</verification>

<success_criteria>
The complete tuple uses the established activation lifecycle and remains recoverable under every required failure point.
</success_criteria>

<output>
Create `.planning/phases/26-coherent-publication-and-dual-runtime-release/26-04-SUMMARY.md` when done.
</output>

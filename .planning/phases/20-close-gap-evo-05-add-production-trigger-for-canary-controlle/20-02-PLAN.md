---
phase: 20-close-gap-evo-05-add-production-trigger-for-canary-controlle
plan: 02
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - src/registry/watcher.mjs
  - src/prompt/compile-index.mjs
  - src/evolution/candidate-calibration-route.mjs
  - tests/router.watcher-canary-trigger.test.mjs
autonomous: true
requirements: [EVO-05]
user_setup: []

must_haves:
  truths:
    - "When the watcher reconciles an eligible+recoveryReady candidate AND a known-good version exists AND the project evidence window is sufficient (>=30 samples), the watcher invokes proposeCandidate -> evaluateCandidate -> applyCanaryDecision instead of calling activateCandidate directly (per D-04 route-fn helper + D-05 demonstrated_benefit derivation + D-06 compatible() export)."
    - "When the evidence window is insufficient (<30 samples or empty), the watcher preserves (writes candidate.json, does not promote) with reason_code='insufficient_evidence_samples' — fail closed, never invokes uncompiled slow path."
    - "When recoverActiveVersion returns no_valid_history (first-ever activation, no active.json), the watcher activates directly via the existing activateCandidate path (bootstrap) — the first activation becomes the known-good with no canary rollback target."
    - "applyCanaryDecision delegates all publication mutation exclusively through REGISTRY_PUBLICATION (activate.mjs activateCandidate/previewRollback/executeRollback) — the watcher never writes active.json directly."
    - "The 6 REQUIRED_GATES (safety, privacy, quality, context_budget, compatibility, latency) are constructed from the reconciliation report, evidence window, perf-measure assessCalibration, and the now-exported standalone compatible() function (D-06) — NOT via COMPILED_INDEX_COMPATIBILITY.compatible (which does not exist as a method)."
    - "The watcher hot path (the reconcile function) runs in the controller child process, NOT in the prompt-routing hot path; src/context/prompt-route.mjs is unchanged and imports none of this wiring."
    - "D-04 helper mechanism: a reusable buildCandidateCalibrationRoute({ registry, mapping, policyFingerprint, now, corpus, deps }) lives in src/evolution/candidate-calibration-route.mjs, wraps routeContextPrompt with the candidate's compiled index published to per-fixture temp ownedRoots (replicating tests/router.compiled-evolution.test.mjs:57-89 buildRealCalibrationRoute pattern), and is shared by watcher (20-02) and CLI promote (20-03 Task 2)."
    - "D-05 demonstrated_benefit derivation: the watcher derives demonstrated_benefit by running measureRoutes on BOTH the candidate route fn (built via the D-04 helper) AND a known-good route fn (built via the SAME D-04 helper against the active.json compiled index recovered through recoverActiveVersion), using the known-good measurement as baseline. The demonstrated_benefit object passed to applyCanaryDecision carries { status, reason_code } with status in { demonstrated, safety_correction, neutral }."
    - "D-05 comparison predicate: status='demonstrated' when all 6 gates pass AND (candidate quality OR context_budget strictly better than known-good) AND latency gate passes (p95<25ms && max<100ms via assessCalibration). status='neutral' when all gates pass and candidate is equal-but-not-better on quality AND context_budget (preserve, never promote on parity — matches Phase 17 'automatic rollback when quality regresses'). status='safety_correction' when all gates pass, candidate neutral on perf, and the reconciliation report indicates a safety/recovery fix. Any gate fail -> evaluation.promotable=false -> applyCanaryDecision rolls back (if published_version) or rejects; demonstrated_benefit is not consulted. latency is a HARD GATE (assessCalibration), NOT a tiebreaker."
    - "D-06 compatible() export: src/prompt/compile-index.mjs exports the standalone compatible function (one-token change: `function compatible(value)` -> `export function compatible(value)`), and the watcher imports { compatible } from '../prompt/compile-index.mjs' to call compatible(candidateMetadata.compatibility)."
    - "Cadence decision (RESEARCH.md Open Question Q3, DEFERRED): v1 runs canary eval on every eligible reconcile WITHOUT candidate-fingerprint caching. assessCalibration is bounded (<1s, 140 route invocations) and the watcher debounces (debounceMs=250, maxLatencyMs=1500), so per-reconcile cost is acceptable for v1; revisit with a last-evaluated-fingerprint cache if reconcile frequency or assessCalibration cost spikes."
    - "Privacy gate is defense-in-depth and structurally a no-op post-20-01: the telemetry bridge skips privacy-denied records at ingest (20-01), so window.records never contains deny_filtered records — the watcher privacy gate (reject if any record carries privacy_guard/deny_filtered/secret_detected/content_detected guard codes) cannot fail given 20-01's skip behavior. Keep the check; note it enforces ingest-time invariants, not a runtime decision."
  artifacts:
    - "src/registry/watcher.mjs (modified: new imports of canary-controller, evidence, perf-measure, compile-index { compatible }, candidate-calibration-route; reconcile eligible+recoveryReady branch at lines 328-348 routes through canary when conditions met)"
    - "src/prompt/compile-index.mjs (one-token export change: `function compatible(value)` -> `export function compatible(value)` — the ONLY change to this file)"
    - "src/evolution/candidate-calibration-route.mjs (NEW file: exports buildCandidateCalibrationRoute and buildKnownGoodCalibrationRoute — the D-04 shared helper)"
    - "tests/router.watcher-canary-trigger.test.mjs (NEW integration test covering promote/preserve/bootstrap/rollback/helper/demonstrated_benefit edges)"
  key_links:
    - "watcher.mjs reconcile -> candidate-calibration-route.mjs buildCandidateCalibrationRoute / buildKnownGoodCalibrationRoute (D-04 helper)"
    - "watcher.mjs reconcile -> canary-controller.mjs proposeCandidate/evaluateCandidate/applyCanaryDecision"
    - "watcher.mjs reconcile -> evidence.mjs createPersistentEvidenceStore.window (sufficiency gate)"
    - "watcher.mjs reconcile -> activate.mjs recoverActiveVersion (known-good + bootstrap branch)"
    - "watcher.mjs reconcile -> perf-measure.mjs assessCalibration + measureRoutes (3 of 6 gates + D-05 derivation) + compile-index.mjs { compatible } (compatibility gate, D-06)"
  backstops:
    - statement: "The D-04 helper must not touch the prompt-routing hot path and must clean up its temp ownedRoots on every reconcile (success and error paths) — a tempdir leak across reconciles would exhaust disk under sustained watcher activity."
      verification: backstop
    - statement: "The D-05 measureRoutes baseline MUST be the known-good compiled index, NOT an arbitrary prior — using a stale or candidate-derived baseline would make a regression look like an improvement."
      verification: backstop
    - statement: "The D-06 export change must be purely additive (add the `export` keyword) — any other modification to compile-index.mjs risks breaking loadCompiledIndex/verifyVersion which call compatible() internally."
      verification: backstop
  prohibitions:
    - "The watcher MUST NOT call COMPILED_INDEX_COMPATIBILITY.compatible(...) — that method does not exist (D-06). The watcher imports { compatible } and calls compatible(candidateMetadata.compatibility)."
    - "The watcher MUST NOT hardcode demonstrated_benefit (the existing test at tests/router.compiled-evolution.test.mjs:128 hardcodes {status:'demonstrated', reason_code:'context_bytes_reduced'} — production must derive it via D-05)."
    - "The watcher MUST NOT promote on parity (status='neutral' preserves via applyCanaryDecision; only 'demonstrated' or 'safety_correction' promote — canary-controller.mjs:195-203)."
    - "The watcher MUST NOT write active.json directly; all publication mutation flows through applyCanaryDecision -> REGISTRY_PUBLICATION -> activate.mjs."
    - "The D-04 helper MUST NOT touch src/context/prompt-route.mjs; it imports routeContextPrompt and calls it with ownedRoot/projectRoot bound to temp ownedRoots."

# Specless probe fallback SKIPPED (visible, recorded choice) — see 20-01-PLAN.md frontmatter
# note for the full rationale. No probe-derived predicates; must_haves.truths derived from
# phase goal + RESEARCH.md + Phase 17 success criterion #4 + audit BLOCKER 2.
# 2026-07-21 revision: closed Gap A (D-04 route-fn helper), Gap B (D-05 demonstrated_benefit
# derivation), Gap C (D-06 compatible() export). See 20-CONTEXT.md decisions D-03..D-06.
---

<objective>
Wire the watcher's eligible-activation branch (src/registry/watcher.mjs:328-348) to route eligible+recoveryReady candidates through the canary-controller (proposeCandidate -> evaluateCandidate -> applyCanaryDecision) instead of calling activateCandidate directly. This is the PRIMARY automatic production trigger that closes audit BLOCKER 2 (lines 177-180): telemetry-driven canary promotion + automatic rollback becomes reachable from the watcher's background control plane.

This is a TARGETED REVISION of the original 20-02 plan. Three gaps closed per 20-CONTEXT.md locked decisions:
- Gap A (D-04): the route-fn helper `<candidate route fn>` placeholder is replaced by a concrete reusable helper `buildCandidateCalibrationRoute` in a new file `src/evolution/candidate-calibration-route.mjs`, replicating `buildRealCalibrationRoute` from tests/router.compiled-evolution.test.mjs:57-89. The helper is shared by watcher (20-02) and CLI promote (20-03 Task 2) — satisfies the D-04 sharing constraint (CONTEXT line 122).
- Gap B (D-05): `demonstrated_benefit` is derived by running `measureRoutes` on BOTH candidate and known-good compiled indexes via the D-04 helper, with the known-good as baseline. The comparison predicate is strict-improve on quality OR context_budget, latency as hard gate, never promote on parity.
- Gap C (D-06): `compatible` is exported from `src/prompt/compile-index.mjs` (one-token change: `function compatible(value)` -> `export function compatible(value)`), imported by name into the watcher. The wrong `COMPILED_INDEX_COMPATIBILITY.compatible(...)` call is removed.

Purpose: The canary-controller library (Plan 01 deployed it) has no production caller. The watcher is the natural trigger — it already builds candidates and holds the activation context. This plan adds the evidence-sufficiency gate, the 6-gate construction (with the D-04 helper + D-05 derivation + D-06 compatible() import), and the promote/rollback/preserve decision routing.
Output: Modified src/registry/watcher.mjs reconcile (lines 328-348), one-token export added to src/prompt/compile-index.mjs, NEW src/evolution/candidate-calibration-route.mjs, NEW tests/router.watcher-canary-trigger.test.mjs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-close-gap-evo-05-add-production-trigger-for-canary-controlle/20-RESEARCH.md
@.planning/phases/20-close-gap-evo-05-add-production-trigger-for-canary-controlle/20-CONTEXT.md
@.planning/phases/20-close-gap-evo-05-add-production-trigger-for-canary-controlle/20-01-SUMMARY.md
@.planning/v1.2-MILESTONE-AUDIT.md
@.claude/CLAUDE.md
@src/registry/watcher.mjs
@src/evolution/canary-controller.mjs
@src/evolution/evidence.mjs
@src/evolution/perf-measure.mjs
@src/registry/activate.mjs
@src/prompt/compile-index.mjs
@src/prompt/publish-index.mjs
@src/context/prompt-route.mjs
@tests/router.compiled-evolution.test.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Watcher canary trigger integration test (promote/preserve/bootstrap/rollback + D-04/D-05/D-06 edges)</name>
  <files>tests/router.watcher-canary-trigger.test.mjs</files>
  <read_first>
    - src/registry/watcher.mjs lines 246-365 (createRegistryReconciler signature, dependencies injection at 258-261 activator/recovery/mapper/verifier/publishCompiledIndex/writeJson/readActive, reconcile function 284-363, eligible+recoveryReady branch 328-348)
    - tests/router.compiled-evolution.test.mjs lines 1-156 (the D-04 helper pattern source: buildRealCalibrationRoute at 57-89, publishCompiledFixture at 34-55, calibrationCapsule at 21-32, the test harness style; the hardcoded demonstrated_benefit at 128 — D-05 production must NOT replicate this; measureRoutes baseline_delta at 203-205)
    - src/prompt/compile-index.mjs lines 66-91 (validRoutes function, the standalone `function compatible(value)` at line 84 — the D-06 export target; COMPILED_INDEX_COMPATIBILITY frozen object at lines 10-17 has NO compatible method)
    - src/evolution/canary-controller.mjs lines 11-12 (REQUIRED_GATES array), lines 54-132 (proposeCandidate, evaluateCandidate), lines 148-217 (applyCanaryDecision — promotion gate on demonstrated_benefit.status at 195-203)
    - src/evolution/perf-measure.mjs lines 14-22 (CALIBRATION_CORPUS 7 fixtures), lines 34-54 (evaluateCalibrationCorpus — route fn contract: returns {outcome, dispatch_eligible, [context_within_budget]}), lines 62-84 (measureRoutes — baseline/baseline_delta), lines 86-96 (assessCalibration — p95<25ms && max<100ms latency gate)
    - src/registry/activate.mjs lines 190-197 (recoverActiveVersion — returns recovery_status:'blocked', reason_code:'no_valid_history' when no versions exist), lines 179-188 (activateCandidate)
    - src/context/prompt-route.mjs lines 84-128 (routeContextPrompt — accepts {prompt, ownedRoot, projectRoot, forceStale, authoritative, now, compiledFs}; loads compiledIndex via loadCompiledIndex({ownedRoot}); the helper wraps this)
    - .planning/phases/20-close-gap-evo-05-add-production-trigger-for-canary-controlle/20-RESEARCH.md (Pattern 3 "Watcher canary trigger integration" lines 284-320; Pitfall 5 "Insufficient evidence must preserve" 407-411; Runtime State "active.json bootstrapping" 370-379)
    - tests/router.evolution-canary.test.mjs lines 194-270 (D-10/D-11/D-12 test patterns — validSignal helper, gate construction, applyCanaryDecision promote/rollback assertions — replicate the test harness style)
    - tests/router.autonomous-lifecycle.test.mjs (grep for createTestRegistryReconciler / dependencies injection patterns — the existing watcher integration test style)
  </read_first>
  <behavior>
    - Test 1 (promote path with real D-05 derivation): given a createTestRegistryReconciler with an eligible candidate, a known-good version present (recoverActiveVersion returns recovery_status:'healthy'), a sufficient evidence window (createPersistentEvidenceStore.window returns sufficient:true with >=30 samples), all 6 gates pass, AND the candidate's measureRoutes quality/context_budget strictly better than the known-good baseline (assert via injected stub measureRoutes that returns candidatePerf with lower p95 OR a fixture where candidate passes more fixtures than known-good) -> reconcile returns activation.activation_status='activated' AND applyCanaryDecision was invoked with demonstrated_benefit.status='demonstrated' and demonstrated_benefit.reason_code in {'quality_improved','context_bytes_reduced','latency_reduced'} AND known_good_version equal to the recovered version.
    - Test 2 (preserve on insufficient evidence): eligible candidate + known-good present + evidence window sufficient:false (injected store returns sample_count < 30) -> reconcile returns activation.activation_status='preserved', reason_code='insufficient_evidence_samples' AND activateCandidate was NOT called (assert the activator spy call count is 0) AND applyCanaryDecision was NOT called AND no D-04 helper temp ownedRoots were created (assert buildCandidateCalibrationRoute spy NOT called).
    - Test 3 (bootstrap direct activation): eligible candidate + recoverActiveVersion returns recovery_status:'blocked', reason_code:'no_valid_history' (no active.json, first-ever) -> reconcile calls activator directly (existing path), activation.activation_status='activated', applyCanaryDecision NOT called, reason:'watcher' (not 'canary'), buildCandidateCalibrationRoute NOT called (no canary eval when no known-good to compare against).
    - Test 4 (rollback on gate failure): eligible candidate + known-good present + sufficient evidence + one gate fails (e.g. quality.pass=false) + published_version exists -> applyCanaryDecision returns status='rolled_back' (rollback branch to known_good_version via previewRollback/executeRollback) AND activateCandidate promote branch NOT called. demonstrated_benefit is NOT consulted (evaluation.promotable=false short-circuits at canary-controller.mjs:175-193).
    - Test 5 (neutral preserves on parity — D-05 predicate): eligible candidate + known-good present + sufficient evidence + all 6 gates pass + candidate measureRoutes EQUAL to known-good baseline (no strict improvement on quality OR context_budget) + non-safety reconciliation -> applyCanaryDecision called with demonstrated_benefit.status='neutral' -> decision.status='preserved' -> activation.activation_status='preserved', reason_code='benefit_not_demonstrated' (canary-controller.mjs:198-203). Confirms never-promote-on-parity (Phase 17 success criterion #4).
    - Test 6 (D-04 helper unit — candidate route fn produces normalized output): call buildCandidateCalibrationRoute directly with a stub registry + stub publishCompiledIndex + stub saveCapsule + stub routeContextPrompt -> returned route(fixture) invokes routeContextPrompt with {prompt: fixture.input.prompt, ownedRoot: <per-fixture temp ownedRoot>, projectRoot: <same>, now, ...forceStale/authoritative handling}, and returns {outcome, dispatch_eligible[, context_within_budget]} matching the buildRealCalibrationRoute normalization at tests/router.compiled-evolution.test.mjs:79-87. cleanup() removes all temp ownedRoots.
    - Test 7 (D-06 compatible() import): dynamic-import src/prompt/compile-index.mjs and assert `typeof compatible === 'function'` (the export exists). Dynamic-import src/registry/watcher.mjs and assert the watcher module imports compatible (grep watcher.mjs source for `import { compatible } from '../prompt/compile-index.mjs'` OR use module introspection — the watcher must NOT call COMPILED_INDEX_COMPATIBILITY.compatible).
  </behavior>
  <action>
    Create tests/router.watcher-canary-trigger.test.mjs using node:test + assert/strict + dynamic import via `new URL('../src/...', import.meta.url)` (replicate tests/router.compiled-evolution.test.mjs lines 1-17 style). Use createTestRegistryReconciler (src/registry/watcher.mjs line 367) with injected dependencies: activator (spy), recovery (stub returning the desired recovery_status), mapper, verifier, publishCompiledIndex (spy/stub), and the new canary decision function (spy/stub returning the desired applyCanaryDecision status — note: applyCanaryDecision itself is NOT injected via the dependencies seam today; either (a) add a `canaryDecision` dependency to createRegistryReconciler's dependencies injection in Task 3, OR (b) stub via module-level import replacement. Prefer (a) — extend the existing dependencies seam, mirroring activator/recovery/publishCompiledIndex). Inject stub measureRoutes / assessCalibration / buildCandidateCalibrationRoute via the same seam OR stub the perf-measure + candidate-calibration-route module imports. For Test 6 + Test 7, call the helper and the compile-index export directly (no watcher). Use mkdtempSync for any tmpdirs; clean up in try/finally or test.after. Construct the 6 gates per RESEARCH.md Pattern 3 lines 290-296: safety from report.disposition==='eligible'; privacy from evidence window (no privacy-denied records — structurally a no-op post-20-01, keep as defense-in-depth); quality/context_budget/latency from assessCalibration (or stub the gate outcomes directly for unit-test speed); compatibility from `compatible(candidateMetadata.compatibility)` (D-06 — the standalone import, NOT COMPILED_INDEX_COMPATIBILITY.compatible). Implement the 7 behavior tests above. The tests must verify the DECISION routing, not just the activation result — assert which spies were called and with what arguments (known_good_version, published_version, candidate fingerprint binding, demonstrated_benefit.{status, reason_code}). For Test 1 + Test 5, the demonstrated_benefit.status assertion is the load-bearing check (D-05 predicate).
  </action>
  <verify>
    <automated>node --test tests/router.watcher-canary-trigger.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - tests/router.watcher-canary-trigger.test.mjs exists and `node --test tests/router.watcher-canary-trigger.test.mjs` exits 0 with 7 passing tests.
    - Test 1 asserts applyCanaryDecision spy was called with demonstrated_benefit.status === 'demonstrated' AND demonstrated_benefit.reason_code in the strict-improve set {quality_improved, context_bytes_reduced, latency_reduced}.
    - Test 2 asserts activator spy call count === 0 AND applyCanaryDecision spy call count === 0 when evidence insufficient.
    - Test 3 asserts activator was called with reason === 'watcher' (NOT 'canary') AND applyCanaryDecision NOT called when recoverActiveVersion returns no_valid_history.
    - Test 4 asserts applyCanaryDecision returns status === 'rolled_back' when evaluation.promotable === false AND published_version present.
    - Test 5 asserts applyCanaryDecision called with demonstrated_benefit.status === 'neutral' AND decision.status === 'preserved' on parity (no strict improvement).
    - Test 6 asserts buildCandidateCalibrationRoute returns a route function that calls routeContextPrompt with ownedRoot and projectRoot bound to a temp ownedRoot, and cleanup() removes all tempdirs (assert via existsSync on each tempdir after cleanup).
    - Test 7 asserts `grep -c 'export function compatible' src/prompt/compile-index.mjs` returns 1 AND `grep -c 'COMPILED_INDEX_COMPATIBILITY.compatible' src/registry/watcher.mjs` returns 0 AND `grep -c "import { compatible } from '../prompt/compile-index.mjs'" src/registry/watcher.mjs` returns 1.
  </acceptance_criteria>
  <done>
    - tests/router.watcher-canary-trigger.test.mjs exists with 7 passing tests covering promote / preserve-insufficient / bootstrap / rollback-gate-failure / neutral-parity / D-04 helper unit / D-06 compatible() export.
    - Tests RED until Task 2 + Task 3 land (TDD cycle).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-04 helper (src/evolution/candidate-calibration-route.mjs) + D-06 compatible() export (src/prompt/compile-index.mjs)</name>
  <files>src/evolution/candidate-calibration-route.mjs, src/prompt/compile-index.mjs</files>
  <read_first>
    - tests/router.compiled-evolution.test.mjs lines 21-89 (calibrationCapsule at 21-32, publishCompiledFixture at 34-55, buildRealCalibrationRoute at 57-89 — the D-04 pattern source to replicate; note routeContextPrompt import at line 13, saveCapsule import at line 12, stableStringify at line 15)
    - src/evolution/perf-measure.mjs lines 14-22 (CALIBRATION_CORPUS — 7 fixtures, each with {id, fixture_class, input, expected, max_context_bytes})
    - src/prompt/compile-index.mjs lines 84-90 (the standalone `function compatible(value)` — the one-token export target; COMPILED_INDEX_COMPATIBILITY at 10-17 is a frozen object with NO compatible method)
    - src/context/prompt-route.mjs lines 84-128 (routeContextPrompt signature: {prompt, ownedRoot, projectRoot, forceStale, authoritative, now, compiledFs}; the helper wraps this)
    - src/prompt/publish-index.mjs (publishCompiledIndex signature — the watcher calls it at watcher.mjs:338-344 with {ownedRoot, registry, registryVersionId, mapping, policyFingerprint, now}; the helper reuses this to publish the candidate compiled index to temp ownedRoots)
    - src/context/capsule.mjs (saveCapsule signature — the helper saves a per-fixture capsule into each temp ownedRoot; calibrationCapsule shape at tests/router.compiled-evolution.test.mjs:21-32)
    - src/registry/activate.mjs lines 190-197 (recoverActiveVersion — the helper's known-good variant reads the compiled index from the activation_root that recoverActiveVersion points at)
  </read_first>
  <behavior>
    - Behavior 1 (D-06 export): `grep -c 'export function compatible' src/prompt/compile-index.mjs` returns 1. The export is purely additive — `loadCompiledIndex`, `verifyVersion`, `validRoutes`, `COMPILED_INDEX_COMPATIBILITY`, `COMPILED_INDEX_SCHEMA_VERSION`, `COMPILED_INDEX_LIMITS` exports unchanged. No other line in compile-index.mjs is modified.
    - Behavior 2 (D-04 helper — candidate variant): buildCandidateCalibrationRoute({ registry, mapping, policyFingerprint, now, corpus=CALIBRATION_CORPUS, deps }) returns { route, captures, cleanup, versionId }. The helper: (a) for each fixture in corpus, mkdtempSync a per-fixture temp ownedRoot; (b) publishCompiledIndex({ ownedRoot: tempRoot, registry, registryVersionId: <generated v1-... id>, mapping, policyFingerprint, now }) into each temp ownedRoot; (c) saveCapsule({ ownedRoot: tempRoot, capsule: calibrationCapsule(fixture) }) into each (replicating tests/router.compiled-evolution.test.mjs:34-55 publishCompiledFixture pattern); (d) build a route fn that, given a fixture, looks up its temp ownedRoot, calls routeContextPrompt({ prompt: fixture.input.prompt, ownedRoot: tempRoot, projectRoot: tempRoot, now, ...forceStale/authoritative handling per the fixture.input.force_stale / fixture.input.tied fields }), captures the routed result, and returns the normalized { outcome, dispatch_eligible[, context_within_budget] } shape (per tests/router.compiled-evolution.test.mjs:79-87); (e) cleanup() rmSync's every temp ownedRoot. deps allows injection of { publishCompiledIndex, saveCapsule, routeContextPrompt, mkdtempSync, rmSync } for tests (defaults to the real implementations).
    - Behavior 3 (D-04 helper — known-good variant): buildKnownGoodCalibrationRoute({ ownedRoot, now, corpus=CALIBRATION_CORPUS, deps }) returns { route, captures, cleanup }. The helper reuses the active compiled index already at ownedRoot (published by the prior successful activation — loadCompiledIndex({ownedRoot, now}) reads it). Per fixture, it mkdtempSync's a temp ownedRoot, copies/symlinks the compiled-index/ directory from the source ownedRoot into the temp (so the candidate's capsule writes don't mutate the real activation_root), saves a per-fixture capsule into the temp, and builds the same route fn shape as Behavior 2 bound to the temp ownedRoots. cleanup() removes the temp ownedRoots. The route fn for the known-good MUST produce the same normalized output shape as the candidate route fn (so measureRoutes compares apples to apples).
    - Behavior 4 (shared export): src/evolution/candidate-calibration-route.mjs exports both buildCandidateCalibrationRoute and buildKnownGoodCalibrationRoute (named exports, ESM). It imports routeContextPrompt from '../context/prompt-route.mjs', saveCapsule from '../context/capsule.mjs', publishCompiledIndex from '../prompt/publish-index.mjs', CALIBRATION_CORPUS from './perf-measure.mjs', and stdlib (mkdtempSync/rmSync from node:fs, tmpdir from node:os, join from node:path, randomUUID from node:crypto). It does NOT touch src/context/prompt-route.mjs (hot path stays read-only).
    - Behavior 5 (D-04 sharing — 20-03 reuse): the helper is importable by src/cli/router-control.mjs (20-03 Task 2) via `import { buildCandidateCalibrationRoute, buildKnownGoodCalibrationRoute } from '../evolution/candidate-calibration-route.mjs'`. The signature is stable and the file has no watcher-specific dependencies.
  </behavior>
  <action>
    Step 1 (D-06 export — one-token change): in src/prompt/compile-index.mjs at the line that reads `function compatible(value)` (search for the standalone function declaration; it is NOT a method on COMPILED_INDEX_COMPATIBILITY), change it to `export function compatible(value)`. Do NOT change any other line. Do NOT add it as a method on the COMPILED_INDEX_COMPATIBILITY frozen object. Do NOT modify the function body. The single change is adding the `export` keyword before `function compatible`.

    Step 2 (D-04 helper — new file): create src/evolution/candidate-calibration-route.mjs. Replicate the buildRealCalibrationRoute + publishCompiledFixture + calibrationCapsule patterns from tests/router.compiled-evolution.test.mjs:21-89 as production code. Export two named functions:

    - `buildCandidateCalibrationRoute({ registry, mapping, policyFingerprint, now = Date.now(), corpus = CALIBRATION_CORPUS, deps = {} })` — publishes the candidate compiled index (via publishCompiledIndex or deps.publishCompiledIndex) into a per-fixture temp ownedRoot, saves a per-fixture calibration capsule (via saveCapsule or deps.saveCapsule), and returns { route, captures, cleanup, versionId }. The route fn wraps routeContextPrompt (or deps.routeContextPrompt) per the test's normalization (lines 79-87). cleanup() removes all temp ownedRoots via rmSync (or deps.rmSync). Use mkdtempSync(join(tmpdir(), `router-canary-${fixture.fixture_class}-`)) for tempdir names (or deps.mkdtempSync).

    - `buildKnownGoodCalibrationRoute({ ownedRoot, now = Date.now(), corpus = CALIBRATION_CORPUS, deps = {} })` — builds the same shape of route fn but reuses the existing compiled index at ownedRoot (the active.json published by a prior successful activation). Per fixture, mkdtemp a temp ownedRoot, copy/symlink the compiled-index/ directory from ownedRoot into the temp (do NOT mutate the real activation_root with per-fixture capsules — keep it read-only), save a per-fixture capsule into the temp, return { route, captures, cleanup }.

    Include a private `calibrationCapsule(fixture, now)` helper in this file (replicate tests/router.compiled-evolution.test.mjs:21-32 — schema_version 1, scope {workspace_id:'calibration', project_id: fixture.id}, goal {id:'phase-17', summary:'Compiled routing calibration'}, position {workflow:'gsd-execute-phase', phase:'17', plan:'05', task: fixture.id}, status (fixture.input.status === 'complete' ? 'completed' : 'active'), artifacts [], blockers [], freshness {captured_at: 1_000, generation: fixture.id}, provenance {source:'calibration-fixture', version:'1'}). Do NOT export calibrationCapsule (it is an internal helper).

    Do NOT modify src/context/prompt-route.mjs. Do NOT modify src/prompt/publish-index.mjs. Do NOT modify src/context/capsule.mjs. The helper file is additive.
  </action>
  <verify>
    <automated>node --test tests/router.watcher-canary-trigger.test.mjs && grep -c 'export function compatible' src/prompt/compile-index.mjs && grep -c 'export function buildCandidateCalibrationRoute' src/evolution/candidate-calibration-route.mjs && grep -c 'export function buildKnownGoodCalibrationRoute' src/evolution/candidate-calibration-route.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export function compatible' src/prompt/compile-index.mjs` returns 1 (the D-06 one-token export landed).
    - `grep -v '^#' src/prompt/compile-index.mjs | wc -l` is unchanged from pre-edit (no other lines added/removed — the ONLY change is adding `export ` before `function compatible`).
    - `grep -c 'export function buildCandidateCalibrationRoute' src/evolution/candidate-calibration-route.mjs` returns 1.
    - `grep -c 'export function buildKnownGoodCalibrationRoute' src/evolution/candidate-calibration-route.mjs` returns 1.
    - `grep -c "import { routeContextPrompt } from '../context/prompt-route.mjs'" src/evolution/candidate-calibration-route.mjs` returns 1.
    - `grep -cE "import .* from '../context/prompt-route.mjs'" src/evolution/candidate-calibration-route.mjs` returns 1 AND `grep -cE "routeContextPrompt" src/context/prompt-route.mjs` is unchanged (the helper imports the existing function — no change to prompt-route.mjs).
    - `node -e "import('./src/prompt/compile-index.mjs').then(m => assert.equal(typeof m.compatible, 'function'))"` succeeds (the export is callable).
    - Test 6 + Test 7 from Task 1 now pass (the helper and the export exist).
  </acceptance_criteria>
  <done>
    - src/prompt/compile-index.mjs exports `compatible` (one-token additive change, no other line modified).
    - src/evolution/candidate-calibration-route.mjs exists and exports buildCandidateCalibrationRoute + buildKnownGoodCalibrationRoute.
    - Tests 6 and 7 from Task 1 pass (the rest of Task 1 stays RED until Task 3 wires the watcher).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire watcher.mjs reconcile to route eligible activations through canary-controller (D-04 helper + D-05 derivation + D-06 compatible import)</name>
  <files>src/registry/watcher.mjs</files>
  <read_first>
    - src/registry/watcher.mjs lines 1-17 (imports — add canary-controller, evidence, perf-measure, compile-index { compatible }, candidate-calibration-route; note existing loadCompiledIndex import at line 17 — compatible is added to a separate import statement OR a new one), lines 246-365 (createRegistryReconciler + reconcile), lines 328-348 (the eligible+recoveryReady branch to modify), lines 258-261 (dependencies injection — add `canaryDecision` and `buildCandidateRoute`/`buildKnownGoodRoute`/`measureRoutes`/`assessCalibration` injection seams for test spies)
    - src/evolution/canary-controller.mjs lines 11-12 (REQUIRED_GATES), lines 54-91 (proposeCandidate signature), lines 91-132 (evaluateCandidate — requires all 6 gates pass independently, no weighted compensation), lines 148-217 (applyCanaryDecision — promotion gate on demonstrated_benefit.status at 195-203: only 'demonstrated' or 'safety_correction' promote; 'neutral'/undefined preserves; !evaluation.promotable rolls back if published_version else rejects)
    - src/evolution/evidence.mjs (createPersistentEvidenceStore — added in Plan 01 Task 2; .window({project_id}) returns { sufficient, sample_count, weighted_samples, source_evidence_fingerprint, records })
    - src/evolution/perf-measure.mjs lines 34-54 (evaluateCalibrationCorpus — {corpus, route, versions} -> {quality, context_budget, fixtures}), lines 62-84 (measureRoutes — {fixtures, route, versions, baseline} -> {warm, baseline_delta}), lines 86-96 (assessCalibration — {evaluation, performance} -> {pass, quality, context_budget, latency}; latency.pass = p95<25ms && max<100ms)
    - src/prompt/compile-index.mjs lines 10-17 (COMPILED_INDEX_COMPATIBILITY.policy_version === 'workflow-transitions-v1') — the policy_version to bind into candidate.evaluation_inputs
    - src/registry/activate.mjs lines 190-197 (recoverActiveVersion — the helper's known-good variant reads the compiled index from the activation_root that recoverActiveVersion points at)
    - src/evolution/candidate-calibration-route.mjs (Task 2 — the D-04 helper to import)
    - tests/router.watcher-canary-trigger.test.mjs (Task 1 — the tests this wiring must pass)
    - .planning/phases/20-close-gap-evo-05-add-production-trigger-for-canary-controlle/20-RESEARCH.md (Pattern 3 lines 284-320 — the integration sketch; Pitfall 5 lines 407-411; bootstrapping 370-379)
  </read_first>
  <behavior>
    - Behavior 1 (promote via D-05 derivation): when report.disposition==='eligible' && recoveryReady && knownGood exists && window.sufficient===true && all 6 gates pass AND measureRoutes comparison shows strict improvement on quality OR context_budget AND latency gate passes (assessCalibration.latency.pass=true) -> applyCanaryDecision called with demonstrated_benefit={status:'demonstrated', reason_code: <one of quality_improved|context_bytes_reduced|latency_reduced>} AND evaluation.promotable=true -> decision.status='promoted' -> activation.activation_status='activated'.
    - Behavior 2 (preserve on insufficient evidence): when window.sufficient===false -> activation={ activation_status:'preserved', reason_code:'insufficient_evidence_samples' }; activator (activateCandidate) NOT called; applyCanaryDecision NOT called; buildCandidateCalibrationRoute NOT called.
    - Behavior 3 (bootstrap): when recoverActiveVersion returns recovery_status:'blocked', reason_code:'no_valid_history' -> the existing activator({...reason:'watcher', test_mode}) path runs directly (no canary eval, no D-04 helper, no applyCanaryDecision).
    - Behavior 4 (rollback on gate failure): when a gate fails AND published_version exists -> applyCanaryDecision called with evaluation.promotable=false -> decision.status='rolled_back' (delegates to previewRollback/executeRollback via REGISTRY_PUBLICATION); activateCandidate promote branch NOT called.
    - Behavior 5 (neutral preserves on parity — D-05): when all 6 gates pass AND candidate measureRoutes NOT strictly better than known-good on quality OR context_budget AND the reconciliation report is not a safety/recovery fix -> demonstrated_benefit={status:'neutral', reason_code:'no_strict_improvement'} -> applyCanaryDecision returns decision.status='preserved' (canary-controller.mjs:198-203) -> activation.activation_status='preserved'.
    - Behavior 6 (safety_correction promotes on parity): when all 6 gates pass AND candidate neutral on perf AND the reconciliation report indicates a safety/recovery fix (e.g. report.disposition==='eligible' && report.verdicts include a safety-code verdict) -> demonstrated_benefit={status:'safety_correction', reason_code:'safety_fix'} -> applyCanaryDecision promotes (canary-controller.mjs:196).
    - Behavior 7 (published_version passed): applyCanaryDecision receives published_version = the current active.version_id (or null for first activation in the non-bootstrap branch).
  </behavior>
  <action>
    Add imports at the top of src/registry/watcher.mjs (after existing imports lines 1-17): `import { proposeCandidate, evaluateCandidate, applyCanaryDecision } from '../evolution/canary-controller.mjs';` `import { createPersistentEvidenceStore } from '../evolution/evidence.mjs';` `import { assessCalibration, evaluateCalibrationCorpus, measureRoutes, CALIBRATION_CORPUS } from '../evolution/perf-measure.mjs';` `import { compatible, COMPILED_INDEX_COMPATIBILITY } from '../prompt/compile-index.mjs';` (D-06 — import compatible by name; keep COMPILED_INDEX_COMPATIBILITY for the policy_version field at line 12, do NOT call .compatible on it) `import { buildCandidateCalibrationRoute, buildKnownGoodCalibrationRoute } from '../evolution/candidate-calibration-route.mjs';` (D-04). Add `recoverActiveVersion` to the existing activate.mjs import at line 14 (already imported there per read — verify; if missing, add it).

    Extend the dependencies injection seam in createRegistryReconciler (lines 258-261): add `const canaryDecision = dependencies.applyCanaryDecision || applyCanaryDecision;` `const buildCandidateRoute = dependencies.buildCandidateCalibrationRoute || buildCandidateCalibrationRoute;` `const buildKnownGoodRoute = dependencies.buildKnownGoodCalibrationRoute || buildKnownGoodCalibrationRoute;` `const measure = dependencies.measureRoutes || measureRoutes;` `const assess = dependencies.assessCalibration || assessCalibration;` `const evaluateCorpus = dependencies.evaluateCalibrationCorpus || evaluateCalibrationCorpus;` `const createEvidenceStore = dependencies.createPersistentEvidenceStore || createPersistentEvidenceStore;` `const compatibleFn = dependencies.compatible || compatible;` (so tests can inject spies per Task 1).

    Modify the `if (report.disposition === 'eligible' && recoveryReady)` block at lines 328-348. After the existing `verification.disposition === 'passing' && verification.complete === true` check at line 335, BEFORE the current `activation = await activator(...)` call at line 336, insert the canary routing:

    1. Call `const recovery = await recovery({ ownedRoot: config.activation_root, test_mode: config.test_mode === true });` (the recovery injection at line 259 — reuse the existing call result if the reconciler already invoked it above; check the existing flow at lines 315-327 — the recovery call is already done at line 316, reuse its result instead of calling again). If `recovery.recovery_status === 'blocked' && recovery.reason_code === 'no_valid_history'` -> fall through to the existing `activator({...})` call (bootstrap path — no canary rollback possible). Set knownGood = null. Do NOT call the D-04 helper or applyCanaryDecision.
    2. Else set knownGood = recovery.version_id (recovery_status in ['healthy','recovered']).
    3. If knownGood !== null: open the persistent evidence store via `const store = createEvidenceStore({ root: join(config.activation_root, 'evidence') }); const window = store.window({ project_id: config.scope_id || config.scope || 'global' });`. If `window.sufficient !== true` -> set `activation = { activation_status:'preserved', reason_code:'insufficient_evidence_samples' }` and skip the activator call (Pitfall 5 — fail closed). Do NOT call applyCanaryDecision. Do NOT call the D-04 helper.
    4. If sufficient: build the candidate route fn via `const candidateRouteCtx = buildCandidateRoute({ registry: built.registry, mapping, policyFingerprint: verification.policy_fingerprint, now: verification.generated_at || Date.now(), deps: dependencies.helperDeps || undefined });` and the known-good route fn via `const knownGoodRouteCtx = buildKnownGoodRoute({ ownedRoot: config.activation_root, now: verification.generated_at || Date.now(), deps: dependencies.helperDeps || undefined });` (D-04 — both via the shared helper; wrap in try/finally to call candidateRouteCtx.cleanup() and knownGoodRouteCtx.cleanup() on every path including exceptions — backstop).
    5. Build the 6 gates per RESEARCH.md Pattern 3 lines 290-296:
       - safety = { pass: report.disposition === 'eligible', reason_code: report.disposition === 'eligible' ? 'safety_passed' : 'safety_uncertain' }
       - privacy = { pass: !window.records.some(r => (r.signal?.confidence_band === 'deny_filtered') || (r.signal?.guard_codes || []).some(c => ['privacy_guard','deny_filtered','secret_detected','content_detected'].includes(c))), reason_code: 'privacy_passed' } (defense-in-depth — structurally a no-op post-20-01, cannot fail given the bridge skips privacy-denied records at ingest)
       - Run `const candidateEvaluation = evaluateCorpus({ corpus: CALIBRATION_CORPUS, route: candidateRouteCtx.route, versions: { candidate: report.candidate_fingerprint, compiled_index: candidateRouteCtx.versionId || 'candidate', policy: COMPILED_INDEX_COMPATIBILITY.policy_version, corpus: 'router-calibration-v1' } });` and `const candidatePerf = measure({ fixtures: CALIBRATION_CORPUS, route: candidateRouteCtx.route, versions: { candidate: report.candidate_fingerprint, compiled_index: candidateRouteCtx.versionId || 'candidate', policy: COMPILED_INDEX_COMPATIBILITY.policy_version, corpus: 'router-calibration-v1' } });`
       - Run `const knownGoodPerf = measure({ fixtures: CALIBRATION_CORPUS, route: knownGoodRouteCtx.route, versions: { candidate: knownGood, compiled_index: knownGood, policy: COMPILED_INDEX_COMPATIBILITY.policy_version, corpus: 'router-calibration-v1' }, baseline: null });` (the known-good measurement; store its warm p50/p95/max as the comparison baseline).
       - quality = candidateEvaluation.quality (pass when all corpus fixtures match expected; the strict-improve comparison: candidate quality.pass === true AND knownGood quality.pass === false -> strict improve; candidate quality.pass === knownGood quality.pass -> parity)
       - context_budget = candidateEvaluation.context_budget (same strict-improve rule: candidate pass && knownGood fail -> strict improve; equal -> parity)
       - latency = assess({ evaluation: candidateEvaluation, performance: candidatePerf }).latency (hard gate: p95<25ms && max<100ms; NOT a tiebreaker)
       - compatibility = { pass: compatibleFn(built.registry.compatibility || built.candidateMetadata?.compatibility), reason_code: 'compatibility_passed' or 'compatibility_uncertain' } (D-06 — call the imported standalone compatible(), NOT COMPILED_INDEX_COMPATIBILITY.compatible)
    6. Call `const candidate = proposeCandidate({ source_evidence_fingerprint: window.source_evidence_fingerprint, policy_version: COMPILED_INDEX_COMPATIBILITY.policy_version, compiled_index_version: candidateRouteCtx.versionId || report.candidate_fingerprint, evaluation_inputs: { corpus: CALIBRATION_CORPUS, gates }, proposal: { registry: built.registry, mapping, reconciliation: report } });`
    7. Call `const evaluation = evaluateCandidate({ candidate, evidence_window: window, gates, known_good_version: knownGood });`
    8. Derive demonstrated_benefit per D-05:
       - If !evaluation.promotable -> demonstrated_benefit = null (applyCanaryDecision rolls back/rejects based on evaluation.promotable alone; demonstrated_benefit is not consulted — canary-controller.mjs:175-193).
       - Else compute strictImproveQuality = candidateEvaluation.quality.pass === true && knownGoodEvaluation.quality.pass === false (run a knownGoodEvaluation via evaluateCorpus on knownGoodRouteCtx.route first, OR derive from the knownGoodPerf — executor picks; the helper comparison must use the SAME corpus and the SAME versions shape). strictImproveContext = candidateEvaluation.context_budget.pass === true && knownGoodEvaluation.context_budget.pass === false. strictImprove = strictImproveQuality || strictImproveContext. latencyPass = assess({ evaluation: candidateEvaluation, performance: candidatePerf }).latency.pass.
       - If strictImprove && latencyPass -> demonstrated_benefit = { status:'demonstrated', reason_code: strictImproveQuality ? 'quality_improved' : (strictImproveContext ? 'context_bytes_reduced' : 'latency_reduced') } (D-05 predicate — strict improve on quality OR context_budget, latency as hard gate).
       - Else if !strictImprove && latencyPass && isSafetyFix(report) -> demonstrated_benefit = { status:'safety_correction', reason_code:'safety_fix' } (the reconciliation report indicates a safety/recovery fix — executor defines isSafetyFix predicate, e.g. report.verdicts contains a verdict with a safety reason_code, OR the candidate carries a safety_marker; if no clean signal, fall through to neutral).
       - Else -> demonstrated_benefit = { status:'neutral', reason_code:'no_strict_improvement' } (preserve on parity — never promote on equal-but-not-better, matches Phase 17 success criterion #4).
    9. Call `const decision = canaryDecision({ evaluation, demonstrated_benefit, activation: { ownedRoot: config.activation_root, candidate: built.registry, reconciliation: report, mapping, policy: config.activation_policy || {}, verification, reason: 'canary', test_mode: config.test_mode === true }, ownedRoot: config.activation_root, known_good_version: knownGood, published_version: active.tuple_version_id || active.version_id || null });`
    10. Map decision.status to activation:
        - 'promoted' -> keep the existing post-activation publishIndex call (lines 337-344); set activation = { ...activation, ...decision } (decision.active_version is the new active.version_id).
        - 'rolled_back' -> activation = { activation_status:'rolled_back', reason_code: decision.reason_code }
        - 'preserved' -> activation = { activation_status:'preserved', reason_code: decision.reason_code || 'canary_preserved' }
        - 'recovery_required' -> activation = { activation_status:'recovery_required', reason_code: decision.reason_code }
        - 'rejected' -> activation = { activation_status:'preserved', reason_code: decision.reason_code || 'canary_rejected' }
    11. finally block: call candidateRouteCtx.cleanup() and knownGoodRouteCtx.cleanup() (backstop — no tempdir leak across reconciles). Skip cleanup when knownGood === null (bootstrap path didn't create tempdirs).

    Preserve all existing behavior for the non-eligible, non-recoveryReady, and bootstrap branches. The existing activator call at line 336 remains for the bootstrap case (knownGood === null) and MUST run with reason:'watcher' (not 'canary') for the bootstrap first-activation.

    Preserve Phase 17 D-decisions (STATE.md lines 136-141): canary never mutates publication authority directly (only via applyCanaryDecision -> REGISTRY_PUBLICATION -> activate.mjs); every mapping target revalidated against exact supplied candidate (evaluateCandidate enforces candidate hash integrity); evidence rejects forbidden fields before persistence (Plan 01); project+aggregate evidence isolated (Plan 01); 7d/24h/30-sample retention (Plan 01); fails closed when compatible compiled state missing (loadCompiledIndex already non-dispatchable — canary does not change this).

    Do NOT touch src/context/prompt-route.mjs (hot path stays read-only).
  </action>
  <verify>
    <automated>node --test tests/router.watcher-canary-trigger.test.mjs tests/router.evolution-canary.test.mjs tests/router.compiled-evolution.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "applyCanaryDecision" src/registry/watcher.mjs` returns >= 1 (wiring present).
    - `grep -c "buildCandidateCalibrationRoute\|buildKnownGoodCalibrationRoute" src/registry/watcher.mjs` returns >= 2 (D-04 helper is used).
    - `grep -c "import { compatible" src/registry/watcher.mjs` returns 1 (D-06 import).
    - `grep -c "COMPILED_INDEX_COMPATIBILITY.compatible" src/registry/watcher.mjs` returns 0 (the wrong method call is absent — D-06).
    - `grep -c "measureRoutes" src/registry/watcher.mjs` returns >= 2 (candidate + known-good measurements — D-05 derivation).
    - `grep -c "demonstrated_benefit" src/registry/watcher.mjs` returns >= 3 (the object is constructed, passed to applyCanaryDecision, and the status field is set).
    - `node --test tests/router.watcher-canary-trigger.test.mjs` exits 0 with 7 passing tests (all of Task 1 GREEN).
    - `node --test tests/router.evolution-canary.test.mjs` exits 0 (existing canary library unbroken).
    - `node --test tests/router.compiled-evolution.test.mjs` exits 0 (the D-04 pattern source + the compile-index export change do not regress).
    - `grep -v '^#' src/context/prompt-route.mjs | grep -c "canary\|evidence\|telemetry-bridge\|buildCandidateCalibrationRoute"` returns 0 (hot path untouched).
  </acceptance_criteria>
  <done>
    - src/registry/watcher.mjs reconcile calls applyCanaryDecision when eligible+recoveryReady+sufficient+knownGood present, with demonstrated_benefit derived via D-05 (candidate + known-good measureRoutes comparison).
    - Insufficient evidence (window.sufficient===false) -> preserve with reason_code='insufficient_evidence_samples', activator NOT called, D-04 helper NOT called.
    - Bootstrap (recoverActiveVersion returns no_valid_history) -> existing activator path runs directly with reason:'watcher', no D-04 helper, no applyCanaryDecision.
    - Gate failure + published_version present -> applyCanaryDecision rollback branch fires (demonstrated_benefit not consulted).
    - Neutral (parity) -> decision.status='preserved' via demonstrated_benefit.status='neutral' (never promote on parity).
    - D-06: watcher imports { compatible } and calls compatible(...) — no COMPILED_INDEX_COMPATIBILITY.compatible(...) call.
    - node --test tests/router.watcher-canary-trigger.test.mjs exits 0 (7 tests pass).
    - node --test tests/router.evolution-canary.test.mjs + tests/router.compiled-evolution.test.mjs exit 0 (no regression).
    - Hot path (src/context/prompt-route.mjs) untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evidence window -> canary decision | Persisted evidence (Plan 01) drives promotion/rollback of the compiled index — a poisoned or insufficient window could promote a bad candidate or block a good one. |
| watcher reconcile -> publication authority | The watcher mutates active.json via applyCanaryDecision -> REGISTRY_PUBLICATION -> activate.mjs; bypassing this chain breaks CAS + fsync + TOCTOU invariants. |
| D-04 helper temp ownedRoots -> filesystem | The helper creates per-fixture temp ownedRoots under os.tmpdir(); a leak across reconciles exhausts disk under sustained watcher activity. cleanup() on every path (success + error) is the mitigation. |
| D-05 measureRoutes baseline -> promotion signal | The comparison baseline MUST be the known-good compiled index; using a stale or candidate-derived baseline would make a regression look like an improvement. |
| telemetry -> evidence -> canary | The full chain: telemetry.jsonl -> bridge -> evidence store -> window -> evaluateCandidate -> applyCanaryDecision. A break anywhere cascades. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-08 | Tampering | watcher canary trigger (insufficient evidence promotion) | high | mitigate | Window.sufficient gate checked BEFORE evaluateCandidate (Pitfall 5 line 410); if insufficient -> preserve with reason_code='insufficient_evidence_samples', activator NOT called (Test 2). 30-sample floor (Phase 17 D-08) prevents low-volume promotion. |
| T-20-09 | Tampering | watcher bypasses REGISTRY_PUBLICATION | high | mitigate | applyCanaryDecision delegates exclusively through REGISTRY_PUBLICATION (canary-controller.mjs:135-141); the watcher never calls writeFileSync(active.json) directly; activateCandidate/previewRollback/executeRollback are the only mutation paths (Phase 17 D-decision STATE.md:140). |
| T-20-10 | Denial of Service | bootstrap chicken-and-egg (no known-good) | high | mitigate | Bootstrap branch: recoverActiveVersion returns no_valid_history -> activate directly via existing path (first activation becomes known-good; no canary rollback possible when nothing to roll back to — RESEARCH.md line 376). Test 3 verifies. |
| T-20-11 | Tampering | weighted score compensates for hard-gate failure | high | mitigate | evaluateCandidate requires ALL 6 REQUIRED_GATES pass independently (canary-controller.mjs:115-120); no weighted compensation (Phase 17 D-decision STATE.md:140). Test 4 verifies rollback on single-gate failure. |
| T-20-12 | Tampering | stale evidence dominates promotion | medium | mitigate | 7d MAX_RETENTION_MS + 24h HALF_LIFE_MS decay enforced on window read (Plan 01 Task 2 + evidence.mjs:15-16); evidenceWindowFingerprint binds window to records. |
| T-20-13 | Tampering | canary trigger runs on hot path | high | mitigate | The reconcile function runs in the controller child process (watcher.mjs createRegistryReconciler), NOT in the prompt-routing hot path. src/context/prompt-route.mjs is unchanged (grep assertion in acceptance criteria). CLAUDE.md <100ms hot-path constraint preserved. |
| T-20-14 | Elevation of Privilege | test_mode leaks into production trigger | medium | mitigate | test_mode is threaded through as config.test_mode === true (existing pattern line 336); production config.test_mode is absent/false; applyCanaryDecision/activateCandidate respect test_mode (activate.mjs:79-87). Phase 20 adds SEPARATE production wiring, does not reuse test_mode as a trigger (RESEARCH.md Anti-Pattern line 336). |
| T-20-24 | Tampering | D-05 baseline substitution (candidate used as its own baseline) | high | mitigate | The known-good route fn is built via buildKnownGoodCalibrationRoute against the activation_root compiled index recovered by recoverActiveVersion (D-04 helper, Behavior 3). The candidate route fn is built via buildCandidateCalibrationRoute against the freshly published candidate. measureRoutes is called twice with distinct route fns; the known-good measurement is the baseline. Test 1 asserts demonstrated_benefit.reason_code is in the strict-improve set when candidate beats known-good; Test 5 asserts status='neutral' on parity. |
| T-20-25 | Denial of Service | D-04 helper tempdir leak | medium | mitigate | The watcher wraps the D-04 helper calls in try/finally; cleanup() is invoked on every path including exceptions (backstop must_have). Test 6 asserts cleanup() removes all tempdirs (existsSync false on every tempdir post-cleanup). |
| T-20-26 | Tampering | D-06 export change breaks loadCompiledIndex/verifyVersion | high | mitigate | The export change is purely additive (add `export ` before `function compatible`); the function body and all internal callers (verifyVersion at compile-index.mjs:102) are unchanged. Acceptance criterion: `grep -v '^#' src/prompt/compile-index.mjs | wc -l` unchanged from pre-edit. node --test tests/router.compiled-evolution.test.mjs exits 0 (regression guard). |
| T-20-SC | Tampering | npm/pip/cargo installs | high | mitigate | N/A — zero-dependency phase (no new packages). |
</threat_model>

<verification>
- node --test tests/router.watcher-canary-trigger.test.mjs exits 0 (7 tests).
- node --test tests/router.evolution-canary.test.mjs exits 0 (existing library unbroken).
- node --test tests/router.compiled-evolution.test.mjs exits 0 (D-04 pattern source + D-06 export change unbroken).
- node --test tests/*.test.mjs | tail -5 shows no new failures introduced (regression baseline).
- grep -c "applyCanaryDecision" src/registry/watcher.mjs returns >= 1.
- grep -c "buildCandidateCalibrationRoute\|buildKnownGoodCalibrationRoute" src/registry/watcher.mjs returns >= 2.
- grep -c "import { compatible" src/registry/watcher.mjs returns 1 (D-06).
- grep -c "COMPILED_INDEX_COMPATIBILITY.compatible" src/registry/watcher.mjs returns 0 (D-06 — wrong method absent).
- grep -c "export function compatible" src/prompt/compile-index.mjs returns 1 (D-06 export).
- grep -v '^#' src/context/prompt-route.mjs | grep -c "canary\|evidence\|telemetry-bridge\|buildCandidateCalibrationRoute" returns 0 (hot path untouched).
</verification>

<success_criteria>
- EVO-05 production trigger (watcher surface) wired: eligible+recoveryReady+sufficient -> canary decision routing with D-04 helper + D-05 derivation + D-06 compatible() import.
- D-04: reusable buildCandidateCalibrationRoute + buildKnownGoodCalibrationRoute helper in src/evolution/candidate-calibration-route.mjs, shared with 20-03 Task 2 (CLI promote).
- D-05: demonstrated_benefit derived via measureRoutes on candidate + known-good; strict-improve predicate on quality OR context_budget; latency as hard gate; never promote on parity; { status, reason_code } object shape.
- D-06: compatible() exported from compile-index.mjs (one-token additive change); watcher imports it by name; the wrong COMPILED_INDEX_COMPATIBILITY.compatible call is absent.
- Fail-closed on insufficient evidence (preserve, no promotion, no uncompiled slow path).
- Bootstrap path for first-ever activation (no canary rollback possible).
- Phase 17 D-decision invariants preserved (REGISTRY_PUBLICATION-only mutation, 6 independent gates, project+aggregate isolation).
- Hot path unchanged (prompt-route.mjs imports none of this wiring).
- D-04 helper tempdirs cleaned up on every reconcile (no leak).
</success_criteria>

<output>
Create `.planning/phases/20-close-gap-evo-05-add-production-trigger-for-canary-controlle/20-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- Modified `src/registry/watcher.mjs`:
  - New imports: `proposeCandidate, evaluateCandidate, applyCanaryDecision` from `../evolution/canary-controller.mjs`; `createPersistentEvidenceStore` from `../evolution/evidence.mjs`; `assessCalibration, evaluateCalibrationCorpus, measureRoutes, CALIBRATION_CORPUS` from `../evolution/perf-measure.mjs`; `compatible, COMPILED_INDEX_COMPATIBILITY` from `../prompt/compile-index.mjs` (D-06 — `compatible` imported by name, NOT as a method on `COMPILED_INDEX_COMPATIBILITY`); `buildCandidateCalibrationRoute, buildKnownGoodCalibrationRoute` from `../evolution/candidate-calibration-route.mjs` (D-04).
  - Extended `createRegistryReconciler` dependencies seam: `canaryDecision`, `buildCandidateRoute`, `buildKnownGoodRoute`, `measure`, `assess`, `evaluateCorpus`, `createEvidenceStore`, `compatibleFn` (test spies — Task 1).
  - Modified reconcile eligible+recoveryReady branch (lines 328-348): canary routing (proposeCandidate -> evaluateCandidate -> D-05 demonstrated_benefit derivation -> applyCanaryDecision) with try/finally cleanup of D-04 helper temp ownedRoots.
- Modified `src/prompt/compile-index.mjs` (D-06 — one-token additive change):
  - `function compatible(value)` -> `export function compatible(value)` (the ONLY line modified; the function body and all internal callers unchanged).
- New file `src/evolution/candidate-calibration-route.mjs` (D-04 shared helper):
  - `export function buildCandidateCalibrationRoute({ registry, mapping, policyFingerprint, now, corpus, deps })` -> `{ route, captures, cleanup, versionId }`.
  - `export function buildKnownGoodCalibrationRoute({ ownedRoot, now, corpus, deps })` -> `{ route, captures, cleanup }`.
  - Private `calibrationCapsule(fixture, now)` (internal helper, NOT exported; replicates tests/router.compiled-evolution.test.mjs:21-32).
  - Imports: `routeContextPrompt` from `../context/prompt-route.mjs`, `saveCapsule` from `../context/capsule.mjs`, `publishCompiledIndex` from `../prompt/publish-index.mjs`, `CALIBRATION_CORPUS` from `./perf-measure.mjs`, stdlib (fs, os, path, crypto).
- New test file `tests/router.watcher-canary-trigger.test.mjs`:
  - 7 tests: promote (D-05 derivation), preserve-insufficient, bootstrap, rollback-gate-failure, neutral-parity (D-05), D-04 helper unit, D-06 compatible() export.
- No new exported symbols from watcher.mjs (createRegistryReconciler/createTestRegistryReconciler signatures unchanged; the `dependencies` parameter accepts additional optional injection keys but the signature is backward-compatible).
- New exported symbols: `buildCandidateCalibrationRoute`, `buildKnownGoodCalibrationRoute` (from src/evolution/candidate-calibration-route.mjs), `compatible` (from src/prompt/compile-index.mjs — newly exported, previously private).
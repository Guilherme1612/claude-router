---
phase: 29-mode-map-curation-and-signal-patterns-expansion
plan: 02
type: execute
wave: 2
depends_on: [29-01]
files_modified:
  - /Users/guilherme/.claude/hooks/router.mjs
  - src/coverage/audit.mjs
  - tests/router.mode-map-v3.test.mjs
  - tests/router.route-targets.test.mjs
  - tests/router.coverage-audit.test.mjs
autonomous: true
requirements: [MAP-03, SIG-01, SIG-02, SIG-03]

must_haves:
  truths:
    - "One shared normalizer gives validation, corpus enrichment, proposal sanitation, and collision lint identical contains semantics."
    - "v2 maps remain readable while v3 accepts strings and contains objects without changing hot-path matching."
    - "Runtime reads fail open, but target/build validation rejects malformed, oversized, or undeclared-collision maps deterministically."
    - "Tests can inject manifest and mode-map objects through the real inspection path without creating another routing engine."
  artifacts:
    - path: "/Users/guilherme/.claude/hooks/router.mjs"
      provides: "Shared contains-pattern normalizer, validation/lint, object injection, and single scoring path"
      exports: ["normalizeSignalPattern", "validateRouteTargets", "inspectDecision"]
    - path: "src/coverage/audit.mjs"
      provides: "Build-time parity using canonical router pattern diagnostics"
    - path: "tests/router.mode-map-v3.test.mjs"
      provides: "Green schema and collision contracts"
  key_links:
    - from: "/Users/guilherme/.claude/hooks/router.mjs"
      to: "buildCorpus"
      via: "normalized values only"
      pattern: "normalizeSignalPattern"
    - from: "inspectDecision"
      to: "main routing pipeline"
      via: "manifest and modeMap options override file loading"
      pattern: "options.*manifest|modeMap"
    - from: "src/coverage/audit.mjs"
      to: "/Users/guilherme/.claude/hooks/router.mjs"
      via: "canonical exported pattern validator/diagnostics"
      pattern: "validate.*Pattern|normalizeSignalPattern"
---

<objective>
Implement the single backward-compatible v3 pattern path and the smallest injectable inspection seam.

Purpose: Make later curation portable and safe without a second parser, scorer, dependency, or hot-path subsystem.
Output: Shared normalization/collision behavior, build-time parity, and green schema/target tests.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-mode-map-curation-and-signal-patterns-expansion/29-RESEARCH.md
@.planning/phases/29-mode-map-curation-and-signal-patterns-expansion/29-PATTERNS.md
@.planning/phases/29-mode-map-curation-and-signal-patterns-expansion/29-01-SUMMARY.md
@/Users/guilherme/.claude/hooks/router.mjs
@src/coverage/audit.mjs
@tests/router.mode-map-v3.test.mjs
@tests/router.route-targets.test.mjs
@tests/router.coverage-audit.test.mjs

<interfaces>
`normalizeSignalPattern(raw, schemaVersion)` returns canonical `{kind,value,collision_group?}` or null.
Strings map to `contains`; objects require schema version 3 and `kind: "contains"`. Objects add metadata without changing matching semantics.
Pattern count is 1..6 per entry. Collision key is `${kind}\0${value}` and all duplicate occurrences require one identical non-empty `collision_group`.
`inspectDecision(prompt, options)` accepts optional `manifest` and `modeMap` objects; when absent, existing paths/loaders remain unchanged.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize and validate patterns once</name>
  <files>/Users/guilherme/.claude/hooks/router.mjs, tests/router.mode-map-v3.test.mjs, tests/router.route-targets.test.mjs</files>
  <behavior>
    - All Wave 0 schema, cap, and collision cases pass.
    - Existing v2 route-target tests remain unchanged in meaning.
    - String and object `contains` patterns operate identically in the existing BM25 decision pipeline.
    - Malformed runtime maps fail open; explicit validation returns stable diagnostics.
  </behavior>
  <action>Add the smallest exported normalizer beside `loadModeMap`, then route every signal-pattern consumer through it: target validation, corpus text, warn documents, and proposal sanitation. Enforce the one-to-six cap and one global collision scan in `validateRouteTargets`; allow the current duplicate `"redesign the ui"` only when both occurrences use the same explicit collision group. Strings remain the compact writer form for `contains`, while objects carry only the same phrase plus metadata. Do not add direct matching, prefix/exact/negative/regex semantics, a schema library, a new module, or a second score calculation. Preserve fail-open prompt behavior and missing-MCP guards.</action>
  <verify>
    <automated>rtk node --test tests/router.mode-map-v3.test.mjs tests/router.route-targets.test.mjs tests/router.bm25.test.mjs</automated>
  </verify>
  <done>Mixed v2/v3 patterns share one canonical path, collisions/caps fail validation, and existing target/BM25 behavior stays green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Inject fixture objects through the existing inspector</name>
  <files>/Users/guilherme/.claude/hooks/router.mjs, tests/router.mode-map-v3.test.mjs</files>
  <behavior>
    - Supplied manifest and mode-map objects take precedence over file paths.
    - Omitted objects retain current live-loader behavior.
    - Inspection remains read-only and uses the same main routing pipeline.
    - Fixture calls cannot mutate supplied objects or write routing cache/telemetry.
  </behavior>
  <action>Extend `inspectDecision` options with only `manifest` and `modeMap` object overrides and thread them into the existing `main`/corpus path. Keep `mutateCache`, telemetry, injection, and evolution disabled as today. Avoid a fixture class, alternate entry point, or temp-file protocol because two optional values are the smaller seam. Prove fallback compatibility and immutability in the Wave 0 test.</action>
  <verify>
    <automated>rtk node --test tests/router.mode-map-v3.test.mjs tests/router.calibrate-importable.test.mjs</automated>
  </verify>
  <done>Tests can route entirely from in-memory fixtures through production inspection, while normal installed-runtime behavior is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Keep build audit diagnostics in parity</name>
  <files>src/coverage/audit.mjs, tests/router.coverage-audit.test.mjs</files>
  <behavior>
    - Coverage audit reports the same pattern-shape, cap, and collision failures as runtime target validation.
    - Forward/reverse capability classification remains deterministic.
    - Missing-MCP agents remain warning-only and never count as mapped dispatch targets.
  </behavior>
  <action>Reuse the canonical exported pattern validation result at the coverage boundary, or pass its deterministic diagnostics into `auditCoverage` from the builder seam if that avoids a runtime-to-repository import. Do not copy the contains parser or collision algorithm into `audit.mjs`. Extend existing parity tests for mixed v3 patterns, cap errors, collisions, and blocked agents while retaining sorted JSON-ready diagnostics.</action>
  <verify>
    <automated>rtk node --test tests/router.coverage-audit.test.mjs tests/router.route-targets.test.mjs</automated>
  </verify>
  <done>The build audit and live validator agree without owning two pattern implementations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mode-map JSON → prompt router | User-reviewed data influences always-on BM25 candidate text. |
| fixture objects → inspector | Tests supply untrusted object shapes to production routing. |
| router validation → build audit | Publication safety depends on diagnostic parity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-01 | Tampering | normalizer | high | mitigate | Closed kind vocabulary, normalized non-empty values, schema-version check, deterministic invalid diagnostics. |
| T-29-02 | Denial of Service | corpus enrichment | high | mitigate | Contains-only normalized phrases, at most six per entry, reuse bounded existing BM25 pipeline. |
| T-29-03 | Tampering | duplicate signals | high | mitigate | Canonical global collision scan with explicit same-group exception only. |
| T-29-04 | Information Disclosure | injected fixtures | high | mitigate | Explicit objects bypass live reads and inspection remains mutation-free. |
| T-29-06 | Elevation of Privilege | target validation | critical | mitigate | Preserve blocked-agent exclusion and warning-only semantics in both validators. |
</threat_model>

<verification>
Run `rtk node --test tests/router.mode-map-v3.test.mjs tests/router.route-targets.test.mjs tests/router.coverage-audit.test.mjs tests/router.bm25.test.mjs tests/router.calibrate-importable.test.mjs`.
</verification>

<success_criteria>
- SIG-01..03 schema and lint contracts pass from one normalizer.
- MAP-03 fixture injection uses the existing inspector and score path.
- No dependency, regex support, second parser, or alternate router is added.
- Existing v2, BM25, target, coverage, and missing-MCP tests remain green.
</success_criteria>

<output>
Create `.planning/phases/29-mode-map-curation-and-signal-patterns-expansion/29-02-SUMMARY.md` when done.
</output>

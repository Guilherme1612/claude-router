---
phase: 17-compiled-prompt-routing-and-safe-evolution
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - src/evolution/evidence.mjs
  - src/evolution/canary-controller.mjs
  - tests/router.evolution-canary.test.mjs
autonomous: true
requirements: [EVO-05]
nyquist_compliant: true
must_haves:
  truths:
    - "D-05/D-06: evolution accepts only bounded content-free signals and rejects forbidden or reversible content before storage."
    - "D-07/D-08: evidence is project-scoped by default, explicitly eligible aggregates are isolated, and deterministic retention/decay plus minimum samples prevent stale or low-volume promotion."
    - "D-09/D-10: every change is an immutable reproducible candidate and promotion requires all independent hard gates plus sufficient canary evidence."
  artifacts:
    - path: src/evolution/evidence.mjs
      provides: "Privacy guard, scoped evidence journal, retention/decay, and minimum-sample window"
    - path: src/evolution/canary-controller.mjs
      provides: "Immutable candidate proposal and deterministic promotion evaluation"
    - path: tests/router.evolution-canary.test.mjs
      provides: "Privacy, scoping, evidence-window, immutability, and promotion-gate matrix"
  key_links:
    - from: src/evolution/canary-controller.mjs
      to: src/evolution/evidence.mjs
      via: "validated bounded evidence window"
    - from: src/evolution/canary-controller.mjs
      to: src/prompt/compile-index.mjs
      via: "candidate/index/policy compatibility metadata"
---

<objective>
Build the privacy-safe evidence and canary evaluation pipeline required by EVO-05 and D-05 through D-10.

Purpose: Allow routing signals and weights to improve only through bounded, project-safe, reproducible candidates whose independent gates all pass.
Output: Evidence guard/store, immutable canary controller, and focused tests.
</objective>

<execution_context>
@/Users/guilherme/.codex/gsd-core/workflows/execute-plan.md
@/Users/guilherme/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-compiled-prompt-routing-and-safe-evolution/17-CONTEXT.md
@.planning/phases/17-compiled-prompt-routing-and-safe-evolution/17-RESEARCH.md
@src/prompt/compile-index.mjs
@router.calibrate.mjs
@tests/router.telemetry.test.mjs
@tests/router.perf-evolved.test.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce the content-free telemetry envelope before persistence</name>
  <files>src/evolution/evidence.mjs, tests/router.evolution-canary.test.mjs</files>
  <behavior>
    - Allowed signals are limited to canonical route identity, confidence band, guard/reason codes, fixture class, bounded latency, candidate/policy versions, and success/regression verdicts.
    - Raw prompts, context bodies, conversation history, secrets, capability payloads, reversible text, unknown fields, and unbounded values are rejected before any write.
    - Prompt signatures are non-reversible and absent whenever a deny/privacy guard fires.</behavior>
  <action>Write privacy tests first, including write-spy assertions, then implement the strict allowlisted envelope in `src/evolution/evidence.mjs`. Validate type, enum, and size bounds before serialization. Cite D-05/D-06 in stable reason-code documentation and ensure denial occurs before hashing or persistence of forbidden content. Use only Node standard-library primitives.</action>
  <verify>
    <automated>node --test tests/router.evolution-canary.test.mjs</automated>
  </verify>
  <done>Only the D-05 bounded signal vocabulary can reach storage, and every D-06 forbidden-content fixture is rejected with zero writes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add scoped, decaying evidence windows with minimum samples</name>
  <files>src/evolution/evidence.mjs, tests/router.evolution-canary.test.mjs</files>
  <behavior>
    - Project evidence cannot affect another project; global baseline evidence requires explicit aggregate eligibility and a separate scope.
    - Retention and exponential decay are deterministic under a supplied clock, with a 24-hour half-life and seven-day maximum retention.
    - Fewer than 30 eligible observations cannot authorize normal promotion.</behavior>
  <action>Extend the tests before implementation. Store project-scoped evidence under an explicit project identity and isolate the separately eligible aggregate baseline per D-07. Implement deterministic pruning and exponential weighting per D-08 using an injected clock; use the research-recommended seven-day window, 24-hour half-life, and 30-observation minimum. Return structured insufficiency reasons and never let stale or low-volume evidence independently authorize promotion.</action>
  <verify>
    <automated>node --test tests/router.evolution-canary.test.mjs</automated>
  </verify>
  <done>Cross-project contamination is impossible, aggregate eligibility is explicit, and deterministic decay/retention/sample gates block stale or insufficient promotion evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create immutable candidates and deterministic promotion evaluation</name>
  <files>src/evolution/canary-controller.mjs, tests/router.evolution-canary.test.mjs</files>
  <behavior>
    - Each candidate binds source-evidence fingerprint, policy version, compiled-index version, and reproducible evaluation inputs in immutable content-addressed state.
    - Promotion requires sufficient evidence plus every safety, privacy, quality, context-budget, compatibility, and latency gate.
    - Uncertainty preserves the current known-good version and no weighted score can compensate for a hard-gate failure.</behavior>
  <action>Write candidate immutability and independent-gate tests first, then implement candidate proposal and evaluation in `src/evolution/canary-controller.mjs`. Per D-09, reject mutation and bind every reproducibility input. Per D-10, represent gates independently and require all to pass; consume only the validated evidence window from Tasks 1–2. Produce a verdict for the publication layer without changing the active pointer in this plan.</action>
  <verify>
    <automated>node --test tests/router.evolution-canary.test.mjs</automated>
  </verify>
  <done>Candidates are immutable and reproducible, and only a sufficiently evidenced candidate passing every deterministic hard gate can receive a promotable verdict.</done>
</task>

</tasks>

<verification>
Run `node --test tests/router.evolution-canary.test.mjs`, then `node --test tests/*.test.mjs` at the wave boundary.
</verification>

<success_criteria>
- EVO-05 privacy-safe candidate evaluation exists without storing prompt content.
- D-05 through D-10 are enforced by focused automated tests.
- Project isolation, decay, minimum samples, immutability, and independent gates are deterministic.</success_criteria>


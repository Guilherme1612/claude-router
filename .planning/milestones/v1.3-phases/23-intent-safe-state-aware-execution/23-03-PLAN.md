---
phase: 23-intent-safe-state-aware-execution
plan: 03
type: execute
wave: 3
depends_on: ['23-01', '23-02']
files_modified:
  - src/orchestrator/approval.mjs
  - src/orchestrator/next-prompt.mjs
  - tests/router.approval.test.mjs
  - tests/router.dispatch-integration.test.mjs
autonomous: true
requirements: [EXEC-05, EXEC-06, EXEC-07, EXEC-08, EXEC-09, EXEC-10]
must_haves:
  truths:
    - "needsApproval(contract) returns true when contract.fields.side_effects value matches destructive/unbounded/external/privileged, OR reversibility value is irreversible, OR risk value is high/critical/unacceptable (with envelope state='known') — EXEC-07"
    - "needsApproval returns false for safe contracts (reversible + low risk + no destructive side effects) — the tracer's safe path"
    - "bindApproval({ capability, args, targets, effects, proposalVersion }) returns a token binding contentFingerprint(capability) + stableStringify(args) + sorted targets + stableStringify(effects) + String(proposalVersion) via createHash('sha256') — EXEC-08"
    - "verifyApproval with missing bound or presented → status='blocked', reason_code='approval_missing' — EXEC-08"
    - "verifyApproval where expected !== bound.token → status='blocked', reason_code='approval_stale' — EXEC-08 (stale args/targets/effects/version)"
    - "verifyApproval where presented.token !== bound.token → status='blocked', reason_code='approval_mismatch' — EXEC-08"
    - "verifyApproval where all legs match → status='approved', reason_code='approval_bound' — EXEC-08"
    - "Execute intent alone never satisfies the approval gate: an execute-classified prompt for a destructive capability with no presented token → dispatch_eligible=false, reason_code='approval_missing' (EXEC-07)"
    - "End-to-end dispatch requires fresh state + exactly one transition + healthy deps + valid args + permission + gates + (approval when destructive); any missing → blocked with the corresponding reason_code (EXEC-05)"
    - "Ties, stale state, terminal workflows, unresolved gaps, missing deps, material side effects, human-required checkpoints all produce status='blocked' or status='clarify' — never a silent dispatch (EXEC-06)"
    - "Hook records (type='hook') are never selected as the dispatch target — filtered in actions.mjs before contract matching and again by select.mjs#resolveDependencies (EXEC-09)"
    - "Discovery records are evidence, not authority — only registry.eligibility[id].eligible===true capabilities dispatch (EXEC-09)"
    - "After a completed safe dispatch, synthesizeNextPrompt re-runs nextValidTransitions on fresh post-work state and emits the framework-neutral next-capability prompt (EXEC-10)"
    - statement: "Approval tokens are session-scoped and not persisted to any data file (A3) — no new file in ~/.claude/router/ is created by Phase 23"
      verification: backstop
    - statement: "Approval token hash precision (SHA-256 hex) is stable across Node 22 minor versions — tested by re-deriving the token twice in the same process and asserting equality"
      verification: backstop
    - statement: "Proposal-version boundary: bumping proposalVersion by 1 invalidates a previously bound token (approval_stale) — tested at the boundary"
      verification: backstop
  prohibitions:
    - "Approval MUST NOT be satisfied by execute intent alone (EXEC-07) — a separate presented token is required"
    - "The approval gate MUST NOT elevate permissions or bypass sandbox/tool restrictions (EXEC-09) — approval only authorizes a destructive capability the runtime already permits"
    - "The dispatch path MUST NOT invoke lifecycle hooks as task capabilities (EXEC-09)"
    - "The dispatch path MUST NOT treat discovery records as authorization (EXEC-09)"
    - "Approval tokens MUST NOT be persisted to disk (A3) — session-scoped only"
    - "The approval gate MUST NOT re-check eligibility (Anti-Pattern) — it consumes evaluateEligibility's derived result"
  artifacts:
    - tests/router.approval.test.mjs
    - tests/router.dispatch-integration.test.mjs (expanded)
  key_links:
    - "needsApproval(contract) → gates resolveAction after capability selection"
    - "bindApproval/verifyApproval → contentFingerprint(capability) + stableStringify(args/targets/effects) + proposalVersion"
    - "synthesizeNextPrompt → fresh post-work nextValidTransitions → framework-neutral next prompt"
---

<objective>
Wire the approval gate for destructive/privileged/irreversible capabilities (EXEC-07, 08, 09) and complete the end-to-end dispatch integration tests covering the full EXEC-05/06/09/10 matrix — fresh state, one transition, healthy deps, valid args, permission, gates, approval, and the framework-neutral next-prompt after work completes.

Purpose: With the tracer (Plan 01) and the action-mapper expansion (Plan 02) in place, this plan closes the dispatch-authority layer by enforcing that destructive actions require a separately bound approval token distinct from execute intent, and that every blocked path produces the exact stable reason code the spec names.
Output: `tests/router.approval.test.mjs` (approval binding + stale/mismatch fail-closed), the expanded `approval.mjs` destructive path, and the expanded `tests/router.dispatch-integration.test.mjs` covering the full dispatch matrix.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-intent-safe-state-aware-execution/23-RESEARCH.md
@.planning/phases/23-intent-safe-state-aware-execution/23-PATTERNS.md
@.planning/phases/23-intent-safe-state-aware-execution/23-01-SUMMARY.md
@.planning/phases/23-intent-safe-state-aware-execution/23-02-SUMMARY.md
@src/orchestrator/approval.mjs
@src/orchestrator/actions.mjs
@src/orchestrator/next-prompt.mjs
@src/orchestrator/transitions.mjs
@src/orchestrator/select.mjs
@src/registry/eligibility.mjs
@src/registry/contract.mjs
@src/registry/identity.mjs
@src/registry/schema.mjs
@src/context/sources.mjs
@tests/router.contract-eligibility.test.mjs
@tests/helpers/inventory-fixture.mjs
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Approval gate — needsApproval, bindApproval, verifyApproval (EXEC-07/08/09)</name>
  <files>src/orchestrator/approval.mjs, tests/router.approval.test.mjs</files>
  <read_first>
    - src/orchestrator/approval.mjs (minimal v1 from Plan 01 — wire the destructive path)
    - src/registry/identity.mjs (contentFingerprint at 37-51, stableCapabilityId, canonicalizeCapability)
    - src/registry/schema.mjs (stableStringify, validateCapability)
    - src/registry/contract.mjs (CONTRACT_FIELDS, ENUM_FIELDS for reversibility/risk at 56-59)
    - src/registry/eligibility.mjs (field/fieldState envelope reader at 32-42; side_effects/reversibility/risk gate at 165-175 — reuse token vocabulary, do NOT re-check eligibility)
    - src/orchestrator/select.mjs (blocked() helper at 9-11)
  </read_first>
  <behavior>
    - needsApproval(safe contract: side_effects=none, reversibility=reversible, risk=low) → false
    - needsApproval(destructive contract: side_effects value contains 'destructive') → true
    - needsApproval(irreversible contract: reversibility value === 'irreversible') → true
    - needsApproval(high-risk contract: risk value === 'high' or 'critical' or 'unacceptable') → true
    - needsApproval with envelope state='unknown' → false (uncertain → not auto-approved, but not auto-blocked either; the eligibility gate handles unknown — approval only blocks known-destructive)
    - bindApproval({ capability, args, targets, effects, proposalVersion }) → { schema_version:1, policy_version:APPROVAL_POLICY_VERSION, token:<hex> }
    - verifyApproval with no bound → blocked 'approval_missing'
    - verifyApproval with no presented → blocked 'approval_missing'
    - verifyApproval where bound.token !== expected (re-derived from current args/targets/effects/version) → blocked 'approval_stale'
    - verifyApproval where presented.token !== bound.token → blocked 'approval_mismatch'
    - verifyApproval where all match → status='approved', reason_code='approval_bound'
    - Bumping proposalVersion by 1 invalidates the prior bound token (approval_stale) — boundary test
    - Changing args (different topic for create-phase) invalidates the prior bound token (approval_stale)
    - Re-deriving bindApproval twice in the same process yields the same token (hash determinism)
  </behavior>
  <action>
    Confirm `src/orchestrator/approval.mjs` implements `needsApproval`, `bindApproval`, `verifyApproval` per RESEARCH Pattern 3 and the Plan 01 tracer module. `needsApproval` reuses the `field()` + `state==='known'` envelope reader pattern from `eligibility.mjs:32-42` and the same token vocabulary (`destructive`,`unbounded`,`external`,`privileged`,`irreversible`,`high`,`critical`,`unacceptable`) from `eligibility.mjs:165-175` — do NOT re-check eligibility, only read the contract envelope (Anti-Pattern). `bindApproval` chains `contentFingerprint(capability)` (from `identity.mjs:37-51`) + `stableStringify(args)` + `stableStringify([...(targets||[])].sort())` + `stableStringify(effects)` + `String(proposalVersion)` through `createHash('sha256')` (ASVS V6 — never hand-roll). `verifyApproval` returns the blocked/approved shapes per RESEARCH Pattern 3.

    Create `tests/router.approval.test.mjs` with the behaviors above as parameterized cases. Build contracts with the `safeRecord` + `contractEvidence` pattern from `tests/router.contract-eligibility.test.mjs:13-23`, overriding `side_effects`/`reversibility`/`risk` envelopes per case. Phase-tag `[phase23-red:approval]`. Assert exact `status` and `reason_code` for each path. Run RED→GREEN.

    Add the EXEC-09 invariant test: a `type:'hook'` record passed as `capability` to `bindApproval` — assert that downstream `resolveAction` never selects a hook (the hook-exclusion filter is in `actions.mjs`, but the approval test asserts the invariant end-to-end: a hook capability never reaches `bindApproval` because `actions.mjs` filters it first; test by asserting `resolveAction` returns `no_eligible_capability` for a hook-only registry).
  </action>
  <verify>
    <automated>rtk node --test tests/router.approval.test.mjs</automated>
  </verify>
  <done>
    - needsApproval true for destructive/irreversible/high-risk; false for safe + unknown-envelope
    - bindApproval token is deterministic (re-derive twice → equal)
    - verifyApproval: missing/stale/mismatch → blocked with exact reason_code; all-match → approved
    - proposalVersion bump and args change each invalidate the prior token
    - Hook capabilities never reach bindApproval (filtered upstream)
    - `rtk node --test tests/router.approval.test.mjs` exits 0
  </done>
  <reversibility rating="reversible">Pure-function module + tests; no persisted state (tokens are session-scoped per A3).</reversibility>
</task>

<task type="tdd" tdd="true">
  <name>Dispatch integration — full EXEC-05/06/09/10 matrix end-to-end</name>
  <files>tests/router.dispatch-integration.test.mjs, src/orchestrator/next-prompt.mjs</files>
  <read_first>
    - tests/router.dispatch-integration.test.mjs (the Plan 01 tracer happy path — expand to the full matrix)
    - src/orchestrator/actions.mjs (from Plan 02 — full verb set)
    - src/orchestrator/approval.mjs (from Task 1 — full approval gate)
    - src/orchestrator/transitions.mjs (nextValidTransitions freshness/terminal/gates; selectWorkflow; clarification)
    - src/orchestrator/select.mjs (selectCapabilities closure; hook exclusion)
    - src/context/sources.mjs (readStateSource, readRoadmapSource — fresh per dispatch)
    - src/orchestrator/next-prompt.mjs (synthesizeNextPrompt — expand for post-work re-run)
  </read_first>
  <behavior>
    - Safe dispatch full path: execute prompt + fresh state + one transition + healthy deps + valid args + permission + gates + safe capability (needsApproval=false) → status='selected', dispatch_eligible=true, next-prompt emitted (EXEC-05)
    - Destructive dispatch with approval: execute prompt + destructive capability + presented approval token matching bound → status='selected', dispatch_eligible=true (EXEC-05, 07, 08)
    - Destructive dispatch without approval: execute prompt + destructive capability + no presented token → blocked 'approval_missing' (EXEC-07)
    - Destructive dispatch with stale approval (bumped proposalVersion): → blocked 'approval_stale' (EXEC-08)
    - Destructive dispatch with mismatched approval (different args): → blocked 'approval_mismatch' (EXEC-08)
    - Stale state (freshness='stale'): → blocked 'authoritative_evidence_stale' (EXEC-06)
    - Terminal workflow (status='terminal'): → blocked 'terminal_workflow' (EXEC-06)
    - Missing dependency (dependencies_safe=false): → blocked 'dependency_unavailable' or 'dependency_conflict' (EXEC-06)
    - Tie (two eligible capabilities for one transition): → clarify 'material_capability_tie', one focused question (EXEC-06)
    - Hook-only registry: → blocked 'no_eligible_capability' (EXEC-09)
    - Post-work next-prompt: after a safe dispatch, synthesizeNextPrompt re-runs nextValidTransitions on fresh post-work state and emits the framework-neutral next-capability prompt (EXEC-10)
    - Newest-explicit-instruction override: a stale capsule hint + a fresh execute prompt → the fresh prompt wins (INT-04 integration)
  </behavior>
  <action>
    Expand `tests/router.dispatch-integration.test.mjs` with the behaviors above as parameterized cases. Reuse the `safeRecord` + `contractEvidence` fixture pattern. For destructive cases, override `side_effects`/`reversibility`/`risk` envelopes to destructive/irreversible/high; use `bindApproval` to mint a bound token and `verifyApproval` to assert the gate. For the post-work next-prompt (EXEC-10), call `synthesizeNextPrompt` after mutating the state fixture to reflect completed work, then assert the prompt is framework-neutral and references the next capability's `invocation` shape.

    Expand `src/orchestrator/next-prompt.mjs` `synthesizeNextPrompt` to re-run `nextValidTransitions` on the fresh post-work state (passed in by the caller) before building the prompt — the prompt reflects the *next* valid transition, not the just-completed one. Keep the `Buffer.byteLength <= MAX_CONTEXT_BYTES` overflow guard.

    Phase-tag `[phase23-red:dispatch]`. Run `rtk node --test tests/router.dispatch-integration.test.mjs` (RED→GREEN).
  </action>
  <verify>
    <automated>rtk node --test tests/router.dispatch-integration.test.mjs</automated>
  </verify>
  <done>
    - Safe + destructive-with-approval paths dispatch (status='selected')
    - Destructive-without-approval, stale, mismatch → blocked with exact reason codes
    - Stale state, terminal, missing-dep, tie, hook-only → blocked/clarify with exact reason codes
    - Post-work next-prompt re-runs nextValidTransitions on fresh state and is framework-neutral
    - `rtk node --test tests/router.dispatch-integration.test.mjs` exits 0
  </done>
  <reversibility rating="reversible">Test expansion + next-prompt post-work re-run; no schema/state change.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| execute intent → approval gate | Execute alone must NOT satisfy approval for destructive capabilities |
| approval token ↔ capability fingerprint | Mismatch (stale args/targets/effects/version) must fail closed |
| post-work state → next-prompt | Must re-read fresh state, never cache across the dispatch boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-03 | Elevation of privilege | approval.mjs (destructive dispatched without approval) | critical | mitigate | `needsApproval(contract)` gate unconditionally after capability selection; execute intent never satisfies it; test asserts blocked 'approval_missing' for destructive-without-token. |
| T-23-04 | Tampering | approval.mjs (stale approval reused for new args/targets) | high | mitigate | Token binds fingerprint+args+targets+effects+proposalVersion; mismatch → `approval_stale`/`approval_mismatch`; boundary test at proposalVersion bump. |
| T-23-05 | Elevation of privilege | dispatch path (hook invoked as task tool) | high | mitigate | `actions.mjs` filters `r.type !== 'hook'` before contract matching; `select.mjs#resolveDependencies` re-excludes; dispatch-integration test asserts hook-only registry blocks. |
| T-23-07 | Elevation of privilege | dispatch path (discovery treated as authority) | medium | mitigate | Only `registry.eligibility[id].eligible === true` capabilities dispatch; discovery records are evidence, never authority. |
| T-23-08 | Tampering | dispatch path (stale state across boundary) | high | mitigate | `readStateSource` per dispatch; `nextValidTransitions` freshness gate; post-work next-prompt re-reads fresh state (no cross-prompt cache). |
| T-23-09 | Elevation of privilege | approval.mjs (permission elevation via approval) | high | mitigate | Approval only authorizes a destructive capability the runtime already permits; it never grants permissions the runtime denies (eligibility permission gate is upstream). |
| T-23-V4 | Access Control | approval.mjs (approval is the access-control boundary for destructive) | high | mitigate | Distinct gate from execute intent; bound to exact fingerprint+args+targets+effects+version; stale/mismatch fails closed. |
| T-23-V6 | Cryptography | approval.mjs (SHA-256 fingerprint) | medium | mitigate | Reuses `contentFingerprint`/`createHash('sha256')` from `identity.mjs`; never hand-roll hashing. |
| T-23-V7 | Error Handling | dispatch path (fail-closed on unknown/ambiguous/stale/mismatch) | high | mitigate | Every blocked path returns `dispatch_eligible: false` + stable `reason_code`; fail-open applies only to prompt injection, never dispatch. |
| T-23-SC | Tampering | (no package installs) | n/a | accept | Stdlib-only; supply-chain gate n/a. |
</threat_model>

<verification>
- `rtk node --test tests/router.approval.test.mjs tests/router.dispatch-integration.test.mjs` exits 0
- Full Phase 23 suite: `rtk node --test tests/router.intent.test.mjs tests/router.intent-adversarial.test.mjs tests/router.actions.test.mjs tests/router.approval.test.mjs tests/router.dispatch-integration.test.mjs` exits 0
- Phase 22 regression: `rtk node --test tests/router.contract-eligibility.test.mjs tests/router.contracts.test.mjs tests/router.relationships.test.mjs` still exits 0
- No new data file in `~/.claude/router/` (approval tokens are session-scoped per A3)
</verification>

<success_criteria>
- Approval gate: destructive/privileged/irreversible require a separately bound token; execute intent alone never satisfies it
- Stale/mismatched approval fails closed with exact reason codes
- Full dispatch matrix: fresh state + one transition + healthy deps + valid args + permission + gates + approval → selected; any missing → blocked/clarify with exact reason codes
- Post-work next-prompt re-reads fresh state and is framework-neutral
- Hooks never selected as task capabilities; discovery never treated as authority
</success_criteria>

<output>
Create `.planning/phases/23-intent-safe-state-aware-execution/23-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

**Plan 03 symbols (new/expanded):**
- `src/orchestrator/approval.mjs`: (full) `needsApproval`, `bindApproval`, `verifyApproval` complete with destructive/privileged/irreversible gating + stale/mismatch fail-closed (exports already declared in Plan 01; behavior completed here)
- `src/orchestrator/next-prompt.mjs`: (expanded) `synthesizeNextPrompt` re-runs `nextValidTransitions` on fresh post-work state before building the framework-neutral prompt
- `tests/router.approval.test.mjs`: new — approval binding + stale/mismatch + boundary tests
- `tests/router.dispatch-integration.test.mjs`: expanded — full EXEC-05/06/09/10 matrix including destructive-with-approval, stale/mismatch, tie, terminal, missing-dep, hook-only, post-work next-prompt
- No new CLI flags, no new struct fields, no new data files.
---
phase: 32.1
slug: phase-32-review-closure-wire-resolver-into-production-hot-pa
status: verified
threats_open: 0
asvs_level: 1
created: 2026-08-01
---

# Phase 32.1 — Security

> Per-phase security contract: threat register, accepted risks, and audit trail.

---

## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| Runtime manifest to index | The active runtime selects the command inventory used by route presence checks. | Manifest route metadata and runtime command names |
| Production hook resolver | Prompt decisions cross the resolver and injection pipeline; only validated local candidates may enter rendered context. | Prompt text, route decision, injected context |
| Build and coverage gates | Mode-map and inventory diagnostics determine strict build outcomes. | Resolve lists, route-target diagnostics, build status |
| Performance subprocess | The live hook runs in an isolated temporary test environment for timing and render proof. | Test prompt, temporary cache state, timing JSON |

---

## Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation | Status |
|-----------|----------|-----------|----------|-------------|------------|--------|
| T-32.1-01 | Spoofing | `buildTargetIndexes` commands set | high | mitigate | `runtime_commands[RUNTIME]` supplies the active runtime inventory; Claude/Codex isolation tests pass. | closed |
| T-32.1-02 | Tampering | `resolveSlashRoute` presence filter | high | mitigate | Runtime-aware index isolation prevents foreign capabilities from appearing present; cross-runtime tests pass. | closed |
| T-32.1-03 | Tampering | `namesFromEntries` fallback path | low | accept | Flat-manifest fallback is retained only for legacy manifests without a usable runtime slice; fallback regression test passes. | closed |
| T-32.1-04 | Spoofing | `inspectDecision` slash emission | high | mitigate | Production emits only `resolveSlashRoute(...).suggested_slash`; quarantined candidates are never injected. | closed |
| T-32.1-05 | Tampering | `resolveSlashRoute` present-sort order | medium | mitigate | Stable weight-only sort preserves declared order; ordering tests pass. | closed |
| T-32.1-06 | DoS | Resolver fault injection on hot path | high | mitigate | Resolver and outer pipeline catches fail open to safe pass-through; live resolver fault probe passes without crash or fabricated slash. | closed |
| T-32.1-07 | Information Disclosure | Quarantined names in rendered context | low | accept | Quarantined names remain in state/telemetry for observability but are excluded from `final_injected_context`; integration tests pass. | closed |
| T-32.1-08 | Tampering | `build-manifest` strict gate | high | mitigate | Near-tie and unresolvable-stale violations fail deterministically under strict coverage; gate fixture passes. | closed |
| T-32.1-09 | Tampering | Coverage audit severity classification | medium | mitigate | Only blocking diagnostics feed strict failure; `quarantined_fallback` remains non-blocking for resolvable routes. | closed |
| T-32.1-10 | DoS | Gate false-positive on clean lists | low | mitigate | Wide-gap clean-list fixture exits successfully while real violations fail. | closed |
| T-32.1-11 | Spoofing | End-to-end performance fixture scope | high | mitigate | Non-empty rendered context and render marker prove the measured path reaches the live hot path. | closed |
| T-32.1-12 | DoS | Flaky performance timing | low | mitigate | Warmup, 30 measured iterations, warm p95/max budgets, and subprocess timeout are enforced. | closed |
| T-32.1-13 | Elevation of Privilege | Performance subprocess environment | low | accept | The subprocess uses Node stdlib/local code and an isolated temporary cache with no privilege-changing operation. | closed |
| T-32.1-SC | Tampering | Package installation surface | high | mitigate | No package installs or manifest changes; phase implementation uses Node stdlib/local imports only. | closed |

*Status: open · closed · open — below high threshold (non-blocking)*
*Severity: critical > high > medium > low — only open threats at or above workflow.security_block_on count toward threats_open*
*Disposition: mitigate (implementation required) · accept (documented risk) · transfer (third-party)*

---

## Accepted Risks Log

| Risk ID | Threat Ref | Rationale | Accepted By | Date |
|---------|------------|-----------|-------------|------|
| AR-32.1-03 | T-32.1-03 | Legacy flat manifests retain `manifest.commands` fallback for backward compatibility when no usable runtime slice exists. | User via security gate | 2026-08-01 |
| AR-32.1-07 | T-32.1-07 | Quarantined candidate names remain available for observability but are excluded from rendered injection. | User via security gate | 2026-08-01 |
| AR-32.1-13 | T-32.1-13 | Isolated stdlib-only performance subprocesses use temporary cache state without privilege-changing operations. | User via security gate | 2026-08-01 |

*Accepted risks do not resurface in future audit runs.*

---

## Security Audit Trail

| Audit Date | Threats Total | Closed | Open | Run By |
|------------|---------------|--------|------|--------|
| 2026-08-01 | 14 | 14 | 0 | gsd-security-auditor; user acceptance gate |

Focused security evidence: 74/74 tests passed, including the resolver-specific fail-open fault probe.

---

## Sign-Off

- [x] All threats have a disposition (mitigate / accept / transfer)
- [x] Accepted risks documented in Accepted Risks Log
- [x] `threats_open: 0` confirmed
- [x] `status: verified` set in frontmatter

**Approval:** verified 2026-08-01

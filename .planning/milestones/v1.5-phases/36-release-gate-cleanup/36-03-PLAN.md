---
phase: 36-release-gate-cleanup
plan: 03
objective: Reconcile v1.4 maintenance debt and run final baseline verification.
---

# Plan 36-03: Debt and Final Baseline

## Tasks

1. Validate the reverse-gap baseline count, classifications, uniqueness, and stale-entry report against the current manifest.
2. Rerun calibration and record the current `T_high` leave-one-out sensitivity; retain the published tuple unless fresh evidence justifies a reviewed change.
3. Attempt watcher-reconcile activation from the operator shell; record activation status and any environment boundary that prevents confirmation.
4. Run the full existing test baseline plus all v1.5 phase suites; reconcile planning metadata only from fresh command output.

## Verification

- `tests/router.coverage-audit.test.mjs`
- `tests/router.calibration-thresholds.test.mjs`
- `rtk node router.calibrate.mjs`
- full `tests/*.mjs` baseline and v1.5 focused suites

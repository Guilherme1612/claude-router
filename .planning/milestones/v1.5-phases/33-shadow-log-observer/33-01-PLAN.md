---
phase: 33-shadow-log-observer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/router.mjs.snapshot
  - tests/router.shadow-log.test.mjs
autonomous: true
requirements:
  - CALIB-01
  - CALIB-02
user_setup: []
estimate:
  tokens: 24000
  raw_tokens: 12000
  tasks: 2
  confidence: medium
must_haves:
  truths:
    - "A fresh suggestion can be settled exactly once as accepted, rejected, or no_signal using only hashed prompt/capability identifiers and bounded capability metadata."
    - "Telemetry rows marked cache_hit are never eligible for shadow outcomes; legacy rows without the field remain backward-compatible and are treated as fresh."
    - "The observer never persists prompt text, command arguments, tool output, transcript content, or other user-entered payloads."
  artifacts:
    - tests/router.mjs.snapshot (shadow observer and telemetry freshness marker)
    - tests/router.shadow-log.test.mjs
  key_links:
    - "telemetryEntryFromState -> cache_hit/cache_status -> latest fresh suggestion selection"
    - "hook event capability -> sha256 invocation signature -> one shadow-log outcome"

<objective>
Add the measure-only, privacy-safe shadow-log primitive at the same deployed hook seam as the existing router. The primitive consumes the existing telemetry JSONL, ignores cache-hit rows, correlates Skill/Agent/Task or direct slash-command events to the latest unsettled fresh suggestion, and settles that suggestion once. A Stop event settles an unmatched suggestion as no_signal. No calibration threshold or routing decision is changed.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-shadow-log-observer/33-CONTEXT.md
@tests/router.mjs.snapshot
@tests/router.health.outcome-schema.test.mjs
@tests/router.telemetry.test.mjs

The installed hook is sourced from tests/router.mjs.snapshot. Reuse its stdlib-only hashing, runtime-root selection, telemetry path, and fail-open append conventions. Claude hook payloads provide command_name on UserPromptExpansion, tool_name/tool_input on PostToolUse and PostToolUseFailure, and stop_hook_active on Stop.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mark fresh versus cached suggestions without changing routing</name>
  <files>tests/router.mjs.snapshot</files>
  <read_first>
    - tests/router.mjs.snapshot: state.cache initialization, cache hit branch, telemetryEntryFromState, routeToInspectShape, and finish
    - tests/router.telemetry.test.mjs: telemetry fixture and privacy assertions
  </read_first>
  <action>
    Extend telemetryEntryFromState with bounded additive metadata: cache_hit is true only when the decision cache status is hit, cache_status is the existing status token, and suggestion_id is a sha256 over the prompt signature, timestamp, runtime, and suggested route/capability identifiers. Keep the existing outcome/downstream fields intact for later consumers. Do not add raw prompt text, session text, command arguments, or tool output. Make the same fields visible in inspectDecision output only if an existing inspect projection already exposes the cache object; do not widen the hot-path payload unnecessarily.
  </action>
  <acceptance_criteria>
    - Fresh routed rows carry cache_hit:false (or equivalent explicit false) and a stable 64-hex suggestion_id.
    - Cache-hit rows carry cache_hit:true and retain the prior route behavior.
    - Existing telemetry and runtime-tag tests remain green.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/router.telemetry.test.mjs tests/router.runtime-tagging.test.mjs tests/router.mutation-safety.test.mjs</automated>
  </verify>
  <done>Every new telemetry row identifies freshness and a privacy-safe suggestion key, with no route-selection behavior change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the one-shot shadow observer and privacy contract tests</name>
  <files>tests/router.mjs.snapshot, tests/router.shadow-log.test.mjs</files>
  <read_first>
    - tests/router.mjs.snapshot: constants, isMain, main, logTelemetry, promptSignature, and runtime detection
    - src/health/outcome-schema.mjs: bounded-token and fingerprint patterns to reuse conceptually without coupling calibration to the nine-kind health schema
    - official Claude hook event shapes recorded in 33-CONTEXT.md research notes
  </read_first>
  <action>
    Add exported stdlib-only helpers in the snapshot: readShadowCandidates, observeShadowEvent, appendShadowOutcome, readShadowOutcomes, and buildShadowDivergenceReport. Use shadow-log.jsonl plus a small atomic shadow-state.json under the runtime router directory. Candidate selection reads only recent telemetry rows for the active runtime, requires a non-null prompt signature and route/capability, excludes cache_hit:true, and uses suggestion_id for dedupe. Normalize event capability names only in memory; persist suggested mode/skills/agents plus hashed invocation_signature, outcome, runtime, timestamp, source_event, and suggestion_id. Use accepted when the invoked capability matches a suggested capability, rejected when a tracked Skill/Agent/Task or slash expansion invokes a different capability, and no_signal when Stop closes an unsettled candidate. All writes are append-only/best-effort, mode 0600, bounded, and fail-open. Ignore Stop recursion when stop_hook_active is true. The report helper must return calibration_enabled:false and deterministic accepted/rejected/no_signal counts grouped by suggested mode and skill; it must not derive thresholds.

  Add focused tests for all three outcomes, cache exclusion, duplicate settlement, malformed input, hashed-only persistence, active-runtime isolation, stop recursion, and deterministic report counts. Tests use temporary HOME/runtime roots and never write the real user home.
  </action>
  <acceptance_criteria>
    - Three-state outcome is exactly accepted/rejected/no_signal and each candidate settles once.
    - A cache-hit-only telemetry file yields no shadow record.
    - Persisted JSON contains no prompt, command_args, tool_input, tool_response, transcript_path, or raw invoked capability; signature fields are 64-hex hashes.
    - Divergence report is measure-only and stable for the same input.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/router.shadow-log.test.mjs</automated>
    <automated>node --test tests/router.telemetry.test.mjs tests/router.health.outcome-schema.test.mjs</automated>
  </verify>
  <done>Pure observer behavior and privacy boundaries are covered independently of lifecycle bindings.</done>
</task>

</tasks>

<verification>
- `node --test tests/router.shadow-log.test.mjs` passes.
- Existing telemetry, runtime-tagging, mutation-safety, and health-schema suites pass.
- Snapshot remains the only production hook source; no calibration or threshold path is imported.
</verification>

<success_criteria>
- CALIB-01 and CALIB-02 are implemented in a measure-only observer with explicit privacy and cache-freshness evidence.
</success_criteria>
